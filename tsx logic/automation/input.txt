```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1Intro',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1Intro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const bgScale = interpolate(frame, [0, durationInFrames], [1, 1.08], { extrapolateRight: 'clamp' });

  const lineReveal = interpolate(frame, [20, 60], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const kunal = sp(10);
  const headline = sp(25);
  const sub = sp(45);

  const floatY = Math.sin(frame * 0.04) * 6;
  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient Background */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at 20% 50%, #2a1f00 0%, #1C1C1C 60%), 
                     radial-gradient(ellipse at 80% 20%, #1a1a2e 0%, transparent 50%)`,
        transform: `scale(${bgScale})`,
      }} />

      {/* Shape Layer 1 — giant circle top right */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <circle
            cx={1600 + Math.sin(frame * 0.02) * 10}
            cy={-80 + Math.cos(frame * 0.015) * 12}
            r={420}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1.5"
            opacity={0.05}
          />
          <circle
            cx={1600 + Math.sin(frame * 0.02) * 10}
            cy={-80 + Math.cos(frame * 0.015) * 12}
            r={300}
            fill="#e9d700"
            opacity={0.03}
          />
        </svg>
      </AbsoluteFill>

      {/* Shape Layer 2 — rectangle bottom left drift */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <rect
            x={-60 + Math.sin(frame * 0.018) * 15}
            y={700 + Math.cos(frame * 0.022) * 8}
            width={350}
            height={350}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="1"
            opacity={0.04}
            transform={`rotate(${15 + frame * 0.03}, 115, 875)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion Grid Texture */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.035 }}>
          {Array.from({ length: 22 }).map((_, i) => (
            <line
              key={`v${i}`}
              x1={i * 90}
              y1={0}
              x2={i * 90}
              y2={1080}
              stroke="#F5F5F5"
              strokeWidth="0.5"
            />
          ))}
          {Array.from({ length: 13 }).map((_, i) => (
            <line
              key={`h${i}`}
              x1={0}
              y1={i * 90}
              x2={1920}
              y2={i * 90}
              stroke="#F5F5F5"
              strokeWidth="0.5"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 28 }).map((_, i) => {
            const px = seededRandom(i * 3) * 1920;
            const py = seededRandom(i * 3 + 1) * 1080;
            const speed = seededRandom(i * 3 + 2) * 0.04 + 0.015;
            const size = seededRandom(i * 7) * 2.5 + 0.8;
            return (
              <circle
                key={i}
                cx={px + Math.sin(frame * speed + i) * 18}
                cy={py + Math.cos(frame * speed * 0.7 + i) * 14}
                r={size}
                fill="#e9d700"
                opacity={0.18 + seededRandom(i * 11) * 0.2}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Horizontal accent line reveal */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: 420,
        width: `${lineReveal * 500}px`,
        height: 3,
        background: 'linear-gradient(90deg, #e9d700, transparent)',
        borderRadius: 2,
      }} />

      {/* KUNAL tag */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: 340,
        opacity: kunal,
        transform: `translateY(${(1 - kunal) * 30}px)`,
        fontFamily: 'Inter, Poppins, sans-serif',
        fontSize: 28,
        fontWeight: 700,
        color: '#e9d700',
        letterSpacing: 6,
        textTransform: 'uppercase',
      }}>
        KUNAL PRESENTS
      </div>

      {/* Main Headline */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: 440,
        opacity: headline,
        transform: `translateY(${(1 - headline) * 50}px)`,
        fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
        fontSize: 130,
        fontWeight: 900,
        color: '#F5F5F5',
        lineHeight: 1.05,
        maxWidth: 900,
      }}>
        जिंदगी की<br />
        <span style={{ color: '#e9d700' }}>दिशा</span> बदल दे
      </div>

      {/* Sub text */}
      <div style={{
        position: 'absolute',
        left: 122,
        top: 750,
        opacity: sub * 0.75,
        transform: `translateY(${(1 - sub) * 25}px)`,
        fontFamily: 'Noto Sans Devanagari, sans-serif',
        fontSize: 38,
        fontWeight: 400,
        color: '#9E9E9E',
        letterSpacing: 1,
      }}>
        एक बात जो आपने शायद कभी सोची नहीं
      </div>

      {/* Stick Figure — Buffett */}
      <div style={{
        position: 'absolute',
        right: 200,
        bottom: 80,
        transform: `translateY(${breathe + floatY * 0.3}px)`,
        opacity: sp(40),
      }}>
        <svg width="160" height="260" viewBox="0 0 160 260">
          {/* Golden glow */}
          <ellipse cx="80" cy="130" rx="70" ry="110" fill="#e9d700" opacity="0.06" />
          {/* Body */}
          <line x1="80" y1="100" x2="80" y2="190" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          {/* Arms */}
          <line x1="80" y1="130" x2="35" y2="160" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="130" x2="125" y2="155" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          {/* Legs */}
          <line x1="80" y1="190" x2="55" y2="245" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="190" x2="105" y2="245" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          {/* Head */}
          <circle
            cx="80"
            cy="75"
            r="28"
            fill="#2D2D2D"
            stroke="#F5F5F5"
            strokeWidth="3"
            transform={`rotate(${headNod}, 80, 75)`}
          />
          {/* Golden Glasses */}
          <g transform={`rotate(${headNod}, 80, 75)`}>
            <ellipse cx="68" cy="76" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <ellipse cx="92" cy="76" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <line x1="78" y1="76" x2="82" y2="76" stroke="#FFC107" strokeWidth="2" />
            <line x1="58" y1="76" x2="52" y2="74" stroke="#FFC107" strokeWidth="2" />
            <line x1="102" y1="76" x2="108" y2="74" stroke="#FFC107" strokeWidth="2" />
          </g>
          {/* Smile */}
          <path
            d="M 70 86 Q 80 94 90 86"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="2"
            strokeLinecap="round"
            transform={`rotate(${headNod}, 80, 75)`}
          />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 1,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default Scene1Intro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2Clock',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2Clock: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const clockRotate = interpolate(frame, [0, durationInFrames], [0, 60], { extrapolateRight: 'clamp' });
  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;
  const floatY = Math.sin(frame * 0.035) * 5;

  const headline = sp(15);
  const numReveal = sp(35);
  const subReveal = sp(55);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Deep dark warm gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, #0d0d0d 0%, #1C1C1C 100%)',
      }} />

      {/* Big Clock shape — center bg element */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <circle cx={960} cy={540} r={480} fill="none" stroke="#e9d700" strokeWidth="1" opacity={0.05} />
          <circle cx={960} cy={540} r={380} fill="none" stroke="#e9d700" strokeWidth="0.5" opacity={0.04} />
          {/* Clock tick marks */}
          {Array.from({ length: 24 }).map((_, i) => {
            const angle = (i / 24) * Math.PI * 2;
            const r1 = 460; const r2 = 480;
            return (
              <line
                key={i}
                x1={960 + Math.cos(angle) * r1}
                y1={540 + Math.sin(angle) * r1}
                x2={960 + Math.cos(angle) * r2}
                y2={540 + Math.sin(angle) * r2}
                stroke="#e9d700"
                strokeWidth={i % 6 === 0 ? 3 : 1}
                opacity={0.12}
              />
            );
          })}
          {/* Sweeping hand */}
          <line
            x1={960}
            y1={540}
            x2={960 + Math.cos((clockRotate * Math.PI) / 180 - Math.PI / 2) * 350}
            y2={540 + Math.sin((clockRotate * Math.PI) / 180 - Math.PI / 2) * 350}
            stroke="#e9d700"
            strokeWidth="2"
            opacity={0.15}
            strokeLinecap="round"
          />
        </svg>
      </AbsoluteFill>

      {/* Drifting triangles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <polygon
            points={`${150 + Math.sin(frame * 0.02) * 12},${200} ${280},${400 + Math.cos(frame * 0.018) * 10} ${20},${400}`}
            fill="#e9d700"
            opacity={0.04}
          />
          <polygon
            points={`${1700},${800} ${1850},${650 + Math.sin(frame * 0.025) * 8} ${1600},${620}`}
            fill="#F5F5F5"
            opacity={0.03}
          />
        </svg>
      </AbsoluteFill>

      {/* Circular dot texture */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 20 }).map((_, row) =>
            Array.from({ length: 35 }).map((_, col) => (
              <circle
                key={`${row}-${col}`}
                cx={col * 58 + 20}
                cy={row * 58 + 20}
                r={1.5}
                fill="#F5F5F5"
              />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 24 }).map((_, i) => {
            const px = seededRandom(i * 5) * 1920;
            const py = seededRandom(i * 5 + 1) * 1080;
            const spd = seededRandom(i * 5 + 2) * 0.035 + 0.01;
            const sz = seededRandom(i * 9) * 3 + 1;
            return (
              <circle
                key={i}
                cx={px + Math.sin(frame * spd + i) * 20}
                cy={py + Math.cos(frame * spd * 0.8 + i) * 15}
                r={sz}
                fill={i % 3 === 0 ? '#e9d700' : '#F5F5F5'}
                opacity={0.12 + seededRandom(i * 13) * 0.15}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* "हम सब" label */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: 200,
        opacity: headline,
        transform: `translateX(${(1 - headline) * -40}px)`,
        fontFamily: 'Noto Sans Devanagari, sans-serif',
        fontSize: 52,
        fontWeight: 700,
        color: '#9E9E9E',
        letterSpacing: 2,
      }}>
        हम सब रोज सुबह उठते हैं...
      </div>

      {/* 24 HOURS — Hero number */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: 310,
        opacity: numReveal,
        transform: `scale(${0.6 + numReveal * 0.4}) translateY(${(1 - numReveal) * 60}px)`,
        transformOrigin: 'left center',
        fontFamily: 'Inter, Poppins, sans-serif',
        fontSize: 260,
        fontWeight: 900,
        color: '#e9d700',
        lineHeight: 1,
        letterSpacing: -8,
      }}>
        24
      </div>

      {/* घंटे */}
      <div style={{
        position: 'absolute',
        left: 125,
        top: 680,
        opacity: numReveal,
        transform: `translateY(${(1 - numReveal) * 30}px)`,
        fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
        fontSize: 90,
        fontWeight: 900,
        color: '#F5F5F5',
        letterSpacing: 3,
      }}>
        घंटे
      </div>

      {/* Sub line */}
      <div style={{
        position: 'absolute',
        left: 125,
        top: 800,
        opacity: subReveal * 0.8,
        transform: `translateY(${(1 - subReveal) * 20}px)`,
        fontFamily: 'Noto Sans Devanagari, sans-serif',
        fontSize: 42,
        color: '#9E9E9E',
        fontWeight: 400,
      }}>
        न किसी के पास पच्चीस, न किसी के पास तेइस
      </div>

      {/* Vertical yellow accent bar */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: 310,
        width: 6,
        height: `${sp(20) * 430}px`,
        background: 'linear-gradient(180deg, #e9d700, transparent)',
        borderRadius: 3,
      }} />

      {/* Stick Figure */}
      <div style={{
        position: 'absolute',
        right: 160,
        top: 200,
        transform: `translateY(${breathe + floatY}px)`,
        opacity: sp(50),
      }}>
        <svg width="160" height="280" viewBox="0 0 160 280">
          <ellipse cx="80" cy="140" rx="68" ry="120" fill="#e9d700" opacity="0.05" />
          <line x1="80" y1="108" x2="80" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="30" y2="170" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="130" y2="165" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="52" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="108" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <circle cx="80" cy="82" r="28" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${headNod}, 80, 82)`} />
          <g transform={`rotate(${headNod}, 80, 82)`}>
            <ellipse cx="68" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <ellipse cx="92" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <line x1="78" y1="83" x2="82" y2="83" stroke="#FFC107" strokeWidth="2" />
            <line x1="58" y1="83" x2="52" y2="81" stroke="#FFC107" strokeWidth="2" />
            <line x1="102" y1="83" x2="108" y2="81" stroke="#FFC107" strokeWidth="2" />
          </g>
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene2Clock;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3Equal',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3Equal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const leftCard = sp(10);
  const vsReveal = sp(30);
  const rightCard = sp(20);
  const equalReveal = sp(50);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Split background */}
      <div style={{
        position: 'absolute', left: 0, top: 0, width: '50%', height: '100%',
        background: 'linear-gradient(135deg, #1a1200 0%, #1C1C1C 100%)',
      }} />
      <div style={{
        position: 'absolute', right: 0, top: 0, width: '50%', height: '100%',
        background: 'linear-gradient(225deg, #0a0a0a 0%, #1C1C1C 100%)',
      }} />

      {/* Center divider line */}
      <div style={{
        position: 'absolute', left: '50%', top: 0,
        width: 2, height: `${sp(5) * 1080}px`,
        background: 'linear-gradient(180deg, transparent, #e9d700, transparent)',
        transform: 'translateX(-50%)',
        opacity: 0.4,
      }} />

      {/* Shape — large diamond */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <polygon
            points={`960,${80 + Math.sin(frame * 0.02) * 8} ${1160},540 960,${1000 - Math.sin(frame * 0.02) * 8} ${760},540`}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
            opacity={0.05}
          />
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 20 }).map((_, i) => {
            const px = seededRandom(i * 7) * 1920;
            const py = seededRandom(i * 7 + 1) * 1080;
            const spd = seededRandom(i * 7 + 2) * 0.03 + 0.01;
            return (
              <circle key={i}
                cx={px + Math.sin(frame * spd + i) * 15}
                cy={py + Math.cos(frame * spd + i) * 12}
                r={seededRandom(i * 11) * 2.5 + 0.8}
                fill="#e9d700"
                opacity={0.15 + seededRandom(i) * 0.15}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* LEFT CARD — Ambani */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: '50%',
        transform: `translateY(-50%) translateX(${(1 - leftCard) * -80}px)`,
        opacity: leftCard,
        width: 700,
      }}>
        {/* Icon — suit/tie SVG */}
        <svg width="90" height="90" viewBox="0 0 90 90" style={{ marginBottom: 20 }}>
          <circle cx="45" cy="45" r="42" fill="#2D2D2D" stroke="#e9d700" strokeWidth="2" />
          <line x1="45" y1="20" x2="45" y2="70" stroke="#e9d700" strokeWidth="3" strokeLinecap="round" />
          <line x1="20" y1="40" x2="70" y2="40" stroke="#e9d700" strokeWidth="3" strokeLinecap="round" />
          <polygon points="45,15 55,35 35,35" fill="#FFC107" opacity="0.8" />
        </svg>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 32,
          fontWeight: 700,
          color: '#e9d700',
          letterSpacing: 3,
          textTransform: 'uppercase',
          marginBottom: 12,
        }}>अंबानी</div>
        <div style={{
          fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
          fontSize: 110,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1,
        }}>24<span style={{ fontSize: 48, color: '#9E9E9E' }}>hrs</span></div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 38,
          color: '#9E9E9E',
          marginTop: 10,
        }}>प्रति दिन</div>
      </div>

      {/* RIGHT CARD — Riksha */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: '50%',
        transform: `translateY(-50%) translateX(${(1 - rightCard) * 80}px)`,
        opacity: rightCard,
        width: 700,
        textAlign: 'right',
      }}>
        <div style={{ display: 'flex', justifyContent: 'flex-end', marginBottom: 20 }}>
          <svg width="90" height="90" viewBox="0 0 90 90">
            <circle cx="45" cy="45" r="42" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="2" />
            <circle cx="45" cy="30" r="12" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="42" x2="45" y2="65" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
            <line x1="28" y1="52" x2="62" y2="52" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
            <line x1="45" y1="65" x2="32" y2="80" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
            <line x1="45" y1="65" x2="58" y2="80" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          </svg>
        </div>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 32,
          fontWeight: 700,
          color: '#F5F5F5',
          letterSpacing: 3,
          textTransform: 'uppercase',
          marginBottom: 12,
        }}>रिक्षा चालक</div>
        <div style={{
          fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
          fontSize: 110,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1,
        }}>24<span style={{ fontSize: 48, color: '#9E9E9E' }}>hrs</span></div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 38,
          color: '#9E9E9E',
          marginTop: 10,
        }}>प्रति दिन</div>
      </div>

      {/* VS + Equal sign center */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '50%',
        transform: `translate(-50%, -50%) scale(${vsReveal})`,
        opacity: vsReveal,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 52,
          fontWeight: 900,
          color: '#e9d700',
          lineHeight: 1,
        }}>=</div>
      </div>

      {/* Bottom equal statement */}
      <div style={{
        position: 'absolute',
        bottom: 100,
        left: '50%',
        transform: `translateX(-50%) translateY(${(1 - equalReveal) * 30}px)`,
        opacity: equalReveal,
        textAlign: 'center',
        whiteSpace: 'nowrap',
      }}>
        <div style={{
          fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
          fontSize: 60,
          fontWeight: 900,
          color: '#e9d700',
        }}>सबकी जेब बराबर है</div>
      </div>

      {/* Stick Figure small — bottom right */}
      <div style={{
        position: 'absolute', right: 50, bottom: 40,
        transform: `translateY(${breathe}px)`,
        opacity: sp(60) * 0.6,
      }}>
        <svg width="100" height="170" viewBox="0 0 160 280">
          <line x1="80" y1="108" x2="80" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="30" y2="170" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="130" y2="165" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="52" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="108" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <circle cx="80" cy="82" r="28" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${headNod}, 80, 82)`} />
          <g transform={`rotate(${headNod}, 80, 82)`}>
            <ellipse cx="68" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <ellipse cx="92" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <line x1="78" y1="83" x2="82" y2="83" stroke="#FFC107" strokeWidth="2" />
          </g>
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene3Equal;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4TenYears',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4TenYears: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const barProgress = interpolate(frame, [30, 150], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const titleIn = sp(5);
  const bar1 = sp(25);
  const bar2 = sp(40);
  const shockText = sp(100);

  const bar1Height = barProgress * 550;
  const bar2Height = barProgress * 120;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, #0f0800 0%, #1C1C1C 40%, #000d00 100%)',
      }} />

      {/* Large transparent rectangles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <rect x={1400} y={-100} width={600} height={800}
            fill="#e9d700" opacity={0.03}
            transform={`rotate(${15 + frame * 0.02}, 1700, 300)`} />
          <rect x={-200} y={400} width={500} height={500}
            fill="#F5F5F5" opacity={0.02}
            transform={`rotate(${-10 + frame * 0.015}, 50, 650)`} />
        </svg>
      </AbsoluteFill>

      {/* Horizontal line texture */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 40 }).map((_, i) => (
            <line key={i} x1={0} y1={i * 28} x2={1920} y2={i * 28}
              stroke="#9E9E9E" strokeWidth="0.5" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 22 }).map((_, i) => {
            const px = seededRandom(i * 6) * 1920;
            const py = seededRandom(i * 6 + 1) * 1080;
            const spd = seededRandom(i * 6 + 2) * 0.03 + 0.01;
            return (
              <circle key={i}
                cx={px + Math.sin(frame * spd + i) * 16}
                cy={py + Math.cos(frame * spd + i) * 12}
                r={seededRandom(i * 9) * 2.5 + 0.8}
                fill={i % 2 === 0 ? '#e9d700' : '#F5F5F5'}
                opacity={0.14 + seededRandom(i) * 0.14}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Title */}
      <div style={{
        position: 'absolute',
        left: 120, top: 80,
        opacity: titleIn,
        transform: `translateY(${(1 - titleIn) * -30}px)`,
        fontFamily: 'Noto Sans Devanagari, sans-serif',
        fontSize: 50,
        fontWeight: 700,
        color: '#9E9E9E',
      }}>
        10 साल बाद का फ़रक
      </div>

      {/* Bar Chart — Person A (Invested) */}
      <div style={{
        position: 'absolute',
        left: 320,
        bottom: 160,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        opacity: bar1,
        transform: `translateY(${(1 - bar1) * 40}px)`,
      }}>
        {/* Bar */}
        <div style={{
          width: 180,
          height: bar1Height,
          background: 'linear-gradient(180deg, #e9d700, #FFC107)',
          borderRadius: '8px 8px 0 0',
          boxShadow: '0 0 40px rgba(233,215,0,0.3)',
          position: 'relative',
        }}>
          <div style={{
            position: 'absolute',
            top: -60,
            left: '50%',
            transform: 'translateX(-50%)',
            fontFamily: 'Inter, sans-serif',
            fontSize: 38,
            fontWeight: 900,
            color: '#e9d700',
            whiteSpace: 'nowrap',
          }}>आज़ाद</div>
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 36,
          fontWeight: 700,
          color: '#F5F5F5',
          marginTop: 16,
        }}>इंसान A</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 26,
          color: '#9E9E9E',
          marginTop: 6,
        }}>समय निवेश किया</div>
      </div>

      {/* Bar Chart — Person B (Wasted) */}
      <div style={{
        position: 'absolute',
        left: 600,
        bottom: 160,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        opacity: bar2,
        transform: `translateY(${(1 - bar2) * 40}px)`,
      }}>
        <div style={{
          width: 180,
          height: bar2Height,
          background: 'linear-gradient(180deg, #444, #2D2D2D)',
          borderRadius: '8px 8px 0 0',
          border: '1px solid #444',
          position: 'relative',
        }}>
          <div style={{
            position: 'absolute',
            top: -60,
            left: '50%',
            transform: 'translateX(-50%)',
            fontFamily: 'Inter, sans-serif',
            fontSize: 38,
            fontWeight: 900,
            color: '#9E9E9E',
            whiteSpace: 'nowrap',
          }}>वहीं</div>
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 36,
          fontWeight: 700,
          color: '#F5F5F5',
          marginTop: 16,
        }}>इंसान B</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 26,
          color: '#9E9E9E',
          marginTop: 6,
        }}>समय गंवाया</div>
      </div>

      {/* RIGHT — Shock text */}
      <div style={{
        position: 'absolute',
        right: 100,
        top: '50%',
        transform: `translateY(-50%) translateX(${(1 - shockText) * 60}px)`,
        opacity: shockText,
        maxWidth: 680,
        textAlign: 'right',
      }}>
        <div style={{
          fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
          fontSize: 100,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1.1,
        }}>हैरान कर<br />
          <span style={{ color: '#e9d700' }}>देने वाला</span><br />
          फ़रक
        </div>
      </div>

      {/* Baseline */}
      <div style={{
        position: 'absolute',
        left: 200,
        bottom: 158,
        width: 700,
        height: 3,
        background: '#9E9E9E',
        opacity: 0.3,
      }} />

      {/* Stick Figure */}
      <div style={{
        position: 'absolute', right: 60, bottom: 40,
        transform: `translateY(${breathe}px)`,
        opacity: sp(55),
      }}>
        <svg width="120" height="200" viewBox="0 0 160 280">
          <ellipse cx="80" cy="140" rx="68" ry="120" fill="#e9d700" opacity="0.05" />
          <line x1="80" y1="108" x2="80" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="30" y2="170" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="130" y2="165" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="52" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="108" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <circle cx="80" cy="82" r="28" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${headNod}, 80, 82)`} />
          <g transform={`rotate(${headNod}, 80, 82)`}>
            <ellipse cx="68" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <ellipse cx="92" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <line x1="78" y1="83" x2="82" y2="83" stroke="#FFC107" strokeWidth="2" />
          </g>
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene4TenYears;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5Freedom',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5Freedom: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;
  const floatY = Math.sin(frame * 0.035) * 6;

  const items = [
    { label: 'आज़ादी', icon: '🔓', delay: 10, color: '#e9d700' },
    { label: 'Skill Master', icon: '⚡', delay: 25, color: '#FFC107' },
    { label: 'विकल्प', icon: '🎯', delay: 40, color: '#F5F5F5' },
  ];

  const orbitAngle = interpolate(frame, [0, durationInFrames], [0, 360], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — golden tones */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 40%, #1a1200 0%, #1C1C1C 55%), radial-gradient(ellipse at 80% 80%, #0a0500 0%, transparent 50%)',
      }} />

      {/* Orbiting rings */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {[300, 420, 530].map((r, i) => (
            <ellipse key={i}
              cx={960} cy={540}
              rx={r} ry={r * 0.4}
              fill="none"
              stroke="#e9d700"
              strokeWidth="1"
              opacity={0.04 + i * 0.01}
              transform={`rotate(${orbitAngle * (i % 2 === 0 ? 1 : -1) * 0.3}, 960, 540)`}
            />
          ))}
          {/* Orbiting dot */}
          <circle
            cx={960 + Math.cos((orbitAngle * Math.PI) / 180) * 420}
            cy={540 + Math.sin((orbitAngle * Math.PI) / 180) * 168}
            r={6}
            fill="#e9d700"
            opacity={0.3}
          />
        </svg>
      </AbsoluteFill>

      {/* Dot matrix texture */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {Array.from({ length: 16 }).map((_, row) =>
            Array.from({ length: 28 }).map((_, col) => (
              <circle key={`${row}-${col}`}
                cx={col * 70 + 35} cy={row * 70 + 35}
                r={1.8} fill="#F5F5F5" />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 30 }).map((_, i) => {
            const px = seededRandom(i * 4) * 1920;
            const py = seededRandom(i * 4 + 1) * 1080;
            const spd = seededRandom(i * 4 + 2) * 0.04 + 0.01;
            return (
              <circle key={i}
                cx={px + Math.sin(frame * spd + i) * 18}
                cy={py + Math.cos(frame * spd + i) * 14}
                r={seededRandom(i * 8) * 3 + 0.8}
                fill="#e9d700"
                opacity={0.15 + seededRandom(i) * 0.18}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Center — BIG "आज़ाद" Hero */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '50%',
        transform: `translate(-50%, -50%) translateY(${floatY}px)`,
        textAlign: 'center',
        opacity: sp(5),
      }}>
        <div style={{
          fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
          fontSize: 200,
          fontWeight: 900,
          color: '#e9d700',
          lineHeight: 1,
          textShadow: '0 0 80px rgba(233,215,0,0.25)',
        }}>
          आज़ाद
        </div>
      </div>

      {/* Three trait cards — LEFT side vertical stack */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: '50%',
        transform: 'translateY(-50%)',
        display: 'flex',
        flexDirection: 'column',
        gap: 28,
      }}>
        {items.map((item, idx) => (
          <div key={idx} style={{
            opacity: sp(item.delay),
            transform: `translateX(${(1 - sp(item.delay)) * -50}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            background: 'rgba(255,255,255,0.03)',
            border: `1px solid ${item.color}22`,
            borderRadius: 14,
            padding: '18px 32px',
            minWidth: 320,
          }}>
            <div style={{
              width: 14, height: 14, borderRadius: '50%',
              background: item.color,
              boxShadow: `0 0 12px ${item.color}`,
              flexShrink: 0,
            }} />
            <div style={{
              fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
              fontSize: 48,
              fontWeight: 800,
              color: item.color,
            }}>{item.label}</div>
          </div>
        ))}
      </div>

      {/* Stick Figure — elevated position, arms up */}
      <div style={{
        position: 'absolute',
        right: 120,
        bottom: 60,
        transform: `translateY(${breathe + floatY * 0.5}px)`,
        opacity: sp(50),
      }}>
        <svg width="160" height="280" viewBox="0 0 160 280">
          <ellipse cx="80" cy="140" rx="70" ry="120" fill="#e9d700" opacity="0.07" />
          <line x1="80" y1="108" x2="80" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          {/* Arms UP — celebratory */}
          <line x1="80" y1="130" x2="25" y2="90" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="130" x2="135" y2="90" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="52" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="108" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <circle cx="80" cy="82" r="28" fill="#2D2D2D" stroke="#e9d700" strokeWidth="3"
            transform={`rotate(${headNod}, 80, 82)`} />
          <g transform={`rotate(${headNod}, 80, 82)`}>
            <ellipse cx="68" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <ellipse cx="92" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <line x1="78" y1="83" x2="82" y2="83" stroke="#FFC107" strokeWidth="2" />
            <line x1="58" y1="83" x2="52" y2="81" stroke="#FFC107" strokeWidth="2" />
            <line x1="102" y1="83" x2="108" y2="81" stroke="#FFC107" strokeWidth="2" />
          </g>
          {/* Smile — happy */}
          <path d="M 68 90 Q 80 100 92 90" fill="none" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round"
            transform={`rotate(${headNod}, 80, 82)`} />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene5Freedom;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6Stuck',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6Stuck: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const loopX = interpolate(frame, [0, durationInFrames], [0, 1], { extrapolateRight: 'clamp' });

  const leftLabel = sp(10);
  const loopReveal = sp(30);
  const captionReveal = sp(60);
  const tagReveal = sp(80);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Muted dark grey gradient — stuck, stagnant feeling */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 30%, #1a1a1a 0%, #0d0d0d 100%)',
      }} />

      {/* Repeating loop shape — circular arrows bg */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {[200, 320, 430].map((r, i) => (
            <circle key={i}
              cx={1400} cy={540}
              r={r}
              fill="none"
              stroke="#9E9E9E"
              strokeWidth="1"
              strokeDasharray={`${r * 0.3} ${r * 0.1}`}
              opacity={0.05}
              transform={`rotate(${frame * (i % 2 === 0 ? 0.3 : -0.2)}, 1400, 540)`}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Grid texture — tight */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {Array.from({ length: 50 }).map((_, i) => (
            <line key={`v${i}`} x1={i * 40} y1={0} x2={i * 40} y2={1080}
              stroke="#9E9E9E" strokeWidth="0.5" />
          ))}
          {Array.from({ length: 28 }).map((_, i) => (
            <line key={`h${i}`} x1={0} y1={i * 40} x2={1920} y2={i * 40}
              stroke="#9E9E9E" strokeWidth="0.5" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles — grey muted */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 18 }).map((_, i) => {
            const px = seededRandom(i * 8) * 1920;
            const py = seededRandom(i * 8 + 1) * 1080;
            const spd = seededRandom(i * 8 + 2) * 0.025 + 0.008;
            return (
              <circle key={i}
                cx={px + Math.sin(frame * spd + i) * 10}
                cy={py + Math.cos(frame * spd + i) * 8}
                r={seededRandom(i * 10) * 2 + 0.5}
                fill="#9E9E9E"
                opacity={0.1 + seededRandom(i) * 0.1}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* LEFT — Attributes */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: '50%',
        transform: `translateY(-50%) translateX(${(1 - leftLabel) * -50}px)`,
        opacity: leftLabel,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 44,
          fontWeight: 400,
          color: '#9E9E9E',
          lineHeight: 1.6,
        }}>
          उतना ही <span style={{ color: '#F5F5F5', fontWeight: 700 }}>समझदार</span><br />
          उतना ही <span style={{ color: '#F5F5F5', fontWeight: 700 }}>काबिल</span><br />
          उतनी ही <span style={{ color: '#F5F5F5', fontWeight: 700 }}>मेहनत</span>
        </div>
      </div>

      {/* Center — STUCK visual */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '50%',
        transform: `translate(-50%, -50%)`,
        textAlign: 'center',
        opacity: loopReveal,
      }}>
        {/* Loop arrow SVG */}
        <svg width="200" height="200" viewBox="0 0 200 200" style={{ marginBottom: 20 }}>
          <circle cx="100" cy="100" r="80" fill="none" stroke="#9E9E9E" strokeWidth="6"
            strokeDasharray="80 420"
            strokeDashoffset={`${-loopX * 500}`}
            strokeLinecap="round" />
          {/* Arrow head */}
          <polygon points="160,60 175,85 145,85" fill="#9E9E9E" opacity={loopReveal * 0.8} />
        </svg>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 28,
          fontWeight: 700,
          color: '#9E9E9E',
          letterSpacing: 4,
          textTransform: 'uppercase',
        }}>LOOP</div>
      </div>

      {/* RIGHT — The result */}
      <div style={{
        position: 'absolute',
        right: 100,
        top: '50%',
        transform: `translateY(-50%) translateX(${(1 - captionReveal) * 60}px)`,
        opacity: captionReveal,
        maxWidth: 550,
        textAlign: 'right',
      }}>
        <div style={{
          fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
          fontSize: 95,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1.1,
        }}>
          वही खड़ा<br />
          <span style={{ color: '#9E9E9E' }}>मिलता है</span>
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 42,
          color: '#9E9E9E',
          marginTop: 16,
        }}>
          जहां दस साल पहले था
        </div>
      </div>

      {/* Bottom tag */}
      <div style={{
        position: 'absolute',
        bottom: 110,
        left: '50%',
        transform: `translateX(-50%) translateY(${(1 - tagReveal) * 20}px)`,
        opacity: tagReveal * 0.6,
        fontFamily: 'Inter, sans-serif',
        fontSize: 28,
        color: '#9E9E9E',
        letterSpacing: 3,
        textTransform: 'uppercase',
        borderTop: '1px solid #333',
        paddingTop: 16,
        whiteSpace: 'nowrap',
      }}>
        TALENT ≠ RESULTS
      </div>

      {/* Stick Figure — slumped posture */}
      <div style={{
        position: 'absolute',
        left: 820,
        bottom: 80,
        transform: `translateY(${breathe}px)`,
        opacity: sp(45),
      }}>
        <svg width="120" height="220" viewBox="0 0 160 280">
          <line x1="80" y1="108" x2="80" y2="200" stroke="#9E9E9E" strokeWidth="4" strokeLinecap="round" />
          {/* Arms down — slumped */}
          <line x1="80" y1="138" x2="40" y2="178" stroke="#9E9E9E" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="120" y2="178" stroke="#9E9E9E" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="52" y2="258" stroke="#9E9E9E" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="108" y2="258" stroke="#9E9E9E" strokeWidth="4" strokeLinecap="round" />
          <circle cx="80" cy="82" r="28" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="3"
            transform={`rotate(${headNod * 0.3}, 80, 82)`} />
          <g transform={`rotate(${headNod * 0.3}, 80, 82)`}>
            <ellipse cx="68" cy="83" rx="10" ry="7" fill="none" stroke="#9E9E9E" strokeWidth="2" />
            <ellipse cx="92" cy="83" rx="10" ry="7" fill="none" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="78" y1="83" x2="82" y2="83" stroke="#9E9E9E" strokeWidth="1.5" />
          </g>
          {/* Sad mouth */}
          <path d="M 68 92 Q 80 84 92 92" fill="none" stroke="#9E9E9E" strokeWidth="2"
            strokeLinecap="round" transform={`rotate(${headNod * 0.3}, 80, 82)`} />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene6Stuck;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7TheGap',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7TheGap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const strikeTalent = sp(20);
  const strikeKismat = sp(40);
  const revealShaam = sp(65);
  const glowPulse = Math.sin(frame * 0.08) * 0.15 + 0.85;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Dark bold gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #0a0500 0%, #1C1C1C 50%, #000a00 100%)',
      }} />

      {/* Large X shapes bg */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <line x1={100} y1={100} x2={400} y2={400} stroke="#ff0000" strokeWidth="1" opacity={0.04} />
          <line x1={400} y1={100} x2={100} y2={400} stroke="#ff0000" strokeWidth="1" opacity={0.04} />
          <line x1={1500} y1={700} x2={1800} y2={1000} stroke="#ff0000" strokeWidth="1" opacity={0.04} />
          <line x1={1800} y1={700} x2={1500} y2={1000} stroke="#ff0000" strokeWidth="1" opacity={0.04} />
          {/* Gold diagonal accent */}
          <line x1={0} y1={1080} x2={1920} y2={0} stroke="#e9d700" strokeWidth="1" opacity={0.04} />
        </svg>
      </AbsoluteFill>

      {/* Diagonal lines texture */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {Array.from({ length: 30 }).map((_, i) => (
            <line key={i}
              x1={i * 70 - 500} y1={0}
              x2={i * 70 + 500} y2={1080}
              stroke="#9E9E9E" strokeWidth="0.5" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 25 }).map((_, i) => {
            const px = seededRandom(i * 5) * 1920;
            const py = seededRandom(i * 5 + 1) * 1080;
            const spd = seededRandom(i * 5 + 2) * 0.03 + 0.01;
            return (
              <circle key={i}
                cx={px + Math.sin(frame * spd + i) * 14}
                cy={py + Math.cos(frame * spd + i) * 11}
                r={seededRandom(i * 11) * 2.5 + 0.8}
                fill={i % 3 === 0 ? '#e9d700' : '#9E9E9E'}
                opacity={0.1 + seededRandom(i) * 0.12}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* STRUCK THROUGH — Talent */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: 140,
        opacity: strikeTalent,
        transform: `translateX(${(1 - strikeTalent) * -40}px)`,
        position: 'relative' as any,
      }}>
        <div style={{
          fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
          fontSize: 110,
          fontWeight: 900,
          color: '#444',
          lineHeight: 1,
          position: 'absolute',
          left: 100,
          top: 140,
        }}>
          टैलेंट
        </div>
        {/* Strikethrough SVG */}
        <svg style={{ position: 'absolute', left: 100, top: 195, pointerEvents: 'none' }} width="400" height="12">
          <line x1={0} y1={6} x2={strikeTalent * 360} y2={6}
            stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
        </svg>
        {/* X */}
        <div style={{
          position: 'absolute',
          left: 470,
          top: 150,
          fontFamily: 'Inter, sans-serif',
          fontSize: 80,
          fontWeight: 900,
          color: '#ff0000',
          opacity: strikeTalent,
        }}>✕</div>
      </div>

      {/* STRUCK THROUGH — Kismat */}
      <div style={{ position: 'relative' as any }}>
        <div style={{
          fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
          fontSize: 110,
          fontWeight: 900,
          color: '#444',
          lineHeight: 1,
          position: 'absolute',
          left: 100,
          top: 320,
          opacity: strikeKismat,
          transform: `translateX(${(1 - strikeKismat) * -40}px)`,
        }}>
          किसमत
        </div>
        <svg style={{ position: 'absolute', left: 100, top: 375, pointerEvents: 'none' }} width="480" height="12">
          <line x1={0} y1={6} x2={strikeKismat * 440} y2={6}
            stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
        </svg>
        <div style={{
          position: 'absolute',
          left: 570,
          top: 328,
          fontFamily: 'Inter, sans-serif',
          fontSize: 80,
          fontWeight: 900,
          color: '#ff0000',
          opacity: strikeKismat,
        }}>✕</div>
      </div>

      {/* REVEAL — The Real Answer */}
      <div style={{
        position: 'absolute',
        left: 100,
        bottom: 140,
        opacity: revealShaam,
        transform: `translateY(${(1 - revealShaam) * 50}px)`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 44,
          color: '#9E9E9E',
          marginBottom: 12,
          letterSpacing: 1,
        }}>
          असली फ़रक है →
        </div>
        <div style={{
          fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
          fontSize: 130,
          fontWeight: 900,
          color: '#e9d700',
          lineHeight: 1,
          textShadow: `0 0 ${60 * glowPulse}px rgba(233,215,0,${0.3 * glowPulse})`,
        }}>
          वो शामें
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 48,
          color: '#F5F5F5',
          marginTop: 10,
        }}>
          निवेश की गई <span style={{ color: '#e9d700' }}>vs</span> गंवाई गई
        </div>
      </div>

      {/* Stick Figure */}
      <div style={{
        position: 'absolute',
        right: 140,
        top: '50%',
        transform: `translateY(${-50 + breathe}%)`,
        opacity: sp(55),
      }}>
        <svg width="160" height="280" viewBox="0 0 160 280">
          <ellipse cx="80" cy="140" rx="68" ry="120" fill="#e9d700" opacity="0.06" />
          <line x1="80" y1="108" x2="80" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="30" y2="170" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="130" y2="155" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="52" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="108" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <circle cx="80" cy="82" r="28" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${headNod}, 80, 82)`} />
          <g transform={`rotate(${headNod}, 80, 82)`}>
            <ellipse cx="68" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <ellipse cx="92" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <line x1="78" y1="83" x2="82" y2="83" stroke="#FFC107" strokeWidth="2" />
            <line x1="58" y1="83" x2="52" y2="81" stroke="#FFC107" strokeWidth="2" />
            <line x1="102" y1="83" x2="108" y2="81" stroke="#FFC107" strokeWidth="2" />
          </g>
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.78) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene7TheGap;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8OpCost',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8OpCost: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;
  const scanLine = interpolate(frame, [20, 100], [0, 1920], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const tagIn = sp(8);
  const termIn = sp(22);
  const defIn = sp(45);
  const arrowIn = sp(65);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Terminal/code-like dark bg */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #050505 0%, #0d0d0d 50%, #1C1C1C 100%)',
      }} />

      {/* Scan line reveal effect */}
      <div style={{
        position: 'absolute',
        left: 0,
        top: 0,
        width: scanLine,
        height: '100%',
        background: 'linear-gradient(90deg, transparent 90%, rgba(233,215,0,0.06) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Shape — large angled rectangle */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <rect x={1100} y={-200} width={900} height={1400}
            fill="#e9d700" opacity={0.03}
            transform={`rotate(-15, 1550, 500) skewX(${Math.sin(frame * 0.015) * 1})`}
          />
          {/* Circuit lines */}
          <line x1={0} y1={540} x2={1920} y2={540} stroke="#e9d700" strokeWidth="0.5" opacity={0.06}
            strokeDasharray="20 40" />
          <line x1={960} y1={0} x2={960} y2={1080} stroke="#e9d700" strokeWidth="0.5" opacity={0.04}
            strokeDasharray="20 40" />
        </svg>
      </AbsoluteFill>

      {/* Monospace dot grid */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.025 }}>
          {Array.from({ length: 22 }).map((_, row) =>
            Array.from({ length: 40 }).map((_, col) => (
              <rect key={`${row}-${col}`}
                x={col * 50 + 10} y={row * 50 + 10}
                width={2} height={2}
                fill="#e9d700" />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 20 }).map((_, i) => {
            const px = seededRandom(i * 6) * 1920;
            const py = seededRandom(i * 6 + 1) * 1080;
            const spd = seededRandom(i * 6 + 2) * 0.035 + 0.01;
            return (
              <circle key={i}
                cx={px + Math.sin(frame * spd + i) * 12}
                cy={py + Math.cos(frame * spd + i) * 10}
                r={seededRandom(i * 9) * 2.5 + 0.8}
                fill="#e9d700"
                opacity={0.12 + seededRandom(i) * 0.12}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Tag */}
      <div style={{
        position: 'absolute',
        left: 120, top: 120,
        opacity: tagIn,
        transform: `translateY(${(1 - tagIn) * -20}px)`,
        fontFamily: 'JetBrains Mono, monospace',
        fontSize: 24,
        color: '#e9d700',
        letterSpacing: 3,
        opacity: tagIn * 0.6,
      }}>
        {'>'} FINANCE_PRINCIPLE_001
      </div>

      {/* OPPORTUNITY COST — Hero term */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: 220,
        opacity: termIn,
        transform: `scale(${0.7 + termIn * 0.3}) translateX(${(1 - termIn) * -30}px)`,
        transformOrigin: 'left center',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, Inter, monospace',
          fontSize: 120,
          fontWeight: 900,
          color: '#e9d700',
          letterSpacing: -3,
          lineHeight: 1,
          textShadow: '0 0 60px rgba(233,215,0,0.2)',
        }}>
          OPPORTUNITY
        </div>
        <div style={{
          fontFamily: 'JetBrains Mono, Inter, monospace',
          fontSize: 120,
          fontWeight: 900,
          color: '#F5F5F5',
          letterSpacing: -3,
          lineHeight: 1,
        }}>
          COST
        </div>
      </div>

      {/* Hindi translation */}
      <div style={{
        position: 'absolute',
        left: 124,
        top: 520,
        opacity: defIn,
        transform: `translateY(${(1 - defIn) * 25}px)`,
        fontFamily: 'Noto Sans Devanagari, sans-serif',
        fontSize: 44,
        color: '#9E9E9E',
        fontWeight: 400,
      }}>
        अवसर की कीमत
      </div>

      {/* Definition box */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: 610,
        opacity: defIn,
        transform: `translateY(${(1 - defIn) * 25}px)`,
        maxWidth: 820,
        borderLeft: '4px solid #e9d700',
        paddingLeft: 24,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 40,
          color: '#F5F5F5',
          lineHeight: 1.5,
          fontWeight: 400,
        }}>
          एक रुपया यहाँ लगाया, तो<br />
          <span style={{ color: '#e9d700', fontWeight: 700 }}>वहाँ नहीं लगा सकते</span>
        </div>
      </div>

      {/* Arrow equation — right side */}
      <div style={{
        position: 'absolute',
        right: 120,
        top: '50%',
        transform: `translateY(-50%) translateX(${(1 - arrowIn) * 60}px)`,
        opacity: arrowIn,
        textAlign: 'center',
      }}>
        <svg width="300" height="300" viewBox="0 0 300 300">
          <circle cx="150" cy="80" r="60" fill="#2D2D2D" stroke="#e9d700" strokeWidth="2" />
          <text x="150" y="90" textAnchor="middle" fill="#e9d700"
            style={{ fontFamily: 'Inter', fontSize: '32px', fontWeight: 900 }}>₹</text>
          {/* Arrow left */}
          <line x1="80" y1="160" x2="30" y2="230" stroke="#e9d700" strokeWidth="3" strokeLinecap="round" />
          <polygon points="20,240 45,220 35,248" fill="#e9d700" />
          {/* Arrow right — grey = missed */}
          <line x1="220" y1="160" x2="270" y2="230" stroke="#9E9E9E" strokeWidth="3" strokeLinecap="round" />
          <polygon points="280,240 255,220 265,248" fill="#9E9E9E" />
          {/* Labels */}
          <text x="10" y="280" fill="#e9d700"
            style={{ fontFamily: 'Noto Sans Devanagari', fontSize: '22px' }}>यहाँ</text>
          <text x="230" y="280" fill="#9E9E9E"
            style={{ fontFamily: 'Noto Sans Devanagari', fontSize: '22px' }}>वहाँ ✕</text>
        </svg>
      </div>

      {/* Stick Figure */}
      <div style={{
        position: 'absolute',
        right: 80,
        bottom: 50,
        transform: `translateY(${breathe}px)`,
        opacity: sp(60) * 0.7,
      }}>
        <svg width="110" height="185" viewBox="0 0 160 280">
          <line x1="80" y1="108" x2="80" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="30" y2="170" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="130" y2="155" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="52" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="108" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <circle cx="80" cy="82" r="28" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${headNod}, 80, 82)`} />
          <g transform={`rotate(${headNod}, 80, 82)`}>
            <ellipse cx="68" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <ellipse cx="92" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <line x1="78" y1="83" x2="82" y2="83" stroke="#FFC107" strokeWidth="2" />
          </g>
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene8OpCost;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9TradeOff',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9TradeOff: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;
  const scrollY = interpolate(frame, [30, 120], [0, 300], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  const phoneIn = sp(10);
  const labelLeft = sp(30);
  const arrowIn = sp(50);
  const labelRight = sp(65);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Blue-grey gradient — phone screen feel */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 20%, #0a0a14 0%, #1C1C1C 60%)',
      }} />

      {/* Phone screen glow */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '50%',
        transform: 'translate(-50%, -50%)',
        width: 340,
        height: 600,
        background: 'radial-gradient(ellipse at center, rgba(100,120,255,0.04) 0%, transparent 70%)',
        borderRadius: 40,
        pointerEvents: 'none',
      }} />

      {/* Shape — hexagons */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {[{ cx: 200, cy: 200, r: 120 }, { cx: 1700, cy: 800, r: 180 }, { cx: 1600, cy: 150, r: 90 }].map((h, i) => (
            <polygon key={i}
              points={Array.from({ length: 6 }).map((_, j) => {
                const a = (j * 60 * Math.PI) / 180 + frame * 0.005 * (i % 2 === 0 ? 1 : -1);
                return `${h.cx + h.r * Math.cos(a)},${h.cy + h.r * Math.sin(a)}`;
              }).join(' ')}
              fill="none"
              stroke="#9E9E9E"
              strokeWidth="1"
              opacity={0.05}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 22 }).map((_, i) => {
            const px = seededRandom(i * 7) * 1920;
            const py = seededRandom(i * 7 + 1) * 1080;
            const spd = seededRandom(i * 7 + 2) * 0.03 + 0.01;
            return (
              <circle key={i}
                cx={px + Math.sin(frame * spd + i) * 14}
                cy={py + Math.cos(frame * spd + i) * 10}
                r={seededRandom(i * 10) * 2.5 + 0.8}
                fill={i % 3 === 0 ? '#e9d700' : '#9E9E9E'}
                opacity={0.1 + seededRandom(i) * 0.12}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* LEFT — Phone scroll */}
      <div style={{
        position: 'absolute',
        left: 180,
        top: '50%',
        transform: `translateY(-50%) translateX(${(1 - phoneIn) * -60}px)`,
        opacity: phoneIn,
        textAlign: 'center',
      }}>
        {/* Phone SVG */}
        <svg width="220" height="400" viewBox="0 0 220 400">
          <rect x={10} y={10} width={200} height={380} rx={28} ry={28}
            fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="2" />
          <rect x={20} y={50} width={180} height={300} rx={4} ry={4}
            fill="#1a1a1a" clipPath="url(#phoneClip)" />
          <defs>
            <clipPath id="phoneClip">
              <rect x={20} y={50} width={180} height={300} rx={4} />
            </clipPath>
          </defs>
          {/* Scroll content — moving reels */}
          {Array.from({ length: 6 }).map((_, i) => (
            <rect key={i}
              x={25}
              y={55 + i * 105 - (scrollY % 105)}
              width={170}
              height={95}
              rx={6}
              fill={['#2a2a35', '#252530', '#2a2a3a', '#252525', '#2a2030', '#202030'][i]}
              clipPath="url(#phoneClip)"
            />
          ))}
          {/* Finger gesture */}
          <circle cx={110} cy={200} r={18} fill="#F5F5F5" opacity={0.15}
            transform={`translateY(${Math.sin(frame * 0.15) * 20})`} />
          {/* Home indicator */}
          <line x1={85} y1={375} x2={135} y2={375} stroke="#9E9E9E" strokeWidth="3" strokeLinecap="round" />
        </svg>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 36,
          color: '#9E9E9E',
          marginTop: 16,
        }}>घंटों Scroll</div>
      </div>

      {/* CENTER — Trade-off arrow */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '50%',
        transform: `translate(-50%, -50%) scale(${arrowIn})`,
        opacity: arrowIn,
        textAlign: 'center',
      }}>
        <svg width="200" height="120" viewBox="0 0 200 120">
          <line x1={10} y1={60} x2={190} y2={60} stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
          <polygon points="185,45 200,60 185,75" fill="#e9d700" />
          <line x1={190} y1={60} x2={10} y2={60} stroke="#ff0000" strokeWidth="2" strokeLinecap="round"
            strokeDasharray="12 8" />
          <polygon points="15,47 0,60 15,73" fill="#ff0000" />
        </svg>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 26,
          fontWeight: 700,
          color: '#e9d700',
          letterSpacing: 2,
          marginTop: 8,
        }}>TRADE-OFF</div>
      </div>

      {/* RIGHT — What's lost */}
      <div style={{
        position: 'absolute',
        right: 120,
        top: '50%',
        transform: `translateY(-50%) translateX(${(1 - labelRight) * 60}px)`,
        opacity: labelRight,
        maxWidth: 500,
        textAlign: 'right',
      }}>
        <div style={{
          fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
          fontSize: 90,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1.1,
        }}>
          समय<br />
          <span style={{ color: '#ff0000', textDecoration: 'line-through', fontSize: 70 }}>बरबाद</span>
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 40,
          color: '#9E9E9E',
          marginTop: 16,
          lineHeight: 1.5,
        }}>
          निवेश नहीं हो रहा<br />
          <span style={{ color: '#e9d700', fontWeight: 700 }}>= खोया हुआ future</span>
        </div>
      </div>

      {/* Left label under phone */}
      <div style={{
        position: 'absolute',
        left: 120,
        bottom: 100,
        opacity: labelLeft,
        fontFamily: 'Noto Sans Devanagari, sans-serif',
        fontSize: 32,
        color: '#9E9E9E',
      }}>
        Reels · Netflix · Sofa
      </div>

      {/* Stick Figure */}
      <div style={{
        position: 'absolute',
        right: 60,
        bottom: 40,
        transform: `translateY(${breathe}px)`,
        opacity: sp(55),
      }}>
        <svg width="110" height="190" viewBox="0 0 160 280">
          <line x1="80" y1="108" x2="80" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="30" y2="170" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="138" x2="130" y2="155" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="52" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="108" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <circle cx="80" cy="82" r="28" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${headNod}, 80, 82)`} />
          <g transform={`rotate(${headNod}, 80, 82)`}>
            <ellipse cx="68" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <ellipse cx="92" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <line x1="78" y1="83" x2="82" y2="83" stroke="#FFC107" strokeWidth="2" />
          </g>
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene9TradeOff;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10Compound',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10Compound: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;
  const floatY = Math.sin(frame * 0.03) * 8;
  const glowPulse = Math.sin(frame * 0.06) * 0.2 + 0.8;

  const curveProgress = interpolate(frame, [20, 160], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const titleIn = sp(5);
  const chip1 = sp(30);
  const chip2 = sp(45);
  const chip3 = sp(60);
  const chip4 = sp(75);
  const bigReveal = sp(90);

  const compounds = [
    { label: 'Skills', color: '#e9d700', delay: 30 },
    { label: 'ध्यान', color: '#FFC107', delay: 45 },
    { label: 'रिश्ते', color: '#F5F5F5', delay: 60 },
    { label: 'सेहत', color: '#e9d700', delay: 75 },
  ];

  // Compound curve points
  const curvePoints = Array.from({ length: 100 }).map((_, i) => {
    const t = i / 99;
    const x = 100 + t * 700;
    const y = 850 - Math.pow(t, 2.5) * 600;
    return { x, y };
  });

  const visiblePoints = curvePoints.slice(0, Math.floor(curveProgress * 100));
  const pathD = visiblePoints.length > 1
    ? `M ${visiblePoints[0].x} ${visiblePoints[0].y} ` +
      visiblePoints.slice(1).map(p => `L ${p.x} ${p.y}`).join(' ')
    : '';

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Rich dark gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 15% 80%, #0f0800 0%, #1C1C1C 50%), radial-gradient(ellipse at 85% 20%, #001200 0%, transparent 50%)',
      }} />

      {/* Compound curve glow bg */}
      <div style={{
        position: 'absolute',
        left: 60,
        top: 250,
        width: 800,
        height: 650,
        background: 'radial-gradient(ellipse at 70% 90%, rgba(233,215,0,0.04) 0%, transparent 60%)',
        pointerEvents: 'none',
      }} />

      {/* Shape — concentric partial arcs */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {[200, 350, 500].map((r, i) => (
            <path key={i}
              d={`M ${1800},${1080 - r} A ${r},${r} 0 0,0 ${1800 - r},${1080}`}
              fill="none"
              stroke="#e9d700"
              strokeWidth="1"
              opacity={0.05 + i * 0.01}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 30 }).map((_, i) => {
            const px = seededRandom(i * 4) * 1920;
            const py = seededRandom(i * 4 + 1) * 1080;
            const spd = seededRandom(i * 4 + 2) * 0.04 + 0.01;
            return (
              <circle key={i}
                cx={px + Math.sin(frame * spd + i) * 16}
                cy={py + Math.cos(frame * spd + i) * 12}
                r={seededRandom(i * 8) * 3 + 0.8}
                fill={i % 2 === 0 ? '#e9d700' : '#FFC107'}
                opacity={0.12 + seededRandom(i) * 0.15}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Compound Curve Chart */}
      <div style={{
        position: 'absolute',
        left: 60,
        top: 220,
        opacity: sp(15),
      }}>
        <svg width="850" height="700" viewBox="0 0 850 700">
          {/* Axes */}
          <line x1={90} y1={20} x2={90} y2={860} stroke="#9E9E9E" strokeWidth="1.5" opacity={0.3} />
          <line x1={90} y1={860} x2={820} y2={860} stroke="#9E9E9E" strokeWidth="1.5" opacity={0.3} />
          {/* Grid */}
          {[200, 400, 600, 800].map((y, i) => (
            <line key={i} x1={90} y1={y - 10} x2={820} y2={y - 10}
              stroke="#9E9E9E" strokeWidth="0.5" opacity={0.12} strokeDasharray="8 12" />
          ))}
          {/* Compound Curve */}
          {pathD && (
            <>
              <defs>
                <linearGradient id="curveGrad" x1="0%" y1="0%" x2="100%" y2="0%">
                  <stop offset="0%" stopColor="#e9d700" stopOpacity="0.4" />
                  <stop offset="100%" stopColor="#e9d700" stopOpacity="1" />
                </linearGradient>
              </defs>
              <path d={pathD} fill="none" stroke="url(#curveGrad)" strokeWidth="4"
                strokeLinecap="round" strokeLinejoin="round" />
              {/* Glow */}
              <path d={pathD} fill="none" stroke="#e9d700" strokeWidth="10"
                strokeLinecap="round" strokeLinejoin="round" opacity={0.08} />
            </>
          )}
          {/* Axis labels */}
          <text x="90" y="890" fill="#9E9E9E"
            style={{ fontFamily: 'Noto Sans Devanagari', fontSize: '22px' }}>शुरुआत</text>
          <text x="680" y="890" fill="#9E9E9E"
            style={{ fontFamily: 'Noto Sans Devanagari', fontSize: '22px' }}>5 साल</text>
          <text x="20" y="50" fill="#9E9E9E"
            style={{ fontFamily: 'Noto Sans Devanagari', fontSize: '22px', writingMode: 'vertical-rl' }}>Growth</text>
        </svg>
      </div>

      {/* Title */}
      <div style={{
        position: 'absolute',
        right: 100,
        top: 80,
        opacity: titleIn,
        transform: `translateY(${(1 - titleIn) * -30}px)`,
        textAlign: 'right',
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 44,
          color: '#9E9E9E',
          marginBottom: 8,
        }}>एक खाली शाम =</div>
        <div style={{
          fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
          fontSize: 80,
          fontWeight: 900,
          color: '#ff0000',
          lineHeight: 1,
        }}>छूटा हुआ</div>
        <div style={{
          fontFamily: 'JetBrains Mono, Inter, monospace',
          fontSize: 60,
          fontWeight: 900,
          color: '#e9d700',
        }}>COMPOUNDING</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 44,
          color: '#9E9E9E',
        }}>event</div>
      </div>

      {/* Compound chips */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: 380,
        display: 'flex',
        flexDirection: 'column',
        gap: 20,
        alignItems: 'flex-end',
      }}>
        {compounds.map((c, i) => (
          <div key={i} style={{
            opacity: sp(c.delay),
            transform: `translateX(${(1 - sp(c.delay)) * 50}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 16,
            background: 'rgba(255,255,255,0.03)',
            border: `1px solid ${c.color}33`,
            borderRadius: 12,
            padding: '14px 28px',
          }}>
            <div style={{
              fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif',
              fontSize: 46,
              fontWeight: 800,
              color: c.color,
            }}>{c.label}</div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              fontWeight: 700,
              color: c.color,
              opacity: 0.7,
            }}>∞</div>
          </div>
        ))}
      </div>

      {/* Big COMPOUND text bottom */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: '50%',
        transform: `translateX(-50%) translateY(${(1 - bigReveal) * 40}px) translateY(${floatY}px)`,
        opacity: bigReveal,
        textAlign: 'center',
        textShadow: `0 0 ${80 * glowPulse}px rgba(233,215,0,${0.2 * glowPulse})`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, Inter, monospace',
          fontSize: 70,
          fontWeight: 900,
          color: '#e9d700',
          letterSpacing: 8,
          textTransform: 'uppercase',
        }}>COMPOUND EVERYTHING</div>
      </div>

      {/* Stick Figure */}
      <div style={{
        position: 'absolute',
        left: 880,
        bottom: 60,
        transform: `translateY(${breathe + floatY * 0.4}px)`,
        opacity: sp(70),
      }}>
        <svg width="130" height="215" viewBox="0 0 160 280">
          <ellipse cx="80" cy="140" rx="68" ry="120" fill="#e9d700" opacity={0.06 * glowPulse} />
          <line x1="80" y1="108" x2="80" y2="200" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="130" x2="25" y2="95" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="130" x2="135" y2="95" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="52" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="200" x2="108" y2="258" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          <circle cx="80" cy="82" r="28" fill="#2D2D2D" stroke="#e9d700" strokeWidth="3"
            transform={`rotate(${headNod}, 80, 82)`} />
          <g transform={`rotate(${headNod}, 80, 82)`}>
            <ellipse cx="68" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <ellipse cx="92" cy="83" rx="10" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.2" />
            <line x1="78" y1="83" x2="82" y2="83" stroke="#FFC107" strokeWidth="2" />
            <line x1="58" y1="83" x2="52" y2="81" stroke="#FFC107" strokeWidth="2" />
            <line x1="102" y1="83" x2="108" y2="81" stroke="#FFC107" strokeWidth="2" />
          </g>
          <path d="M 68 90 Q 80 100 92 90" fill="none" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round"
            transform={`rotate(${headNod}, 80, 82)`} />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.78) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene10Compound;
```

