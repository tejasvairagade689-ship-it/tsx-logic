```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01TwoEconomies',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01TwoEconomies: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { extrapolateRight: 'clamp' });

  // Split reveal
  const splitProg = interpolate(sp(20), [0, 1], [0, 1]);
  const leftX = interpolate(sp(30), [0, 1], [-200, 0]);
  const rightX = interpolate(sp(45), [0, 1], [200, 0]);

  // "TWO" big number pop
  const twoScale = sp(60, { stiffness: 90 });
  const subText = interpolate(sp(95), [0, 1], [0, 1]);

  // Stat reveal
  const statReveal = interpolate(sp(150), [0, 1], [0, 1]);
  const barFill = interpolate(frame, [165, 220], [0, 50], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEFABD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 40%, rgba(243,117,194,0.10), transparent 60%), linear-gradient(135deg, #EEFABD 0%, #E2F3A8 100%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Animated shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 1);
        const drift = Math.sin(frame * 0.012 + i) * 30;
        const rot = frame * 0.1 * (i % 2 === 0 ? 1 : -1);
        const size = 300 + r * 350;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: `${10 + r * 70}%`,
              top: `${5 + seededRandom(i + 9) * 70}%`,
              width: size,
              height: size,
              borderRadius: i % 2 === 0 ? '50%' : '20px',
              border: `2px solid #263B6A`,
              opacity: 0.05,
              transform: `translateY(${drift}px) rotate(${rot}deg)`,
            }}
          />
        );
      })}

      {/* Motion texture: vertical grid lines */}
      {Array.from({ length: 14 }).map((_, i) => (
        <div
          key={`g${i}`}
          style={{
            position: 'absolute',
            left: `${(i / 14) * 100}%`,
            top: 0,
            width: 1,
            height: '100%',
            background: '#263B6A',
            opacity: 0.04,
            transform: `translateY(${Math.sin(frame * 0.02 + i) * 8}px)`,
          }}
        />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 50);
        const y = (frame * (0.4 + r) + r * 1080) % 1120 - 20;
        return (
          <div
            key={`p${i}`}
            style={{
              position: 'absolute',
              left: `${r * 100}%`,
              top: y,
              width: 4 + r * 5,
              height: 4 + r * 5,
              borderRadius: '50%',
              background: i % 2 === 0 ? '#F375C2' : '#263B6A',
              opacity: 0.25,
            }}
          />
        );
      })}

      {/* Center divider line */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '12%',
          width: 3,
          height: `${splitProg * 64}%`,
          background: '#F375C2',
          transform: 'translateX(-50%)',
          boxShadow: '0 0 20px rgba(243,117,194,0.5)',
        }}
      />

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Left label */}
        <div
          style={{
            position: 'absolute',
            left: '8%',
            top: '20%',
            transform: `translateX(${leftX}px)`,
            opacity: splitProg,
          }}
        >
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 40, fontWeight: 600, color: '#263B6A' }}>ECONOMY</div>
          <div style={{ fontFamily: 'Mont Heavy', fontSize: 119, fontWeight: 900, color: '#263B6A', lineHeight: 1 }}>ONE</div>
        </div>

        {/* Right label */}
        <div
          style={{
            position: 'absolute',
            right: '8%',
            top: '20%',
            textAlign: 'right',
            transform: `translateX(${rightX}px)`,
            opacity: splitProg,
          }}
        >
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 40, fontWeight: 600, color: '#F375C2' }}>ECONOMY</div>
          <div style={{ fontFamily: 'Mont Heavy', fontSize: 119, fontWeight: 900, color: '#F375C2', lineHeight: 1 }}>TWO</div>
        </div>

        {/* Big TWO statement */}
        <div
          style={{
            position: 'absolute',
            width: '100%',
            top: '44%',
            textAlign: 'center',
            transform: `scale(${twoScale})`,
          }}
        >
          <div style={{ fontFamily: 'Nexa Bold', fontSize: 187, fontWeight: 900, color: '#263B6A', letterSpacing: -4 }}>
            NOT 1 — BUT 2
          </div>
        </div>

        {/* Sub statement */}
        <div
          style={{
            position: 'absolute',
            width: '100%',
            top: '66%',
            textAlign: 'center',
            opacity: subText,
            transform: `translateY(${interpolate(subText, [0, 1], [25, 0])}px)`,
          }}
        >
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 51, fontWeight: 600, color: '#263B6A' }}>
            Two Americas. Two realities.
          </div>
        </div>

        {/* Stat: top 10% half of spending */}
        <div
          style={{
            position: 'absolute',
            width: '70%',
            left: '15%',
            top: '76%',
            opacity: statReveal,
            transform: `translateY(${interpolate(statReveal, [0, 1], [30, 0])}px)`,
          }}
        >
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 600, color: '#263B6A', marginBottom: 12 }}>
            Top 10% of households = <span style={{ color: '#F375C2', fontWeight: 700 }}>~50% of all consumer spending</span>
          </div>
          {/* Spending bar */}
          <div style={{ width: '100%', height: 34, borderRadius: 17, background: 'rgba(38,59,106,0.12)', overflow: 'hidden' }}>
            <div
              style={{
                width: `${barFill}%`,
                height: '100%',
                borderRadius: 17,
                background: 'linear-gradient(90deg, #F375C2, #263B6A)',
              }}
            />
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          boxShadow: 'inset 0 0 320px rgba(38,59,106,0.30)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#263B6A',
          opacity: 0.4,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene01TwoEconomies;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02TheSplit',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02TheSplit: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -40], { extrapolateRight: 'clamp' });

  // Top group (wealthy items) reveal
  const topReveal = sp(20);
  // Bottom group (struggling) reveal
  const botReveal = sp(110);

  // Split word slam
  const splitSlam = sp(420 / fps > 0 ? 250 : 250, { stiffness: 80 });
  const shake = frame > 250 && frame < 268 ? Math.sin(frame * 3) * 6 : 0;

  const luxItems = ['Vacations', 'Kitchen upgrades', 'Second homes'];
  const cutItems = ['Cutting groceries', 'Skipping doctors', 'Praying for the car'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEFABD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, #F2F6C0 0%, #EEFABD 50%, #E0EFA0 100%)',
          transform: `translateX(${camX}px)`,
        }}
      />

      {/* Animated shape layers — triangles */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 3);
        const drift = Math.cos(frame * 0.01 + i) * 25;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: `${15 + r * 60}%`,
              top: `${10 + seededRandom(i + 7) * 60}%`,
              width: 0,
              height: 0,
              borderLeft: '150px solid transparent',
              borderRight: '150px solid transparent',
              borderBottom: `260px solid #F375C2`,
              opacity: 0.05,
              transform: `translateY(${drift}px) rotate(${frame * 0.06 * (i % 2 ? 1 : -1)}deg)`,
            }}
          />
        );
      })}

      {/* Motion texture: dotted rows */}
      {Array.from({ length: 10 }).map((_, i) => (
        <div
          key={`d${i}`}
          style={{
            position: 'absolute',
            top: `${(i / 10) * 100}%`,
            left: 0,
            width: '100%',
            height: 1,
            background: 'repeating-linear-gradient(90deg, #263B6A 0 4px, transparent 4px 14px)',
            opacity: 0.05,
          }}
        />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 60);
        const y = (frame * (0.3 + r) + r * 1080) % 1120 - 20;
        return (
          <div
            key={`p${i}`}
            style={{
              position: 'absolute',
              left: `${r * 100}%`,
              top: y,
              width: 5,
              height: 5,
              borderRadius: '50%',
              background: '#263B6A',
              opacity: 0.2,
            }}
          />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX + shake}px)` }}>
        {/* TOP: the 10% */}
        <div style={{ position: 'absolute', left: '7%', top: '6%', opacity: topReveal }}>
          <div style={{ fontFamily: 'Mont Heavy', fontSize: 68, fontWeight: 900, color: '#F375C2' }}>THE TOP 10%</div>
        </div>
        {luxItems.map((t, i) => {
          const it = sp(30 + i * 18);
          return (
            <div
              key={t}
              style={{
                position: 'absolute',
                left: '8%',
                top: `${17 + i * 8}%`,
                opacity: it,
                transform: `translateX(${interpolate(it, [0, 1], [-60, 0])}px)`,
                display: 'flex',
                alignItems: 'center',
                gap: 18,
              }}
            >
              <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
                <path d="M5 12l5 5L20 7" stroke="#263B6A" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
              <span style={{ fontFamily: 'Inter, Poppins', fontSize: 51, fontWeight: 600, color: '#263B6A' }}>{t}</span>
            </div>
          );
        })}

        {/* Divider */}
        <div
          style={{
            position: 'absolute',
            left: '5%',
            top: '47%',
            width: '90%',
            height: 2,
            background: '#263B6A',
            opacity: 0.3,
          }}
        />

        {/* BOTTOM: the 90% */}
        <div style={{ position: 'absolute', right: '7%', top: '50%', textAlign: 'right', opacity: botReveal }}>
          <div style={{ fontFamily: 'Mont Heavy', fontSize: 68, fontWeight: 900, color: '#263B6A' }}>THE OTHER 90%</div>
        </div>
        {cutItems.map((t, i) => {
          const it = sp(120 + i * 18);
          return (
            <div
              key={t}
              style={{
                position: 'absolute',
                right: '8%',
                top: `${61 + i * 8}%`,
                opacity: it,
                transform: `translateX(${interpolate(it, [0, 1], [60, 0])}px)`,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'flex-end',
                gap: 18,
              }}
            >
              <span style={{ fontFamily: 'Inter, Poppins', fontSize: 51, fontWeight: 600, color: '#263B6A' }}>{t}</span>
              <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
                <path d="M19 12H5M12 5l-7 7 7 7" stroke="#F375C2" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
            </div>
          );
        })}

        {/* SPLIT slam word */}
        <div
          style={{
            position: 'absolute',
            width: '100%',
            top: '40%',
            textAlign: 'center',
            transform: `scale(${splitSlam})`,
            opacity: splitSlam,
          }}
        >
          <div
            style={{
              fontFamily: 'Nexa Bold',
              fontSize: 153,
              fontWeight: 900,
              color: '#F375C2',
              letterSpacing: 6,
              textShadow: '0 6px 0 rgba(38,59,106,0.25)',
            }}
          >
            A CLEAN SPLIT
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(38,59,106,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#263B6A',
          opacity: 0.4,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene02TheSplit;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03KShape',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03KShape: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom-out
  const camScale = interpolate(frame, [0, durationInFrames], [1.08, 1], { extrapolateRight: 'clamp' });

  // K letter draw
  const kStem = interpolate(sp(20), [0, 1], [0, 1]);
  const upArm = interpolate(frame, [60, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const downArm = interpolate(frame, [90, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Labels
  const title = sp(170);
  const nameTag = sp(260);

  // SVG line dash lengths
  const armLen = 380;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEFABD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, #F4F8C4 0%, #EEFABD 55%, #DDEC9A 100%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Animated shape layers — concentric circles breathing */}
      {[0, 1, 2].map((i) => {
        const breathe = 1 + Math.sin(frame * 0.03 + i) * 0.04;
        const size = 500 + i * 300;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: '50%',
              top: '50%',
              width: size,
              height: size,
              marginLeft: -size / 2,
              marginTop: -size / 2,
              borderRadius: '50%',
              border: '2px solid #263B6A',
              opacity: 0.05,
              transform: `scale(${breathe})`,
            }}
          />
        );
      })}

      {/* Motion texture: radial ticks */}
      {Array.from({ length: 24 }).map((_, i) => {
        const ang = (i / 24) * Math.PI * 2;
        return (
          <div
            key={`t${i}`}
            style={{
              position: 'absolute',
              left: '50%',
              top: '50%',
              width: 2,
              height: 40,
              background: '#F375C2',
              opacity: 0.06,
              transform: `rotate(${ang}rad) translateY(-460px)`,
              transformOrigin: 'center',
            }}
          />
        );
      })}

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 70);
        const y = (frame * (0.35 + r) + r * 1080) % 1120 - 20;
        return (
          <div
            key={`p${i}`}
            style={{
              position: 'absolute',
              left: `${r * 100}%`,
              top: y,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: i % 2 ? '#F375C2' : '#263B6A',
              opacity: 0.25,
            }}
          />
        );
      })}

      {/* Main content: The K graph */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {/* Vertical stem */}
          <line
            x1="760" y1="280" x2="760" y2="780"
            stroke="#263B6A" strokeWidth="14" strokeLinecap="round"
            strokeDasharray="500" strokeDashoffset={500 - kStem * 500}
          />
          {/* Up arm (pink, rising) */}
          <line
            x1="760" y1="530" x2="1160" y2="250"
            stroke="#F375C2" strokeWidth="14" strokeLinecap="round"
            strokeDasharray={armLen + 90} strokeDashoffset={(armLen + 90) - upArm * (armLen + 90)}
          />
          {/* Down arm (navy, falling) */}
          <line
            x1="760" y1="530" x2="1160" y2="810"
            stroke="#263B6A" strokeWidth="14" strokeLinecap="round"
            strokeDasharray={armLen + 90} strokeDashoffset={(armLen + 90) - downArm * (armLen + 90)}
          />
          {/* Up arrow head */}
          {upArm > 0.9 && (
            <path d="M1160 250 l-30 6 l18 24 z" fill="#F375C2" />
          )}
          {/* Down arrow head */}
          {downArm > 0.9 && (
            <path d="M1160 810 l-30 -6 l18 -24 z" fill="#263B6A" />
          )}
        </svg>

        {/* Up label */}
        <div style={{ position: 'absolute', left: '62%', top: '20%', opacity: upArm }}>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 45, fontWeight: 700, color: '#F375C2' }}>Going UP</div>
        </div>
        {/* Down label */}
        <div style={{ position: 'absolute', left: '62%', top: '74%', opacity: downArm }}>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 45, fontWeight: 700, color: '#263B6A' }}>Going DOWN</div>
        </div>

        {/* Title */}
        <div
          style={{
            position: 'absolute',
            left: '6%',
            top: '8%',
            opacity: title,
            transform: `translateY(${interpolate(title, [0, 1], [-30, 0])}px)`,
          }}
        >
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 600, color: '#263B6A' }}>Economists call it</div>
          <div style={{ fontFamily: 'Nexa Bold', fontSize: 153, fontWeight: 900, color: '#263B6A', lineHeight: 1, letterSpacing: -2 }}>
            THE K-SHAPED
          </div>
          <div style={{ fontFamily: 'Nexa Bold', fontSize: 153, fontWeight: 900, color: '#F375C2', lineHeight: 1, letterSpacing: -2 }}>
            ECONOMY
          </div>
        </div>

        {/* Name tag */}
        <div
          style={{
            position: 'absolute',
            left: '6%',
            bottom: '8%',
            opacity: nameTag,
            transform: `translateX(${interpolate(nameTag, [0, 1], [-40, 0])}px)`,
            background: '#263B6A',
            padding: '14px 30px',
            borderRadius: 12,
          }}
        >
          <span style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700, color: '#EEFABD' }}>
            I'm Kunal — no sugarcoating.
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(38,59,106,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#263B6A',
          opacity: 0.4,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene03KShape;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04Struggling',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04Struggling: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counting number 0 -> 34
  const counter = Math.round(interpolate(frame, [25, 90], [0, 34], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-1, 1], { extrapolateRight: 'clamp' });

  // Comparison bars (2021 vs now)
  const bar2021 = interpolate(frame, [150, 200], [0, 22], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const barNow = interpolate(frame, [180, 240], [0, 34], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const compReveal = sp(140);

  const subline = sp(290);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEFABD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(120deg, #EEFABD 0%, #E8F4B0 60%, #F2E5F4 100%)',
          transform: `rotate(${camRot * 0.3}deg) scale(1.05)`,
        }}
      />

      {/* Animated shape layers — rounded rectangles */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 11);
        const drift = Math.sin(frame * 0.015 + i) * 28;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: `${5 + r * 75}%`,
              top: `${5 + seededRandom(i + 17) * 70}%`,
              width: 260 + r * 280,
              height: 180 + r * 180,
              borderRadius: 28,
              border: '2px solid #F375C2',
              opacity: 0.05,
              transform: `translateY(${drift}px) rotate(${frame * 0.05 * (i % 2 ? -1 : 1)}deg)`,
            }}
          />
        );
      })}

      {/* Motion texture: water-line waves at bottom */}
      <svg width="1920" height="200" style={{ position: 'absolute', bottom: 0, left: 0, opacity: 0.12 }}>
        <path
          d={`M0 ${100 + Math.sin(frame * 0.05) * 12} Q 480 ${60 + Math.sin(frame * 0.05) * 12} 960 ${100 + Math.sin(frame * 0.05) * 12} T 1920 ${100 + Math.sin(frame * 0.05) * 12} V200 H0 Z`}
          fill="#263B6A"
        />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 80);
        const y = (frame * (0.3 + r) + r * 1080) % 1120 - 20;
        return (
          <div
            key={`p${i}`}
            style={{
              position: 'absolute',
              left: `${r * 100}%`,
              top: y,
              width: 5,
              height: 5,
              borderRadius: '50%',
              background: '#263B6A',
              opacity: 0.2,
            }}
          />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `rotate(${camRot * 0.15}deg)` }}>
        {/* Giant counter */}
        <div style={{ position: 'absolute', left: '6%', top: '12%' }}>
          <div style={{ fontFamily: 'Nexa Bold', fontSize: 255, fontWeight: 900, color: '#263B6A', lineHeight: 0.9 }}>
            {counter}%
          </div>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 51, fontWeight: 600, color: '#263B6A', marginTop: 8 }}>
            of adults feel <span style={{ color: '#F375C2', fontWeight: 700 }}>struggling or in crisis</span>
          </div>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 500, color: '#263B6A', opacity: 0.75 }}>
            ≈ 88 million people
          </div>
        </div>

        {/* Comparison bars */}
        <div
          style={{
            position: 'absolute',
            right: '7%',
            top: '20%',
            opacity: compReveal,
            display: 'flex',
            gap: 60,
            alignItems: 'flex-end',
          }}
        >
          {/* 2021 */}
          <div style={{ textAlign: 'center' }}>
            <div
              style={{
                width: 130,
                height: bar2021 * 11,
                background: 'rgba(38,59,106,0.4)',
                borderRadius: '14px 14px 0 0',
              }}
            />
            <div style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 700, color: '#263B6A', marginTop: 14 }}>2021</div>
            <div style={{ fontFamily: 'Inter', fontSize: 32, color: '#263B6A' }}>22%</div>
          </div>
          {/* NOW */}
          <div style={{ textAlign: 'center' }}>
            <div
              style={{
                width: 130,
                height: barNow * 11,
                background: 'linear-gradient(180deg, #F375C2, #263B6A)',
                borderRadius: '14px 14px 0 0',
              }}
            />
            <div style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 700, color: '#F375C2', marginTop: 14 }}>NOW</div>
            <div style={{ fontFamily: 'Inter', fontSize: 32, color: '#F375C2' }}>34%</div>
          </div>
        </div>

        {/* Subline */}
        <div
          style={{
            position: 'absolute',
            width: '88%',
            left: '6%',
            bottom: '12%',
            opacity: subline,
            transform: `translateY(${interpolate(subline, [0, 1], [25, 0])}px)`,
          }}
        >
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 600, color: '#263B6A' }}>
            In under 5 years — tens of millions slipped underwater.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(38,59,106,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#263B6A',
          opacity: 0.4,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene04Struggling;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05PaycheckToPaycheck',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05PaycheckToPaycheck: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateRight: 'clamp' });

  // 54% headline
  const head = sp(20);
  // 100 dot grid fill (54 active)
  const gridReveal = sp(70);
  const activeDots = Math.round(interpolate(frame, [80, 180], [0, 54], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // tags
  const tag1 = sp(190);
  const tag2 = sp(215);

  // $1000 teaser
  const teaser = sp(290);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEFABD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 30%, #F4F8C6, #EEFABD 60%, #E2EF9E 100%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Animated shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 21);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              right: `${5 + r * 50}%`,
              top: `${10 + seededRandom(i + 23) * 60}%`,
              width: 320 + r * 250,
              height: 320 + r * 250,
              borderRadius: '50%',
              border: '2px solid #263B6A',
              opacity: 0.05,
              transform: `scale(${1 + Math.sin(frame * 0.025 + i) * 0.05})`,
            }}
          />
        );
      })}

      {/* Motion texture: diagonal lines */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div
          key={`l${i}`}
          style={{
            position: 'absolute',
            left: `${i * 9 - 20}%`,
            top: 0,
            width: 1,
            height: '140%',
            background: '#F375C2',
            opacity: 0.05,
            transform: 'rotate(20deg)',
            transformOrigin: 'top',
          }}
        />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 90);
        const y = (frame * (0.3 + r) + r * 1080) % 1120 - 20;
        return (
          <div
            key={`p${i}`}
            style={{
              position: 'absolute',
              left: `${r * 100}%`,
              top: y,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: '#263B6A',
              opacity: 0.2,
            }}
          />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            left: '6%',
            top: '10%',
            opacity: head,
            transform: `translateX(${interpolate(head, [0, 1], [-50, 0])}px)`,
          }}
        >
          <div style={{ fontFamily: 'Nexa Bold', fontSize: 221, fontWeight: 900, color: '#263B6A', lineHeight: 0.9 }}>54%</div>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 51, fontWeight: 600, color: '#263B6A' }}>
            live <span style={{ color: '#F375C2', fontWeight: 700 }}>paycheck to paycheck</span>
          </div>
        </div>

        {/* 100-dot grid */}
        <div
          style={{
            position: 'absolute',
            right: '6%',
            top: '14%',
            width: 520,
            display: 'flex',
            flexWrap: 'wrap',
            gap: 14,
            opacity: gridReveal,
          }}
        >
          {Array.from({ length: 100 }).map((_, i) => (
            <div
              key={i}
              style={{
                width: 38,
                height: 38,
                borderRadius: 8,
                background: i < activeDots ? '#F375C2' : 'rgba(38,59,106,0.15)',
              }}
            />
          ))}
        </div>

        {/* Tags */}
        <div
          style={{
            position: 'absolute',
            left: '6%',
            top: '60%',
            opacity: tag1,
            transform: `translateY(${interpolate(tag1, [0, 1], [20, 0])}px)`,
            background: '#263B6A',
            padding: '12px 26px',
            borderRadius: 30,
            display: 'inline-block',
          }}
        >
          <span style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 600, color: '#EEFABD' }}>Even six-figure earners</span>
        </div>
        <div
          style={{
            position: 'absolute',
            left: '6%',
            top: '70%',
            opacity: tag2,
            transform: `translateY(${interpolate(tag2, [0, 1], [20, 0])}px)`,
            background: '#F375C2',
            padding: '12px 26px',
            borderRadius: 30,
            display: 'inline-block',
          }}
        >
          <span style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 600, color: '#EEFABD' }}>Even "safe" suburbs</span>
        </div>

        {/* $1000 teaser */}
        <div
          style={{
            position: 'absolute',
            width: '100%',
            bottom: '8%',
            textAlign: 'center',
            opacity: teaser,
            transform: `scale(${interpolate(teaser, [0, 1], [0.85, 1])})`,
          }}
        >
          <div style={{ fontFamily: 'Mont Heavy', fontSize: 68, fontWeight: 900, color: '#263B6A' }}>
            And 60% can't find <span style={{ color: '#F375C2' }}>$1,000…</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(38,59,106,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#263B6A',
          opacity: 0.4,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene05PaycheckToPaycheck;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06ThousandDollar',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06ThousandDollar: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camX = interpolate(frame, [0, durationInFrames], [-30, 30], { extrapolateRight: 'clamp' });

  // $1000 slam
  const slam = sp(20, { stiffness: 80 });
  const shake = frame > 20 && frame < 38 ? Math.sin(frame * 2.5) * 5 : 0;

  // 3 cost cards
  const cards = [
    { t: 'Transmission repair', icon: 'gear' },
    { t: 'Emergency root canal', icon: 'tooth' },
    { t: '2 nights in hospital', icon: 'cross' },
  ];

  // 3 of 5 figures
  const figReveal = sp(230);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEFABD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #EEFABD 0%, #F1F3BB 50%, #EAD7EC 100%)',
          transform: `translateX(${camX}px) scale(1.05)`,
        }}
      />

      {/* Animated shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 31);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: `${10 + r * 70}%`,
              top: `${5 + seededRandom(i + 33) * 65}%`,
              width: 280 + r * 260,
              height: 280 + r * 260,
              borderRadius: i % 2 ? '24px' : '50%',
              border: '2px solid #F375C2',
              opacity: 0.05,
              transform: `rotate(${frame * 0.07 * (i % 2 ? 1 : -1)}deg)`,
            }}
          />
        );
      })}

      {/* Motion texture: grid */}
      {Array.from({ length: 18 }).map((_, i) => (
        <div
          key={`g${i}`}
          style={{
            position: 'absolute',
            left: `${(i / 18) * 100}%`,
            top: 0,
            width: 1,
            height: '100%',
            background: '#263B6A',
            opacity: 0.035,
          }}
        />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 100);
        const y = (frame * (0.3 + r) + r * 1080) % 1120 - 20;
        return (
          <div
            key={`p${i}`}
            style={{
              position: 'absolute',
              left: `${r * 100}%`,
              top: y,
              width: 5,
              height: 5,
              borderRadius: '50%',
              background: '#F375C2',
              opacity: 0.2,
            }}
          />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX + shake}px)` }}>
        {/* $1000 */}
        <div
          style={{
            position: 'absolute',
            width: '100%',
            top: '8%',
            textAlign: 'center',
            transform: `scale(${slam})`,
          }}
        >
          <div style={{ fontFamily: 'Nexa Bold', fontSize: 238, fontWeight: 900, color: '#263B6A', letterSpacing: -4 }}>
            $1,000
          </div>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 600, color: '#F375C2' }}>
            One emergency away from debt
          </div>
        </div>

        {/* Cost cards */}
        <div
          style={{
            position: 'absolute',
            width: '100%',
            top: '46%',
            display: 'flex',
            justifyContent: 'center',
            gap: 40,
          }}
        >
          {cards.map((c, i) => {
            const cr = sp(80 + i * 22);
            return (
              <div
                key={c.t}
                style={{
                  opacity: cr,
                  transform: `translateY(${interpolate(cr, [0, 1], [40, 0])}px)`,
                  background: 'rgba(255,255,255,0.5)',
                  border: '2px solid #263B6A',
                  borderRadius: 20,
                  width: 380,
                  padding: '30px 24px',
                  textAlign: 'center',
                }}
              >
                <svg width="64" height="64" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 12 }}>
                  {c.icon === 'gear' && (
                    <path d="M12 8a4 4 0 100 8 4 4 0 000-8zM12 2v3M12 19v3M2 12h3M19 12h3M5 5l2 2M17 17l2 2M5 19l2-2M17 7l2-2"
                      stroke="#F375C2" strokeWidth="2" strokeLinecap="round" />
                  )}
                  {c.icon === 'tooth' && (
                    <path d="M12 3c-3 0-5 2-5 5 0 4 1 11 3 11 1 0 1-3 2-3s1 3 2 3c2 0 3-7 3-11 0-3-2-5-5-5z"
                      stroke="#F375C2" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
                  )}
                  {c.icon === 'cross' && (
                    <path d="M12 5v14M5 12h14" stroke="#F375C2" strokeWidth="3" strokeLinecap="round" />
                  )}
                </svg>
                <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 600, color: '#263B6A' }}>{c.t}</div>
              </div>
            );
          })}
        </div>

        {/* 3 of 5 figures */}
        <div
          style={{
            position: 'absolute',
            width: '100%',
            bottom: '8%',
            textAlign: 'center',
            opacity: figReveal,
          }}
        >
          <div style={{ display: 'flex', justifyContent: 'center', gap: 24, marginBottom: 10 }}>
            {[0, 1, 2, 3, 4].map((i) => (
              <svg key={i} width="56" height="80" viewBox="0 0 24 32">
                <circle cx="12" cy="6" r="5" fill={i < 3 ? '#F375C2' : 'rgba(38,59,106,0.25)'} />
                <rect x="6" y="13" width="12" height="16" rx="5" fill={i < 3 ? '#F375C2' : 'rgba(38,59,106,0.25)'} />
              </svg>
            ))}
          </div>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 42, fontWeight: 600, color: '#263B6A' }}>
            3 out of 5 adults — no savings cushion.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(38,59,106,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#263B6A',
          opacity: 0.4,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene06ThousandDollar;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07WhereYouStand',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07WhereYouStand: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1], { extrapolateRight: 'clamp' });

  // Question
  const q = sp(20);
  // comment bubble
  const bubble = sp(60);
  const bubbleFloat = Math.sin(frame * 0.05) * 8;

  // Two missed paychecks meter
  const meterReveal = sp(150);
  const meterFill = interpolate(frame, [165, 230], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const notAlone = sp(290);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEFABD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 70% 30%, #F2E0F2, transparent 50%), linear-gradient(135deg, #EEFABD, #E4F1A4)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Animated shape layers — speech bubble outlines */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 41);
        const drift = Math.sin(frame * 0.02 + i) * 26;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: `${5 + r * 75}%`,
              top: `${5 + seededRandom(i + 43) * 70}%`,
              width: 240 + r * 200,
              height: 200 + r * 160,
              borderRadius: '30px',
              border: '2px solid #263B6A',
              opacity: 0.05,
              transform: `translateY(${drift}px)`,
            }}
          />
        );
      })}

      {/* Motion texture: circles */}
      {Array.from({ length: 8 }).map((_, i) => (
        <div
          key={`c${i}`}
          style={{
            position: 'absolute',
            left: `${seededRandom(i + 200) * 100}%`,
            top: `${seededRandom(i + 210) * 100}%`,
            width: 70,
            height: 70,
            borderRadius: '50%',
            border: '1px solid #F375C2',
            opacity: 0.08,
            transform: `scale(${1 + Math.sin(frame * 0.04 + i) * 0.15})`,
          }}
        />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 110);
        const y = (frame * (0.3 + r) + r * 1080) % 1120 - 20;
        return (
          <div
            key={`p${i}`}
            style={{
              position: 'absolute',
              left: `${r * 100}%`,
              top: y,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: '#263B6A',
              opacity: 0.2,
            }}
          />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Big question */}
        <div
          style={{
            position: 'absolute',
            left: '6%',
            top: '14%',
            width: '60%',
            opacity: q,
            transform: `translateY(${interpolate(q, [0, 1], [-30, 0])}px)`,
          }}
        >
          <div style={{ fontFamily: 'Mont Heavy', fontSize: 119, fontWeight: 900, color: '#263B6A', lineHeight: 1 }}>
            WHERE DO YOU
          </div>
          <div style={{ fontFamily: 'Mont Heavy', fontSize: 119, fontWeight: 900, color: '#F375C2', lineHeight: 1 }}>
            STAND?
          </div>
        </div>

        {/* Comment bubble */}
        <div
          style={{
            position: 'absolute',
            right: '8%',
            top: '12%',
            opacity: bubble,
            transform: `translateY(${bubbleFloat}px) scale(${bubble})`,
          }}
        >
          <div
            style={{
              background: '#263B6A',
              padding: '24px 36px',
              borderRadius: 24,
              maxWidth: 420,
            }}
          >
            <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 600, color: '#EEFABD' }}>
              💬 Drop it in the comments
            </div>
          </div>
        </div>

        {/* Two missed paychecks meter */}
        <div
          style={{
            position: 'absolute',
            left: '6%',
            top: '52%',
            width: '60%',
            opacity: meterReveal,
          }}
        >
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 42, fontWeight: 600, color: '#263B6A', marginBottom: 14 }}>
            Two missed paychecks from collapse
          </div>
          <div style={{ width: '100%', height: 40, borderRadius: 20, background: 'rgba(38,59,106,0.12)', overflow: 'hidden', position: 'relative' }}>
            <div
              style={{
                width: `${meterFill}%`,
                height: '100%',
                borderRadius: 20,
                background: 'linear-gradient(90deg, #F375C2, #263B6A)',
              }}
            />
          </div>
        </div>

        {/* Not alone */}
        <div
          style={{
            position: 'absolute',
            width: '100%',
            bottom: '9%',
            textAlign: 'center',
            opacity: notAlone,
            transform: `scale(${interpolate(notAlone, [0, 1], [0.9, 1])})`,
          }}
        >
          <div style={{ fontFamily: 'Nexa Bold', fontSize: 85, fontWeight: 900, color: '#F375C2' }}>
            You're NOT alone.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(38,59,106,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#263B6A',
          opacity: 0.4,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene07WhereYouStand;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08CableNews',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08CableNews: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateRight: 'clamp' });

  const headlines = ['S&P at fresh highs', 'Unemployment at lows', 'GDP growing'];

  // 71% wealth pie
  const pieReveal = sp(180);
  const pieAngle = interpolate(frame, [195, 260], [0, 71 * 3.6], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const headTitle = sp(20);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEFABD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(160deg, #EEFABD, #EFF2BC 55%, #E7D9EC 100%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Animated shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 51);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: `${5 + r * 70}%`,
              top: `${5 + seededRandom(i + 53) * 65}%`,
              width: 300 + r * 260,
              height: 300 + r * 260,
              borderRadius: '18px',
              border: '2px solid #263B6A',
              opacity: 0.05,
              transform: `rotate(${frame * 0.05 * (i % 2 ? 1 : -1)}deg)`,
            }}
          />
        );
      })}

      {/* Motion texture: news ticker bars */}
      {Array.from({ length: 5 }).map((_, i) => (
        <div
          key={`tk${i}`}
          style={{
            position: 'absolute',
            top: `${15 + i * 18}%`,
            left: `${-((frame * (1 + i * 0.3)) % 120)}%`,
            width: '200%',
            height: 2,
            background: 'repeating-linear-gradient(90deg, #F375C2 0 30px, transparent 30px 80px)',
            opacity: 0.06,
          }}
        />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 120);
        const y = (frame * (0.3 + r) + r * 1080) % 1120 - 20;
        return (
          <div
            key={`p${i}`}
            style={{
              position: 'absolute',
              left: `${r * 100}%`,
              top: y,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: '#263B6A',
              opacity: 0.2,
            }}
          />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            left: '6%',
            top: '7%',
            opacity: headTitle,
          }}
        >
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 600, color: '#263B6A' }}>Cable news shows you:</div>
          <div style={{ fontFamily: 'Mont Heavy', fontSize: 76, fontWeight: 900, color: '#263B6A' }}>"TECHNICALLY TRUE"</div>
        </div>

        {/* Headline cards (left) */}
        <div style={{ position: 'absolute', left: '6%', top: '30%' }}>
          {headlines.map((h, i) => {
            const hr = sp(40 + i * 25);
            return (
              <div
                key={h}
                style={{
                  opacity: hr,
                  transform: `translateX(${interpolate(hr, [0, 1], [-50, 0])}px)`,
                  marginBottom: 22,
                  display: 'flex',
                  alignItems: 'center',
                  gap: 18,
                  background: 'rgba(255,255,255,0.4)',
                  border: '2px solid #F375C2',
                  borderRadius: 14,
                  padding: '16px 26px',
                  width: 620,
                }}
              >
                <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                  <path d="M3 17l6-6 4 4 7-7" stroke="#F375C2" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
                  <path d="M16 8h5v5" stroke="#F375C2" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
                <span style={{ fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 600, color: '#263B6A' }}>{h}</span>
              </div>
            );
          })}
        </div>

        {/* 71% pie chart (right) */}
        <div
          style={{
            position: 'absolute',
            right: '8%',
            top: '32%',
            opacity: pieReveal,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              width: 400,
              height: 400,
              borderRadius: '50%',
              background: `conic-gradient(#F375C2 0deg ${pieAngle}deg, rgba(38,59,106,0.18) ${pieAngle}deg 360deg)`,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <div
              style={{
                width: 240,
                height: 240,
                borderRadius: '50%',
                background: '#EEFABD',
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
              }}
            >
              <div style={{ fontFamily: 'Nexa Bold', fontSize: 102, fontWeight: 900, color: '#263B6A', lineHeight: 1 }}>71%</div>
            </div>
          </div>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 600, color: '#263B6A', marginTop: 16 }}>
            of wealth held by top 20%
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(38,59,106,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#263B6A',
          opacity: 0.4,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene08CableNews;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09AveragesLie',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09AveragesLie: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camX = interpolate(frame, [0, durationInFrames], [0, 35], { extrapolateRight: 'clamp' });

  // Two growth bars: top 1% +25%, middle <10%
  const barsReveal = sp(20);
  const topBar = interpolate(frame, [40, 110], [0, 25], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const midBar = interpolate(frame, [70, 140], [0, 9], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // "AVERAGE vs MEDIAN" type
  const avgT = sp(170);
  const medT = sp(200);
  const realT = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEFABD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #EEFABD, #ECF1B4 55%, #EAD9EC 100%)',
          transform: `translateX(${camX}px) scale(1.05)`,
        }}
      />

      {/* Animated shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 61);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              right: `${5 + r * 60}%`,
              top: `${10 + seededRandom(i + 63) * 60}%`,
              width: 320 + r * 220,
              height: 320 + r * 220,
              borderRadius: '50%',
              border: '2px solid #F375C2',
              opacity: 0.05,
              transform: `scale(${1 + Math.sin(frame * 0.025 + i) * 0.05})`,
            }}
          />
        );
      })}

      {/* Motion texture: horizontal rule lines */}
      {Array.from({ length: 8 }).map((_, i) => (
        <div
          key={`h${i}`}
          style={{
            position: 'absolute',
            top: `${(i / 8) * 100}%`,
            left: 0,
            width: '100%',
            height: 1,
            background: '#263B6A',
            opacity: 0.04,
          }}
        />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 130);
        const y = (frame * (0.3 + r) + r * 1080) % 1120 - 20;
        return (
          <div
            key={`p${i}`}
            style={{
              position: 'absolute',
              left: `${r * 100}%`,
              top: y,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: '#263B6A',
              opacity: 0.2,
            }}
          />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* Title */}
        <div style={{ position: 'absolute', left: '6%', top: '7%' }}>
          <div style={{ fontFamily: 'Nexa Bold', fontSize: 110, fontWeight: 900, color: '#263B6A', lineHeight: 1 }}>
            THE AVERAGES
          </div>
          <div style={{ fontFamily: 'Nexa Bold', fontSize: 110, fontWeight: 900, color: '#F375C2', lineHeight: 1 }}>
            LIE TO YOU
          </div>
        </div>

        {/* Growth bars */}
        <div
          style={{
            position: 'absolute',
            left: '6%',
            top: '40%',
            opacity: barsReveal,
            display: 'flex',
            gap: 80,
            alignItems: 'flex-end',
          }}
        >
          {/* Top 1% */}
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'Inter', fontSize: 40, fontWeight: 700, color: '#F375C2', marginBottom: 8 }}>+{Math.round(topBar)}%</div>
            <div
              style={{
                width: 150,
                height: topBar * 14,
                background: 'linear-gradient(180deg, #F375C2, #263B6A)',
                borderRadius: '14px 14px 0 0',
              }}
            />
            <div style={{ fontFamily: 'Inter', fontSize: 34, fontWeight: 600, color: '#263B6A', marginTop: 14 }}>Top 1%</div>
          </div>
          {/* Middle */}
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'Inter', fontSize: 40, fontWeight: 700, color: '#263B6A', marginBottom: 8 }}>+{Math.round(midBar)}%</div>
            <div
              style={{
                width: 150,
                height: midBar * 14,
                background: 'rgba(38,59,106,0.35)',
                borderRadius: '14px 14px 0 0',
              }}
            />
            <div style={{ fontFamily: 'Inter', fontSize: 34, fontWeight: 600, color: '#263B6A', marginTop: 14 }}>Middle class</div>
          </div>
        </div>

        {/* Average / Median / Reality */}
        <div style={{ position: 'absolute', right: '7%', top: '38%', width: 640 }}>
          <div
            style={{
              opacity: avgT,
              transform: `translateX(${interpolate(avgT, [0, 1], [40, 0])}px)`,
              marginBottom: 20,
            }}
          >
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 40, fontWeight: 700, color: '#263B6A' }}>
              AVERAGE → "everyone got a raise"
            </div>
          </div>
          <div
            style={{
              opacity: medT,
              transform: `translateX(${interpolate(medT, [0, 1], [40, 0])}px)`,
              marginBottom: 20,
            }}
          >
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 40, fontWeight: 700, color: '#263B6A' }}>
              MEDIAN → "treading water"
            </div>
          </div>
          <div
            style={{
              opacity: realT,
              transform: `translateX(${interpolate(realT, [0, 1], [40, 0])}px)`,
              background: '#F375C2',
              padding: '16px 24px',
              borderRadius: 14,
            }}
          >
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 40, fontWeight: 700, color: '#EEFABD' }}>
              REALITY → many are drowning
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(38,59,106,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#263B6A',
          opacity: 0.4,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene09AveragesLie;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10WideningGap',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10WideningGap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.07], { extrapolateRight: 'clamp' });

  // Diverging arms animation
  const gap = interpolate(frame, [20, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const upY = interpolate(gap, [0, 1], [540, 230]);
  const downY = interpolate(gap, [0, 1], [540, 850]);

  // Buffett quote
  const quoteReveal = sp(220);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEFABD', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 45%, #F4F8C4, #EEFABD 55%, #DCEB98 100%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Animated shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 71);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: `${10 + r * 65}%`,
              top: `${5 + seededRandom(i + 73) * 60}%`,
              width: 320 + r * 240,
              height: 320 + r * 240,
              borderRadius: i % 2 ? '24px' : '50%',
              border: '2px solid #263B6A',
              opacity: 0.05,
              transform: `rotate(${frame * 0.05 * (i % 2 ? -1 : 1)}deg)`,
            }}
          />
        );
      })}

      {/* Motion texture: vertical ticks */}
      {Array.from({ length: 20 }).map((_, i) => (
        <div
          key={`v${i}`}
          style={{
            position: 'absolute',
            left: `${(i / 20) * 100}%`,
            bottom: 0,
            width: 1,
            height: `${20 + Math.sin(frame * 0.04 + i) * 10}%`,
            background: '#F375C2',
            opacity: 0.05,
          }}
        />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 140);
        const y = (frame * (0.3 + r) + r * 1080) % 1120 - 20;
        return (
          <div
            key={`p${i}`}
            style={{
              position: 'absolute',
              left: `${r * 100}%`,
              top: y,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: '#263B6A',
              opacity: 0.2,
            }}
          />
        );
      })}

      {/* Main content: diverging arms */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
          {/* Up arm */}
          <line x1="500" y1="540" x2="1450" y2={upY} stroke="#F375C2" strokeWidth="14" strokeLinecap="round" />
          {/* Down arm */}
          <line x1="500" y1="540" x2="1450" y2={downY} stroke="#263B6A" strokeWidth="14" strokeLinecap="round" />
          {/* Gap shading */}
          <path d={`M1450 ${upY} L1450 ${downY} L500 540 Z`} fill="#F375C2" opacity={gap * 0.08} />
        </svg>

        {/* Gap label */}
        <div
          style={{
            position: 'absolute',
            right: '7%',
            top: '46%',
            opacity: gap,
          }}
        >
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 40, fontWeight: 600, color: '#263B6A' }}>The widening gap</div>
        </div>

        {/* Title */}
        <div style={{ position: 'absolute', left: '5%', top: '8%' }}>
          <div style={{ fontFamily: 'Mont Heavy', fontSize: 85, fontWeight: 900, color: '#263B6A' }}>
            THE GAP IS WIDENING
          </div>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 500, color: '#263B6A', opacity: 0.8 }}>
            Faster than any point in modern history
          </div>
        </div>

        {/* Buffett quote */}
        <div
          style={{
            position: 'absolute',
            width: '80%',
            left: '10%',
            bottom: '8%',
            textAlign: 'center',
            opacity: quoteReveal,
            transform: `translateY(${interpolate(quoteReveal, [0, 1], [25, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 44,
              fontWeight: 600,
              fontStyle: 'italic',
              color: '#263B6A',
              background: 'rgba(255,255,255,0.4)',
              border: '2px solid #F375C2',
              borderRadius: 18,
              padding: '24px 34px',
            }}
          >
            "Only when the tide goes out do you discover who's been swimming naked."
            <span style={{ display: 'block', fontStyle: 'normal', fontSize: 32, color: '#F375C2', marginTop: 10 }}>
              — Warren Buffett
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(38,59,106,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#263B6A',
          opacity: 0.4,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene10WideningGap;
```

