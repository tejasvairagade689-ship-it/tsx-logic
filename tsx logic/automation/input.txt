```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_ShockCall',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_ShockCall: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08]);

  // Phone ring shake
  const ringShake = frame < 90 ? Math.sin(frame * 0.8) * 4 * Math.max(0, 1 - frame / 90) : 0;

  // Bill numbers reveal
  const oldBillProg = sp(140);
  const newBillProg = sp(220);
  const arrowProg = sp(260);
  const shockFlash = interpolate(frame, [240, 260, 280], [0, 0.6, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Crack lines progress
  const crackProg = interpolate(frame, [280, 360], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Phone slide in
  const phoneSlide = sp(20);
  const phoneX = interpolate(phoneSlide, [0, 1], [-400, 0]);

  // Counter animation for old bill
  const oldBillNum = Math.round(interpolate(oldBillProg, [0, 1], [0, 2000]));
  const newBillNum = Math.round(interpolate(newBillProg, [0, 1], [0, 5500]));

  // Floating particles
  const particles = Array.from({ length: 25 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#303841', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 40%, rgba(255,109,31,0.18) 0%, rgba(48,56,65,0) 55%), radial-gradient(circle at 75% 70%, rgba(9,20,19,0.6) 0%, rgba(48,56,65,0) 60%)',
        }}
      />

      {/* Animated shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 900,
          height: 900,
          borderRadius: '50%',
          border: '2px solid #FF6D1F',
          opacity: 0.05,
          left: -200,
          top: -200,
          transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          border: '2px solid #FF6D1F',
          opacity: 0.04,
          right: -150,
          bottom: -150,
          transform: `rotate(${-frame * 0.2}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 400,
          height: 400,
          background: '#091413',
          opacity: 0.25,
          right: 200,
          top: 100,
          transform: `rotate(${45 + Math.sin(frame * 0.03) * 5}deg) scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
          borderRadius: 40,
        }}
      />

      {/* Grid texture */}
      <svg
        style={{ position: 'absolute', inset: 0, opacity: 0.07 }}
        width="100%"
        height="100%"
      >
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#FF6D1F" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid1)" />
      </svg>

      {/* Floating particles */}
      {particles.map((i) => {
        const seed = seededRandom(i + 1);
        const xPos = seed * 1920;
        const yPos = ((frame * (0.4 + seed * 0.6) + i * 80) % 1200) - 100;
        const size = 3 + seed * 5;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: xPos,
              top: yPos,
              width: size,
              height: size,
              borderRadius: '50%',
              background: '#FF6D1F',
              opacity: 0.35,
            }}
          />
        );
      })}

      {/* Main camera container */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Shock flash overlay */}
        <AbsoluteFill style={{ background: '#FF6D1F', opacity: shockFlash, mixBlendMode: 'screen' }} />

        {/* LEFT SIDE - Phone illustration */}
        <div
          style={{
            position: 'absolute',
            left: 180,
            top: 280,
            transform: `translateX(${phoneX}px) rotate(${ringShake}deg)`,
          }}
        >
          {/* Phone body */}
          <svg width="280" height="500" viewBox="0 0 280 500">
            <rect x="10" y="10" width="260" height="480" rx="35" fill="#091413" stroke="#FF6D1F" strokeWidth="3" />
            <rect x="30" y="50" width="220" height="380" rx="8" fill="#303841" />
            {/* Speaker */}
            <rect x="115" y="28" width="50" height="6" rx="3" fill="#FF6D1F" opacity="0.6" />
            {/* Home button */}
            <circle cx="140" cy="460" r="14" fill="none" stroke="#FF6D1F" strokeWidth="2" opacity="0.7" />
            {/* Screen content - call icon */}
            <circle cx="140" cy="200" r="50" fill="none" stroke="#FF6D1F" strokeWidth="4">
              <animate attributeName="r" values="50;55;50" dur="0.6s" repeatCount="indefinite" />
            </circle>
            <path
              d="M 115 200 Q 140 175 165 200 L 158 210 Q 140 192 122 210 Z"
              fill="#FF6D1F"
            />
            <text x="140" y="290" textAnchor="middle" fill="#F5F5F5" fontSize="22" fontFamily="Inter" fontWeight="600">
              रिश्तेदार
            </text>
            <text x="140" y="320" textAnchor="middle" fill="#9E9E9E" fontSize="16" fontFamily="Inter">
              incoming call...
            </text>
          </svg>

          {/* Ring waves */}
          {frame < 120 && [0, 1, 2].map((i) => {
            const waveProg = ((frame + i * 20) % 60) / 60;
            return (
              <div
                key={i}
                style={{
                  position: 'absolute',
                  left: 140 - 50 * (1 + waveProg * 2),
                  top: 200 - 50 * (1 + waveProg * 2),
                  width: 100 * (1 + waveProg * 2),
                  height: 100 * (1 + waveProg * 2),
                  borderRadius: '50%',
                  border: '2px solid #FF6D1F',
                  opacity: (1 - waveProg) * 0.5,
                }}
              />
            );
          })}
        </div>

        {/* RIGHT SIDE - Bill comparison */}
        <div
          style={{
            position: 'absolute',
            right: 160,
            top: 180,
            width: 760,
          }}
        >
          {/* Title */}
          <div
            style={{
              opacity: sp(80),
              transform: `translateY(${interpolate(sp(80), [0, 1], [-30, 0])}px)`,
              marginBottom: 40,
            }}
          >
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 76,
                color: '#F5F5F5',
                fontWeight: 700,
                letterSpacing: -1,
                lineHeight: 1,
              }}
            >
              बिजली का बिल
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 28,
                color: '#FF6D1F',
                fontWeight: 600,
                marginTop: 12,
                letterSpacing: 2,
              }}
            >
              REALITY CHECK
            </div>
          </div>

          {/* OLD BILL */}
          <div
            style={{
              opacity: sp(120),
              transform: `translateX(${interpolate(sp(120), [0, 1], [60, 0])}px)`,
              background: 'rgba(9,20,19,0.5)',
              border: '2px solid rgba(245,245,245,0.15)',
              borderRadius: 16,
              padding: '24px 36px',
              marginBottom: 24,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-between',
            }}
          >
            <div>
              <div style={{ fontFamily: 'Inter', fontSize: 24, color: '#9E9E9E', letterSpacing: 2 }}>BEFORE</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 32, color: '#F5F5F5', fontWeight: 500 }}>
                पहले
              </div>
            </div>
            <div style={{ display: 'flex', alignItems: 'baseline' }}>
              <span style={{ fontFamily: 'JetBrains Mono', fontSize: 50, color: '#9E9E9E', marginRight: 8 }}>₹</span>
              <span style={{ fontFamily: 'JetBrains Mono', fontSize: 110, color: '#F5F5F5', fontWeight: 700, lineHeight: 1 }}>
                {oldBillNum.toLocaleString('en-IN')}
              </span>
            </div>
          </div>

          {/* Arrow down */}
          <div
            style={{
              opacity: arrowProg,
              transform: `translateY(${interpolate(arrowProg, [0, 1], [-20, 0])}px) scale(${arrowProg})`,
              textAlign: 'center',
              margin: '8px 0',
            }}
          >
            <svg width="60" height="60" viewBox="0 0 60 60">
              <path d="M 30 5 L 30 50 M 15 35 L 30 50 L 45 35" stroke="#FF6D1F" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>

          {/* NEW BILL - SHOCK */}
          <div
            style={{
              opacity: sp(200),
              transform: `translateX(${interpolate(sp(200), [0, 1], [60, 0])}px) scale(${1 + Math.sin(Math.max(0, frame - 240) * 0.3) * 0.02})`,
              background: 'linear-gradient(135deg, rgba(255,109,31,0.25) 0%, rgba(9,20,19,0.7) 100%)',
              border: '3px solid #FF6D1F',
              borderRadius: 16,
              padding: '28px 36px',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-between',
              boxShadow: '0 0 60px rgba(255,109,31,0.4)',
            }}
          >
            <div>
              <div style={{ fontFamily: 'Inter', fontSize: 24, color: '#FF6D1F', letterSpacing: 2, fontWeight: 700 }}>NOW</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 32, color: '#F5F5F5', fontWeight: 600 }}>
                अब
              </div>
            </div>
            <div style={{ display: 'flex', alignItems: 'baseline' }}>
              <span style={{ fontFamily: 'JetBrains Mono', fontSize: 60, color: '#FF6D1F', marginRight: 8 }}>₹</span>
              <span style={{ fontFamily: 'JetBrains Mono', fontSize: 140, color: '#FF6D1F', fontWeight: 800, lineHeight: 1, textShadow: '0 0 30px rgba(255,109,31,0.5)' }}>
                {newBillNum.toLocaleString('en-IN')}
              </span>
            </div>
          </div>

          {/* Percentage tag */}
          <div
            style={{
              opacity: sp(290),
              transform: `translateY(${interpolate(sp(290), [0, 1], [20, 0])}px)`,
              marginTop: 30,
              display: 'flex',
              justifyContent: 'flex-end',
              alignItems: 'center',
              gap: 16,
            }}
          >
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 36,
                color: '#F5F5F5',
                fontWeight: 500,
              }}
            >
              बढ़ोतरी
            </div>
            <div
              style={{
                background: '#FF6D1F',
                color: '#091413',
                padding: '12px 28px',
                borderRadius: 8,
                fontFamily: 'JetBrains Mono',
                fontSize: 48,
                fontWeight: 800,
              }}
            >
              +175%
            </div>
          </div>
        </div>

        {/* Crack lines across screen for impact */}
        <svg
          style={{ position: 'absolute', inset: 0, pointerEvents: 'none', opacity: crackProg * 0.25 }}
          width="1920"
          height="1080"
        >
          <path
            d="M 0 540 L 400 480 L 800 600 L 1300 460 L 1920 580"
            stroke="#FF6D1F"
            strokeWidth="2"
            fill="none"
            strokeDasharray="2000"
            strokeDashoffset={2000 - crackProg * 2000}
          />
          <path
            d="M 600 0 L 700 300 L 580 600 L 720 900 L 660 1080"
            stroke="#FF6D1F"
            strokeWidth="2"
            fill="none"
            strokeDasharray="2000"
            strokeDashoffset={2000 - crackProg * 2000}
          />
        </svg>

        {/* Bottom caption */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(330),
            transform: `translateY(${interpolate(sp(330), [0, 1], [30, 0])}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '14px 40px',
              background: 'rgba(9,20,19,0.7)',
              border: '1px solid rgba(255,109,31,0.4)',
              borderRadius: 50,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 38,
              color: '#F5F5F5',
              fontWeight: 500,
            }}
          >
            "भाई कुनाल, पैर के नीचे से जमीन खिसक गई"
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(0,0,0,0) 40%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_ShockCall;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_MeterSwap',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_MeterSwap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan left to right
  const camX = interpolate(frame, [0, durationInFrames], [-30, 30]);

  // Old meter rotation (analog disc)
  const discRot = frame * 2;

  // Old meter exit at frame 180, new meter enter at 220
  const oldOpacity = interpolate(frame, [170, 200], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const oldX = interpolate(frame, [170, 220], [0, -300], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const newScale = sp(220, { damping: 10, stiffness: 100 });
  const newOpacity = interpolate(frame, [210, 250], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // VS divider pulse
  const vsPulse = 1 + Math.sin(frame * 0.15) * 0.05;

  // Screen flicker on new meter
  const screenFlicker = 0.85 + Math.sin(frame * 0.5) * 0.15;

  // Digital readout counter
  const digitProg = Math.max(0, frame - 280);
  const digitVal = (digitProg * 1.7).toFixed(2);

  // Particles
  const particles = Array.from({ length: 20 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#303841', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(9,20,19,0.5) 0%, rgba(48,56,65,0) 50%, rgba(255,109,31,0.12) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          borderRadius: '50%',
          background: '#FF6D1F',
          opacity: 0.04,
          left: 100,
          top: 50,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          background: '#091413',
          opacity: 0.3,
          right: 80,
          bottom: 80,
          transform: `rotate(${frame * 0.1}deg)`,
          borderRadius: 30,
        }}
      />

      {/* Diagonal lines texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="lines2" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 0 40 L 40 0" stroke="#FF6D1F" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#lines2)" />
      </svg>

      {/* Particles */}
      {particles.map((i) => {
        const seed = seededRandom(i + 5);
        const xPos = seed * 1920;
        const yPos = ((frame * (0.3 + seed * 0.5) + i * 60) % 1200) - 100;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: xPos,
              top: yPos,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: '#FF6D1F',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* Title top */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(10),
            transform: `translateY(${interpolate(sp(10), [0, 1], [-40, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              color: '#FF6D1F',
              letterSpacing: 6,
              fontWeight: 700,
            }}
          >
            THE ONLY CHANGE
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 80,
              color: '#F5F5F5',
              fontWeight: 700,
              letterSpacing: -1,
              marginTop: 8,
            }}
          >
            एक ही बदलाव
          </div>
        </div>

        {/* OLD METER - Left */}
        <div
          style={{
            position: 'absolute',
            left: 250,
            top: 320,
            opacity: oldOpacity,
            transform: `translateX(${oldX}px) scale(${sp(40)})`,
          }}
        >
          <svg width="380" height="450" viewBox="0 0 380 450">
            {/* Outer box */}
            <rect x="20" y="20" width="340" height="410" rx="20" fill="#091413" stroke="#9E9E9E" strokeWidth="3" />
            {/* Label */}
            <rect x="60" y="50" width="260" height="40" rx="4" fill="#303841" />
            <text x="190" y="78" textAnchor="middle" fill="#9E9E9E" fontFamily="Inter" fontSize="18" fontWeight="700">
              ANALOG METER
            </text>
            {/* Dial circle */}
            <circle cx="190" cy="230" r="110" fill="#303841" stroke="#9E9E9E" strokeWidth="4" />
            <circle cx="190" cy="230" r="95" fill="none" stroke="#9E9E9E" strokeWidth="1" opacity="0.4" />
            {/* Rotating disc marker */}
            <g transform={`rotate(${discRot} 190 230)`}>
              <rect x="186" y="135" width="8" height="95" fill="#FF6D1F" />
              <circle cx="190" cy="135" r="6" fill="#FF6D1F" />
            </g>
            <circle cx="190" cy="230" r="10" fill="#9E9E9E" />
            {/* Tick marks */}
            {Array.from({ length: 12 }).map((_, i) => {
              const angle = (i * 30) * Math.PI / 180;
              const x1 = 190 + Math.cos(angle) * 100;
              const y1 = 230 + Math.sin(angle) * 100;
              const x2 = 190 + Math.cos(angle) * 110;
              const y2 = 230 + Math.sin(angle) * 110;
              return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#9E9E9E" strokeWidth="2" />;
            })}
            {/* Bottom dial display */}
            <rect x="80" y="360" width="220" height="50" rx="4" fill="#303841" stroke="#9E9E9E" strokeWidth="1" />
            <text x="190" y="395" textAnchor="middle" fill="#F5F5F5" fontFamily="JetBrains Mono" fontSize="28" fontWeight="700">
              0 0 2 4 8 5
            </text>
          </svg>
          <div
            style={{
              textAlign: 'center',
              marginTop: 20,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 38,
              color: '#9E9E9E',
              fontWeight: 600,
            }}
          >
            पुराना गोल मीटर
          </div>
        </div>

        {/* VS divider */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: 460,
            transform: `translateX(-50%) scale(${vsPulse}) scale(${sp(100)})`,
            opacity: sp(100),
          }}
        >
          <div
            style={{
              width: 100,
              height: 100,
              borderRadius: '50%',
              background: '#FF6D1F',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Inter',
              fontSize: 36,
              fontWeight: 900,
              color: '#091413',
              boxShadow: '0 0 40px rgba(255,109,31,0.6)',
            }}
          >
            VS
          </div>
        </div>

        {/* Arrow flowing left to right */}
        {frame > 150 && frame < 230 && (
          <div
            style={{
              position: 'absolute',
              left: '50%',
              top: 480,
              transform: 'translateX(-50%)',
            }}
          >
            <svg width="600" height="60" viewBox="0 0 600 60">
              <path
                d="M 50 30 L 550 30 M 530 15 L 550 30 L 530 45"
                stroke="#FF6D1F"
                strokeWidth="4"
                fill="none"
                strokeLinecap="round"
                strokeDasharray="500"
                strokeDashoffset={interpolate(frame, [150, 220], [500, 0])}
              />
            </svg>
          </div>
        )}

        {/* NEW SMART METER - Right */}
        <div
          style={{
            position: 'absolute',
            right: 250,
            top: 320,
            opacity: newOpacity,
            transform: `scale(${newScale})`,
          }}
        >
          <svg width="380" height="450" viewBox="0 0 380 450">
            {/* Outer box */}
            <rect x="20" y="20" width="340" height="410" rx="20" fill="#091413" stroke="#FF6D1F" strokeWidth="3" />
            <rect x="20" y="20" width="340" height="410" rx="20" fill="none" stroke="#FF6D1F" strokeWidth="3" opacity={0.4 + Math.sin(frame * 0.1) * 0.3} />
            {/* Label */}
            <rect x="60" y="50" width="260" height="40" rx="4" fill="#FF6D1F" />
            <text x="190" y="78" textAnchor="middle" fill="#091413" fontFamily="Inter" fontSize="18" fontWeight="800">
              SMART METER
            </text>
            {/* Digital screen */}
            <rect x="50" y="120" width="280" height="180" rx="8" fill="#091413" stroke="#FF6D1F" strokeWidth="2" opacity={screenFlicker} />
            <text x="70" y="160" fill="#FF6D1F" fontFamily="JetBrains Mono" fontSize="16" fontWeight="700">
              kWh USAGE
            </text>
            <text x="190" y="235" textAnchor="middle" fill="#FF6D1F" fontFamily="JetBrains Mono" fontSize="64" fontWeight="800" opacity={screenFlicker}>
              {digitVal}
            </text>
            <text x="70" y="280" fill="#F5F5F5" fontFamily="JetBrains Mono" fontSize="14">
              LIVE • ENCRYPTED
            </text>
            {/* Signal bars */}
            <g transform="translate(280, 270)">
              {[0, 1, 2, 3].map((i) => (
                <rect key={i} x={i * 8} y={20 - (i + 1) * 4} width="5" height={(i + 1) * 4} fill="#FF6D1F" opacity={(frame % 60) / 60 > i / 5 ? 1 : 0.3} />
              ))}
            </g>
            {/* Status LEDs */}
            <circle cx="80" cy="350" r="8" fill="#FF6D1F" opacity={0.5 + Math.sin(frame * 0.4) * 0.5} />
            <text x="100" y="356" fill="#F5F5F5" fontFamily="Inter" fontSize="16">ACTIVE</text>
            <circle cx="240" cy="350" r="8" fill="#FF6D1F" opacity={0.5 + Math.sin(frame * 0.3 + 2) * 0.5} />
            <text x="260" y="356" fill="#F5F5F5" fontFamily="Inter" fontSize="16">SYNC</text>
          </svg>
          <div
            style={{
              textAlign: 'center',
              marginTop: 20,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 38,
              color: '#FF6D1F',
              fontWeight: 700,
            }}
          >
            चमकती स्क्रीन वाला डिबा
          </div>
        </div>

        {/* Bottom callout */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(310),
            transform: `translateY(${interpolate(sp(310), [0, 1], [30, 0])}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '16px 50px',
              background: '#091413',
              border: '2px solid #FF6D1F',
              borderRadius: 10,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 42,
              color: '#F5F5F5',
              fontWeight: 600,
            }}
          >
            सब बोल रहे — <span style={{ color: '#FF6D1F', fontWeight: 800 }}>"स्मार्ट मीटर"</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(0,0,0,0) 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_MeterSwap;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_RumorWarning',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_RumorWarning: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera orbit (slight zoom in then out)
  const camScale = interpolate(frame, [0, 200, durationInFrames], [1.05, 1.0, 1.05]);

  // Channel logo animation
  const logoScale = sp(30, { damping: 8, stiffness: 80 });

  // Rumor bubbles floating around
  const rumors = [
    { text: 'स्कैम है!', x: 200, y: 250, delay: 80, size: 38 },
    { text: 'सरकार लूट रही!', x: 1400, y: 220, delay: 120, size: 42 },
    { text: 'रात को unit बढ़ता', x: 180, y: 700, delay: 160, size: 36 },
    { text: 'WhatsApp forward', x: 1450, y: 720, delay: 200, size: 34 },
    { text: 'प्राइवेट साजिश!', x: 1500, y: 450, delay: 240, size: 38 },
    { text: 'डिजिटल फ्रॉड', x: 150, y: 480, delay: 280, size: 36 },
  ];

  // Big warning at end
  const warningProg = sp(380);

  // Particles
  const particles = Array.from({ length: 30 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#303841', overflow: 'hidden', opacity: exitOp }}>
      {/* Radial gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(255,109,31,0.15) 0%, rgba(48,56,65,0) 50%), radial-gradient(circle at 50% 100%, rgba(9,20,19,0.6) 0%, rgba(48,56,65,0) 60%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 1200,
          height: 1200,
          borderRadius: '50%',
          border: '1px solid #FF6D1F',
          opacity: 0.06,
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) scale(${0.8 + Math.sin(frame * 0.02) * 0.05}) rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 800,
          height: 800,
          borderRadius: '50%',
          border: '1px solid #FF6D1F',
          opacity: 0.08,
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) scale(${0.9 + Math.sin(frame * 0.025) * 0.04}) rotate(${-frame * 0.15}deg)`,
        }}
      />

      {/* Dots texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
        <defs>
          <pattern id="dots3" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#FF6D1F" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots3)" />
      </svg>

      {/* Floating particles */}
      {particles.map((i) => {
        const seed = seededRandom(i + 11);
        const xPos = seed * 1920;
        const yPos = ((frame * (0.4 + seed * 0.6) + i * 50) % 1200) - 100;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: xPos,
              top: yPos,
              width: 3 + seed * 4,
              height: 3 + seed * 4,
              borderRadius: '50%',
              background: '#FF6D1F',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* CENTER - Channel identity */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '38%',
            transform: `translate(-50%, -50%) scale(${logoScale})`,
            textAlign: 'center',
          }}
        >
          {/* Identity card */}
          <div
            style={{
              padding: '36px 60px',
              background: '#091413',
              border: '3px solid #FF6D1F',
              borderRadius: 20,
              boxShadow: '0 0 80px rgba(255,109,31,0.3)',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 24,
                color: '#9E9E9E',
                letterSpacing: 4,
                fontWeight: 600,
              }}
            >
              YOUR HOST
            </div>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 120,
                color: '#F5F5F5',
                fontWeight: 700,
                lineHeight: 1,
                marginTop: 8,
                letterSpacing: -2,
              }}
            >
              कुनाल
            </div>
            <div
              style={{
                marginTop: 16,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                gap: 12,
              }}
            >
              <div style={{ width: 40, height: 3, background: '#FF6D1F' }} />
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 30,
                  color: '#FF6D1F',
                  fontWeight: 800,
                  letterSpacing: 1,
                }}
              >
                ProBuffet HQ
              </div>
              <div style={{ width: 40, height: 3, background: '#FF6D1F' }} />
            </div>
          </div>
        </div>

        {/* Floating rumor bubbles */}
        {rumors.map((r, i) => {
          const bubbleSp = sp(r.delay, { damping: 10, stiffness: 90 });
          const float = Math.sin(frame * 0.04 + i) * 12;
          // Fade out as warning appears
          const fadeOut = interpolate(frame, [360, 400], [1, 0.15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: r.x,
                top: r.y + float,
                opacity: bubbleSp * fadeOut,
                transform: `scale(${bubbleSp}) rotate(${(seededRandom(i + 20) - 0.5) * 8}deg)`,
              }}
            >
              <div
                style={{
                  padding: '14px 28px',
                  background: 'rgba(9,20,19,0.85)',
                  border: '2px solid rgba(255,109,31,0.5)',
                  borderRadius: 30,
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: r.size,
                  color: '#F5F5F5',
                  fontWeight: 600,
                  whiteSpace: 'nowrap',
                }}
              >
                "{r.text}"
              </div>
            </div>
          );
        })}

        {/* Connecting lines from logo to rumors */}
        <svg style={{ position: 'absolute', inset: 0, pointerEvents: 'none', opacity: interpolate(frame, [250, 350], [0, 0.3]) }} width="1920" height="1080">
          {rumors.map((r, i) => (
            <line
              key={i}
              x1="960"
              y1="410"
              x2={r.x + 100}
              y2={r.y + 30}
              stroke="#FF6D1F"
              strokeWidth="1"
              strokeDasharray="6 6"
            />
          ))}
        </svg>

        {/* Bottom warning */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: warningProg,
            transform: `translateY(${interpolate(warningProg, [0, 1], [40, 0])}px) scale(${warningProg})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              color: '#FF6D1F',
              letterSpacing: 5,
              fontWeight: 700,
              marginBottom: 12,
            }}
          >
            FINANCIAL TRUTH
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 76,
              color: '#F5F5F5',
              fontWeight: 700,
              letterSpacing: -1,
              lineHeight: 1.1,
            }}
          >
            अफ़वाह से ज्यादा <span style={{ color: '#FF6D1F' }}>खतरनाक</span> कुछ नहीं
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(0,0,0,0) 40%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_RumorWarning;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_NeutralStance',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_NeutralStance: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-0.5, 0.5]);

  // Balance scale tilt animation
  const scaleTilt = Math.sin(frame * 0.04) * 4;

  // Crossed out items
  const cross1Prog = sp(80);
  const cross2Prog = sp(160);

  // Center reveal
  const centerProg = sp(240, { damping: 10 });

  // Eye iris pulse
  const irisPulse = 1 + Math.sin(frame * 0.1) * 0.05;

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#303841', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(9,20,19,0.4) 0%, rgba(48,56,65,0) 50%, rgba(255,109,31,0.1) 100%)',
        }}
      />

      {/* Shape layers - triangles */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <polygon
          points="200,100 400,500 0,500"
          fill="#FF6D1F"
          transform={`rotate(${frame * 0.1} 200 300)`}
        />
        <polygon
          points="1700,200 1920,700 1500,700"
          fill="#FF6D1F"
          transform={`rotate(${-frame * 0.08} 1700 450)`}
        />
        <rect x="800" y="900" width="300" height="300" fill="#091413" opacity="0.5" transform={`rotate(${frame * 0.05} 950 1050)`} />
      </svg>

      {/* Concentric circles texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="100%" height="100%">
        {[100, 200, 300, 400, 500].map((r) => (
          <circle key={r} cx="960" cy="540" r={r} fill="none" stroke="#FF6D1F" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((i) => {
        const seed = seededRandom(i + 30);
        const xPos = seed * 1920;
        const yPos = ((frame * (0.3 + seed * 0.5) + i * 60) % 1200) - 100;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: xPos,
              top: yPos,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: '#FF6D1F',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(10),
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '10px 30px',
              background: 'rgba(9,20,19,0.7)',
              border: '1px solid rgba(255,109,31,0.4)',
              borderRadius: 40,
              fontFamily: 'Inter',
              fontSize: 24,
              color: '#FF6D1F',
              letterSpacing: 4,
              fontWeight: 700,
            }}
          >
            HONEST APPROACH
          </div>
        </div>

        {/* LEFT - Crossed out: सरकार की चमचागिरी */}
        <div
          style={{
            position: 'absolute',
            left: 140,
            top: 280,
            width: 560,
            opacity: sp(50),
            transform: `translateX(${interpolate(sp(50), [0, 1], [-80, 0])}px)`,
          }}
        >
          <div
            style={{
              padding: '30px 36px',
              background: 'rgba(9,20,19,0.6)',
              border: '2px solid rgba(158,158,158,0.3)',
              borderRadius: 16,
              position: 'relative',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 22,
                color: '#9E9E9E',
                letterSpacing: 3,
                fontWeight: 600,
                marginBottom: 12,
              }}
            >
              NO SUCK-UP
            </div>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 56,
                color: '#F5F5F5',
                fontWeight: 700,
                lineHeight: 1.1,
              }}
            >
              सरकार की चमचागिरी
            </div>
            {/* Cross line */}
            <svg
              style={{ position: 'absolute', inset: 0, pointerEvents: 'none' }}
              width="100%"
              height="100%"
              viewBox="0 0 560 180"
              preserveAspectRatio="none"
            >
              <line
                x1="20"
                y1="160"
                x2="540"
                y2="20"
                stroke="#FF6D1F"
                strokeWidth="6"
                strokeLinecap="round"
                strokeDasharray="700"
                strokeDashoffset={700 - cross1Prog * 700}
              />
            </svg>
          </div>
        </div>

        {/* RIGHT - Crossed out: बेवजह डराना */}
        <div
          style={{
            position: 'absolute',
            right: 140,
            top: 280,
            width: 560,
            opacity: sp(130),
            transform: `translateX(${interpolate(sp(130), [0, 1], [80, 0])}px)`,
          }}
        >
          <div
            style={{
              padding: '30px 36px',
              background: 'rgba(9,20,19,0.6)',
              border: '2px solid rgba(158,158,158,0.3)',
              borderRadius: 16,
              position: 'relative',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 22,
                color: '#9E9E9E',
                letterSpacing: 3,
                fontWeight: 600,
                marginBottom: 12,
              }}
            >
              NO FEAR-MONGERING
            </div>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 56,
                color: '#F5F5F5',
                fontWeight: 700,
                lineHeight: 1.1,
              }}
            >
              बेवजह डराना
            </div>
            {/* Cross line */}
            <svg
              style={{ position: 'absolute', inset: 0, pointerEvents: 'none' }}
              width="100%"
              height="100%"
              viewBox="0 0 560 180"
              preserveAspectRatio="none"
            >
              <line
                x1="20"
                y1="160"
                x2="540"
                y2="20"
                stroke="#FF6D1F"
                strokeWidth="6"
                strokeLinecap="round"
                strokeDasharray="700"
                strokeDashoffset={700 - cross2Prog * 700}
              />
            </svg>
          </div>
        </div>

        {/* CENTER bottom - The lens / approach */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: 560,
            transform: `translateX(-50%) scale(${centerProg})`,
            opacity: centerProg,
            textAlign: 'center',
          }}
        >
          {/* Eye / lens icon */}
          <div style={{ marginBottom: 20, display: 'flex', justifyContent: 'center' }}>
            <svg width="180" height="120" viewBox="0 0 180 120">
              <ellipse cx="90" cy="60" rx="80" ry="45" fill="none" stroke="#FF6D1F" strokeWidth="4" />
              <circle cx="90" cy="60" r={35 * irisPulse} fill="#091413" stroke="#FF6D1F" strokeWidth="3" />
              <circle cx="90" cy="60" r={14 * irisPulse} fill="#FF6D1F" />
              <circle cx="98" cy="52" r="6" fill="#F5F5F5" />
            </svg>
          </div>

          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              color: '#FF6D1F',
              letterSpacing: 5,
              fontWeight: 700,
              marginBottom: 8,
            }}
          >
            THROUGH THE LENS OF
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 92,
              color: '#F5F5F5',
              fontWeight: 700,
              lineHeight: 1,
              letterSpacing: -1,
            }}
          >
            <span style={{ color: '#FF6D1F' }}>फाइनेंशल</span> एडवाइजर
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(0,0,0,0) 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_NeutralStance;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_Questions',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_Questions: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera vertical pan
  const camY = interpolate(frame, [0, durationInFrames], [-20, 20]);

  const questions = [
    { num: '01', q: 'बिल क्यों बढ़ रहा है?', en: 'WHY IS BILL RISING', delay: 30 },
    { num: '02', q: 'कितना बढ़ना सही?', en: 'HOW MUCH IS FAIR', delay: 90 },
    { num: '03', q: 'कितना गलत?', en: 'HOW MUCH IS WRONG', delay: 150 },
    { num: '04', q: 'बिल कैसे कम करें?', en: 'HOW TO REDUCE', delay: 210 },
    { num: '05', q: 'शिकायत कहाँ करें?', en: 'WHERE TO COMPLAIN', delay: 270 },
  ];

  // Particles
  const particles = Array.from({ length: 25 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#303841', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(45deg, rgba(9,20,19,0.5) 0%, rgba(48,56,65,0) 40%), radial-gradient(circle at 80% 20%, rgba(255,109,31,0.15) 0%, rgba(48,56,65,0) 50%)',
        }}
      />

      {/* Big number shape in background */}
      <div
        style={{
          position: 'absolute',
          right: -100,
          top: -80,
          fontFamily: 'Yantra One',
          fontSize: 800,
          color: '#FF6D1F',
          opacity: 0.04,
          fontWeight: 900,
          transform: `rotate(${Math.sin(frame * 0.01) * 3}deg)`,
        }}
      >
        ?
      </div>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          background: '#091413',
          opacity: 0.4,
          left: -200,
          bottom: -200,
          transform: `rotate(${45 + frame * 0.08}deg)`,
          borderRadius: 40,
        }}
      />

      {/* Vertical lines texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="100%" height="100%">
        <defs>
          <pattern id="vlines5" width="30" height="30" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="30" stroke="#FF6D1F" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#vlines5)" />
      </svg>

      {/* Particles */}
      {particles.map((i) => {
        const seed = seededRandom(i + 50);
        const xPos = seed * 1920;
        const yPos = ((frame * (0.4 + seed * 0.5) + i * 70) % 1200) - 100;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: xPos,
              top: yPos,
              width: 3 + seed * 3,
              height: 3 + seed * 3,
              borderRadius: '50%',
              background: '#FF6D1F',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 120,
            opacity: sp(0),
            transform: `translateX(${interpolate(sp(0), [0, 1], [-60, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              color: '#FF6D1F',
              letterSpacing: 6,
              fontWeight: 700,
            }}
          >
            TODAY WE ANSWER
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 90,
              color: '#F5F5F5',
              fontWeight: 700,
              lineHeight: 1,
              marginTop: 8,
              letterSpacing: -2,
            }}
          >
            5 असली सवाल
          </div>
        </div>

        {/* Questions list */}
        <div
          style={{
            position: 'absolute',
            left: 140,
            top: 290,
            display: 'flex',
            flexDirection: 'column',
            gap: 22,
          }}
        >
          {questions.map((item, i) => {
            const itemSp = sp(item.delay, { damping: 11 });
            const isLast = i === questions.length - 1;
            return (
              <div
                key={i}
                style={{
                  opacity: itemSp,
                  transform: `translateX(${interpolate(itemSp, [0, 1], [-80, 0])}px)`,
                  display: 'flex',
                  alignItems: 'center',
                  gap: 30,
                  padding: '20px 40px',
                  background: isLast ? 'linear-gradient(90deg, rgba(255,109,31,0.2) 0%, rgba(9,20,19,0.5) 100%)' : 'rgba(9,20,19,0.55)',
                  border: isLast ? '2px solid #FF6D1F' : '2px solid rgba(158,158,158,0.2)',
                  borderRadius: 14,
                  width: 1100,
                  boxShadow: isLast ? '0 0 40px rgba(255,109,31,0.25)' : 'none',
                }}
              >
                {/* Number */}
                <div
                  style={{
                    fontFamily: 'JetBrains Mono',
                    fontSize: 64,
                    color: isLast ? '#FF6D1F' : '#9E9E9E',
                    fontWeight: 800,
                    minWidth: 100,
                    lineHeight: 1,
                  }}
                >
                  {item.num}
                </div>

                {/* Divider */}
                <div style={{ width: 3, height: 70, background: isLast ? '#FF6D1F' : 'rgba(158,158,158,0.4)' }} />

                {/* Content */}
                <div style={{ flex: 1 }}>
                  <div
                    style={{
                      fontFamily: 'Inter',
                      fontSize: 18,
                      color: isLast ? '#FF6D1F' : '#9E9E9E',
                      letterSpacing: 3,
                      fontWeight: 600,
                      marginBottom: 4,
                    }}
                  >
                    {item.en}
                  </div>
                  <div
                    style={{
                      fontFamily: 'Noto Sans Devanagari',
                      fontSize: 48,
                      color: '#F5F5F5',
                      fontWeight: 600,
                      lineHeight: 1.1,
                    }}
                  >
                    {item.q}
                  </div>
                </div>

                {/* Pin marker for last */}
                {isLast && (
                  <div
                    style={{
                      background: '#FF6D1F',
                      color: '#091413',
                      padding: '8px 18px',
                      borderRadius: 6,
                      fontFamily: 'Inter',
                      fontSize: 18,
                      fontWeight: 800,
                      letterSpacing: 2,
                    }}
                  >
                    MOST IMPORTANT
                  </div>
                )}
              </div>
            );
          })}
        </div>

        {/* Bottom strip */}
        <div
          style={{
            position: 'absolute',
            bottom: 50,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(340),
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 32,
              color: '#9E9E9E',
              fontWeight: 500,
            }}
          >
            ये सिर्फ बिजली नहीं — <span style={{ color: '#FF6D1F', fontWeight: 700 }}>आपकी जेब का मामला है</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(0,0,0,0) 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_Questions;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_AluminumDisc',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_AluminumDisc: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom into disc
  const camScale = interpolate(frame, [0, 200, durationInFrames], [1, 1.1, 1.05]);

  // Disc rotation - starts slow, speeds up
  const speedMultiplier = interpolate(frame, [60, 200, 400], [0.5, 4, 6]);
  const discRot = frame * speedMultiplier;

  // Lightning bolt pulses representing electricity
  const boltPulse = (delay: number) => Math.max(0, Math.sin((frame - delay) * 0.3));

  // Speed indicator
  const speedLevel = interpolate(frame, [60, 200, 400], [10, 60, 95], { extrapolateRight: 'clamp' });

  // Particles
  const particles = Array.from({ length: 20 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#303841', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(255,109,31,0.18) 0%, rgba(48,56,65,0) 50%), linear-gradient(180deg, rgba(9,20,19,0.4) 0%, rgba(48,56,65,0) 60%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 1000,
          height: 1000,
          borderRadius: '50%',
          border: '2px solid #FF6D1F',
          opacity: 0.05,
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) rotate(${frame * 0.5}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '1px dashed #FF6D1F',
          opacity: 0.1,
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) rotate(${-frame * 0.8}deg)`,
        }}
      />

      {/* Concentric circles */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
        {[150, 250, 350].map((r) => (
          <circle key={r} cx="960" cy="540" r={r} fill="none" stroke="#FF6D1F" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((i) => {
        const seed = seededRandom(i + 60);
        const xPos = seed * 1920;
        const yPos = ((frame * (0.4 + seed * 0.5) + i * 60) % 1200) - 100;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: xPos,
              top: yPos,
              width: 3 + seed * 4,
              height: 3 + seed * 4,
              borderRadius: '50%',
              background: '#FF6D1F',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(0),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 24,
              color: '#FF6D1F',
              letterSpacing: 6,
              fontWeight: 700,
            }}
          >
            HOW IT WORKED — OLD ANALOG METER
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 76,
              color: '#F5F5F5',
              fontWeight: 700,
              marginTop: 8,
              letterSpacing: -1,
            }}
          >
            एलूमिनियम की डिस्क
          </div>
        </div>

        {/* LEFT - Lightning input (electricity in) */}
        <div
          style={{
            position: 'absolute',
            left: 100,
            top: 460,
            opacity: sp(60),
            transform: `translateX(${interpolate(sp(60), [0, 1], [-60, 0])}px)`,
            textAlign: 'center',
          }}
        >
          <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
            {[0, 1, 2].map((i) => (
              <svg key={i} width="80" height="100" viewBox="0 0 80 100" style={{ opacity: 0.4 + boltPulse(i * 10) * 0.6 }}>
                <path
                  d="M 45 0 L 15 55 L 35 55 L 25 100 L 65 40 L 45 40 L 55 0 Z"
                  fill="#FF6D1F"
                  stroke="#FF6D1F"
                  strokeWidth="2"
                />
              </svg>
            ))}
          </div>
          <div
            style={{
              marginTop: 20,
              fontFamily: 'Inter',
              fontSize: 20,
              color: '#FF6D1F',
              letterSpacing: 3,
              fontWeight: 700,
            }}
          >
            POWER IN
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 28,
              color: '#F5F5F5',
              fontWeight: 500,
            }}
          >
            बिजली
          </div>
        </div>

        {/* CENTER - The spinning disc (main visual) */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            transform: `translate(-50%, -50%) scale(${sp(40)})`,
          }}
        >
          {/* Meter housing */}
          <div
            style={{
              padding: 30,
              background: '#091413',
              border: '4px solid #FF6D1F',
              borderRadius: 24,
              boxShadow: '0 0 60px rgba(255,109,31,0.3)',
            }}
          >
            <svg width="460" height="460" viewBox="0 0 460 460">
              {/* Outer ring */}
              <circle cx="230" cy="230" r="220" fill="#303841" stroke="#FF6D1F" strokeWidth="3" />
              <circle cx="230" cy="230" r="200" fill="none" stroke="#9E9E9E" strokeWidth="1" opacity="0.3" />

              {/* Speed marks around ring */}
              {Array.from({ length: 24 }).map((_, i) => {
                const angle = (i * 15 - 90) * Math.PI / 180;
                const x1 = 230 + Math.cos(angle) * 200;
                const y1 = 230 + Math.sin(angle) * 200;
                const x2 = 230 + Math.cos(angle) * 220;
                const y2 = 230 + Math.sin(angle) * 220;
                return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#FF6D1F" strokeWidth={i % 6 === 0 ? 3 : 1} opacity={i % 6 === 0 ? 0.8 : 0.4} />;
              })}

              {/* The rotating aluminum disc */}
              <g transform={`rotate(${discRot} 230 230)`}>
                <circle cx="230" cy="230" r="160" fill="#9E9E9E" opacity="0.85" />
                <circle cx="230" cy="230" r="160" fill="url(#discGrad)" />
                {/* Marker spot on disc */}
                <circle cx="230" cy="90" r="14" fill="#FF6D1F" />
                <rect x="226" y="80" width="8" height="50" fill="#FF6D1F" />
                {/* Radial lines on disc */}
                {[0, 60, 120, 180, 240, 300].map((a) => {
                  const rad = a * Math.PI / 180;
                  const x = 230 + Math.cos(rad) * 140;
                  const y = 230 + Math.sin(rad) * 140;
                  return <line key={a} x1="230" y1="230" x2={x} y2={y} stroke="#303841" strokeWidth="2" opacity="0.6" />;
                })}
              </g>

              {/* Center hub */}
              <circle cx="230" cy="230" r="20" fill="#091413" stroke="#FF6D1F" strokeWidth="2" />
              <circle cx="230" cy="230" r="8" fill="#FF6D1F" />

              <defs>
                <radialGradient id="discGrad">
                  <stop offset="0%" stopColor="#F5F5F5" stopOpacity="0.9" />
                  <stop offset="100%" stopColor="#9E9E9E" stopOpacity="0.7" />
                </radialGradient>
              </defs>
            </svg>
          </div>

          {/* Speed indicator below */}
          <div style={{ marginTop: 28, textAlign: 'center', opacity: sp(140) }}>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 20,
                color: '#9E9E9E',
                letterSpacing: 3,
                fontWeight: 700,
                marginBottom: 8,
              }}
            >
              ROTATION SPEED
            </div>
            <div
              style={{
                width: 400,
                height: 16,
                background: 'rgba(9,20,19,0.7)',
                borderRadius: 8,
                overflow: 'hidden',
                border: '1px solid rgba(255,109,31,0.4)',
                margin: '0 auto',
              }}
            >
              <div
                style={{
                  width: `${speedLevel}%`,
                  height: '100%',
                  background: 'linear-gradient(90deg, #FF6D1F 0%, #F5F5F5 100%)',
                  transition: 'width 0.2s',
                }}
              />
            </div>
          </div>
        </div>

        {/* RIGHT - Speed label */}
        <div
          style={{
            position: 'absolute',
            right: 90,
            top: 440,
            opacity: sp(180),
            transform: `translateX(${interpolate(sp(180), [0, 1], [60, 0])}px)`,
            textAlign: 'right',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 22,
              color: '#FF6D1F',
              letterSpacing: 3,
              fontWeight: 700,
            }}
          >
            MORE POWER
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 64,
              color: '#F5F5F5',
              fontWeight: 700,
              lineHeight: 1,
              marginTop: 8,
            }}
          >
            ज़्यादा खर्च
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              color: '#FF6D1F',
              fontWeight: 700,
              marginTop: 16,
            }}
          >
            ↓
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 42,
              color: '#F5F5F5',
              fontWeight: 600,
              marginTop: 8,
            }}
          >
            उतनी <span style={{ color: '#FF6D1F' }}>तेज़</span> घूमती
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(0,0,0,0) 40%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_AluminumDisc;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_OldSystem',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_OldSystem: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera horizontal pan
  const camX = interpolate(frame, [0, durationInFrames], [40, -40]);

  // Three scenarios
  const items = [
    { tag: 'KABHI SAHI', label: 'कभी सही', sub: 'Read correctly', delay: 60, accuracy: 100 },
    { tag: 'KABHI ANDAZA', label: 'कभी अंदाज़े से', sub: 'Guessed', delay: 140, accuracy: 60 },
    { tag: 'KABHI AVERAGE', label: 'कभी एवरेज', sub: 'Average billed', delay: 220, accuracy: 40 },
  ];

  // Decade marker
  const decadeProg = sp(310);

  // Calendar pages flipping
  const pageRot = (frame * 4) % 360;

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#303841', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(9,20,19,0.5) 0%, rgba(48,56,65,0) 40%, rgba(255,109,31,0.1) 100%)',
        }}
      />

      {/* Big year text background */}
      <div
        style={{
          position: 'absolute',
          left: -40,
          bottom: -100,
          fontFamily: 'Yantra One',
          fontSize: 500,
          color: '#FF6D1F',
          opacity: 0.04,
          fontWeight: 900,
          letterSpacing: -20,
        }}
      >
        DECADES
      </div>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          background: '#091413',
          opacity: 0.35,
          right: -150,
          top: -100,
          transform: `rotate(${30 + frame * 0.05}deg)`,
          borderRadius: 50,
        }}
      />

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="100%" height="100%">
        <defs>
          <pattern id="grid7" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#FF6D1F" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid7)" />
      </svg>

      {/* Particles */}
      {particles.map((i) => {
        const seed = seededRandom(i + 70);
        const xPos = seed * 1920;
        const yPos = ((frame * (0.4 + seed * 0.5) + i * 70) % 1200) - 100;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: xPos,
              top: yPos,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: '#FF6D1F',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(0),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 24,
              color: '#FF6D1F',
              letterSpacing: 6,
              fontWeight: 700,
            }}
          >
            METER READING — THE OLD WAY
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 76,
              color: '#F5F5F5',
              fontWeight: 700,
              marginTop: 8,
              letterSpacing: -1,
            }}
          >
            महिने के अंत में
          </div>
        </div>

        {/* Three columns */}
        <div
          style={{
            position: 'absolute',
            top: 290,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 50,
            padding: '0 100px',
          }}
        >
          {items.map((item, i) => {
            const itemSp = sp(item.delay, { damping: 11 });
            return (
              <div
                key={i}
                style={{
                  width: 460,
                  opacity: itemSp,
                  transform: `translateY(${interpolate(itemSp, [0, 1], [60, 0])}px)`,
                }}
              >
                {/* Card */}
                <div
                  style={{
                    background: 'rgba(9,20,19,0.7)',
                    border: '2px solid rgba(255,109,31,0.4)',
                    borderRadius: 16,
                    padding: 32,
                    position: 'relative',
                  }}
                >
                  {/* Number badge */}
                  <div
                    style={{
                      position: 'absolute',
                      top: -25,
                      left: 32,
                      background: '#FF6D1F',
                      color: '#091413',
                      padding: '6px 18px',
                      borderRadius: 6,
                      fontFamily: 'Inter',
                      fontSize: 16,
                      fontWeight: 800,
                      letterSpacing: 2,
                    }}
                  >
                    {item.tag}
                  </div>

                  {/* Visual icon - clipboard with reading */}
                  <div style={{ display: 'flex', justifyContent: 'center', marginBottom: 24 }}>
                    <svg width="160" height="160" viewBox="0 0 160 160">
                      <rect x="30" y="20" width="100" height="130" rx="6" fill="#303841" stroke="#FF6D1F" strokeWidth="2" />
                      <rect x="55" y="10" width="50" height="20" rx="3" fill="#FF6D1F" />
                      {/* Reading lines */}
                      <line x1="45" y1="50" x2="115" y2="50" stroke="#9E9E9E" strokeWidth="2" />
                      <line x1="45" y1="70" x2="115" y2="70" stroke="#9E9E9E" strokeWidth="2" />
                      <line x1="45" y1="90" x2="100" y2="90" stroke="#9E9E9E" strokeWidth="2" />
                      {/* Number */}
                      <text x="80" y="125" textAnchor="middle" fill="#FF6D1F" fontFamily="JetBrains Mono" fontSize="22" fontWeight="800">
                        {i === 0 ? '00248' : i === 1 ? '00???' : 'AVG'}
                      </text>
                    </svg>
                  </div>

                  {/* Label */}
                  <div
                    style={{
                      fontFamily: 'Yantra One',
                      fontSize: 50,
                      color: '#F5F5F5',
                      fontWeight: 700,
                      textAlign: 'center',
                      lineHeight: 1.1,
                    }}
                  >
                    {item.label}
                  </div>
                  <div
                    style={{
                      fontFamily: 'Inter',
                      fontSize: 20,
                      color: '#FF6D1F',
                      letterSpacing: 2,
                      fontWeight: 600,
                      textAlign: 'center',
                      marginTop: 8,
                    }}
                  >
                    {item.sub}
                  </div>

                  {/* Accuracy bar */}
                  <div style={{ marginTop: 24 }}>
                    <div
                      style={{
                        fontFamily: 'JetBrains Mono',
                        fontSize: 14,
                        color: '#9E9E9E',
                        letterSpacing: 2,
                        marginBottom: 6,
                        display: 'flex',
                        justifyContent: 'space-between',
                      }}
                    >
                      <span>ACCURACY</span>
                      <span style={{ color: '#FF6D1F', fontWeight: 700 }}>{item.accuracy}%</span>
                    </div>
                    <div
                      style={{
                        width: '100%',
                        height: 10,
                        background: 'rgba(48,56,65,0.8)',
                        borderRadius: 5,
                        overflow: 'hidden',
                      }}
                    >
                      <div
                        style={{
                          width: `${item.accuracy * Math.min(1, (frame - item.delay - 30) / 30)}%`,
                          height: '100%',
                          background: item.accuracy > 80 ? '#FF6D1F' : item.accuracy > 50 ? '#FF6D1F' : 'rgba(255,109,31,0.5)',
                        }}
                      />
                    </div>
                  </div>
                </div>
              </div>
            );
          })}
        </div>

        {/* Bottom - decades marker */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: decadeProg,
            transform: `translateY(${interpolate(decadeProg, [0, 1], [30, 0])}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 24,
              padding: '16px 40px',
              background: 'linear-gradient(90deg, rgba(255,109,31,0.2) 0%, rgba(9,20,19,0.7) 50%, rgba(255,109,31,0.2) 100%)',
              border: '2px solid #FF6D1F',
              borderRadius: 12,
            }}
          >
            {/* Calendar icon */}
            <svg width="50" height="50" viewBox="0 0 50 50" style={{ transform: `rotate(${Math.sin(frame * 0.1) * 5}deg)` }}>
              <rect x="5" y="10" width="40" height="35" rx="3" fill="none" stroke="#FF6D1F" strokeWidth="3" />
              <line x1="5" y1="20" x2="45" y2="20" stroke="#FF6D1F" strokeWidth="3" />
              <line x1="15" y1="5" x2="15" y2="15" stroke="#FF6D1F" strokeWidth="3" strokeLinecap="round" />
              <line x1="35" y1="5" x2="35" y2="15" stroke="#FF6D1F" strokeWidth="3" strokeLinecap="round" />
            </svg>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 48,
                color: '#F5F5F5',
                fontWeight: 700,
              }}
            >
              ये सिस्टम <span style={{ color: '#FF6D1F' }}>दशकों</span> से चल रहा था
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(0,0,0,0) 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_OldSystem;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_StandbyLoads',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_StandbyLoads: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slight zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  // Items
  const items = [
    { label: 'TV स्टैंडबाइ', en: 'TV STANDBY', power: '3W', delay: 60 },
    { label: 'फ्रिज कंप्रेसर', en: 'FRIDGE COMPRESSOR', power: '120W', delay: 130 },
    { label: 'फोन चार्जर', en: 'PHONE CHARGER', power: '2W', delay: 200 },
  ];

  // Hidden / missed counter
  const missedProg = sp(310);
  const missedNum = Math.round(interpolate(missedProg, [0, 1], [0, 15]));

  // Particles
  const particles = Array.from({ length: 25 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#303841', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at top, rgba(255,109,31,0.15) 0%, rgba(48,56,65,0) 60%), linear-gradient(180deg, rgba(48,56,65,0) 50%, rgba(9,20,19,0.5) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 800,
          height: 800,
          borderRadius: '50%',
          border: '2px solid #FF6D1F',
          opacity: 0.05,
          right: -200,
          top: -150,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 400,
          height: 400,
          background: '#091413',
          opacity: 0.4,
          left: -100,
          bottom: -80,
          transform: `rotate(${45 - frame * 0.08}deg)`,
          borderRadius: 30,
        }}
      />

      {/* Hex pattern texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="hex8" width="60" height="52" patternUnits="userSpaceOnUse">
            <polygon points="30,2 56,17 56,37 30,52 4,37 4,17" fill="none" stroke="#FF6D1F" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hex8)" />
      </svg>

      {/* Particles */}
      {particles.map((i) => {
        const seed = seededRandom(i + 80);
        const xPos = seed * 1920;
        const yPos = ((frame * (0.4 + seed * 0.5) + i * 50) % 1200) - 100;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: xPos,
              top: yPos,
              width: 3 + seed * 3,
              height: 3 + seed * 3,
              borderRadius: '50%',
              background: '#FF6D1F',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(0),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 24,
              color: '#FF6D1F',
              letterSpacing: 6,
              fontWeight: 700,
            }}
          >
            THE HIDDEN POWER DRAIN
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 76,
              color: '#F5F5F5',
              fontWeight: 700,
              marginTop: 8,
              letterSpacing: -1,
            }}
          >
            जो पुराना मीटर <span style={{ color: '#FF6D1F' }}>नहीं</span> गिनता था
          </div>
        </div>

        {/* Three appliance cards */}
        <div
          style={{
            position: 'absolute',
            top: 300,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 50,
            padding: '0 120px',
          }}
        >
          {items.map((item, i) => {
            const itemSp = sp(item.delay, { damping: 10 });
            const pulse = 0.7 + Math.sin((frame + i * 30) * 0.15) * 0.3;
            return (
              <div
                key={i}
                style={{
                  width: 430,
                  opacity: itemSp,
                  transform: `translateY(${interpolate(itemSp, [0, 1], [80, 0])}px)`,
                }}
              >
                <div
                  style={{
                    background: 'rgba(9,20,19,0.75)',
                    border: '2px solid rgba(255,109,31,0.4)',
                    borderRadius: 16,
                    padding: 32,
                    position: 'relative',
                    overflow: 'hidden',
                  }}
                >
                  {/* Pulsing LED dot */}
                  <div
                    style={{
                      position: 'absolute',
                      top: 24,
                      right: 24,
                      width: 14,
                      height: 14,
                      borderRadius: '50%',
                      background: '#FF6D1F',
                      opacity: pulse,
                      boxShadow: `0 0 ${15 * pulse}px #FF6D1F`,
                    }}
                  />

                  {/* Appliance illustration */}
                  <div style={{ display: 'flex', justifyContent: 'center', marginBottom: 20, height: 160 }}>
                    {i === 0 && (
                      // TV
                      <svg width="180" height="140" viewBox="0 0 180 140">
                        <rect x="10" y="10" width="160" height="105" rx="6" fill="#091413" stroke="#FF6D1F" strokeWidth="3" />
                        <rect x="20" y="20" width="140" height="85" fill="#303841" />
                        <text x="90" y="70" textAnchor="middle" fill="#FF6D1F" fontFamily="JetBrains Mono" fontSize="22" fontWeight="800">OFF</text>
                        <rect x="60" y="115" width="60" height="6" fill="#FF6D1F" />
                        <rect x="50" y="121" width="80" height="14" fill="#091413" stroke="#FF6D1F" strokeWidth="2" />
                        <circle cx="160" cy="100" r="4" fill="#FF6D1F" opacity={pulse} />
                      </svg>
                    )}
                    {i === 1 && (
                      // Fridge
                      <svg width="120" height="170" viewBox="0 0 120 170">
                        <rect x="10" y="10" width="100" height="150" rx="6" fill="#091413" stroke="#FF6D1F" strokeWidth="3" />
                        <line x1="10" y1="60" x2="110" y2="60" stroke="#FF6D1F" strokeWidth="3" />
                        <rect x="20" y="20" width="6" height="30" fill="#FF6D1F" />
                        <rect x="20" y="70" width="6" height="80" fill="#FF6D1F" />
                        {/* Compressor wave */}
                        <path d="M 30 130 Q 45 120 60 130 T 90 130" stroke="#FF6D1F" strokeWidth="2" fill="none" opacity={pulse} />
                      </svg>
                    )}
                    {i === 2 && (
                      // Charger plug
                      <svg width="180" height="140" viewBox="0 0 180 140">
                        {/* Adapter brick */}
                        <rect x="20" y="35" width="80" height="70" rx="8" fill="#091413" stroke="#FF6D1F" strokeWidth="3" />
                        {/* Prongs */}
                        <rect x="35" y="20" width="6" height="20" fill="#FF6D1F" />
                        <rect x="80" y="20" width="6" height="20" fill="#FF6D1F" />
                        {/* Cable */}
                        <path d="M 100 70 Q 130 60 150 80 Q 170 100 165 120" stroke="#FF6D1F" strokeWidth="4" fill="none" />
                        {/* USB end - empty */}
                        <rect x="155" y="115" width="20" height="14" rx="2" fill="none" stroke="#FF6D1F" strokeWidth="2" strokeDasharray="3 2" />
                        {/* Power LED */}
                        <circle cx="60" cy="70" r="6" fill="#FF6D1F" opacity={pulse} />
                      </svg>
                    )}
                  </div>

                  {/* Label */}
                  <div
                    style={{
                      fontFamily: 'Yantra One',
                      fontSize: 42,
                      color: '#F5F5F5',
                      fontWeight: 700,
                      textAlign: 'center',
                      lineHeight: 1.1,
                    }}
                  >
                    {item.label}
                  </div>
                  <div
                    style={{
                      fontFamily: 'Inter',
                      fontSize: 16,
                      color: '#9E9E9E',
                      letterSpacing: 3,
                      fontWeight: 600,
                      textAlign: 'center',
                      marginTop: 6,
                    }}
                  >
                    {item.en}
                  </div>

                  {/* Power consumption */}
                  <div
                    style={{
                      marginTop: 20,
                      padding: '12px 0',
                      borderTop: '1px solid rgba(255,109,31,0.3)',
                      textAlign: 'center',
                    }}
                  >
                    <div
                      style={{
                        fontFamily: 'JetBrains Mono',
                        fontSize: 14,
                        color: '#9E9E9E',
                        letterSpacing: 2,
                      }}
                    >
                      SILENT DRAW
                    </div>
                    <div
                      style={{
                        fontFamily: 'JetBrains Mono',
                        fontSize: 42,
                        color: '#FF6D1F',
                        fontWeight: 800,
                        marginTop: 4,
                      }}
                    >
                      ~{item.power}
                    </div>
                  </div>
                </div>
              </div>
            );
          })}
        </div>

        {/* Bottom verdict */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: missedProg,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 24,
              padding: '20px 50px',
              background: '#091413',
              border: '2px dashed #FF6D1F',
              borderRadius: 12,
            }}
          >
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 42,
                color: '#F5F5F5',
                fontWeight: 600,
              }}
            >
              पुराना मीटर मिस करता था लगभग
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 60,
                color: '#FF6D1F',
                fontWeight: 800,
              }}
            >
              {missedNum}%
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(0,0,0,0) 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_StandbyLoads;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_SmartSensor',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_SmartSensor: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera small zoom in
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  // Sensor scan waves
  const wave1 = ((frame * 1.5) % 100) / 100;
  const wave2 = ((frame * 1.5 + 33) % 100) / 100;
  const wave3 = ((frame * 1.5 + 66) % 100) / 100;

  // Real-time graph data
  const graphPoints = Array.from({ length: 40 }, (_, i) => {
    const x = i * 25;
    const noise = Math.sin((frame * 0.1 + i * 0.4)) * 30 + Math.sin((frame * 0.05 + i * 0.2)) * 20;
    const y = 100 + noise;
    return `${x},${y}`;
  }).join(' ');

  // Sensitivity meter
  const sensProg = sp(180);
  const sensLevel = interpolate(sensProg, [0, 1], [0, 99]);

  // Particles
  const particles = Array.from({ length: 25 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#303841', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 50%, rgba(255,109,31,0.18) 0%, rgba(48,56,65,0) 50%), linear-gradient(135deg, rgba(9,20,19,0.4) 0%, rgba(48,56,65,0) 60%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 900,
          height: 900,
          borderRadius: '50%',
          border: '1px solid #FF6D1F',
          opacity: 0.06,
          left: 200,
          top: '50%',
          transform: `translateY(-50%) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          background: '#091413',
          opacity: 0.35,
          right: -150,
          top: -100,
          transform: `rotate(${frame * 0.08}deg)`,
          borderRadius: 40,
        }}
      />

      {/* Circuit-like texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="circuit9" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M 0 50 L 30 50 L 30 30 L 70 30 L 70 70 L 100 70" fill="none" stroke="#FF6D1F" strokeWidth="1" />
            <circle cx="30" cy="50" r="3" fill="#FF6D1F" />
            <circle cx="70" cy="30" r="3" fill="#FF6D1F" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#circuit9)" />
      </svg>

      {/* Particles */}
      {particles.map((i) => {
        const seed = seededRandom(i + 90);
        const xPos = seed * 1920;
        const yPos = ((frame * (0.4 + seed * 0.5) + i * 60) % 1200) - 100;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: xPos,
              top: yPos,
              width: 3 + seed * 3,
              height: 3 + seed * 3,
              borderRadius: '50%',
              background: '#FF6D1F',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(0),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 24,
              color: '#FF6D1F',
              letterSpacing: 6,
              fontWeight: 700,
            }}
          >
            ENTER — ELECTRONIC SENSOR
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 80,
              color: '#F5F5F5',
              fontWeight: 700,
              marginTop: 8,
              letterSpacing: -1,
            }}
          >
            अब आया <span style={{ color: '#FF6D1F' }}>स्मार्ट मीटर</span>
          </div>
        </div>

        {/* LEFT - Sensor visualization */}
        <div
          style={{
            position: 'absolute',
            left: 200,
            top: 320,
            opacity: sp(50, { damping: 10 }),
            transform: `scale(${sp(50, { damping: 10 })})`,
          }}
        >
          {/* Sensor housing */}
          <div
            style={{
              width: 460,
              padding: 24,
              background: '#091413',
              border: '3px solid #FF6D1F',
              borderRadius: 20,
              boxShadow: '0 0 60px rgba(255,109,31,0.25)',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 16,
                color: '#FF6D1F',
                letterSpacing: 3,
                fontWeight: 700,
                marginBottom: 12,
              }}
            >
              ELECTRONIC SENSOR • LIVE
            </div>

            {/* Sensor with scan waves */}
            <div style={{ position: 'relative', height: 280, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
              {/* Scan rings */}
              {[wave1, wave2, wave3].map((w, i) => (
                <div
                  key={i}
                  style={{
                    position: 'absolute',
                    width: 80 + w * 240,
                    height: 80 + w * 240,
                    borderRadius: '50%',
                    border: '2px solid #FF6D1F',
                    opacity: (1 - w) * 0.6,
                  }}
                />
              ))}
              {/* Sensor core */}
              <div
                style={{
                  width: 100,
                  height: 100,
                  borderRadius: '50%',
                  background: 'radial-gradient(circle, #FF6D1F 0%, #091413 80%)',
                  boxShadow: '0 0 40px rgba(255,109,31,0.8)',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                }}
              >
                <svg width="50" height="50" viewBox="0 0 50 50">
                  <circle cx="25" cy="25" r="20" fill="none" stroke="#091413" strokeWidth="3" />
                  <circle cx="25" cy="25" r="8" fill="#091413" />
                  <line x1="25" y1="5" x2="25" y2="12" stroke="#091413" strokeWidth="3" />
                  <line x1="25" y1="38" x2="25" y2="45" stroke="#091413" strokeWidth="3" />
                  <line x1="5" y1="25" x2="12" y2="25" stroke="#091413" strokeWidth="3" />
                  <line x1="38" y1="25" x2="45" y2="25" stroke="#091413" strokeWidth="3" />
                </svg>
              </div>
            </div>

            {/* Stats below */}
            <div
              style={{
                marginTop: 16,
                display: 'flex',
                justifyContent: 'space-around',
                padding: '12px 0',
                borderTop: '1px solid rgba(255,109,31,0.3)',
              }}
            >
              <div style={{ textAlign: 'center' }}>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 14, color: '#9E9E9E', letterSpacing: 2 }}>SAMPLES</div>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#FF6D1F', fontWeight: 800 }}>1000/sec</div>
              </div>
              <div style={{ textAlign: 'center' }}>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 14, color: '#9E9E9E', letterSpacing: 2 }}>RESOLUTION</div>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#FF6D1F', fontWeight: 800 }}>0.01W</div>
              </div>
            </div>
          </div>
        </div>

        {/* RIGHT - Live graph */}
        <div
          style={{
            position: 'absolute',
            right: 140,
            top: 320,
            opacity: sp(120),
            transform: `translateX(${interpolate(sp(120), [0, 1], [60, 0])}px)`,
            width: 740,
          }}
        >
          {/* Graph card */}
          <div
            style={{
              background: 'rgba(9,20,19,0.7)',
              border: '2px solid rgba(255,109,31,0.4)',
              borderRadius: 16,
              padding: 28,
            }}
          >
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 16 }}>
              <div>
                <div
                  style={{
                    fontFamily: 'Inter',
                    fontSize: 18,
                    color: '#FF6D1F',
                    letterSpacing: 3,
                    fontWeight: 700,
                  }}
                >
                  LIVE CONSUMPTION
                </div>
                <div
                  style={{
                    fontFamily: 'Noto Sans Devanagari',
                    fontSize: 32,
                    color: '#F5F5F5',
                    fontWeight: 600,
                    marginTop: 4,
                  }}
                >
                  हर खपत रिकॉर्ड
                </div>
              </div>
              <div
                style={{
                  padding: '6px 14px',
                  background: '#FF6D1F',
                  color: '#091413',
                  borderRadius: 4,
                  fontFamily: 'JetBrains Mono',
                  fontSize: 14,
                  fontWeight: 800,
                  letterSpacing: 1,
                }}
              >
                ● REC
              </div>
            </div>

            {/* Graph SVG */}
            <svg width="100%" height="220" viewBox="0 0 1000 220">
              {/* Grid */}
              {[0, 55, 110, 165, 220].map((y) => (
                <line key={y} x1="0" y1={y} x2="1000" y2={y} stroke="#FF6D1F" strokeWidth="1" opacity="0.15" />
              ))}
              {[0, 200, 400, 600, 800, 1000].map((x) => (
                <line key={x} x1={x} y1="0" x2={x} y2="220" stroke="#FF6D1F" strokeWidth="1" opacity="0.15" />
              ))}
              {/* Filled area */}
              <polyline
                points={`0,220 ${graphPoints} 1000,220`}
                fill="url(#areaGrad)"
                opacity="0.5"
              />
              {/* Line */}
              <polyline
                points={graphPoints}
                fill="none"
                stroke="#FF6D1F"
                strokeWidth="3"
                strokeLinejoin="round"
              />
              <defs>
                <linearGradient id="areaGrad" x1="0" x2="0" y1="0" y2="1">
                  <stop offset="0%" stopColor="#FF6D1F" stopOpacity="0.8" />
                  <stop offset="100%" stopColor="#FF6D1F" stopOpacity="0" />
                </linearGradient>
              </defs>
            </svg>

            {/* Sensitivity bar */}
            <div style={{ marginTop: 20 }}>
              <div
                style={{
                  display: 'flex',
                  justifyContent: 'space-between',
                  marginBottom: 6,
                  fontFamily: 'JetBrains Mono',
                  fontSize: 14,
                  color: '#9E9E9E',
                  letterSpacing: 2,
                }}
              >
                <span>SENSITIVITY</span>
                <span style={{ color: '#FF6D1F', fontWeight: 700 }}>{sensLevel.toFixed(0)}%</span>
              </div>
              <div
                style={{
                  width: '100%',
                  height: 12,
                  background: 'rgba(48,56,65,0.8)',
                  borderRadius: 6,
                  overflow: 'hidden',
                  border: '1px solid rgba(255,109,31,0.3)',
                }}
              >
                <div
                  style={{
                    width: `${sensLevel}%`,
                    height: '100%',
                    background: 'linear-gradient(90deg, #FF6D1F 0%, #F5F5F5 100%)',
                  }}
                />
              </div>
            </div>
          </div>

          {/* Bottom tag */}
          <div
            style={{
              marginTop: 24,
              padding: '16px 24px',
              background: 'linear-gradient(90deg, rgba(255,109,31,0.25) 0%, rgba(9,20,19,0.5) 100%)',
              border: '1px solid #FF6D1F',
              borderRadius: 10,
              opacity: sp(280),
            }}
          >
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 32,
                color: '#F5F5F5',
                fontWeight: 600,
              }}
            >
              हर <span style={{ color: '#FF6D1F', fontWeight: 800 }}>छोटी से छोटी</span> खपत — दर्ज
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(0,0,0,0) 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_SmartSensor;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_NotFastButAccurate',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_NotFastButAccurate: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom punch on the verdict
  const camScale = interpolate(frame, [0, 240, 280, durationInFrames], [1, 1, 1.08, 1.04]);

  // "NOT FAST" cross
  const notFastProg = sp(80);
  const crossProg = sp(160);

  // "ACCURATE" reveal with bullseye
  const accurateProg = sp(240, { damping: 9 });

  // Bullseye target rings expanding
  const targetRing = (delay: number) => {
    const t = Math.max(0, frame - delay);
    return (t % 60) / 60;
  };

  // Crosshair pulse
  const crosshairPulse = 1 + Math.sin(frame * 0.15) * 0.05;

  // Particles
  const particles = Array.from({ length: 24 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#303841', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 60%, rgba(255,109,31,0.2) 0%, rgba(48,56,65,0) 55%), linear-gradient(180deg, rgba(9,20,19,0.45) 0%, rgba(48,56,65,0) 50%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 1100,
          height: 1100,
          borderRadius: '50%',
          border: '2px solid #FF6D1F',
          opacity: 0.05,
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          background: '#091413',
          opacity: 0.3,
          left: -150,
          top: -100,
          transform: `rotate(${frame * 0.06}deg)`,
          borderRadius: 40,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 400,
          height: 400,
          background: '#091413',
          opacity: 0.3,
          right: -100,
          bottom: -80,
          transform: `rotate(${-frame * 0.08}deg)`,
          borderRadius: 30,
        }}
      />

      {/* Plus pattern */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="plus10" width="80" height="80" patternUnits="userSpaceOnUse">
            <line x1="40" y1="35" x2="40" y2="45" stroke="#FF6D1F" strokeWidth="2" />
            <line x1="35" y1="40" x2="45" y2="40" stroke="#FF6D1F" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#plus10)" />
      </svg>

      {/* Particles */}
      {particles.map((i) => {
        const seed = seededRandom(i + 100);
        const xPos = seed * 1920;
        const yPos = ((frame * (0.4 + seed * 0.5) + i * 60) % 1200) - 100;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: xPos,
              top: yPos,
              width: 3 + seed * 3,
              height: 3 + seed * 3,
              borderRadius: '50%',
              background: '#FF6D1F',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(0),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              color: '#FF6D1F',
              letterSpacing: 6,
              fontWeight: 700,
            }}
          >
            THE TECHNICAL TRUTH
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 64,
              color: '#F5F5F5',
              fontWeight: 700,
              marginTop: 8,
              letterSpacing: -1,
            }}
          >
            अब सब कुछ <span style={{ color: '#FF6D1F' }}>दर्ज</span> हो रहा है
          </div>
        </div>

        {/* MYTH vs FACT layout */}
        {/* MYTH - Top section, will be crossed */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: 280,
            transform: `translateX(-50%) scale(${notFastProg})`,
            opacity: notFastProg,
            width: 900,
          }}
        >
          <div
            style={{
              padding: '24px 50px',
              background: 'rgba(9,20,19,0.6)',
              border: '2px solid rgba(158,158,158,0.4)',
              borderRadius: 14,
              position: 'relative',
              textAlign: 'center',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 22,
                color: '#9E9E9E',
                letterSpacing: 4,
                fontWeight: 700,
                marginBottom: 4,
              }}
            >
              THE MYTH
            </div>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 60,
                color: '#F5F5F5',
                fontWeight: 700,
                lineHeight: 1,
              }}
            >
              "स्मार्ट मीटर <span style={{ color: '#FF6D1F' }}>तेज़</span> चलता है"
            </div>

            {/* Big X cross */}
            <svg
              style={{ position: 'absolute', top: '50%', left: '50%', transform: 'translate(-50%, -50%)', pointerEvents: 'none' }}
              width="900"
              height="140"
              viewBox="0 0 900 140"
            >
              <line
                x1="40"
                y1="20"
                x2="860"
                y2="120"
                stroke="#FF6D1F"
                strokeWidth="6"
                strokeLinecap="round"
                strokeDasharray="1000"
                strokeDashoffset={1000 - crossProg * 1000}
              />
              <line
                x1="40"
                y1="120"
                x2="860"
                y2="20"
                stroke="#FF6D1F"
                strokeWidth="6"
                strokeLinecap="round"
                strokeDasharray="1000"
                strokeDashoffset={1000 - crossProg * 1000}
              />
            </svg>
          </div>
        </div>

        {/* Arrow down */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: 470,
            transform: `translateX(-50%) scale(${sp(220)})`,
            opacity: sp(220),
          }}
        >
          <svg width="60" height="60" viewBox="0 0 60 60">
            <path
              d="M 30 5 L 30 50 M 12 35 L 30 50 L 48 35"
              stroke="#FF6D1F"
              strokeWidth="5"
              fill="none"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
        </div>

        {/* TRUTH - Bottom section with bullseye */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: 560,
            transform: `translateX(-50%) scale(${accurateProg})`,
            opacity: accurateProg,
          }}
        >
          <div
            style={{
              padding: '40px 60px',
              background: 'linear-gradient(135deg, rgba(255,109,31,0.25) 0%, rgba(9,20,19,0.8) 100%)',
              border: '3px solid #FF6D1F',
              borderRadius: 18,
              boxShadow: '0 0 80px rgba(255,109,31,0.4)',
              display: 'flex',
              alignItems: 'center',
              gap: 50,
            }}
          >
            {/* Bullseye */}
            <div style={{ position: 'relative', width: 200, height: 200, transform: `scale(${crosshairPulse})` }}>
              {/* Expanding scan rings */}
              {[0, 30].map((delay, i) => {
                const r = targetRing(delay);
                return (
                  <div
                    key={i}
                    style={{
                      position: 'absolute',
                      width: 60 + r * 140,
                      height: 60 + r * 140,
                      borderRadius: '50%',
                      border: '2px solid #FF6D1F',
                      opacity: (1 - r) * 0.6,
                      top: '50%',
                      left: '50%',
                      transform: 'translate(-50%, -50%)',
                    }}
                  />
                );
              })}
              <svg width="200" height="200" viewBox="0 0 200 200">
                <circle cx="100" cy="100" r="90" fill="none" stroke="#FF6D1F" strokeWidth="3" opacity="0.6" />
                <circle cx="100" cy="100" r="65" fill="none" stroke="#FF6D1F" strokeWidth="3" opacity="0.7" />
                <circle cx="100" cy="100" r="40" fill="none" stroke="#FF6D1F" strokeWidth="3" opacity="0.85" />
                <circle cx="100" cy="100" r="18" fill="#FF6D1F" />
                <circle cx="100" cy="100" r="6" fill="#091413" />
                {/* Crosshair */}
                <line x1="100" y1="10" x2="100" y2="35" stroke="#FF6D1F" strokeWidth="3" />
                <line x1="100" y1="165" x2="100" y2="190" stroke="#FF6D1F" strokeWidth="3" />
                <line x1="10" y1="100" x2="35" y2="100" stroke="#FF6D1F" strokeWidth="3" />
                <line x1="165" y1="100" x2="190" y2="100" stroke="#FF6D1F" strokeWidth="3" />
              </svg>
            </div>

            {/* Text */}
            <div>
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 24,
                  color: '#FF6D1F',
                  letterSpacing: 5,
                  fontWeight: 800,
                  marginBottom: 8,
                }}
              >
                THE REALITY
              </div>
              <div
                style={{
                  fontFamily: 'Yantra One',
                  fontSize: 110,
                  color: '#F5F5F5',
                  fontWeight: 800,
                  lineHeight: 1,
                  letterSpacing: -2,
                }}
              >
                वो <span style={{ color: '#FF6D1F' }}>एक्यूरेट</span>
              </div>
              <div
                style={{
                  fontFamily: 'Yantra One',
                  fontSize: 60,
                  color: '#F5F5F5',
                  fontWeight: 700,
                  lineHeight: 1.1,
                  marginTop: 6,
                }}
              >
                चल रहा है
              </div>
            </div>
          </div>
        </div>

        {/* Bottom accuracy comparison */}
        <div
          style={{
            position: 'absolute',
            bottom: 50,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(340),
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 30,
              padding: '12px 36px',
              background: 'rgba(9,20,19,0.7)',
              border: '1px solid rgba(255,109,31,0.4)',
              borderRadius: 40,
            }}
          >
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E' }}>
              पहले: <span style={{ color: '#F5F5F5', fontWeight: 700 }}>85-90%</span>
            </div>
            <div style={{ width: 30, height: 2, background: '#FF6D1F' }} />
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#FF6D1F', fontWeight: 800 }}>
              अब: 99-100%
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(0,0,0,0) 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_NotFastButAccurate;
```

