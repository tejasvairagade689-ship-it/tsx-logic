```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1TruthBomb',
  durationInSeconds: 4,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1TruthBomb: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const word1 = sp(10);
  const word2 = sp(30);
  const word3 = sp(55);
  const underline = interpolate(sp(70), [0, 1], [0, 1]);
  const shake = Math.sin(frame * 0.8) * (frame > 70 && frame < 90 ? 4 : 0);
  const bgPulse = 1 + Math.sin(frame * 0.04) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFDE42', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(255,222,66,0) 0%, rgba(31,21,12,0.15) 100%)',
      }} />

      {/* Floating shape layers */}
      {[0, 1, 2, 3, 4].map((i) => {
        const x = seededRandom(i + 1) * 1920;
        const y = seededRandom(i + 7) * 1080;
        const size = 200 + seededRandom(i + 3) * 300;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: '3px solid #1F150C',
            borderRadius: i % 2 === 0 ? '50%' : '0%',
            opacity: 0.06,
            transform: `rotate(${frame * 0.2 + i * 30}deg) scale(${bgPulse})`,
          }} />
        );
      })}

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={i} x1={i * 100 - 200} y1={0} x2={i * 100 + 300} y2={1080} stroke="#1F150C" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = (seededRandom(i + 20) * 1920 + frame * 0.5) % 1920;
        const py = (seededRandom(i + 30) * 1080 + Math.sin(frame * 0.02 + i) * 30);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 6,
            height: 6,
            backgroundColor: '#1F150C',
            borderRadius: '50%',
            opacity: 0.3,
          }} />
        );
      })}

      {/* Main content */}
      <div style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        transform: `translate(-50%, -50%) translateX(${shake}px)`,
        textAlign: 'center',
        width: '90%',
      }}>
        <div style={{
          fontFamily: "'Yantra One', sans-serif",
          fontSize: 102,
          color: '#1F150C',
          opacity: word1,
          transform: `translateY(${(1 - word1) * 30}px)`,
          letterSpacing: '-2px',
        }}>
          एक सच्ची बात
        </div>
        <div style={{
          fontFamily: "'Yantra One', sans-serif",
          fontSize: 187,
          color: '#080616',
          opacity: word2,
          transform: `scale(${0.7 + word2 * 0.3})`,
          marginTop: 20,
          lineHeight: 1,
          fontWeight: 900,
        }}>
          बताऊं<span style={{ color: '#ff0000' }}>?</span>
        </div>
        <div style={{
          fontFamily: "'Noto Sans Devanagari', sans-serif",
          fontSize: 51,
          color: '#1F150C',
          opacity: word3,
          marginTop: 40,
          fontWeight: 600,
        }}>
          थोड़ी <span style={{
            position: 'relative',
            display: 'inline-block',
            color: '#ff0000',
          }}>
            चुभेगी
            <div style={{
              position: 'absolute',
              bottom: -8,
              left: 0,
              height: 6,
              width: `${underline * 100}%`,
              backgroundColor: '#ff0000',
            }} />
          </span> भी।
        </div>
      </div>

      {/* Corner accent */}
      <div style={{
        position: 'absolute',
        top: 40,
        left: 40,
        fontFamily: "'JetBrains Mono', monospace",
        fontSize: 25,
        color: '#1F150C',
        opacity: 0.5,
        letterSpacing: '4px',
      }}>
        TRUTH · 001
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#1F150C',
        opacity: 0.4,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1TruthBomb;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2NeedlePain',
  durationInSeconds: 5,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2NeedlePain: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const brainScale = sp(15);
  const needleX = interpolate(sp(40), [0, 1], [-400, 0]);
  const impactPulse = interpolate(frame, [70, 80, 90], [0, 1.4, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const text1 = sp(90);
  const text2 = sp(115);
  const ripple1 = (frame - 80) > 0 ? Math.min((frame - 80) * 8, 400) : 0;
  const ripple2 = (frame - 95) > 0 ? Math.min((frame - 95) * 8, 400) : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #DFF1F1 0%, #FFDE42 200%)',
        opacity: 0.6,
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        {Array.from({ length: 25 }).map((_, i) => (
          <line key={`v${i}`} x1={i * 80} y1={0} x2={i * 80} y2={1080} stroke="#1F150C" strokeWidth="0.5" />
        ))}
        {Array.from({ length: 15 }).map((_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 80} x2={1920} y2={i * 80} stroke="#1F150C" strokeWidth="0.5" />
        ))}
      </svg>

      {/* Shape layers */}
      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: 200 + i * 600,
          top: 100 + i * 300,
          width: 350,
          height: 350,
          border: '2px solid #1F150C',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `rotate(${frame * 0.3 + i * 45}deg)`,
        }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 50) * 1920;
        const py = (seededRandom(i + 60) * 1080 + frame * 0.3) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 5,
            height: 5,
            backgroundColor: '#1F150C',
            borderRadius: '50%',
            opacity: 0.25,
          }} />
        );
      })}

      {/* Brain (circle with lines) */}
      <div style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        transform: `translate(-50%, -50%) scale(${brainScale * impactPulse || brainScale})`,
      }}>
        <svg width="500" height="500" viewBox="0 0 500 500">
          {/* Ripples */}
          {ripple1 > 0 && (
            <circle cx="250" cy="250" r={ripple1} fill="none" stroke="#ff0000" strokeWidth="3" opacity={Math.max(0, 1 - ripple1 / 400)} />
          )}
          {ripple2 > 0 && (
            <circle cx="250" cy="250" r={ripple2} fill="none" stroke="#ff0000" strokeWidth="3" opacity={Math.max(0, 1 - ripple2 / 400)} />
          )}
          <circle cx="250" cy="250" r="180" fill="#FFDE42" stroke="#1F150C" strokeWidth="6" />
          <path d="M 130 250 Q 250 130, 370 250 Q 250 370, 130 250" fill="none" stroke="#1F150C" strokeWidth="4" />
          <path d="M 250 130 Q 320 250, 250 370" fill="none" stroke="#1F150C" strokeWidth="3" />
          <path d="M 180 180 Q 250 250, 180 320" fill="none" stroke="#1F150C" strokeWidth="3" />
          <path d="M 320 180 Q 250 250, 320 320" fill="none" stroke="#1F150C" strokeWidth="3" />
          <text x="250" y="270" textAnchor="middle" fontFamily="Yantra One" fontSize="60" fill="#1F150C" fontWeight="900">दिमाग़</text>
        </svg>
      </div>

      {/* Needle */}
      <svg width="400" height="60" style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        transform: `translate(${needleX - 300}px, -50%)`,
      }}>
        <line x1="0" y1="30" x2="350" y2="30" stroke="#1F150C" strokeWidth="8" />
        <polygon points="350,15 390,30 350,45" fill="#ff0000" />
        <circle cx="10" cy="30" r="15" fill="#1F150C" />
      </svg>

      {/* Text */}
      <div style={{
        position: 'absolute',
        top: 80,
        left: '50%',
        transform: `translateX(-50%) translateY(${(1 - text1) * 30}px)`,
        fontFamily: "'Yantra One', sans-serif",
        fontSize: 76,
        color: '#1F150C',
        opacity: text1,
        textAlign: 'center',
      }}>
        चुभनी <span style={{ color: '#ff0000' }}>ज़रूरी</span> है
      </div>

      <div style={{
        position: 'absolute',
        bottom: 100,
        left: '50%',
        transform: `translateX(-50%) translateY(${(1 - text2) * 30}px)`,
        fontFamily: "'Noto Sans Devanagari', sans-serif",
        fontSize: 51,
        color: '#080616',
        opacity: text2,
        textAlign: 'center',
        fontWeight: 700,
        maxWidth: 1400,
      }}>
        जब तक चुभती नहीं → दिमाग़ में उतरती नहीं
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.4,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene2NeedlePain;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3BiggestLie',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3BiggestLie: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const tag = sp(10);
  const lieWord = sp(35);
  const lieScale = 1 + Math.sin(frame * 0.15) * 0.04;
  const strike = interpolate(sp(120), [0, 1], [0, 1]);
  const newText = sp(150);
  const arrowProg = interpolate(sp(170), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFDE42', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 70%, #DFF1F1 0%, transparent 60%)',
        opacity: 0.5,
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 1) * 1700,
          top: seededRandom(i + 5) * 900,
          width: 250 + i * 80,
          height: 250 + i * 80,
          border: '3px solid #080616',
          opacity: 0.04,
          transform: `rotate(${frame * 0.2 + i * 60}deg)`,
          borderRadius: i % 2 === 0 ? '50%' : '15%',
        }} />
      ))}

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.12 }}>
        {Array.from({ length: 20 }).map((_, i) =>
          Array.from({ length: 12 }).map((_, j) => (
            <circle key={`${i}-${j}`} cx={i * 100 + 50} cy={j * 100 + 50} r="2" fill="#1F150C" />
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (seededRandom(i + 70) * 1920 + frame * 0.4) % 1920,
          top: seededRandom(i + 80) * 1080,
          width: 4, height: 4,
          backgroundColor: '#1F150C', borderRadius: '50%', opacity: 0.3,
        }} />
      ))}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 70, left: '50%',
        transform: `translateX(-50%) scale(${tag})`,
        fontFamily: "'JetBrains Mono', monospace",
        fontSize: 30,
        color: '#1F150C',
        letterSpacing: '8px',
        backgroundColor: '#080616',
        color: '#FFDE42',
        padding: '12px 32px',
      }}>
        भारत का सबसे बड़ा झूठ
      </div>

      {/* Big LIE word with stroke through */}
      <div style={{
        position: 'absolute',
        top: 220,
        left: '50%',
        transform: `translateX(-50%) scale(${lieScale})`,
        opacity: lieWord,
        fontFamily: "'Yantra One', sans-serif",
        fontSize: 221,
        color: '#1F150C',
        fontWeight: 900,
        position: 'relative',
      }}>
        <div style={{ position: 'relative', display: 'inline-block' }}>
          "अमीर बनना मुश्किल है"
          <div style={{
            position: 'absolute',
            top: '55%',
            left: 0,
            width: `${strike * 100}%`,
            height: 14,
            backgroundColor: '#ff0000',
            transform: 'translateY(-50%)',
          }} />
        </div>
      </div>

      {/* X mark */}
      <div style={{
        position: 'absolute',
        top: 470,
        left: '50%',
        transform: `translateX(-50%) scale(${strike})`,
        fontSize: 102,
        color: '#ff0000',
        fontWeight: 900,
        fontFamily: 'Inter, sans-serif',
      }}>
        ✗ ग़लत
      </div>

      {/* Arrow down */}
      <svg width="80" height="120" style={{
        position: 'absolute',
        top: 620,
        left: '50%',
        transform: `translateX(-50%) scaleY(${arrowProg})`,
        transformOrigin: 'top',
      }}>
        <line x1="40" y1="0" x2="40" y2="90" stroke="#080616" strokeWidth="8" />
        <polygon points="20,80 40,115 60,80" fill="#080616" />
      </svg>

      {/* Bottom truth */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: '50%',
        transform: `translateX(-50%) scale(${newText})`,
        fontFamily: "'Yantra One', sans-serif",
        fontSize: 68,
        color: '#080616',
        textAlign: 'center',
        opacity: newText,
        backgroundColor: '#FFDE42',
        border: '5px solid #080616',
        padding: '20px 50px',
      }}>
        असली झूठ कुछ और है →
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.4,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene3BiggestLie;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4OldRoadmap',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4OldRoadmap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(8);
  const map = sp(30);
  const stamp = sp(110);
  const stampRot = interpolate(sp(110), [0, 1], [-30, -12]);
  const conclusion = sp(150);

  // Roadmap path
  const pathDraw = interpolate(sp(40), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #DFF1F1 0%, #FFDE42 200%)',
        opacity: 0.5,
      }} />

      {/* Shape layers */}
      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: 100 + i * 700,
          top: 200 + i * 200,
          width: 400, height: 400,
          border: '3px solid #1F150C',
          opacity: 0.04,
          borderRadius: '20px',
          transform: `rotate(${frame * 0.15 + i * 30}deg)`,
        }} />
      ))}

      {/* Texture - circles */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <circle key={i} cx={200 + i * 200} cy={540} r={150 + i * 20} fill="none" stroke="#1F150C" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 100) * 1920,
          top: (seededRandom(i + 200) * 1080 + frame * 0.5) % 1080,
          width: 5, height: 5,
          backgroundColor: '#1F150C', borderRadius: '50%', opacity: 0.25,
        }} />
      ))}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 60, left: '50%',
        transform: `translateX(-50%) scale(${title})`,
        fontFamily: "'Yantra One', sans-serif",
        fontSize: 76, color: '#1F150C',
        opacity: title, textAlign: 'center',
      }}>
        बचपन का पुराना <span style={{ color: '#ff0000' }}>नक्शा</span>
      </div>

      {/* Roadmap with stops */}
      <div style={{
        position: 'absolute', top: 280, left: 130, right: 130,
        opacity: map,
      }}>
        <svg width="1660" height="500" viewBox="0 0 1660 500">
          {/* Dashed path */}
          <path
            d="M 80 250 Q 350 100, 600 250 T 1200 250 T 1580 250"
            fill="none"
            stroke="#080616"
            strokeWidth="6"
            strokeDasharray="20 15"
            strokeDashoffset={(1 - pathDraw) * 2000}
          />

          {/* Stops */}
          {[
            { x: 80, y: 250, label: 'पढ़ाई', delay: 50 },
            { x: 480, y: 150, label: 'अच्छे नंबर', delay: 65 },
            { x: 880, y: 280, label: 'नौकरी', delay: 80 },
            { x: 1280, y: 200, label: 'FD', delay: 90 },
            { x: 1580, y: 250, label: 'रिटायर', delay: 100 },
          ].map((s, i) => {
            const a = sp(s.delay);
            return (
              <g key={i} opacity={a}>
                <circle cx={s.x} cy={s.y} r={28 * a} fill="#FFDE42" stroke="#1F150C" strokeWidth="5" />
                <text x={s.x} y={s.y + 8} textAnchor="middle" fontFamily="Yantra One" fontSize="24" fill="#1F150C" fontWeight="900">{i + 1}</text>
                <text x={s.x} y={s.y + 75} textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="34" fill="#080616" fontWeight="700">{s.label}</text>
              </g>
            );
          })}
        </svg>
      </div>

      {/* OUTDATED STAMP */}
      <div style={{
        position: 'absolute',
        top: 350,
        left: '50%',
        transform: `translateX(-50%) rotate(${stampRot}deg) scale(${stamp})`,
        opacity: stamp * 0.85,
        border: '8px solid #ff0000',
        padding: '18px 48px',
        fontFamily: "'Yantra One', sans-serif",
        fontSize: 119,
        color: '#ff0000',
        backgroundColor: 'transparent',
        fontWeight: 900,
        letterSpacing: '8px',
      }}>
        EXPIRED
      </div>

      {/* Bottom conclusion */}
      <div style={{
        position: 'absolute', bottom: 80, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - conclusion) * 30}px)`,
        opacity: conclusion,
        fontFamily: "'Noto Sans Devanagari', sans-serif",
        fontSize: 47, color: '#080616', fontWeight: 700,
        textAlign: 'center',
      }}>
        लेकिन क्या वो नक्शा <span style={{ color: '#ff0000' }}>आज भी</span> काम करता है?
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.4,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene4OldRoadmap;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5AdviceChorus',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5AdviceChorus: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(8);
  const b1 = sp(30);
  const b2 = sp(85);
  const b3 = sp(140);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFDE42', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 80% 20%, #DFF1F1 0%, transparent 50%)',
        opacity: 0.6,
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 1) * 1500,
          top: seededRandom(i + 5) * 800,
          width: 300 + i * 60,
          height: 300 + i * 60,
          borderRadius: '50%',
          border: '3px solid #1F150C',
          opacity: 0.05,
          transform: `rotate(${frame * 0.2 + i * 45}deg) scale(${1 + Math.sin(frame * 0.03 + i) * 0.05})`,
        }} />
      ))}

      {/* Lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 15 }).map((_, i) => (
          <line key={i} x1={0} y1={i * 80} x2={1920} y2={i * 80} stroke="#1F150C" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (seededRandom(i + 90) * 1920 + frame * 0.6) % 1920,
          top: seededRandom(i + 110) * 1080,
          width: 4, height: 4,
          backgroundColor: '#1F150C', borderRadius: '50%', opacity: 0.25,
        }} />
      ))}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 50, left: '50%',
        transform: `translateX(-50%) scale(${title})`,
        fontFamily: "'JetBrains Mono', monospace",
        fontSize: 30, color: '#FFDE42',
        backgroundColor: '#080616',
        padding: '12px 32px',
        letterSpacing: '6px',
        opacity: title,
      }}>
        सबकी सलाह
      </div>

      {/* 3 quote bubbles */}
      <div style={{ position: 'absolute', top: 180, left: 0, right: 0, padding: '0 80px' }}>
        {/* Bubble 1 - Mom/Dad */}
        <div style={{
          opacity: b1,
          transform: `translateX(${(1 - b1) * -200}px)`,
          backgroundColor: '#DFF1F1',
          border: '5px solid #080616',
          borderRadius: '30px',
          padding: '30px 50px',
          marginBottom: 35,
          display: 'flex',
          alignItems: 'center',
          gap: 30,
          maxWidth: 1500,
          position: 'relative',
        }}>
          <div style={{
            width: 100, height: 100, borderRadius: '50%',
            backgroundColor: '#FFDE42', border: '5px solid #080616',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Yantra One', fontSize: 47, color: '#080616', flexShrink: 0,
          }}>👪</div>
          <div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 23, color: '#ff0000', letterSpacing: 3, marginBottom: 8 }}>मम्मी-पापा</div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 61, color: '#1F150C', lineHeight: 1.1 }}>
              "बेटा, पढ़ाई कर लो"
            </div>
          </div>
        </div>

        {/* Bubble 2 - Teacher */}
        <div style={{
          opacity: b2,
          transform: `translateX(${(1 - b2) * 200}px)`,
          backgroundColor: '#FFDE42',
          border: '5px solid #080616',
          borderRadius: '30px',
          padding: '30px 50px',
          marginBottom: 35,
          marginLeft: 200,
          display: 'flex',
          alignItems: 'center',
          gap: 30,
          maxWidth: 1300,
        }}>
          <div style={{
            width: 100, height: 100, borderRadius: '50%',
            backgroundColor: '#DFF1F1', border: '5px solid #080616',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Yantra One', fontSize: 38, color: '#080616', flexShrink: 0,
          }}>T</div>
          <div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 23, color: '#ff0000', letterSpacing: 3, marginBottom: 8 }}>टीचर</div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 61, color: '#1F150C', lineHeight: 1.1 }}>
              "अच्छे नंबर लाओ"
            </div>
          </div>
        </div>

        {/* Bubble 3 - Relatives */}
        <div style={{
          opacity: b3,
          transform: `translateX(${(1 - b3) * -200}px)`,
          backgroundColor: '#080616',
          border: '5px solid #080616',
          borderRadius: '30px',
          padding: '30px 50px',
          marginLeft: 80,
          display: 'flex',
          alignItems: 'center',
          gap: 30,
          maxWidth: 1500,
        }}>
          <div style={{
            width: 100, height: 100, borderRadius: '50%',
            backgroundColor: '#FFDE42', border: '5px solid #FFDE42',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Yantra One', fontSize: 38, color: '#080616', flexShrink: 0,
          }}>R</div>
          <div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 23, color: '#FFDE42', letterSpacing: 3, marginBottom: 8 }}>रिश्तेदार</div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 55, color: '#FFDE42', lineHeight: 1.1 }}>
              "सरकारी नौकरी - ज़िंदगी सेट!"
            </div>
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.5,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene5AdviceChorus;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6FriendsBank',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6FriendsBank: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const leftCard = sp(15);
  const rightCard = sp(90);
  const vs = sp(180);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, #DFF1F1 0%, #FFDE42 100%)',
        opacity: 0.4,
      }} />

      {/* Diagonal split line */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.15 }}>
        <line x1="960" y1="0" x2="960" y2="1080" stroke="#1F150C" strokeWidth="3" strokeDasharray="20 10" />
      </svg>

      {/* Shape layers */}
      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: i * 700 + 100,
          top: 50 + i * 200,
          width: 300, height: 300,
          border: '3px solid #1F150C', opacity: 0.05,
          transform: `rotate(${frame * 0.2 + i * 60}deg)`,
        }} />
      ))}

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (seededRandom(i + 30) * 1920 + frame * 0.4) % 1920,
          top: seededRandom(i + 60) * 1080,
          width: 4, height: 4, backgroundColor: '#1F150C', borderRadius: '50%', opacity: 0.25,
        }} />
      ))}

      {/* LEFT: Friends */}
      <div style={{
        position: 'absolute',
        top: 130, left: 80,
        width: 800,
        opacity: leftCard,
        transform: `translateX(${(1 - leftCard) * -100}px)`,
      }}>
        <div style={{
          fontFamily: "'JetBrains Mono', monospace",
          fontSize: 26, color: '#FFDE42', backgroundColor: '#080616',
          padding: '10px 24px', letterSpacing: 4, display: 'inline-block',
          marginBottom: 30,
        }}>
          दोस्त की सलाह
        </div>
        <div style={{
          fontFamily: "'Yantra One', sans-serif",
          fontSize: 110, color: '#1F150C', lineHeight: 1, marginBottom: 25,
        }}>
          "एंजॉय<br />कर ले!"
        </div>
        <div style={{
          fontFamily: "'Noto Sans Devanagari', sans-serif",
          fontSize: 38, color: '#080616', fontWeight: 700,
          padding: '20px',
          backgroundColor: '#FFDE42',
          border: '4px solid #080616',
          display: 'inline-block',
        }}>
          🍻 जवानी एक बार आती है
        </div>
        {/* Animated drinks/spending icons */}
        <div style={{ marginTop: 30, display: 'flex', gap: 20 }}>
          {['पार्टी', 'शॉपिंग', 'ट्रिप'].map((t, i) => {
            const a = sp(40 + i * 12);
            return (
              <div key={i} style={{
                opacity: a,
                fontFamily: 'Noto Sans Devanagari', fontSize: 28,
                padding: '14px 24px',
                backgroundColor: '#DFF1F1',
                border: '3px solid #1F150C',
                borderRadius: 8,
                color: '#1F150C', fontWeight: 700,
                transform: `translateY(${(1 - a) * 20}px)`,
              }}>{t}</div>
            );
          })}
        </div>
      </div>

      {/* RIGHT: Bank Uncle */}
      <div style={{
        position: 'absolute',
        top: 130, right: 80,
        width: 800,
        opacity: rightCard,
        transform: `translateX(${(1 - rightCard) * 100}px)`,
      }}>
        <div style={{
          fontFamily: "'JetBrains Mono', monospace",
          fontSize: 26, color: '#FFDE42', backgroundColor: '#080616',
          padding: '10px 24px', letterSpacing: 4, display: 'inline-block',
          marginBottom: 30,
        }}>
          बैंक अंकल
        </div>
        <div style={{
          fontFamily: "'Yantra One', sans-serif",
          fontSize: 110, color: '#1F150C', lineHeight: 1, marginBottom: 25,
        }}>
          "FD में<br />डाल दो!"
        </div>
        {/* FD certificate card */}
        <div style={{
          backgroundColor: '#FFDE42',
          border: '5px solid #080616',
          padding: '24px 32px',
          fontFamily: 'Yantra One',
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 19, color: '#1F150C', letterSpacing: 3, marginBottom: 10 }}>
            FIXED DEPOSIT · CERT
          </div>
          <div style={{ fontSize: 51, color: '#1F150C', fontWeight: 900 }}>
            6.5% <span style={{ fontSize: 28, color: '#080616' }}>p.a.</span>
          </div>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#080616', marginTop: 8 }}>
            "सुरक्षित रहेगा"
          </div>
          <div style={{
            marginTop: 14, height: 8, backgroundColor: '#080616', borderRadius: 4,
            position: 'relative',
          }}>
            <div style={{
              position: 'absolute', top: 0, left: 0,
              width: '30%', height: '100%', backgroundColor: '#ff0000',
            }} />
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 18, color: '#ff0000', marginTop: 6 }}>
            ↓ महंगाई से कम
          </div>
        </div>
      </div>

      {/* VS center */}
      <div style={{
        position: 'absolute',
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) scale(${vs})`,
        width: 140, height: 140, borderRadius: '50%',
        backgroundColor: '#ff0000', color: '#FFDE42',
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        fontFamily: 'Yantra One', fontSize: 64, fontWeight: 900,
        border: '6px solid #080616',
        boxShadow: '0 0 0 8px #FFDE42',
      }}>
        VS
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.4,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene6FriendsBank;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7DeadMap',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7DeadMap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const t1 = sp(8);
  const heart = sp(40);
  const heartPulse = 1 + Math.sin(frame * 0.15) * 0.06;
  const but = sp(100);
  const mapShow = sp(140);
  const cracks = interpolate(sp(200), [0, 1], [0, 1]);
  const ended = sp(240);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFDE42', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 100%, #DFF1F1 0%, transparent 60%)',
        opacity: 0.5,
      }} />

      {[0, 1, 2, 3].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 1) * 1500,
          top: seededRandom(i + 5) * 800,
          width: 280 + i * 50, height: 280 + i * 50,
          border: '3px solid #1F150C', opacity: 0.04,
          borderRadius: i % 2 ? '50%' : '20%',
          transform: `rotate(${frame * 0.2 + i * 50}deg)`,
        }} />
      ))}

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 18 }).map((_, i) => (
          <circle key={i} cx={100 + i * 110} cy={540} r="6" fill="#1F150C" />
        ))}
      </svg>

      {Array.from({ length: 16 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (seededRandom(i + 70) * 1920 + frame * 0.4) % 1920,
          top: seededRandom(i + 80) * 1080,
          width: 4, height: 4, backgroundColor: '#1F150C', borderRadius: '50%', opacity: 0.25,
        }} />
      ))}

      {/* Top: They love you */}
      <div style={{
        position: 'absolute', top: 70, left: '50%',
        transform: `translateX(-50%) scale(${t1})`,
        opacity: t1,
        fontFamily: "'Yantra One', sans-serif",
        fontSize: 68, color: '#080616', textAlign: 'center',
      }}>
        ये सब आपके <span style={{ color: '#ff0000' }}>दुश्मन नहीं</span> हैं
      </div>

      {/* Heart */}
      <svg width="180" height="160" style={{
        position: 'absolute',
        top: 200, left: '50%',
        transform: `translateX(-50%) scale(${heart * heartPulse})`,
      }}>
        <path d="M 90 145 C 90 145, 15 95, 15 50 C 15 25, 35 10, 55 10 C 70 10, 85 20, 90 35 C 95 20, 110 10, 125 10 C 145 10, 165 25, 165 50 C 165 95, 90 145, 90 145 Z"
          fill="#ff0000" stroke="#080616" strokeWidth="5" />
      </svg>

      {/* BUT */}
      <div style={{
        position: 'absolute', top: 420, left: '50%',
        transform: `translateX(-50%) scale(${but})`,
        opacity: but,
        fontFamily: "'Yantra One', sans-serif",
        fontSize: 102, color: '#1F150C', fontWeight: 900,
        backgroundColor: '#080616', color: '#FFDE42',
        padding: '8px 40px',
      }}>
        लेकिन...
      </div>

      {/* Old broken map */}
      <div style={{
        position: 'absolute', top: 580, left: '50%',
        transform: `translateX(-50%) scale(${mapShow})`,
        opacity: mapShow,
      }}>
        <svg width="900" height="380" viewBox="0 0 900 380">
          {/* Map rectangle */}
          <rect x="20" y="20" width="860" height="340" fill="#DFF1F1" stroke="#080616" strokeWidth="6" />
          {/* Old map texture */}
          <text x="60" y="80" fontFamily="JetBrains Mono" fontSize="22" fill="#080616" opacity="0.6">MAP · 1985</text>

          {/* Roads */}
          <path d="M 80 200 Q 250 100, 450 200 T 820 200" fill="none" stroke="#080616" strokeWidth="4" />
          <circle cx="80" cy="200" r="12" fill="#1F150C" />
          <circle cx="450" cy="200" r="12" fill="#1F150C" />
          <circle cx="820" cy="200" r="12" fill="#1F150C" />

          {/* Labels */}
          <text x="80" y="250" fontFamily="Yantra One" fontSize="24" fill="#1F150C">पेंशन</text>
          <text x="420" y="250" fontFamily="Yantra One" fontSize="24" fill="#1F150C">गारंटी</text>
          <text x="780" y="250" fontFamily="Yantra One" fontSize="24" fill="#1F150C">सुरक्षा</text>

          {/* Cracks - growing red lines */}
          <line x1="100" y1="40" x2={100 + cracks * 700} y2={40 + cracks * 280} stroke="#ff0000" strokeWidth="5" />
          <line x1="800" y1="60" x2={800 - cracks * 500} y2={60 + cracks * 260} stroke="#ff0000" strokeWidth="4" />
          <line x1="450" y1="20" x2={450 - cracks * 200} y2={20 + cracks * 320} stroke="#ff0000" strokeWidth="4" />
        </svg>
      </div>

      {/* ENDED stamp */}
      <div style={{
        position: 'absolute',
        top: 700, left: '50%',
        transform: `translateX(-50%) rotate(-8deg) scale(${ended})`,
        opacity: ended,
        border: '6px solid #ff0000',
        padding: '12px 36px',
        fontFamily: "'Yantra One', sans-serif",
        fontSize: 76, color: '#ff0000', fontWeight: 900,
        letterSpacing: '6px',
        backgroundColor: 'rgba(255,222,66,0.9)',
      }}>
        वो दुनिया ख़त्म
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.4,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene7DeadMap;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8OldEra',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8OldEra: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(8);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #DFF1F1 0%, rgba(255,222,66,0.3) 100%)',
      }} />

      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 1) * 1500,
          top: seededRandom(i + 5) * 800,
          width: 350, height: 350,
          border: '3px solid #1F150C', opacity: 0.04,
          borderRadius: '20%',
          transform: `rotate(${frame * 0.15 + i * 60}deg)`,
        }} />
      ))}

      {/* Vintage paper grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 25 }).map((_, i) => (
          <line key={`v${i}`} x1={i * 80} y1={0} x2={i * 80} y2={1080} stroke="#1F150C" strokeWidth="0.5" />
        ))}
        {Array.from({ length: 15 }).map((_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 80} x2={1920} y2={i * 80} stroke="#1F150C" strokeWidth="0.5" />
        ))}
      </svg>

      {Array.from({ length: 14 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (seededRandom(i + 90) * 1920 + frame * 0.3) % 1920,
          top: seededRandom(i + 110) * 1080,
          width: 4, height: 4, backgroundColor: '#1F150C', borderRadius: '50%', opacity: 0.2,
        }} />
      ))}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 50, left: '50%',
        transform: `translateX(-50%) scale(${title})`,
        opacity: title,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: "'JetBrains Mono', monospace", fontSize: 26,
          color: '#1F150C', letterSpacing: 6, marginBottom: 10,
        }}>
          ERA · 1960 — 2000
        </div>
        <div style={{
          fontFamily: "'Yantra One', sans-serif", fontSize: 81,
          color: '#080616',
        }}>
          वो पुराना ज़माना
        </div>
      </div>

      {/* 4 cards timeline */}
      <div style={{
        position: 'absolute', top: 280, left: 60, right: 60,
        display: 'flex', gap: 25,
      }}>
        {[
          { num: '40', label: 'साल एक फैक्ट्री', sub: 'एक ही जॉब' },
          { num: '₹', label: 'पेंशन', sub: 'गारंटीड' },
          { num: '🏠', label: 'पुश्तैनी मकान', sub: 'बाप → बेटा' },
          { num: '60', label: 'पर रिटायर', sub: 'आराम' },
        ].map((c, i) => {
          const a = sp(30 + i * 25);
          return (
            <div key={i} style={{
              flex: 1,
              opacity: a,
              transform: `translateY(${(1 - a) * 60}px)`,
              backgroundColor: i % 2 === 0 ? '#FFDE42' : '#DFF1F1',
              border: '5px solid #080616',
              padding: '40px 25px',
              textAlign: 'center',
              minHeight: 380,
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'space-between',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 19,
                color: '#ff0000', letterSpacing: 3,
              }}>
                STEP 0{i + 1}
              </div>
              <div style={{
                fontFamily: "'Yantra One', sans-serif",
                fontSize: 136, color: '#1F150C', lineHeight: 1, fontWeight: 900,
              }}>
                {c.num}
              </div>
              <div>
                <div style={{
                  fontFamily: 'Yantra One', fontSize: 36,
                  color: '#080616', lineHeight: 1.1,
                }}>{c.label}</div>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari', fontSize: 25,
                  color: '#1F150C', marginTop: 8, opacity: 0.7,
                }}>{c.sub}</div>
              </div>
            </div>
          );
        })}
      </div>

      {/* Bottom stamp */}
      <div style={{
        position: 'absolute', bottom: 60, left: '50%',
        transform: `translateX(-50%) scale(${sp(160)})`,
        opacity: sp(160),
        fontFamily: "'Yantra One', sans-serif",
        fontSize: 51, color: '#FFDE42',
        backgroundColor: '#080616',
        padding: '16px 40px',
        border: '4px solid #ff0000',
      }}>
        ये सब अब <span style={{ color: '#ff0000' }}>नहीं है</span>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.4,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene8OldEra;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9NewReality',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9NewReality: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const banner = sp(8);
  const x1 = sp(35);
  const x2 = sp(70);
  const graphProg = interpolate(sp(110), [0, 1], [0, 1]);
  const conclusion = sp(180);

  // Price graph data
  const pricePoints = [
    { x: 50, y: 280, label: '2000' },
    { x: 200, y: 250, label: '2005' },
    { x: 350, y: 200, label: '2010' },
    { x: 500, y: 140, label: '2015' },
    { x: 650, y: 80, label: '2020' },
    { x: 800, y: 30, label: '2025' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFDE42', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 20% 30%, rgba(255,0,0,0.08) 0%, transparent 50%)',
      }} />

      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 1) * 1500,
          top: seededRandom(i + 5) * 800,
          width: 300, height: 300,
          border: '3px solid #1F150C', opacity: 0.05,
          borderRadius: '50%',
          transform: `rotate(${frame * 0.2 + i * 60}deg)`,
        }} />
      ))}

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <line key={i} x1={i * 140} y1={0} x2={i * 140 + 200} y2={1080} stroke="#1F150C" strokeWidth="1" />
        ))}
      </svg>

      {Array.from({ length: 16 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (seededRandom(i + 90) * 1920 + frame * 0.5) % 1920,
          top: seededRandom(i + 110) * 1080,
          width: 5, height: 5, backgroundColor: '#1F150C', borderRadius: '50%', opacity: 0.25,
        }} />
      ))}

      {/* Top banner */}
      <div style={{
        position: 'absolute', top: 50, left: '50%',
        transform: `translateX(-50%) scale(${banner})`,
        opacity: banner,
        backgroundColor: '#ff0000', color: '#FFDE42',
        fontFamily: "'Yantra One', sans-serif",
        fontSize: 64, padding: '14px 50px',
        letterSpacing: 2, border: '5px solid #080616',
      }}>
        आज की कड़वी हक़ीक़त
      </div>

      {/* Left side: X marks */}
      <div style={{
        position: 'absolute', top: 220, left: 100, width: 700,
      }}>
        {[
          { label: 'पेंशन', delay: 35 },
          { label: 'नौकरी की गारंटी', delay: 70 },
        ].map((item, i) => {
          const a = i === 0 ? x1 : x2;
          return (
            <div key={i} style={{
              opacity: a,
              transform: `translateX(${(1 - a) * -100}px)`,
              display: 'flex', alignItems: 'center', gap: 25,
              marginBottom: 35,
              backgroundColor: '#080616',
              padding: '24px 32px',
              border: '4px solid #ff0000',
            }}>
              <div style={{
                width: 80, height: 80, borderRadius: '50%',
                backgroundColor: '#ff0000', color: '#FFDE42',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                fontFamily: 'Yantra One', fontSize: 51, fontWeight: 900,
              }}>✗</div>
              <div>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 21, color: '#ff0000', letterSpacing: 3 }}>
                  GONE · ख़त्म
                </div>
                <div style={{ fontFamily: 'Yantra One', fontSize: 72, color: '#FFDE42', lineHeight: 1 }}>
                  {item.label}
                </div>
              </div>
            </div>
          );
        })}
      </div>

      {/* Right: Property price graph */}
      <div style={{
        position: 'absolute', top: 220, right: 80,
        width: 900, height: 420,
        backgroundColor: '#DFF1F1',
        border: '5px solid #080616',
        padding: 30,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 22, color: '#ff0000', letterSpacing: 3,
        }}>PROPERTY PRICES</div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 47, color: '#1F150C', marginTop: 4,
        }}>मकान के दाम ↑↑↑</div>

        <svg width="840" height="280" viewBox="0 0 840 320" style={{ marginTop: 14 }}>
          {/* Grid */}
          {[0, 1, 2, 3, 4].map(i => (
            <line key={i} x1={0} y1={i * 70} x2={840} y2={i * 70} stroke="#1F150C" strokeWidth="1" opacity="0.2" />
          ))}
          {/* Axis */}
          <line x1={0} y1={300} x2={840} y2={300} stroke="#080616" strokeWidth="3" />

          {/* Animated bars */}
          {pricePoints.map((p, i) => {
            const barHeight = (300 - p.y) * graphProg;
            return (
              <g key={i}>
                <rect
                  x={p.x - 30}
                  y={300 - barHeight}
                  width={60}
                  height={barHeight}
                  fill={i >= 4 ? '#ff0000' : '#1F150C'}
                />
                <text x={p.x} y={315} textAnchor="middle"
                  fontFamily="JetBrains Mono" fontSize="18" fill="#080616">
                  {p.label}
                </text>
              </g>
            );
          })}

          {/* Line on top */}
          <polyline
            points={pricePoints.map(p => `${p.x},${300 - (300 - p.y) * graphProg}`).join(' ')}
            fill="none" stroke="#ff0000" strokeWidth="4"
          />
        </svg>
      </div>

      {/* Bottom conclusion */}
      <div style={{
        position: 'absolute', bottom: 60, left: '50%',
        transform: `translateX(-50%) scale(${conclusion})`,
        opacity: conclusion,
        fontFamily: 'Yantra One', fontSize: 55,
        color: '#FFDE42', backgroundColor: '#080616',
        padding: '16px 40px',
        textAlign: 'center',
      }}>
        EMI से कमर टूट जाती है 💔
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.4,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene9NewReality;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10BrokenTape',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10BrokenTape: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(8);
  const tape = sp(35);
  const tapeRotate = (frame - 35) * 4;
  const loop1 = sp(80);
  const loop2 = sp(105);
  const loop3 = sp(130);
  const stop = sp(170);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, #FFDE42 0%, transparent 80%)',
        opacity: 0.3,
      }} />

      {[0, 1, 2, 3].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 1) * 1500,
          top: seededRandom(i + 5) * 800,
          width: 280 + i * 40, height: 280 + i * 40,
          border: '3px solid #1F150C', opacity: 0.05,
          borderRadius: '50%',
          transform: `rotate(${frame * 0.2 + i * 50}deg)`,
        }} />
      ))}

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <circle key={i} cx={960} cy={540} r={150 + i * 80} fill="none" stroke="#1F150C" strokeWidth="1" />
        ))}
      </svg>

      {Array.from({ length: 14 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (seededRandom(i + 90) * 1920 + frame * 0.4) % 1920,
          top: seededRandom(i + 110) * 1080,
          width: 4, height: 4, backgroundColor: '#1F150C', borderRadius: '50%', opacity: 0.25,
        }} />
      ))}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 70, left: '50%',
        transform: `translateX(-50%) scale(${title})`,
        opacity: title,
        fontFamily: "'Yantra One', sans-serif",
        fontSize: 81, color: '#080616', textAlign: 'center',
      }}>
        सलाह वही <span style={{ color: '#ff0000' }}>पुरानी</span>...
      </div>

      {/* Cassette tape */}
      <div style={{
        position: 'absolute',
        top: '45%', left: '50%',
        transform: `translate(-50%, -50%) scale(${tape})`,
        opacity: tape,
      }}>
        <svg width="600" height="380" viewBox="0 0 600 380">
          {/* Cassette body */}
          <rect x="20" y="20" width="560" height="340" rx="15" fill="#1F150C" stroke="#080616" strokeWidth="6" />
          {/* Label */}
          <rect x="60" y="50" width="480" height="110" fill="#FFDE42" stroke="#080616" strokeWidth="4" />
          <text x="300" y="100" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="22" fill="#080616" letterSpacing="3">SIDE A · 1985</text>
          <text x="300" y="135" textAnchor="middle" fontFamily="Yantra One" fontSize="32" fill="#1F150C">पुरानी सलाह</text>

          {/* Reels */}
          <g transform={`translate(170, 270) rotate(${tapeRotate})`}>
            <circle cx="0" cy="0" r="60" fill="#FFDE42" stroke="#080616" strokeWidth="4" />
            {[0, 60, 120, 180, 240, 300].map(a => (
              <line key={a} x1="0" y1="0"
                x2={Math.cos(a * Math.PI / 180) * 55}
                y2={Math.sin(a * Math.PI / 180) * 55}
                stroke="#1F150C" strokeWidth="6" />
            ))}
            <circle cx="0" cy="0" r="15" fill="#1F150C" />
          </g>
          <g transform={`translate(430, 270) rotate(${tapeRotate})`}>
            <circle cx="0" cy="0" r="60" fill="#FFDE42" stroke="#080616" strokeWidth="4" />
            {[0, 60, 120, 180, 240, 300].map(a => (
              <line key={a} x1="0" y1="0"
                x2={Math.cos(a * Math.PI / 180) * 55}
                y2={Math.sin(a * Math.PI / 180) * 55}
                stroke="#1F150C" strokeWidth="6" />
            ))}
            <circle cx="0" cy="0" r="15" fill="#1F150C" />
          </g>

          {/* Tape connecting */}
          <line x1="230" y1="270" x2="370" y2="270" stroke="#FFDE42" strokeWidth="4" />
        </svg>
      </div>

      {/* Loop indicators - same words repeating */}
      <div style={{
        position: 'absolute', top: '50%', right: 100,
        transform: 'translateY(-50%)',
      }}>
        {['पढ़ाई → नौकरी', 'पढ़ाई → नौकरी', 'पढ़ाई → नौकरी'].map((t, i) => {
          const a = [loop1, loop2, loop3][i];
          return (
            <div key={i} style={{
              opacity: a * (1 - i * 0.3),
              fontFamily: 'Yantra One',
              fontSize: 47 - i * 8,
              color: '#1F150C',
              marginBottom: 15,
              transform: `translateX(${(1 - a) * 50}px)`,
            }}>
              ↻ {t}
            </div>
          );
        })}
      </div>

      {/* Stop sign */}
      <div style={{
        position: 'absolute', bottom: 70, left: '50%',
        transform: `translateX(-50%) scale(${stop})`,
        opacity: stop,
        display: 'flex', alignItems: 'center', gap: 20,
      }}>
        <div style={{
          width: 80, height: 80,
          backgroundColor: '#ff0000', color: '#FFDE42',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          fontFamily: 'Yantra One', fontSize: 47, fontWeight: 900,
          border: '5px solid #080616',
          transform: 'rotate(45deg)',
        }}>
          <span style={{ transform: 'rotate(-45deg)' }}>!</span>
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 55, color: '#080616',
          backgroundColor: '#FFDE42', padding: '14px 32px',
          border: '5px solid #080616',
        }}>
          टेप बदलने का वक़्त आ गया
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.4,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene10BrokenTape;
```

