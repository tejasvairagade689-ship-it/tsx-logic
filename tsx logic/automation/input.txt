```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_FilmiTasveer',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_FilmiTasveer: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Film strip horizontal scroll
  const filmScroll = interpolate(frame, [0, durationInFrames], [0, -300], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Spotlight sweep
  const spotlightX = interpolate(frame, [0, 60, 120, 180, 240, 300, 360], [200, 900, 500, 1400, 700, 1100, 300], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const spotlightY = interpolate(frame, [0, 60, 120, 180, 240, 300, 360], [300, 600, 200, 700, 400, 250, 550], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Word-by-word reveal
  const word1Op = interpolate(frame, [10, 20], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const word2Op = interpolate(frame, [20, 32], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const word3Op = interpolate(frame, [32, 44], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const word4Op = interpolate(frame, [44, 56], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const word1Y = interpolate(frame, [10, 22], [40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const word2Y = interpolate(frame, [20, 34], [40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const word3Y = interpolate(frame, [32, 46], [40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const word4Y = interpolate(frame, [44, 58], [40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Camera slow zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Film grain flicker
  const grainOp = interpolate(frame % 4, [0, 2, 4], [0.04, 0.09, 0.04], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Headline scale punch
  const headlineScale = sp(8, { damping: 8, stiffness: 150, mass: 0.3 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Deep cinematic gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 50%, #2a1a00 0%, #1C1C1C 55%, #0a0a0a 100%)',
        transform: `scale(${camZoom})`,
      }} />

      {/* Amber warm center glow */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse 700px 500px at ${spotlightX}px ${spotlightY}px, rgba(233,215,0,0.06) 0%, transparent 70%)`,
        transition: 'none',
      }} />

      {/* Horizontal film strip top */}
      <div style={{
        position: 'absolute', top: 0, left: filmScroll, width: 2600, height: 60,
        display: 'flex', alignItems: 'center',
      }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <div key={i} style={{
            width: 80, height: 40, border: '2px solid rgba(233,215,0,0.15)',
            marginRight: 8, flexShrink: 0, borderRadius: 3,
            background: i % 3 === 0 ? 'rgba(233,215,0,0.04)' : 'transparent',
          }} />
        ))}
      </div>

      {/* Film strip bottom */}
      <div style={{
        position: 'absolute', bottom: 0, left: -filmScroll, width: 2600, height: 60,
        display: 'flex', alignItems: 'center',
      }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <div key={i} style={{
            width: 80, height: 40, border: '2px solid rgba(233,215,0,0.12)',
            marginRight: 8, flexShrink: 0, borderRadius: 3,
          }} />
        ))}
      </div>

      {/* Large background circle — shape layer */}
      <div style={{
        position: 'absolute', top: -200, right: -150,
        width: 900, height: 900, borderRadius: '50%',
        border: '1.5px solid rgba(233,215,0,0.05)',
        transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.03) * 0.02})`,
      }} />
      <div style={{
        position: 'absolute', top: -100, right: -50,
        width: 700, height: 700, borderRadius: '50%',
        border: '1px solid rgba(233,215,0,0.04)',
        transform: `rotate(${-frame * 0.05}deg)`,
      }} />

      {/* Triangle shape layer left */}
      <svg style={{ position: 'absolute', left: -60, bottom: 100, opacity: 0.05 }} width="400" height="400" viewBox="0 0 400 400">
        <polygon
          points="200,20 380,380 20,380"
          fill="none" stroke="#e9d700" strokeWidth="1.5"
          transform={`rotate(${frame * 0.06}, 200, 200)`}
        />
      </svg>

      {/* Floating grid texture */}
      <svg style={{ position: 'absolute', top: 0, left: 0, opacity: 0.03 }} width="1920" height="1080">
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={`h${i}`} x1="0" y1={i * 60} x2="1920" y2={i * 60} stroke="#e9d700" strokeWidth="0.5" />
        ))}
        {Array.from({ length: 35 }).map((_, i) => (
          <line key={`v${i}`} x1={i * 60} y1="0" x2={i * 60} y2="1080" stroke="#e9d700" strokeWidth="0.5" />
        ))}
      </svg>

      {/* Film grain overlay */}
      <AbsoluteFill style={{
        background: `url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.4'/%3E%3C/svg%3E")`,
        opacity: grainOp,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i * 11) * 1920;
        const py = seededRandom(i * 17) * 1080;
        const speed = 0.3 + seededRandom(i * 7) * 0.6;
        const size = 2 + seededRandom(i * 13) * 4;
        const opacity = 0.15 + seededRandom(i * 3) * 0.25;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px,
            top: (py + frame * speed * 40) % 1080,
            width: size, height: size, borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#e9d700' : '#F5F5F5',
            opacity,
          }} />
        );
      })}

      {/* "CHILDHOOD FANTASY" tag — small English label */}
      <div style={{
        position: 'absolute', top: 100, left: 80,
        opacity: interpolate(frame, [15, 30], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        transform: `translateX(${interpolate(frame, [15, 30], [-30, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 22, color: '#e9d700', letterSpacing: 6, opacity: 0.7,
          textTransform: 'uppercase',
        }}>▶ CHILDHOOD FANTASY</div>
      </div>

      {/* Episode marker */}
      <div style={{
        position: 'absolute', top: 100, right: 80,
        opacity: interpolate(frame, [20, 35], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 20, color: '#9E9E9E', letterSpacing: 4 }}>EP. 01 / 08</div>
      </div>

      {/* Main Hindi text — word by word */}
      <div style={{
        position: 'absolute',
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) scale(${headlineScale * 0.3 + 0.7})`,
        textAlign: 'center',
        width: 1600,
      }}>
        {/* Line 1 */}
        <div style={{ display: 'flex', justifyContent: 'center', gap: 20, marginBottom: 12, flexWrap: 'wrap' }}>
          <span style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 100, fontWeight: 900, color: '#F5F5F5',
            opacity: word1Op, transform: `translateY(${word1Y}px)`, display: 'inline-block',
          }}>बचपन से</span>
          <span style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 100, fontWeight: 900, color: '#e9d700',
            opacity: word2Op, transform: `translateY(${word2Y}px)`, display: 'inline-block',
          }}>दिमाग में</span>
        </div>

        {/* Line 2 */}
        <div style={{ display: 'flex', justifyContent: 'center', gap: 20, flexWrap: 'wrap' }}>
          <span style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 100, fontWeight: 900, color: '#F5F5F5',
            opacity: word3Op, transform: `translateY(${word3Y}px)`, display: 'inline-block',
          }}>अमीरी की</span>
          <span style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 100, fontWeight: 900,
            background: 'linear-gradient(135deg, #e9d700 0%, #FFC107 100%)',
            WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent',
            opacity: word4Op, transform: `translateY(${word4Y}px)`, display: 'inline-block',
          }}>फिल्मी तस्वीर</span>
        </div>
      </div>

      {/* Stick figure — Buffett character */}
      <div style={{
        position: 'absolute', bottom: 100, left: 120,
        transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(50)})`,
        opacity: sp(50),
      }}>
        <svg width="110" height="200" viewBox="0 0 110 200">
          {/* Golden glow */}
          <defs>
            <radialGradient id="glow1" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.4" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="55" cy="100" rx="45" ry="80" fill="url(#glow1)" />
          {/* Head */}
          <circle cx="55" cy="35" r="22"
            fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.04) * 4}, 55, 35)`}
          />
          {/* Golden glasses */}
          <rect x="38" y="32" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <rect x="58" y="32" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="52" y1="36" x2="58" y2="36" stroke="#e9d700" strokeWidth="2" />
          {/* Body */}
          <line x1="55" y1="57" x2="55" y2="130" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arms — slight wave */}
          <line x1="55" y1="75"
            x2={30 + Math.sin(frame * 0.06) * 4} y2={100 + Math.cos(frame * 0.06) * 3}
            stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
          />
          <line x1="55" y1="75"
            x2={80 - Math.sin(frame * 0.06) * 4} y2={100 - Math.cos(frame * 0.06) * 3}
            stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
          />
          {/* Legs */}
          <line x1="55" y1="130" x2="38" y2="175" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="55" y1="130" x2="72" y2="175" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Bottom line animated */}
      <div style={{
        position: 'absolute', bottom: 80, left: '50%',
        transform: 'translateX(-50%)',
        width: interpolate(frame, [60, 100], [0, 600], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        height: 2, background: 'linear-gradient(90deg, transparent, #e9d700, transparent)',
        overflow: 'hidden',
      }} />

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene1_FilmiTasveer;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_FakeSymbols',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_FakeSymbols: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Car drives in from right
  const carX = interpolate(frame, [0, 45], [2200, 600], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Brand tags drop in staggered
  const tag1Y = sp(50, { damping: 10, stiffness: 200, mass: 0.3 });
  const tag2Y = sp(65, { damping: 10, stiffness: 200, mass: 0.3 });
  const tag3Y = sp(80, { damping: 10, stiffness: 200, mass: 0.3 });

  // Background pan
  const bgPan = interpolate(frame, [0, durationInFrames], [0, -80], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Red warning pulse
  const redPulse = Math.sin(frame * 0.15) * 0.3 + 0.7;

  // Diagonal stripe movement
  const stripeOffset = (frame * 1.5) % 120;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Background — dark red tinted gradient */}
      <AbsoluteFill style={{
        background: `linear-gradient(135deg, #200000 0%, #1C1C1C 40%, #0d0d0d 100%)`,
        transform: `translateX(${bgPan}px)`,
      }} />

      {/* Moving diagonal stripes */}
      <svg style={{ position: 'absolute', top: 0, left: 0, opacity: 0.04 }} width="2200" height="1080">
        {Array.from({ length: 20 }).map((_, i) => (
          <line
            key={i}
            x1={i * 120 - stripeOffset} y1="0"
            x2={i * 120 - stripeOffset + 600} y2="1080"
            stroke="#ff0000" strokeWidth="40"
          />
        ))}
      </svg>

      {/* Large diamond shape layer */}
      <svg style={{ position: 'absolute', top: '50%', left: '50%', transform: 'translate(-50%, -50%)', opacity: 0.04 }} width="800" height="800">
        <rect x="200" y="200" width="400" height="400"
          fill="none" stroke="#e9d700" strokeWidth="1"
          transform={`rotate(${45 + frame * 0.04}, 400, 400)`}
        />
        <rect x="150" y="150" width="500" height="500"
          fill="none" stroke="#e9d700" strokeWidth="0.5"
          transform={`rotate(${45 + frame * 0.04 + 10}, 400, 400)`}
        />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i * 7) * 1920;
        const py = seededRandom(i * 13) * 1080;
        const size = 1.5 + seededRandom(i * 5) * 3;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (px + frame * seededRandom(i * 3) * 25) % 1920,
            top: py,
            width: size, height: size, borderRadius: '50%',
            backgroundColor: i % 4 === 0 ? '#ff0000' : '#9E9E9E',
            opacity: 0.2 + seededRandom(i * 9) * 0.2,
          }} />
        );
      })}

      {/* SVG Car */}
      <div style={{
        position: 'absolute', top: 320, left: carX,
        opacity: interpolate(frame, [0, 20], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <svg width="650" height="240" viewBox="0 0 650 240">
          {/* Car glow */}
          <defs>
            <radialGradient id="carGlow" cx="50%" cy="80%" r="60%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.15" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="325" cy="210" rx="280" ry="30" fill="url(#carGlow)" />
          {/* Body */}
          <rect x="60" y="130" width="530" height="80" rx="12" fill="#2D2D2D" stroke="#e9d700" strokeWidth="1.5" />
          {/* Cabin */}
          <path d="M180 130 Q220 60 320 55 Q420 60 460 130 Z" fill="#1C1C1C" stroke="#e9d700" strokeWidth="1.5" />
          {/* Windows */}
          <path d="M200 128 Q230 75 315 72 L315 128 Z" fill="rgba(233,215,0,0.08)" stroke="#e9d700" strokeWidth="1" />
          <path d="M325 72 Q410 75 440 128 L325 128 Z" fill="rgba(233,215,0,0.08)" stroke="#e9d700" strokeWidth="1" />
          {/* Headlights */}
          <rect x="560" y="145" width="40" height="18" rx="4" fill="#e9d700" opacity="0.9" />
          <rect x="50" y="145" width="30" height="18" rx="4" fill="#ff0000" opacity="0.6" />
          {/* Wheels */}
          <circle cx="165" cy="205" r="38" fill="#1C1C1C" stroke="#9E9E9E" strokeWidth="3" />
          <circle cx="165" cy="205" r="22" fill="none" stroke="#e9d700" strokeWidth="2" />
          <circle cx="490" cy="205" r="38" fill="#1C1C1C" stroke="#9E9E9E" strokeWidth="3" />
          <circle cx="490" cy="205" r="22" fill="none" stroke="#e9d700" strokeWidth="2" />
          {/* Brand badge */}
          <circle cx="325" cy="148" r="12" fill="#e9d700" />
          <text x="325" y="153" textAnchor="middle" fontSize="10" fontWeight="bold" fill="#1C1C1C">★</text>
        </svg>
      </div>

      {/* "लंबी विदेशी गाड़ी" label */}
      <div style={{
        position: 'absolute', top: 580, left: 620,
        opacity: interpolate(frame, [55, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        transform: `translateY(${interpolate(frame, [55, 70], [20, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 52, fontWeight: 700, color: '#e9d700',
          textShadow: '0 0 30px rgba(233,215,0,0.4)',
        }}>लंबी विदेशी गाड़ी ↑</div>
      </div>

      {/* Brand tags — right side */}
      <div style={{
        position: 'absolute', right: 100, top: 200,
        display: 'flex', flexDirection: 'column', gap: 20,
      }}>
        {/* Tag 1 */}
        <div style={{
          transform: `translateY(${(1 - tag1Y) * 80}px)`,
          opacity: tag1Y,
          background: 'rgba(255,0,0,0.12)',
          border: '1.5px solid rgba(255,0,0,0.5)',
          borderRadius: 12, padding: '14px 32px',
          backdropFilter: 'blur(10px)',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 900, color: '#F5F5F5', letterSpacing: 6 }}>GUCCI</div>
        </div>

        {/* Tag 2 */}
        <div style={{
          transform: `translateY(${(1 - tag2Y) * 80}px)`,
          opacity: tag2Y,
          background: 'rgba(255,0,0,0.12)',
          border: '1.5px solid rgba(255,0,0,0.4)',
          borderRadius: 12, padding: '14px 32px',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 900, color: '#F5F5F5', letterSpacing: 6 }}>PRADA</div>
        </div>

        {/* Tag 3 */}
        <div style={{
          transform: `translateY(${(1 - tag3Y) * 80}px)`,
          opacity: tag3Y,
          background: 'rgba(255,0,0,0.12)',
          border: '1.5px solid rgba(255,0,0,0.3)',
          borderRadius: 12, padding: '14px 32px',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 900, color: '#F5F5F5', letterSpacing: 6 }}>ARMANI</div>
        </div>

        {/* Sub label */}
        <div style={{
          opacity: sp(95),
          fontFamily: 'Noto Sans Devanagari', fontSize: 38, fontWeight: 700,
          color: '#9E9E9E', textAlign: 'right',
        }}>बड़े-बड़े ब्रांड्स के कपड़े</div>
      </div>

      {/* Headline left */}
      <div style={{
        position: 'absolute', top: 130, left: 80,
        opacity: sp(20, { damping: 14 }),
        transform: `scale(${sp(20, { damping: 14 }) * 0.2 + 0.8})`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 80, fontWeight: 900,
          color: '#F5F5F5', lineHeight: 1.2,
        }}>अमीरी की</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 80, fontWeight: 900,
          color: '#ff0000', lineHeight: 1.2,
          opacity: redPulse,
        }}>निशानियाँ?</div>
      </div>

      {/* Warning X mark */}
      <div style={{
        position: 'absolute', top: 160, right: 430,
        opacity: sp(110) * 0.5,
      }}>
        <svg width="80" height="80" viewBox="0 0 80 80">
          <circle cx="40" cy="40" r="36" fill="none" stroke="#ff0000" strokeWidth="3" opacity="0.6" />
          <line x1="22" y1="22" x2="58" y2="58" stroke="#ff0000" strokeWidth="4" strokeLinecap="round" />
          <line x1="58" y1="22" x2="22" y2="58" stroke="#ff0000" strokeWidth="4" strokeLinecap="round" />
        </svg>
      </div>

      {/* Stick figure — looking at car */}
      <div style={{
        position: 'absolute', bottom: 80, left: 80,
        transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(30)})`,
        opacity: sp(30),
      }}>
        <svg width="90" height="170" viewBox="0 0 90 170">
          <defs>
            <radialGradient id="glow2" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.3" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="45" cy="85" rx="38" ry="65" fill="url(#glow2)" />
          <circle cx="45" cy="28" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.04) * 5}, 45, 28)`}
          />
          <rect x="32" y="25" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <rect x="47" y="25" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="43" y1="29" x2="47" y2="29" stroke="#e9d700" strokeWidth="2" />
          <line x1="45" y1="48" x2="45" y2="110" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arm pointing at car */}
          <line x1="45" y1="65" x2="80" y2="75" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="65" x2="18" y2="85" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="110" x2="30" y2="148" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="110" x2="60" y2="148" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene2_FakeSymbols;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_MoneyDrain',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_MoneyDrain: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Money drain — coins falling
  const coinsDraining = interpolate(frame, [40, 180], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Water wave animation
  const waveOffset = (frame * 2) % 1920;

  // Plane takeoff
  const planeX = interpolate(frame, [20, 100], [-200, 1000], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.in(Easing.quad),
  });
  const planeY = interpolate(frame, [20, 100], [400, 200], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Rupee counter drain
  const rupeeCount = Math.floor(interpolate(frame, [40, 200], [100000, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Split background — top blue travel / bottom dark drain */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #0a1628 0%, #1C1C1C 50%, #0d0000 100%)',
      }} />

      {/* Horizon glow */}
      <div style={{
        position: 'absolute', top: '45%', left: 0, right: 0, height: 4,
        background: 'linear-gradient(90deg, transparent, #e9d700, transparent)',
        opacity: 0.4,
      }} />

      {/* Water wave SVG at bottom */}
      <svg style={{ position: 'absolute', bottom: 0, left: -waveOffset, opacity: 0.12 }} width="4000" height="200">
        <path d={`M0,100 Q${200},20 ${400},100 T${800},100 T${1200},100 T${1600},100 T${2000},100 T${2400},100 T${2800},100 T${3200},100 T${3600},100 T${4000},100 L4000,200 L0,200 Z`}
          fill="#0066ff" />
        <path d={`M0,120 Q${200},50 ${400},120 T${800},120 T${1200},120 T${1600},120 T${2000},120 T${2400},120 T${2800},120 T${3200},120 T${3600},120 T${4000},120 L4000,200 L0,200 Z`}
          fill="#0044cc" opacity="0.5" />
      </svg>

      {/* Rotating hexagon shape layer */}
      <svg style={{ position: 'absolute', bottom: 100, right: 50, opacity: 0.05 }} width="600" height="600">
        <polygon
          points="300,50 500,175 500,425 300,550 100,425 100,175"
          fill="none" stroke="#e9d700" strokeWidth="1.5"
          transform={`rotate(${frame * 0.05}, 300, 300)`}
        />
        <polygon
          points="300,100 460,200 460,400 300,500 140,400 140,200"
          fill="none" stroke="#e9d700" strokeWidth="0.8"
          transform={`rotate(${-frame * 0.03}, 300, 300)`}
        />
      </svg>

      {/* Airplane SVG */}
      <div style={{
        position: 'absolute',
        left: planeX,
        top: planeY,
        transform: `rotate(-15deg)`,
        opacity: interpolate(frame, [20, 35, 90, 100], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <svg width="200" height="80" viewBox="0 0 200 80">
          <path d="M20,40 L160,20 L180,40 L160,50 Z" fill="#F5F5F5" opacity="0.9" />
          <path d="M80,20 L140,0 L150,20 Z" fill="#F5F5F5" opacity="0.7" />
          <path d="M80,50 L130,70 L140,50 Z" fill="#F5F5F5" opacity="0.7" />
          <circle cx="175" cy="40" r="5" fill="#e9d700" />
          {/* Jet trail */}
          <line x1="20" y1="40" x2="-80" y2="42" stroke="#F5F5F5" strokeWidth="2" opacity="0.2" strokeDasharray="8,8" />
        </svg>
      </div>

      {/* विदेश TRIP label */}
      <div style={{
        position: 'absolute', top: 100, left: 80,
        opacity: sp(25, { damping: 14 }),
        transform: `translateX(${(1 - sp(25, { damping: 14 })) * -50}px)`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 110, fontWeight: 900,
          color: '#F5F5F5', lineHeight: 1,
        }}>हर साल</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 80, fontWeight: 700,
          color: '#e9d700',
        }}>विदेश ट्रिप ✈</div>
      </div>

      {/* Rupee drain counter — CENTER */}
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        transform: `translate(-50%, -50%)`,
        textAlign: 'center',
        opacity: interpolate(frame, [38, 52], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 160, fontWeight: 900,
          color: '#ff0000',
          textShadow: '0 0 40px rgba(255,0,0,0.5)',
        }}>
          ₹{rupeeCount.toLocaleString('en-IN')}
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 50, fontWeight: 700,
          color: '#9E9E9E', marginTop: -10,
        }}>पानी की तरह बह रहा है</div>
      </div>

      {/* Falling coins */}
      {Array.from({ length: 20 }).map((_, i) => {
        const startX = 200 + seededRandom(i * 7) * 1500;
        const speed = 2 + seededRandom(i * 3) * 3;
        const delay = seededRandom(i * 11) * 80;
        const coinY = ((frame * speed + delay * speed) % 1200) - 100;
        const coinOp = coinsDraining * (0.4 + seededRandom(i * 5) * 0.6);
        const wobble = Math.sin(frame * 0.1 + i) * 20;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: startX + wobble,
            top: coinY,
            opacity: coinOp,
          }}>
            <svg width="36" height="36" viewBox="0 0 36 36">
              <circle cx="18" cy="18" r="16" fill="#e9d700" opacity="0.9" />
              <circle cx="18" cy="18" r="12" fill="none" stroke="#1C1C1C" strokeWidth="1.5" />
              <text x="18" y="23" textAnchor="middle" fontSize="12" fontWeight="bold" fill="#1C1C1C">₹</text>
            </svg>
          </div>
        );
      })}

      {/* Water drain SVG icon — bottom center */}
      <div style={{
        position: 'absolute', bottom: 120, right: 120,
        opacity: sp(80, { damping: 14 }),
        transform: `scale(${sp(80, { damping: 14 })})`,
      }}>
        <svg width="160" height="160" viewBox="0 0 160 160">
          {/* Drain hole */}
          <circle cx="80" cy="80" r="70" fill="none" stroke="#ff0000" strokeWidth="3" opacity="0.4" />
          <circle cx="80" cy="80" r="50" fill="none" stroke="#ff0000" strokeWidth="2" opacity="0.3" />
          <circle cx="80" cy="80" r="25" fill="#ff0000" opacity="0.2" />
          {/* Spinning arrows */}
          <path d="M80,30 A50,50 0 0,1 130,80" fill="none" stroke="#ff0000" strokeWidth="4" strokeLinecap="round"
            transform={`rotate(${frame * 3}, 80, 80)`}
          />
          <path d="M80,130 A50,50 0 0,1 30,80" fill="none" stroke="#ff0000" strokeWidth="4" strokeLinecap="round"
            transform={`rotate(${frame * 3}, 80, 80)`}
          />
        </svg>
      </div>

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 11) * 1920,
          top: (seededRandom(i * 17) * 1080 + frame * (0.5 + seededRandom(i * 7))) % 1080,
          width: 3, height: 3, borderRadius: '50%',
          backgroundColor: '#e9d700',
          opacity: 0.15 + seededRandom(i * 3) * 0.2,
        }} />
      ))}

      {/* Stick figure — looking shocked */}
      <div style={{
        position: 'absolute', bottom: 90, left: 80,
        transform: `
          translateY(${Math.sin(frame * 0.05) * 3}px)
          rotate(${Math.sin(frame * 0.08) * 2}deg)
          scale(${sp(35)})
        `,
        opacity: sp(35),
      }}>
        <svg width="100" height="180" viewBox="0 0 100 180">
          <defs>
            <radialGradient id="glow3" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.3" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="50" cy="90" rx="40" ry="70" fill="url(#glow3)" />
          {/* Head — surprised tilt */}
          <circle cx="50" cy="30" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${10 + Math.sin(frame * 0.1) * 5}, 50, 30)`}
          />
          <rect x="37" y="27" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <rect x="52" y="27" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="48" y1="31" x2="52" y2="31" stroke="#e9d700" strokeWidth="2" />
          {/* Open mouth — shocked */}
          <ellipse cx="50" cy="44" rx="5" ry="4" fill="none" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="50" y1="52" x2="50" y2="125" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arms raised in shock */}
          <line x1="50" y1="70"
            x2={18 + Math.sin(frame * 0.15) * 5} y2={48 + Math.cos(frame * 0.15) * 3}
            stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
          />
          <line x1="50" y1="70"
            x2={82 - Math.sin(frame * 0.15) * 5} y2={48 - Math.cos(frame * 0.15) * 3}
            stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
          />
          <line x1="50" y1="125" x2="33" y2="165" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="125" x2="67" y2="165" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 30%, rgba(0,0,0,0.85) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene3_MoneyDrain;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4_SocietyJudge',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_SocietyJudge: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Race track scrolling
  const trackScroll = interpolate(frame, [0, durationInFrames], [0, -400], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Trophy drop
  const trophyScale = sp(50, { damping: 7, stiffness: 180, mass: 0.5 });

  // Crowd clapping — scale pulse
  const crowdPulse = 1 + Math.sin(frame * 0.2) * 0.03;

  // "WINNER" stamp rotation settle
  const stampRot = interpolate(frame, [70, 90], [-15, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.back(2)),
  });
  const stampScale = sp(70, { damping: 8, stiffness: 200, mass: 0.4 });

  // Confetti
  const confettiActive = frame > 65 ? 1 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Stadium gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 80%, #1a0a00 0%, #1C1C1C 50%, #050510 100%)',
      }} />

      {/* Race track dashes — moving */}
      <div style={{
        position: 'absolute', bottom: 250, left: trackScroll, width: 2600, height: 8,
        display: 'flex', gap: 20,
      }}>
        {Array.from({ length: 60 }).map((_, i) => (
          <div key={i} style={{ width: 60, height: 8, backgroundColor: '#e9d700', opacity: 0.4, flexShrink: 0 }} />
        ))}
      </div>
      <div style={{
        position: 'absolute', bottom: 200, left: trackScroll, width: 2600, height: 3,
        display: 'flex', gap: 20,
      }}>
        {Array.from({ length: 60 }).map((_, i) => (
          <div key={i} style={{ width: 60, height: 3, backgroundColor: '#9E9E9E', opacity: 0.2, flexShrink: 0 }} />
        ))}
      </div>

      {/* Circular rings — arena feel */}
      {[800, 950, 1100].map((r, i) => (
        <div key={i} style={{
          position: 'absolute', bottom: -r * 0.6, left: '50%',
          transform: `translateX(-50%)`,
          width: r * 2, height: r,
          borderRadius: `${r}px ${r}px 0 0`,
          border: `1px solid rgba(233,215,0,${0.04 - i * 0.01})`,
        }} />
      ))}

      {/* Trophy SVG — center */}
      <div style={{
        position: 'absolute', top: 180, left: '50%',
        transform: `translateX(-50%) scale(${trophyScale})`,
        opacity: trophyScale,
      }}>
        <svg width="200" height="250" viewBox="0 0 200 250">
          <defs>
            <radialGradient id="trophyGlow" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.5" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="100" cy="130" rx="80" ry="80" fill="url(#trophyGlow)" />
          {/* Cup */}
          <path d="M60,30 L60,120 Q60,160 100,170 Q140,160 140,120 L140,30 Z"
            fill="none" stroke="#e9d700" strokeWidth="3" />
          {/* Handles */}
          <path d="M60,50 Q30,50 30,80 Q30,110 60,100" fill="none" stroke="#e9d700" strokeWidth="3" strokeLinecap="round" />
          <path d="M140,50 Q170,50 170,80 Q170,110 140,100" fill="none" stroke="#e9d700" strokeWidth="3" strokeLinecap="round" />
          {/* Base */}
          <rect x="80" y="170" width="40" height="15" fill="#e9d700" opacity="0.8" />
          <rect x="65" y="185" width="70" height="10" rx="3" fill="#e9d700" opacity="0.6" />
          {/* Star */}
          <text x="100" y="105" textAnchor="middle" fontSize="50" fill="#e9d700">★</text>
          {/* Shimmer */}
          <line x1="90" y1="50" x2="85" y2="80" stroke="#F5F5F5" strokeWidth="2" opacity="0.4" />
        </svg>
      </div>

      {/* "WINNER" stamp */}
      <div style={{
        position: 'absolute', top: 160, right: 120,
        transform: `rotate(${stampRot}deg) scale(${stampScale})`,
        opacity: stampScale,
        border: '6px solid #e9d700',
        borderRadius: 16, padding: '16px 40px',
        background: 'rgba(233,215,0,0.08)',
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 80, fontWeight: 900,
          color: '#e9d700', letterSpacing: 8,
          textShadow: '0 0 30px rgba(233,215,0,0.6)',
        }}>WINNER!</div>
      </div>

      {/* Society's assumption text */}
      <div style={{
        position: 'absolute', top: 430, left: 80, right: 80,
        textAlign: 'center',
        opacity: sp(30, { damping: 14 }),
        transform: `translateY(${(1 - sp(30, { damping: 14 })) * 50}px)`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 88, fontWeight: 900, color: '#F5F5F5',
          lineHeight: 1.3,
        }}>
          समाज मान लेता है —
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 72, fontWeight: 700,
          color: '#ff0000', marginTop: 8,
        }}>
          जिंदगी की दौड़ जीत चुका है?
        </div>
      </div>

      {/* Confetti burst */}
      {confettiActive === 1 && Array.from({ length: 30 }).map((_, i) => {
        const angle = seededRandom(i * 7) * Math.PI * 2;
        const dist = 200 + seededRandom(i * 11) * 400;
        const confX = 960 + Math.cos(angle) * dist * ((frame - 65) / 80);
        const confY = 300 + Math.sin(angle) * dist * ((frame - 65) / 80) + ((frame - 65) * 2);
        const confColor = i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#ff0000' : '#F5F5F5';
        const confOp = Math.max(0, 1 - (frame - 65) / 80);
        return (
          <div key={i} style={{
            position: 'absolute', left: confX, top: confY,
            width: 8 + seededRandom(i * 5) * 10,
            height: 4 + seededRandom(i * 3) * 6,
            backgroundColor: confColor, opacity: confOp,
            transform: `rotate(${seededRandom(i * 9) * 360 + frame * 5}deg)`,
          }} />
        );
      })}

      {/* Small crowd silhouette bottom */}
      <div style={{
        position: 'absolute', bottom: 80, left: 0, right: 0,
        transform: `scale(${crowdPulse})`,
        display: 'flex', justifyContent: 'space-evenly',
        opacity: 0.15,
      }}>
        {Array.from({ length: 25 }).map((_, i) => (
          <svg key={i} width="30" height="80" viewBox="0 0 30 80">
            <circle cx="15" cy="15" r="10" fill="#9E9E9E" />
            <rect x="8" y="25" width="14" height="40" rx="4" fill="#9E9E9E" />
          </svg>
        ))}
      </div>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 7) * 1920,
          top: (seededRandom(i * 13) * 1080 + frame * (1 + seededRandom(i * 3) * 0.8)) % 1080,
          width: 3, height: 3, borderRadius: '50%',
          backgroundColor: '#e9d700',
          opacity: 0.12,
        }} />
      ))}

      {/* Stick figure — arms raised victory */}
      <div style={{
        position: 'absolute', bottom: 90, right: 100,
        transform: `translateY(${Math.sin(frame * 0.06) * 4}px) scale(${sp(20)})`,
        opacity: sp(20),
      }}>
        <svg width="100" height="185" viewBox="0 0 100 185">
          <defs>
            <radialGradient id="glow4" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.35" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="50" cy="92" rx="42" ry="72" fill="url(#glow4)" />
          <circle cx="50" cy="28" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <rect x="37" y="24" width="11" height="8" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <rect x="52" y="24" width="11" height="8" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="48" y1="28" x2="52" y2="28" stroke="#e9d700" strokeWidth="2" />
          <line x1="50" y1="50" x2="50" y2="125" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arms raised up like winner */}
          <line x1="50" y1="70"
            x2={22 + Math.sin(frame * 0.12) * 4} y2={38 + Math.cos(frame * 0.12) * 3}
            stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
          />
          <line x1="50" y1="70"
            x2={78 - Math.sin(frame * 0.12) * 4} y2={38 - Math.cos(frame * 0.12) * 3}
            stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
          />
          <line x1="50" y1="125" x2="34" y2="168" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="125" x2="66" y2="168" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene4_SocietyJudge;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5_QuietWealth',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_QuietWealth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Slow camera drift right
  const camX = interpolate(frame, [0, durationInFrames], [0, -40], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Typewriter effect for "खामोश"
  const charCount = Math.floor(interpolate(frame, [20, 60], [0, 6], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));
  const word = 'खामोश';
  const visibleText = word.slice(0, charCount);

  // Soft breathing scale
  const breathe = 1 + Math.sin(frame * 0.04) * 0.008;

  // Market shelf items appearing
  const shelf1 = sp(55, { damping: 14 });
  const shelf2 = sp(70, { damping: 14 });
  const shelf3 = sp(85, { damping: 14 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Calm warm earthy gradient — contrast with previous red scenes */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, #0d1a0a 0%, #1C1C1C 45%, #1a1500 100%)',
        transform: `translateX(${camX}px) scale(${breathe})`,
      }} />

      {/* Soft amber area light */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 1000px 600px at 60% 60%, rgba(233,215,0,0.05) 0%, transparent 70%)',
      }} />

      {/* Horizontal line texture — supermarket shelves feel */}
      <svg style={{ position: 'absolute', top: 0, left: 0, opacity: 0.04 }} width="1920" height="1080">
        {[200, 400, 600, 750, 850].map((y, i) => (
          <line key={i} x1="0" y1={y} x2="1920" y2={y} stroke="#e9d700" strokeWidth="1" />
        ))}
      </svg>

      {/* Large concentric circles — meditative shape layer */}
      <div style={{ position: 'absolute', top: '50%', left: '20%', transform: 'translate(-50%, -50%)' }}>
        {[400, 320, 240, 160].map((r, i) => (
          <div key={i} style={{
            position: 'absolute',
            top: '50%', left: '50%',
            transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.02 + i * 0.5) * 0.02})`,
            width: r * 2, height: r * 2, borderRadius: '50%',
            border: `1px solid rgba(233,215,0,${0.04 + i * 0.01})`,
          }} />
        ))}
      </div>

      {/* Big word: खामोश — typewriter */}
      <div style={{
        position: 'absolute', top: 100, left: 80,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 220, fontWeight: 900,
          color: '#F5F5F5', lineHeight: 1,
          textShadow: '0 0 80px rgba(233,215,0,0.1)',
          opacity: 0.9,
        }}>
          {visibleText}
          <span style={{
            opacity: Math.sin(frame * 0.4) > 0 ? 1 : 0,
            color: '#e9d700',
          }}>|</span>
        </div>
        <div style={{
          fontFamily: 'Inter', fontSize: 28, color: '#9E9E9E',
          letterSpacing: 8, marginTop: -10,
          opacity: interpolate(frame, [65, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>SILENCE IS WEALTH</div>
      </div>

      {/* Supermarket shelf visual — right side */}
      <div style={{
        position: 'absolute', right: 80, top: 200,
        width: 700,
      }}>
        {/* Shelf board */}
        <div style={{
          width: '100%', height: 12, backgroundColor: '#2D2D2D',
          marginBottom: 20, borderRadius: 4,
          opacity: sp(45),
        }} />

        {/* Items on shelf */}
        <div style={{
          display: 'flex', gap: 16, alignItems: 'flex-end',
          marginBottom: 30,
        }}>
          {/* Vegetable SVG items */}
          {['🥦', '🥕', '🍅', '🥬', '🧅'].map((_, i) => (
            <div key={i} style={{
              opacity: [shelf1, shelf1, shelf2, shelf2, shelf3][i],
              transform: `translateY(${(1 - [shelf1, shelf1, shelf2, shelf2, shelf3][i]) * 40}px)`,
            }}>
              <svg width="90" height="110" viewBox="0 0 90 110">
                <rect x="15" y="30" width="60" height="70" rx="8"
                  fill={['#1a3320', '#3d1a00', '#3d0000', '#1a2d0a', '#2d2000'][i]}
                  stroke={['#2d5535', '#6b3300', '#cc0000', '#2d4d12', '#4d3800'][i]}
                  strokeWidth="2"
                />
                {/* Simple veggie shape */}
                <ellipse cx="45" cy="55" rx="22" ry="28"
                  fill={['#1a5c35', '#cc5500', '#cc2200', '#1a4d0a', '#4d3300'][i]}
                  opacity="0.8"
                />
                {/* Price tag */}
                <rect x="8" y="8" width="74" height="22" rx="4" fill="#e9d700" opacity="0.9" />
                <text x="45" y="23" textAnchor="middle" fontSize="11" fontWeight="bold" fill="#1C1C1C">
                  ₹{[28, 45, 30, 22, 18][i]}
                </text>
              </svg>
            </div>
          ))}
        </div>

        {/* Second shelf board */}
        <div style={{
          width: '100%', height: 12, backgroundColor: '#2D2D2D',
          marginBottom: 20, borderRadius: 4,
          opacity: sp(65),
        }} />

        {/* Label */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 44, fontWeight: 700,
          color: '#9E9E9E',
          opacity: sp(90, { damping: 14 }),
        }}>
          लोकल सुपरमार्केट
        </div>
        <div style={{
          fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E',
          letterSpacing: 4, opacity: 0.5,
          marginTop: 4,
        }}>
          WHERE REAL FREEDOM SHOPS
        </div>
      </div>

      {/* Main Hindi body text */}
      <div style={{
        position: 'absolute', bottom: 180, left: 80, right: 800,
        opacity: sp(40, { damping: 15 }),
        transform: `translateX(${(1 - sp(40, { damping: 15 })) * -40}px)`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 58, fontWeight: 700,
          color: '#F5F5F5', lineHeight: 1.4,
        }}>
          जो इंसान सच में
          <span style={{ color: '#e9d700' }}> फाइनेंशियली आज़ाद</span> है...
        </div>
      </div>

      {/* Floating dust particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i * 13) * 1920;
        const speed = 0.2 + seededRandom(i * 7) * 0.4;
        const size = 1.5 + seededRandom(i * 5) * 2.5;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + Math.sin(frame * 0.02 + i) * 8,
            top: (seededRandom(i * 17) * 1080 + frame * speed * 15) % 1080,
            width: size, height: size, borderRadius: '50%',
            backgroundColor: i % 5 === 0 ? '#e9d700' : '#F5F5F5',
            opacity: 0.08 + seededRandom(i * 3) * 0.12,
          }} />
        );
      })}

      {/* Stick figure — calm, relaxed browsing */}
      <div style={{
        position: 'absolute', bottom: 80, left: 80,
        transform: `translateY(${Math.sin(frame * 0.04) * 2}px) scale(${sp(25)})`,
        opacity: sp(25),
      }}>
        <svg width="105" height="195" viewBox="0 0 105 195">
          <defs>
            <radialGradient id="glow5" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.35" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="52" cy="97" rx="44" ry="75" fill="url(#glow5)" />
          {/* Head — calm slight tilt */}
          <circle cx="52" cy="28" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.03) * 2}, 52, 28)`}
          />
          <rect x="38" y="25" width="12" height="8" rx="3.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <rect x="54" y="25" width="12" height="8" rx="3.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="50" y1="29" x2="54" y2="29" stroke="#e9d700" strokeWidth="2" />
          {/* Calm smile */}
          <path d="M44,40 Q52,46 60,40" fill="none" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
          <line x1="52" y1="50" x2="52" y2="130" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* One arm reaching for shelf */}
          <line x1="52" y1="75" x2="88" y2="80" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="52" y1="75" x2="24" y2="95" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="52" y1="130" x2="36" y2="172" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="52" y1="130" x2="68" y2="172" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Small basket */}
          <path d="M88,80 Q98,82 96,92 L92,92 Q92,84 88,84 Z" fill="none" stroke="#e9d700" strokeWidth="2" />
        </svg>
      </div>

      {/* Vignette — soft */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene5_QuietWealth;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6_FreedomWeapon',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_FreedomWeapon: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Chain breaking — scale burst
  const chainBreak = sp(80, { damping: 6, stiffness: 250, mass: 0.3 });
  const chainBreakShake = frame > 80 && frame < 95 ? Math.sin(frame * 0.8) * 6 : 0;

  // Left vs Right reveal
  const leftReveal = sp(20, { damping: 14 });
  const rightReveal = sp(55, { damping: 14 });

  // Key rotation
  const keyRotate = interpolate(frame, [60, 120], [0, 360], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  // Divider line grow
  const dividerW = interpolate(frame, [40, 80], [0, 1920], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Two-tone split background */}
      <div style={{
        position: 'absolute', top: 0, left: 0, width: '50%', height: '100%',
        background: 'linear-gradient(180deg, #1a0000 0%, #2d0000 100%)',
      }} />
      <div style={{
        position: 'absolute', top: 0, right: 0, width: '50%', height: '100%',
        background: 'linear-gradient(180deg, #0a1a00 0%, #001a0a 100%)',
      }} />

      {/* Center divider line */}
      <div style={{
        position: 'absolute', top: 0, left: '50%',
        transform: 'translateX(-50%)',
        width: 3, height: '100%',
        background: 'linear-gradient(180deg, transparent, #e9d700, transparent)',
        opacity: 0.4,
      }} />

      {/* Horizontal divider at 50% */}
      <div style={{
        position: 'absolute', top: '50%', left: 0,
        width: dividerW, height: 2,
        background: 'linear-gradient(90deg, rgba(233,215,0,0.6), rgba(233,215,0,0))',
      }} />

      {/* Rotating key SVG — center */}
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        transform: `translate(-50%, -50%) rotate(${keyRotate}deg) scale(${sp(55, { damping: 8 })}) translateX(${chainBreakShake}px)`,
        opacity: sp(55, { damping: 8 }),
        zIndex: 10,
      }}>
        <svg width="120" height="120" viewBox="0 0 120 120">
          <defs>
            <radialGradient id="keyGlow" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.6" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <circle cx="60" cy="60" r="55" fill="url(#keyGlow)" />
          {/* Key shape */}
          <circle cx="45" cy="45" r="22" fill="none" stroke="#e9d700" strokeWidth="5" />
          <line x1="67" y1="60" x2="105" y2="90" stroke="#e9d700" strokeWidth="5" strokeLinecap="round" />
          <line x1="88" y1="75" x2="88" y2="88" stroke="#e9d700" strokeWidth="5" strokeLinecap="round" />
          <line x1="96" y1="82" x2="96" y2="92" stroke="#e9d700" strokeWidth="5" strokeLinecap="round" />
        </svg>
      </div>

      {/* LEFT — "हैसियत दिखाना" (wrong approach — RED) */}
      <div style={{
        position: 'absolute', top: 120, left: 80,
        opacity: leftReveal,
        transform: `translateX(${(1 - leftReveal) * -60}px)`,
        maxWidth: 800,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 24, color: '#ff0000',
          letterSpacing: 6, marginBottom: 12, opacity: 0.8,
        }}>✗ WRONG APPROACH</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 80, fontWeight: 900,
          color: '#ff0000', lineHeight: 1.2,
        }}>हैसियत</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 80, fontWeight: 900,
          color: '#ff0000', lineHeight: 1.2,
        }}>दिखाना</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 44, fontWeight: 600,
          color: '#9E9E9E', marginTop: 16, lineHeight: 1.4,
        }}>पैसा = status symbol</div>
      </div>

      {/* RIGHT — "आज़ादी" (right approach — GOLD) */}
      <div style={{
        position: 'absolute', top: 120, right: 80,
        opacity: rightReveal,
        transform: `translateX(${(1 - rightReveal) * 60}px)`,
        textAlign: 'right', maxWidth: 800,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 24, color: '#e9d700',
          letterSpacing: 6, marginBottom: 12, opacity: 0.8,
        }}>✓ RIGHT APPROACH</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 80, fontWeight: 900,
          color: '#e9d700', lineHeight: 1.2,
        }}>आज़ादी</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 80, fontWeight: 900,
          color: '#e9d700', lineHeight: 1.2,
        }}>खरीदना</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 44, fontWeight: 600,
          color: '#9E9E9E', marginTop: 16, lineHeight: 1.4,
        }}>पैसा = स्वतंत्रता का हथियार</div>
      </div>

      {/* Bottom quote */}
      <div style={{
        position: 'absolute', bottom: 160, left: '50%',
        transform: `translateX(-50%)`,
        textAlign: 'center', width: 1400,
        opacity: sp(95, { damping: 14 }),
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 60, fontWeight: 700,
          color: '#F5F5F5', lineHeight: 1.5,
        }}>
          "उसे किसी महफ़िल में
          <span style={{ color: '#e9d700' }}> खुद को साबित</span> नहीं करना पड़ता"
        </div>
      </div>

      {/* Chain break effect — scatter pieces */}
      {frame > 80 && Array.from({ length: 8 }).map((_, i) => {
        const angle = (i / 8) * Math.PI * 2;
        const dist = (frame - 80) * 3;
        const cx = 960 + Math.cos(angle) * dist;
        const cy = 540 + Math.sin(angle) * dist;
        const op = Math.max(0, 1 - (frame - 80) / 40);
        return (
          <div key={i} style={{
            position: 'absolute', left: cx, top: cy, opacity: op,
            transform: `rotate(${frame * 8 + i * 45}deg)`,
          }}>
            <svg width="24" height="12" viewBox="0 0 24 12">
              <rect x="2" y="2" width="20" height="8" rx="3" fill="none" stroke="#9E9E9E" strokeWidth="2" />
            </svg>
          </div>
        );
      })}

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 11) * 1920,
          top: (seededRandom(i * 7) * 1080 + frame * (0.5 + seededRandom(i * 3) * 0.8)) % 1080,
          width: 2, height: 2, borderRadius: '50%',
          backgroundColor: i % 2 === 0 ? '#e9d700' : '#ff0000',
          opacity: 0.15,
        }} />
      ))}

      {/* Geometric shape — pentagon drift */}
      <svg style={{ position: 'absolute', top: 400, left: 50, opacity: 0.05 }} width="300" height="300">
        <polygon
          points="150,20 280,110 230,260 70,260 20,110"
          fill="none" stroke="#e9d700" strokeWidth="1.5"
          transform={`rotate(${frame * 0.04}, 150, 150)`}
        />
      </svg>

      {/* Stick figure — confident pose center bottom */}
      <div style={{
        position: 'absolute', bottom: 80, left: '50%',
        transform: `translateX(-50%) translateY(${Math.sin(frame * 0.04) * 2}px) scale(${sp(30)})`,
        opacity: sp(30),
      }}>
        <svg width="100" height="190" viewBox="0 0 100 190">
          <defs>
            <radialGradient id="glow6" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.45" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="50" cy="95" rx="44" ry="78" fill="url(#glow6)" />
          <circle cx="50" cy="28" r="22" fill="none" stroke="#e9d700" strokeWidth="3.5" />
          <rect x="36" y="25" width="12" height="8" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <rect x="52" y="25" width="12" height="8" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="48" y1="29" x2="52" y2="29" stroke="#e9d700" strokeWidth="2" />
          {/* Confident smile */}
          <path d="M42,42 Q50,50 58,42" fill="none" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="50" y1="50" x2="50" y2="130" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          {/* Arms out wide — confident T-pose */}
          <line x1="50" y1="72" x2="10" y2="85" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="72" x2="90" y2="85" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="34" y2="172" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="130" x2="66" y2="172" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.82) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene6_FreedomWeapon;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7_WrongIdol',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_WrongIdol: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Microscope zoom animation
  const zoomScale = interpolate(frame, [0, 60], [0.85, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Wrong idol red X reveal
  const xReveal = sp(75, { damping: 8, stiffness: 200, mass: 0.3 });
  const xShake = frame > 75 && frame < 90 ? Math.sin(frame * 1.2) * 8 : 0;

  // Text lines stagger
  const line1 = sp(15, { damping: 14 });
  const line2 = sp(35, { damping: 14 });
  const line3 = sp(55, { damping: 14 });

  // Fog/blur reveal
  const fogOp = interpolate(frame, [0, 50], [0.5, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Dark purple-grey gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(145deg, #0d0015 0%, #1C1C1C 50%, #001520 100%)',
        transform: `scale(${zoomScale})`,
      }} />

      {/* Radial reveal from center */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse ${300 + frame * 8}px ${200 + frame * 5}px at 50% 50%, rgba(233,215,0,0.04) 0%, transparent 70%)`,
      }} />

      {/* Fog layer dissipating */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(13,0,21,0.8) 0%, transparent 50%, rgba(13,0,21,0.8) 100%)',
        opacity: fogOp,
      }} />

      {/* Magnifying glass SVG — center background */}
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        transform: 'translate(-50%, -50%)',
        opacity: 0.04,
      }}>
        <svg width="700" height="700" viewBox="0 0 700 700">
          <circle cx="300" cy="300" r="240" fill="none" stroke="#e9d700" strokeWidth="4" />
          <circle cx="300" cy="300" r="180" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="490" y1="490" x2="650" y2="650" stroke="#e9d700" strokeWidth="20" strokeLinecap="round" />
          <line x1="520" y1="460" x2="540" y2="490" stroke="#e9d700" strokeWidth="8" strokeLinecap="round" />
        </svg>
      </div>

      {/* Scattered X marks — wrong idols */}
      {Array.from({ length: 6 }).map((_, i) => {
        const positions = [
          [150, 150], [400, 120], [650, 200],
          [250, 350], [500, 300], [750, 400],
        ];
        const delay = 30 + i * 15;
        const sc = sp(delay, { damping: 8, stiffness: 200 });
        return (
          <div key={i} style={{
            position: 'absolute',
            left: positions[i][0], top: positions[i][1],
            transform: `scale(${sc})`,
            opacity: sc * 0.3,
          }}>
            <svg width="60" height="60" viewBox="0 0 60 60">
              <circle cx="30" cy="30" r="26" fill="none" stroke="#ff0000" strokeWidth="2.5" />
              <line x1="16" y1="16" x2="44" y2="44" stroke="#ff0000" strokeWidth="3" strokeLinecap="round" />
              <line x1="44" y1="16" x2="16" y2="44" stroke="#ff0000" strokeWidth="3" strokeLinecap="round" />
            </svg>
          </div>
        );
      })}

      {/* Main WRONG IDOL icon — large, right side */}
      <div style={{
        position: 'absolute', right: 140, top: 180,
        transform: `scale(${xReveal}) translateX(${xShake}px)`,
        opacity: xReveal,
      }}>
        <svg width="280" height="320" viewBox="0 0 280 320">
          <defs>
            <radialGradient id="wrongGlow" cx="50%" cy="40%" r="50%">
              <stop offset="0%" stopColor="#ff0000" stopOpacity="0.25" />
              <stop offset="100%" stopColor="#ff0000" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="140" cy="160" rx="120" ry="130" fill="url(#wrongGlow)" />
          {/* Person silhouette */}
          <circle cx="140" cy="70" r="45" fill="none" stroke="#F5F5F5" strokeWidth="3" opacity="0.7" />
          <rect x="100" y="115" width="80" height="120" rx="12" fill="none" stroke="#F5F5F5" strokeWidth="3" opacity="0.7" />
          {/* Pedestal */}
          <rect x="100" y="235" width="80" height="20" rx="4" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="2" />
          <rect x="80" y="255" width="120" height="15" rx="3" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="1.5" />
          {/* Big X overlay */}
          <line x1="60" y1="60" x2="220" y2="260" stroke="#ff0000" strokeWidth="8" strokeLinecap="round" />
          <line x1="220" y1="60" x2="60" y2="260" stroke="#ff0000" strokeWidth="8" strokeLinecap="round" />
          {/* Label */}
          <rect x="50" y="285" width="180" height="30" rx="6" fill="#ff0000" opacity="0.8" />
          <text x="140" y="305" textAnchor="middle" fontSize="14" fontWeight="bold" fill="#F5F5F5">WRONG IDOL</text>
        </svg>
      </div>

      {/* Main text — left column staggered */}
      <div style={{ position: 'absolute', top: 150, left: 80, maxWidth: 950 }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 90, fontWeight: 900,
          color: '#F5F5F5', lineHeight: 1.2,
          opacity: line1,
          transform: `translateY(${(1 - line1) * 40}px)`,
        }}>सबसे बारीक</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 90, fontWeight: 900,
          color: '#e9d700', lineHeight: 1.2,
          opacity: line2,
          transform: `translateY(${(1 - line2) * 40}px)`,
        }}>फ़र्क</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 55, fontWeight: 700,
          color: '#9E9E9E', lineHeight: 1.4, marginTop: 16,
          opacity: line3,
          transform: `translateY(${(1 - line3) * 30}px)`,
        }}>
          जिसे हम नज़रअंदाज़ कर देते हैं
        </div>
      </div>

      {/* Bottom insight bar */}
      <div style={{
        position: 'absolute', bottom: 140, left: 80, right: 80,
        padding: '24px 48px',
        background: 'rgba(255,0,0,0.08)',
        border: '1.5px solid rgba(255,0,0,0.3)',
        borderRadius: 16,
        opacity: sp(100, { damping: 14 }),
        transform: `translateY(${(1 - sp(100, { damping: 14 })) * 40}px)`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 52, fontWeight: 700,
          color: '#F5F5F5', textAlign: 'center',
        }}>
          "गलत लोगों को
          <span style={{ color: '#ff0000' }}> आइडल</span> मान बैठते हैं"
        </div>
      </div>

      {/* Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 9) * 1920,
          top: (seededRandom(i * 5) * 1080 + frame * seededRandom(i * 7) * 25) % 1080,
          width: 2.5, height: 2.5, borderRadius: '50%',
          backgroundColor: i % 3 === 0 ? '#ff0000' : i % 3 === 1 ? '#e9d700' : '#F5F5F5',
          opacity: 0.12,
        }} />
      ))}

      {/* Stick figure — looking puzzled, hand on chin */}
      <div style={{
        position: 'absolute', bottom: 80, left: '50%',
        transform: `translateX(-50%) translateY(${Math.sin(frame * 0.04) * 2}px) scale(${sp(25)})`,
        opacity: sp(25),
      }}>
        <svg width="100" height="185" viewBox="0 0 100 185">
          <defs>
            <radialGradient id="glow7" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.3" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="50" cy="92" rx="42" ry="72" fill="url(#glow7)" />
          {/* Head tilted — thinking */}
          <circle cx="50" cy="28" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${-8 + Math.sin(frame * 0.06) * 3}, 50, 28)`}
          />
          <rect x="36" y="24" width="11" height="8" rx="3.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <rect x="53" y="24" width="11" height="8" rx="3.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="47" y1="28" x2="53" y2="28" stroke="#e9d700" strokeWidth="2" />
          {/* Question mark thought bubble */}
          <text x="72" y="16" fontSize="22" fill="#e9d700" opacity="0.8">?</text>
          <line x1="50" y1="50" x2="50" y2="128" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* One arm on chin */}
          <line x1="50" y1="72" x2="72" y2="62" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="72" x2="22" y2="90" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="128" x2="34" y2="168" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="128" x2="66" y2="168" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.85) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene7_WrongIdol;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8_BehaviourVsBuy',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_BehaviourVsBuy: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Scale bar fill animation
  const barFillLeft = interpolate(frame, [40, 100], [0, 100], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  });
  const barFillRight = interpolate(frame, [60, 120], [0, 30], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  });

  // Big number counter
  const counterVal = Math.floor(interpolate(frame, [20, 100], [0, 100], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));

  // Horizontal camera push
  const camPush = interpolate(frame, [0, durationInFrames], [0, 30], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Charcoal tech gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, #0a0a0a 0%, #1C1C1C 40%, #001a10 100%)',
        transform: `translateX(${-camPush}px)`,
      }} />

      {/* Circuit board grid lines */}
      <svg style={{ position: 'absolute', top: 0, left: 0, opacity: 0.04 }} width="1920" height="1080">
        {Array.from({ length: 16 }).map((_, i) => (
          <line key={`h${i}`} x1="0" y1={i * 72} x2="1920" y2={i * 72} stroke="#e9d700" strokeWidth="0.5" />
        ))}
        {Array.from({ length: 28 }).map((_, i) => (
          <line key={`v${i}`} x1={i * 72} y1="0" x2={i * 72} y2="1080" stroke="#e9d700" strokeWidth="0.5" />
        ))}
        {/* Node dots at intersections */}
        {Array.from({ length: 8 }).map((_, i) =>
          Array.from({ length: 6 }).map((_, j) => (
            <circle key={`n${i}${j}`}
              cx={i * 240 + 120} cy={j * 180 + 90} r="3"
              fill="#e9d700" opacity="0.15"
            />
          ))
        )}
      </svg>

      {/* VS comparison layout */}
      {/* LEFT — "क्या खरीदा" */}
      <div style={{
        position: 'absolute', top: 150, left: 80,
        width: 780,
        opacity: sp(20, { damping: 14 }),
        transform: `translateX(${(1 - sp(20, { damping: 14 })) * -50}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 22, letterSpacing: 6, color: '#9E9E9E',
          marginBottom: 16,
        }}>WHAT THEY BUY</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 82, fontWeight: 900, color: '#9E9E9E',
          lineHeight: 1.2, textDecoration: 'line-through',
          textDecorationColor: '#ff0000',
        }}>क्या खरीदा?</div>

        {/* Shopping items list */}
        {['BMW 7 Series', 'Rolex Watch', 'Foreign Trip', 'Designer Clothes'].map((item, i) => (
          <div key={i} style={{
            display: 'flex', alignItems: 'center', gap: 16, marginTop: 16,
            opacity: sp(30 + i * 15, { damping: 14 }) * 0.5,
            transform: `translateX(${(1 - sp(30 + i * 15, { damping: 14 })) * -30}px)`,
          }}>
            <svg width="24" height="24" viewBox="0 0 24 24">
              <circle cx="12" cy="12" r="10" fill="none" stroke="#ff0000" strokeWidth="2" />
              <line x1="6" y1="6" x2="18" y2="18" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
              <line x1="18" y1="6" x2="6" y2="18" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
            </svg>
            <span style={{ fontFamily: 'Inter', fontSize: 34, color: '#9E9E9E' }}>{item}</span>
          </div>
        ))}

        {/* Bar — "not important" */}
        <div style={{ marginTop: 30 }}>
          <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#9E9E9E', letterSpacing: 4, marginBottom: 8 }}>WEALTH SIGNAL</div>
          <div style={{ width: 600, height: 12, background: '#2D2D2D', borderRadius: 6, overflow: 'hidden' }}>
            <div style={{ width: `${barFillRight}%`, height: '100%', background: '#ff0000', borderRadius: 6 }} />
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#ff0000', marginTop: 8 }}>{Math.floor(barFillRight)}%</div>
        </div>
      </div>

      {/* VS divider */}
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        transform: 'translate(-50%, -50%)',
        opacity: sp(40, { damping: 10 }),
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 80, fontWeight: 900,
          color: '#e9d700', letterSpacing: 4,
          textShadow: '0 0 40px rgba(233,215,0,0.4)',
        }}>VS</div>
      </div>

      {/* RIGHT — "कैसा बर्ताव" */}
      <div style={{
        position: 'absolute', top: 150, right: 80,
        width: 780,
        opacity: sp(45, { damping: 14 }),
        transform: `translateX(${(1 - sp(45, { damping: 14 })) * 50}px)`,
        textAlign: 'right',
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 22, letterSpacing: 6, color: '#e9d700',
          marginBottom: 16,
        }}>HOW THEY BEHAVE</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 82, fontWeight: 900, color: '#e9d700',
          lineHeight: 1.2,
        }}>कैसा बर्ताव?</div>

        {/* Behaviour traits */}
        {['शांत रहना', 'लंबा सोचना', 'बचत पहले', 'भावना से नहीं'].map((item, i) => (
          <div key={i} style={{
            display: 'flex', alignItems: 'center', justifyContent: 'flex-end', gap: 16, marginTop: 16,
            opacity: sp(55 + i * 15, { damping: 14 }),
            transform: `translateX(${(1 - sp(55 + i * 15, { damping: 14 })) * 30}px)`,
          }}>
            <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 34, color: '#F5F5F5' }}>{item}</span>
            <svg width="24" height="24" viewBox="0 0 24 24">
              <circle cx="12" cy="12" r="10" fill="none" stroke="#e9d700" strokeWidth="2" />
              <polyline points="6,12 10,16 18,8" fill="none" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>
        ))}

        {/* Bar — "very important" */}
        <div style={{ marginTop: 30 }}>
          <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#e9d700', letterSpacing: 4, marginBottom: 8, textAlign: 'right' }}>WEALTH SIGNAL</div>
          <div style={{ width: 600, height: 12, background: '#2D2D2D', borderRadius: 6, overflow: 'hidden', marginLeft: 'auto' }}>
            <div style={{ width: `${barFillLeft}%`, height: '100%', background: 'linear-gradient(90deg, #e9d700, #FFC107)', borderRadius: 6 }} />
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#e9d700', marginTop: 8 }}>{counterVal}%</div>
        </div>
      </div>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 9) * 1920,
          top: (seededRandom(i * 13) * 1080 + frame * seededRandom(i * 3) * 20) % 1080,
          width: 2, height: 2, borderRadius: '50%',
          backgroundColor: i % 2 === 0 ? '#e9d700' : '#9E9E9E',
          opacity: 0.12,
        }} />
      ))}

      {/* Stick figure — neutral, analytical pose */}
      <div style={{
        position: 'absolute', bottom: 80, left: '50%',
        transform: `translateX(-50%) translateY(${Math.sin(frame * 0.04) * 2}px) scale(${sp(20)})`,
        opacity: sp(20),
      }}>
        <svg width="100" height="185" viewBox="0 0 100 185">
          <defs>
            <radialGradient id="glow8" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.3" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="50" cy="92" rx="42" ry="72" fill="url(#glow8)" />
          <circle cx="50" cy="28" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <rect x="36" y="25" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <rect x="53" y="25" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="47" y1="29" x2="53" y2="29" stroke="#e9d700" strokeWidth="2" />
          <line x1="50" y1="50" x2="50" y2="128" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="72" x2="16" y2="86" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="72" x2="84" y2="86" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="128" x2="34" y2="168" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="128" x2="66" y2="168" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.82) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene8_BehaviourVsBuy;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9_HostIntro',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_HostIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Name card slide up
  const nameCardY = sp(25, { damping: 10, stiffness: 160 });

  // Channel logo pulse
  const logoPulse = 1 + Math.sin(frame * 0.08) * 0.03;

  // Stock chart line animate
  const chartProgress = interpolate(frame, [60, 160], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Market wave — random walk
  const chartPoints = Array.from({ length: 20 }).map((_, i) => {
    const t = i / 19;
    const baseY = 200;
    const noise = Math.sin(i * 2.3) * 40 + Math.cos(i * 1.7) * 25 + Math.sin(i * 3.1) * 15;
    return { x: 100 + t * 700, y: baseY + noise };
  });

  const visiblePoints = chartPoints.slice(0, Math.ceil(chartProgress * 20));
  const pathD = visiblePoints.length > 1
    ? `M ${visiblePoints.map(p => `${p.x},${p.y}`).join(' L ')}`
    : '';

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Premium broadcast gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #0a0a15 0%, #1C1C1C 50%, #100a00 100%)',
      }} />

      {/* Spotlight from top */}
      <AbsoluteFill style={{
        background: `conic-gradient(from 270deg at 50% -10%, rgba(233,215,0,0.08) 0deg, transparent 30deg, transparent 330deg, rgba(233,215,0,0.08) 360deg)`,
      }} />

      {/* Diagonal accent lines */}
      <svg style={{ position: 'absolute', top: 0, right: 0, opacity: 0.06 }} width="600" height="1080">
        {Array.from({ length: 12 }).map((_, i) => (
          <line key={i}
            x1={600 - i * 50} y1="0"
            x2={600 - i * 50 - 400} y2="1080"
            stroke="#e9d700" strokeWidth="1"
          />
        ))}
      </svg>

      {/* Stock chart — right panel */}
      <div style={{
        position: 'absolute', right: 80, top: 180,
        opacity: sp(55, { damping: 14 }),
      }}>
        <svg width="800" height="350" viewBox="0 0 900 350">
          <defs>
            <linearGradient id="chartFill" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.2" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </linearGradient>
          </defs>
          {/* Grid */}
          {[100, 150, 200, 250, 300].map(y => (
            <line key={y} x1="100" y1={y} x2="800" y2={y} stroke="#2D2D2D" strokeWidth="1" />
          ))}
          {/* Fill area */}
          {visiblePoints.length > 1 && (
            <path
              d={`${pathD} L ${visiblePoints[visiblePoints.length - 1].x},330 L 100,330 Z`}
              fill="url(#chartFill)"
            />
          )}
          {/* Chart line */}
          {pathD && (
            <path d={pathD} fill="none" stroke="#e9d700" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
          )}
          {/* Live dot */}
          {visiblePoints.length > 0 && (
            <circle
              cx={visiblePoints[visiblePoints.length - 1].x}
              cy={visiblePoints[visiblePoints.length - 1].y}
              r={5 + Math.sin(frame * 0.2) * 2}
              fill="#e9d700"
            />
          )}
          {/* Axis */}
          <line x1="100" y1="50" x2="100" y2="330" stroke="#9E9E9E" strokeWidth="1.5" />
          <line x1="100" y1="330" x2="820" y2="330" stroke="#9E9E9E" strokeWidth="1.5" />
          {/* Label */}
          <text x="450" y="30" textAnchor="middle" fontSize="18" fill="#9E9E9E" fontFamily="JetBrains Mono">ProBuffet HQ — Market Experience</text>
        </svg>
      </div>

      {/* Name card — main focal */}
      <div style={{
        position: 'absolute', top: 200, left: 80,
        transform: `translateY(${(1 - nameCardY) * 80}px)`,
        opacity: nameCardY,
      }}>
        {/* Greeting line */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 44, fontWeight: 400,
          color: '#9E9E9E', letterSpacing: 2, marginBottom: 8,
        }}>नमस्ते, मेरा नाम है</div>

        {/* Name — BIG */}
        <div style={{
          fontFamily: 'Inter', fontSize: 180, fontWeight: 900,
          color: '#F5F5F5', lineHeight: 0.9, letterSpacing: -4,
          textShadow: '0 0 60px rgba(233,215,0,0.15)',
        }}>KUNAL</div>

        {/* Underline animated */}
        <div style={{
          width: interpolate(frame, [30, 70], [0, 600], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          height: 4,
          background: 'linear-gradient(90deg, #e9d700, transparent)',
          marginTop: 8, borderRadius: 2,
        }} />
      </div>

      {/* Channel badge */}
      <div style={{
        position: 'absolute', top: 580, left: 80,
        transform: `scale(${logoPulse}) translateY(${(1 - sp(45, { damping: 12 })) * 40}px)`,
        opacity: sp(45, { damping: 12 }),
        display: 'flex', alignItems: 'center', gap: 20,
        background: 'rgba(233,215,0,0.08)',
        border: '1.5px solid rgba(233,215,0,0.4)',
        borderRadius: 12, padding: '16px 32px',
        width: 'fit-content',
      }}>
        {/* Channel icon */}
        <svg width="48" height="48" viewBox="0 0 48 48">
          <circle cx="24" cy="24" r="22" fill="#e9d700" />
          <text x="24" y="31" textAnchor="middle" fontSize="22" fontWeight="bold" fill="#1C1C1C">P</text>
        </svg>
        <div>
          <div style={{ fontFamily: 'Inter', fontSize: 30, fontWeight: 800, color: '#e9d700', letterSpacing: 2 }}>ProBuffet HQ</div>
          <div style={{ fontFamily: 'Inter', fontSize: 16, color: '#9E9E9E', letterSpacing: 4 }}>FINANCE EDUCATION</div>
        </div>
      </div>

      {/* Stats row */}
      <div style={{
        position: 'absolute', bottom: 180, left: 80,
        display: 'flex', gap: 40,
        opacity: sp(80, { damping: 14 }),
        transform: `translateY(${(1 - sp(80, { damping: 14 })) * 30}px)`,
      }}>
        {[
          { label: 'बाज़ार का अनुभव', value: '10+ साल' },
          { label: 'निवेशकों की मानसिकता', value: 'गहराई से' },
          { label: 'भारतीय बाज़ार', value: 'जिया है' },
        ].map((stat, i) => (
          <div key={i} style={{
            background: 'rgba(255,255,255,0.04)',
            border: '1px solid rgba(255,255,255,0.1)',
            borderRadius: 10, padding: '16px 28px',
          }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, fontWeight: 700, color: '#e9d700' }}>{stat.value}</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 22, color: '#9E9E9E', marginTop: 4 }}>{stat.label}</div>
          </div>
        ))}
      </div>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 11) * 1920,
          top: (seededRandom(i * 7) * 1080 + frame * seededRandom(i * 3) * 18) % 1080,
          width: 2.5, height: 2.5, borderRadius: '50%',
          backgroundColor: '#e9d700',
          opacity: 0.12 + seededRandom(i * 5) * 0.1,
        }} />
      ))}

      {/* Stick figure — Buffett, presenting confidently */}
      <div style={{
        position: 'absolute', bottom: 80, right: 80,
        transform: `translateY(${Math.sin(frame * 0.04) * 2}px) scale(${sp(20)})`,
        opacity: sp(20),
      }}>
        <svg width="115" height="210" viewBox="0 0 115 210">
          <defs>
            <radialGradient id="glow9" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.45" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="57" cy="105" rx="48" ry="85" fill="url(#glow9)" />
          <circle cx="57" cy="30" r="26" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
          <rect x="41" y="26" width="14" height="10" rx="4" fill="none" stroke="#e9d700" strokeWidth="3" />
          <rect x="60" y="26" width="14" height="10" rx="4" fill="none" stroke="#e9d700" strokeWidth="3" />
          <line x1="55" y1="31" x2="60" y2="31" stroke="#e9d700" strokeWidth="2.5" />
          {/* Confident smile */}
          <path d="M46,45 Q57,55 68,45" fill="none" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="57" y1="56" x2="57" y2="140" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          {/* Presenting arm */}
          <line x1="57" y1="80"
            x2={20 + Math.sin(frame * 0.05) * 3} y2={65 + Math.cos(frame * 0.05) * 2}
            stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round"
          />
          <line x1="57" y1="80" x2="88" y2="100" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="57" y1="140" x2="40" y2="185" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="57" y1="140" x2="74" y2="185" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.78) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene9_HostIntro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_BigQuestion',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_BigQuestion: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big question mark spin in
  const qScale = sp(15, { damping: 6, stiffness: 120, mass: 0.6 });
  const qRot = interpolate(frame, [15, 50], [-180, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.back(1.5)),
  });

  // Mirror split — BECOME vs LOOK
  const splitReveal = sp(55, { damping: 12 });

  // Timer countdown feel
  const timerFill = interpolate(frame, [100, durationInFrames - 15], [100, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Breathing pulse background
  const bgPulse = 1 + Math.sin(frame * 0.06) * 0.015;

  // CTA blink
  const ctaBlink = Math.sin(frame * 0.25) > 0 ? 1 : 0.6;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Deep dramatic gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 40%, #1a1000 0%, #1C1C1C 55%, #000005 100%)',
        transform: `scale(${bgPulse})`,
      }} />

      {/* Radial gold burst — behind question mark */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse 500px 500px at 50% 38%, rgba(233,215,0,0.08) 0%, transparent 70%)`,
      }} />

      {/* Concentric question circles */}
      {[200, 300, 400, 500].map((r, i) => (
        <div key={i} style={{
          position: 'absolute', top: `38%`, left: '50%',
          transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.03 + i * 0.8) * 0.025})`,
          width: r * 2, height: r * 2, borderRadius: '50%',
          border: `1px solid rgba(233,215,0,${0.06 - i * 0.01})`,
        }} />
      ))}

      {/* Giant ? mark */}
      <div style={{
        position: 'absolute', top: '10%', left: '50%',
        transform: `translateX(-50%) rotate(${qRot}deg) scale(${qScale})`,
        opacity: qScale,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 280, fontWeight: 900,
          color: '#e9d700', lineHeight: 1,
          textShadow: '0 0 80px rgba(233,215,0,0.35), 0 0 160px rgba(233,215,0,0.1)',
        }}>?</div>
      </div>

      {/* BECOME vs LOOK — bottom half */}
      <div style={{
        position: 'absolute', bottom: 260, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', gap: 80, alignItems: 'center',
      }}>
        {/* BECOME */}
        <div style={{
          transform: `translateX(${(1 - splitReveal) * -100}px)`,
          opacity: splitReveal,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 110, fontWeight: 900,
            color: '#e9d700', lineHeight: 1,
            textShadow: '0 0 40px rgba(233,215,0,0.3)',
          }}>अमीर बनना</div>
          <div style={{
            fontFamily: 'Inter', fontSize: 22, color: '#e9d700',
            letterSpacing: 6, marginTop: 8, opacity: 0.7,
          }}>ACTUALLY RICH</div>
        </div>

        {/* OR */}
        <div style={{
          fontFamily: 'Inter', fontSize: 60, fontWeight: 900,
          color: '#9E9E9E',
          opacity: splitReveal * 0.6,
        }}>या</div>

        {/* LOOK */}
        <div style={{
          transform: `translateX(${(1 - splitReveal) * 100}px)`,
          opacity: splitReveal,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 110, fontWeight: 900,
            color: '#9E9E9E', lineHeight: 1,
            textDecoration: 'line-through',
            textDecorationColor: '#ff0000',
          }}>अमीर दिखना</div>
          <div style={{
            fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E',
            letterSpacing: 6, marginTop: 8, opacity: 0.6,
          }}>JUST APPEARANCES</div>
        </div>
      </div>

      {/* CTA — "एक पल रुककर सोचिए" */}
      <div style={{
        position: 'absolute', bottom: 120, left: '50%',
        transform: 'translateX(-50%)',
        opacity: sp(100, { damping: 14 }) * ctaBlink,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 52, fontWeight: 700,
          color: '#F5F5F5',
          border: '2px solid rgba(233,215,0,0.3)',
          borderRadius: 50, padding: '12px 60px',
          background: 'rgba(233,215,0,0.06)',
        }}>
          ⏸ एक पल रुककर सोचिए...
        </div>
      </div>

      {/* Timer bar at bottom */}
      <div style={{
        position: 'absolute', bottom: 78, left: '50%',
        transform: 'translateX(-50%)',
        width: 600, height: 3,
        background: '#2D2D2D', borderRadius: 2, overflow: 'hidden',
        opacity: sp(110, { damping: 14 }),
      }}>
        <div style={{
          width: `${timerFill}%`, height: '100%',
          background: 'linear-gradient(90deg, #e9d700, #FFC107)',
          borderRadius: 2,
          transition: 'none',
        }} />
      </div>

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i * 13) * 1920;
        const speed = 0.3 + seededRandom(i * 7) * 0.7;
        const size = 2 + seededRandom(i * 5) * 4;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + Math.sin(frame * 0.02 + i * 0.5) * 10,
            top: (seededRandom(i * 17) * 1080 + frame * speed * 12) % 1080,
            width: size, height: size, borderRadius: '50%',
            backgroundColor: i % 4 === 0 ? '#e9d700' : i % 4 === 1 ? '#FFC107' : '#F5F5F5',
            opacity: 0.08 + seededRandom(i * 3) * 0.1,
          }} />
        );
      })}

      {/* Stick figure — both hands on head, thinking */}
      <div style={{
        position: 'absolute', bottom: 160, right: 100,
        transform: `
          translateY(${Math.sin(frame * 0.05) * 3}px)
          rotate(${Math.sin(frame * 0.03) * 1.5}deg)
          scale(${sp(30)})
        `,
        opacity: sp(30),
      }}>
        <svg width="120" height="210" viewBox="0 0 120 210">
          <defs>
            <radialGradient id="glow10" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.5" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <ellipse cx="60" cy="105" rx="52" ry="88" fill="url(#glow10)" />
          {/* Head with big golden glow — thinking hard */}
          <circle cx="60" cy="32" r="28" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
          <rect x="44" y="28" width="14" height="10" rx="4" fill="none" stroke="#e9d700" strokeWidth="3" />
          <rect x="63" y="28" width="14" height="10" rx="4" fill="none" stroke="#e9d700" strokeWidth="3" />
          <line x1="58" y1="33" x2="63" y2="33" stroke="#e9d700" strokeWidth="2.5" />
          {/* Furrowed brow */}
          <line x1="44" y1="22" x2="52" y2="25" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
          <line x1="76" y1="25" x2="68" y2="22" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
          <line x1="60" y1="62" x2="60" y2="148" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          {/* Both hands on head */}
          <line x1="60" y1="85"
            x2={30 + Math.sin(frame * 0.1) * 3} y2={42 + Math.cos(frame * 0.1) * 2}
            stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round"
          />
          <line x1="60" y1="85"
            x2={90 - Math.sin(frame * 0.1) * 3} y2={42 - Math.cos(frame * 0.1) * 2}
            stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round"
          />
          <line x1="60" y1="148" x2="42" y2="192" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="60" y1="148" x2="78" y2="192" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          {/* Floating ? near head */}
          <text x="95" y="15" fontSize="28" fill="#e9d700" opacity={0.7 + Math.sin(frame * 0.12) * 0.3}>?</text>
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.88) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene10_BigQuestion;
```

