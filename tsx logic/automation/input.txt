```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_ZeroTaxState',
  durationInSeconds: 6,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_ZeroTaxState: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.3, stiffness: 100, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Micro movements
  const microBreath = Math.sin(frame * 0.08) * 1.5;
  const microSway = Math.sin(frame * 0.06) * 0.5;
  const microPulse = 1 + Math.sin(frame * 0.1) * 0.015;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFFFFF', overflow: 'hidden', opacity: exitOp }}>
      {/* Subtle Typography Background */}
      <div style={{ position: 'absolute', top: '50%', left: '50%', transform: 'translate(-50%, -50%) rotate(-90deg)', opacity: 0.03 }}>
        <span style={{ fontFamily: 'Inter', fontSize: 800, fontWeight: 900, color: '#1C1C1C', whiteSpace: 'nowrap', letterSpacing: 50 }}>
          TAX FREE TAX FREE TAX FREE TAX FREE TAX FREE TAX FREE TAX FREE
        </span>
      </div>

      {/* Floating Word Elements */}
      {[...Array(8)].map((_, i) => {
        const seed = i * 111.222;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 1) * 1080;
        const delay = seededRandom(seed + 2) * 30;
        const s = sp(delay);
        
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              fontFamily: 'Inter',
              fontSize: 28 + seededRandom(seed + 3) * 20,
              fontWeight: 700,
              color: i % 2 === 0 ? '#e9d700' : '#1C1C1C',
              opacity: 0.08 * s,
              transform: `translateY(${microBreath + (frame + i * 15) % 50 - 25}px) rotate(${microSway}deg)`,
              letterSpacing: 3,
            }}
          >
            {['ZERO', 'TAX', 'FREE', 'IRA', '401K', 'PENSION', 'SS', 'RETIRE'][i]}
          </div>
        );
      })}

      {/* Main Content Container */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 60,
          transform: `scale(${microPulse})`,
        }}
      >
        {/* Hero Typography */}
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 320,
            fontWeight: 900,
            color: '#1C1C1C',
            textAlign: 'center',
            letterSpacing: -10,
            lineHeight: 0.9,
            opacity: sp(5),
            transform: `translateY(${(1 - sp(5)) * -60}px) skewX(${(1 - sp(5)) * -3}deg)`,
            textShadow: '4px 4px 0px #e9d700',
          }}
        >
          $0
        </div>

        {/* Four Pillars */}
        <div style={{ display: 'flex', gap: 80, alignItems: 'center' }}>
          {['Social Security', 'Pension', '401k', 'IRA'].map((item, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 18,
                opacity: sp(20 + i * 12),
                transform: `translateY(${(1 - sp(20 + i * 12)) * -40}px) translateX(${microSway}px)`,
              }}
            >
              <div
                style={{
                  width: 16,
                  height: 16,
                  borderRadius: '50%',
                  backgroundColor: '#e9d700',
                  boxShadow: `0 0 15px #e9d700`,
                }}
              />
              <span
                style={{
                  fontFamily: 'Inter',
                  fontSize: 56,
                  fontWeight: 800,
                  color: '#1C1C1C',
                  letterSpacing: 1,
                }}
              >
                {item}
              </span>
            </div>
          ))}
        </div>

        {/* Bottom Banner */}
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 48,
            fontWeight: 900,
            color: '#FFFFFF',
            backgroundColor: '#1C1C1C',
            padding: '25px 80px',
            borderRadius: 8,
            opacity: sp(60),
            transform: `scale(${sp(60)}) rotate(${(1 - sp(60)) * 2}deg)`,
            letterSpacing: 4,
          }}
        >
          THE ZERO TAX STATE
        </div>
      </AbsoluteFill>

      {/* Stick Figure Character */}
      <div
        style={{
          position: 'absolute',
          bottom: 200,
          right: 180,
          opacity: sp(40),
          transform: `translateY(${(1 - sp(40)) * 80 + microBreath}px)`,
        }}
      >
        <svg width="120" height="180" viewBox="0 0 100 140">
          {/* Head */}
          <circle cx="50" cy="20" r="14" fill="#FFFFFF" stroke="#1C1C1C" strokeWidth="2.5" />
          {/* Glasses */}
          <rect x="38" y="17" width="24" height="7" fill="none" stroke="#e9d700" strokeWidth="2" rx="2" />
          {/* Body */}
          <line x1="50" y1="34" x2="50" y2="75" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Arms */}
          <line x1="50" y1="48" x2="32" y2="58" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="48" x2="68" y2="58" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Legs */}
          <line x1="50" y1="75" x2="40" y2="105" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="75" x2="60" y2="105" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 24,
          fontWeight: 600,
          color: '#1C1C1C',
          opacity: interpolate(frame, [0, 30], [0, 0.2], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1_ZeroTaxState;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_NoCapNoLimit',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_NoCapNoLimit: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.3, stiffness: 100, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const microWiggle = Math.sin(frame * 0.12) * 2;
  const microFloat = Math.sin(frame * 0.07) * 3;
  const microPulse = 1 + Math.sin(frame * 0.09) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Background Typography */}
      <div style={{ position: 'absolute', top: '10%', left: '-5%', opacity: 0.04 }}>
        <span style={{ fontFamily: 'Inter', fontSize: 400, fontWeight: 900, color: '#1C1C1C', letterSpacing: 20 }}>
          NO LIMIT
        </span>
      </div>

      {/* Floating Letters */}
      {[...'LIMIT'].split('').map((char, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            top: 20 + (i * 12),
            right: 50 + (i * 60),
            fontFamily: 'Inter',
            fontSize: 80,
            fontWeight: 900,
            color: i % 2 === 0 ? '#e9d700' : '#1C1C1C',
            opacity: 0.06,
            transform: `translateY(${microFloat + (frame * 0.5 + i * 10) % 80 - 40}px)`,
          }}
        >
          {char}
        </div>
      ))}

      {/* Main Content */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 80,
          transform: `scale(${microPulse})`,
        }}
      >
        {/* Top Phrases */}
        <div style={{ display: 'flex', gap: 40 }}>
          {['NO CAP', 'NO PHASE OUT', 'NO INCOME LIMIT'].map((phrase, i) => (
            <div
              key={i}
              style={{
                fontFamily: 'Inter',
                fontSize: i === 2 ? 72 : 64,
                fontWeight: 900,
                color: '#1C1C1C',
                backgroundColor: i === 2 ? '#e9d700' : '#FFFFFF',
                border: '4px solid #1C1C1C',
                padding: '30px 50px',
                borderRadius: 4,
                boxShadow: i === 2 ? '8px 8px 0px #1C1C1C' : '4px 4px 0px #9E9E9E',
                opacity: sp(15 + i * 18),
                transform: `translateY(${(1 - sp(15 + i * 18)) * -80}px) rotate(${(1 - sp(15 + i * 18)) * 5}deg)`,
                letterSpacing: 2,
              }}
            >
              {phrase}
            </div>
          ))}
        </div>

        {/* Divider */}
        <div
          style={{
            width: '80%',
            height: 6,
            backgroundColor: '#1C1C1C',
            borderRadius: 3,
            opacity: sp(50),
            transform: `scaleX(${sp(50)})`,
          }}
        />

        {/* Problem Statement - Bold Typography */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 30,
            opacity: sp(65),
            transform: `translateX(${microWiggle}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 100,
              fontWeight: 900,
              color: '#1C1C1C',
              textAlign: 'center',
            }}
          >
            RETIREES <span style={{ color: '#ff0000', textDecoration: 'underline', textDecorationThickness: 6 }}>NEVER</span> CONSIDERED IT
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 60,
              fontWeight: 600,
              color: '#9E9E9E',
              textAlign: 'center',
              fontStyle: 'italic',
            }}
          >
            "income tax" = stop reading
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure */}
      <div
        style={{
          position: 'absolute',
          bottom: 150,
          left: 100,
          opacity: sp(45),
          transform: `translateY(${(1 - sp(45)) * 80 + Math.sin(frame * 0.08) * 2}px) rotate(${Math.sin(frame * 0.06) * 2}deg)`,
        }}
      >
        <svg width="130" height="190" viewBox="0 0 100 140">
          {/* Head */}
          <circle cx="50" cy="20" r="14" fill="#FFFFFF" stroke="#1C1C1C" strokeWidth="2.5" />
          {/* Glasses */}
          <rect x="38" y="17" width="24" height="7" fill="none" stroke="#e9d700" strokeWidth="2" rx="2" />
          {/* Question mark above head */}
          <text x="50" y="0" fontFamily="Inter" fontSize="16" fontWeight="900" fill="#1C1C1C" textAnchor="middle">?</text>
          {/* Body */}
          <line x1="50" y1="34" x2="50" y2="75" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Arms - thinking */}
          <line x1="50" y1="48" x2="35" y2="42" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="48" x2="65" y2="42" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Legs */}
          <line x1="50" y1="75" x2="42" y2="105" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="75" x2="58" y2="105" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 24,
          fontWeight: 600,
          color: '#1C1C1C',
          opacity: interpolate(frame, [0, 30], [0, 0.2], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_NoCapNoLimit;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_MoneyLoss',
  durationInSeconds: 6,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_MoneyLoss: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.3, stiffness: 100, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const microShake = Math.sin(frame * 0.15) * (frame > 50 ? 3 : 0);
  const microPulse = 1 + Math.sin(frame * 0.12) * 0.025;
  const microFloat = Math.sin(frame * 0.06) * 2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFFFFF', overflow: 'hidden', opacity: exitOp }}>
      {/* Bold Typography Background */}
      <div style={{ position: 'absolute', bottom: '5%', right: '-10%', opacity: 0.05 }}>
        <span style={{ fontFamily: 'Inter', fontSize: 600, fontWeight: 900, color: '#ff0000', letterSpacing: 15 }}>
          LOST LOST LOST
        </span>
      </div>

      {/* Dollar Signs Floating */}
      {[...Array(15)].map((_, i) => {
        const seed = i * 444.555;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 1) * 1080;
        
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              fontFamily: 'JetBrains Mono',
              fontSize: 50,
              fontWeight: 900,
              color: '#ff0000',
              opacity: 0.08,
              transform: `translateY(${microFloat + (frame * 0.8 + i * 20) % 150 - 75}px)`,
            }}
          >
            $
          </div>
        );
      })}

      {/* Warning Header */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: '50%',
          transform: 'translateX(-50%)',
          fontFamily: 'Inter',
          fontSize: 120,
          fontWeight: 900,
          color: '#ff0000',
          letterSpacing: 5,
          opacity: sp(8),
          transform: `translateX(-50%) translateY(${(1 - sp(8)) * -50}px) scale(${sp(8)})`,
        }}
      >
        MEANWHILE...
      </div>

      {/* Main Content */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 70,
          transform: `translateX(${microShake}px) scale(${microPulse})`,
        }}
      >
        {/* Money Amount Cards */}
        <div style={{ display: 'flex', gap: 80, alignItems: 'center' }}>
          {['$3,000', '$4,000', '$5,000'].map((amount, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                gap: 15,
                opacity: sp(25 + i * 15),
                transform: `translateY(${(1 - sp(25 + i * 15)) * 60}px) scale(${sp(25 + i * 15)})`,
              }}
            >
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 160,
                  fontWeight: 900,
                  color: '#ff0000',
                  position: 'relative',
                }}
              >
                {amount}
                {/* Strike Line */}
                <div
                  style={{
                    position: 'absolute',
                    top: '45%',
                    left: 0,
                    width: interpolate(sp(40 + i * 15), [0, 1], [0, 100], { extrapolateRight: 'clamp' }) + '%',
                    height: 10,
                    backgroundColor: '#ff0000',
                  }}
                />
              </div>
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 40,
                  fontWeight: 700,
                  color: '#9E9E9E',
                  textTransform: 'uppercase',
                  letterSpacing: 3,
                }}
              >
                PER YEAR
              </div>
            </div>
          ))}
        </div>

        {/* Impact Banner */}
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 80,
            fontWeight: 900,
            color: '#FFFFFF',
            backgroundColor: '#ff0000',
            padding: '35px 100px',
            borderRadius: 6,
            opacity: sp(75),
            transform: `scale(${sp(75)})`,
            letterSpacing: 3,
          }}
        >
          LOST TO STATE TAXES
        </div>

        {/* Bottom Text */}
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 56,
            fontWeight: 700,
            color: '#1C1C1C',
            textAlign: 'center',
            opacity: sp(90),
          }}
        >
          States that <span style={{ color: '#e9d700', fontSize: 72 }}>DOZEN'S</span> don't tax at all
        </div>
      </AbsoluteFill>

      {/* Stick Figure - Shocked */}
      <div
        style={{
          position: 'absolute',
          bottom: 130,
          right: 130,
          opacity: sp(50),
          transform: `translateY(${(1 - sp(50)) * 80 + Math.sin(frame * 0.1) * 4}px)`,
        }}
      >
        <svg width="140" height="200" viewBox="0 0 100 140">
          {/* Head - shocked */}
          <circle cx="50" cy="20" r="14" fill="#FFFFFF" stroke="#1C1C1C" strokeWidth="2.5" />
          {/* Glasses */}
          <rect x="38" y="17" width="24" height="7" fill="none" stroke="#e9d700" strokeWidth="2" rx="2" />
          {/* Shocked O mouth */}
          <circle cx="50" cy="26" r="4" fill="#1C1C1C" />
          {/* Body */}
          <line x1="50" y1="34" x2="50" y2="75" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Arms - raised */}
          <line x1="50" y1="48" x2="28" y2="35" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="48" x2="72" y2="35" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Legs */}
          <line x1="50" y1="75" x2="42" y2="105" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="75" x2="58" y2="105" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 24,
          fontWeight: 600,
          color: '#1C1C1C',
          opacity: interpolate(frame, [0, 30], [0, 0.2], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3_MoneyLoss;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4_RankingsIntro',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_RankingsIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.3, stiffness: 100, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const microRotate = Math.sin(frame * 0.04) * 1.5;
  const microBreath = Math.sin(frame * 0.07) * 2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFFFFF', overflow: 'hidden', opacity: exitOp }}>
      {/* Typography Background */}
      <div style={{ position: 'absolute', top: '20%', left: '20%', opacity: 0.04 }}>
        <span style={{ fontFamily: 'Inter', fontSize: 350, fontWeight: 900, color: '#1C1C1C', letterSpacing: 30 }}>
          RANKINGS
        </span>
      </div>

      {/* Floating Ranking Numbers */}
      {[5, 3].map((num, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            top: i === 0 ? '15%' : '55%',
            left: '15%',
            fontFamily: 'Inter',
            fontSize: 500,
            fontWeight: 900,
            color: i === 0 ? '#e9d700' : '#ff0000',
            opacity: 0.08,
            transform: `rotate(${microRotate}deg) translateY(${microBreath}px)`,
          }}
        >
          {num}
        </div>
      ))}

      {/* Main Content */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 90,
        }}
      >
        {/* Header */}
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 70,
            fontWeight: 800,
            color: '#9E9E9E',
            textTransform: 'uppercase',
            letterSpacing: 8,
            opacity: sp(10),
            transform: `translateY(${(1 - sp(10)) * -40}px)`,
          }}
        >
          TODAY'S RANKINGS
        </div>

        {/* Two Big Cards */}
        <div style={{ display: 'flex', gap: 100 }}>
          {/* BEST Card */}
          <div
            style={{
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              gap: 25,
              backgroundColor: '#FFFFFF',
              border: '8px solid #1C1C1C',
              borderRadius: 12,
              padding: '60px 80px',
              boxShadow: '12px 12px 0px #1C1C1C',
              opacity: sp(35),
              transform: `translateX(${(1 - sp(35)) * -120}px) rotate(${(1 - sp(35)) * -5}deg)`,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 180,
                fontWeight: 900,
                color: '#e9d700',
                lineHeight: 1,
              }}
            >
              5
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 72,
                fontWeight: 900,
                color: '#1C1C1C',
                textTransform: 'uppercase',
                letterSpacing: 3,
              }}
            >
              BEST
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 48,
                fontWeight: 700,
                color: '#9E9E9E',
              }}
            >
              STATES
            </div>
          </div>

          {/* WORST Card */}
          <div
            style={{
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              gap: 25,
              backgroundColor: '#ff0000',
              border: '8px solid #1C1C1C',
              borderRadius: 12,
              padding: '60px 80px',
              boxShadow: '12px 12px 0px #1C1C1C',
              opacity: sp(55),
              transform: `translateX(${(1 - sp(55)) * 120}px) rotate(${(1 - sp(55)) * 5}deg)`,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 180,
                fontWeight: 900,
                color: '#FFFFFF',
                lineHeight: 1,
              }}
            >
              3
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 72,
                fontWeight: 900,
                color: '#FFFFFF',
                textTransform: 'uppercase',
                letterSpacing: 3,
              }}
            >
              WORST
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 48,
                fontWeight: 700,
                color: 'rgba(255,255,255,0.7)',
              }}
            >
              STATES
            </div>
          </div>
        </div>

        {/* Bottom Statement */}
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 72,
            fontWeight: 900,
            color: '#1C1C1C',
            textAlign: 'center',
            opacity: sp(90),
            transform: `translateY(${(1 - sp(90)) * 40}px)`,
          }}
        >
          NOT HEADLINES. NOT OPINIONS.
        </div>
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 50,
            fontWeight: 600,
            color: '#e9d700',
            textAlign: 'center',
            opacity: sp(100),
          }}
        >
          ACTUAL DATA ONLY
        </div>
      </AbsoluteFill>

      {/* Stick Figure */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(45)) * 80 + Math.sin(frame * 0.06) * 2}px)`,
          opacity: sp(45),
        }}
      >
        <svg width="150" height="210" viewBox="0 0 100 140">
          {/* Head */}
          <circle cx="50" cy="20" r="14" fill="#FFFFFF" stroke="#1C1C1C" strokeWidth="2.5" />
          {/* Glasses */}
          <rect x="38" y="17" width="24" height="7" fill="none" stroke="#e9d700" strokeWidth="2" rx="2" />
          {/* Smile */}
          <path d="M 44 23 Q 50 27 56 23" stroke="#1C1C1C" strokeWidth="2" fill="none" strokeLinecap="round" />
          {/* Body */}
          <line x1="50" y1="34" x2="50" y2="75" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Arms - presenting */}
          <line x1="50" y1="50" x2="28" y2="58" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="50" x2="72" y2="58" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Legs */}
          <line x1="50" y1="75" x2="44" y2="105" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="75" x2="56" y2="105" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 24,
          fontWeight: 600,
          color: '#1C1C1C',
          opacity: interpolate(frame, [0, 30], [0, 0.2], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4_RankingsIntro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5_ActualData',
  durationInSeconds: 5,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_ActualData: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.3, stiffness: 100, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const microPulse = 1 + Math.sin(frame * 0.1) * 0.02;
  const microFloat = Math.sin(frame * 0.05) * 2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Typography Background */}
      <div style={{ position: 'absolute', top: '10%', left: '50%', transform: 'translateX(-50%)', opacity: 0.04 }}>
        <span style={{ fontFamily: 'Inter', fontSize: 280, fontWeight: 900, color: '#1C1C1C', letterSpacing: 20 }}>
          2026 DATA
        </span>
      </div>

      {/* Main Content */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 80,
          transform: `scale(${microPulse})`,
        }}
      >
        {/* Strike Through Section */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 25 }}>
          {[
            { text: 'HEADLINES', strike: true },
            { text: "NEIGHBOR'S OPINION", strike: true },
          ].map((item, i) => (
            <div
              key={i}
              style={{
                fontFamily: 'Inter',
                fontSize: 88,
                fontWeight: 900,
                color: '#9E9E9E',
                textDecoration: item.strike ? 'line-through' : 'none',
                textDecorationThickness: 8,
                textDecorationColor: '#ff0000',
                opacity: sp(8 + i * 15),
                transform: `translateX(${(1 - sp(8 + i * 15)) * -100}px)`,
                letterSpacing: 3,
              }}
            >
              {item.text}
            </div>
          ))}
        </div>

        {/* Arrow Down */}
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 120,
            fontWeight: 900,
            color: '#1C1C1C',
            opacity: sp(45),
            transform: `translateY(${(1 - sp(45)) * 30}px) rotate(90deg)`,
          }}
        >
          ↓
        </div>

        {/* 2026 Badge */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 20,
            opacity: sp(60),
            transform: `scale(${sp(60)})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 220,
              fontWeight: 900,
              color: '#e9d700',
              letterSpacing: 10,
            }}
          >
            2026
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 72,
              fontWeight: 900,
              color: '#1C1C1C',
              textTransform: 'uppercase',
              letterSpacing: 5,
            }}
          >
            ACTUAL DATA
          </div>
        </div>

        {/* Four Pillars Grid */}
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 30 }}>
          {['PENSION', 'SOCIAL SECURITY', '401K', 'IRA'].map((type, i) => (
            <div
              key={i}
              style={{
                backgroundColor: '#FFFFFF',
                border: '4px solid #1C1C1C',
                borderRadius: 6,
                padding: '30px 40px',
                opacity: sp(75 + i * 10),
                transform: `translateY(${microFloat}px) scale(${sp(75 + i * 10)})`,
                boxShadow: '6px 6px 0px #1C1C1C',
              }}
            >
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 36,
                  fontWeight: 900,
                  color: '#1C1C1C',
                  textAlign: 'center',
                  letterSpacing: 2,
                }}
              >
                {type}
              </div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Stick Figure */}
      <div
        style={{
          position: 'absolute',
          bottom: 120,
          right: 130,
          opacity: sp(50),
          transform: `translateY(${(1 - sp(50)) * 80 + Math.sin(frame * 0.06) * 2}px)`,
        }}
      >
        <svg width="130" height="190" viewBox="0 0 100 140">
          {/* Head */}
          <circle cx="50" cy="20" r="14" fill="#FFFFFF" stroke="#1C1C1C" strokeWidth="2.5" />
          {/* Glasses */}
          <rect x="38" y="17" width="24" height="7" fill="none" stroke="#e9d700" strokeWidth="2" rx="2" />
          {/* Focused eyes */}
          <circle cx="44" cy="19" r="2" fill="#1C1C1C" />
          <circle cx="56" cy="19" r="2" fill="#1C1C1C" />
          {/* Body */}
          <line x1="50" y1="34" x2="50" y2="75" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Arms - analytical */}
          <line x1="50" y1="48" x2="32" y2="58" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="48" x2="68" y2="58" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Legs */}
          <line x1="50" y1="75" x2="44" y2="105" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="75" x2="56" y2="105" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 24,
          fontWeight: 600,
          color: '#1C1C1C',
          opacity: interpolate(frame, [0, 30], [0, 0.2], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5_ActualData;
```

