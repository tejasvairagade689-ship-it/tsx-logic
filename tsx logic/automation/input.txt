```tsx
SCENE 1 | [00:00 - 00:09]

Audio: "Hello friends, if you're turning 65 this year, or you just turned 65, I need you to stay with me until the very end of this video."

import React from 'react'; import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion'; export const compositionConfig = { id: 'Scene01_HookOpening', durationInSeconds: 9, fps: 30, width: 1920, height: 1080, }; const seededRandom = (seed: number): number => { const x = Math.sin(seed * 9999) * 10000; return x - Math.floor(x); }; const Scene01_HookOpening: React.FC = () => { const frame = useCurrentFrame(); const { fps, durationInFrames } = useVideoConfig(); const sp = (delay: number, config?: object) => spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } }); const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', }); // Camera slow zoom in const cameraZoom = interpolate(frame, [0, durationInFrames], [1, 1.08], { extrapolateRight: 'clamp' }); // Gradient breathing const gradShift = Math.sin(frame * 0.02) * 8; // Hero "65" entrance const heroScale = sp(20, { damping: 10, stiffness: 90 }); const heroOp = interpolate(frame, [20, 40], [0, 1], { extrapolateRight: 'clamp' }); // Headline reveal const headlineX = interpolate(sp(55), [0, 1], [-150, 0]); const headlineOp = interpolate(frame, [55, 75], [0, 1], { extrapolateRight: 'clamp' }); // Sub reveal const subOp = interpolate(frame, [90, 110], [0, 1], { extrapolateRight: 'clamp' }); const subY = interpolate(sp(90), [0, 1], [30, 0]); // Pulsing ring around 65 const ringScale = 1 + Math.sin(frame * 0.08) * 0.05; const ringOp = 0.3 + Math.sin(frame * 0.08) * 0.15; // Stick figure entrance const figOp = interpolate(frame, [60, 90], [0, 1], { extrapolateRight: 'clamp' }); const figY = Math.sin(frame * 0.05) * 2; const figHeadRot = Math.sin(frame * 0.04) * 3; // Shake at frame 35 (impact) const shakeX = frame > 35 && frame < 50 ? Math.sin(frame * 2) * (50 - frame) * 0.4 : 0; return ( <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}> {/* Gradient background */} <AbsoluteFill style={{ background: `radial-gradient(circle at ${50 + gradShift}% ${40 + gradShift}%, #2D2D2D 0%, #1C1C1C 65%)`, }} /> {/* Shape Layer - large drifting geometric forms */} <div style={{ position: 'absolute', top: -200, left: -100, width: 700, height: 700, borderRadius: '50%', border: '2px solid #e9d700', opacity: 0.06, transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`, }} /> <div style={{ position: 'absolute', bottom: -150, right: -100, width: 550, height: 550, background: '#FFC107', opacity: 0.04, transform: `rotate(${-frame * 0.08}deg)`, }} /> <div style={{ position: 'absolute', top: '20%', right: '15%', width: 0, height: 0, borderLeft: '120px solid transparent', borderRight: '120px solid transparent', borderBottom: '200px solid #e9d700', opacity: 0.05, transform: `rotate(${frame * 0.15}deg)`, }} /> {/* Motion Texture - horizontal lines */} <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.08 }}> {Array.from({ length: 18 }).map((_, i) => ( <line key={i} x1={0} y1={i * 60 + (frame * 0.5) % 60} x2={1920} y2={i * 60 + (frame * 0.5) % 60} stroke="#e9d700" strokeWidth={1} /> ))} </svg> {/* Floating Particles */} {Array.from({ length: 30 }).map((_, i) => { const seed = seededRandom(i + 1); const x = seed * 1920; const y = (seededRandom(i + 50) * 1080 + frame * 0.7) % 1080; const size = 2 + seededRandom(i + 100) * 4; return ( <div key={i} style={{ position: 'absolute', left: x, top: y, width: size, height: size, borderRadius: '50%', background: '#e9d700', opacity: 0.5, }} /> ); })} {/* Main Content */} <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', transform: `scale(${cameraZoom}) translateX(${shakeX}px)`, }} > {/* Pulsing ring behind 65 */} <div style={{ position: 'absolute', top: '20%', width: 500, height: 500, borderRadius: '50%', border: '4px solid #e9d700', opacity: ringOp * heroOp, transform: `scale(${ringScale * heroScale})`, }} /> {/* Hero "65" */} <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 260, fontWeight: 900, color: '#e9d700', lineHeight: 1, opacity: heroOp, transform: `scale(${heroScale})`, textShadow: '0 0 40px rgba(233, 215, 0, 0.5)', marginBottom: 20, }} > 65 </div> {/* Headline */} <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 110, fontWeight: 800, color: '#F5F5F5', opacity: headlineOp, transform: `translateX(${headlineX}px)`, letterSpacing: -2, marginBottom: 20, }} > TURNING <span style={{ color: '#ff0000' }}>65?</span> </div> {/* Subhead */} <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 50, fontWeight: 600, color: '#9E9E9E', opacity: subOp, transform: `translateY(${subY}px)`, letterSpacing: 4, textTransform: 'uppercase', }}> Watch Until The End </div> </div> {/* Stick Figure with Buffett glasses */} <div style={{ position: 'absolute', bottom: 80, left: 100, opacity: figOp, transform: `translateY(${figY}px)`, }} > {/* Golden glow */} <div style={{ position: 'absolute', top: 20, left: -20, width: 140, height: 140, borderRadius: '50%', background: '#FFC107', filter: 'blur(40px)', opacity: 0.3, }} /> <svg width={120} height={220} viewBox="0 0 120 220"> {/* Head */} <g transform={`rotate(${figHeadRot} 60 50)`}> <circle cx={60} cy={50} r={28} fill="none" stroke="#F5F5F5" strokeWidth={4} /> {/* Buffett glasses */} <circle cx={50} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <circle cx={70} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <line x1={59} y1={48} x2={61} y2={48} stroke="#FFC107" strokeWidth={3} /> {/* Smile */} <path d="M 50 60 Q 60 67 70 60" fill="none" stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" /> </g> {/* Body */} <line x1={60} y1={80} x2={60} y2={150} stroke="#F5F5F5" strokeWidth={4} /> {/* Arms */} <line x1={60} y1={100} x2={30} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={100} x2={90} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> {/* Legs */} <line x1={60} y1={150} x2={40} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={80} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> </svg> </div> {/* Vignette */} <AbsoluteFill style={{ background: 'radial-gradient(circle at center, transparent 40%, rgba(0,0,0,0.6) 100%)', pointerEvents: 'none', }} /> {/* Watermark */} <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3, letterSpacing: 1, }} > Senior Fund USA </div> </AbsoluteFill> ); }; export default Scene01_HookOpening; 

SCENE 2 | [00:09 - 00:17]

Audio: "Because what I'm about to share could add hundreds, and in some cases thousands, of dollars to your social security check."

import React from 'react'; import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion'; export const compositionConfig = { id: 'Scene02_MoneyPromise', durationInSeconds: 8, fps: 30, width: 1920, height: 1080, }; const seededRandom = (seed: number): number => { const x = Math.sin(seed * 9999) * 10000; return x - Math.floor(x); }; const Scene02_MoneyPromise: React.FC = () => { const frame = useCurrentFrame(); const { fps, durationInFrames } = useVideoConfig(); const sp = (delay: number, config?: object) => spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } }); const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', }); // Camera pan right const cameraX = interpolate(frame, [0, durationInFrames], [30, -30], { extrapolateRight: 'clamp' }); // Counter animation: hundreds → thousands const counter1 = Math.floor(interpolate(frame, [20, 80], [0, 500], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })); const counter2 = Math.floor(interpolate(frame, [100, 180], [500, 4800], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })); const displayValue = frame < 100 ? counter1 : counter2; const counterScale = sp(20, { damping: 10 }); const counterColor = frame < 100 ? '#e9d700' : '#FFC107'; // Label flip const labelOp1 = interpolate(frame, [20, 35, 95, 105], [0, 1, 1, 0], { extrapolateRight: 'clamp' }); const labelOp2 = interpolate(frame, [105, 120], [0, 1], { extrapolateRight: 'clamp' }); // SS Check entrance from right const checkX = interpolate(sp(40), [0, 1], [300, 0]); const checkOp = interpolate(frame, [40, 60], [0, 1], { extrapolateRight: 'clamp' }); // Arrow up pulse const arrowY = Math.sin(frame * 0.15) * 8; // Figure const figOp = interpolate(frame, [50, 80], [0, 1], { extrapolateRight: 'clamp' }); const figY = Math.sin(frame * 0.05) * 2; const figHeadRot = Math.sin(frame * 0.04) * 3; // Impact shake at counter switch const shakeY = frame > 95 && frame < 110 ? Math.sin(frame * 3) * (110 - frame) * 0.5 : 0; return ( <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}> {/* Gradient background - diagonal */} <AbsoluteFill style={{ background: 'linear-gradient(135deg, #2D2D2D 0%, #1C1C1C 50%, #2D2D2D 100%)', }} /> {/* Shape Layer */} <div style={{ position: 'absolute', top: '10%', left: '5%', width: 400, height: 400, background: '#e9d700', opacity: 0.05, clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)', transform: `rotate(${frame * 0.1}deg)`, }} /> <div style={{ position: 'absolute', bottom: '5%', right: '8%', width: 480, height: 480, borderRadius: '50%', border: '3px dashed #FFC107', opacity: 0.08, transform: `rotate(${-frame * 0.12}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`, }} /> {/* Motion Texture - grid */} <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.06 }}> {Array.from({ length: 22 }).map((_, i) => ( <line key={`v${i}`} x1={i * 90} y1={0} x2={i * 90} y2={1080} stroke="#9E9E9E" strokeWidth={1} /> ))} {Array.from({ length: 13 }).map((_, i) => ( <line key={`h${i}`} x1={0} y1={i * 90} x2={1920} y2={i * 90} stroke="#9E9E9E" strokeWidth={1} /> ))} </svg> {/* Floating dollar particles */} {Array.from({ length: 18 }).map((_, i) => { const seed = seededRandom(i + 10); const x = seed * 1920; const y = (seededRandom(i + 30) * 1080 - frame * 1.2) % 1080; return ( <div key={i} style={{ position: 'absolute', left: x, top: y < 0 ? y + 1080 : y, fontFamily: 'Inter, sans-serif', fontSize: 24, fontWeight: 800, color: '#FFC107', opacity: 0.25, }} > $ </div> ); })} <div style={{ position: 'absolute', inset: 0, display: 'flex', alignItems: 'center', justifyContent: 'center', transform: `translateX(${cameraX}px) translateY(${shakeY}px)`, }} > <div style={{ display: 'flex', alignItems: 'center', gap: 80 }}> {/* Left: SS Check */} <div style={{ opacity: checkOp, transform: `translateX(${-checkX}px)`, background: '#F5F5F5', padding: '40px 50px', borderRadius: 12, border: '3px solid #2D2D2D', boxShadow: '0 20px 50px rgba(0,0,0,0.5)', width: 480, }} > <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E', fontWeight: 600, marginBottom: 8 }}> SOCIAL SECURITY ADMIN. </div> <div style={{ borderBottom: '2px solid #2D2D2D', marginBottom: 20 }} /> <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#2D2D2D', fontWeight: 600, marginBottom: 6 }}> Pay To The Order Of: </div> <div style={{ fontFamily: 'Inter', fontSize: 36, color: '#1C1C1C', fontWeight: 800, marginBottom: 20 }}> YOU </div> <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 56, color: '#e9d700', fontWeight: 900, background: '#1C1C1C', padding: '12px 24px', borderRadius: 8, textAlign: 'right', }} > ${displayValue.toLocaleString()} </div> </div> {/* Right: Counter & Arrow */} <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 20 }}> {/* Arrow up */} <div style={{ transform: `translateY(${arrowY}px)`, opacity: checkOp }}> <svg width={100} height={100} viewBox="0 0 100 100"> <path d="M 50 10 L 90 60 L 65 60 L 65 90 L 35 90 L 35 60 L 10 60 Z" fill="#FFC107" /> </svg> </div> {/* Label */} <div style={{ position: 'relative', height: 80 }}> <div style={{ position: 'absolute', fontFamily: 'Inter', fontSize: 70, fontWeight: 900, color: counterColor, opacity: labelOp1, letterSpacing: 2, }} > +HUNDREDS </div> <div style={{ position: 'absolute', fontFamily: 'Inter', fontSize: 70, fontWeight: 900, color: counterColor, opacity: labelOp2, letterSpacing: 2, transform: `scale(${1 + (frame > 105 ? sp(105) * 0.1 : 0)})`, }} > +THOUSANDS </div> </div> <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 600, color: '#F5F5F5', opacity: checkOp, letterSpacing: 3, textTransform: 'uppercase', }} > Added Monthly </div> </div> </div> </div> {/* Stick Figure */} <div style={{ position: 'absolute', bottom: 60, right: 120, opacity: figOp, transform: `translateY(${figY}px)`, }} > <div style={{ position: 'absolute', top: 20, left: -20, width: 140, height: 140, borderRadius: '50%', background: '#FFC107', filter: 'blur(40px)', opacity: 0.4, }} /> <svg width={120} height={220} viewBox="0 0 120 220"> <g transform={`rotate(${figHeadRot} 60 50)`}> <circle cx={60} cy={50} r={28} fill="none" stroke="#F5F5F5" strokeWidth={4} /> <circle cx={50} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <circle cx={70} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <line x1={59} y1={48} x2={61} y2={48} stroke="#FFC107" strokeWidth={3} /> <path d="M 48 62 Q 60 70 72 62" fill="none" stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" /> </g> <line x1={60} y1={80} x2={60} y2={150} stroke="#F5F5F5" strokeWidth={4} /> {/* Arm pointing up */} <line x1={60} y1={100} x2={25} y2={70} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={100} x2={95} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={40} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={80} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> </svg> </div> {/* Vignette */} <AbsoluteFill style={{ background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none', }} /> {/* Watermark */} <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3, letterSpacing: 1, }} > Senior Fund USA </div> </AbsoluteFill> ); }; export default Scene02_MoneyPromise; 

SCENE 3 | [00:17 - 00:25]

Audio: "And I mean real money. Not the kind of savings where you clip coupons and feel good about fifty cents off a can of soup."

import React from 'react'; import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion'; export const compositionConfig = { id: 'Scene03_NotCoupons', durationInSeconds: 8, fps: 30, width: 1920, height: 1080, }; const seededRandom = (seed: number): number => { const x = Math.sin(seed * 9999) * 10000; return x - Math.floor(x); }; const Scene03_NotCoupons: React.FC = () => { const frame = useCurrentFrame(); const { fps, durationInFrames } = useVideoConfig(); const sp = (delay: number, config?: object) => spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } }); const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', }); // "REAL MONEY" entrance const realOp = interpolate(frame, [10, 30], [0, 1], { extrapolateRight: 'clamp' }); const realScale = sp(10, { damping: 8, stiffness: 110 }); // Coupon entrance const couponOp = interpolate(frame, [60, 80], [0, 1], { extrapolateRight: 'clamp' }); const couponRot = interpolate(sp(60), [0, 1], [-30, -8]); // Soup can entrance const soupOp = interpolate(frame, [90, 110], [0, 1], { extrapolateRight: 'clamp' }); const soupY = interpolate(sp(90), [0, 1], [200, 0]); // Red X over coupon area at frame 140 const xScale = sp(140, { damping: 8, stiffness: 130 }); const xOp = interpolate(frame, [140, 160], [0, 1], { extrapolateRight: 'clamp' }); // Figure const figOp = interpolate(frame, [30, 60], [0, 1], { extrapolateRight: 'clamp' }); const figY = Math.sin(frame * 0.05) * 2; const figHeadRot = Math.sin(frame * 0.04) * 3; // Camera tilt const cameraRot = interpolate(frame, [0, durationInFrames], [-0.5, 0.5], { extrapolateRight: 'clamp' }); return ( <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}> {/* Gradient background */} <AbsoluteFill style={{ background: 'linear-gradient(180deg, #1C1C1C 0%, #2D2D2D 100%)', }} /> {/* Shape Layer */} <div style={{ position: 'absolute', top: '15%', left: '10%', width: 350, height: 350, background: '#ff0000', opacity: 0.04, borderRadius: 40, transform: `rotate(${frame * 0.08}deg)`, }} /> <div style={{ position: 'absolute', top: '50%', right: '8%', width: 420, height: 420, borderRadius: '50%', background: '#e9d700', opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`, }} /> <div style={{ position: 'absolute', bottom: '10%', left: '20%', width: 300, height: 300, border: '4px solid #F5F5F5', opacity: 0.04, transform: `rotate(${frame * 0.12}deg)`, }} /> {/* Motion Texture - diagonal lines */} <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.05 }}> {Array.from({ length: 25 }).map((_, i) => ( <line key={i} x1={-200 + i * 100} y1={0} x2={i * 100} y2={1080} stroke="#e9d700" strokeWidth={1} /> ))} </svg> {/* Particles */} {Array.from({ length: 22 }).map((_, i) => { const seed = seededRandom(i + 5); const x = (seed * 1920 + Math.sin(frame * 0.02 + i) * 30) % 1920; const y = (seededRandom(i + 70) * 1080 + frame * 0.6) % 1080; return ( <div key={i} style={{ position: 'absolute', left: x, top: y, width: 3, height: 3, borderRadius: '50%', background: '#FFC107', opacity: 0.5, }} /> ); })} {/* Main Content */} <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', alignItems: 'center', paddingTop: 120, transform: `rotate(${cameraRot}deg)`, }} > {/* Hero text */} <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 180, fontWeight: 900, color: '#F5F5F5', opacity: realOp, transform: `scale(${realScale})`, letterSpacing: -3, lineHeight: 1, }} > REAL <span style={{ color: '#e9d700' }}>MONEY.</span> </div> <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 44, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [40, 60], [0, 1], { extrapolateRight: 'clamp' }), marginTop: 20, letterSpacing: 2, textTransform: 'uppercase', }} > Not Couponing Pennies </div> {/* Coupon + Soup Row */} <div style={{ display: 'flex', gap: 100, marginTop: 80, alignItems: 'center', position: 'relative', }} > {/* Coupon */} <div style={{ opacity: couponOp, transform: `rotate(${couponRot}deg)`, background: '#e9d700', padding: '30px 50px', border: '4px dashed #2D2D2D', borderRadius: 8, position: 'relative', }} > <div style={{ fontFamily: 'Inter', fontSize: 24, fontWeight: 700, color: '#2D2D2D', letterSpacing: 2 }}> COUPON </div> <div style={{ fontFamily: 'Inter', fontSize: 80, fontWeight: 900, color: '#1C1C1C', lineHeight: 1, marginTop: 8 }}> 50¢ </div> <div style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 600, color: '#2D2D2D', marginTop: 6 }}> OFF ANY ITEM </div> </div> {/* Soup Can */} <div style={{ opacity: soupOp, transform: `translateY(${soupY}px)`, }} > <svg width={180} height={240} viewBox="0 0 180 240"> <ellipse cx={90} cy={30} rx={70} ry={15} fill="#9E9E9E" /> <rect x={20} y={30} width={140} height={180} fill="#ff0000" /> <ellipse cx={90} cy={210} rx={70} ry={15} fill="#2D2D2D" /> <rect x={20} y={90} width={140} height={60} fill="#F5F5F5" /> <text x={90} y={125} fontFamily="Inter" fontSize={28} fontWeight={900} fill="#ff0000" textAnchor="middle"> SOUP </text> <text x={90} y={175} fontFamily="Inter" fontSize={14} fontWeight={600} fill="#F5F5F5" textAnchor="middle"> CONDENSED </text> </svg> </div> {/* Red X over both */} <div style={{ position: 'absolute', top: '50%', left: '50%', transform: `translate(-50%, -50%) scale(${xScale})`, opacity: xOp, }} > <svg width={520} height={300} viewBox="0 0 520 300"> <line x1={40} y1={40} x2={480} y2={260} stroke="#ff0000" strokeWidth={20} strokeLinecap="round" /> <line x1={480} y1={40} x2={40} y2={260} stroke="#ff0000" strokeWidth={20} strokeLinecap="round" /> </svg> </div> </div> </div> {/* Stick Figure */} <div style={{ position: 'absolute', bottom: 70, left: 80, opacity: figOp, transform: `translateY(${figY}px)`, }} > <div style={{ position: 'absolute', top: 20, left: -20, width: 140, height: 140, borderRadius: '50%', background: '#FFC107', filter: 'blur(40px)', opacity: 0.3, }} /> <svg width={120} height={220} viewBox="0 0 120 220"> <g transform={`rotate(${figHeadRot} 60 50)`}> <circle cx={60} cy={50} r={28} fill="none" stroke="#F5F5F5" strokeWidth={4} /> <circle cx={50} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <circle cx={70} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <line x1={59} y1={48} x2={61} y2={48} stroke="#FFC107" strokeWidth={3} /> {/* Serious mouth */} <line x1={50} y1={65} x2={70} y2={65} stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" /> </g> <line x1={60} y1={80} x2={60} y2={150} stroke="#F5F5F5" strokeWidth={4} /> <line x1={60} y1={100} x2={30} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={100} x2={90} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={40} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={80} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> </svg> </div> {/* Vignette */} <AbsoluteFill style={{ background: 'radial-gradient(circle at center, transparent 40%, rgba(0,0,0,0.6) 100%)', pointerEvents: 'none', }} /> {/* Watermark */} <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3, letterSpacing: 1, }} > Senior Fund USA </div> </AbsoluteFill> ); }; export default Scene03_NotCoupons; 

SCENE 4 | [00:25 - 00:34]

Audio: "I'm talking about five specific social security strategies and bonuses that you're legally entitled to claim right now, but almost nobody actually does."

import React from 'react'; import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion'; export const compositionConfig = { id: 'Scene04_FiveBonuses', durationInSeconds: 9, fps: 30, width: 1920, height: 1080, }; const seededRandom = (seed: number): number => { const x = Math.sin(seed * 9999) * 10000; return x - Math.floor(x); }; const Scene04_FiveBonuses: React.FC = () => { const frame = useCurrentFrame(); const { fps, durationInFrames } = useVideoConfig(); const sp = (delay: number, config?: object) => spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } }); const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', }); // Big "5" reveal const fiveScale = sp(15, { damping: 9, stiffness: 100 }); const fiveOp = interpolate(frame, [15, 35], [0, 1], { extrapolateRight: 'clamp' }); // "BONUSES" reveal const bonusOp = interpolate(frame, [45, 65], [0, 1], { extrapolateRight: 'clamp' }); const bonusX = interpolate(sp(45), [0, 1], [200, 0]); // Subhead const subOp = interpolate(frame, [75, 95], [0, 1], { extrapolateRight: 'clamp' }); // 5 circle dots stagger const dotStarts = [110, 125, 140, 155, 170]; // "LEGAL" stamp const stampScale = sp(200, { damping: 6, stiffness: 140 }); const stampOp = interpolate(frame, [200, 215], [0, 1], { extrapolateRight: 'clamp' }); const stampRot = interpolate(sp(200), [0, 1], [-25, -12]); // Figure const figOp = interpolate(frame, [60, 90], [0, 1], { extrapolateRight: 'clamp' }); const figY = Math.sin(frame * 0.05) * 2; const figHeadRot = Math.sin(frame * 0.04) * 3; // Camera zoom out then in const cameraZoom = interpolate(frame, [0, 80, durationInFrames], [1.15, 1, 1.05], { extrapolateRight: 'clamp' }); return ( <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}> {/* Gradient background - radial gold */} <AbsoluteFill style={{ background: 'radial-gradient(ellipse at 50% 30%, #2D2D2D 0%, #1C1C1C 70%)', }} /> {/* Shape Layer - large pentagon */} <svg style={{ position: 'absolute', top: '15%', left: '50%', transform: `translateX(-50%) rotate(${frame * 0.1}deg)`, opacity: 0.05, }} width={700} height={700} viewBox="0 0 200 200" > <polygon points="100,10 190,80 155,190 45,190 10,80" fill="#e9d700" /> </svg> <div style={{ position: 'absolute', top: '50%', right: -100, width: 500, height: 500, borderRadius: '50%', border: '3px solid #FFC107', opacity: 0.06, transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})`, }} /> <div style={{ position: 'absolute', bottom: -100, left: -50, width: 400, height: 400, background: '#e9d700', opacity: 0.04, transform: `rotate(${-frame * 0.08}deg)`, }} /> {/* Motion Texture - concentric circles */} <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.06 }}> {Array.from({ length: 8 }).map((_, i) => ( <circle key={i} cx={960} cy={540} r={150 + i * 80 + Math.sin(frame * 0.03 + i) * 10} fill="none" stroke="#e9d700" strokeWidth={1} /> ))} </svg> {/* Particles */} {Array.from({ length: 30 }).map((_, i) => { const seed = seededRandom(i + 3); const x = seed * 1920; const y = (seededRandom(i + 60) * 1080 - frame * 0.8) % 1080; return ( <div key={i} style={{ position: 'absolute', left: x, top: y < 0 ? y + 1080 : y, width: 3, height: 3, borderRadius: '50%', background: '#FFC107', opacity: 0.5, }} /> ); })} {/* Main Content */} <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', transform: `scale(${cameraZoom})`, }} > {/* Big 5 with BONUSES */} <div style={{ display: 'flex', alignItems: 'center', gap: 40 }}> <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 360, fontWeight: 900, color: '#e9d700', lineHeight: 1, opacity: fiveOp, transform: `scale(${fiveScale})`, textShadow: '0 0 60px rgba(233, 215, 0, 0.5)', }} > 5 </div> <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 130, fontWeight: 900, color: '#F5F5F5', opacity: bonusOp, transform: `translateX(${bonusX}px)`, letterSpacing: -2, lineHeight: 1, }} > BONUSES </div> </div> <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 44, fontWeight: 600, color: '#9E9E9E', opacity: subOp, marginTop: 30, letterSpacing: 3, textTransform: 'uppercase', }} > Hidden Inside Social Security </div> {/* 5 number dots */} <div style={{ display: 'flex', gap: 30, marginTop: 60 }}> {[1, 2, 3, 4, 5].map((n, i) => { const dotScale = sp(dotStarts[i], { damping: 8, stiffness: 130 }); const dotOp = interpolate(frame, [dotStarts[i], dotStarts[i] + 15], [0, 1], { extrapolateRight: 'clamp' }); return ( <div key={n} style={{ width: 90, height: 90, borderRadius: '50%', background: '#FFC107', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'Inter', fontSize: 50, fontWeight: 900, color: '#1C1C1C', opacity: dotOp, transform: `scale(${dotScale})`, boxShadow: '0 8px 24px rgba(255, 193, 7, 0.4)', }} > {n} </div> ); })} </div> {/* LEGAL stamp */} <div style={{ marginTop: 50, border: '6px solid #ff0000', padding: '12px 40px', opacity: stampOp, transform: `rotate(${stampRot}deg) scale(${stampScale})`, fontFamily: 'Inter', fontSize: 56, fontWeight: 900, color: '#ff0000', letterSpacing: 6, }} > 100% LEGAL </div> </div> {/* Stick Figure */} <div style={{ position: 'absolute', bottom: 60, right: 100, opacity: figOp, transform: `translateY(${figY}px)`, }} > <div style={{ position: 'absolute', top: 20, left: -20, width: 140, height: 140, borderRadius: '50%', background: '#FFC107', filter: 'blur(40px)', opacity: 0.4, }} /> <svg width={120} height={220} viewBox="0 0 120 220"> <g transform={`rotate(${figHeadRot} 60 50)`}> <circle cx={60} cy={50} r={28} fill="none" stroke="#F5F5F5" strokeWidth={4} /> <circle cx={50} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <circle cx={70} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <line x1={59} y1={48} x2={61} y2={48} stroke="#FFC107" strokeWidth={3} /> <path d="M 48 60 Q 60 68 72 60" fill="none" stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" /> </g> <line x1={60} y1={80} x2={60} y2={150} stroke="#F5F5F5" strokeWidth={4} /> <line x1={60} y1={100} x2={30} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={100} x2={90} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={40} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={80} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> </svg> </div> {/* Vignette */} <AbsoluteFill style={{ background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none', }} /> {/* Watermark */} <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3, letterSpacing: 1, }} > Senior Fund USA </div> </AbsoluteFill> ); }; export default Scene04_FiveBonuses; 

SCENE 5 | [00:34 - 00:43]

Audio: "Here's the thing most people never find out. The Social Security Administration doesn't automatically give you the highest payout you qualify for. They just give you what you ask for."

import React from 'react'; import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion'; export const compositionConfig = { id: 'Scene05_SSASecret', durationInSeconds: 9, fps: 30, width: 1920, height: 1080, }; const seededRandom = (seed: number): number => { const x = Math.sin(seed * 9999) * 10000; return x - Math.floor(x); }; const Scene05_SSASecret: React.FC = () => { const frame = useCurrentFrame(); const { fps, durationInFrames } = useVideoConfig(); const sp = (delay: number, config?: object) => spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } }); const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', }); // Headline drop const headOp = interpolate(frame, [10, 30], [0, 1], { extrapolateRight: 'clamp' }); const headY = interpolate(sp(10), [0, 1], [-50, 0]); // SSA building entrance const buildingOp = interpolate(frame, [50, 75], [0, 1], { extrapolateRight: 'clamp' }); const buildingScale = sp(50, { damping: 10 }); // "DOESN'T AUTOMATICALLY" stamp const stampOp = interpolate(frame, [100, 120], [0, 1], { extrapolateRight: 'clamp' }); const stampScale = sp(100, { damping: 7, stiffness: 130 }); // "YOU MUST ASK" punchline const askOp = interpolate(frame, [170, 195], [0, 1], { extrapolateRight: 'clamp' }); const askScale = sp(170, { damping: 8, stiffness: 120 }); // Arrow const arrowDraw = interpolate(frame, [140, 170], [0, 1], { extrapolateRight: 'clamp' }); // Figure const figOp = interpolate(frame, [80, 110], [0, 1], { extrapolateRight: 'clamp' }); const figY = Math.sin(frame * 0.05) * 2; const figHeadRot = Math.sin(frame * 0.04) * 3; // Camera slow pan const cameraY = interpolate(frame, [0, durationInFrames], [-15, 15], { extrapolateRight: 'clamp' }); return ( <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}> {/* Gradient */} <AbsoluteFill style={{ background: 'linear-gradient(160deg, #2D2D2D 0%, #1C1C1C 60%, #2D2D2D 100%)', }} /> {/* Shape Layer */} <div style={{ position: 'absolute', top: '10%', right: '5%', width: 500, height: 500, borderRadius: '20%', background: '#ff0000', opacity: 0.04, transform: `rotate(${frame * 0.1}deg)`, }} /> <div style={{ position: 'absolute', bottom: -100, left: -100, width: 600, height: 600, borderRadius: '50%', border: '4px solid #e9d700', opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`, }} /> {/* Motion texture - dot grid */} <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.08 }}> {Array.from({ length: 12 }).map((_, row) => Array.from({ length: 22 }).map((_, col) => ( <circle key={`${row}-${col}`} cx={col * 90 + 40} cy={row * 90 + 40} r={2} fill="#9E9E9E" /> )) )} </svg> {/* Particles */} {Array.from({ length: 20 }).map((_, i) => { const seed = seededRandom(i + 12); const x = seed * 1920; const y = (seededRandom(i + 80) * 1080 + frame * 0.5) % 1080; return ( <div key={i} style={{ position: 'absolute', left: x, top: y, width: 3, height: 3, borderRadius: '50%', background: '#e9d700', opacity: 0.4, }} /> ); })} {/* Content */} <div style={{ position: 'absolute', inset: 0, padding: '60px 100px', transform: `translateY(${cameraY}px)`, }} > {/* Headline at top */} <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 80, fontWeight: 900, color: '#F5F5F5', opacity: headOp, transform: `translateY(${headY}px)`, textAlign: 'center', letterSpacing: -1, marginBottom: 50, }} > THE <span style={{ color: '#e9d700' }}>SSA SECRET</span> </div> {/* Main row: Building + Stamp */} <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 80, marginTop: 30 }}> {/* SSA Building SVG */} <div style={{ opacity: buildingOp, transform: `scale(${buildingScale})` }}> <svg width={340} height={380} viewBox="0 0 340 380"> {/* Steps */} <rect x={20} y={340} width={300} height={40} fill="#9E9E9E" /> {/* Columns */} <rect x={50} y={140} width={30} height={200} fill="#F5F5F5" /> <rect x={110} y={140} width={30} height={200} fill="#F5F5F5" /> <rect x={170} y={140} width={30} height={200} fill="#F5F5F5" /> <rect x={230} y={140} width={30} height={200} fill="#F5F5F5" /> <rect x={290} y={140} width={30} height={200} fill="#F5F5F5" /> {/* Roof base */} <rect x={30} y={120} width={290} height={20} fill="#F5F5F5" /> {/* Pediment */} <polygon points="20,120 175,40 330,120" fill="#FFC107" /> {/* Sign */} <rect x={100} y={75} width={150} height={30} fill="#1C1C1C" /> <text x={175} y={97} fontFamily="Inter" fontSize={18} fontWeight={900} fill="#F5F5F5" textAnchor="middle"> SSA </text> </svg> </div> {/* Stamp text */} <div style={{ opacity: stampOp, transform: `scale(${stampScale}) rotate(-4deg)`, border: '6px solid #ff0000', padding: '20px 40px', maxWidth: 700, }} > <div style={{ fontFamily: 'Inter', fontSize: 56, fontWeight: 900, color: '#ff0000', lineHeight: 1.1, letterSpacing: 1, }} > WON'T GIVE YOU </div> <div style={{ fontFamily: 'Inter', fontSize: 56, fontWeight: 900, color: '#F5F5F5', lineHeight: 1.1, letterSpacing: 1, }} > THE HIGHEST </div> <div style={{ fontFamily: 'Inter', fontSize: 56, fontWeight: 900, color: '#e9d700', lineHeight: 1.1, letterSpacing: 1, }} > PAYOUT </div> </div> </div> {/* Arrow + Punchline */} <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', marginTop: 30 }}> <svg width={80} height={60} viewBox="0 0 80 60" style={{ opacity: arrowDraw }}> <path d={`M 40 0 L 40 ${40 * arrowDraw}`} stroke="#FFC107" strokeWidth={6} fill="none" strokeLinecap="round" /> {arrowDraw > 0.9 && ( <path d="M 25 35 L 40 55 L 55 35" stroke="#FFC107" strokeWidth={6} fill="none" strokeLinecap="round" strokeLinejoin="round" /> )} </svg> <div style={{ fontFamily: 'Inter', fontSize: 110, fontWeight: 900, color: '#FFC107', opacity: askOp, transform: `scale(${askScale})`, letterSpacing: -1, marginTop: 10, textShadow: '0 0 30px rgba(255, 193, 7, 0.4)', }} > YOU MUST ASK. </div> </div> </div> {/* Stick Figure */} <div style={{ position: 'absolute', bottom: 60, left: 80, opacity: figOp, transform: `translateY(${figY}px)`, }} > <div style={{ position: 'absolute', top: 20, left: -20, width: 140, height: 140, borderRadius: '50%', background: '#FFC107', filter: 'blur(40px)', opacity: 0.3, }} /> <svg width={120} height={220} viewBox="0 0 120 220"> <g transform={`rotate(${figHeadRot} 60 50)`}> <circle cx={60} cy={50} r={28} fill="none" stroke="#F5F5F5" strokeWidth={4} /> <circle cx={50} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <circle cx={70} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <line x1={59} y1={48} x2={61} y2={48} stroke="#FFC107" strokeWidth={3} /> <path d="M 48 65 Q 60 60 72 65" fill="none" stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" /> </g> <line x1={60} y1={80} x2={60} y2={150} stroke="#F5F5F5" strokeWidth={4} /> {/* Hand at chin */} <line x1={60} y1={100} x2={50} y2={75} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={100} x2={90} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={40} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={80} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> </svg> </div> {/* Vignette */} <AbsoluteFill style={{ background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.6) 100%)', pointerEvents: 'none', }} /> {/* Watermark */} <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3, letterSpacing: 1, }} > Senior Fund USA </div> </AbsoluteFill> ); }; export default Scene05_SSASecret; 

SCENE 6 | [00:43 - 00:52]

Audio: "The money is sitting there. The rules are written in your favor. But every month that goes by without you making the right move is another month of leaving money on the table."

import React from 'react'; import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion'; export const compositionConfig = { id: 'Scene06_MoneyOnTable', durationInSeconds: 9, fps: 30, width: 1920, height: 1080, }; const seededRandom = (seed: number): number => { const x = Math.sin(seed * 9999) * 10000; return x - Math.floor(x); }; const Scene06_MoneyOnTable: React.FC = () => { const frame = useCurrentFrame(); const { fps, durationInFrames } = useVideoConfig(); const sp = (delay: number, config?: object) => spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } }); const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', }); // Table reveal const tableOp = interpolate(frame, [15, 35], [0, 1], { extrapolateRight: 'clamp' }); const tableY = interpolate(sp(15), [0, 1], [100, 0]); // Money stacks drop in (stagger) const stackStarts = [50, 65, 80, 95]; // Calendar entrance const calOp = interpolate(frame, [130, 155], [0, 1], { extrapolateRight: 'clamp' }); const calX = interpolate(sp(130), [0, 1], [-200, 0]); // Days flipping animation const dayFlip = Math.floor(interpolate(frame, [160, 240], [1, 12], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })); // Bottom headline const headOp = interpolate(frame, [200, 225], [0, 1], { extrapolateRight: 'clamp' }); const headY = interpolate(sp(200), [0, 1], [40, 0]); // Figure const figOp = interpolate(frame, [50, 80], [0, 1], { extrapolateRight: 'clamp' }); const figY = Math.sin(frame * 0.05) * 2; const figHeadRot = Math.sin(frame * 0.04) * 3; // Camera slow zoom const cameraZoom = interpolate(frame, [0, durationInFrames], [1.05, 1], { extrapolateRight: 'clamp' }); return ( <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}> {/* Gradient - warm gold base */} <AbsoluteFill style={{ background: 'radial-gradient(ellipse at 50% 60%, #2D2D2D 0%, #1C1C1C 70%)', }} /> {/* Shape Layer */} <div style={{ position: 'absolute', top: '5%', left: '5%', width: 450, height: 450, borderRadius: '50%', background: '#FFC107', opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06})`, }} /> <div style={{ position: 'absolute', top: '10%', right: '8%', width: 380, height: 380, border: '3px solid #e9d700', opacity: 0.07, transform: `rotate(${frame * 0.12}deg)`, }} /> <div style={{ position: 'absolute', bottom: -100, right: -100, width: 500, height: 500, background: '#e9d700', opacity: 0.04, clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)', }} /> {/* Motion texture - vertical lines */} <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.06 }}> {Array.from({ length: 20 }).map((_, i) => ( <line key={i} x1={i * 100} y1={0} x2={i * 100} y2={1080} stroke="#9E9E9E" strokeWidth={1} /> ))} </svg> {/* Money particles falling */} {Array.from({ length: 20 }).map((_, i) => { const seed = seededRandom(i + 20); const x = seed * 1920; const y = (seededRandom(i + 90) * 1080 + frame * 1.5) % 1080; return ( <div key={i} style={{ position: 'absolute', left: x, top: y, width: 12, height: 6, background: '#FFC107', opacity: 0.3, transform: `rotate(${frame * 2 + i * 30}deg)`, }} /> ); })} {/* Content */} <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', alignItems: 'center', paddingTop: 80, transform: `scale(${cameraZoom})`, }} > {/* Top text */} <div style={{ fontFamily: 'Inter', fontSize: 64, fontWeight: 900, color: '#F5F5F5', opacity: interpolate(frame, [0, 20], [0, 1], { extrapolateRight: 'clamp' }), letterSpacing: 1, marginBottom: 30, textAlign: 'center', }} > THE MONEY IS <span style={{ color: '#e9d700' }}>SITTING THERE.</span> </div> {/* Table with money */} <div style={{ position: 'relative', opacity: tableOp, transform: `translateY(${tableY}px)`, marginTop: 30, }} > {/* Table top */} <div style={{ width: 900, height: 30, background: 'linear-gradient(180deg, #FFC107 0%, #c89800 100%)', borderRadius: '6px 6px 0 0', position: 'relative', }} /> {/* Money stacks */} <div style={{ position: 'absolute', top: -110, left: 0, right: 0, display: 'flex', justifyContent: 'space-around', }} > {[0, 1, 2, 3].map((i) => { const stackOp = interpolate(frame, [stackStarts[i], stackStarts[i] + 20], [0, 1], { extrapolateRight: 'clamp' }); const stackY = interpolate(sp(stackStarts[i]), [0, 1], [-150, 0]); return ( <div key={i} style={{ width: 130, height: 110, opacity: stackOp, transform: `translateY(${stackY}px)`, position: 'relative', }} > {/* Money stack layers */} {[0, 1, 2, 3, 4].map((j) => ( <div key={j} style={{ position: 'absolute', bottom: j * 18, left: 0, width: 130, height: 22, background: '#e9d700', border: '2px solid #2D2D2D', borderRadius: 3, display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'Inter', fontSize: 14, fontWeight: 900, color: '#1C1C1C', }} > $100 </div> ))} </div> ); })} </div> {/* Table legs */} <div style={{ position: 'absolute', bottom: -120, left: 30, width: 20, height: 120, background: '#9E9E9E' }} /> <div style={{ position: 'absolute', bottom: -120, right: 30, width: 20, height: 120, background: '#9E9E9E' }} /> </div> {/* Calendar with ticking days */} <div style={{ position: 'absolute', top: 200, right: 100, opacity: calOp, transform: `translateX(${calX}px)`, background: '#F5F5F5', padding: 16, borderRadius: 8, width: 200, boxShadow: '0 12px 30px rgba(0,0,0,0.4)', }} > <div style={{ background: '#ff0000', padding: '8px 0', fontFamily: 'Inter', fontSize: 18, fontWeight: 800, color: '#F5F5F5', textAlign: 'center', letterSpacing: 2, borderRadius: 4, }} > MONTH {dayFlip} </div> <div style={{ fontFamily: 'Inter', fontSize: 90, fontWeight: 900, color: '#1C1C1C', textAlign: 'center', lineHeight: 1.1, marginTop: 8, }} > {dayFlip} </div> <div style={{ fontFamily: 'Inter', fontSize: 14, fontWeight: 600, color: '#9E9E9E', textAlign: 'center', letterSpacing: 1, }} > LOST FOREVER </div> </div> {/* Bottom punchline */} <div style={{ position: 'absolute', bottom: 140, fontFamily: 'Inter', fontSize: 60, fontWeight: 900, color: '#ff0000', opacity: headOp, transform: `translateY(${headY}px)`, textAlign: 'center', letterSpacing: -1, }} > LEFT ON THE TABLE. </div> </div> {/* Stick Figure */} <div style={{ position: 'absolute', bottom: 50, left: 80, opacity: figOp, transform: `translateY(${figY}px)`, }} > <div style={{ position: 'absolute', top: 20, left: -20, width: 140, height: 140, borderRadius: '50%', background: '#FFC107', filter: 'blur(40px)', opacity: 0.3, }} /> <svg width={120} height={220} viewBox="0 0 120 220"> <g transform={`rotate(${figHeadRot} 60 50)`}> <circle cx={60} cy={50} r={28} fill="none" stroke="#F5F5F5" strokeWidth={4} /> <circle cx={50} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <circle cx={70} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <line x1={59} y1={48} x2={61} y2={48} stroke="#FFC107" strokeWidth={3} /> {/* Concerned mouth */} <path d="M 50 67 Q 60 62 70 67" fill="none" stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" /> </g> <line x1={60} y1={80} x2={60} y2={150} stroke="#F5F5F5" strokeWidth={4} /> {/* Arm pointing at table */} <line x1={60} y1={100} x2={100} y2={115} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={100} x2={25} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={40} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={80} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> </svg> </div> {/* Vignette */} <AbsoluteFill style={{ background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.6) 100%)', pointerEvents: 'none', }} /> {/* Watermark */} <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3, letterSpacing: 1, }} > Senior Fund USA </div> </AbsoluteFill> ); }; export default Scene06_MoneyOnTable; 

SCENE 7 | [00:52 - 01:01]

Audio: "I went through the latest SSA guidelines, the 2026 cost of living adjustments, and the updated Medicare rules."

import React from 'react'; import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion'; export const compositionConfig = { id: 'Scene07_Research', durationInSeconds: 9, fps: 30, width: 1920, height: 1080, }; const seededRandom = (seed: number): number => { const x = Math.sin(seed * 9999) * 10000; return x - Math.floor(x); }; const Scene07_Research: React.FC = () => { const frame = useCurrentFrame(); const { fps, durationInFrames } = useVideoConfig(); const sp = (delay: number, config?: object) => spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } }); const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', }); // Headline const headOp = interpolate(frame, [10, 30], [0, 1], { extrapolateRight: 'clamp' }); // 3 document cards stagger const cardStarts = [50, 100, 150]; const cardTitles = ['SSA GUIDELINES', '2026 COLA', 'MEDICARE RULES']; const cardSubs = ['Latest Updates', 'Cost of Living', 'New Provisions']; const cardIcons = ['📋', '📊', '🏥']; // will use SVG below instead const cardColors = ['#e9d700', '#FFC107', '#ff0000']; // Stamp at end const stampOp = interpolate(frame, [210, 230], [0, 1], { extrapolateRight: 'clamp' }); const stampScale = sp(210, { damping: 7, stiffness: 120 }); // Figure const figOp = interpolate(frame, [60, 90], [0, 1], { extrapolateRight: 'clamp' }); const figY = Math.sin(frame * 0.05) * 2; const figHeadRot = Math.sin(frame * 0.04) * 3; // Camera pan const cameraX = interpolate(frame, [0, durationInFrames], [-20, 20], { extrapolateRight: 'clamp' }); return ( <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}> {/* Gradient */} <AbsoluteFill style={{ background: 'linear-gradient(135deg, #1C1C1C 0%, #2D2D2D 50%, #1C1C1C 100%)', }} /> {/* Shape Layer */} <div style={{ position: 'absolute', top: '10%', left: '5%', width: 350, height: 350, borderRadius: 30, border: '4px solid #e9d700', opacity: 0.06, transform: `rotate(${frame * 0.1}deg)`, }} /> <div style={{ position: 'absolute', bottom: '5%', right: '5%', width: 450, height: 450, background: '#FFC107', opacity: 0.04, borderRadius: '50%', transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`, }} /> <div style={{ position: 'absolute', top: '50%', left: '50%', width: 600, height: 600, border: '2px dashed #9E9E9E', opacity: 0.05, borderRadius: '50%', transform: `translate(-50%, -50%) rotate(${frame * 0.05}deg)`, }} /> {/* Motion texture - blueprint grid */} <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.05 }}> {Array.from({ length: 20 }).map((_, i) => ( <line key={`v${i}`} x1={i * 100} y1={0} x2={i * 100} y2={1080} stroke="#e9d700" strokeWidth={1} /> ))} {Array.from({ length: 12 }).map((_, i) => ( <line key={`h${i}`} x1={0} y1={i * 100} x2={1920} y2={i * 100} stroke="#e9d700" strokeWidth={1} /> ))} </svg> {/* Particles */} {Array.from({ length: 25 }).map((_, i) => { const seed = seededRandom(i + 7); const x = seed * 1920; const y = (seededRandom(i + 40) * 1080 - frame * 0.6) % 1080; return ( <div key={i} style={{ position: 'absolute', left: x, top: y < 0 ? y + 1080 : y, width: 2, height: 2, borderRadius: '50%', background: '#e9d700', opacity: 0.4, }} /> ); })} {/* Content */} <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', alignItems: 'center', paddingTop: 80, transform: `translateX(${cameraX}px)`, }} > {/* Headline */} <div style={{ fontFamily: 'Inter', fontSize: 72, fontWeight: 900, color: '#F5F5F5', opacity: headOp, letterSpacing: -1, textAlign: 'center', marginBottom: 20, }} > I DID THE <span style={{ color: '#e9d700' }}>RESEARCH.</span> </div> <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 600, color: '#9E9E9E', opacity: headOp, letterSpacing: 3, textTransform: 'uppercase', marginBottom: 70, }} > So You Don't Have To </div> {/* 3 document cards */} <div style={{ display: 'flex', gap: 50 }}> {cardTitles.map((title, i) => { const cardOp = interpolate(frame, [cardStarts[i], cardStarts[i] + 25], [0, 1], { extrapolateRight: 'clamp' }); const cardY = interpolate(sp(cardStarts[i]), [0, 1], [150, 0]); const cardRot = Math.sin(frame * 0.03 + i) * 1; return ( <div key={i} style={{ width: 360, height: 460, background: '#F5F5F5', borderRadius: 12, opacity: cardOp, transform: `translateY(${cardY}px) rotate(${cardRot}deg)`, padding: 30, boxShadow: '0 20px 50px rgba(0,0,0,0.5)', border: `5px solid ${cardColors[i]}`, display: 'flex', flexDirection: 'column', }} > {/* Top tab */} <div style={{ background: cardColors[i], color: cardColors[i] === '#FFC107' || cardColors[i] === '#e9d700' ? '#1C1C1C' : '#F5F5F5', padding: '6px 14px', borderRadius: 4, fontFamily: 'Inter', fontSize: 14, fontWeight: 800, letterSpacing: 2, width: 'fit-content', }} > DOC #{i + 1} </div> {/* Icon */} <div style={{ marginTop: 30, marginBottom: 20 }}> {i === 0 && ( <svg width={90} height={90} viewBox="0 0 90 90"> <rect x={15} y={10} width={60} height={75} fill={cardColors[i]} rx={4} /> <line x1={25} y1={28} x2={65} y2={28} stroke="#1C1C1C" strokeWidth={3} /> <line x1={25} y1={42} x2={65} y2={42} stroke="#1C1C1C" strokeWidth={3} /> <line x1={25} y1={56} x2={50} y2={56} stroke="#1C1C1C" strokeWidth={3} /> </svg> )} {i === 1 && ( <svg width={90} height={90} viewBox="0 0 90 90"> <rect x={15} y={50} width={12} height={35} fill={cardColors[i]} /> <rect x={32} y={35} width={12} height={50} fill={cardColors[i]} /> <rect x={49} y={20} width={12} height={65} fill={cardColors[i]} /> <rect x={66} y={10} width={12} height={75} fill={cardColors[i]} /> </svg> )} {i === 2 && ( <svg width={90} height={90} viewBox="0 0 90 90"> <rect x={10} y={20} width={70} height={60} fill={cardColors[i]} rx={4} /> <rect x={38} y={32} width={14} height={36} fill="#F5F5F5" /> <rect x={27} y={43} width={36} height={14} fill="#F5F5F5" /> </svg> )} </div> {/* Title */} <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 900, color: '#1C1C1C', lineHeight: 1.1, }} > {title} </div> <div style={{ fontFamily: 'Inter', fontSize: 20, fontWeight: 600, color: '#9E9E9E', marginTop: 12, letterSpacing: 1, }} > {cardSubs[i]} </div> {/* Bottom lines */} <div style={{ marginTop: 'auto' }}> <div style={{ height: 4, background: '#9E9E9E', opacity: 0.3, marginBottom: 8 }} /> <div style={{ height: 4, background: '#9E9E9E', opacity: 0.3, marginBottom: 8, width: '80%' }} /> <div style={{ height: 4, background: '#9E9E9E', opacity: 0.3, width: '60%' }} /> </div> </div> ); })} </div> {/* "VERIFIED" stamp */} <div style={{ marginTop: 40, border: '5px solid #ff0000', padding: '10px 30px', opacity: stampOp, transform: `scale(${stampScale}) rotate(-6deg)`, fontFamily: 'Inter', fontSize: 38, fontWeight: 900, color: '#ff0000', letterSpacing: 4, }} > ✓ VERIFIED 2026 </div> </div> {/* Stick Figure */} <div style={{ position: 'absolute', bottom: 50, right: 80, opacity: figOp, transform: `translateY(${figY}px)`, }} > <div style={{ position: 'absolute', top: 20, left: -20, width: 140, height: 140, borderRadius: '50%', background: '#FFC107', filter: 'blur(40px)', opacity: 0.4, }} /> <svg width={120} height={220} viewBox="0 0 120 220"> <g transform={`rotate(${figHeadRot} 60 50)`}> <circle cx={60} cy={50} r={28} fill="none" stroke="#F5F5F5" strokeWidth={4} /> <circle cx={50} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <circle cx={70} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <line x1={59} y1={48} x2={61} y2={48} stroke="#FFC107" strokeWidth={3} /> <path d="M 48 60 Q 60 68 72 60" fill="none" stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" /> </g> <line x1={60} y1={80} x2={60} y2={150} stroke="#F5F5F5" strokeWidth={4} /> <line x1={60} y1={100} x2={30} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={100} x2={90} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={40} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={80} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> </svg> </div> {/* Vignette */} <AbsoluteFill style={{ background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none', }} /> {/* Watermark */} <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3, letterSpacing: 1, }} > Senior Fund USA </div> </AbsoluteFill> ); }; export default Scene07_Research; 

SCENE 8 | [01:01 - 01:10]

Audio: "And I found five specific bonuses that millions of seniors turning 65 are missing out on."

import React from 'react'; import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion'; export const compositionConfig = { id: 'Scene08_MillionsMissing', durationInSeconds: 9, fps: 30, width: 1920, height: 1080, }; const seededRandom = (seed: number): number => { const x = Math.sin(seed * 9999) * 10000; return x - Math.floor(x); }; const Scene08_MillionsMissing: React.FC = () => { const frame = useCurrentFrame(); const { fps, durationInFrames } = useVideoConfig(); const sp = (delay: number, config?: object) => spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } }); const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', }); // Number counter const counterVal = Math.floor(interpolate(frame, [15, 90], [0, 11400000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })); const counterScale = sp(15, { damping: 10 }); // Label const labelOp = interpolate(frame, [40, 60], [0, 1], { extrapolateRight: 'clamp' }); // Crowd silhouettes - stagger const crowdOp = interpolate(frame, [100, 130], [0, 1], { extrapolateRight: 'clamp' }); // Red "MISSING OUT" stamp const missOp = interpolate(frame, [160, 180], [0, 1], { extrapolateRight: 'clamp' }); const missScale = sp(160, { damping: 7, stiffness: 130 }); // Bonus icons (5 small ones drifting up) const iconOp = interpolate(frame, [200, 230], [0, 1], { extrapolateRight: 'clamp' }); // Figure const figOp = interpolate(frame, [70, 100], [0, 1], { extrapolateRight: 'clamp' }); const figY = Math.sin(frame * 0.05) * 2; const figHeadRot = Math.sin(frame * 0.04) * 3; // Camera zoom const cameraZoom = interpolate(frame, [0, 90, durationInFrames], [1.1, 1, 1.04], { extrapolateRight: 'clamp' }); // Shake on counter hit const shakeY = frame > 85 && frame < 100 ? Math.sin(frame * 3) * (100 - frame) * 0.4 : 0; return ( <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}> {/* Gradient */} <AbsoluteFill style={{ background: 'radial-gradient(ellipse at 50% 40%, #2D2D2D 0%, #1C1C1C 70%)', }} /> {/* Shape Layer */} <div style={{ position: 'absolute', top: -100, right: -100, width: 550, height: 550, borderRadius: '50%', background: '#ff0000', opacity: 0.05, }} /> <div style={{ position: 'absolute', bottom: -150, left: -100, width: 500, height: 500, border: '4px solid #e9d700', opacity: 0.06, transform: `rotate(${frame * 0.1}deg)`, }} /> <div style={{ position: 'absolute', top: '40%', left: '40%', width: 300, height: 300, background: '#FFC107', opacity: 0.04, borderRadius: 20, transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`, }} /> {/* Motion texture - radial lines */} <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.05 }}> {Array.from({ length: 16 }).map((_, i) => { const angle = (i / 16) * Math.PI * 2; return ( <line key={i} x1={960} y1={540} x2={960 + Math.cos(angle) * 1500} y2={540 + Math.sin(angle) * 1500} stroke="#9E9E9E" strokeWidth={1} /> ); })} </svg> {/* Particles */} {Array.from({ length: 25 }).map((_, i) => { const seed = seededRandom(i + 9); const x = seed * 1920; const y = (seededRandom(i + 33) * 1080 - frame * 0.7) % 1080; return ( <div key={i} style={{ position: 'absolute', left: x, top: y < 0 ? y + 1080 : y, width: 3, height: 3, borderRadius: '50%', background: '#FFC107', opacity: 0.5, }} /> ); })} {/* Content */} <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', alignItems: 'center', paddingTop: 100, transform: `scale(${cameraZoom}) translateY(${shakeY}px)`, }} > {/* Counter */} <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 220, fontWeight: 900, color: '#ff0000', lineHeight: 1, transform: `scale(${counterScale})`, textShadow: '0 0 50px rgba(255, 0, 0, 0.5)', }} > {counterVal.toLocaleString()} </div> <div style={{ fontFamily: 'Inter', fontSize: 60, fontWeight: 900, color: '#F5F5F5', opacity: labelOp, letterSpacing: 1, marginTop: 10, }} > SENIORS TURNING <span style={{ color: '#e9d700' }}>65</span> </div> {/* Crowd silhouettes */} <div style={{ display: 'flex', justifyContent: 'center', gap: 8, marginTop: 50, opacity: crowdOp, }} > {Array.from({ length: 12 }).map((_, i) => ( <svg key={i} width={60} height={100} viewBox="0 0 60 100"> <circle cx={30} cy={20} r={14} fill={i % 3 === 0 ? '#ff0000' : '#9E9E9E'} opacity={0.7} /> <path d="M 10 100 L 10 50 Q 30 35 50 50 L 50 100 Z" fill={i % 3 === 0 ? '#ff0000' : '#9E9E9E'} opacity={0.7} /> </svg> ))} </div> {/* Missing out stamp */} <div style={{ marginTop: 40, border: '5px solid #ff0000', padding: '14px 40px', background: 'rgba(255, 0, 0, 0.1)', opacity: missOp, transform: `scale(${missScale}) rotate(-4deg)`, fontFamily: 'Inter', fontSize: 56, fontWeight: 900, color: '#ff0000', letterSpacing: 4, }} > MISSING OUT </div> {/* 5 floating bonus dots */} <div style={{ display: 'flex', gap: 24, marginTop: 30, opacity: iconOp }}> {[1, 2, 3, 4, 5].map((n, i) => ( <div key={n} style={{ width: 60, height: 60, borderRadius: '50%', background: '#FFC107', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'Inter', fontSize: 32, fontWeight: 900, color: '#1C1C1C', transform: `translateY(${Math.sin(frame * 0.08 + i * 0.5) * 6}px)`, boxShadow: '0 6px 20px rgba(255, 193, 7, 0.4)', }} > {n} </div> ))} </div> </div> {/* Stick Figure */} <div style={{ position: 'absolute', bottom: 50, left: 80, opacity: figOp, transform: `translateY(${figY}px)`, }} > <div style={{ position: 'absolute', top: 20, left: -20, width: 140, height: 140, borderRadius: '50%', background: '#FFC107', filter: 'blur(40px)', opacity: 0.3, }} /> <svg width={120} height={220} viewBox="0 0 120 220"> <g transform={`rotate(${figHeadRot} 60 50)`}> <circle cx={60} cy={50} r={28} fill="none" stroke="#F5F5F5" strokeWidth={4} /> <circle cx={50} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <circle cx={70} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <line x1={59} y1={48} x2={61} y2={48} stroke="#FFC107" strokeWidth={3} /> <path d="M 50 67 Q 60 62 70 67" fill="none" stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" /> </g> <line x1={60} y1={80} x2={60} y2={150} stroke="#F5F5F5" strokeWidth={4} /> <line x1={60} y1={100} x2={30} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={100} x2={90} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={40} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={80} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> </svg> </div> {/* Vignette */} <AbsoluteFill style={{ background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.6) 100%)', pointerEvents: 'none', }} /> {/* Watermark */} <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3, letterSpacing: 1, }} > Senior Fund USA </div> </AbsoluteFill> ); }; export default Scene08_MillionsMissing; 

SCENE 9 | [01:10 - 01:19]

Audio: "I'll walk through each one, tell you exactly who qualifies, what the numbers look like, and what to do about it."

import React from 'react'; import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion'; export const compositionConfig = { id: 'Scene09_WalkThrough', durationInSeconds: 9, fps: 30, width: 1920, height: 1080, }; const seededRandom = (seed: number): number => { const x = Math.sin(seed * 9999) * 10000; return x - Math.floor(x); }; const Scene09_WalkThrough: React.FC = () => { const frame = useCurrentFrame(); const { fps, durationInFrames } = useVideoConfig(); const sp = (delay: number, config?: object) => spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } }); const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', }); // Headline const headOp = interpolate(frame, [10, 30], [0, 1], { extrapolateRight: 'clamp' }); const headY = interpolate(sp(10), [0, 1], [-40, 0]); // Three checklist items stagger const items = ['WHO QUALIFIES', 'THE NUMBERS', 'WHAT TO DO']; const itemStarts = [50, 110, 170]; // Big "EVERYTHING" reveal const everyOp = interpolate(frame, [225, 250], [0, 1], { extrapolateRight: 'clamp' }); const everyScale = sp(225, { damping: 8, stiffness: 110 }); // Figure const figOp = interpolate(frame, [60, 90], [0, 1], { extrapolateRight: 'clamp' }); const figY = Math.sin(frame * 0.05) * 2; const figHeadRot = Math.sin(frame * 0.04) * 3; // Camera slow pan up const cameraY = interpolate(frame, [0, durationInFrames], [20, -20], { extrapolateRight: 'clamp' }); return ( <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}> {/* Gradient */} <AbsoluteFill style={{ background: 'linear-gradient(180deg, #2D2D2D 0%, #1C1C1C 100%)', }} /> {/* Shape Layer */} <div style={{ position: 'absolute', top: '10%', right: '10%', width: 400, height: 400, borderRadius: '50%', border: '4px solid #FFC107', opacity: 0.06, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`, }} /> <div style={{ position: 'absolute', bottom: -100, right: -100, width: 450, height: 450, background: '#e9d700', opacity: 0.04, borderRadius: 40, transform: `rotate(${frame * 0.1}deg)`, }} /> <div style={{ position: 'absolute', top: '40%', left: '5%', width: 280, height: 280, background: '#FFC107', opacity: 0.05, clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)', transform: `rotate(${-frame * 0.08}deg)`, }} /> {/* Motion texture - subtle plus signs */} <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.05 }}> {Array.from({ length: 8 }).map((_, row) => Array.from({ length: 14 }).map((_, col) => ( <g key={`${row}-${col}`}> <line x1={col * 140 + 60} y1={row * 140 + 50} x2={col * 140 + 60} y2={row * 140 + 70} stroke="#e9d700" strokeWidth={2} /> <line x1={col * 140 + 50} y1={row * 140 + 60} x2={col * 140 + 70} y2={row * 140 + 60} stroke="#e9d700" strokeWidth={2} /> </g> )) )} </svg> {/* Particles */} {Array.from({ length: 22 }).map((_, i) => { const seed = seededRandom(i + 11); const x = seed * 1920; const y = (seededRandom(i + 55) * 1080 + frame * 0.6) % 1080; return ( <div key={i} style={{ position: 'absolute', left: x, top: y, width: 3, height: 3, borderRadius: '50%', background: '#FFC107', opacity: 0.4, }} /> ); })} {/* Content */} <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', alignItems: 'center', paddingTop: 80, transform: `translateY(${cameraY}px)`, }} > {/* Headline */} <div style={{ fontFamily: 'Inter', fontSize: 88, fontWeight: 900, color: '#F5F5F5', opacity: headOp, transform: `translateY(${headY}px)`, letterSpacing: -2, textAlign: 'center', lineHeight: 1.1, }} > I'LL COVER <span style={{ color: '#e9d700' }}>EVERYTHING.</span> </div> {/* Checklist */} <div style={{ marginTop: 90, display: 'flex', flexDirection: 'column', gap: 35 }}> {items.map((label, i) => { const itemOp = interpolate(frame, [itemStarts[i], itemStarts[i] + 25], [0, 1], { extrapolateRight: 'clamp' }); const itemX = interpolate(sp(itemStarts[i]), [0, 1], [-200, 0]); const checkScale = sp(itemStarts[i] + 15, { damping: 6, stiffness: 140 }); return ( <div key={i} style={{ display: 'flex', alignItems: 'center', gap: 30, opacity: itemOp, transform: `translateX(${itemX}px)`, }} > {/* Checkbox */} <div style={{ width: 90, height: 90, border: '5px solid #e9d700', borderRadius: 12, background: '#1C1C1C', display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0, }} > <svg width={60} height={60} viewBox="0 0 60 60" style={{ transform: `scale(${checkScale})` }}> <path d="M 10 30 L 25 45 L 50 15" stroke="#FFC107" strokeWidth={8} fill="none" strokeLinecap="round" strokeLinejoin="round" /> </svg> </div> {/* Label */} <div style={{ fontFamily: 'Inter', fontSize: 72, fontWeight: 900, color: '#F5F5F5', letterSpacing: -1, }} > {label} </div> </div> ); })} </div> {/* No fluff line */} <div style={{ marginTop: 60, fontFamily: 'Inter', fontSize: 42, fontWeight: 700, color: '#FFC107', opacity: everyOp, transform: `scale(${everyScale})`, letterSpacing: 2, textTransform: 'uppercase', }} > ━━ No Fluff. Just Facts. ━━ </div> </div> {/* Stick Figure */} <div style={{ position: 'absolute', bottom: 50, right: 100, opacity: figOp, transform: `translateY(${figY}px)`, }} > <div style={{ position: 'absolute', top: 20, left: -20, width: 140, height: 140, borderRadius: '50%', background: '#FFC107', filter: 'blur(40px)', opacity: 0.4, }} /> <svg width={120} height={220} viewBox="0 0 120 220"> <g transform={`rotate(${figHeadRot} 60 50)`}> <circle cx={60} cy={50} r={28} fill="none" stroke="#F5F5F5" strokeWidth={4} /> <circle cx={50} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <circle cx={70} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <line x1={59} y1={48} x2={61} y2={48} stroke="#FFC107" strokeWidth={3} /> <path d="M 48 60 Q 60 70 72 60" fill="none" stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" /> </g> <line x1={60} y1={80} x2={60} y2={150} stroke="#F5F5F5" strokeWidth={4} /> {/* Thumbs up arm */} <line x1={60} y1={100} x2={35} y2={75} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <circle cx={35} cy={70} r={6} fill="#FFC107" /> <line x1={60} y1={100} x2={90} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={40} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={80} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> </svg> </div> {/* Vignette */} <AbsoluteFill style={{ background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none', }} /> {/* Watermark */} <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3, letterSpacing: 1, }} > Senior Fund USA </div> </AbsoluteFill> ); }; export default Scene09_WalkThrough; 

SCENE 10 | [01:19 - 01:30]

Audio: "Stay until the end, because bonus number five is the one almost nobody talks about. And for married couples, it could be the biggest financial advantage of your entire life."

import React from 'react'; import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion'; export const compositionConfig = { id: 'Scene10_StayBonus5', durationInSeconds: 11, fps: 30, width: 1920, height: 1080, }; const seededRandom = (seed: number): number => { const x = Math.sin(seed * 9999) * 10000; return x - Math.floor(x); }; const Scene10_StayBonus5: React.FC = () => { const frame = useCurrentFrame(); const { fps, durationInFrames } = useVideoConfig(); const sp = (delay: number, config?: object) => spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } }); const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', }); // "STAY" warning entrance const stayOp = interpolate(frame, [10, 30], [0, 1], { extrapolateRight: 'clamp' }); const stayScale = sp(10, { damping: 8, stiffness: 110 }); // Big BONUS #5 reveal const bonusOp = interpolate(frame, [55, 80], [0, 1], { extrapolateRight: 'clamp' }); const bonusScale = sp(55, { damping: 7, stiffness: 100 }); // Pulsing #5 glow const fiveGlow = 0.4 + Math.sin(frame * 0.1) * 0.2; // "NOBODY TALKS ABOUT IT" text const nobodyOp = interpolate(frame, [110, 135], [0, 1], { extrapolateRight: 'clamp' }); const nobodyY = interpolate(sp(110), [0, 1], [40, 0]); // Couple silhouette entrance const coupleOp = interpolate(frame, [170, 200], [0, 1], { extrapolateRight: 'clamp' }); const coupleScale = sp(170, { damping: 10 }); // "BIGGEST ADVANTAGE" final const advOp = interpolate(frame, [230, 260], [0, 1], { extrapolateRight: 'clamp' }); const advScale = sp(230, { damping: 7, stiffness: 130 }); // Figure (smaller, just an accent) const figOp = interpolate(frame, [80, 110], [0, 1], { extrapolateRight: 'clamp' }); const figY = Math.sin(frame * 0.05) * 2; const figHeadRot = Math.sin(frame * 0.04) * 3; // Camera dramatic zoom const cameraZoom = interpolate(frame, [0, 80, 200, durationInFrames], [1.15, 1, 1.02, 1.08], { extrapolateRight: 'clamp' }); // Multiple impact shakes const shake1 = frame > 75 && frame < 90 ? Math.sin(frame * 3) * (90 - frame) * 0.5 : 0; const shake2 = frame > 225 && frame < 240 ? Math.sin(frame * 3) * (240 - frame) * 0.5 : 0; const shakeX = shake1 + shake2; // Pulsing red border const borderOp = 0.3 + Math.sin(frame * 0.15) * 0.2; return ( <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}> {/* Gradient */} <AbsoluteFill style={{ background: 'radial-gradient(ellipse at 50% 50%, #2D2D2D 0%, #1C1C1C 70%)', }} /> {/* Pulsing red border (urgency) */} <div style={{ position: 'absolute', inset: 30, border: '4px solid #ff0000', opacity: borderOp, pointerEvents: 'none', }} /> {/* Shape Layer */} <div style={{ position: 'absolute', top: -100, left: -100, width: 500, height: 500, borderRadius: '50%', background: '#ff0000', opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`, }} /> <div style={{ position: 'absolute', bottom: -120, right: -80, width: 600, height: 600, border: '5px solid #FFC107', opacity: 0.06, transform: `rotate(${frame * 0.1}deg)`, }} /> <div style={{ position: 'absolute', top: '30%', right: '10%', width: 250, height: 250, background: '#e9d700', opacity: 0.05, borderRadius: 30, transform: `rotate(${-frame * 0.12}deg)`, }} /> {/* Motion texture - radial bursts */} <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.07 }}> {Array.from({ length: 20 }).map((_, i) => { const angle = (i / 20) * Math.PI * 2 + frame * 0.005; return ( <line key={i} x1={960 + Math.cos(angle) * 300} y1={540 + Math.sin(angle) * 300} x2={960 + Math.cos(angle) * 1200} y2={540 + Math.sin(angle) * 1200} stroke="#FFC107" strokeWidth={1} /> ); })} </svg> {/* Particles */} {Array.from({ length: 35 }).map((_, i) => { const seed = seededRandom(i + 14); const x = seed * 1920; const y = (seededRandom(i + 88) * 1080 + frame * 0.9) % 1080; return ( <div key={i} style={{ position: 'absolute', left: x, top: y, width: 3, height: 3, borderRadius: '50%', background: i % 4 === 0 ? '#ff0000' : '#FFC107', opacity: 0.5, }} /> ); })} {/* Content */} <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', alignItems: 'center', paddingTop: 60, transform: `scale(${cameraZoom}) translateX(${shakeX}px)`, }} > {/* "STAY UNTIL THE END" warning */} <div style={{ display: 'flex', alignItems: 'center', gap: 20, opacity: stayOp, transform: `scale(${stayScale})`, background: '#ff0000', padding: '12px 30px', borderRadius: 6, }} > <svg width={36} height={36} viewBox="0 0 36 36"> <polygon points="18,4 34,30 2,30" fill="#F5F5F5" /> <text x={18} y={26} fontFamily="Inter" fontSize={20} fontWeight={900} fill="#ff0000" textAnchor="middle"> ! </text> </svg> <div style={{ fontFamily: 'Inter', fontSize: 42, fontWeight: 900, color: '#F5F5F5', letterSpacing: 4, }} > STAY UNTIL THE END </div> </div> {/* BONUS #5 mega */} <div style={{ display: 'flex', alignItems: 'center', gap: 30, marginTop: 40, opacity: bonusOp, transform: `scale(${bonusScale})`, }} > <div style={{ fontFamily: 'Inter', fontSize: 100, fontWeight: 900, color: '#F5F5F5', letterSpacing: -2, }} > BONUS </div> <div style={{ fontFamily: 'Inter', fontSize: 240, fontWeight: 900, color: '#e9d700', lineHeight: 1, textShadow: `0 0 ${60 * fiveGlow}px rgba(233, 215, 0, ${fiveGlow})`, }} > #5 </div> </div> {/* "NOBODY TALKS ABOUT IT" */} <div style={{ fontFamily: 'Inter', fontSize: 50, fontWeight: 800, color: '#9E9E9E', opacity: nobodyOp, transform: `translateY(${nobodyY}px)`, letterSpacing: 2, marginTop: 10, textTransform: 'uppercase', }} > Nobody Talks About It. </div> {/* Couple silhouette */} <div style={{ display: 'flex', alignItems: 'flex-end', gap: 0, marginTop: 40, opacity: coupleOp, transform: `scale(${coupleScale})`, }} > <svg width={120} height={180} viewBox="0 0 120 180"> <circle cx={60} cy={30} r={22} fill="#FFC107" /> <path d="M 20 180 L 20 80 Q 60 60 100 80 L 100 180 Z" fill="#FFC107" /> </svg> <div style={{ fontSize: 80, color: '#ff0000', margin: '0 -10px', marginBottom: 40 }}>♥</div> <svg width={120} height={180} viewBox="0 0 120 180"> <circle cx={60} cy={30} r={22} fill="#e9d700" /> <path d="M 20 180 L 20 80 Q 60 60 100 80 L 100 180 Z" fill="#e9d700" /> </svg> </div> {/* Biggest advantage */} <div style={{ marginTop: 25, fontFamily: 'Inter', fontSize: 56, fontWeight: 900, color: '#FFC107', opacity: advOp, transform: `scale(${advScale})`, textAlign: 'center', letterSpacing: 1, textShadow: '0 0 30px rgba(255, 193, 7, 0.5)', }} > BIGGEST ADVANTAGE OF YOUR LIFE </div> </div> {/* Stick Figure (small, corner) */} <div style={{ position: 'absolute', bottom: 60, left: 80, opacity: figOp * 0.7, transform: `translateY(${figY}px) scale(0.7)`, }} > <div style={{ position: 'absolute', top: 20, left: -20, width: 140, height: 140, borderRadius: '50%', background: '#FFC107', filter: 'blur(40px)', opacity: 0.3, }} /> <svg width={120} height={220} viewBox="0 0 120 220"> <g transform={`rotate(${figHeadRot} 60 50)`}> <circle cx={60} cy={50} r={28} fill="none" stroke="#F5F5F5" strokeWidth={4} /> <circle cx={50} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <circle cx={70} cy={48} r={9} fill="none" stroke="#FFC107" strokeWidth={3} /> <line x1={59} y1={48} x2={61} y2={48} stroke="#FFC107" strokeWidth={3} /> <path d="M 48 60 Q 60 68 72 60" fill="none" stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" /> </g> <line x1={60} y1={80} x2={60} y2={150} stroke="#F5F5F5" strokeWidth={4} /> {/* Finger to lips - shh */} <line x1={60} y1={100} x2={55} y2={70} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={100} x2={90} y2={130} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={40} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> <line x1={60} y1={150} x2={80} y2={200} stroke="#F5F5F5" strokeWidth={4} strokeLinecap="round" /> </svg> </div> {/* Vignette */} <AbsoluteFill style={{ background: 'radial-gradient(circle at center, transparent 40%, rgba(0,0,0,0.65) 100%)', pointerEvents: 'none', }} /> {/* Watermark */} <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3, letterSpacing: 1, }} > Senior Fund USA </div> </AbsoluteFill> ); }; export default Scene10_StayBonus5;
```

