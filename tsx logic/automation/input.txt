```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_FiveYearsSame',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_FiveYearsSame: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom slow
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08], { extrapolateRight: 'clamp' });

  // Calendar squares — 5 years = 60 months
  const months = Array.from({ length: 60 });

  // Big "400€" reveal late
  const revealStart = 360;
  const revealProgress = sp(revealStart, { damping: 8, stiffness: 90 });
  const shake = frame > revealStart + 10 && frame < revealStart + 30
    ? Math.sin(frame * 2) * 6 : 0;

  // Two columns: YOU vs KOLLEGE
  const colYou = sp(180);
  const colKolleg = sp(220);

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9F9DF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient layer */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(255,193,7,0.18), transparent 55%), radial-gradient(circle at 80% 80%, rgba(0,0,0,0.08), transparent 50%)',
        transform: `scale(${camScale})`,
      }} />

      {/* Shape Layers — drifting geometric forms */}
      {[0, 1, 2, 3, 4].map((i) => {
        const seed = seededRandom(i + 1);
        const drift = Math.sin((frame + i * 60) * 0.012) * 30;
        return (
          <div key={`shape-${i}`} style={{
            position: 'absolute',
            left: `${seed * 90}%`,
            top: `${seededRandom(i + 12) * 90}%`,
            width: 180 + seed * 220,
            height: 180 + seed * 220,
            borderRadius: i % 2 === 0 ? '50%' : '20px',
            border: `2px solid rgba(0,0,0,${0.05 + seed * 0.04})`,
            backgroundColor: i % 3 === 0 ? 'rgba(233,215,0,0.04)' : 'transparent',
            transform: `translate(${drift}px, ${drift * 0.6}px) rotate(${frame * 0.1 + i * 30}deg)`,
          }} />
        );
      })}

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="grid01" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid01)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const s = seededRandom(i + 50);
        const yMove = (frame * (0.4 + s * 0.6)) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: yMove,
            width: 4,
            height: 4,
            borderRadius: '50%',
            backgroundColor: '#000000',
            opacity: 0.15,
          }} />
        );
      })}

      {/* TOP HEADLINE — 5 JAHRE */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: '50%',
        transform: `translateX(-50%) scale(${sp(10)})`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 76,
          color: '#000000',
          letterSpacing: 4,
          lineHeight: 1,
        }}>
          SEIT 5 JAHREN
        </div>
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: 32,
          color: '#000000',
          opacity: 0.7,
          marginTop: 8,
          fontWeight: 600,
        }}>
          gleiche Abteilung · gleiches Projekt
        </div>
      </div>

      {/* Calendar grid — 60 months ticking */}
      <div style={{
        position: 'absolute',
        top: 240,
        left: '50%',
        transform: 'translateX(-50%)',
        display: 'grid',
        gridTemplateColumns: 'repeat(12, 56px)',
        gap: 8,
        opacity: sp(40),
      }}>
        {months.map((_, i) => {
          const appear = sp(50 + i * 1.5);
          return (
            <div key={`m-${i}`} style={{
              width: 56,
              height: 56,
              backgroundColor: '#000000',
              borderRadius: 6,
              opacity: appear * 0.85,
              transform: `scale(${appear})`,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 14,
              color: '#EEEEEE',
              fontWeight: 700,
            }}>
              {i + 1}
            </div>
          );
        })}
      </div>

      {/* TWO PERSON ICONS — YOU vs KOLLEGE */}
      <div style={{
        position: 'absolute',
        top: 640,
        left: '50%',
        transform: `translateX(${-50 + shake * 0.3}%)`,
        display: 'flex',
        gap: 120,
        alignItems: 'flex-end',
      }}>
        {/* YOU */}
        <div style={{
          transform: `scale(${colYou})`,
          textAlign: 'center',
        }}>
          <svg width="140" height="160" viewBox="0 0 140 160">
            <circle cx="70" cy="40" r="30" fill="#000000" />
            <rect x="40" y="75" width="60" height="75" rx="14" fill="#000000" />
          </svg>
          <div style={{
            marginTop: 12,
            fontFamily: 'Anton, sans-serif',
            fontSize: 54,
            color: '#000000',
            letterSpacing: 2,
          }}>
            DU
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 28,
            color: '#000000',
            opacity: 0.6,
            marginTop: 4,
          }}>
            2.800 €
          </div>
        </div>

        {/* VS */}
        <div style={{
          alignSelf: 'center',
          fontFamily: 'Anton, sans-serif',
          fontSize: 72,
          color: '#FFC107',
          transform: `scale(${sp(260)})`,
          WebkitTextStroke: '3px #000000',
        }}>
          VS
        </div>

        {/* KOLLEGE */}
        <div style={{
          transform: `scale(${colKolleg}) translateY(${shake * 0.4}px)`,
          textAlign: 'center',
        }}>
          <svg width="140" height="160" viewBox="0 0 140 160">
            <circle cx="70" cy="40" r="30" fill="#000000" />
            <rect x="40" y="75" width="60" height="75" rx="14" fill="#000000" />
            <circle cx="105" cy="35" r="18" fill="#FFC107" stroke="#000000" strokeWidth="3" />
            <text x="105" y="42" textAnchor="middle" fontSize="22" fontWeight="900" fill="#000000" fontFamily="Anton">+</text>
          </svg>
          <div style={{
            marginTop: 12,
            fontFamily: 'Anton, sans-serif',
            fontSize: 54,
            color: '#000000',
            letterSpacing: 2,
          }}>
            KOLLEGE
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 28,
            color: '#000000',
            opacity: 0.6,
            marginTop: 4,
          }}>
            3.200 €
          </div>
        </div>
      </div>

      {/* BIG 400€ DIFFERENCE TAG */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: '50%',
        transform: `translateX(-50%) scale(${revealProgress}) translateY(${(1 - revealProgress) * 40}px)`,
        backgroundColor: '#FFC107',
        padding: '18px 56px',
        borderRadius: 14,
        border: '4px solid #000000',
        boxShadow: '8px 8px 0px #000000',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 108,
          color: '#000000',
          letterSpacing: 2,
          lineHeight: 1,
        }}>
          +400 € / MONAT
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.3,
      }}>
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_FiveYearsSame;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_VerhandlungChange',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_VerhandlungChange: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Panel pan
  const panX = interpolate(frame, [0, durationInFrames], [0, -40]);

  // Checklist items strike-through
  const strikes = [
    sp(40),
    sp(80),
  ];

  // "WIE FÜHLT SICH DAS AN?" emphasis
  const askIn = sp(150);
  const pulse = 1 + Math.sin(frame * 0.15) * 0.02;

  // Millions counter
  const millionsCount = Math.floor(interpolate(frame, [240, 360], [0, 18.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) * 10) / 10;

  // Law impact
  const lawIn = sp(420, { damping: 10 });
  const shake = frame > 420 && frame < 450 ? Math.sin(frame * 1.8) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9F9DF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,193,7,0.1) 0%, transparent 40%, rgba(0,0,0,0.06) 100%)',
        transform: `translateX(${panX}px)`,
      }} />

      {/* Shapes */}
      {[0, 1, 2, 3].map((i) => {
        const s = seededRandom(i + 200);
        return (
          <div key={`sh-${i}`} style={{
            position: 'absolute',
            left: `${s * 85}%`,
            top: `${seededRandom(i + 210) * 80}%`,
            width: 240 + s * 200,
            height: 240 + s * 200,
            border: `3px solid rgba(0,0,0,0.05)`,
            borderRadius: i === 1 ? 0 : '50%',
            transform: `rotate(${frame * 0.15 + i * 45}deg)`,
          }} />
        );
      })}

      {/* Dotted texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots02" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots02)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 80);
        return (
          <div key={`pt-${i}`} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${(frame * 0.5 + s * 1080) % 1080}px`,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#FFC107',
            opacity: 0.4,
          }} />
        );
      })}

      {/* TOP: Checklist of "NICHT die Gründe" */}
      <div style={{
        position: 'absolute',
        top: 70,
        left: 110,
        transform: `scale(${sp(5)})`,
        transformOrigin: 'left top',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 42,
          color: '#000000',
          opacity: 0.55,
          marginBottom: 18,
          letterSpacing: 2,
        }}>
          NICHT WEIL...
        </div>

        {/* Item 1 */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 24, marginBottom: 18, position: 'relative' }}>
          <svg width="48" height="48" viewBox="0 0 48 48">
            <rect x="4" y="4" width="40" height="40" rx="6" fill="none" stroke="#000000" strokeWidth="3" />
            <path d="M 10 24 L 20 34 L 38 14" fill="none" stroke="#ff0000" strokeWidth="4"
              strokeDasharray="60" strokeDashoffset={60 - sp(35) * 60} strokeLinecap="round" />
          </svg>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 64,
            color: '#000000',
            position: 'relative',
          }}>
            BESSERER ABSCHLUSS
            <div style={{
              position: 'absolute',
              left: 0,
              top: '55%',
              height: 5,
              backgroundColor: '#ff0000',
              width: `${strikes[0] * 100}%`,
            }} />
          </span>
        </div>

        {/* Item 2 */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 24, position: 'relative' }}>
          <svg width="48" height="48" viewBox="0 0 48 48">
            <rect x="4" y="4" width="40" height="40" rx="6" fill="none" stroke="#000000" strokeWidth="3" />
            <path d="M 10 24 L 20 34 L 38 14" fill="none" stroke="#ff0000" strokeWidth="4"
              strokeDasharray="60" strokeDashoffset={60 - sp(75) * 60} strokeLinecap="round" />
          </svg>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 64,
            color: '#000000',
            position: 'relative',
          }}>
            ZUSATZQUALIFIKATION
            <div style={{
              position: 'absolute',
              left: 0,
              top: '55%',
              height: 5,
              backgroundColor: '#ff0000',
              width: `${strikes[1] * 100}%`,
            }} />
          </span>
        </div>
      </div>

      {/* MIDDLE — "SONDERN: VERHANDLUNG" with handshake icon */}
      <div style={{
        position: 'absolute',
        top: 430,
        left: 110,
        transform: `scale(${sp(115)})`,
        transformOrigin: 'left',
        display: 'flex',
        alignItems: 'center',
        gap: 30,
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 90,
          color: '#000000',
          letterSpacing: 2,
        }}>
          NUR EIN <span style={{ color: '#000000', backgroundColor: '#FFC107', padding: '0 16px' }}>GESPRÄCH</span>
        </div>
      </div>

      {/* "Wie fühlt sich das an?" — emotion */}
      <div style={{
        position: 'absolute',
        top: 580,
        right: 120,
        transform: `scale(${askIn * pulse}) rotate(-3deg)`,
        backgroundColor: '#000000',
        padding: '20px 38px',
        borderRadius: 12,
        boxShadow: '6px 6px 0 #FFC107',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 56,
          color: '#EEEEEE',
          letterSpacing: 2,
        }}>
          WIE FÜHLT SICH DAS AN?
        </div>
      </div>

      {/* Millions counter */}
      <div style={{
        position: 'absolute',
        bottom: 220,
        left: 110,
        transform: `translateX(${shake}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 28,
          color: '#000000',
          opacity: 0.7,
          marginBottom: 8,
          fontWeight: 700,
        }}>
          BETROFFENE ARBEITNEHMER:
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 160,
          color: '#000000',
          lineHeight: 1,
          letterSpacing: 2,
        }}>
          {millionsCount.toFixed(1)} <span style={{ color: '#FFC107', WebkitTextStroke: '3px #000000' }}>MIO</span>
        </div>
      </div>

      {/* BOTTOM — Law gavel impact bar */}
      <div style={{
        position: 'absolute',
        bottom: 60,
        right: 80,
        transform: `scale(${lawIn}) translateX(${shake}px)`,
        backgroundColor: '#ff0000',
        padding: '14px 36px',
        borderRadius: 8,
        border: '4px solid #000000',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 48,
          color: '#EEEEEE',
          letterSpacing: 3,
        }}>
          NEUES GESETZ → TRITT IN KRAFT
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 60%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_VerhandlungChange;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_EUtoDE',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_EUtoDE: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleIn = sp(10);

  // EU stamp
  const euIn = sp(60);
  // Arrow path
  const arrowProg = interpolate(frame, [120, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  // DE stamp
  const deIn = sp(220);

  // Date pulse
  const datePulse = 1 + Math.sin(frame * 0.18) * 0.03;

  // Bottom impact: account "zap"
  const zapIn = sp(380);
  const flash = frame > 380 && frame < 400 ? Math.sin(frame * 1.5) * 0.4 + 0.6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9F9DF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 30%, rgba(255,193,7,0.15), transparent 60%)',
      }} />

      {/* Shapes — drifting triangles */}
      {[0, 1, 2, 3, 4, 5].map((i) => {
        const s = seededRandom(i + 300);
        const drift = Math.sin((frame + i * 80) * 0.01) * 25;
        return (
          <svg key={`tr-${i}`} width="180" height="180" style={{
            position: 'absolute',
            left: `${s * 88}%`,
            top: `${seededRandom(i + 320) * 85}%`,
            transform: `translate(${drift}px, ${drift * 0.5}px) rotate(${frame * 0.2 + i * 30}deg)`,
            opacity: 0.08,
          }}>
            <polygon points="90,15 170,165 10,165" fill="#000000" />
          </svg>
        );
      })}

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="lines03" width="30" height="30" patternUnits="userSpaceOnUse">
            <path d="M 0 30 L 30 0" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#lines03)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const s = seededRandom(i + 400);
        return (
          <div key={`p3-${i}`} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${(frame * (0.3 + s * 0.5)) % 1080}px`,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#000000',
            opacity: 0.2,
          }} />
        );
      })}

      {/* TITLE */}
      <div style={{
        position: 'absolute',
        top: 50,
        left: '50%',
        transform: `translateX(-50%) scale(${titleIn})`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 88,
          color: '#000000',
          letterSpacing: 3,
          lineHeight: 1,
        }}>
          <span style={{ backgroundColor: '#FFC107', padding: '0 22px' }}>ENTGELT</span>TRANSPARENZ
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 88,
          color: '#000000',
          letterSpacing: 3,
          lineHeight: 1,
        }}>
          RICHTLINIE
        </div>
      </div>

      {/* EU → DE FLOW */}
      <div style={{
        position: 'absolute',
        top: 320,
        left: 0,
        right: 0,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 60,
      }}>
        {/* EU Block */}
        <div style={{
          transform: `scale(${euIn})`,
          backgroundColor: '#000000',
          padding: '40px 60px',
          borderRadius: 16,
          textAlign: 'center',
          minWidth: 320,
          boxShadow: '8px 8px 0 #FFC107',
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 130,
            color: '#FFC107',
            lineHeight: 1,
            letterSpacing: 4,
          }}>
            EU
          </div>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: 26,
            color: '#EEEEEE',
            marginTop: 8,
            fontWeight: 700,
          }}>
            VORGABE 2023
          </div>
        </div>

        {/* ARROW */}
        <svg width="280" height="80" viewBox="0 0 280 80">
          <line x1="10" y1="40" x2={10 + arrowProg * 230} y2="40"
            stroke="#000000" strokeWidth="8" strokeLinecap="round" />
          {arrowProg > 0.85 && (
            <polygon points={`${240},20 ${270},40 ${240},60`} fill="#000000" />
          )}
        </svg>

        {/* DE Block */}
        <div style={{
          transform: `scale(${deIn})`,
          backgroundColor: '#FFC107',
          padding: '40px 60px',
          borderRadius: 16,
          textAlign: 'center',
          minWidth: 320,
          border: '5px solid #000000',
          boxShadow: '8px 8px 0 #000000',
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 130,
            color: '#000000',
            lineHeight: 1,
            letterSpacing: 4,
          }}>
            DE
          </div>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: 26,
            color: '#000000',
            marginTop: 8,
            fontWeight: 700,
          }}>
            RECHT JETZT
          </div>
        </div>
      </div>

      {/* DATE STAMP */}
      <div style={{
        position: 'absolute',
        top: 600,
        left: '50%',
        transform: `translateX(-50%) scale(${sp(280) * datePulse})`,
        border: '5px solid #ff0000',
        padding: '12px 40px',
        borderRadius: 8,
        backgroundColor: '#D9F9DF',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 42,
          color: '#ff0000',
          fontWeight: 900,
          letterSpacing: 4,
        }}>
          JUNI 2026 — ACTIVE
        </div>
      </div>

      {/* BOTTOM: account zap */}
      <div style={{
        position: 'absolute',
        bottom: 90,
        left: '50%',
        transform: `translateX(-50%) scale(${zapIn})`,
        display: 'flex',
        alignItems: 'center',
        gap: 24,
      }}>
        <svg width="80" height="80" viewBox="0 0 80 80">
          <rect x="8" y="20" width="64" height="44" rx="6" fill="#000000" />
          <rect x="8" y="28" width="64" height="8" fill="#FFC107" />
          <text x="40" y="58" textAnchor="middle" fontSize="18" fill="#EEEEEE" fontFamily="Anton" letterSpacing="2">KONTO</text>
        </svg>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 64,
          color: '#000000',
          letterSpacing: 2,
        }}>
          GREIFT IN DEIN <span style={{
            color: '#ff0000',
            textShadow: `0 0 ${flash * 30}px #ff0000`,
          }}>GEHALTSKONTO</span> EIN
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_EUtoDE;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_ChannelIntro',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_ChannelIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Brand block
  const brandIn = sp(10, { damping: 9 });

  // Paragraph icon scrap-out
  const paraOut = interpolate(frame, [100, 160], [1, 0.15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const chartOut = interpolate(frame, [120, 180], [1, 0.15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Three pillars
  const p1 = sp(220);
  const p2 = sp(260);
  const p3 = sp(300);

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9F9DF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(255,193,7,0.18), transparent 70%)',
      }} />

      {/* Shapes — concentric rings */}
      {[1, 2, 3, 4].map((i) => {
        const breath = 1 + Math.sin((frame + i * 30) * 0.04) * 0.04;
        return (
          <div key={`ring-${i}`} style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            width: 400 + i * 220,
            height: 400 + i * 220,
            borderRadius: '50%',
            border: '2px solid rgba(0,0,0,0.05)',
            transform: `translate(-50%, -50%) scale(${breath}) rotate(${frame * 0.1 * i}deg)`,
          }} />
        );
      })}

      {/* Texture grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="grid04" width="80" height="80" patternUnits="userSpaceOnUse">
            <circle cx="40" cy="40" r="2" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid04)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const s = seededRandom(i + 500);
        return (
          <div key={`pa-${i}`} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${(frame * 0.4 + s * 1080) % 1080}px`,
            width: 4, height: 4,
            backgroundColor: '#FFC107',
            opacity: 0.5,
            transform: 'rotate(45deg)',
          }} />
        );
      })}

      {/* BRAND BLOCK — top */}
      <div style={{
        position: 'absolute',
        top: 80,
        left: '50%',
        transform: `translateX(-50%) scale(${brandIn})`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: 28,
          color: '#000000',
          opacity: 0.7,
          letterSpacing: 6,
          marginBottom: 12,
          fontWeight: 700,
        }}>
          ICH BIN KUNAL
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 150,
          color: '#000000',
          letterSpacing: 6,
          lineHeight: 0.95,
        }}>
          <span style={{ backgroundColor: '#000000', color: '#FFC107', padding: '0 22px' }}>KUX</span> LEGT AN
        </div>
      </div>

      {/* CROSSED OUT — Paragraphs & Charts */}
      <div style={{
        position: 'absolute',
        top: 380,
        left: 140,
        opacity: paraOut,
        transform: `rotate(${(1 - paraOut) * -8}deg) scale(${0.6 + paraOut * 0.4})`,
      }}>
        <div style={{ position: 'relative' }}>
          <svg width="160" height="160" viewBox="0 0 160 160">
            <text x="80" y="120" textAnchor="middle" fontSize="140" fill="#000000" fontFamily="Anton">§</text>
          </svg>
          {frame > 110 && (
            <div style={{
              position: 'absolute',
              top: '50%',
              left: -10,
              right: -10,
              height: 8,
              backgroundColor: '#ff0000',
              transform: `scaleX(${sp(110)})`,
              transformOrigin: 'left',
            }} />
          )}
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: 24,
            color: '#000000',
            textAlign: 'center',
            fontWeight: 700,
            marginTop: 4,
          }}>
            TROCKENE PARAGRAPHEN
          </div>
        </div>
      </div>

      <div style={{
        position: 'absolute',
        top: 380,
        right: 140,
        opacity: chartOut,
        transform: `rotate(${(1 - chartOut) * 8}deg) scale(${0.6 + chartOut * 0.4})`,
      }}>
        <div style={{ position: 'relative' }}>
          <svg width="180" height="160" viewBox="0 0 180 160">
            <polyline points="10,140 40,80 70,110 100,40 130,70 170,20"
              fill="none" stroke="#000000" strokeWidth="6" strokeLinejoin="round" />
            <line x1="10" y1="140" x2="170" y2="140" stroke="#000000" strokeWidth="4" />
          </svg>
          {frame > 130 && (
            <div style={{
              position: 'absolute',
              top: '50%',
              left: -10,
              right: -10,
              height: 8,
              backgroundColor: '#ff0000',
              transform: `scaleX(${sp(130)})`,
              transformOrigin: 'left',
            }} />
          )}
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: 24,
            color: '#000000',
            textAlign: 'center',
            fontWeight: 700,
            marginTop: 4,
          }}>
            BÖRSENKURSE
          </div>
        </div>
      </div>

      {/* CENTER — SONDERN */}
      <div style={{
        position: 'absolute',
        top: 470,
        left: '50%',
        transform: `translateX(-50%) scale(${sp(180)})`,
        backgroundColor: '#FFC107',
        padding: '12px 44px',
        border: '4px solid #000000',
        borderRadius: 8,
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 64,
          color: '#000000',
          letterSpacing: 4,
        }}>
          SONDERN →
        </div>
      </div>

      {/* THREE PILLARS — Leben, Konto, Familie */}
      <div style={{
        position: 'absolute',
        bottom: 110,
        left: 0,
        right: 0,
        display: 'flex',
        justifyContent: 'center',
        gap: 50,
      }}>
        {[
          { label: 'DEIN LEBEN', icon: 'life', delay: p1 },
          { label: 'DEIN KONTO', icon: 'money', delay: p2 },
          { label: 'DEINE FAMILIE', icon: 'family', delay: p3 },
        ].map((item, idx) => (
          <div key={idx} style={{
            transform: `translateY(${(1 - item.delay) * 60}px) scale(${item.delay})`,
            opacity: item.delay,
            backgroundColor: '#000000',
            padding: '24px 36px',
            borderRadius: 14,
            textAlign: 'center',
            minWidth: 280,
            boxShadow: '6px 6px 0 #FFC107',
          }}>
            <svg width="64" height="64" viewBox="0 0 64 64" style={{ marginBottom: 8 }}>
              {item.icon === 'life' && (
                <path d="M32 56 L8 32 A14 14 0 0 1 32 16 A14 14 0 0 1 56 32 Z" fill="#FFC107" />
              )}
              {item.icon === 'money' && (
                <>
                  <circle cx="32" cy="32" r="24" fill="#FFC107" />
                  <text x="32" y="44" textAnchor="middle" fontSize="32" fontWeight="900" fill="#000000" fontFamily="Anton">€</text>
                </>
              )}
              {item.icon === 'family' && (
                <>
                  <circle cx="20" cy="22" r="8" fill="#FFC107" />
                  <circle cx="44" cy="22" r="8" fill="#FFC107" />
                  <circle cx="32" cy="42" r="6" fill="#FFC107" />
                  <rect x="10" y="32" width="20" height="20" rx="4" fill="#FFC107" />
                  <rect x="34" y="32" width="20" height="20" rx="4" fill="#FFC107" />
                  <rect x="24" y="50" width="16" height="10" rx="3" fill="#FFC107" />
                </>
              )}
            </svg>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 42,
              color: '#EEEEEE',
              letterSpacing: 2,
            }}>
              {item.label}
            </div>
          </div>
        ))}
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_ChannelIntro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_Staatsgeheimnis',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_Staatsgeheimnis: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-0.3, 0.3]);

  // Text reveal sequence
  const ans1 = sp(10);
  const ans2 = sp(60);
  const bigAnswer = sp(110, { damping: 8 });
  const shake = frame > 110 && frame < 140 ? Math.sin(frame * 1.8) * 5 : 0;

  // Folder reveal
  const folderIn = sp(200);
  const stampIn = sp(280);
  const stampRot = -10 + sp(280) * 18;

  // Speech bubble
  const bubbleIn = sp(360);

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9F9DF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, transparent, rgba(0,0,0,0.08))',
        transform: `rotate(${camRot}deg)`,
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const s = seededRandom(i + 600);
        return (
          <div key={`sq-${i}`} style={{
            position: 'absolute',
            left: `${s * 88}%`,
            top: `${seededRandom(i + 610) * 85}%`,
            width: 200 + s * 250,
            height: 200 + s * 250,
            backgroundColor: 'transparent',
            border: '2px solid rgba(0,0,0,0.06)',
            transform: `rotate(${frame * 0.15 + i * 60}deg)`,
            borderRadius: i % 2 ? '50%' : 0,
          }} />
        );
      })}

      {/* Crosshatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="cross05" width="20" height="20" patternUnits="userSpaceOnUse">
            <path d="M 0 0 L 20 20 M 20 0 L 0 20" stroke="#000000" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross05)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 700);
        return (
          <div key={`fp-${i}`} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${(frame * 0.5 + s * 1080) % 1080}px`,
            width: 6, height: 2,
            backgroundColor: '#000000',
            opacity: 0.2,
          }} />
        );
      })}

      {/* TOP — Answer build-up */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: '50%',
        transform: `translateX(-50%)`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: 36,
          color: '#000000',
          opacity: ans1 * 0.7,
          letterSpacing: 4,
          fontWeight: 700,
        }}>
          DIE ANTWORT IST
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 96,
          color: '#000000',
          letterSpacing: 4,
          marginTop: 6,
          transform: `scale(${ans2})`,
          lineHeight: 1,
        }}>
          <span style={{ backgroundColor: '#FFC107', padding: '0 22px' }}>GLASKLAR</span>
        </div>
      </div>

      {/* BIG ANSWER */}
      <div style={{
        position: 'absolute',
        top: 280,
        left: '50%',
        transform: `translateX(-50%) translateX(${shake}px) scale(${bigAnswer})`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 170,
          color: '#ff0000',
          letterSpacing: 4,
          lineHeight: 1,
          WebkitTextStroke: '4px #000000',
        }}>
          VERDAMMT VIEL
        </div>
      </div>

      {/* FOLDER with TOP SECRET stamp */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: 140,
        transform: `scale(${folderIn})`,
      }}>
        <svg width="380" height="280" viewBox="0 0 380 280">
          {/* Folder body */}
          <path d="M 10 50 L 130 50 L 160 70 L 370 70 L 370 270 L 10 270 Z" fill="#000000" />
          <rect x="20" y="80" width="340" height="180" fill="#2D2D2D" />
          {/* Lines */}
          <line x1="40" y1="120" x2="200" y2="120" stroke="#9E9E9E" strokeWidth="3" />
          <line x1="40" y1="150" x2="280" y2="150" stroke="#9E9E9E" strokeWidth="3" />
          <line x1="40" y1="180" x2="240" y2="180" stroke="#9E9E9E" strokeWidth="3" />
          <line x1="40" y1="210" x2="180" y2="210" stroke="#9E9E9E" strokeWidth="3" />
        </svg>

        {/* STAMP */}
        <div style={{
          position: 'absolute',
          top: 80,
          right: -30,
          transform: `rotate(${stampRot}deg) scale(${stampIn})`,
          border: '6px solid #ff0000',
          padding: '8px 22px',
          backgroundColor: 'rgba(255,255,255,0.92)',
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 44,
            color: '#ff0000',
            letterSpacing: 4,
            lineHeight: 1,
          }}>
            STAATS<br/>GEHEIMNIS
          </div>
        </div>

        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: 22,
          color: '#000000',
          marginTop: 14,
          fontWeight: 700,
          letterSpacing: 2,
        }}>
          GEHALT IN DEUTSCHLAND — BIS HEUTE
        </div>
      </div>

      {/* SPEECH BUBBLE — du wusstest nur deins */}
      <div style={{
        position: 'absolute',
        bottom: 200,
        right: 140,
        transform: `scale(${bubbleIn}) rotate(2deg)`,
        backgroundColor: '#EEEEEE',
        border: '4px solid #000000',
        borderRadius: 24,
        padding: '24px 40px',
        boxShadow: '6px 6px 0 #000000',
        maxWidth: 480,
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 38,
          color: '#000000',
          letterSpacing: 1,
          lineHeight: 1.1,
        }}>
          DU WUSSTEST NUR<br/>
          WAS <span style={{ backgroundColor: '#FFC107' }}>DU</span> VERDIENST
        </div>
        {/* tail */}
        <div style={{
          position: 'absolute',
          bottom: -30,
          left: 60,
          width: 0,
          height: 0,
          borderLeft: '20px solid transparent',
          borderRight: '20px solid transparent',
          borderTop: '30px solid #000000',
        }} />
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_Staatsgeheimnis;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_Blackbox',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_Blackbox: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Beer foam bubbles
  const beerIn = sp(10);
  const beerWobble = Math.sin(frame * 0.1) * 2;

  // Blackbox reveal
  const boxIn = sp(170, { damping: 9 });
  const boxPulse = 1 + Math.sin(frame * 0.12) * 0.015;

  // Three salary tags floating
  const tag1 = sp(280);
  const tag2 = sp(310);
  const tag3 = sp(340);

  // Question marks
  const qIn = sp(420);

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9F9DF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 70% 40%, rgba(255,193,7,0.12), transparent 55%), radial-gradient(ellipse at 30% 80%, rgba(0,0,0,0.1), transparent 50%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3, 4].map((i) => {
        const s = seededRandom(i + 800);
        const breath = 1 + Math.sin((frame + i * 50) * 0.03) * 0.05;
        return (
          <div key={`sx-${i}`} style={{
            position: 'absolute',
            left: `${s * 90}%`,
            top: `${seededRandom(i + 810) * 90}%`,
            width: 220 + s * 200,
            height: 220 + s * 200,
            border: '3px solid rgba(0,0,0,0.05)',
            transform: `scale(${breath}) rotate(${frame * 0.1 + i * 45}deg)`,
            borderRadius: i === 2 ? '50%' : 18,
          }} />
        );
      })}

      {/* Hex texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="hex06" width="60" height="52" patternUnits="userSpaceOnUse">
            <polygon points="30,2 58,18 58,42 30,58 2,42 2,18" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hex06)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const s = seededRandom(i + 900);
        return (
          <div key={`b-${i}`} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${(frame * (0.3 + s * 0.7)) % 1080}px`,
            width: 3 + s * 4,
            height: 3 + s * 4,
            borderRadius: '50%',
            backgroundColor: '#FFC107',
            opacity: 0.4,
          }} />
        );
      })}

      {/* TOP LEFT — BEER + INFORMAL */}
      <div style={{
        position: 'absolute',
        top: 80,
        left: 100,
        transform: `scale(${beerIn})`,
      }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 26 }}>
          {/* Beer mug SVG */}
          <svg width="120" height="140" viewBox="0 0 120 140" style={{ transform: `rotate(${beerWobble}deg)` }}>
            {/* Foam */}
            <ellipse cx="55" cy="25" rx="40" ry="14" fill="#EEEEEE" />
            <circle cx="38" cy="20" r="10" fill="#EEEEEE" />
            <circle cx="55" cy="14" r="12" fill="#EEEEEE" />
            <circle cx="72" cy="20" r="10" fill="#EEEEEE" />
            {/* Mug */}
            <rect x="15" y="30" width="80" height="100" rx="6" fill="#FFC107" stroke="#000000" strokeWidth="4" />
            {/* Liquid lines */}
            <line x1="22" y1="60" x2="88" y2="60" stroke="#000000" strokeWidth="2" opacity="0.3" />
            <line x1="22" y1="80" x2="88" y2="80" stroke="#000000" strokeWidth="2" opacity="0.3" />
            {/* Handle */}
            <path d="M 95 50 Q 115 50 115 80 Q 115 110 95 110" fill="none" stroke="#000000" strokeWidth="4" />
          </svg>
          <div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#000000',
              opacity: 0.6,
              fontWeight: 700,
            }}>
              NACH DEM 3. BIER
            </div>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 58,
              color: '#000000',
              letterSpacing: 2,
              lineHeight: 1,
              marginTop: 6,
            }}>
              "PSST... ICH BEKOMM<br/>3.400 €"
            </div>
          </div>
        </div>
      </div>

      {/* DIVIDER LINE — "ABER OFFIZIELL" */}
      <div style={{
        position: 'absolute',
        top: 350,
        left: '50%',
        transform: `translateX(-50%) scale(${sp(120)})`,
        backgroundColor: '#ff0000',
        padding: '8px 30px',
        borderRadius: 6,
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 44,
          color: '#EEEEEE',
          letterSpacing: 4,
        }}>
          ABER OFFIZIELL...
        </div>
      </div>

      {/* CENTER — BLACKBOX */}
      <div style={{
        position: 'absolute',
        top: 460,
        left: '50%',
        transform: `translate(-50%, 0) scale(${boxIn * boxPulse})`,
      }}>
        <div style={{
          width: 600,
          height: 280,
          backgroundColor: '#000000',
          borderRadius: 18,
          border: '4px solid #1C1C1C',
          boxShadow: '0 0 60px rgba(0,0,0,0.5), inset 0 0 60px rgba(0,0,0,0.8)',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          flexDirection: 'column',
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 130,
            color: '#FFC107',
            letterSpacing: 6,
            lineHeight: 1,
          }}>
            BLACK
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 130,
            color: '#FFC107',
            letterSpacing: 6,
            lineHeight: 1,
          }}>
            BOX
          </div>
        </div>
      </div>

      {/* THREE FLOATING SALARY TAGS — different for same position */}
      {[
        { val: '2.800 €', x: 80, y: 480, rot: -8, prog: tag1 },
        { val: '3.500 €', x: 1500, y: 460, rot: 6, prog: tag2 },
        { val: '4.100 €', x: 1550, y: 720, rot: -4, prog: tag3 },
      ].map((t, idx) => (
        <div key={idx} style={{
          position: 'absolute',
          left: t.x,
          top: t.y + Math.sin((frame + idx * 30) * 0.08) * 8,
          transform: `scale(${t.prog}) rotate(${t.rot}deg)`,
          backgroundColor: '#FFC107',
          padding: '14px 28px',
          border: '4px solid #000000',
          borderRadius: 10,
          boxShadow: '4px 4px 0 #000000',
        }}>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: 18,
            color: '#000000',
            opacity: 0.7,
            fontWeight: 700,
          }}>
            GLEICHE POSITION
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 56,
            color: '#000000',
            letterSpacing: 2,
            lineHeight: 1,
          }}>
            {t.val}
          </div>
        </div>
      ))}

      {/* QUESTION MARKS — niemand konnte überprüfen */}
      {[
        { x: 320, y: 880, size: 80, delay: qIn },
        { x: 1550, y: 880, size: 80, delay: sp(440) },
        { x: 920, y: 880, size: 80, delay: sp(460) },
      ].map((q, idx) => (
        <div key={idx} style={{
          position: 'absolute',
          left: q.x,
          top: q.y,
          fontFamily: 'Anton, sans-serif',
          fontSize: q.size,
          color: '#ff0000',
          transform: `scale(${q.delay})`,
          WebkitTextStroke: '3px #000000',
        }}>
          ?
        </div>
      ))}

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_Blackbox;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_OldLaw2017',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_OldLaw2017: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Old book reveal
  const bookIn = sp(10);
  // Hurdles bar chart
  const hurdle1 = interpolate(frame, [80, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const hurdle2 = interpolate(frame, [110, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const hurdle3 = interpolate(frame, [140, 190], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // % usage stat
  const pctVal = Math.floor(interpolate(frame, [220, 320], [100, 4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // VORBEI stamp slam
  const stampIn = sp(380, { damping: 6, stiffness: 90 });
  const slamShake = frame > 380 && frame < 410 ? Math.sin(frame * 1.6) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9F9DF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(45deg, rgba(0,0,0,0.06), transparent 50%, rgba(255,193,7,0.1))',
      }} />

      {/* Shapes */}
      {[0, 1, 2, 3].map((i) => {
        const s = seededRandom(i + 1100);
        return (
          <div key={`sh07-${i}`} style={{
            position: 'absolute',
            left: `${s * 88}%`,
            top: `${seededRandom(i + 1110) * 85}%`,
            width: 260 + s * 220,
            height: 260 + s * 220,
            border: '2px solid rgba(0,0,0,0.06)',
            borderRadius: i === 1 ? '50%' : 14,
            transform: `rotate(${frame * 0.12 + i * 40}deg)`,
          }} />
        );
      })}

      {/* Texture vertical lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="vert07" width="40" height="40" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#vert07)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 1200);
        return (
          <div key={`p7-${i}`} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${(frame * 0.4 + s * 1080) % 1080}px`,
            width: 4, height: 4,
            borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.18,
          }} />
        );
      })}

      {/* TITLE + OLD LAW BOOK */}
      <div style={{
        position: 'absolute',
        top: 70,
        left: 100,
        transform: `scale(${bookIn})`,
        transformOrigin: 'left top',
        display: 'flex',
        alignItems: 'center',
        gap: 36,
      }}>
        {/* Dusty book */}
        <svg width="180" height="200" viewBox="0 0 180 200">
          <rect x="20" y="20" width="140" height="170" fill="#2D2D2D" rx="4" />
          <rect x="20" y="20" width="20" height="170" fill="#000000" />
          <text x="100" y="80" textAnchor="middle" fontSize="56" fontWeight="900" fill="#FFC107" fontFamily="Anton">§</text>
          <text x="100" y="140" textAnchor="middle" fontSize="32" fontWeight="900" fill="#EEEEEE" fontFamily="Anton">2017</text>
          {/* Dust */}
          {[0, 1, 2].map(i => (
            <circle key={i} cx={40 + i * 50} cy={10 - i * 4} r={2 + i} fill="#9E9E9E" opacity="0.5" />
          ))}
        </svg>
        <div>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 24,
            color: '#000000',
            opacity: 0.6,
            fontWeight: 700,
            letterSpacing: 3,
          }}>
            SEIT 2017
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 64,
            color: '#000000',
            letterSpacing: 2,
            lineHeight: 1,
            marginTop: 4,
          }}>
            ENTGELT­TRANSPARENZ-<br/>
            <span style={{ backgroundColor: '#9E9E9E', padding: '0 12px', color: '#000000' }}>GESETZ</span>
          </div>
        </div>
      </div>

      {/* THE 3 HURDLES — bar chart style */}
      <div style={{
        position: 'absolute',
        top: 400,
        left: 100,
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 36,
          color: '#000000',
          letterSpacing: 3,
          marginBottom: 18,
          opacity: sp(70),
        }}>
          DIE HÜRDEN WAREN:
        </div>

        {[
          { label: 'HOHE HÜRDEN', val: hurdle1, color: '#ff0000' },
          { label: 'KOMPLIZIERT', val: hurdle2, color: '#ff0000' },
          { label: 'UNBEKANNT', val: hurdle3, color: '#ff0000' },
        ].map((bar, idx) => (
          <div key={idx} style={{ display: 'flex', alignItems: 'center', gap: 18, marginBottom: 14 }}>
            <div style={{
              width: 280,
              fontFamily: 'Anton, sans-serif',
              fontSize: 38,
              color: '#000000',
              letterSpacing: 2,
            }}>
              {bar.label}
            </div>
            <div style={{
              height: 42,
              width: bar.val * 480,
              backgroundColor: bar.color,
              border: '3px solid #000000',
              borderRadius: 4,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'flex-end',
              paddingRight: 16,
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22,
                color: '#EEEEEE',
                fontWeight: 900,
              }}>
                {Math.floor(bar.val * 100)}%
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* RIGHT SIDE — Usage stat */}
      <div style={{
        position: 'absolute',
        top: 380,
        right: 120,
        transform: `scale(${sp(210)})`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: 26,
          color: '#000000',
          opacity: 0.7,
          fontWeight: 700,
          letterSpacing: 2,
        }}>
          GEBRAUCH GEMACHT:
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 240,
          color: '#000000',
          lineHeight: 1,
          letterSpacing: 2,
          marginTop: 8,
        }}>
          {pctVal}<span style={{ color: '#ff0000' }}>%</span>
        </div>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 22,
          color: '#000000',
          opacity: 0.6,
          marginTop: -8,
        }}>
          DER ARBEITNEHMER
        </div>
      </div>

      {/* VORBEI STAMP */}
      <div style={{
        position: 'absolute',
        bottom: 60,
        right: 100,
        transform: `translateX(${slamShake}px) scale(${stampIn}) rotate(-8deg)`,
        border: '8px solid #ff0000',
        padding: '16px 60px',
        backgroundColor: '#EEEEEE',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 100,
          color: '#ff0000',
          letterSpacing: 8,
          lineHeight: 1,
        }}>
          VORBEI
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_OldLaw2017;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_FlipScript',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_FlipScript: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Timeline 2023 -> 2026
  const t23 = sp(10);
  const lineGrow = interpolate(frame, [60, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const t26 = sp(200);

  // Big flip
  const flipProg = interpolate(frame, [300, 380], [0, 180], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const flipShake = frame > 360 && frame < 400 ? Math.sin(frame * 1.8) * 4 : 0;

  // After flip — UMGEDREHT label
  const labelIn = sp(420);

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9F9DF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 70%, rgba(255,193,7,0.18), transparent 60%)',
      }} />

      {/* Shapes */}
      {[0, 1, 2, 3, 4].map((i) => {
        const s = seededRandom(i + 1500);
        return (
          <div key={`sh8-${i}`} style={{
            position: 'absolute',
            left: `${s * 88}%`,
            top: `${seededRandom(i + 1510) * 85}%`,
            width: 200 + s * 240,
            height: 200 + s * 240,
            border: '2px solid rgba(0,0,0,0.05)',
            borderRadius: i % 2 ? 0 : '50%',
            transform: `rotate(${frame * 0.13 + i * 35}deg) scale(${1 + Math.sin((frame + i * 40) * 0.04) * 0.04})`,
          }} />
        );
      })}

      {/* Texture dots */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="dot08" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dot08)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const s = seededRandom(i + 1600);
        return (
          <div key={`p8-${i}`} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${(frame * 0.5 + s * 1080) % 1080}px`,
            width: 5, height: 5,
            backgroundColor: '#FFC107', opacity: 0.4,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* TITLE */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: '50%',
        transform: `translateX(-50%) scale(${sp(5)})`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 80,
          color: '#000000',
          letterSpacing: 3,
          lineHeight: 1,
        }}>
          DIE NEUE REGELUNG
        </div>
      </div>

      {/* TIMELINE 2023 → 2026 */}
      <div style={{
        position: 'absolute',
        top: 220,
        left: 0,
        right: 0,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 0,
      }}>
        {/* 2023 EU */}
        <div style={{ transform: `scale(${t23})`, textAlign: 'center' }}>
          <div style={{
            backgroundColor: '#000000',
            color: '#FFC107',
            padding: '14px 30px',
            borderRadius: 10,
            fontFamily: 'Anton, sans-serif',
            fontSize: 64,
            letterSpacing: 3,
            lineHeight: 1,
          }}>
            2023
          </div>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: 22,
            color: '#000000',
            fontWeight: 700,
            marginTop: 8,
            letterSpacing: 2,
          }}>
            EU-RICHTLINIE
          </div>
        </div>

        {/* Line */}
        <div style={{
          height: 8,
          width: 360,
          backgroundColor: '#000000',
          opacity: 0.15,
          margin: '0 30px',
          marginBottom: 30,
          position: 'relative',
          borderRadius: 4,
        }}>
          <div style={{
            position: 'absolute',
            left: 0, top: 0, bottom: 0,
            width: `${lineGrow * 100}%`,
            backgroundColor: '#FFC107',
            borderRadius: 4,
          }} />
          {/* arrow */}
          {lineGrow > 0.9 && (
            <div style={{
              position: 'absolute',
              right: -20,
              top: '50%',
              transform: 'translateY(-50%)',
              width: 0, height: 0,
              borderTop: '16px solid transparent',
              borderBottom: '16px solid transparent',
              borderLeft: '24px solid #000000',
            }} />
          )}
        </div>

        {/* 2026 DE */}
        <div style={{ transform: `scale(${t26})`, textAlign: 'center' }}>
          <div style={{
            backgroundColor: '#FFC107',
            color: '#000000',
            padding: '14px 30px',
            borderRadius: 10,
            fontFamily: 'Anton, sans-serif',
            fontSize: 64,
            letterSpacing: 3,
            lineHeight: 1,
            border: '4px solid #000000',
          }}>
            JUNI 2026
          </div>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: 22,
            color: '#000000',
            fontWeight: 700,
            marginTop: 8,
            letterSpacing: 2,
          }}>
            DEUTSCHES RECHT
          </div>
        </div>
      </div>

      {/* FLIPPING BLOCK — "DEN SPIESS UMDREHEN" */}
      <div style={{
        position: 'absolute',
        bottom: 200,
        left: '50%',
        transform: `translate(-50%, 0) translateX(${flipShake}px) perspective(1200px) rotateX(${flipProg}deg)`,
        transformStyle: 'preserve-3d',
      }}>
        <div style={{
          backgroundColor: '#000000',
          padding: '40px 80px',
          borderRadius: 16,
          border: '5px solid #FFC107',
          backfaceVisibility: 'hidden',
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 110,
            color: '#FFC107',
            letterSpacing: 5,
            lineHeight: 1,
          }}>
            SPIESS
          </div>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: 30,
            color: '#EEEEEE',
            opacity: 0.7,
            fontWeight: 700,
            textAlign: 'center',
            marginTop: 6,
          }}>
            ALTE REGEL
          </div>
        </div>
      </div>

      {/* UMGEDREHT label after flip */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: '50%',
        transform: `translateX(-50%) scale(${labelIn})`,
        backgroundColor: '#ff0000',
        padding: '12px 40px',
        borderRadius: 8,
        border: '4px solid #000000',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 56,
          color: '#EEEEEE',
          letterSpacing: 4,
        }}>
          → KOMPLETT UMGEDREHT ←
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_FlipScript;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_Beweislast',
  durationInSeconds: 24,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_Beweislast: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Scale tilt — left heavy then right heavy
  const tiltProg = interpolate(frame, [120, 280], [-15, 15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const scaleIn = sp(20);

  // Labels
  const arbnIn = sp(60);
  const arbgIn = sp(80);

  // Stamp BEWEISLAST
  const stampIn = sp(300);

  // Finanzamt analogy
  const fIn = sp(400);

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9F9DF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 40%, rgba(255,193,7,0.15), transparent 65%)',
      }} />

      {/* Shapes */}
      {[0, 1, 2, 3].map((i) => {
        const s = seededRandom(i + 1900);
        return (
          <div key={`sh9-${i}`} style={{
            position: 'absolute',
            left: `${s * 88}%`,
            top: `${seededRandom(i + 1910) * 85}%`,
            width: 240 + s * 200,
            height: 240 + s * 200,
            border: '2px solid rgba(0,0,0,0.05)',
            borderRadius: i === 0 ? '50%' : 16,
            transform: `rotate(${frame * 0.12 + i * 50}deg)`,
          }} />
        );
      })}

      {/* Texture grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="gr09" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 50 0 L 0 0 0 50" fill="none" stroke="#000000" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#gr09)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 2000);
        return (
          <div key={`p9-${i}`} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${(frame * 0.35 + s * 1080) % 1080}px`,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.18,
          }} />
        );
      })}

      {/* TITLE */}
      <div style={{
        position: 'absolute',
        top: 50,
        left: '50%',
        transform: `translateX(-50%) scale(${sp(5)})`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 76,
          color: '#000000',
          letterSpacing: 3,
          lineHeight: 1,
        }}>
          UMKEHR DER <span style={{ backgroundColor: '#FFC107', padding: '0 18px' }}>BEWEISLAST</span>
        </div>
      </div>

      {/* JUSTICE SCALE — animated tilt */}
      <div style={{
        position: 'absolute',
        top: 230,
        left: '50%',
        transform: `translateX(-50%) scale(${scaleIn})`,
      }}>
        <svg width="900" height="380" viewBox="0 0 900 380">
          {/* Base */}
          <rect x="430" y="320" width="40" height="50" fill="#000000" />
          <rect x="380" y="365" width="140" height="14" fill="#000000" rx="3" />
          {/* Pole */}
          <rect x="446" y="50" width="8" height="280" fill="#000000" />
          {/* Beam — tilted */}
          <g transform={`rotate(${tiltProg} 450 60)`}>
            <rect x="100" y="55" width="700" height="10" fill="#000000" rx="3" />
            {/* Left pan ropes */}
            <line x1="150" y1="60" x2="150" y2="160" stroke="#000000" strokeWidth="3" />
            {/* Left pan */}
            <path d="M 80 160 L 220 160 L 200 200 L 100 200 Z" fill="#ff0000" stroke="#000000" strokeWidth="4" />
            <text x="150" y="190" textAnchor="middle" fontSize="22" fontWeight="900" fill="#EEEEEE" fontFamily="Anton" letterSpacing="2">DU</text>
            {/* Right pan ropes */}
            <line x1="750" y1="60" x2="750" y2="160" stroke="#000000" strokeWidth="3" />
            {/* Right pan */}
            <path d="M 680 160 L 820 160 L 800 200 L 700 200 Z" fill="#FFC107" stroke="#000000" strokeWidth="4" />
            <text x="750" y="190" textAnchor="middle" fontSize="22" fontWeight="900" fill="#000000" fontFamily="Anton" letterSpacing="2">CHEF</text>
          </g>
        </svg>
      </div>

      {/* LEFT LABEL — VORHER */}
      <div style={{
        position: 'absolute',
        top: 290,
        left: 80,
        transform: `scale(${arbnIn})`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 22,
          color: '#000000',
          opacity: 0.6,
          fontWeight: 700,
          letterSpacing: 3,
        }}>
          VORHER
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 48,
          color: '#000000',
          letterSpacing: 2,
          lineHeight: 1,
          marginTop: 6,
        }}>
          DU MUSST<br/>
          <span style={{ color: '#ff0000' }}>BEWEISEN</span>
        </div>
      </div>

      {/* RIGHT LABEL — JETZT */}
      <div style={{
        position: 'absolute',
        top: 290,
        right: 80,
        transform: `scale(${arbgIn})`,
        textAlign: 'right',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 22,
          color: '#000000',
          opacity: 0.6,
          fontWeight: 700,
          letterSpacing: 3,
        }}>
          JETZT
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 48,
          color: '#000000',
          letterSpacing: 2,
          lineHeight: 1,
          marginTop: 6,
        }}>
          CHEF MUSS<br/>
          <span style={{ backgroundColor: '#FFC107', padding: '0 10px' }}>BEWEISEN</span>
        </div>
      </div>

      {/* BOTTOM — Finanzamt analogy split */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: 100,
        right: 100,
        display: 'flex',
        gap: 30,
        transform: `scale(${fIn})`,
        transformOrigin: 'center',
      }}>
        {/* Card 1 - früher Steuerzahler */}
        <div style={{
          flex: 1,
          backgroundColor: '#000000',
          padding: '22px 28px',
          borderRadius: 14,
          border: '3px solid #2D2D2D',
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 10 }}>
            <svg width="48" height="48" viewBox="0 0 48 48">
              <rect x="6" y="6" width="36" height="36" rx="4" fill="#ff0000" />
              <text x="24" y="32" textAnchor="middle" fontSize="22" fontWeight="900" fill="#EEEEEE" fontFamily="Anton">€</text>
            </svg>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 28,
              color: '#FFC107',
              letterSpacing: 2,
            }}>
              FRÜHER · FINANZAMT
            </div>
          </div>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: 30,
            color: '#EEEEEE',
            fontWeight: 700,
            lineHeight: 1.2,
          }}>
            DU musst alles belegen → dein Problem
          </div>
        </div>

        {/* Arrow */}
        <div style={{
          alignSelf: 'center',
          fontFamily: 'Anton, sans-serif',
          fontSize: 60,
          color: '#000000',
        }}>
          →
        </div>

        {/* Card 2 — analogie gehalt */}
        <div style={{
          flex: 1,
          backgroundColor: '#FFC107',
          padding: '22px 28px',
          borderRadius: 14,
          border: '4px solid #000000',
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 10 }}>
            <svg width="48" height="48" viewBox="0 0 48 48">
              <circle cx="24" cy="24" r="20" fill="#000000" />
              <path d="M 14 24 L 22 32 L 36 16" fill="none" stroke="#FFC107" strokeWidth="4" strokeLinecap="round" />
            </svg>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 28,
              color: '#000000',
              letterSpacing: 2,
            }}>
              JETZT · GEHALT
            </div>
          </div>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: 30,
            color: '#000000',
            fontWeight: 700,
            lineHeight: 1.2,
          }}>
            CHEF muss beweisen → sein Problem
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_Beweislast;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_100Plus',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_100Plus: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter for 100+ employees
  const countVal = Math.floor(interpolate(frame, [40, 180], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const counterScale = sp(20);

  // Pulse on 100
  const hitPulse = frame > 180 && frame < 210 ? 1 + Math.sin((frame - 180) * 0.5) * 0.05 : 1;

  // Reports stack appear
  const reports = [sp(240), sp(280), sp(320), sp(360)];

  // Three pillars
  const pillarsBase = 500;
  const p1 = sp(pillarsBase);
  const p2 = sp(pillarsBase + 40);
  const p3 = sp(pillarsBase + 80);

  // Grid of employees icons
  const empGrid = Array.from({ length: 100 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#D9F9DF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 30%, rgba(255,193,7,0.18), transparent 50%), radial-gradient(ellipse at 80% 80%, rgba(0,0,0,0.08), transparent 50%)',
      }} />

      {/* Shapes */}
      {[0, 1, 2, 3, 4, 5].map((i) => {
        const s = seededRandom(i + 2200);
        const breath = 1 + Math.sin((frame + i * 40) * 0.03) * 0.04;
        return (
          <div key={`shx-${i}`} style={{
            position: 'absolute',
            left: `${s * 88}%`,
            top: `${seededRandom(i + 2210) * 85}%`,
            width: 180 + s * 220,
            height: 180 + s * 220,
            border: '2px solid rgba(0,0,0,0.05)',
            transform: `scale(${breath}) rotate(${frame * 0.1 + i * 30}deg)`,
            borderRadius: i % 2 ? '50%' : 12,
          }} />
        );
      })}

      {/* Texture lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="px10" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 0 30 L 60 30 M 30 0 L 30 60" stroke="#000000" strokeWidth="0.6" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#px10)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const s = seededRandom(i + 2300);
        return (
          <div key={`pa10-${i}`} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${(frame * 0.4 + s * 1080) % 1080}px`,
            width: 4, height: 4,
            borderRadius: '50%',
            backgroundColor: '#FFC107', opacity: 0.4,
          }} />
        );
      })}

      {/* TOP — TITLE */}
      <div style={{
        position: 'absolute',
        top: 50,
        left: '50%',
        transform: `translateX(-50%) scale(${sp(5)})`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 64,
          color: '#000000',
          letterSpacing: 3,
          lineHeight: 1,
        }}>
          UNTERNEHMEN MIT MEHR ALS
        </div>
      </div>

      {/* LEFT — Counter 100+ */}
      <div style={{
        position: 'absolute',
        top: 200,
        left: 130,
        transform: `scale(${counterScale * hitPulse})`,
      }}>
        <div style={{
          backgroundColor: '#000000',
          padding: '30px 50px',
          borderRadius: 18,
          border: '5px solid #FFC107',
          textAlign: 'center',
          boxShadow: '8px 8px 0 #FFC107',
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 260,
            color: '#FFC107',
            letterSpacing: 4,
            lineHeight: 0.9,
          }}>
            {countVal}+
          </div>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: 30,
            color: '#EEEEEE',
            fontWeight: 700,
            letterSpacing: 3,
            marginTop: 8,
          }}>
            BESCHÄFTIGTE
          </div>
        </div>
      </div>

      {/* RIGHT — Mini employee grid */}
      <div style={{
        position: 'absolute',
        top: 220,
        right: 130,
        width: 480,
        display: 'grid',
        gridTemplateColumns: 'repeat(10, 1fr)',
        gap: 8,
      }}>
        {empGrid.map((_, i) => {
          const appear = sp(50 + i * 1.2);
          return (
            <svg key={`emp-${i}`} width="40" height="44" viewBox="0 0 40 44" style={{
              opacity: appear,
              transform: `scale(${appear})`,
            }}>
              <circle cx="20" cy="12" r="8" fill="#000000" />
              <rect x="8" y="22" width="24" height="22" rx="4" fill="#000000" />
            </svg>
          );
        })}
      </div>

      {/* MIDDLE BANNER — MÜSSEN BERICHTEN */}
      <div style={{
        position: 'absolute',
        top: 620,
        left: '50%',
        transform: `translateX(-50%) scale(${sp(200)})`,
        backgroundColor: '#ff0000',
        padding: '12px 50px',
        borderRadius: 8,
        border: '4px solid #000000',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 56,
          color: '#EEEEEE',
          letterSpacing: 5,
        }}>
          MÜSSEN GEHALTS-BERICHTE VERÖFFENTLICHEN
        </div>
      </div>

      {/* THREE PILLARS — zugänglich, nachvollziehbar, vergleichbar */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: 0,
        right: 0,
        display: 'flex',
        justifyContent: 'center',
        gap: 40,
      }}>
        {[
          { label: 'ZUGÄNGLICH', icon: 'access', prog: p1 },
          { label: 'NACHVOLLZIEHBAR', icon: 'trace', prog: p2 },
          { label: 'VERGLEICHBAR', icon: 'compare', prog: p3 },
        ].map((pillar, idx) => (
          <div key={idx} style={{
            transform: `translateY(${(1 - pillar.prog) * 50}px) scale(${pillar.prog})`,
            opacity: pillar.prog,
            backgroundColor: '#FFC107',
            padding: '24px 30px',
            borderRadius: 14,
            border: '4px solid #000000',
            minWidth: 340,
            textAlign: 'center',
            boxShadow: '6px 6px 0 #000000',
          }}>
            <svg width="56" height="56" viewBox="0 0 56 56" style={{ marginBottom: 8 }}>
              {pillar.icon === 'access' && (
                <>
                  <circle cx="28" cy="28" r="22" fill="none" stroke="#000000" strokeWidth="4" />
                  <path d="M 18 28 L 28 38 L 42 18" fill="none" stroke="#000000" strokeWidth="5" strokeLinecap="round" />
                </>
              )}
              {pillar.icon === 'trace' && (
                <>
                  <rect x="8" y="14" width="40" height="28" rx="3" fill="none" stroke="#000000" strokeWidth="4" />
                  <line x1="14" y1="22" x2="42" y2="22" stroke="#000000" strokeWidth="3" />
                  <line x1="14" y1="30" x2="38" y2="30" stroke="#000000" strokeWidth="3" />
                  <line x1="14" y1="36" x2="34" y2="36" stroke="#000000" strokeWidth="3" />
                </>
              )}
              {pillar.icon === 'compare' && (
                <>
                  <rect x="8" y="32" width="14" height="16" fill="#000000" />
                  <rect x="24" y="20" width="14" height="28" fill="#000000" />
                  <rect x="40" y="10" width="10" height="38" fill="#000000" />
                </>
              )}
            </svg>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 42,
              color: '#000000',
              letterSpacing: 2,
              lineHeight: 1,
            }}>
              {pillar.label}
            </div>
          </div>
        ))}
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_100Plus;
```

