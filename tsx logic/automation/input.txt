```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01ProfitsPinkSlips',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01ProfitsPinkSlips: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow push-in
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08], { easing: Easing.inOut(Easing.ease) });

  // Stock line reveal
  const lineReveal = interpolate(frame, [140, 260], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#E4B028', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 20%, rgba(235,237,227,0.25) 0%, rgba(228,176,40,0) 45%), linear-gradient(135deg, #E4B028 0%, #C6951F 100%)' }} />

      {/* Shape Layers */}
      <div style={{ position: 'absolute', width: 900, height: 900, borderRadius: '50%', border: '3px solid rgba(28,28,28,0.06)', left: -250, top: -300, transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame * 0.02) * 0.03})` }} />
      <div style={{ position: 'absolute', width: 600, height: 600, background: 'rgba(235,237,227,0.05)', right: -150, bottom: -200, transform: `rotate(${45 + frame * 0.03}deg)` }} />

      {/* Texture: vertical ticker lines */}
      {Array.from({ length: 14 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', left: i * 140, top: 0, width: 1, height: 1080, background: 'rgba(28,28,28,0.05)' }} />
      ))}

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 7);
        return (
          <div key={i} style={{
            position: 'absolute', width: 5 + s * 6, height: 5 + s * 6, borderRadius: '50%',
            background: i % 3 === 0 ? '#8FD14F' : 'rgba(235,237,227,0.5)',
            left: s * 1920, top: ((s * 1080) + frame * (0.3 + s * 0.5)) % 1080,
            opacity: 0.35,
          }} />
        );
      })}

      {/* Content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>
        <div style={{ textAlign: 'center' }}>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 155, fontWeight: 900, lineHeight: 1.0,
            color: '#1C1C1C', transform: `translateY(${(1 - sp(10)) * 80}px)`, opacity: sp(10),
          }}>
            RECORD PROFITS.
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 155, fontWeight: 900, lineHeight: 1.0,
            color: '#EBEDE3', transform: `translateY(${(1 - sp(30)) * 80}px)`, opacity: sp(30),
            textShadow: '0 6px 0 rgba(28,28,28,0.2)',
          }}>
            PINK SLIPS.
          </div>
          <div style={{
            marginTop: 30, fontFamily: 'Inter, sans-serif', fontSize: 52, fontWeight: 700,
            color: '#8FD14F', background: '#1C1C1C', display: 'inline-block', padding: '14px 40px',
            transform: `scale(${sp(55)})`,
          }}>
            SAME PRESS RELEASE
          </div>
        </div>

        {/* Stock pop line (SVG) */}
        <svg width={1400} height={260} viewBox="0 0 1400 260" style={{ position: 'absolute', bottom: 90, opacity: interpolate(frame, [140, 170], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          <path
            d="M 0 220 L 200 200 L 380 210 L 560 160 L 760 170 L 950 90 L 1150 100 L 1400 20"
            stroke="#8FD14F" strokeWidth={10} fill="none" strokeLinecap="round"
            strokeDasharray={1600} strokeDashoffset={1600 * (1 - lineReveal)}
          />
          <circle cx={1400 * lineReveal} cy={220 - lineReveal * 200} r={14} fill="#EBEDE3" />
        </svg>

        {/* Falling email cards */}
        {[0, 1, 2].map((i) => {
          const drop = sp(320 + i * 20, { damping: 14 });
          return (
            <div key={i} style={{
              position: 'absolute', top: 120 + i * 30, right: 140 + i * 90,
              width: 200, height: 130, background: '#EBEDE3', borderRadius: 10,
              transform: `translateY(${(1 - drop) * -400}px) rotate(${-6 + i * 5}deg)`,
              boxShadow: '0 14px 30px rgba(28,28,28,0.25)', padding: 18, opacity: drop,
            }}>
              <div style={{ width: '70%', height: 12, background: '#1C1C1C', borderRadius: 6 }} />
              <div style={{ width: '90%', height: 8, background: '#9E9E9E', borderRadius: 4, marginTop: 12 }} />
              <div style={{ width: '85%', height: 8, background: '#9E9E9E', borderRadius: 4, marginTop: 8 }} />
              <div style={{ marginTop: 14, fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 800, color: '#ff0000' }}>TERMINATED</div>
            </div>
          );
        })}
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, rgba(0,0,0,0) 55%, rgba(28,28,28,0.35) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene01ProfitsPinkSlips;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02OppositeFlow',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02OppositeFlow: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: lateral pan
  const camX = interpolate(frame, [0, durationInFrames], [-30, 30], { easing: Easing.inOut(Easing.ease) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#E4B028', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #C6951F 0%, #E4B028 40%, #E4B028 60%, #B0821A 100%)' }} />

      {/* Shape Layers */}
      <div style={{ position: 'absolute', width: 0, height: 0, borderLeft: '350px solid transparent', borderRight: '350px solid transparent', borderBottom: '600px solid rgba(235,237,227,0.04)', left: 100, top: 200, transform: `rotate(${frame * 0.04}deg)` }} />
      <div style={{ position: 'absolute', width: 700, height: 700, borderRadius: '50%', background: 'rgba(28,28,28,0.04)', right: -200, top: -250, transform: `scale(${1 + Math.sin(frame * 0.025) * 0.04})` }} />

      {/* Texture: horizontal grid */}
      {Array.from({ length: 8 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', top: i * 140, left: 0, width: 1920, height: 1, background: 'rgba(28,28,28,0.05)' }} />
      ))}

      {/* Particles: rising left (money), falling right (jobs) */}
      {Array.from({ length: 24 }).map((_, i) => {
        const s = seededRandom(i + 21);
        const left = i < 12;
        const y = left ? (1080 - ((s * 1080 + frame * (1 + s)) % 1080)) : ((s * 1080 + frame * (1 + s)) % 1080);
        return (
          <div key={i} style={{
            position: 'absolute', width: 8, height: 8, borderRadius: left ? '50%' : 0,
            background: left ? '#8FD14F' : '#1C1C1C',
            left: left ? 80 + s * 700 : 1140 + s * 700, top: y, opacity: 0.4,
          }} />
        );
      })}

      {/* Content */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        <div style={{
          position: 'absolute', top: 90, width: '100%', textAlign: 'center',
          fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 110, fontWeight: 900, color: '#1C1C1C',
          transform: `translateY(${(1 - sp(8)) * -60}px)`, opacity: sp(8),
        }}>
          THIS IS <span style={{ color: '#EBEDE3', textShadow: '0 5px 0 rgba(28,28,28,0.25)' }}>NOT</span> A RECESSION
        </div>

        {/* Left column: MONEY IN */}
        <div style={{ position: 'absolute', left: 180, top: 320, width: 620, opacity: sp(40) }}>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 56, fontWeight: 800, color: '#8FD14F', marginBottom: 24 }}>MONEY POURING IN</div>
          {[0.5, 0.7, 1].map((h, i) => (
            <div key={i} style={{
              display: 'inline-block', width: 130, marginRight: 34, verticalAlign: 'bottom',
              height: 300 * h * sp(60 + i * 18), background: '#8FD14F', borderRadius: '8px 8px 0 0',
              boxShadow: '0 10px 24px rgba(28,28,28,0.2)',
            }} />
          ))}
          <div style={{ height: 6, width: 480, background: '#1C1C1C', marginTop: 0 }} />
        </div>

        {/* Right column: JOBS OUT */}
        <div style={{ position: 'absolute', right: 180, top: 320, width: 620, opacity: sp(50) }}>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 56, fontWeight: 800, color: '#1C1C1C', marginBottom: 24 }}>JOBS POURING OUT</div>
          {[1, 0.65, 0.35].map((h, i) => (
            <div key={i} style={{
              display: 'inline-block', width: 130, marginRight: 34, verticalAlign: 'bottom',
              height: 300 * h * sp(70 + i * 18), background: '#1C1C1C', borderRadius: '8px 8px 0 0',
              opacity: 0.9,
            }} />
          ))}
          <div style={{ height: 6, width: 480, background: '#1C1C1C' }} />
        </div>

        {/* Host intro badge */}
        <div style={{
          position: 'absolute', bottom: 110, width: '100%', textAlign: 'center',
          transform: `scale(${sp(430, { damping: 10 })})`,
        }}>
          <div style={{ display: 'inline-block', background: '#1C1C1C', padding: '22px 60px', borderRadius: 16 }}>
            <span style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 60, fontWeight: 900, color: '#EBEDE3' }}>I'M KUNAL — </span>
            <span style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 60, fontWeight: 900, color: '#8FD14F' }}>FAILED FINANCES</span>
          </div>
          <div style={{ marginTop: 18, fontFamily: 'JetBrains Mono, monospace', fontSize: 34, color: '#1C1C1C', opacity: sp(480) }}>
            months of layoff filings + earnings calls + government data
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, rgba(0,0,0,0) 55%, rgba(28,28,28,0.35) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene02OppositeFlow;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03TheNumber',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03TheNumber: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Count-up to 101,743
  const count = Math.floor(interpolate(frame, [150, 330], [0, 101743], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));

  // Donut: 23%
  const donutPct = interpolate(frame, [400, 520], [0, 23], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const circumference = 2 * Math.PI * 130;

  // Screen shake on number lock
  const shake = frame > 330 && frame < 345 ? Math.sin(frame * 3) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#E4B028', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 40%, #E9BC42 0%, #E4B028 50%, #A87A14 100%)' }} />

      {/* Shape Layers */}
      <div style={{ position: 'absolute', width: 1100, height: 1100, borderRadius: '50%', border: '2px solid rgba(235,237,227,0.08)', left: 410, top: -60, transform: `rotate(${frame * 0.06}deg)` }} />
      <div style={{ position: 'absolute', width: 400, height: 400, background: 'rgba(28,28,28,0.05)', left: -120, bottom: -120, transform: `rotate(${frame * -0.05}deg)` }} />

      {/* Texture: torn headline strips */}
      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute', top: 100 + i * 70, left: -50, width: 2020, height: 44,
          background: i % 2 ? 'rgba(28,28,28,0.06)' : 'rgba(235,237,227,0.1)',
          transform: `rotate(${i % 2 ? 1.5 : -1.5}deg) translateX(${Math.sin(frame * 0.01 + i) * 20}px)`,
        }} />
      ))}

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 33);
        return (
          <div key={i} style={{
            position: 'absolute', width: 4 + s * 5, height: 4 + s * 5,
            background: i % 4 === 0 ? '#8FD14F' : 'rgba(235,237,227,0.45)', borderRadius: '50%',
            left: (s * 1920 + frame * 0.4) % 1920, top: s * 1080, opacity: 0.4,
          }} />
        );
      })}

      {/* Content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `translateX(${shake}px)` }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 58, fontWeight: 800, color: '#1C1C1C',
          letterSpacing: 6, opacity: sp(10), transform: `translateY(${(1 - sp(10)) * -40}px)`,
        }}>
          HEADLINES ARE LYING — BOTH DIRECTIONS
        </div>

        <div style={{
          fontFamily: 'JetBrains Mono, monospace', fontSize: 210, fontWeight: 900, color: '#EBEDE3',
          textShadow: '0 10px 0 rgba(28,28,28,0.25)', marginTop: 30,
          transform: `scale(${0.9 + sp(150) * 0.1})`, opacity: interpolate(frame, [145, 165], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          {count.toLocaleString('en-US')}
        </div>

        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 48, fontWeight: 700, color: '#1C1C1C',
          background: '#8FD14F', padding: '10px 36px', borderRadius: 10, marginTop: 10,
          transform: `scale(${sp(350)})`,
        }}>
          JOB CUTS — AI NAMED AS THE REASON (H1)
        </div>

        {/* 23% Donut */}
        <div style={{ position: 'absolute', right: 150, bottom: 120, opacity: interpolate(frame, [395, 420], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          <svg width={320} height={320} viewBox="0 0 320 320">
            <circle cx={160} cy={160} r={130} stroke="rgba(28,28,28,0.15)" strokeWidth={38} fill="none" />
            <circle
              cx={160} cy={160} r={130} stroke="#8FD14F" strokeWidth={38} fill="none"
              strokeLinecap="round" transform="rotate(-90 160 160)"
              strokeDasharray={circumference}
              strokeDashoffset={circumference * (1 - donutPct / 100)}
            />
            <text x={160} y={150} textAnchor="middle" fontFamily="Mont Heavy, Inter, sans-serif" fontSize={72} fontWeight={900} fill="#EBEDE3">
              {Math.round(donutPct)}%
            </text>
            <text x={160} y={205} textAnchor="middle" fontFamily="Inter, sans-serif" fontSize={30} fontWeight={700} fill="#1C1C1C">
              OF ALL CUTS
            </text>
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, rgba(0,0,0,0) 55%, rgba(28,28,28,0.35) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene03TheNumber;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04RisingShare',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04RisingShare: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow tilt-zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1], { easing: Easing.inOut(Easing.ease) });

  const months = ['MAR', 'APR', 'MAY', 'JUN'];
  const values = [23, 25, 28, 31];

  return (
    <AbsoluteFill style={{ backgroundColor: '#E4B028', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'linear-gradient(115deg, #B0821A 0%, #E4B028 45%, #E9BC42 100%)' }} />

      {/* Shape Layers */}
      <div style={{ position: 'absolute', width: 800, height: 800, border: '3px solid rgba(28,28,28,0.06)', right: -200, top: -300, transform: `rotate(${frame * 0.04}deg)` }} />
      <div style={{ position: 'absolute', width: 500, height: 500, borderRadius: '50%', background: 'rgba(143,209,79,0.06)', left: -150, bottom: -150, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})` }} />

      {/* Texture: dotted matrix */}
      {Array.from({ length: 60 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', width: 4, height: 4, borderRadius: '50%', background: 'rgba(28,28,28,0.07)',
          left: (i % 10) * 210 + 60, top: Math.floor(i / 10) * 190 + 50,
        }} />
      ))}

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 51);
        return (
          <div key={i} style={{
            position: 'absolute', width: 6, height: 6, borderRadius: '50%',
            background: 'rgba(235,237,227,0.5)',
            left: s * 1920, top: (s * 1080 + frame * 0.6) % 1080, opacity: 0.4,
          }} />
        );
      })}

      {/* Content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        <div style={{
          position: 'absolute', top: 80, width: '100%', textAlign: 'center',
          fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 96, fontWeight: 900, color: '#1C1C1C',
          opacity: sp(8), transform: `translateY(${(1 - sp(8)) * -50}px)`,
        }}>
          THE SHARE KEEPS <span style={{ color: '#EBEDE3', textShadow: '0 5px 0 rgba(28,28,28,0.25)' }}>CLIMBING</span>
        </div>

        {/* Bar chart: months */}
        <div style={{ position: 'absolute', bottom: 240, width: '100%', textAlign: 'center' }}>
          {months.map((m, i) => {
            const grow = sp(90 + i * 45, { damping: 13 });
            const isJune = i === 3;
            return (
              <div key={m} style={{ display: 'inline-block', width: 240, margin: '0 40px', verticalAlign: 'bottom' }}>
                <div style={{
                  fontFamily: 'JetBrains Mono, monospace', fontSize: isJune ? 84 : 60, fontWeight: 900,
                  color: isJune ? '#8FD14F' : '#EBEDE3', opacity: grow,
                  textShadow: isJune ? '0 5px 0 rgba(28,28,28,0.3)' : 'none',
                }}>
                  {values[i]}%
                </div>
                <div style={{
                  width: '100%', height: values[i] * 12 * grow, borderRadius: '12px 12px 0 0',
                  background: isJune ? '#8FD14F' : '#EBEDE3',
                  boxShadow: '0 12px 26px rgba(28,28,28,0.2)',
                }} />
                <div style={{ marginTop: 14, fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 800, color: '#1C1C1C' }}>{m}</div>
              </div>
            );
          })}
          <div style={{ height: 8, width: 1300, background: '#1C1C1C', margin: '10px auto 0' }} />
        </div>

        {/* Four consecutive months stamp */}
        <div style={{
          position: 'absolute', bottom: 90, width: '100%', textAlign: 'center',
          transform: `scale(${sp(430, { damping: 9 })}) rotate(${(1 - sp(430)) * -8}deg)`,
        }}>
          <div style={{
            display: 'inline-block', border: '6px solid #1C1C1C', color: '#1C1C1C',
            fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 54, fontWeight: 900,
            padding: '10px 46px', letterSpacing: 4, background: 'rgba(235,237,227,0.85)',
          }}>
            #1 STATED REASON — 4 MONTHS STRAIGHT
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, rgba(0,0,0,0) 55%, rgba(28,28,28,0.35) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene04RisingShare;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05StructuralShift',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05StructuralShift: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Timeline reveal 2023 -> 2026
  const lineProg = interpolate(frame, [80, 300], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  // Seismograph: flat then violent
  const seismoPath = (() => {
    let d = 'M 0 130';
    for (let x = 0; x <= 1400; x += 10) {
      const t = x / 1400;
      const amp = t < 0.6 ? 4 : (t - 0.6) * 280;
      const y = 130 + Math.sin(x * 0.15 + frame * 0.12) * amp;
      d += ` L ${x} ${y.toFixed(1)}`;
    }
    return d;
  })();

  const years = ['2023', '2024', '2025', '2026'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#E4B028', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 80%, #E9BC42 0%, #E4B028 45%, #9C7112 100%)' }} />

      {/* Shape Layers */}
      <div style={{ position: 'absolute', width: 900, height: 900, borderRadius: '50%', border: '2px dashed rgba(28,28,28,0.08)', left: 510, top: 90, transform: `rotate(${frame * -0.05}deg)` }} />
      <div style={{ position: 'absolute', width: 460, height: 460, background: 'rgba(235,237,227,0.05)', left: -140, top: -140, transform: `rotate(${20 + frame * 0.03}deg)` }} />

      {/* Texture: diagonal hatch */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', width: 2600, height: 1, background: 'rgba(28,28,28,0.05)',
          left: -300, top: i * 110, transform: 'rotate(-8deg)',
        }} />
      ))}

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 71);
        return (
          <div key={i} style={{
            position: 'absolute', width: 5, height: 5, background: i % 3 === 0 ? '#8FD14F' : 'rgba(235,237,227,0.5)',
            left: (s * 1920 - frame * (0.3 + s * 0.4) + 1920) % 1920, top: s * 1080,
            borderRadius: '50%', opacity: 0.4,
          }} />
        );
      })}

      {/* Content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
        <div style={{
          fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 118, fontWeight: 900, color: '#EBEDE3',
          textShadow: '0 7px 0 rgba(28,28,28,0.25)', textAlign: 'center', lineHeight: 1.05,
          transform: `translateY(${(1 - sp(10)) * 70}px)`, opacity: sp(10),
        }}>
          A STREAK WITH<br />
          <span style={{ color: '#1C1C1C', textShadow: 'none' }}>NO PRECEDENT</span>
        </div>

        {/* Seismograph */}
        <svg width={1400} height={260} viewBox="0 0 1400 260" style={{ marginTop: 40, opacity: interpolate(frame, [70, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          <path d={seismoPath} stroke="#8FD14F" strokeWidth={6} fill="none"
            strokeDasharray={4200} strokeDashoffset={4200 * (1 - lineProg)} />
        </svg>

        {/* Timeline years */}
        <div style={{ width: 1400, display: 'flex', justifyContent: 'space-between', marginTop: 4 }}>
          {years.map((y, i) => (
            <div key={y} style={{ textAlign: 'center', opacity: sp(140 + i * 40) }}>
              <div style={{ width: 20, height: 20, borderRadius: '50%', background: i === 3 ? '#8FD14F' : '#1C1C1C', margin: '0 auto 10px' }} />
              <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 44, fontWeight: 800, color: i === 3 ? '#EBEDE3' : '#1C1C1C' }}>{y}</div>
            </div>
          ))}
        </div>

        <div style={{
          marginTop: 50, background: '#1C1C1C', padding: '18px 54px', borderRadius: 14,
          transform: `scale(${sp(420, { damping: 10 })})`,
        }}>
          <span style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 56, fontWeight: 900, color: '#8FD14F' }}>STRUCTURAL SHIFT</span>
          <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 44, fontWeight: 600, color: '#EBEDE3' }}> — never recorded before</span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, rgba(0,0,0,0) 55%, rgba(28,28,28,0.35) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene05StructuralShift;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06NewNormal',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06NewNormal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: gentle orbit feel via rotation
  const camRot = Math.sin(frame * 0.008) * 1.2;

  // Strike-through NOT A BLIP
  const strike = interpolate(frame, [240, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#E4B028', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'linear-gradient(200deg, #E9BC42 0%, #E4B028 50%, #B0821A 100%)' }} />

      {/* Shape Layers */}
      <div style={{ position: 'absolute', width: 760, height: 760, borderRadius: '50%', background: 'rgba(28,28,28,0.04)', right: -220, bottom: -260, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})` }} />
      <div style={{ position: 'absolute', width: 520, height: 520, border: '3px solid rgba(235,237,227,0.1)', left: -160, top: 140, transform: `rotate(${frame * 0.06}deg)` }} />

      {/* Texture: concentric circles */}
      {[220, 380, 540].map((r, i) => (
        <div key={i} style={{
          position: 'absolute', width: r * 2, height: r * 2, borderRadius: '50%',
          border: '1px solid rgba(28,28,28,0.06)', left: 960 - r, top: 540 - r,
        }} />
      ))}

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const s = seededRandom(i + 91);
        const ang = s * Math.PI * 2 + frame * 0.004;
        return (
          <div key={i} style={{
            position: 'absolute', width: 6, height: 6, borderRadius: '50%',
            background: i % 3 === 0 ? '#8FD14F' : 'rgba(235,237,227,0.5)',
            left: 960 + Math.cos(ang) * (300 + s * 500), top: 540 + Math.sin(ang) * (200 + s * 320),
            opacity: 0.4,
          }} />
        );
      })}

      {/* Content */}
      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Four #1 badges */}
        <div style={{ display: 'flex', gap: 44, marginBottom: 50 }}>
          {['MAR', 'APR', 'MAY', 'JUN'].map((m, i) => {
            const pop = sp(30 + i * 30, { damping: 9 });
            return (
              <div key={m} style={{
                width: 190, height: 190, borderRadius: 24, background: '#1C1C1C',
                display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center',
                transform: `scale(${pop}) rotate(${(1 - pop) * 20}deg)`,
                boxShadow: '0 16px 34px rgba(28,28,28,0.3)',
              }}>
                <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 74, fontWeight: 900, color: '#8FD14F' }}>#1</div>
                <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 34, fontWeight: 700, color: '#EBEDE3' }}>{m}</div>
              </div>
            );
          })}
        </div>

        {/* NOT A BLIP -> NEW NORMAL */}
        <div style={{ position: 'relative', opacity: sp(200) }}>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 100, fontWeight: 900, color: '#EBEDE3', textShadow: '0 6px 0 rgba(28,28,28,0.2)' }}>
            NOT A BLIP
          </div>
          <div style={{ position: 'absolute', top: '52%', left: 0, height: 12, background: '#1C1C1C', width: `${strike * 100}%` }} />
        </div>

        <div style={{
          fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 128, fontWeight: 900, color: '#1C1C1C',
          marginTop: 8, transform: `scale(${sp(300, { damping: 10 })})`,
        }}>
          A NEW <span style={{ color: '#8FD14F', textShadow: '0 6px 0 rgba(28,28,28,0.3)' }}>NORMAL</span>
        </div>

        {/* Disclaimer card */}
        <div style={{
          marginTop: 60, background: 'rgba(235,237,227,0.92)', border: '4px solid #1C1C1C',
          padding: '20px 50px', borderRadius: 12, maxWidth: 1240, textAlign: 'center',
          transform: `translateY(${(1 - sp(480)) * 120}px)`, opacity: sp(480),
        }}>
          <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 36, fontWeight: 700, color: '#1C1C1C' }}>
            DISCLAIMER: research + starting point — not personalized financial advice.
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, rgba(0,0,0,0) 55%, rgba(28,28,28,0.35) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene06NewNormal;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07TheParadox',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07TheParadox: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // See-saw tilt: money side goes down (heavier), people side up
  const tilt = interpolate(sp(180, { damping: 11 }), [0, 1], [0, -9]);

  // Impact shake on payoff
  const shake = frame > 480 && frame < 496 ? Math.sin(frame * 2.8) * 7 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#E4B028', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 20% 80%, #E9BC42 0%, #E4B028 40%, #A87A14 100%)' }} />

      {/* Shape Layers */}
      <div style={{ position: 'absolute', width: 850, height: 850, borderRadius: '50%', border: '2px solid rgba(235,237,227,0.09)', right: -260, top: -260, transform: `rotate(${frame * 0.05}deg)` }} />
      <div style={{ position: 'absolute', width: 0, height: 0, borderLeft: '260px solid transparent', borderRight: '260px solid transparent', borderTop: '440px solid rgba(28,28,28,0.04)', left: 40, bottom: -100, transform: `rotate(${frame * -0.03}deg)` }} />

      {/* Texture: question marks grid, very subtle */}
      {Array.from({ length: 10 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 180, fontWeight: 900,
          color: 'rgba(28,28,28,0.035)', left: (i % 5) * 400 + 60, top: Math.floor(i / 5) * 520 + 40,
          transform: `rotate(${i % 2 ? 12 : -10}deg)`,
        }}>?</div>
      ))}

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 111);
        return (
          <div key={i} style={{
            position: 'absolute', width: 5 + s * 5, height: 5 + s * 5, borderRadius: '50%',
            background: 'rgba(235,237,227,0.45)',
            left: s * 1920, top: (s * 1080 + frame * (0.4 + s * 0.4)) % 1080, opacity: 0.4,
          }} />
        );
      })}

      {/* Content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `translateX(${shake}px)` }}>
        <div style={{
          fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 104, fontWeight: 900, color: '#1C1C1C',
          textAlign: 'center', lineHeight: 1.08, transform: `translateY(${(1 - sp(10)) * -60}px)`, opacity: sp(10),
        }}>
          FIRE THOUSANDS.<br />
          <span style={{ color: '#EBEDE3', textShadow: '0 6px 0 rgba(28,28,28,0.25)' }}>EARN MORE THAN EVER.</span>
        </div>

        {/* See-saw */}
        <div style={{ position: 'relative', width: 1200, height: 340, marginTop: 60, opacity: sp(150) }}>
          <div style={{
            position: 'absolute', left: '50%', top: 60, width: 1100, height: 22, marginLeft: -550,
            background: '#1C1C1C', borderRadius: 11, transform: `rotate(${tilt}deg)`, transformOrigin: 'center',
          }}>
            {/* Money block (heavy, left) */}
            <div style={{
              position: 'absolute', left: 30, top: -150, width: 300, height: 140, background: '#8FD14F',
              borderRadius: 14, display: 'flex', justifyContent: 'center', alignItems: 'center',
              boxShadow: '0 14px 30px rgba(28,28,28,0.25)',
            }}>
              <span style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 52, fontWeight: 900, color: '#1C1C1C' }}>REVENUE</span>
            </div>
            {/* People block (light, right) */}
            <div style={{
              position: 'absolute', right: 30, top: -130, width: 300, height: 120, background: '#EBEDE3',
              borderRadius: 14, display: 'flex', justifyContent: 'center', alignItems: 'center',
              boxShadow: '0 14px 30px rgba(28,28,28,0.25)',
            }}>
              <span style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 52, fontWeight: 900, color: '#1C1C1C' }}>PAYROLL</span>
            </div>
          </div>
          {/* Fulcrum */}
          <div style={{ position: 'absolute', left: '50%', top: 82, width: 0, height: 0, marginLeft: -70, borderLeft: '70px solid transparent', borderRight: '70px solid transparent', borderBottom: '130px solid #1C1C1C' }} />
        </div>

        {/* Payoff line */}
        <div style={{
          marginTop: 40, background: '#1C1C1C', padding: '20px 60px', borderRadius: 16,
          transform: `scale(${sp(470, { damping: 9 })})`,
        }}>
          <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 72, fontWeight: 900, color: '#EBEDE3' }}>THEY FOUND SOMETHING </span>
          <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 72, fontWeight: 900, color: '#8FD14F' }}>CHEAPER THAN YOU</span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, rgba(0,0,0,0) 55%, rgba(28,28,28,0.35) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene07TheParadox;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08SevenHundredBillion',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08SevenHundredBillion: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Count-up to 700
  const count = Math.floor(interpolate(frame, [200, 380], [0, 700], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));

  // Comparison bars: 2025 vs 2026
  const bar25 = sp(430);
  const bar26 = sp(470);

  const companies = ['AMAZON', 'MICROSOFT', 'ALPHABET', 'META'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#E4B028', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #E4B028 0%, #D3A423 55%, #8F6810 100%)' }} />

      {/* Shape Layers */}
      <div style={{ position: 'absolute', width: 1000, height: 1000, borderRadius: '50%', border: '3px solid rgba(143,209,79,0.08)', left: 460, top: -180, transform: `scale(${1 + Math.sin(frame * 0.018) * 0.03})` }} />
      <div style={{ position: 'absolute', width: 420, height: 420, background: 'rgba(235,237,227,0.05)', right: -110, bottom: -140, transform: `rotate(${frame * 0.04}deg)` }} />

      {/* Texture: circuit-style lines */}
      <svg width={1920} height={1080} style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 7 }).map((_, i) => (
          <path key={i} d={`M 0 ${140 + i * 130} H ${500 + i * 150} V ${240 + i * 130} H 1920`} stroke="#1C1C1C" strokeWidth={2} fill="none" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 131);
        return (
          <div key={i} style={{
            position: 'absolute', width: 4 + s * 6, height: 4 + s * 6,
            background: i % 4 === 0 ? '#8FD14F' : 'rgba(235,237,227,0.4)', borderRadius: 2,
            left: (s * 1920 + frame * 0.5) % 1920, top: s * 1080, opacity: 0.4,
          }} />
        );
      })}

      {/* Content */}
      <AbsoluteFill style={{ alignItems: 'center' }}>
        {/* Company chips */}
        <div style={{ display: 'flex', gap: 30, marginTop: 90 }}>
          {companies.map((c, i) => (
            <div key={c} style={{
              background: '#1C1C1C', padding: '16px 42px', borderRadius: 50,
              transform: `translateY(${(1 - sp(20 + i * 22)) * -100}px)`, opacity: sp(20 + i * 22),
            }}>
              <span style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 44, fontWeight: 900, color: '#EBEDE3' }}>{c}</span>
            </div>
          ))}
        </div>

        {/* Giant number */}
        <div style={{ textAlign: 'center', marginTop: 70 }}>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 50, fontWeight: 800, color: '#1C1C1C', letterSpacing: 8, opacity: sp(170) }}>
            2026 CAPEX GUIDANCE
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 220, fontWeight: 900, lineHeight: 1,
            color: '#EBEDE3', textShadow: '0 12px 0 rgba(28,28,28,0.25)',
            opacity: interpolate(frame, [195, 215], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}>
            {'\u0024'}{count}<span style={{ color: '#8FD14F' }}>B</span>
          </div>
        </div>

        {/* 2025 vs 2026 comparison bars */}
        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 70, marginTop: 60, height: 230 }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{ width: 240, height: 110 * bar25, background: '#EBEDE3', borderRadius: '10px 10px 0 0', opacity: 0.85 }} />
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 40, fontWeight: 800, color: '#1C1C1C', marginTop: 10 }}>2025 ACTUAL</div>
          </div>
          <div style={{ textAlign: 'center' }}>
            <div style={{ width: 240, height: 220 * bar26, background: '#8FD14F', borderRadius: '10px 10px 0 0', boxShadow: '0 12px 26px rgba(28,28,28,0.25)' }} />
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 40, fontWeight: 800, color: '#1C1C1C', marginTop: 10 }}>2026 = ~2X</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, rgba(0,0,0,0) 55%, rgba(28,28,28,0.35) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene08SevenHundredBillion;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09PayrollToProcessors',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09PayrollToProcessors: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow zoom out
  const camScale = interpolate(frame, [0, durationInFrames], [1.1, 1], { easing: Easing.inOut(Easing.ease) });

  // Money dots flowing left -> right
  const flowOn = frame > 160;

  return (
    <AbsoluteFill style={{ backgroundColor: '#E4B028', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 110%, #E9BC42 0%, #E4B028 45%, #9C7112 100%)' }} />

      {/* Shape Layers */}
      <div style={{ position: 'absolute', width: 640, height: 640, borderRadius: '50%', border: '2px solid rgba(28,28,28,0.07)', left: -180, top: -180, transform: `rotate(${frame * 0.05}deg)` }} />
      <div style={{ position: 'absolute', width: 520, height: 520, background: 'rgba(143,209,79,0.05)', right: -160, top: -120, transform: `rotate(${30 + frame * 0.03}deg)` }} />

      {/* Texture: chip grid */}
      {Array.from({ length: 24 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', width: 60, height: 60, border: '1px solid rgba(28,28,28,0.06)',
          left: (i % 8) * 250 + 45, top: Math.floor(i / 8) * 380 + 60, borderRadius: 8,
        }} />
      ))}

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 151);
        return (
          <div key={i} style={{
            position: 'absolute', width: 6, height: 6, borderRadius: '50%',
            background: 'rgba(235,237,227,0.5)',
            left: s * 1920, top: (s * 1080 + frame * 0.5) % 1080, opacity: 0.35,
          }} />
        );
      })}

      {/* Content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>
        <div style={{
          fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 96, fontWeight: 900, color: '#1C1C1C',
          opacity: sp(10), transform: `translateY(${(1 - sp(10)) * -50}px)`, marginBottom: 70,
        }}>
          THE BUDGET DIDN'T DISAPPEAR — <span style={{ color: '#EBEDE3', textShadow: '0 5px 0 rgba(28,28,28,0.25)' }}>IT MOVED</span>
        </div>

        <div style={{ display: 'flex', alignItems: 'center', gap: 60 }}>
          {/* PAYROLL box */}
          <div style={{
            width: 460, height: 320, background: '#EBEDE3', borderRadius: 20, padding: 34,
            transform: `translateX(${(1 - sp(60)) * -300}px)`, opacity: sp(60),
            boxShadow: '0 16px 36px rgba(28,28,28,0.25)',
          }}>
            <div style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 58, fontWeight: 900, color: '#1C1C1C' }}>PAYROLL</div>
            {[0, 1, 2, 3].map((i) => (
              <div key={i} style={{
                height: 26, borderRadius: 13, background: '#9E9E9E', marginTop: 20,
                width: `${90 - i * 18 - (flowOn ? Math.min(40, (frame - 160) * 0.15) : 0)}%`,
                transition: 'none',
              }} />
            ))}
          </div>

          {/* Flow arrow + money dots */}
          <div style={{ position: 'relative', width: 300, height: 120 }}>
            <svg width={300} height={120} viewBox="0 0 300 120">
              <path d="M 0 60 H 240" stroke="#1C1C1C" strokeWidth={10} strokeLinecap="round" />
              <path d="M 230 25 L 295 60 L 230 95 Z" fill="#1C1C1C" />
            </svg>
            {flowOn && [0, 1, 2, 3, 4].map((i) => (
              <div key={i} style={{
                position: 'absolute', top: 44, width: 30, height: 30, borderRadius: '50%',
                background: '#8FD14F', border: '3px solid #1C1C1C',
                left: ((frame * 4 + i * 70) % 300) - 15,
                display: 'flex', justifyContent: 'center', alignItems: 'center',
                fontFamily: 'JetBrains Mono, monospace', fontSize: 18, fontWeight: 800, color: '#1C1C1C',
              }}>{'\u0024'}</div>
            ))}
          </div>

          {/* PROCESSORS chip */}
          <div style={{
            width: 460, height: 320, background: '#1C1C1C', borderRadius: 20, padding: 34, position: 'relative',
            transform: `translateX(${(1 - sp(100)) * 300}px)`, opacity: sp(100),
            boxShadow: '0 16px 36px rgba(28,28,28,0.35)',
          }}>
            <div style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 58, fontWeight: 900, color: '#8FD14F' }}>PROCESSORS</div>
            {/* Chip SVG */}
            <svg width={170} height={170} viewBox="0 0 170 170" style={{ marginTop: 16 }}>
              <rect x={35} y={35} width={100} height={100} rx={12} fill="#8FD14F" opacity={0.9 + Math.sin(frame * 0.1) * 0.1} />
              <rect x={60} y={60} width={50} height={50} rx={6} fill="#1C1C1C" />
              {[0, 1, 2, 3].map((i) => (
                <g key={i}>
                  <rect x={48 + i * 22} y={10} width={8} height={25} fill="#EBEDE3" />
                  <rect x={48 + i * 22} y={135} width={8} height={25} fill="#EBEDE3" />
                  <rect x={10} y={48 + i * 22} width={25} height={8} fill="#EBEDE3" />
                  <rect x={135} y={48 + i * 22} width={25} height={8} fill="#EBEDE3" />
                </g>
              ))}
            </svg>
          </div>
        </div>

        {/* Read that again */}
        <div style={{
          marginTop: 70, fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 92, fontWeight: 900,
          color: '#EBEDE3', textShadow: '0 6px 0 rgba(28,28,28,0.25)',
          transform: `scale(${sp(440, { damping: 9 })})`,
        }}>
          READ IT AGAIN: <span style={{ color: '#8FD14F' }}>{'\u0024'}700,000,000,000</span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, rgba(0,0,0,0) 55%, rgba(28,28,28,0.35) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene09PayrollToProcessors;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10MetaWhiplash',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10MetaWhiplash: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Impact shake as each card lands
  const impacts = [80, 220, 360];
  const shake = impacts.some((t) => frame > t + 8 && frame < t + 20) ? Math.sin(frame * 3) * 6 : 0;

  const cards = [
    { delay: 80, label: 'Q1 2026 REVENUE', value: '\u002456.3B', sub: 'UP 33% YoY', accent: '#8FD14F', bg: '#1C1C1C' },
    { delay: 220, label: 'SAME COMPANY — MAY', value: '8,000', sub: 'LAYOFFS EXECUTED', accent: '#E4B028', bg: '#EBEDE3' },
    { delay: 360, label: '2026 CAPEX PUSHED TO', value: '\u0024145B', sub: 'AI INFRASTRUCTURE', accent: '#8FD14F', bg: '#1C1C1C' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#E4B028', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'linear-gradient(125deg, #E9BC42 0%, #E4B028 40%, #A87A14 100%)' }} />

      {/* Shape Layers */}
      <div style={{ position: 'absolute', width: 950, height: 950, borderRadius: '50%', border: '3px solid rgba(235,237,227,0.08)', left: -300, bottom: -400, transform: `rotate(${frame * -0.04}deg) scale(${1 + Math.sin(frame * 0.02) * 0.03})` }} />
      <div style={{ position: 'absolute', width: 480, height: 480, background: 'rgba(28,28,28,0.05)', right: -120, top: -160, transform: `rotate(${frame * 0.05}deg)` }} />

      {/* Texture: zigzag whiplash lines */}
      <svg width={1920} height={1080} style={{ position: 'absolute', opacity: 0.06 }}>
        {[0, 1, 2].map((i) => (
          <path key={i} d={`M 0 ${250 + i * 300} L 320 ${170 + i * 300} L 640 ${300 + i * 300} L 960 ${180 + i * 300} L 1280 ${310 + i * 300} L 1600 ${190 + i * 300} L 1920 ${280 + i * 300}`} stroke="#1C1C1C" strokeWidth={4} fill="none" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 171);
        return (
          <div key={i} style={{
            position: 'absolute', width: 5 + s * 5, height: 5 + s * 5, borderRadius: '50%',
            background: i % 3 === 0 ? '#8FD14F' : 'rgba(235,237,227,0.45)',
            left: (s * 1920 - frame * 0.5 + 1920) % 1920, top: s * 1080, opacity: 0.4,
          }} />
        );
      })}

      {/* Content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `translateX(${shake}px)` }}>
        <div style={{
          fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 64, fontWeight: 900, color: '#1C1C1C',
          letterSpacing: 12, opacity: sp(10), marginBottom: 60,
        }}>
          META — THE WHOLE MOVIE IN ONE SENTENCE
        </div>

        {/* Three stat cards */}
        <div style={{ display: 'flex', gap: 50 }}>
          {cards.map((c, i) => {
            const pop = sp(c.delay, { damping: 10 });
            return (
              <div key={i} style={{
                width: 480, height: 400, background: c.bg, borderRadius: 24, padding: 40,
                transform: `translateY(${(1 - pop) * 500}px) rotate(${(1 - pop) * (i % 2 ? 6 : -6)}deg)`,
                boxShadow: '0 20px 44px rgba(28,28,28,0.3)',
                textAlign: 'center',
              }}>
                <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 36, fontWeight: 700, color: c.bg === '#1C1C1C' ? '#9E9E9E' : '#1C1C1C', letterSpacing: 3 }}>
                  {c.label}
                </div>
                <div style={{
                  fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 128, fontWeight: 900, marginTop: 40,
                  color: c.bg === '#1C1C1C' ? '#EBEDE3' : '#1C1C1C', lineHeight: 1,
                }}>
                  {c.value}
                </div>
                <div style={{
                  display: 'inline-block', marginTop: 40, padding: '10px 30px', borderRadius: 10,
                  background: c.accent, fontFamily: 'Inter, sans-serif', fontSize: 34, fontWeight: 800, color: '#1C1C1C',
                }}>
                  {c.sub}
                </div>
              </div>
            );
          })}
        </div>

        {/* Kicker */}
        <div style={{
          marginTop: 70, fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 84, fontWeight: 900,
          color: '#EBEDE3', textShadow: '0 6px 0 rgba(28,28,28,0.25)',
          transform: `scale(${sp(520, { damping: 9 })})`,
        }}>
          REVENUE UP <span style={{ color: '#8FD14F' }}>1/3</span> — 8,000 PEOPLE <span style={{ color: '#1C1C1C', textShadow: 'none' }}>OUT</span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, rgba(0,0,0,0) 55%, rgba(28,28,28,0.35) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene10MetaWhiplash;
```

