```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_ThePromise',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_ThePromise: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const contentExit = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const zoom = interpolate(frame, [0, durationInFrames], [1, 1.08]);
  const pan = interpolate(frame, [0, durationInFrames], [-15, 15]);

  const quoteOp = interpolate(sp(5), [0, 1], [0, 1]);
  const helpfulScale = sp(20);
  const strikeThrough = interpolate(frame, [140, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const revealTruth = interpolate(sp(180), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden' }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(244,242,242,0.18) 0%, transparent 55%), radial-gradient(circle at 75% 70%, rgba(29,33,40,0.22) 0%, transparent 60%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: '2px solid #1D2128', opacity: 0.06,
        left: 200 + pan, top: -300 + Math.sin(frame / 40) * 20,
        transform: `scale(${zoom}) rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 600, height: 600,
        border: '3px solid #F4F2F2', opacity: 0.08,
        right: 100 - pan, bottom: -200,
        transform: `rotate(${45 + frame * 0.1}deg) scale(${zoom})`,
      }} />

      {/* Motion texture — dot grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots1" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="4" cy="4" r="1.5" fill="#1D2128" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots1)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 1);
        const y = ((frame * (0.5 + r * 1.2)) + i * 60) % 1200 - 60;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: y,
            width: 6 + r * 8,
            height: 6 + r * 8,
            borderRadius: '50%',
            background: i % 2 === 0 ? '#1D2128' : '#F4F2F2',
            opacity: 0.25,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ opacity: contentExit, transform: `scale(${zoom})` }}>
        {/* Top corner label */}
        <div style={{
          position: 'absolute', top: 60, left: 80,
          fontFamily: 'JetBrains Mono, monospace', fontSize: 26,
          color: '#1D2128', letterSpacing: 4, fontWeight: 700,
          opacity: quoteOp,
        }}>
          [ CORPORATE PITCH • 2023 ]
        </div>

        {/* Quote block */}
        <div style={{
          position: 'absolute', top: 210, left: 120, width: 1680,
          opacity: quoteOp,
        }}>
          <div style={{ fontFamily: 'Nexa Bold', fontSize: 100, color: '#1D2128', lineHeight: 1, marginBottom: 20 }}>
            &ldquo;
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 96, color: '#1D2128',
            lineHeight: 1.05, fontWeight: 900,
            transform: `scale(${0.9 + helpfulScale * 0.1})`, transformOrigin: 'left',
          }}>
            AI is just your{' '}
            <span style={{ position: 'relative', display: 'inline-block' }}>
              <span style={{ color: '#F4F2F2', background: '#1D2128', padding: '4px 18px' }}>
                HELPFUL
              </span>
              <div style={{
                position: 'absolute', top: '50%', left: 0,
                height: 8, background: '#1D2128',
                width: `${strikeThrough * 100}%`,
              }} />
            </span>{' '}
            little<br />
            digital assistant.
          </div>
        </div>

        {/* Bottom reveal */}
        <div style={{
          position: 'absolute', bottom: 130, left: 120,
          opacity: revealTruth,
          transform: `translateY(${(1 - revealTruth) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace', fontSize: 32,
            color: '#1D2128', opacity: 0.7, letterSpacing: 3,
          }}>
            — WHAT THEY TOLD YOU
          </div>
        </div>

        {/* Decorative bracket icons */}
        <svg style={{ position: 'absolute', top: 200, right: 100, opacity: quoteOp * 0.3 }} width="180" height="180" viewBox="0 0 100 100">
          <path d="M20 20 L20 80 L30 80" stroke="#1D2128" strokeWidth="4" fill="none" />
          <path d="M80 20 L80 80 L70 80" stroke="#1D2128" strokeWidth="4" fill="none" />
        </svg>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1D2128', opacity: 0.4,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_ThePromise;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_TheyLied',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_TheyLied: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const contentExit = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on "LIED" impact
  const shakeX = frame < 40 ? Math.sin(frame * 2) * (1 - frame / 40) * 15 : 0;
  const shakeY = frame < 40 ? Math.cos(frame * 2.3) * (1 - frame / 40) * 12 : 0;

  const liedScale = sp(0, { damping: 8, stiffness: 200 });
  const eraseProgress = interpolate(frame, [90, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const year2026 = sp(180);
  const bottomLine = sp(240);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', transform: `translate(${shakeX}px, ${shakeY}px)` }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(29,33,40,0.25) 0%, transparent 45%, rgba(29,33,40,0.15) 100%)',
      }} />

      {/* Animated triangle shape */}
      <svg style={{ position: 'absolute', top: -100, left: -100, opacity: 0.06 }} width="800" height="800">
        <polygon points="400,50 750,700 50,700" stroke="#1D2128" strokeWidth="3" fill="none"
          transform={`rotate(${frame * 0.2} 400 400)`} />
      </svg>
      <svg style={{ position: 'absolute', bottom: -200, right: -100, opacity: 0.08 }} width="900" height="900">
        <rect x="150" y="150" width="600" height="600" stroke="#F4F2F2" strokeWidth="4" fill="none"
          transform={`rotate(${-frame * 0.15} 450 450)`} />
      </svg>

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid2" x="0" y="0" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" stroke="#1D2128" strokeWidth="1" fill="none" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid2)" />
      </svg>

      {/* Floating particles — falling debris feel */}
      {Array.from({ length: 30 }).map((_, i) => {
        const r = seededRandom(i + 10);
        const y = ((frame * (1 + r * 2)) + i * 40) % 1200;
        return (
          <div key={i} style={{
            position: 'absolute', left: r * 1920, top: y,
            width: 4, height: 4 + r * 20,
            background: '#1D2128', opacity: 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ opacity: contentExit }}>
        {/* THEY LIED — massive */}
        <div style={{
          position: 'absolute', top: 180, left: 0, width: '100%',
          textAlign: 'center',
          transform: `scale(${0.5 + liedScale * 0.5})`,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 200, fontWeight: 900,
            color: '#1D2128', letterSpacing: -6, lineHeight: 0.9,
          }}>
            THEY LIED.
          </div>
        </div>

        {/* Erasing bar animation */}
        <div style={{
          position: 'absolute', top: 460, left: 0, width: '100%',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Nexa Bold', fontSize: 68, color: '#1D2128',
            letterSpacing: 2, position: 'relative', display: 'inline-block',
          }}>
            THE BLOODBATH HAS BEGUN
            <div style={{
              position: 'absolute', top: 0, left: 0, height: '100%',
              background: '#1D2128', width: `${eraseProgress * 100}%`,
              mixBlendMode: 'multiply',
            }} />
          </div>
        </div>

        {/* 2026 marker */}
        <div style={{
          position: 'absolute', top: 620, left: '50%',
          transform: `translateX(-50%) scale(${year2026})`,
          display: 'flex', alignItems: 'center', gap: 24,
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <circle cx="30" cy="30" r="12" fill="#ff0000" />
            <circle cx="30" cy="30" r="20" stroke="#ff0000" strokeWidth="3" fill="none" opacity={0.5 + Math.sin(frame / 5) * 0.5} />
          </svg>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace', fontSize: 42,
            color: '#1D2128', fontWeight: 700, letterSpacing: 6,
          }}>
            LIVE • 2026
          </div>
        </div>

        {/* Bottom message */}
        <div style={{
          position: 'absolute', bottom: 130, left: 0, width: '100%',
          textAlign: 'center', opacity: bottomLine,
          transform: `translateY(${(1 - bottomLine) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 44, color: '#F4F2F2',
            background: '#1D2128', display: 'inline-block', padding: '18px 40px',
            fontWeight: 700, letterSpacing: 2,
          }}>
            SILENT ERASURE OF WHITE-COLLAR AMERICA
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(29,33,40,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1D2128', opacity: 0.4,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_TheyLied;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_TheTarget',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_TheTarget: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const contentExit = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Target crosshair zoom in
  const crosshairScale = interpolate(sp(10), [0, 1], [3, 1]);
  const crosshairRotate = interpolate(frame, [0, durationInFrames], [0, 360]);
  const targetLock = sp(120);
  const youAreScale = sp(160);
  const bottomSlide = sp(240);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden' }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(255,158,32,1) 0%, rgba(200,120,20,1) 100%)',
      }} />

      {/* Concentric target rings */}
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        transform: `translate(-50%, -50%) scale(${crosshairScale})`,
      }}>
        {[900, 700, 500, 300, 150].map((size, i) => (
          <div key={i} style={{
            position: 'absolute', top: -size / 2, left: -size / 2,
            width: size, height: size, borderRadius: '50%',
            border: `${3 - i * 0.3}px solid #1D2128`,
            opacity: 0.15 + i * 0.05,
          }} />
        ))}
      </div>

      {/* Rotating crosshair lines */}
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        transform: `translate(-50%, -50%) rotate(${crosshairRotate}deg)`,
        opacity: 0.2,
      }}>
        <div style={{ position: 'absolute', width: 1400, height: 2, background: '#1D2128', top: -1, left: -700 }} />
        <div style={{ position: 'absolute', width: 2, height: 1400, background: '#1D2128', left: -1, top: -700 }} />
      </div>

      {/* Diagonal scan lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="diag3" x="0" y="0" width="30" height="30" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="30" stroke="#1D2128" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag3)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 30);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: (r * 1080 + Math.sin((frame + i * 10) / 30) * 40) % 1080,
            width: 5, height: 5, borderRadius: '50%',
            background: '#1D2128', opacity: 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ opacity: contentExit }}>
        {/* Top status bar */}
        <div style={{
          position: 'absolute', top: 50, left: 80, right: 80,
          display: 'flex', justifyContent: 'space-between', alignItems: 'center',
          fontFamily: 'JetBrains Mono, monospace', fontSize: 24,
          color: '#1D2128', fontWeight: 700, letterSpacing: 3,
        }}>
          <div>◉ TARGET: WHITE-COLLAR</div>
          <div style={{ opacity: targetLock }}>◉ STATUS: LOCKED</div>
        </div>

        {/* Icon row */}
        <div style={{
          position: 'absolute', top: 180, left: 0, width: '100%',
          display: 'flex', justifyContent: 'center', gap: 60,
        }}>
          {/* Office building */}
          <div style={{ transform: `scale(${sp(20)})` }}>
            <svg width="130" height="130" viewBox="0 0 100 100">
              <rect x="20" y="15" width="60" height="80" fill="#1D2128" />
              <rect x="28" y="25" width="10" height="10" fill="#FF9E20" />
              <rect x="45" y="25" width="10" height="10" fill="#FF9E20" />
              <rect x="62" y="25" width="10" height="10" fill="#FF9E20" />
              <rect x="28" y="42" width="10" height="10" fill="#FF9E20" />
              <rect x="45" y="42" width="10" height="10" fill="#FF9E20" />
              <rect x="62" y="42" width="10" height="10" fill="#FF9E20" />
              <rect x="28" y="59" width="10" height="10" fill="#FF9E20" />
              <rect x="45" y="59" width="10" height="10" fill="#FF9E20" />
              <rect x="62" y="59" width="10" height="10" fill="#FF9E20" />
              <rect x="42" y="78" width="16" height="17" fill="#FF9E20" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 24, color: '#1D2128', textAlign: 'center', fontWeight: 700, marginTop: 8 }}>OFFICE</div>
          </div>
          {/* Degree */}
          <div style={{ transform: `scale(${sp(40)})` }}>
            <svg width="130" height="130" viewBox="0 0 100 100">
              <polygon points="50,20 90,40 50,60 10,40" fill="#1D2128" />
              <path d="M 25 46 L 25 65 L 50 78 L 75 65 L 75 46" stroke="#1D2128" strokeWidth="5" fill="none" />
              <line x1="90" y1="40" x2="90" y2="70" stroke="#1D2128" strokeWidth="4" />
              <circle cx="90" cy="72" r="5" fill="#1D2128" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 24, color: '#1D2128', textAlign: 'center', fontWeight: 700, marginTop: 8 }}>DEGREE</div>
          </div>
          {/* AC unit */}
          <div style={{ transform: `scale(${sp(60)})` }}>
            <svg width="130" height="130" viewBox="0 0 100 100">
              <rect x="15" y="25" width="70" height="35" rx="4" fill="#1D2128" />
              <line x1="20" y1="35" x2="80" y2="35" stroke="#FF9E20" strokeWidth="2" />
              <line x1="20" y1="42" x2="80" y2="42" stroke="#FF9E20" strokeWidth="2" />
              <line x1="20" y1="49" x2="80" y2="49" stroke="#FF9E20" strokeWidth="2" />
              <path d="M 30 68 Q 35 78 30 88" stroke="#1D2128" strokeWidth="3" fill="none" />
              <path d="M 50 68 Q 55 78 50 88" stroke="#1D2128" strokeWidth="3" fill="none" />
              <path d="M 70 68 Q 75 78 70 88" stroke="#1D2128" strokeWidth="3" fill="none" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 24, color: '#1D2128', textAlign: 'center', fontWeight: 700, marginTop: 8 }}>SALARY</div>
          </div>
        </div>

        {/* YOU ARE THE TARGET */}
        <div style={{
          position: 'absolute', top: 420, left: 0, width: '100%',
          textAlign: 'center', transform: `scale(${youAreScale})`,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 170, fontWeight: 900,
            color: '#1D2128', letterSpacing: -5, lineHeight: 0.95,
          }}>
            YOU ARE
          </div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 170, fontWeight: 900,
            color: '#F4F2F2', background: '#1D2128', display: 'inline-block',
            padding: '0 40px', letterSpacing: -5, lineHeight: 1,
            marginTop: 10,
          }}>
            THE TARGET
          </div>
        </div>

        {/* Bottom label */}
        <div style={{
          position: 'absolute', bottom: 110, left: 0, width: '100%',
          textAlign: 'center', opacity: bottomSlide,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace', fontSize: 34,
            color: '#1D2128', letterSpacing: 4, fontWeight: 700,
          }}>
            ↓ SILICON VALLEY + WALL STREET NUMBERS ↓
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 40%, rgba(29,33,40,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1D2128', opacity: 0.4,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_TheTarget;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_BlockMeta',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_BlockMeta: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const contentExit = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const blockCount = interpolate(frame, [30, 100], [0, 40], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const metaCount = interpolate(frame, [140, 210], [0, 20], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const blockCardIn = sp(15);
  const metaCardIn = sp(130);
  const barFillBlock = interpolate(frame, [30, 110], [0, 0.4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const barFillMeta = interpolate(frame, [140, 220], [0, 0.2], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden' }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(255,158,32,1) 0%, rgba(230,140,25,1) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: -300, right: -200,
        width: 800, height: 800, borderRadius: '50%',
        background: '#1D2128', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame / 40) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: -200, left: -150,
        width: 600, height: 600,
        background: '#F4F2F2', opacity: 0.06,
        transform: `rotate(${45 + frame * 0.1}deg)`,
      }} />

      {/* Vertical bars texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 40 }).map((_, i) => (
          <line key={i} x1={i * 50} y1="0" x2={i * 50} y2="1080" stroke="#1D2128" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const r = seededRandom(i + 40);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: ((r * 1080) + frame * (r * 1.5)) % 1080,
            width: 4, height: 4,
            background: '#1D2128', opacity: 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ opacity: contentExit }}>
        {/* Header */}
        <div style={{
          position: 'absolute', top: 60, left: 0, width: '100%',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace', fontSize: 28,
            color: '#1D2128', letterSpacing: 5, fontWeight: 700, opacity: 0.7,
          }}>
            QUARTERLY EARNINGS • 2026
          </div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 90, color: '#1D2128',
            fontWeight: 900, letterSpacing: -2, marginTop: 8,
          }}>
            THE WRITING ON THE WALL
          </div>
        </div>

        {/* Two card layout */}
        <div style={{
          position: 'absolute', top: 280, left: 0, width: '100%',
          display: 'flex', justifyContent: 'center', gap: 60,
        }}>
          {/* BLOCK CARD */}
          <div style={{
            width: 780, height: 560,
            background: '#1D2128', padding: 50,
            transform: `translateY(${(1 - blockCardIn) * 60}px) scale(${blockCardIn})`,
            opacity: blockCardIn,
            border: '4px solid #1D2128',
            position: 'relative',
          }}>
            {/* Block icon */}
            <div style={{ display: 'flex', alignItems: 'center', gap: 20, marginBottom: 30 }}>
              <svg width="90" height="90" viewBox="0 0 100 100">
                <rect x="15" y="15" width="70" height="70" rx="14" fill="#F4F2F2" />
                <path d="M 35 35 L 65 35 L 65 65 L 35 65 Z M 42 42 L 58 42 L 58 58 L 42 58 Z" fill="#1D2128" />
              </svg>
              <div style={{ fontFamily: 'Mont Heavy', fontSize: 76, color: '#F4F2F2', fontWeight: 900 }}>
                BLOCK
              </div>
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 34, color: '#F4F2F2', opacity: 0.6, marginBottom: 20 }}>
              Staff Cut
            </div>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 240, color: '#ff0000',
              fontWeight: 900, lineHeight: 0.9,
            }}>
              {Math.floor(blockCount)}%
            </div>
            {/* Bar */}
            <div style={{
              position: 'absolute', bottom: 50, left: 50, right: 50,
              height: 24, background: '#F4F2F2', opacity: 0.15,
            }}>
              <div style={{
                height: '100%', background: '#ff0000',
                width: `${barFillBlock * 100}%`,
              }} />
            </div>
            <div style={{
              position: 'absolute', bottom: 30, left: 50,
              fontFamily: 'JetBrains Mono', fontSize: 20, color: '#F4F2F2', opacity: 0.5,
            }}>
              GUTTED
            </div>
          </div>

          {/* META CARD */}
          <div style={{
            width: 780, height: 560,
            background: '#F4F2F2', padding: 50,
            transform: `translateY(${(1 - metaCardIn) * 60}px) scale(${metaCardIn})`,
            opacity: metaCardIn,
            position: 'relative',
          }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 20, marginBottom: 30 }}>
              <svg width="90" height="90" viewBox="0 0 100 100">
                <path d="M 20 70 Q 20 30 40 30 Q 55 30 60 50 Q 65 30 80 30 Q 100 30 100 70" 
                  stroke="#1D2128" strokeWidth="10" fill="none" transform="translate(-5,0)" />
                <path d="M 20 70 Q 20 30 40 30 Q 55 30 60 50 Q 65 30 80 30" 
                  stroke="#1D2128" strokeWidth="10" fill="none" transform="translate(-5,0)" />
              </svg>
              <div style={{ fontFamily: 'Mont Heavy', fontSize: 76, color: '#1D2128', fontWeight: 900 }}>
                META
              </div>
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 34, color: '#1D2128', opacity: 0.6, marginBottom: 20 }}>
              Workforce Slash
            </div>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 240, color: '#ff0000',
              fontWeight: 900, lineHeight: 0.9,
            }}>
              {Math.floor(metaCount)}%
            </div>
            <div style={{
              position: 'absolute', bottom: 50, left: 50, right: 50,
              height: 24, background: '#1D2128', opacity: 0.15,
            }}>
              <div style={{
                height: '100%', background: '#ff0000',
                width: `${barFillMeta * 100}%`,
              }} />
            </div>
            <div style={{
              position: 'absolute', bottom: 30, left: 50,
              fontFamily: 'JetBrains Mono', fontSize: 20, color: '#1D2128', opacity: 0.5,
            }}>
              MULTIPLE ROUNDS
            </div>
          </div>
        </div>

        {/* Bottom tag */}
        <div style={{
          position: 'absolute', bottom: 80, left: 0, width: '100%',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 40, color: '#1D2128',
            fontWeight: 800, letterSpacing: 3, opacity: sp(220),
          }}>
            NO EVOLUTION → JUST ERASURE
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1D2128', opacity: 0.4,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_BlockMeta;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_HumanVsCompute',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_HumanVsCompute: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const contentExit = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const giants = ['AMAZON', 'SALESFORCE', 'ATLASSIAN'];
  const swapProgress = interpolate(frame, [150, 240], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden' }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(255,158,32,1) 0%, rgba(240,150,30,1) 50%, rgba(255,158,32,1) 100%)',
      }} />

      {/* Shape layers */}
      <svg style={{ position: 'absolute', top: 100, left: -100, opacity: 0.07 }} width="700" height="700">
        <polygon points="350,50 650,650 50,650" stroke="#1D2128" strokeWidth="3" fill="none"
          transform={`rotate(${frame * 0.1} 350 400)`} />
      </svg>
      <div style={{
        position: 'absolute', top: 400, right: -100,
        width: 500, height: 500, borderRadius: '50%',
        border: '3px solid #F4F2F2', opacity: 0.1,
        transform: `scale(${1 + Math.sin(frame / 30) * 0.05})`,
      }} />

      {/* Hex grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="hex5" x="0" y="0" width="60" height="52" patternUnits="userSpaceOnUse">
            <polygon points="30,2 55,15 55,40 30,52 5,40 5,15" stroke="#1D2128" strokeWidth="1" fill="none" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hex5)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const r = seededRandom(i + 50);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r * 1920 + frame * (r < 0.5 ? 1 : -1)) % 1920,
            top: r * 1080,
            width: 6, height: 6, borderRadius: '50%',
            background: '#1D2128', opacity: 0.25,
          }} />
        );
      })}

      <AbsoluteFill style={{ opacity: contentExit }}>
        {/* Top header */}
        <div style={{
          position: 'absolute', top: 55, left: 0, width: '100%',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 28,
            color: '#1D2128', letterSpacing: 5, fontWeight: 700, opacity: 0.7,
          }}>
            ▲ TRIMMING THE FAT ▲
          </div>
        </div>

        {/* Giant company tags */}
        <div style={{
          position: 'absolute', top: 130, left: 0, width: '100%',
          display: 'flex', justifyContent: 'center', gap: 40,
        }}>
          {giants.map((g, i) => {
            const s = sp(10 + i * 15);
            return (
              <div key={g} style={{
                background: '#1D2128',
                padding: '22px 46px',
                fontFamily: 'Mont Heavy', fontSize: 54, color: '#F4F2F2',
                fontWeight: 900, letterSpacing: 2,
                transform: `translateY(${(1 - s) * 40}px)`,
                opacity: s,
              }}>
                {g}
              </div>
            );
          })}
        </div>

        {/* The equation: HUMAN → COMPUTE */}
        <div style={{
          position: 'absolute', top: 340, left: 0, width: '100%',
          display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 40,
        }}>
          {/* HUMAN side */}
          <div style={{
            width: 500, height: 500, background: '#F4F2F2',
            display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
            transform: `scale(${sp(80)}) rotate(${swapProgress * -8}deg)`,
            opacity: 1 - swapProgress * 0.5,
            position: 'relative',
          }}>
            <svg width="200" height="200" viewBox="0 0 100 100">
              <circle cx="50" cy="32" r="18" fill="#1D2128" />
              <path d="M 20 90 Q 20 55 50 55 Q 80 55 80 90 Z" fill="#1D2128" />
            </svg>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 60, color: '#1D2128',
              fontWeight: 900, marginTop: 20, letterSpacing: 2,
            }}>
              PAYROLL
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#ff0000', fontWeight: 700, marginTop: 8 }}>
              $$$ / MONTH
            </div>
          </div>

          {/* Arrow */}
          <div style={{ transform: `scale(${sp(120)})` }}>
            <svg width="200" height="120" viewBox="0 0 200 120">
              <path d="M 20 60 L 160 60 M 130 30 L 170 60 L 130 90" stroke="#1D2128" strokeWidth="10" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 22, color: '#1D2128',
              fontWeight: 700, textAlign: 'center', letterSpacing: 3,
            }}>
              REPLACED BY
            </div>
          </div>

          {/* COMPUTE side */}
          <div style={{
            width: 500, height: 500, background: '#1D2128',
            display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
            transform: `scale(${sp(160)}) rotate(${swapProgress * 8}deg)`,
          }}>
            {/* CPU chip icon */}
            <svg width="200" height="200" viewBox="0 0 100 100">
              <rect x="25" y="25" width="50" height="50" rx="4" fill="#FF9E20" />
              <rect x="35" y="35" width="30" height="30" fill="#1D2128" />
              {/* pins */}
              {[30, 45, 60].map(p => (
                <React.Fragment key={p}>
                  <line x1={p} y1="15" x2={p} y2="25" stroke="#FF9E20" strokeWidth="4" />
                  <line x1={p} y1="75" x2={p} y2="85" stroke="#FF9E20" strokeWidth="4" />
                  <line x1="15" y1={p} x2="25" y2={p} stroke="#FF9E20" strokeWidth="4" />
                  <line x1="75" y1={p} x2="85" y2={p} stroke="#FF9E20" strokeWidth="4" />
                </React.Fragment>
              ))}
            </svg>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 60, color: '#F4F2F2',
              fontWeight: 900, marginTop: 20, letterSpacing: 2,
            }}>
              COMPUTE
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#FF9E20', fontWeight: 700, marginTop: 8 }}>
              PENNIES / MONTH
            </div>
          </div>
        </div>

        {/* Bottom tag */}
        <div style={{
          position: 'absolute', bottom: 60, left: 0, width: '100%',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 44, color: '#F4F2F2',
            background: '#1D2128', display: 'inline-block',
            padding: '18px 40px', fontWeight: 800, letterSpacing: 2,
            opacity: sp(200),
          }}>
            STRUCTURAL REPLACEMENT — NOT COST CUTTING
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(29,33,40,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1D2128', opacity: 0.4,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_HumanVsCompute;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_MeatGrinder',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_MeatGrinder: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const contentExit = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const collideProgress = interpolate(frame, [30, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const grinderScale = sp(120, { damping: 8, stiffness: 200 });
  const middleFade = sp(200);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden' }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(255,158,32,1) 0%, rgba(200,110,15,1) 100%)',
      }} />

      {/* Shape rotating gears feel */}
      {[0, 1, 2].map(i => (
        <div key={i} style={{
          position: 'absolute',
          top: 200 + i * 200, left: -200 + i * 800,
          width: 500, height: 500,
          border: '4px dashed #1D2128',
          borderRadius: '50%', opacity: 0.05,
          transform: `rotate(${frame * (i % 2 === 0 ? 0.3 : -0.3)}deg)`,
        }} />
      ))}

      {/* Cross-hatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="cross6" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="40" y2="40" stroke="#1D2128" strokeWidth="1" />
            <line x1="40" y1="0" x2="0" y2="40" stroke="#1D2128" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross6)" />
      </svg>

      {/* Particles — money bills falling */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 60);
        const y = ((frame * (0.8 + r)) + i * 80) % 1200 - 100;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: y,
            width: 30, height: 15,
            background: '#1D2128', opacity: 0.2,
            transform: `rotate(${r * 360 + frame}deg)`,
          }} />
        );
      })}

      <AbsoluteFill style={{ opacity: contentExit }}>
        {/* Top intersection visual */}
        <div style={{
          position: 'absolute', top: 90, left: 0, width: '100%',
          display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 20,
        }}>
          {/* Wall Street */}
          <div style={{
            transform: `translateX(${(1 - collideProgress) * -200}px)`,
            display: 'flex', flexDirection: 'column', alignItems: 'center',
          }}>
            <svg width="130" height="130" viewBox="0 0 100 100">
              <rect x="10" y="70" width="80" height="8" fill="#1D2128" />
              <rect x="15" y="30" width="8" height="40" fill="#1D2128" />
              <rect x="30" y="30" width="8" height="40" fill="#1D2128" />
              <rect x="45" y="30" width="8" height="40" fill="#1D2128" />
              <rect x="60" y="30" width="8" height="40" fill="#1D2128" />
              <rect x="75" y="30" width="8" height="40" fill="#1D2128" />
              <polygon points="10,30 50,10 90,30" fill="#1D2128" />
            </svg>
            <div style={{ fontFamily: 'Mont Heavy', fontSize: 46, color: '#1D2128', fontWeight: 900, letterSpacing: 1 }}>
              WALL STREET
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 26, color: '#ff0000', fontWeight: 700 }}>GREED</div>
          </div>

          {/* Plus */}
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 90, color: '#1D2128',
            fontWeight: 900, transform: `scale(${collideProgress})`,
          }}>×</div>

          {/* Silicon Valley */}
          <div style={{
            transform: `translateX(${(1 - collideProgress) * 200}px)`,
            display: 'flex', flexDirection: 'column', alignItems: 'center',
          }}>
            <svg width="130" height="130" viewBox="0 0 100 100">
              <rect x="20" y="20" width="60" height="60" rx="6" fill="#1D2128" />
              <rect x="32" y="32" width="36" height="36" fill="#F4F2F2" />
              <rect x="42" y="42" width="16" height="16" fill="#1D2128" />
            </svg>
            <div style={{ fontFamily: 'Mont Heavy', fontSize: 46, color: '#1D2128', fontWeight: 900, letterSpacing: 1 }}>
              SILICON VALLEY
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 26, color: '#ff0000', fontWeight: 700 }}>INNOVATION</div>
          </div>
        </div>

        {/* Equals arrow */}
        <div style={{
          position: 'absolute', top: 430, left: 0, width: '100%',
          textAlign: 'center', opacity: sp(110),
        }}>
          <svg width="80" height="80" viewBox="0 0 100 100">
            <path d="M 50 20 L 50 75 M 30 55 L 50 80 L 70 55" stroke="#1D2128" strokeWidth="10" fill="none" strokeLinecap="round" />
          </svg>
        </div>

        {/* MEAT GRINDER label */}
        <div style={{
          position: 'absolute', top: 540, left: 0, width: '100%',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 30,
            color: '#1D2128', letterSpacing: 6, fontWeight: 700, opacity: 0.7,
          }}>
            NOT A UTOPIA. INSTEAD →
          </div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 180, color: '#F4F2F2',
            background: '#1D2128', display: 'inline-block',
            padding: '10px 50px', fontWeight: 900, letterSpacing: -3,
            transform: `scale(${grinderScale})`,
            marginTop: 20, lineHeight: 1,
          }}>
            MEAT GRINDER
          </div>
        </div>

        {/* Bottom */}
        <div style={{
          position: 'absolute', bottom: 90, left: 0, width: '100%',
          textAlign: 'center', opacity: middleFade,
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 42, color: '#1D2128',
            fontWeight: 800, letterSpacing: 2,
          }}>
            DESIGNED TO SEPARATE THE MIDDLE CLASS FROM THEIR INCOME
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(29,33,40,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1D2128', opacity: 0.4,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_MeatGrinder;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_KuxAIBuilt',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_KuxAIBuilt: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const contentExit = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Curtain reveal
  const curtainLeft = interpolate(frame, [30, 120], [0, -1000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const curtainRight = interpolate(frame, [30, 120], [0, 1000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const brandScale = sp(90, { damping: 9, stiffness: 150 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden' }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(255,180,60,1) 0%, rgba(220,130,20,1) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        transform: `translate(-50%, -50%) rotate(${frame * 0.2}deg)`,
        width: 1200, height: 1200,
        border: '2px solid #1D2128', opacity: 0.05,
        borderRadius: '50%',
      }} />
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        transform: `translate(-50%, -50%) rotate(${-frame * 0.15}deg)`,
        width: 900, height: 900,
        border: '2px solid #F4F2F2', opacity: 0.1,
      }} />

      {/* Radial line burst */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 32 }).map((_, i) => {
          const angle = (i / 32) * Math.PI * 2;
          return (
            <line key={i}
              x1={960} y1={540}
              x2={960 + Math.cos(angle) * 1100}
              y2={540 + Math.sin(angle) * 1100}
              stroke="#1D2128" strokeWidth="1"
            />
          );
        })}
      </svg>

      {/* Particles */}
      {Array.from({ length: 26 }).map((_, i) => {
        const r = seededRandom(i + 70);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920, top: r * 1080,
            width: 5, height: 5, borderRadius: '50%',
            background: '#1D2128', opacity: 0.3,
            transform: `translate(${Math.sin((frame + i * 20) / 40) * 30}px, 0)`,
          }} />
        );
      })}

      <AbsoluteFill style={{ opacity: contentExit }}>
        {/* Background truth message revealed under curtain */}
        <div style={{
          position: 'absolute', top: 280, left: 0, width: '100%',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 30,
            color: '#1D2128', letterSpacing: 5, fontWeight: 700, marginBottom: 20, opacity: 0.7,
          }}>
            [ THE UNVARNISHED TRUTH ]
          </div>
          <div style={{
            transform: `scale(${brandScale})`,
            display: 'inline-block',
          }}>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 240, color: '#1D2128',
              fontWeight: 900, letterSpacing: -8, lineHeight: 0.95,
            }}>
              KUXAI
            </div>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 100,
              color: '#F4F2F2', background: '#1D2128',
              display: 'inline-block', padding: '8px 32px',
              fontWeight: 900, letterSpacing: 4, marginTop: 6,
            }}>
              .7X
            </div>
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 40, color: '#1D2128',
            fontWeight: 700, letterSpacing: 3, marginTop: 30, opacity: sp(140),
          }}>
            PROTECT YOUR FAMILY&apos;S WEALTH
          </div>
        </div>

        {/* Curtains sliding away */}
        <div style={{
          position: 'absolute', top: 0, left: 0, width: '50%', height: '100%',
          background: 'linear-gradient(90deg, #1D2128 0%, #2D2D2D 100%)',
          transform: `translateX(${curtainLeft}px)`,
          borderRight: '4px solid #FF9E20',
        }}>
          {/* Curtain vertical lines */}
          {Array.from({ length: 10 }).map((_, i) => (
            <div key={i} style={{
              position: 'absolute', top: 0, height: '100%',
              left: `${(i + 1) * 9}%`, width: 2,
              background: '#F4F2F2', opacity: 0.1,
            }} />
          ))}
          <div style={{
            position: 'absolute', top: '50%', right: 60,
            transform: 'translateY(-50%)',
            fontFamily: 'JetBrains Mono', fontSize: 28,
            color: '#F4F2F2', writingMode: 'vertical-rl',
            letterSpacing: 6, opacity: 0.5,
          }}>
            CORPORATE FOG
          </div>
        </div>
        <div style={{
          position: 'absolute', top: 0, right: 0, width: '50%', height: '100%',
          background: 'linear-gradient(-90deg, #1D2128 0%, #2D2D2D 100%)',
          transform: `translateX(${curtainRight}px)`,
          borderLeft: '4px solid #FF9E20',
        }}>
          {Array.from({ length: 10 }).map((_, i) => (
            <div key={i} style={{
              position: 'absolute', top: 0, height: '100%',
              right: `${(i + 1) * 9}%`, width: 2,
              background: '#F4F2F2', opacity: 0.1,
            }} />
          ))}
          <div style={{
            position: 'absolute', top: '50%', left: 60,
            transform: 'translateY(-50%)',
            fontFamily: 'JetBrains Mono', fontSize: 28,
            color: '#F4F2F2', writingMode: 'vertical-rl',
            letterSpacing: 6, opacity: 0.5,
          }}>
            LIES & PR SPIN
          </div>
        </div>

        {/* Bottom shield icon */}
        <div style={{
          position: 'absolute', bottom: 80, left: 0, width: '100%',
          textAlign: 'center', opacity: sp(200),
        }}>
          <svg width="80" height="80" viewBox="0 0 100 100" style={{ display: 'inline-block', verticalAlign: 'middle' }}>
            <path d="M 50 10 L 85 25 L 85 55 Q 85 80 50 92 Q 15 80 15 55 L 15 25 Z" fill="#1D2128" />
            <path d="M 35 50 L 45 62 L 68 38" stroke="#FF9E20" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div style={{
            fontFamily: 'Inter', fontSize: 34, color: '#1D2128',
            fontWeight: 800, letterSpacing: 2, marginTop: 8,
          }}>
            RIPPING THE CURTAIN BACK
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(29,33,40,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#F4F2F2', opacity: 0.5,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_KuxAIBuilt;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_GroundShift',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_GroundShift: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const contentExit = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Ground tremor
  const tremorX = Math.sin(frame * 1.2) * 6;
  const tremorY = Math.cos(frame * 1.5) * 4;

  const crackWidth = interpolate(frame, [20, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shieldIn = sp(130, { damping: 9, stiffness: 140 });
  const disclaimerIn = sp(200);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden' }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(255,158,32,1) 0%, rgba(200,110,15,1) 100%)',
      }} />

      {/* Shape layers — floating rectangles like ground plates */}
      {[0, 1, 2, 3].map(i => (
        <div key={i} style={{
          position: 'absolute',
          left: 200 + i * 400 + Math.sin((frame + i * 20) / 30) * 20,
          top: 200 + (i % 2) * 400,
          width: 300, height: 200,
          border: '3px solid #1D2128', opacity: 0.06,
          transform: `rotate(${i * 15 + frame * 0.1}deg)`,
        }} />
      ))}

      {/* Texture — horizontal fault lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 90} x2="1920" y2={i * 90 + Math.sin(i) * 10} stroke="#1D2128" strokeWidth="1" strokeDasharray="8 4" />
        ))}
      </svg>

      {/* Particles — rubble */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 80);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920 + tremorX * (r - 0.5) * 4,
            top: r * 1080 + tremorY * (r - 0.5) * 4,
            width: 6 + r * 8, height: 6 + r * 8,
            background: '#1D2128', opacity: 0.25,
            transform: `rotate(${frame * (r - 0.5) * 4}deg)`,
          }} />
        );
      })}

      <AbsoluteFill style={{ opacity: contentExit, transform: `translate(${tremorX}px, ${tremorY}px)` }}>
        {/* Top header */}
        <div style={{
          position: 'absolute', top: 60, left: 0, width: '100%',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 130, color: '#1D2128',
            fontWeight: 900, letterSpacing: -3, lineHeight: 1,
          }}>
            THE GROUND IS
          </div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 130, color: '#F4F2F2',
            background: '#1D2128', display: 'inline-block',
            padding: '4px 30px', fontWeight: 900, letterSpacing: -3,
            marginTop: 8,
          }}>
            SHIFTING.
          </div>
        </div>

        {/* Cracked ground SVG */}
        <div style={{
          position: 'absolute', top: 430, left: 0, width: '100%',
          textAlign: 'center',
        }}>
          <svg width="1400" height="180" viewBox="0 0 1400 180">
            {/* main horizon line */}
            <line x1="0" y1="90" x2="1400" y2="90" stroke="#1D2128" strokeWidth="6" />
            {/* crack */}
            <path
              d={`M 700 90 L ${700 - crackWidth * 40} 105 L ${700 + crackWidth * 30} 125 L ${700 - crackWidth * 20} 150 L ${700 + crackWidth * 50} 175`}
              stroke="#1D2128" strokeWidth={8 * crackWidth} fill="none" strokeLinecap="round"
            />
            <path
              d={`M 700 90 L ${700 + crackWidth * 40} 75 L ${700 - crackWidth * 30} 55 L ${700 + crackWidth * 20} 30 L ${700 - crackWidth * 50} 5`}
              stroke="#1D2128" strokeWidth={8 * crackWidth} fill="none" strokeLinecap="round"
            />
            {/* small cracks */}
            <path d={`M ${400} 90 L ${400 + crackWidth * 30} 130 L ${400 - crackWidth * 10} 170`} stroke="#1D2128" strokeWidth={4 * crackWidth} fill="none" />
            <path d={`M ${1000} 90 L ${1000 - crackWidth * 30} 130 L ${1000 + crackWidth * 10} 170`} stroke="#1D2128" strokeWidth={4 * crackWidth} fill="none" />
          </svg>
        </div>

        {/* Legal shield */}
        <div style={{
          position: 'absolute', top: 620, left: 0, width: '100%',
          display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 30,
          transform: `scale(${shieldIn})`,
        }}>
          <svg width="120" height="120" viewBox="0 0 100 100">
            <path d="M 50 8 L 88 22 L 88 55 Q 88 82 50 94 Q 12 82 12 55 L 12 22 Z" fill="#1D2128" />
            <text x="50" y="60" textAnchor="middle" fontFamily="Mont Heavy" fontSize="30" fontWeight="900" fill="#FF9E20">§</text>
          </svg>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 66, color: '#1D2128',
            fontWeight: 900, letterSpacing: -1,
          }}>
            LEGAL SHIELD ENGAGED
          </div>
        </div>

        {/* Disclaimer strip */}
        <div style={{
          position: 'absolute', bottom: 70, left: 0, width: '100%',
          textAlign: 'center', opacity: disclaimerIn,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 32,
            color: '#F4F2F2', background: '#1D2128',
            display: 'inline-block', padding: '18px 36px',
            fontWeight: 700, letterSpacing: 3,
          }}>
            NOT PERSONALIZED FINANCIAL ADVICE
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(29,33,40,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1D2128', opacity: 0.4,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_GroundShift;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_HRHides',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_HRHides: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const contentExit = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const listIn = (delay: number) => {
    const s = sp(delay);
    return { opacity: s, transform: `translateX(${(1 - s) * -60}px)` };
  };

  const folderIn = sp(140, { damping: 8, stiffness: 180 });
  const hidingReveal = interpolate(frame, [180, 260], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden' }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,158,32,1) 0%, rgba(230,140,25,1) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: -200, left: -200,
        width: 700, height: 700,
        border: '3px solid #1D2128', opacity: 0.06,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: -150, right: -150,
        width: 550, height: 550, borderRadius: '50%',
        border: '3px solid #F4F2F2', opacity: 0.1,
      }} />

      {/* Texture — document lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 45 }).map((_, i) => (
          <line key={i} x1="100" y1={i * 24 + 20} x2="1820" y2={i * 24 + 20} stroke="#1D2128" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 90);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920, top: r * 1080,
            width: 4, height: 4, borderRadius: '50%',
            background: '#1D2128', opacity: 0.25,
            transform: `translate(${Math.sin((frame + i * 12) / 25) * 20}px, ${Math.cos((frame + i * 12) / 25) * 20}px)`,
          }} />
        );
      })}

      <AbsoluteFill style={{ opacity: contentExit }}>
        {/* LEFT — checklist */}
        <div style={{
          position: 'absolute', top: 80, left: 100, width: 900,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 26,
            color: '#1D2128', letterSpacing: 4, fontWeight: 700, opacity: 0.7,
          }}>
            YOUR DUE DILIGENCE CHECKLIST
          </div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 76, color: '#1D2128',
            fontWeight: 900, letterSpacing: -2, marginTop: 8, lineHeight: 1,
          }}>
            I AM NOT YOUR<br />FIDUCIARY.
          </div>

          {/* Checklist items */}
          <div style={{ marginTop: 40 }}>
            {[
              { d: 20, t: 'Do your own research' },
              { d: 45, t: 'Verify every claim' },
              { d: 70, t: 'Move money slowly' },
            ].map((item, i) => (
              <div key={i} style={{
                display: 'flex', alignItems: 'center', gap: 24,
                marginBottom: 24, ...listIn(item.d),
              }}>
                <svg width="52" height="52" viewBox="0 0 52 52">
                  <rect x="4" y="4" width="44" height="44" rx="6" fill="#1D2128" />
                  <path d="M 14 26 L 22 34 L 38 18" stroke="#FF9E20" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
                <div style={{
                  fontFamily: 'Inter', fontSize: 42, color: '#1D2128',
                  fontWeight: 700,
                }}>
                  {item.t}
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* RIGHT — HR Folder */}
        <div style={{
          position: 'absolute', top: 180, right: 100,
          width: 700, transform: `scale(${folderIn}) rotate(${folderIn * -4}deg)`,
        }}>
          <div style={{
            background: '#1D2128', padding: 40,
            position: 'relative',
            boxShadow: '20px 20px 0 rgba(29,33,40,0.3)',
          }}>
            {/* Folder tab */}
            <div style={{
              position: 'absolute', top: -32, left: 40,
              background: '#1D2128', padding: '10px 32px',
              fontFamily: 'JetBrains Mono', fontSize: 22,
              color: '#FF9E20', fontWeight: 700, letterSpacing: 3,
            }}>
              HR • CONFIDENTIAL
            </div>

            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 60, color: '#F4F2F2',
              fontWeight: 900, lineHeight: 1, marginBottom: 30,
            }}>
              WHAT HR<br />IS HIDING
            </div>

            {/* Redacted lines */}
            {[280, 220, 320, 260, 300].map((w, i) => (
              <div key={i} style={{
                background: '#F4F2F2', height: 22,
                width: w, marginBottom: 14, opacity: 0.9,
                transform: `translateX(${hidingReveal * (i % 2 === 0 ? 20 : -20)}px)`,
              }} />
            ))}

            {/* Stamp */}
            <div style={{
              position: 'absolute', top: -20, right: -30,
              background: '#ff0000', color: '#F4F2F2',
              fontFamily: 'Mont Heavy', fontSize: 40, fontWeight: 900,
              padding: '10px 24px', letterSpacing: 4,
              border: '5px solid #ff0000',
              transform: 'rotate(12deg)',
              opacity: sp(180),
            }}>
              CLASSIFIED
            </div>
          </div>
        </div>

        {/* Bottom */}
        <div style={{
          position: 'absolute', bottom: 60, left: 0, width: '100%',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 40, color: '#F4F2F2',
            background: '#1D2128', display: 'inline-block',
            padding: '16px 36px', fontWeight: 800, letterSpacing: 2,
            opacity: sp(240),
          }}>
            MACRO-LEVEL REALITY — DELIVERED RAW
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1D2128', opacity: 0.4,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_HRHides;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_DarioAmodei',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_DarioAmodei: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Content fade out but keep bg orange
  const contentExit = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const profileCardIn = sp(10, { damping: 10, stiffness: 130 });
  const nameIn = sp(50);
  const titleIn = sp(80);
  const arenaIn = sp(120);
  const predictionIn = sp(200, { damping: 9, stiffness: 150 });
  const pulse = 0.9 + Math.sin(frame / 8) * 0.1;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden' }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 50%, rgba(255,180,60,1) 0%, rgba(210,120,15,1) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: -150, right: -150,
        width: 700, height: 700, borderRadius: '50%',
        background: '#1D2128', opacity: 0.05,
      }} />
      <div style={{
        position: 'absolute', bottom: -200, left: -200,
        width: 800, height: 800,
        border: '4px solid #F4F2F2', opacity: 0.08,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Texture — circuit lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="circ10" x="0" y="0" width="120" height="120" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 60 40 L 100 40 L 100 80 L 60 80 L 60 120" stroke="#1D2128" strokeWidth="1.5" fill="none" />
            <circle cx="60" cy="40" r="3" fill="#1D2128" />
            <circle cx="100" cy="80" r="3" fill="#1D2128" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#circ10)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 26 }).map((_, i) => {
        const r = seededRandom(i + 100);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: ((r * 1080 + frame * (0.5 + r)) % 1080),
            width: 5, height: 5,
            background: '#1D2128', opacity: 0.28,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ opacity: contentExit }}>
        {/* Top header — arms race */}
        <div style={{
          position: 'absolute', top: 50, left: 0, width: '100%',
          textAlign: 'center', opacity: arenaIn,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 28,
            color: '#1D2128', letterSpacing: 6, fontWeight: 700,
          }}>
            ⟶ THE AI ARMS RACE ⟵
          </div>
        </div>

        {/* Profile card LEFT */}
        <div style={{
          position: 'absolute', top: 180, left: 120,
          width: 700, background: '#1D2128', padding: 50,
          transform: `scale(${profileCardIn})`,
          opacity: profileCardIn,
        }}>
          {/* Avatar placeholder (abstract) */}
          <div style={{
            width: 180, height: 180,
            background: '#FF9E20',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            marginBottom: 30,
          }}>
            <svg width="140" height="140" viewBox="0 0 100 100">
              <circle cx="50" cy="35" r="18" fill="#1D2128" />
              <path d="M 22 90 Q 22 55 50 55 Q 78 55 78 90 Z" fill="#1D2128" />
            </svg>
          </div>

          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 22,
            color: '#FF9E20', letterSpacing: 3, fontWeight: 700, marginBottom: 8,
            opacity: nameIn,
          }}>
            SOURCE • ON THE RECORD
          </div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 74, color: '#F4F2F2',
            fontWeight: 900, letterSpacing: -2, lineHeight: 1,
            opacity: nameIn,
            transform: `translateX(${(1 - nameIn) * -30}px)`,
          }}>
            DARIO<br />AMODEI
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 32, color: '#F4F2F2',
            opacity: titleIn * 0.7, marginTop: 20, fontWeight: 600,
          }}>
            CEO — ANTHROPIC
          </div>

          {/* LIVE indicator */}
          <div style={{
            marginTop: 24, display: 'flex', alignItems: 'center', gap: 14,
            opacity: titleIn,
          }}>
            <div style={{
              width: 14, height: 14, borderRadius: '50%',
              background: '#ff0000', transform: `scale(${pulse})`,
            }} />
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 22,
              color: '#F4F2F2', letterSpacing: 3, fontWeight: 700,
            }}>
              CHILLING PREDICTION INCOMING
            </div>
          </div>
        </div>

        {/* Right — Quote frame */}
        <div style={{
          position: 'absolute', top: 240, right: 100,
          width: 780, transform: `scale(${predictionIn})`,
          opacity: predictionIn,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 180, color: '#1D2128',
            fontWeight: 900, lineHeight: 0.8,
          }}>
            &ldquo;
          </div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 72, color: '#1D2128',
            fontWeight: 900, lineHeight: 1.05, letterSpacing: -1,
            marginTop: -30,
          }}>
            ONE OF THE MOST POWERFUL MINDS IN AI...
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 36, color: '#F4F2F2',
            background: '#1D2128', display: 'inline-block',
            padding: '14px 28px', fontWeight: 700, letterSpacing: 2,
            marginTop: 30,
          }}>
            HIS WARNING IS NEXT →
          </div>
        </div>

        {/* Bottom ticker */}
        <div style={{
          position: 'absolute', bottom: 60, left: 0, width: '100%',
          background: '#1D2128', padding: '18px 0',
          overflow: 'hidden',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 28,
            color: '#FF9E20', fontWeight: 700, letterSpacing: 4,
            whiteSpace: 'nowrap',
            transform: `translateX(${-frame * 4}px)`,
          }}>
            ● ANTHROPIC CEO WARNS ● 50% WHITE-COLLAR JOBS AT RISK ● UNPRECEDENTED SHIFT ● LISTEN CAREFULLY ● ANTHROPIC CEO WARNS ● 50% WHITE-COLLAR JOBS AT RISK ●
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(29,33,40,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#F4F2F2', opacity: 0.5,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_DarioAmodei;
```

