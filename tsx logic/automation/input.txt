```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1Intro',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1Intro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const nameScale = sp(20);
  const channelSlide = sp(45);
  const questionRise = sp(80);
  const warningPulse = 1 + Math.sin(frame * 0.15) * 0.04;
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.08]);
  const bgShift = Math.sin(frame * 0.02) * 30;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${50 + bgShift}% 40%, #FF3030 0%, #FF0000 45%, #080616 100%)`,
      }} />

      {/* Animated shape layers - large transparent geometric forms */}
      <div style={{
        position: 'absolute', top: -200, left: -150, width: 800, height: 800,
        borderRadius: '50%', border: '3px solid #F5F5F5', opacity: 0.06,
        transform: `rotate(${frame * 0.3}deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: -250, right: -200, width: 900, height: 900,
        background: '#080616', opacity: 0.18,
        transform: `rotate(${-frame * 0.2}deg)`,
        clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
      }} />
      <div style={{
        position: 'absolute', top: '30%', right: '10%', width: 400, height: 400,
        border: '2px solid #F5F5F5', opacity: 0.05,
        transform: `rotate(${frame * 0.5}deg)`,
      }} />

      {/* Motion texture - vertical lines grid */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.08 }}>
        {Array.from({ length: 40 }).map((_, i) => (
          <line key={i} x1={i * 48} y1={0} x2={i * 48 + Math.sin(frame * 0.02 + i) * 20} y2={1080}
            stroke="#F5F5F5" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const r = seededRandom(i + 1);
        const r2 = seededRandom(i + 100);
        const x = r * 1920;
        const y = (r2 * 1080 + frame * (0.5 + r)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 4 + r * 6, height: 4 + r * 6, borderRadius: '50%',
            background: '#F5F5F5', opacity: 0.3 + r * 0.4,
            boxShadow: '0 0 10px #F5F5F5',
          }} />
        );
      })}

      {/* Camera zoom wrapper */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center center' }}>

        {/* Top bar - PROBUFFET HQ presents */}
        <div style={{
          position: 'absolute', top: 80, left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(5)) * -50}px)`,
          opacity: sp(5),
          display: 'flex', alignItems: 'center', gap: 16,
        }}>
          <div style={{ width: 60, height: 2, background: '#F5F5F5' }} />
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 26, letterSpacing: 8,
            color: '#F5F5F5', fontWeight: 700,
          }}>
            PROBUFFET HQ • EPISODE 01
          </div>
          <div style={{ width: 60, height: 2, background: '#F5F5F5' }} />
        </div>

        {/* Greeting NAMASKAR */}
        <div style={{
          position: 'absolute', top: 180, left: '50%',
          transform: `translateX(-50%) scale(${sp(15) * 0.85 + 0.15})`,
          opacity: sp(15),
          fontFamily: 'Yantra One', fontSize: 102,
          color: '#F5F5F5', fontWeight: 700,
          textShadow: '0 8px 0 #080616',
        }}>
          नमस्कार
        </div>

        {/* Main name reveal block */}
        <div style={{
          position: 'absolute', top: 340, left: '50%',
          transform: `translateX(-50%) scale(${nameScale})`,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 32, letterSpacing: 6,
            color: '#080616', fontWeight: 800, marginBottom: 12,
          }}>
            ── MAIN HOON ──
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 204,
            color: '#F5F5F5', fontWeight: 900, lineHeight: 0.95,
            textShadow: '0 12px 0 #080616, 0 0 60px rgba(245,245,245,0.4)',
            letterSpacing: -2,
          }}>
            कुनाल
          </div>
        </div>

        {/* Channel badge */}
        <div style={{
          position: 'absolute', top: 640, left: '50%',
          transform: `translateX(-50%) translateY(${(1 - channelSlide) * 60}px)`,
          opacity: channelSlide,
          display: 'flex', alignItems: 'center', gap: 20,
          padding: '18px 40px',
          background: '#080616',
          border: '3px solid #F5F5F5',
          borderRadius: 8,
        }}>
          <svg width="40" height="40" viewBox="0 0 40 40">
            <circle cx="20" cy="20" r="18" fill="#F5F5F5" />
            <path d="M14 14 L14 26 L20 22 L26 26 L26 14 L20 18 Z" fill="#FF0000" />
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 38,
            color: '#F5F5F5', fontWeight: 800, letterSpacing: 2,
          }}>
            ProBuffet HQ
          </div>
        </div>

        {/* Question warning block */}
        <div style={{
          position: 'absolute', bottom: 140, left: '50%',
          transform: `translateX(-50%) translateY(${(1 - questionRise) * 80}px) scale(${warningPulse})`,
          opacity: questionRise,
          display: 'flex', alignItems: 'center', gap: 24,
          padding: '24px 50px',
          background: '#F5F5F5',
          borderRadius: 4,
          boxShadow: '0 15px 0 #080616',
        }}>
          {/* Warning icon SVG */}
          <svg width="60" height="60" viewBox="0 0 60 60">
            <path d="M30 5 L55 50 L5 50 Z" fill="#FF0000" stroke="#080616" strokeWidth="3" />
            <rect x="27" y="22" width="6" height="16" fill="#080616" />
            <circle cx="30" cy="44" r="3" fill="#080616" />
          </svg>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 56,
            color: '#080616', fontWeight: 700, letterSpacing: 1,
          }}>
            एक असहज सवाल...
          </div>
        </div>

        {/* Side accent bars */}
        <div style={{
          position: 'absolute', left: 0, top: '40%',
          width: 12, height: 200 * sp(30),
          background: '#080616',
        }} />
        <div style={{
          position: 'absolute', right: 0, top: '50%',
          width: 12, height: 200 * sp(35),
          background: '#F5F5F5',
        }} />

      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(8,6,22,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1Intro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2Question',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2Question: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Count up animation for the amount
  const countProgress = interpolate(frame, [40, 110], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const amount = Math.floor(countProgress * 1000000);
  const formattedAmount = '₹ ' + amount.toLocaleString('en-IN');

  // Question mark grows huge
  const qScale = sp(120, { damping: 8, stiffness: 100 });
  const camPan = interpolate(frame, [0, durationInFrames], [-20, 20]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #080616 0%, #FF0000 50%, #080616 100%)',
      }} />

      {/* Shape layers - hexagons */}
      <div style={{
        position: 'absolute', top: '15%', left: '5%', width: 500, height: 500,
        background: '#F5F5F5', opacity: 0.04,
        clipPath: 'polygon(25% 5%, 75% 5%, 100% 50%, 75% 95%, 25% 95%, 0% 50%)',
        transform: `rotate(${frame * 0.4}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', right: '8%', width: 600, height: 600,
        background: '#080616', opacity: 0.25,
        clipPath: 'polygon(25% 5%, 75% 5%, 100% 50%, 75% 95%, 25% 95%, 0% 50%)',
        transform: `rotate(${-frame * 0.3}deg)`,
      }} />

      {/* Motion texture - grid dots */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.15 }}>
        {Array.from({ length: 20 }).map((_, i) =>
          Array.from({ length: 12 }).map((_, j) => (
            <circle key={`${i}-${j}`} cx={i * 100 + 50} cy={j * 100 + 50}
              r={1.5 + Math.sin(frame * 0.05 + i + j) * 0.5} fill="#F5F5F5" />
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 50);
        const y = (seededRandom(i + 200) * 1080 - frame * (1 + r * 2)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: r * 1920, top: y < 0 ? y + 1080 : y,
            width: 3, height: 3, borderRadius: '50%',
            background: '#F5F5F5', opacity: 0.6,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>

        {/* Top label */}
        <div style={{
          position: 'absolute', top: 100, left: '50%',
          transform: `translateX(-50%)`, opacity: sp(5),
          fontFamily: 'Inter, Poppins', fontSize: 28, letterSpacing: 10,
          color: '#F5F5F5', fontWeight: 700,
          padding: '12px 32px', border: '2px solid #F5F5F5',
        }}>
          आपका बैंक अकाउंट
        </div>

        {/* Bank account card visualization */}
        <div style={{
          position: 'absolute', top: 220, left: '50%',
          transform: `translateX(-50%) scale(${sp(20)})`,
          width: 1100, height: 320,
          background: '#080616',
          borderRadius: 20,
          border: '4px solid #F5F5F5',
          boxShadow: '0 20px 0 rgba(245,245,245,0.2)',
          padding: 40,
        }}>
          {/* Card header */}
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 24, color: '#F5F5F5',
              opacity: 0.7, letterSpacing: 4,
            }}>
              SAVINGS ACCOUNT
            </div>
            {/* Bank icon */}
            <svg width="60" height="60" viewBox="0 0 60 60">
              <rect x="5" y="25" width="50" height="30" fill="#F5F5F5" />
              <polygon points="30,5 5,25 55,25" fill="#F5F5F5" />
              <rect x="12" y="32" width="4" height="18" fill="#080616" />
              <rect x="22" y="32" width="4" height="18" fill="#080616" />
              <rect x="34" y="32" width="4" height="18" fill="#080616" />
              <rect x="44" y="32" width="4" height="18" fill="#080616" />
            </svg>
          </div>

          {/* Balance label */}
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 22, color: '#F5F5F5',
            opacity: 0.6, marginTop: 30, letterSpacing: 2,
          }}>
            AVAILABLE BALANCE
          </div>

          {/* Amount big */}
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 130, color: '#F5F5F5',
            fontWeight: 800, marginTop: 8, letterSpacing: -2,
            opacity: sp(40),
          }}>
            {formattedAmount}
          </div>
        </div>

        {/* The piercing question */}
        <div style={{
          position: 'absolute', bottom: 200, left: '50%',
          transform: `translateX(-50%)`, opacity: sp(110),
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 102, color: '#F5F5F5',
            fontWeight: 700, lineHeight: 1.1,
            textShadow: '0 6px 0 #080616',
          }}>
            कितने <span style={{ color: '#080616', background: '#F5F5F5', padding: '0 20px' }}>असल में</span> आपके?
          </div>
        </div>

        {/* Giant Question mark - background */}
        <div style={{
          position: 'absolute', right: 80, bottom: 80,
          fontFamily: 'Yantra One', fontSize: 360,
          color: '#F5F5F5', opacity: 0.12, fontWeight: 900,
          transform: `scale(${qScale}) rotate(${Math.sin(frame * 0.05) * 5}deg)`,
          transformOrigin: 'center',
        }}>
          ?
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 35%, rgba(8,6,22,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2Question;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3Trust',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3Trust: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Equation reveal
  const leftIn = sp(15);
  const equalsIn = sp(50);
  const rightIn = sp(70);
  const stampIn = sp(130, { damping: 6, stiffness: 80 });
  const camTilt = Math.sin(frame * 0.02) * 1.5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at top, #FF2020 0%, #FF0000 40%, #080616 100%)',
      }} />

      {/* Shape layers - drifting rectangles */}
      <div style={{
        position: 'absolute', top: '10%', left: '15%',
        width: 400, height: 600, background: '#F5F5F5', opacity: 0.05,
        transform: `rotate(${-15 + Math.sin(frame * 0.02) * 3}deg) translateY(${Math.sin(frame * 0.03) * 20}px)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', right: '10%',
        width: 350, height: 500, background: '#080616', opacity: 0.3,
        transform: `rotate(${20 + Math.sin(frame * 0.025) * 3}deg)`,
      }} />

      {/* Motion texture - concentric circles */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.1 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <circle key={i} cx="960" cy="540" r={100 + i * 120 + Math.sin(frame * 0.03 + i) * 10}
            fill="none" stroke="#F5F5F5" strokeWidth="1" strokeDasharray="8 12" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const r = seededRandom(i + 70);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r * 1920 + Math.sin(frame * 0.02 + i) * 30),
            top: ((seededRandom(i + 300) * 1080 + frame * 0.6) % 1080),
            width: 2 + r * 4, height: 2 + r * 4, borderRadius: '50%',
            background: '#F5F5F5', opacity: 0.4,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camTilt}deg) scale(${interpolate(frame, [0, durationInFrames], [1, 1.05])})` }}>

        {/* Top header */}
        <div style={{
          position: 'absolute', top: 90, left: '50%',
          transform: `translateX(-50%)`, opacity: sp(5),
          fontFamily: 'Yantra One', fontSize: 56,
          color: '#F5F5F5', fontWeight: 700,
          background: '#080616', padding: '14px 50px',
          borderRadius: 4,
        }}>
          भारतीय सोच का सबसे बड़ा सच
        </div>

        {/* The equation: BANK = SAFETY */}
        <div style={{
          position: 'absolute', top: 320, left: '50%',
          transform: `translateX(-50%)`,
          display: 'flex', alignItems: 'center', gap: 50,
        }}>
          {/* LEFT side - BANK */}
          <div style={{
            transform: `translateX(${(1 - leftIn) * -200}px) scale(${leftIn})`,
            opacity: leftIn,
            width: 420, height: 380,
            background: '#080616',
            border: '5px solid #F5F5F5',
            borderRadius: 16,
            display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center',
            padding: 30,
          }}>
            {/* Bank Building SVG */}
            <svg width="180" height="160" viewBox="0 0 180 160">
              <polygon points="90,10 10,55 170,55" fill="#F5F5F5" />
              <rect x="10" y="55" width="160" height="12" fill="#F5F5F5" />
              <rect x="20" y="70" width="14" height="70" fill="#F5F5F5" />
              <rect x="50" y="70" width="14" height="70" fill="#F5F5F5" />
              <rect x="80" y="70" width="14" height="70" fill="#F5F5F5" />
              <rect x="110" y="70" width="14" height="70" fill="#F5F5F5" />
              <rect x="140" y="70" width="14" height="70" fill="#F5F5F5" />
              <rect x="5" y="140" width="170" height="14" fill="#F5F5F5" />
            </svg>
            <div style={{
              fontFamily: 'Yantra One', fontSize: 72, color: '#F5F5F5',
              fontWeight: 800, marginTop: 18,
            }}>
              बैंक
            </div>
          </div>

          {/* EQUALS sign */}
          <div style={{
            opacity: equalsIn,
            transform: `scale(${equalsIn * 1.5})`,
            fontFamily: 'Inter, Poppins', fontSize: 180,
            color: '#F5F5F5', fontWeight: 900,
            textShadow: '0 8px 0 #080616',
          }}>
            =
          </div>

          {/* RIGHT side - SURAKSHA */}
          <div style={{
            transform: `translateX(${(1 - rightIn) * 200}px) scale(${rightIn})`,
            opacity: rightIn,
            width: 420, height: 380,
            background: '#F5F5F5',
            border: '5px solid #080616',
            borderRadius: 16,
            display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center',
            padding: 30,
          }}>
            {/* Shield/Lock SVG */}
            <svg width="170" height="170" viewBox="0 0 170 170">
              <path d="M85 10 L150 35 L150 90 Q150 140 85 160 Q20 140 20 90 L20 35 Z"
                fill="#080616" stroke="#FF0000" strokeWidth="4" />
              <rect x="60" y="75" width="50" height="50" fill="#F5F5F5" rx="4" />
              <path d="M68 75 L68 60 Q68 45 85 45 Q102 45 102 60 L102 75"
                fill="none" stroke="#F5F5F5" strokeWidth="6" />
              <circle cx="85" cy="98" r="6" fill="#080616" />
            </svg>
            <div style={{
              fontFamily: 'Yantra One', fontSize: 64, color: '#080616',
              fontWeight: 800, marginTop: 14,
            }}>
              सुरक्षा
            </div>
          </div>
        </div>

        {/* Bottom - GUARANTEE stamp */}
        <div style={{
          position: 'absolute', bottom: 110, left: '50%',
          transform: `translateX(-50%) rotate(${-8 + (1 - stampIn) * 30}deg) scale(${stampIn})`,
          opacity: stampIn,
          padding: '20px 60px',
          border: '6px solid #F5F5F5',
          background: 'rgba(8,6,22,0.85)',
          borderRadius: 8,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 64,
            color: '#F5F5F5', fontWeight: 900, letterSpacing: 8,
          }}>
            गारंटी ✕ विश्वास
          </div>
        </div>

        {/* Side label - generations */}
        <div style={{
          position: 'absolute', top: 220, right: 60,
          opacity: sp(90),
          fontFamily: 'Inter, Poppins', fontSize: 26,
          color: '#F5F5F5', fontWeight: 600,
          writingMode: 'vertical-rl', letterSpacing: 6,
        }}>
          पीढ़ी दर पीढ़ी
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(8,6,22,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3Trust;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4LifeSavings',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4LifeSavings: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const item1 = sp(10);
  const item2 = sp(35);
  const item3 = sp(60);
  const arrowFlow = sp(95);
  const vaultIn = sp(130, { damping: 10, stiffness: 90 });
  const lockClick = interpolate(frame, [180, 200], [0, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #080616 0%, #FF0000 40%, #FF2020 100%)',
      }} />

      {/* Shape layer - large triangle */}
      <div style={{
        position: 'absolute', top: '20%', right: '5%',
        width: 0, height: 0,
        borderLeft: '300px solid transparent',
        borderRight: '300px solid transparent',
        borderBottom: '500px solid #F5F5F5',
        opacity: 0.05,
        transform: `rotate(${frame * 0.2}deg)`,
        transformOrigin: 'center',
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', left: '5%',
        width: 600, height: 600, borderRadius: '50%',
        background: '#080616', opacity: 0.2,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
      }} />

      {/* Motion texture - diagonal lines */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.08 }}>
        {Array.from({ length: 25 }).map((_, i) => (
          <line key={i} x1={-200 + i * 100} y1={0} x2={i * 100} y2={1080}
            stroke="#F5F5F5" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles - coins falling */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 30);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: ((seededRandom(i + 90) * 1080 + frame * (1.5 + r)) % 1280) - 100,
            width: 8, height: 8, borderRadius: '50%',
            background: '#F5F5F5', opacity: 0.5,
            boxShadow: '0 0 8px #F5F5F5',
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute', top: 60, left: '50%',
        transform: `translateX(-50%)`, opacity: sp(3),
        fontFamily: 'Inter, Poppins', fontSize: 26, letterSpacing: 8,
        color: '#F5F5F5', fontWeight: 700,
        padding: '10px 30px', borderTop: '2px solid #F5F5F5',
        borderBottom: '2px solid #F5F5F5',
      }}>
        जिंदगी भर की मेहनत
      </div>

      {/* Three stacked items on left */}
      <div style={{
        position: 'absolute', left: 100, top: 200,
        display: 'flex', flexDirection: 'column', gap: 30,
      }}>
        {/* Item 1 - Life earnings */}
        <div style={{
          opacity: item1,
          transform: `translateX(${(1 - item1) * -300}px)`,
          width: 580, padding: '24px 32px',
          background: '#F5F5F5',
          borderLeft: '12px solid #080616',
          display: 'flex', alignItems: 'center', gap: 20,
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <circle cx="30" cy="30" r="26" fill="#080616" />
            <text x="30" y="42" fontSize="32" fontFamily="Yantra One" fill="#F5F5F5" textAnchor="middle" fontWeight="900">₹</text>
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 22, color: '#080616', opacity: 0.6, letterSpacing: 2 }}>01</div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 48, color: '#080616', fontWeight: 700, lineHeight: 1 }}>
              जिंदगी की कमाई
            </div>
          </div>
        </div>

        {/* Item 2 - Hard work fruit */}
        <div style={{
          opacity: item2,
          transform: `translateX(${(1 - item2) * -300}px)`,
          width: 580, padding: '24px 32px',
          background: '#080616',
          borderLeft: '12px solid #F5F5F5',
          display: 'flex', alignItems: 'center', gap: 20,
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <path d="M15 30 L25 40 L45 18" stroke="#F5F5F5" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            <circle cx="30" cy="30" r="26" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 22, color: '#F5F5F5', opacity: 0.6, letterSpacing: 2 }}>02</div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 48, color: '#F5F5F5', fontWeight: 700, lineHeight: 1 }}>
              मेहनत का फल
            </div>
          </div>
        </div>

        {/* Item 3 - Retirement money */}
        <div style={{
          opacity: item3,
          transform: `translateX(${(1 - item3) * -300}px)`,
          width: 580, padding: '24px 32px',
          background: '#F5F5F5',
          borderLeft: '12px solid #080616',
          display: 'flex', alignItems: 'center', gap: 20,
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <circle cx="30" cy="20" r="10" fill="#080616" />
            <path d="M12 50 Q30 32 48 50" fill="#080616" />
            <path d="M20 48 L40 48" stroke="#F5F5F5" strokeWidth="2" />
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 22, color: '#080616', opacity: 0.6, letterSpacing: 2 }}>03</div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 48, color: '#080616', fontWeight: 700, lineHeight: 1 }}>
              रिटायरमेंट फंड
            </div>
          </div>
        </div>
      </div>

      {/* Flow arrow in middle */}
      <div style={{
        position: 'absolute', left: 720, top: 480,
        opacity: arrowFlow,
        transform: `translateX(${(1 - arrowFlow) * -50}px)`,
      }}>
        <svg width="180" height="80" viewBox="0 0 180 80">
          <defs>
            <marker id="arr" markerWidth="12" markerHeight="12" refX="6" refY="6" orient="auto">
              <polygon points="0 0, 12 6, 0 12" fill="#F5F5F5" />
            </marker>
          </defs>
          <line x1="0" y1="40" x2={20 + (frame % 60)} y2="40" stroke="#F5F5F5" strokeWidth="8" strokeDasharray="20 10" markerEnd="url(#arr)" />
        </svg>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 22, color: '#F5F5F5',
          fontWeight: 700, marginTop: 4, letterSpacing: 2, textAlign: 'center',
        }}>
          सब कुछ →
        </div>
      </div>

      {/* VAULT on right */}
      <div style={{
        position: 'absolute', right: 120, top: 240,
        transform: `scale(${vaultIn})`,
        opacity: vaultIn,
      }}>
        <div style={{
          width: 480, height: 540,
          background: '#080616',
          border: '8px solid #F5F5F5',
          borderRadius: 16,
          position: 'relative',
          boxShadow: '0 20px 0 rgba(245,245,245,0.15)',
        }}>
          {/* Vault door circle */}
          <div style={{
            position: 'absolute', top: '50%', left: '50%',
            transform: `translate(-50%, -50%) rotate(${lockClick * 90}deg)`,
            width: 320, height: 320, borderRadius: '50%',
            background: '#F5F5F5',
            border: '12px solid #080616',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
          }}>
            {/* Lock spokes */}
            {Array.from({ length: 8 }).map((_, i) => (
              <div key={i} style={{
                position: 'absolute', width: 8, height: 60,
                background: '#080616',
                transform: `rotate(${i * 45}deg) translateY(-110px)`,
                transformOrigin: 'center 110px',
                borderRadius: 4,
              }} />
            ))}
            <div style={{
              width: 80, height: 80, borderRadius: '50%',
              background: '#FF0000', border: '6px solid #080616',
            }} />
          </div>

          {/* Top label */}
          <div style={{
            position: 'absolute', top: 20, left: 0, right: 0,
            textAlign: 'center',
            fontFamily: 'Inter, Poppins', fontSize: 20, letterSpacing: 6,
            color: '#F5F5F5', fontWeight: 700,
          }}>
            BANK VAULT
          </div>
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 64, color: '#F5F5F5',
          fontWeight: 900, textAlign: 'center', marginTop: 18,
          textShadow: '0 6px 0 #080616',
        }}>
          तिजोरी
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(8,6,22,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4LifeSavings;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5Illusion',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5Illusion: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake at impact
  const shake = frame > 80 && frame < 100 ? Math.sin(frame * 2) * 6 : 0;
  const crackIn = sp(75, { damping: 8 });
  const wordIn = sp(110);
  const subTextIn = sp(160);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp, transform: `translate(${shake}px, ${shake * 0.5}px)` }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: `conic-gradient(from ${frame * 0.5}deg at 50% 50%, #080616, #FF0000, #FF3030, #080616, #FF0000)`,
        opacity: 0.85,
      }} />
      <AbsoluteFill style={{ background: 'rgba(255,0,0,0.4)' }} />

      {/* Shape layer */}
      <div style={{
        position: 'absolute', top: '20%', left: '40%',
        width: 700, height: 700, borderRadius: '50%',
        border: '4px solid #F5F5F5', opacity: 0.08,
        transform: `scale(${1 + Math.sin(frame * 0.06) * 0.08})`,
      }} />
      <div style={{
        position: 'absolute', top: '10%', left: '10%',
        width: 400, height: 400,
        background: '#080616', opacity: 0.2,
        clipPath: 'polygon(50% 0%, 100% 38%, 82% 100%, 18% 100%, 0% 38%)',
        transform: `rotate(${frame * 0.3}deg)`,
      }} />

      {/* Motion texture */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.12 }}>
        {Array.from({ length: 6 }).map((_, i) => (
          <circle key={i} cx="960" cy="540" r={150 + i * 100} fill="none"
            stroke="#F5F5F5" strokeWidth="2" strokeDasharray="4 16" />
        ))}
      </svg>

      {/* Particles - exploding outward */}
      {Array.from({ length: 30 }).map((_, i) => {
        const angle = (i / 30) * Math.PI * 2;
        const dist = (frame * 3) % 800;
        return (
          <div key={i} style={{
            position: 'absolute', left: 960 + Math.cos(angle) * dist,
            top: 540 + Math.sin(angle) * dist,
            width: 4, height: 4, borderRadius: '50%',
            background: '#F5F5F5', opacity: Math.max(0, 1 - dist / 800),
          }} />
        );
      })}

      {/* Top label - WARNING strip */}
      <div style={{
        position: 'absolute', top: 80, left: 0, right: 0,
        opacity: sp(5),
        background: '#080616',
        padding: '14px 0',
        display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 30,
      }}>
        <div style={{ fontFamily: 'Inter, Poppins', fontSize: 28, color: '#F5F5F5', fontWeight: 800, letterSpacing: 6 }}>
          ◆ SYSTEM ALERT ◆ SYSTEM ALERT ◆ SYSTEM ALERT ◆
        </div>
      </div>

      {/* "BUT WHAT IF..." label */}
      <div style={{
        position: 'absolute', top: 200, left: '50%',
        transform: `translateX(-50%)`, opacity: sp(20),
        fontFamily: 'Yantra One', fontSize: 64,
        color: '#F5F5F5', fontWeight: 700,
        textShadow: '0 6px 0 #080616',
      }}>
        लेकिन अगर ये एक...
      </div>

      {/* CRACKED GLASS effect with the word BHRAM */}
      <div style={{
        position: 'absolute', top: 330, left: '50%',
        transform: `translateX(-50%) scale(${crackIn})`,
        opacity: crackIn,
      }}>
        {/* Crack lines SVG */}
        <svg width="1200" height="380" viewBox="0 0 1200 380" style={{ position: 'absolute', top: 0, left: 0 }}>
          <g stroke="#080616" strokeWidth="4" fill="none" opacity={frame > 80 ? 0.7 : 0}>
            <line x1="100" y1="190" x2="400" y2="120" />
            <line x1="400" y1="120" x2="350" y2="250" />
            <line x1="350" y1="250" x2="600" y2="200" />
            <line x1="600" y1="200" x2="800" y2="80" />
            <line x1="800" y1="80" x2="900" y2="280" />
            <line x1="900" y1="280" x2="1100" y2="180" />
            <line x1="600" y1="200" x2="500" y2="350" />
            <line x1="600" y1="200" x2="700" y2="350" />
            <line x1="400" y1="120" x2="200" y2="50" />
          </g>
        </svg>

        {/* The word */}
        <div style={{
          fontFamily: 'Yantra One', fontSize: 280,
          color: '#F5F5F5', fontWeight: 900, lineHeight: 1,
          textShadow: '0 16px 0 #080616, 0 0 80px rgba(8,6,22,0.6)',
          letterSpacing: -4,
          padding: '20px 60px',
        }}>
          भ्रम!
        </div>
      </div>

      {/* Bottom explanation banner */}
      <div style={{
        position: 'absolute', bottom: 140, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - subTextIn) * 80}px)`,
        opacity: subTextIn,
        background: '#F5F5F5',
        padding: '28px 50px',
        borderRadius: 4,
        boxShadow: '0 14px 0 #080616',
        display: 'flex', alignItems: 'center', gap: 24,
        maxWidth: 1300,
      }}>
        <svg width="56" height="56" viewBox="0 0 56 56">
          <path d="M28 6 L52 48 L4 48 Z" fill="#FF0000" stroke="#080616" strokeWidth="4" />
          <rect x="25" y="20" width="6" height="14" fill="#080616" />
          <circle cx="28" cy="40" r="3" fill="#080616" />
        </svg>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 48,
          color: '#080616', fontWeight: 700, lineHeight: 1.15,
        }}>
          पूरी बैंकिंग व्यवस्था इसी एक भ्रम पर टिकी है
        </div>
      </div>

      {/* Side accent - SYSTEM LIE */}
      <div style={{
        position: 'absolute', top: 260, left: 60,
        opacity: sp(40),
        transform: `rotate(-90deg)`,
        transformOrigin: 'left top',
        fontFamily: 'Inter, Poppins', fontSize: 28,
        color: '#F5F5F5', fontWeight: 800, letterSpacing: 10,
      }}>
        SYSTEM'S BIGGEST LIE
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 30%, rgba(8,6,22,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5Illusion;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6CommonPeople',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6CommonPeople: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pyramid build
  const layer1 = sp(15);
  const layer2 = sp(40);
  const layer3 = sp(65);
  const arrowDrop = sp(110);
  const impactPulse = 1 + (frame > 140 ? Math.sin(frame * 0.4) * 0.05 : 0);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #080616 0%, #FF0000 70%, #FF2020 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '5%', right: '5%',
        width: 500, height: 500, border: '3px solid #F5F5F5', opacity: 0.06,
        transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', left: '5%',
        width: 600, height: 400, background: '#080616', opacity: 0.25,
        transform: `skew(-15deg) translateY(${Math.sin(frame * 0.02) * 15}px)`,
      }} />

      {/* Motion texture - horizontal scanning lines */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.06 }}>
        {Array.from({ length: 40 }).map((_, i) => (
          <line key={i} x1={0} y1={i * 28} x2={1920} y2={i * 28} stroke="#F5F5F5" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles falling down */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 11);
        return (
          <div key={i} style={{
            position: 'absolute', left: r * 1920,
            top: ((seededRandom(i + 88) * 1080 + frame * 1.2) % 1080),
            width: 3, height: 12, background: '#F5F5F5', opacity: 0.4,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 70, left: '50%',
        transform: `translateX(-50%)`, opacity: sp(3),
        fontFamily: 'Yantra One', fontSize: 58,
        color: '#F5F5F5', fontWeight: 700,
        background: '#080616', padding: '12px 40px',
      }}>
        नुकसान का पिरामिड
      </div>

      {/* Pyramid hierarchy - LEFT side */}
      <div style={{
        position: 'absolute', left: 140, top: 200,
        display: 'flex', flexDirection: 'column', gap: 12,
      }}>
        {/* Top - Big institutions (small width) */}
        <div style={{
          opacity: layer1,
          transform: `translateY(${(1 - layer1) * -50}px)`,
          width: 320, height: 100,
          background: '#F5F5F5',
          margin: '0 auto',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          gap: 16, borderRadius: 6,
        }}>
          <svg width="40" height="40" viewBox="0 0 40 40">
            <rect x="5" y="15" width="30" height="22" fill="#080616" />
            <polygon points="20,5 5,15 35,15" fill="#080616" />
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 14, color: '#080616', opacity: 0.6, letterSpacing: 2 }}>TIER 1</div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 26, color: '#080616', fontWeight: 700, lineHeight: 1 }}>संस्थान</div>
          </div>
        </div>

        {/* Middle - Rich/Insiders */}
        <div style={{
          opacity: layer2,
          transform: `translateY(${(1 - layer2) * -50}px)`,
          width: 480, height: 120,
          background: '#9E9E9E',
          margin: '0 auto',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          gap: 20, borderRadius: 6,
        }}>
          <svg width="50" height="50" viewBox="0 0 50 50">
            <circle cx="25" cy="18" r="9" fill="#080616" />
            <path d="M8 45 Q25 28 42 45" fill="#080616" />
            <text x="25" y="48" fontSize="10" fill="#F5F5F5" textAnchor="middle" fontFamily="Inter">VIP</text>
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 16, color: '#080616', opacity: 0.7, letterSpacing: 2 }}>TIER 2</div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 32, color: '#080616', fontWeight: 700, lineHeight: 1 }}>अंदरूनी लोग</div>
          </div>
        </div>

        {/* Bottom - Common people (WIDE) */}
        <div style={{
          opacity: layer3,
          transform: `translateY(${(1 - layer3) * -50}px) scale(${impactPulse})`,
          width: 720, height: 180,
          background: '#080616',
          border: '5px solid #F5F5F5',
          margin: '0 auto',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          gap: 24, borderRadius: 6,
          boxShadow: '0 10px 0 rgba(245,245,245,0.2)',
        }}>
          {/* Multiple people SVG */}
          <svg width="100" height="80" viewBox="0 0 100 80">
            <circle cx="20" cy="22" r="10" fill="#F5F5F5" />
            <path d="M5 60 Q20 38 35 60" fill="#F5F5F5" />
            <circle cx="50" cy="22" r="10" fill="#F5F5F5" />
            <path d="M35 60 Q50 38 65 60" fill="#F5F5F5" />
            <circle cx="80" cy="22" r="10" fill="#F5F5F5" />
            <path d="M65 60 Q80 38 95 60" fill="#F5F5F5" />
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 20, color: '#FF0000', fontWeight: 800, letterSpacing: 3 }}>TIER 3 — आप & मैं</div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 52, color: '#F5F5F5', fontWeight: 800, lineHeight: 1 }}>आम जमाकर्ता</div>
          </div>
        </div>
      </div>

      {/* RIGHT side - Damage indicator */}
      <div style={{
        position: 'absolute', right: 80, top: 200,
        opacity: sp(85),
        display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 20,
        width: 600,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 24, letterSpacing: 6,
          color: '#F5F5F5', fontWeight: 700,
        }}>
          IMPACT SCALE
        </div>

        {/* Damage bars */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 18, width: '100%' }}>
          {/* Institutions - tiny */}
          <div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 20, color: '#F5F5F5', marginBottom: 6, fontWeight: 600 }}>संस्थान</div>
            <div style={{ width: '100%', height: 30, background: 'rgba(245,245,245,0.15)', borderRadius: 4 }}>
              <div style={{ width: `${10 * layer1}%`, height: '100%', background: '#F5F5F5', borderRadius: 4 }} />
            </div>
          </div>
          {/* Insiders - medium */}
          <div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 20, color: '#F5F5F5', marginBottom: 6, fontWeight: 600 }}>अंदरूनी</div>
            <div style={{ width: '100%', height: 30, background: 'rgba(245,245,245,0.15)', borderRadius: 4 }}>
              <div style={{ width: `${30 * layer2}%`, height: '100%', background: '#9E9E9E', borderRadius: 4 }} />
            </div>
          </div>
          {/* Common - MAX */}
          <div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 24, color: '#F5F5F5', marginBottom: 6, fontWeight: 800 }}>आम जनता</div>
            <div style={{ width: '100%', height: 50, background: 'rgba(245,245,245,0.15)', borderRadius: 4, border: '2px solid #F5F5F5' }}>
              <div style={{
                width: `${100 * layer3}%`, height: '100%',
                background: '#080616',
                borderRadius: 4,
                display: 'flex', alignItems: 'center', justifyContent: 'flex-end',
                paddingRight: 16,
              }}>
                <span style={{ fontFamily: 'JetBrains Mono', color: '#F5F5F5', fontSize: 24, fontWeight: 800 }}>
                  100%
                </span>
              </div>
            </div>
          </div>
        </div>

        {/* Down arrow */}
        <div style={{
          opacity: arrowDrop,
          transform: `translateY(${(1 - arrowDrop) * -40}px)`,
          marginTop: 10,
        }}>
          <svg width="100" height="80" viewBox="0 0 100 80">
            <path d="M50 0 L50 60 M25 40 L50 65 L75 40" stroke="#F5F5F5" strokeWidth="8" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>
      </div>

      {/* Bottom takeaway */}
      <div style={{
        position: 'absolute', bottom: 60, left: '50%',
        transform: `translateX(-50%)`, opacity: sp(150),
        fontFamily: 'Yantra One', fontSize: 44,
        color: '#080616', background: '#F5F5F5',
        padding: '14px 40px', fontWeight: 700,
      }}>
        सबसे पहले, सबसे ज्यादा नुकसान — आम आदमी को
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 38%, rgba(8,6,22,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6CommonPeople;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7DiveDeep',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7DiveDeep: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Diving camera effect - zoom into deep
  const diveZoom = interpolate(frame, [0, 150], [1, 1.4], { extrapolateRight: 'clamp', easing: Easing.in(Easing.cubic) });
  const titleIn = sp(10);
  const statementIn = sp(60);
  const eyeIn = sp(140, { damping: 8 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Tunnel gradient - depth */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, #FF0000 0%, #FF0000 20%, #080616 80%, #080616 100%)',
      }} />

      {/* Diving rings - perspective tunnel */}
      {Array.from({ length: 8 }).map((_, i) => {
        const ringFrame = (frame + i * 25) % 200;
        const scale = ringFrame / 200;
        return (
          <div key={i} style={{
            position: 'absolute', top: '50%', left: '50%',
            width: 1800 * scale, height: 1800 * scale,
            transform: `translate(-50%, -50%)`,
            border: '3px solid #F5F5F5',
            borderRadius: '50%',
            opacity: (1 - scale) * 0.5,
          }} />
        );
      })}

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '20%', left: '10%',
        width: 300, height: 300,
        background: '#F5F5F5', opacity: 0.04,
        clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
        transform: `rotate(${frame * 0.5}deg)`,
      }} />

      {/* Motion texture - speed lines toward center */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.12 }}>
        {Array.from({ length: 24 }).map((_, i) => {
          const angle = (i / 24) * Math.PI * 2;
          const startR = 100;
          const endR = 900;
          return (
            <line key={i}
              x1={960 + Math.cos(angle) * startR} y1={540 + Math.sin(angle) * startR}
              x2={960 + Math.cos(angle) * endR} y2={540 + Math.sin(angle) * endR}
              stroke="#F5F5F5" strokeWidth="2" strokeDasharray="20 30" strokeDashoffset={-frame * 3} />
          );
        })}
      </svg>

      {/* Particles streaming inward */}
      {Array.from({ length: 35 }).map((_, i) => {
        const r = seededRandom(i + 5);
        const angle = r * Math.PI * 2;
        const dist = 900 - ((frame * 4 + i * 30) % 900);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: 960 + Math.cos(angle) * dist,
            top: 540 + Math.sin(angle) * dist,
            width: 4, height: 4, borderRadius: '50%',
            background: '#F5F5F5', opacity: (1 - dist / 900) * 0.8,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${diveZoom})` }}>
        {/* Header */}
        <div style={{
          position: 'absolute', top: 80, left: '50%',
          transform: `translateX(-50%)`, opacity: titleIn,
          display: 'flex', alignItems: 'center', gap: 20,
        }}>
          <div style={{ width: 80, height: 3, background: '#F5F5F5' }} />
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 26, letterSpacing: 10,
            color: '#F5F5F5', fontWeight: 700,
          }}>
            कहानी की गहराई में
          </div>
          <div style={{ width: 80, height: 3, background: '#F5F5F5' }} />
        </div>

        {/* Main headline */}
        <div style={{
          position: 'absolute', top: 170, left: '50%',
          transform: `translateX(-50%) scale(${titleIn})`,
          opacity: titleIn, textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 120,
            color: '#F5F5F5', fontWeight: 900, lineHeight: 1,
            textShadow: '0 10px 0 #080616',
          }}>
            उतरते हैं भीतर
          </div>
        </div>

        {/* Bank statement mockup in middle */}
        <div style={{
          position: 'absolute', top: 370, left: '50%',
          transform: `translateX(-50%) scale(${statementIn}) rotate(${-3 + (1 - statementIn) * 10}deg)`,
          opacity: statementIn,
          width: 880, height: 380,
          background: '#F5F5F5',
          padding: 36,
          boxShadow: '0 25px 60px rgba(8,6,22,0.6)',
          borderRadius: 8,
        }}>
          {/* Statement header */}
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderBottom: '3px solid #080616', paddingBottom: 12 }}>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 22, color: '#080616', fontWeight: 800, letterSpacing: 3 }}>
              BANK STATEMENT
            </div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 18, color: '#080616', opacity: 0.6 }}>
              MAY 2026
            </div>
          </div>

          {/* Transaction rows */}
          {[
            { label: 'OPENING BALANCE', val: '₹ 8,45,000', col: '#080616' },
            { label: 'CREDIT — SALARY', val: '+ ₹ 1,20,000', col: '#080616' },
            { label: 'DEBIT — EMI', val: '- ₹ 35,000', col: '#FF0000' },
            { label: 'CLOSING BALANCE', val: '₹ 9,30,000', col: '#080616' },
          ].map((row, i) => (
            <div key={i} style={{
              display: 'flex', justifyContent: 'space-between',
              padding: '14px 0', borderBottom: '1px solid rgba(8,6,22,0.15)',
              opacity: sp(80 + i * 8),
            }}>
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 22, color: '#080616', fontWeight: 600 }}>{row.label}</div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: row.col, fontWeight: 800 }}>{row.val}</div>
            </div>
          ))}
        </div>

        {/* Magnifying eye over statement */}
        <div style={{
          position: 'absolute', top: 430, right: 220,
          transform: `scale(${eyeIn})`, opacity: eyeIn,
        }}>
          <svg width="220" height="220" viewBox="0 0 220 220">
            <circle cx="110" cy="110" r="100" fill="#080616" stroke="#F5F5F5" strokeWidth="8" />
            <ellipse cx="110" cy="110" rx="70" ry="45" fill="#F5F5F5" />
            <circle cx="110" cy="110" r="32" fill="#FF0000" />
            <circle cx="110" cy="110" r="18" fill="#080616" />
            <circle cx="118" cy="102" r="6" fill="#F5F5F5" />
          </svg>
        </div>

        {/* Bottom warning */}
        <div style={{
          position: 'absolute', bottom: 90, left: '50%',
          transform: `translateX(-50%)`, opacity: sp(170),
          background: '#080616', color: '#F5F5F5',
          padding: '20px 50px', borderRadius: 4,
          fontFamily: 'Yantra One', fontSize: 42,
          fontWeight: 700, letterSpacing: 1,
          border: '3px solid #F5F5F5',
        }}>
          अब ये नंबर वही नहीं रहेंगे...
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 30%, rgba(8,6,22,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7DiveDeep;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8LegalTransfer',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8LegalTransfer: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Money transfer from person to bank
  const personIn = sp(15);
  const bankIn = sp(15);
  const moneySlide = interpolate(frame, [50, 110], [0, 1], { extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const stampIn = sp(140, { damping: 7, stiffness: 80 });
  const verdictIn = sp(180);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(110deg, #080616 0%, #FF0000 35%, #FF0000 65%, #080616 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '15%', left: '50%',
        transform: `translateX(-50%) rotate(${frame * 0.3}deg)`,
        width: 800, height: 800, borderRadius: '50%',
        border: '2px solid #F5F5F5', opacity: 0.06,
      }} />
      <div style={{
        position: 'absolute', bottom: 0, left: 0,
        width: 600, height: 400,
        background: '#080616', opacity: 0.25,
        clipPath: 'polygon(0 100%, 100% 100%, 80% 0)',
      }} />
      <div style={{
        position: 'absolute', bottom: 0, right: 0,
        width: 600, height: 400,
        background: '#080616', opacity: 0.25,
        clipPath: 'polygon(0 100%, 100% 100%, 20% 0)',
      }} />

      {/* Motion texture */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.08 }}>
        {Array.from({ length: 18 }).map((_, i) => (
          <rect key={i} x={i * 110 + 20} y={400 + Math.sin(frame * 0.04 + i) * 30}
            width="6" height="20" fill="#F5F5F5" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 25);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: ((r * 1920 + frame * 0.8) % 1920),
            top: r * 1080,
            width: 3, height: 3, borderRadius: '50%',
            background: '#F5F5F5', opacity: 0.4,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 70, left: '50%',
        transform: `translateX(-50%)`, opacity: sp(3),
        fontFamily: 'Inter, Poppins', fontSize: 26, letterSpacing: 8,
        color: '#F5F5F5', fontWeight: 700,
        padding: '12px 36px', border: '2px solid #F5F5F5',
      }}>
        बुनियादी सच — कानूनी हकीकत
      </div>

      <div style={{
        position: 'absolute', top: 135, left: '50%',
        transform: `translateX(-50%)`,
        fontFamily: 'Yantra One', fontSize: 64,
        color: '#F5F5F5', fontWeight: 700,
        opacity: sp(15),
      }}>
        जिस पल पैसा जमा हुआ...
      </div>

      {/* Main transfer scene */}
      <div style={{
        position: 'absolute', top: 280, left: 0, right: 0,
        display: 'flex', justifyContent: 'space-between',
        alignItems: 'center', padding: '0 120px',
      }}>
        {/* LEFT - YOU */}
        <div style={{
          opacity: personIn, transform: `scale(${personIn})`,
          display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 16,
        }}>
          <div style={{
            width: 280, height: 280,
            background: '#F5F5F5',
            borderRadius: '50%',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            border: '6px solid #080616',
          }}>
            <svg width="160" height="180" viewBox="0 0 160 180">
              <circle cx="80" cy="55" r="40" fill="#080616" />
              <path d="M20 175 Q80 90 140 175" fill="#080616" />
            </svg>
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 64,
            color: '#F5F5F5', fontWeight: 800,
            textShadow: '0 6px 0 #080616',
          }}>
            आप
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 20, color: '#F5F5F5',
            letterSpacing: 4, opacity: 0.8,
          }}>
            DEPOSITOR
          </div>
        </div>

        {/* Middle - Flowing money */}
        <div style={{ position: 'relative', flex: 1, height: 200, margin: '0 40px' }}>
          {/* Track line */}
          <div style={{
            position: 'absolute', top: '50%', left: 0, right: 0,
            height: 4, background: 'rgba(245,245,245,0.3)',
            transform: 'translateY(-50%)',
          }} />

          {/* Money packet sliding */}
          <div style={{
            position: 'absolute', top: '50%',
            left: `${moneySlide * 100}%`,
            transform: `translate(-50%, -50%) rotate(${moneySlide * 360}deg) scale(${interpolate(moneySlide, [0, 0.5, 1], [1, 1.3, 0.7])})`,
          }}>
            <div style={{
              width: 140, height: 90,
              background: '#F5F5F5',
              border: '4px solid #080616',
              borderRadius: 6,
              display: 'flex', alignItems: 'center', justifyContent: 'center',
            }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 32, color: '#080616', fontWeight: 900 }}>
                ₹₹₹
              </div>
            </div>
          </div>

          {/* Arrow indicator */}
          <div style={{
            position: 'absolute', top: 10, left: '50%', transform: 'translateX(-50%)',
            fontFamily: 'Inter, Poppins', fontSize: 22, color: '#F5F5F5',
            fontWeight: 700, letterSpacing: 3, opacity: sp(40),
          }}>
            → DEPOSIT →
          </div>
          <div style={{
            position: 'absolute', bottom: 10, left: '50%', transform: 'translateX(-50%)',
            fontFamily: 'Inter, Poppins', fontSize: 18, color: '#F5F5F5',
            opacity: 0.7, letterSpacing: 2,
          }}>
            ownership transfer
          </div>
        </div>

        {/* RIGHT - BANK */}
        <div style={{
          opacity: bankIn, transform: `scale(${bankIn})`,
          display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 16,
        }}>
          <div style={{
            width: 280, height: 280,
            background: '#080616',
            borderRadius: 12,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            border: '6px solid #F5F5F5',
          }}>
            <svg width="180" height="160" viewBox="0 0 180 160">
              <polygon points="90,10 10,55 170,55" fill="#F5F5F5" />
              <rect x="10" y="55" width="160" height="10" fill="#F5F5F5" />
              <rect x="20" y="70" width="14" height="70" fill="#F5F5F5" />
              <rect x="50" y="70" width="14" height="70" fill="#F5F5F5" />
              <rect x="80" y="70" width="14" height="70" fill="#F5F5F5" />
              <rect x="110" y="70" width="14" height="70" fill="#F5F5F5" />
              <rect x="140" y="70" width="14" height="70" fill="#F5F5F5" />
              <rect x="5" y="140" width="170" height="14" fill="#F5F5F5" />
            </svg>
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 64,
            color: '#F5F5F5', fontWeight: 800,
            textShadow: '0 6px 0 #080616',
          }}>
            बैंक
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 20, color: '#F5F5F5',
            letterSpacing: 4, opacity: 0.8,
          }}>
            NEW OWNER
          </div>
        </div>
      </div>

      {/* LEGAL stamp */}
      <div style={{
        position: 'absolute', top: 380, left: '50%',
        transform: `translateX(-50%) rotate(${-12 + (1 - stampIn) * 40}deg) scale(${stampIn})`,
        opacity: stampIn * 0.95,
        padding: '14px 36px',
        border: '6px double #080616',
        background: 'rgba(245,245,245,0.95)',
        borderRadius: 8,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 32,
          color: '#080616', fontWeight: 900, letterSpacing: 4,
        }}>
          LEGALLY TRANSFERRED
        </div>
      </div>

      {/* VERDICT bottom */}
      <div style={{
        position: 'absolute', bottom: 80, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - verdictIn) * 60}px)`,
        opacity: verdictIn,
        background: '#080616',
        padding: '24px 50px',
        border: '4px solid #F5F5F5',
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 22, color: '#FF0000',
          fontWeight: 800, letterSpacing: 6, marginBottom: 6,
        }}>
          ── कानूनी सच ──
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 56, color: '#F5F5F5',
          fontWeight: 800, lineHeight: 1,
        }}>
          वो पैसा अब आपका नहीं रहा
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 38%, rgba(8,6,22,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8LegalTransfer;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9IOU',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9IOU: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const moneyToAsset = sp(15);
  const arrowIn = sp(55);
  const iouIn = sp(90, { damping: 9, stiffness: 90 });
  const claimsIn1 = sp(140);
  const claimsIn2 = sp(160);
  const claimsIn3 = sp(180);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 50%, #FF2020 0%, #FF0000 40%, #080616 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '10%', right: '15%',
        width: 500, height: 500,
        background: '#F5F5F5', opacity: 0.05,
        clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
        transform: `rotate(${frame * 0.4}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', left: '10%',
        width: 450, height: 450,
        border: '3px solid #F5F5F5', opacity: 0.08,
        transform: `rotate(${-frame * 0.3}deg)`,
      }} />

      {/* Motion texture - paper grain dots */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.06 }}>
        {Array.from({ length: 50 }).map((_, i) => (
          <circle key={i} cx={seededRandom(i + 1) * 1920} cy={seededRandom(i + 50) * 1080}
            r={1.5} fill="#F5F5F5" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 77);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: ((r * 1920 + frame * 0.4) % 1920),
            top: ((seededRandom(i + 220) * 1080 - frame * 0.5) % 1080 + 1080) % 1080,
            width: 5, height: 5,
            background: '#F5F5F5', opacity: 0.4,
            transform: `rotate(45deg)`,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 60, left: '50%',
        transform: `translateX(-50%)`, opacity: sp(3),
        fontFamily: 'Yantra One', fontSize: 58,
        color: '#F5F5F5', fontWeight: 700,
        background: '#080616', padding: '12px 40px',
      }}>
        अब वो बैंक की संपत्ति है
      </div>

      {/* Transformation visualization */}
      <div style={{
        position: 'absolute', top: 200, left: 100,
        width: 480, opacity: moneyToAsset,
        transform: `translateX(${(1 - moneyToAsset) * -50}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 22, color: '#F5F5F5',
          letterSpacing: 4, marginBottom: 10, opacity: 0.7,
        }}>
          आपका पैसा →
        </div>
        <div style={{
          background: '#080616',
          border: '4px solid #F5F5F5',
          borderRadius: 10,
          padding: 30,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 18, color: '#F5F5F5',
            letterSpacing: 3, opacity: 0.5,
          }}>
            BANK BALANCE SHEET
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 26, color: '#F5F5F5',
            fontWeight: 700, marginTop: 8, marginBottom: 14,
          }}>
            ASSET SIDE
          </div>
          <div style={{
            display: 'flex', alignItems: 'center', gap: 16,
            padding: '14px 18px', background: '#F5F5F5', borderRadius: 6,
          }}>
            <svg width="40" height="40" viewBox="0 0 40 40">
              <rect x="6" y="12" width="28" height="20" fill="#080616" rx="2" />
              <circle cx="20" cy="22" r="6" fill="#F5F5F5" />
              <text x="20" y="26" fontSize="9" fill="#080616" textAnchor="middle" fontWeight="bold">₹</text>
            </svg>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 36, color: '#080616',
              fontWeight: 800,
            }}>
              ₹ 10,00,000
            </div>
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 16, color: '#F5F5F5',
            marginTop: 12, opacity: 0.6, fontStyle: 'italic',
          }}>
            BANK की संपत्ति बन गई
          </div>
        </div>
      </div>

      {/* Big arrow in middle */}
      <div style={{
        position: 'absolute', top: 340, left: '50%',
        transform: `translateX(-50%) scale(${arrowIn})`,
        opacity: arrowIn,
      }}>
        <svg width="200" height="100" viewBox="0 0 200 100">
          <defs>
            <marker id="bigArr" markerWidth="14" markerHeight="14" refX="7" refY="7" orient="auto">
              <polygon points="0 0, 14 7, 0 14" fill="#F5F5F5" />
            </marker>
          </defs>
          <line x1="20" y1="50" x2="170" y2="50" stroke="#F5F5F5" strokeWidth="10"
            strokeDasharray="20 12" strokeDashoffset={-frame * 2} markerEnd="url(#bigArr)" />
        </svg>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 20, color: '#F5F5F5',
          fontWeight: 700, textAlign: 'center', letterSpacing: 3, marginTop: 4,
        }}>
          आपको मिलती है...
        </div>
      </div>

      {/* IOU receipt card */}
      <div style={{
        position: 'absolute', top: 200, right: 80,
        width: 540, transform: `scale(${iouIn}) rotate(${4 - (1 - iouIn) * 15}deg)`,
        opacity: iouIn,
      }}>
        <div style={{
          background: '#F5F5F5',
          border: '4px dashed #080616',
          padding: 32,
          boxShadow: '0 20px 50px rgba(8,6,22,0.5)',
          borderRadius: 4,
        }}>
          {/* Header */}
          <div style={{
            borderBottom: '3px solid #080616', paddingBottom: 12,
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
          }}>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 32, color: '#080616',
              fontWeight: 900, letterSpacing: 4,
            }}>
              I.O.U.
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 14, color: '#080616',
              opacity: 0.6, letterSpacing: 2,
            }}>
              I OWE YOU
            </div>
          </div>

          <div style={{
            fontFamily: 'Yantra One', fontSize: 42, color: '#080616',
            fontWeight: 700, marginTop: 16, lineHeight: 1.1,
          }}>
            एक रसीद
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 22, color: '#080616',
            marginTop: 8, opacity: 0.75,
          }}>
            एक वादा, एक दावा — कि बैंक मांगने पर वापस देगा
          </div>

          {/* Stamp */}
          <div style={{
            marginTop: 20, padding: '10px 20px',
            background: '#FF0000', color: '#F5F5F5',
            display: 'inline-block', transform: 'rotate(-3deg)',
            fontFamily: 'Inter, Poppins', fontSize: 18,
            fontWeight: 800, letterSpacing: 3,
            border: '3px solid #080616',
          }}>
            PROMISE ONLY
          </div>

          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 14, color: '#080616',
            opacity: 0.6, marginTop: 16, borderTop: '1px solid rgba(8,6,22,0.2)', paddingTop: 8,
          }}>
            Signed: BANK | Holder: YOU
          </div>
        </div>
      </div>

      {/* Bottom 3 claim chips */}
      <div style={{
        position: 'absolute', bottom: 80, left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex', gap: 24,
      }}>
        {[
          { o: claimsIn1, label: 'एक रसीद', sub: 'RECEIPT' },
          { o: claimsIn2, label: 'एक IOU', sub: 'PROMISE' },
          { o: claimsIn3, label: 'एक दावा', sub: 'CLAIM ONLY' },
        ].map((c, i) => (
          <div key={i} style={{
            opacity: c.o, transform: `translateY(${(1 - c.o) * 40}px)`,
            background: '#080616', border: '3px solid #F5F5F5',
            padding: '20px 38px', borderRadius: 6,
            display: 'flex', flexDirection: 'column', alignItems: 'center',
          }}>
            <div style={{ fontFamily: 'Yantra One', fontSize: 42, color: '#F5F5F5', fontWeight: 700 }}>
              {c.label}
            </div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 16, color: '#F5F5F5', opacity: 0.7, letterSpacing: 3 }}>
              {c.sub}
            </div>
          </div>
        ))}
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(8,6,22,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9IOU;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10UnsecuredCreditor',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10UnsecuredCreditor: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const labelIn = sp(15, { damping: 8 });
  const tagIn = sp(60);
  const noGuaranteeIn = sp(120, { damping: 7, stiffness: 80 });
  const finalIn = sp(200);
  const warningPulse = 1 + Math.sin(frame * 0.2) * 0.03;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, #FF0000 0%, #080616 60%, #FF0000 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '5%', left: '5%',
        width: 700, height: 700,
        background: '#080616', opacity: 0.2,
        clipPath: 'polygon(0 0, 100% 0, 60% 100%, 0 100%)',
      }} />
      <div style={{
        position: 'absolute', top: '50%', right: '5%',
        width: 500, height: 500, borderRadius: '50%',
        border: '3px solid #F5F5F5', opacity: 0.06,
        transform: `translateY(-50%) scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
      }} />

      {/* Motion texture - X pattern */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.07 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <g key={i}>
            <line x1={i * 100} y1={0} x2={i * 100 + 200} y2={1080} stroke="#F5F5F5" strokeWidth="1" />
            <line x1={i * 100 + 200} y1={0} x2={i * 100} y2={1080} stroke="#F5F5F5" strokeWidth="1" />
          </g>
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const r = seededRandom(i + 33);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: ((seededRandom(i + 444) * 1080 + frame * 0.8) % 1080),
            width: 4, height: 4, borderRadius: '50%',
            background: '#F5F5F5', opacity: 0.45,
          }} />
        );
      })}

      {/* Top - Status label */}
      <div style={{
        position: 'absolute', top: 60, left: '50%',
        transform: `translateX(-50%)`, opacity: sp(3),
        display: 'flex', alignItems: 'center', gap: 14,
      }}>
        <div style={{ width: 60, height: 3, background: '#F5F5F5' }} />
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 24, letterSpacing: 8,
          color: '#F5F5F5', fontWeight: 700,
        }}>
          आपकी असली पहचान
        </div>
        <div style={{ width: 60, height: 3, background: '#F5F5F5' }} />
      </div>

      {/* MAIN ID CARD - Center stage */}
      <div style={{
        position: 'absolute', top: 160, left: '50%',
        transform: `translateX(-50%) scale(${labelIn})`,
        opacity: labelIn,
        width: 1100,
        background: '#F5F5F5',
        border: '6px solid #080616',
        borderRadius: 12,
        padding: 40,
        boxShadow: '0 25px 60px rgba(8,6,22,0.6)',
      }}>
        {/* Card header */}
        <div style={{
          display: 'flex', justifyContent: 'space-between',
          borderBottom: '3px solid #080616', paddingBottom: 16,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 22, color: '#080616',
            fontWeight: 800, letterSpacing: 4,
          }}>
            DEPOSITOR STATUS CARD
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 18, color: '#FF0000',
            fontWeight: 700,
          }}>
            ID #UNSECURED
          </div>
        </div>

        {/* Big title - English */}
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 108, color: '#080616',
          fontWeight: 900, lineHeight: 1, marginTop: 24,
          letterSpacing: -2,
        }}>
          UNSECURED
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 108, color: '#FF0000',
          fontWeight: 900, lineHeight: 1,
          letterSpacing: -2,
        }}>
          CREDITOR
        </div>

        {/* Hindi translation */}
        <div style={{
          fontFamily: 'Yantra One', fontSize: 52, color: '#080616',
          fontWeight: 700, marginTop: 16, opacity: tagIn,
        }}>
          = एक लेनदार, बिना गारंटी के
        </div>
      </div>

      {/* NO GUARANTEE - big crossed out badge */}
      <div style={{
        position: 'absolute', bottom: 180, left: 120,
        transform: `scale(${noGuaranteeIn}) rotate(${-10 + (1 - noGuaranteeIn) * 30}deg)`,
        opacity: noGuaranteeIn,
      }}>
        <div style={{
          position: 'relative',
          padding: '24px 50px',
          background: '#080616',
          border: '5px solid #FF0000',
          borderRadius: 8,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 18, color: '#FF0000',
            letterSpacing: 4, fontWeight: 700,
          }}>
            STATUS
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 64, color: '#F5F5F5',
            fontWeight: 800, lineHeight: 1, position: 'relative',
          }}>
            कोई गारंटी नहीं
            {/* Red strike line */}
            <div style={{
              position: 'absolute', top: '50%', left: '-10px', right: '-10px',
              height: 6, background: '#FF0000', transform: 'translateY(-50%) rotate(-2deg)',
            }} />
          </div>
        </div>
      </div>

      {/* Right side - Hierarchy / Priority list */}
      <div style={{
        position: 'absolute', bottom: 160, right: 80,
        opacity: sp(140), width: 540,
        transform: `scale(${warningPulse})`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 18, color: '#F5F5F5',
          letterSpacing: 4, fontWeight: 700, marginBottom: 10,
        }}>
          ── PAYMENT PRIORITY ──
        </div>

        {[
          { rank: '1.', name: 'Secured Creditors', sub: 'पूरी सुरक्षा', col: '#F5F5F5' },
          { rank: '2.', name: 'Bondholders', sub: 'मध्यम सुरक्षा', col: '#9E9E9E' },
          { rank: 'LAST', name: 'YOU — Depositor', sub: 'कोई सुरक्षा नहीं', col: '#FF0000' },
        ].map((row, i) => (
          <div key={i} style={{
            display: 'flex', alignItems: 'center', gap: 16,
            padding: '12px 18px', marginBottom: 8,
            background: i === 2 ? '#FF0000' : 'rgba(245,245,245,0.1)',
            border: i === 2 ? '3px solid #F5F5F5' : '1px solid rgba(245,245,245,0.3)',
            borderRadius: 6,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 24,
              color: i === 2 ? '#F5F5F5' : row.col, fontWeight: 800,
              minWidth: 60,
            }}>
              {row.rank}
            </div>
            <div style={{ flex: 1 }}>
              <div style={{
                fontFamily: 'Inter, Poppins', fontSize: 24,
                color: i === 2 ? '#F5F5F5' : row.col, fontWeight: 800,
              }}>
                {row.name}
              </div>
              <div style={{
                fontFamily: 'Yantra One', fontSize: 18,
                color: i === 2 ? '#F5F5F5' : row.col, opacity: 0.85,
              }}>
                {row.sub}
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* Bottom - Final hammer line */}
      <div style={{
        position: 'absolute', bottom: 30, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - finalIn) * 30}px)`,
        opacity: finalIn,
        fontFamily: 'Inter, Poppins', fontSize: 22, color: '#F5F5F5',
        letterSpacing: 6, fontWeight: 700,
        background: '#080616', padding: '10px 24px',
        borderRadius: 4,
      }}>
        बैंक डूबा — तो आप last line में खड़े हैं
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 38%, rgba(8,6,22,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#F5F5F5', opacity: 0.5,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10UnsecuredCreditor;
```

