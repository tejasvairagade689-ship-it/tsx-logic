```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S01_VaultBelowManhattan',
  durationInSeconds: 25,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S01_VaultBelowManhattan: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow push-in
  const camScale = interpolate(frame, [0, durationInFrames], [1.08, 1.0], { extrapolateRight: 'clamp' });

  const titleIn = sp(15);
  const subIn = sp(45);
  const lineIn = sp(70);

  // Vault depth lines drifting up (metal moving)
  const depthLines = Array.from({ length: 7 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 30%, #FF7043 0%, #FF5722 45%, #E64A19 100%)',
        }}
      />

      {/* Shape layers — large drifting transparent geometry */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            width: 900,
            height: 900,
            borderRadius: '50%',
            border: '2px solid #F5F5F5',
            opacity: 0.05,
            top: 200 + Math.sin(frame * 0.02) * 30,
            left: 1100,
            transform: `scale(${1 + Math.sin(frame * 0.015) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            width: 600,
            height: 600,
            backgroundColor: '#303841',
            opacity: 0.06,
            top: -150,
            left: -150,
            transform: `rotate(${frame * 0.15}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — vertical depth lines drifting up */}
      <AbsoluteFill>
        {depthLines.map((_, i) => {
          const x = 150 + i * 250;
          const drift = (frame * 3 + i * 80) % 1200;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: 1080 - drift,
                width: 2,
                height: 160,
                background: 'linear-gradient(#F5F5F5, transparent)',
                opacity: 0.12,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 22 }).map((_, i) => {
          const r = seededRandom(i + 1);
          const r2 = seededRandom(i + 50);
          const py = (r2 * 1080 - frame * (0.4 + r)) % 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: r * 1920,
                top: py < 0 ? py + 1080 : py,
                width: 4 + r * 4,
                height: 4 + r * 4,
                borderRadius: '50%',
                backgroundColor: '#F5F5F5',
                opacity: 0.25,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', paddingLeft: 130 }}>
        {/* Vault SVG icon */}
        <div style={{ opacity: subIn, transform: `translateY(${(1 - subIn) * 30}px)`, marginBottom: 30 }}>
          <svg width="120" height="120" viewBox="0 0 100 100" fill="none">
            <rect x="10" y="14" width="80" height="72" rx="6" stroke="#303841" strokeWidth="5" />
            <circle cx="50" cy="50" r="20" stroke="#303841" strokeWidth="5" />
            <circle cx="50" cy="50" r="6" fill="#F5F5F5" />
            <g transform={`rotate(${frame * 1.2} 50 50)`}>
              <line x1="50" y1="30" x2="50" y2="20" stroke="#303841" strokeWidth="5" strokeLinecap="round" />
              <line x1="70" y1="50" x2="80" y2="50" stroke="#303841" strokeWidth="5" strokeLinecap="round" />
            </g>
          </svg>
        </div>

        <div
          style={{
            fontFamily: "'Mont Heavy', 'Inter', sans-serif",
            fontWeight: 900,
            fontSize: 108,
            lineHeight: 1.0,
            color: '#303841',
            letterSpacing: -2,
            opacity: titleIn,
            transform: `translateX(${(1 - titleIn) * -60}px)`,
          }}
        >
          RIGHT NOW,
        </div>
        <div
          style={{
            fontFamily: "'Mont Heavy', 'Inter', sans-serif",
            fontWeight: 900,
            fontSize: 108,
            lineHeight: 1.05,
            color: '#F5F5F5',
            letterSpacing: -2,
            opacity: subIn,
            transform: `translateX(${(1 - subIn) * -60}px)`,
          }}
        >
          METAL IS MOVING
        </div>

        <div
          style={{
            width: lineIn * 520,
            height: 8,
            backgroundColor: '#303841',
            marginTop: 28,
            borderRadius: 4,
          }}
        />
        <div
          style={{
            fontFamily: "'Inter', sans-serif",
            fontWeight: 600,
            fontSize: 46,
            color: '#303841',
            marginTop: 26,
            opacity: lineIn,
          }}
        >
          Beneath Lower Manhattan
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          boxShadow: 'inset 0 0 320px rgba(0,0,0,0.55)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: "'Inter', sans-serif",
          fontSize: 22,
          fontWeight: 600,
          color: '#F5F5F5',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default S01_VaultBelowManhattan;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S02_NotOnAScreen',
  durationInSeconds: 24,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S02_NotOnAScreen: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Negate beats: "Not on a screen" -> "Not in an app" -> "physical gold bars"
  const neg1 = sp(15);
  const neg2 = sp(50);
  const goldIn = sp(95, { damping: 9 });

  // strike-through animation for negated words
  const strike1 = interpolate(frame, [30, 48], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const strike2 = interpolate(frame, [65, 83], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const camPan = interpolate(frame, [0, durationInFrames], [-30, 30], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{ background: 'linear-gradient(135deg, #FF7043 0%, #FF5722 50%, #D84315 100%)' }}
      />

      {/* Shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            width: 700,
            height: 700,
            border: '3px solid #303841',
            opacity: 0.05,
            top: 400,
            left: 100,
            transform: `rotate(${frame * 0.2}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            width: 0,
            height: 0,
            borderLeft: '300px solid transparent',
            borderRight: '300px solid transparent',
            borderBottom: '500px solid #F5F5F5',
            opacity: 0.04,
            top: 150,
            right: 150,
            transform: `rotate(${-frame * 0.15}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — grid */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <div key={'h' + i} style={{ position: 'absolute', top: i * 90, left: 0, width: '100%', height: 1, backgroundColor: '#303841' }} />
        ))}
        {Array.from({ length: 20 }).map((_, i) => (
          <div key={'v' + i} style={{ position: 'absolute', left: i * 100, top: 0, height: '100%', width: 1, backgroundColor: '#303841' }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 18 }).map((_, i) => {
          const r = seededRandom(i + 3);
          const r2 = seededRandom(i + 70);
          const px = (r * 1920 + frame * (0.5 + r)) % 1920;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: px,
                top: r2 * 1080,
                width: 5,
                height: 5,
                borderRadius: '50%',
                backgroundColor: '#F5F5F5',
                opacity: 0.2,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `translateX(${camPan}px)` }}>
        {/* Negated line 1 */}
        <div style={{ position: 'relative', opacity: neg1, transform: `translateY(${(1 - neg1) * 25}px)`, marginBottom: 14 }}>
          <div style={{ fontFamily: "'Mont Heavy', 'Inter', sans-serif", fontWeight: 900, fontSize: 92, color: '#303841' }}>
            NOT ON A SCREEN
          </div>
          <div style={{ position: 'absolute', top: '52%', left: 0, width: `${strike1 * 100}%`, height: 8, backgroundColor: '#F5F5F5' }} />
        </div>

        {/* Negated line 2 */}
        <div style={{ position: 'relative', opacity: neg2, transform: `translateY(${(1 - neg2) * 25}px)`, marginBottom: 40 }}>
          <div style={{ fontFamily: "'Mont Heavy', 'Inter', sans-serif", fontWeight: 900, fontSize: 92, color: '#303841' }}>
            NOT IN AN APP
          </div>
          <div style={{ position: 'absolute', top: '52%', left: 0, width: `${strike2 * 100}%`, height: 8, backgroundColor: '#F5F5F5' }} />
        </div>

        {/* Payoff — gold bars */}
        <div style={{ display: 'flex', gap: 18, marginBottom: 22, transform: `scale(${goldIn})` }}>
          {[0, 1, 2].map((i) => (
            <svg key={i} width="150" height="80" viewBox="0 0 150 80">
              <polygon points="20,20 130,20 150,60 0,60" fill="#F5F5F5" stroke="#303841" strokeWidth="4" />
              <line x1="30" y1="35" x2="120" y2="35" stroke="#303841" strokeWidth="3" opacity="0.5" />
            </svg>
          ))}
        </div>
        <div
          style={{
            fontFamily: "'Mont Heavy', 'Inter', sans-serif",
            fontWeight: 900,
            fontSize: 110,
            color: '#F5F5F5',
            opacity: goldIn,
            letterSpacing: -2,
            textShadow: '0 6px 0 #303841',
          }}
        >
          PHYSICAL GOLD
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', sans-serif", fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default S02_NotOnAScreen;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S03_27Pounds',
  durationInSeconds: 25,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S03_27Pounds: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big number counter 0 -> 27
  const countRaw = interpolate(frame, [20, 70], [0, 27], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const count = Math.round(countRaw);
  const numIn = sp(15, { damping: 10 });
  const labelIn = sp(60);
  const palletIn = sp(90);

  // weight "drop" shake when number lands
  const shake = frame > 70 && frame < 80 ? Math.sin(frame * 3) * 4 : 0;
  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.06], { extrapolateRight: 'clamp' });

  // pallet boxes sliding deeper
  const palletShift = interpolate(frame, [90, durationInFrames], [0, -120], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 60%, #FF7043 0%, #FF5722 50%, #E64A19 100%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', width: 800, height: 800, borderRadius: '50%', backgroundColor: '#303841', opacity: 0.05, top: -200, right: -200, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06})` }} />
        <div style={{ position: 'absolute', width: 400, height: 400, border: '4px solid #F5F5F5', opacity: 0.06, bottom: 100, left: 200, transform: `rotate(${frame * 0.25}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — diagonal lines */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <div key={i} style={{ position: 'absolute', top: -200, left: i * 160 - frame * 0.6, width: 2, height: 1500, backgroundColor: '#303841', transform: 'rotate(25deg)' }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 16 }).map((_, i) => {
          const r = seededRandom(i + 9);
          const r2 = seededRandom(i + 33);
          const py = (r2 * 1080 + frame * (0.3 + r)) % 1080;
          return <div key={i} style={{ position: 'absolute', left: r * 1920, top: py, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#F5F5F5', opacity: 0.22 }} />;
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `scale(${camZoom}) translateX(${shake}px)` }}>
        <div style={{ display: 'flex', alignItems: 'baseline', transform: `scale(${numIn})` }}>
          <div style={{ fontFamily: "'Mont Heavy', 'Inter', sans-serif", fontWeight: 900, fontSize: 300, color: '#303841', lineHeight: 0.9, letterSpacing: -8 }}>
            {count}
          </div>
          <div style={{ fontFamily: "'Mont Heavy', 'Inter', sans-serif", fontWeight: 900, fontSize: 80, color: '#F5F5F5', marginLeft: 20 }}>
            LBS
          </div>
        </div>
        <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 700, fontSize: 50, color: '#303841', opacity: labelIn, marginTop: -10 }}>
          PER SINGLE GOLD BAR
        </div>

        {/* pallets sliding deeper */}
        <div style={{ display: 'flex', gap: 14, marginTop: 50, opacity: palletIn, transform: `translateX(${palletShift}px)` }}>
          {[0, 1, 2, 3].map((i) => (
            <svg key={i} width="120" height="100" viewBox="0 0 120 100" style={{ opacity: 1 - i * 0.18 }}>
              <rect x="10" y="20" width="100" height="55" fill="#F5F5F5" stroke="#303841" strokeWidth="4" />
              <rect x="0" y="78" width="120" height="14" fill="#303841" />
              <line x1="30" y1="35" x2="90" y2="35" stroke="#303841" strokeWidth="3" opacity="0.4" />
            </svg>
          ))}
        </div>
        <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 600, fontSize: 40, color: '#F5F5F5', opacity: palletIn, marginTop: 24 }}>
          Transferred deeper into storage
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', sans-serif", fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default S03_27Pounds;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S04_NeverAccess',
  durationInSeconds: 24,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S04_NeverAccess: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Three "NEVER" rows stagger
  const rows = [
    { word: 'NEVER SEE', icon: 'eye', d: 15 },
    { word: 'NEVER HEAR', icon: 'ear', d: 45 },
    { word: 'NEVER ACCESS', icon: 'lock', d: 75 },
  ];
  const closeIn = sp(110);
  const camTilt = interpolate(frame, [0, durationInFrames], [-2, 2], { extrapolateRight: 'clamp' });

  const renderIcon = (type: string) => {
    if (type === 'eye') {
      return (
        <svg width="70" height="70" viewBox="0 0 100 100">
          <path d="M10 50 Q50 15 90 50 Q50 85 10 50 Z" stroke="#303841" strokeWidth="6" fill="none" />
          <circle cx="50" cy="50" r="12" fill="#303841" />
          <line x1="20" y1="80" x2="80" y2="20" stroke="#F5F5F5" strokeWidth="6" strokeLinecap="round" />
        </svg>
      );
    }
    if (type === 'ear') {
      return (
        <svg width="70" height="70" viewBox="0 0 100 100">
          <path d="M30 35 Q30 15 55 18 Q78 22 72 48 Q68 62 60 70 Q56 80 48 82" stroke="#303841" strokeWidth="6" fill="none" strokeLinecap="round" />
          <line x1="20" y1="80" x2="80" y2="20" stroke="#F5F5F5" strokeWidth="6" strokeLinecap="round" />
        </svg>
      );
    }
    return (
      <svg width="70" height="70" viewBox="0 0 100 100">
        <rect x="25" y="45" width="50" height="40" rx="6" stroke="#303841" strokeWidth="6" fill="none" />
        <path d="M35 45 V32 Q35 18 50 18 Q65 18 65 32 V45" stroke="#303841" strokeWidth="6" fill="none" />
        <circle cx="50" cy="62" r="6" fill="#303841" />
      </svg>
    );
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #FF5722 0%, #D84315 100%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', width: 1000, height: 1000, borderRadius: '50%', border: '60px solid #303841', opacity: 0.04, top: 100, left: 1200, transform: `scale(${1 + Math.sin(frame * 0.018) * 0.05})` }} />
        <div style={{ position: 'absolute', width: 500, height: 500, backgroundColor: '#F5F5F5', opacity: 0.04, bottom: -200, left: -150, transform: `rotate(${frame * 0.18}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — concentric ripples */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
        {Array.from({ length: 4 }).map((_, i) => {
          const s = ((frame * 2 + i * 90) % 360) / 360;
          return <div key={i} style={{ position: 'absolute', width: 200 + s * 1400, height: 200 + s * 1400, borderRadius: '50%', border: '2px solid #F5F5F5', opacity: 0.1 * (1 - s) }} />;
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 14 }).map((_, i) => {
          const r = seededRandom(i + 11);
          const r2 = seededRandom(i + 44);
          const py = (r2 * 1080 - frame * (0.3 + r)) % 1080;
          return <div key={i} style={{ position: 'absolute', left: r * 1920, top: py < 0 ? py + 1080 : py, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#F5F5F5', opacity: 0.2 }} />;
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', paddingLeft: 160, transform: `rotate(${camTilt}deg)` }}>
        {rows.map((row, i) => {
          const a = sp(row.d);
          return (
            <div key={i} style={{ display: 'flex', alignItems: 'center', gap: 28, marginBottom: 30, opacity: a, transform: `translateX(${(1 - a) * -80}px)` }}>
              {renderIcon(row.icon)}
              <div style={{ fontFamily: "'Mont Heavy', 'Inter', sans-serif", fontWeight: 900, fontSize: 96, color: i === 2 ? '#F5F5F5' : '#303841', letterSpacing: -2 }}>
                {row.word}
              </div>
            </div>
          );
        })}
        <div style={{ marginTop: 30, opacity: closeIn, transform: `translateY(${(1 - closeIn) * 25}px)` }}>
          <div style={{ display: 'inline-block', backgroundColor: '#303841', padding: '14px 30px', borderRadius: 6 }}>
            <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 700, fontSize: 44, color: '#F5F5F5' }}>
              The part nobody's talking about
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', sans-serif", fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default S04_NeverAccess;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S05_BrokerageTicker',
  durationInSeconds: 25,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S05_BrokerageTicker: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const phoneIn = sp(15, { damping: 11 });
  const titleIn = sp(50);

  // ticker scroll
  const tickerX = -(frame * 4) % 2000;

  // portfolio rows stagger
  const portRows = [
    { name: 'GOLD', val: '+2.4%' },
    { name: 'SILVER', val: '+1.8%' },
    { name: 'GLD ETF', val: '+2.1%' },
  ];

  const camZoom = interpolate(frame, [0, durationInFrames], [1.05, 1.0], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 25% 70%, #FF7043 0%, #FF5722 55%, #E64A19 100%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', width: 700, height: 700, border: '2px solid #303841', borderRadius: 40, opacity: 0.05, top: 150, right: 80, transform: `rotate(${frame * 0.12}deg)` }} />
        <div style={{ position: 'absolute', width: 400, height: 400, borderRadius: '50%', backgroundColor: '#F5F5F5', opacity: 0.05, bottom: -100, left: 150, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.07})` }} />
      </AbsoluteFill>

      {/* Motion texture — dotted grid */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {Array.from({ length: 90 }).map((_, i) => {
          const col = i % 15;
          const rowN = Math.floor(i / 15);
          return <div key={i} style={{ position: 'absolute', left: 80 + col * 130, top: 60 + rowN * 160, width: 6, height: 6, borderRadius: '50%', backgroundColor: '#303841' }} />;
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 14 }).map((_, i) => {
          const r = seededRandom(i + 13);
          const r2 = seededRandom(i + 55);
          const px = (r * 1920 + frame * (0.4 + r)) % 1920;
          return <div key={i} style={{ position: 'absolute', left: px, top: r2 * 1080, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#F5F5F5', opacity: 0.2 }} />;
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `scale(${camZoom})` }}>
        <div style={{ fontFamily: "'Mont Heavy', 'Inter', sans-serif", fontWeight: 900, fontSize: 80, color: '#303841', opacity: titleIn, marginBottom: 30, transform: `translateY(${(1 - titleIn) * 20}px)` }}>
          "FEELING SAFE"
        </div>

        {/* Phone mockup */}
        <div
          style={{
            width: 520,
            height: 560,
            backgroundColor: '#303841',
            borderRadius: 40,
            border: '8px solid #F5F5F5',
            padding: 24,
            transform: `scale(${phoneIn}) translateY(${Math.sin(frame * 0.05) * 6}px)`,
            boxShadow: '0 30px 60px rgba(0,0,0,0.4)',
          }}
        >
          {/* ticker bar */}
          <div style={{ overflow: 'hidden', height: 50, borderRadius: 8, backgroundColor: '#FF5722', display: 'flex', alignItems: 'center', marginBottom: 20 }}>
            <div style={{ whiteSpace: 'nowrap', transform: `translateX(${tickerX}px)`, fontFamily: "'JetBrains Mono', monospace", fontSize: 26, fontWeight: 700, color: '#F5F5F5' }}>
              GOLD ▲ 2.4%   SILVER ▲ 1.8%   GLD ▲ 2.1%   SLV ▲ 1.5%   GOLD ▲ 2.4%   SILVER ▲ 1.8%
            </div>
          </div>
          {/* portfolio rows */}
          {portRows.map((p, i) => {
            const a = sp(70 + i * 20);
            return (
              <div key={i} style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', backgroundColor: '#F5F5F5', borderRadius: 10, padding: '18px 22px', marginBottom: 16, opacity: a, transform: `translateX(${(1 - a) * 40}px)` }}>
                <span style={{ fontFamily: "'Inter', sans-serif", fontWeight: 800, fontSize: 36, color: '#303841' }}>{p.name}</span>
                <span style={{ fontFamily: "'JetBrains Mono', monospace", fontWeight: 700, fontSize: 34, color: '#2E7D32' }}>{p.val}</span>
              </div>
            );
          })}
        </div>
        <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 600, fontSize: 40, color: '#F5F5F5', opacity: titleIn, marginTop: 28 }}>
          Neat little rows on a screen
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', sans-serif", fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default S05_BrokerageTicker;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S06_CouldYouGetIt',
  durationInSeconds: 24,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S06_CouldYouGetIt: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const qMarkIn = sp(15, { damping: 8 });
  const line1 = sp(45);
  const line2 = sp(75);
  const bigQ = sp(110, { damping: 9 });

  // big question mark pulse
  const pulse = 1 + Math.sin(frame * 0.08) * 0.04;
  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.07], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 40%, #FF7043 0%, #FF5722 50%, #D84315 100%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', width: 900, height: 900, borderRadius: '50%', backgroundColor: '#303841', opacity: 0.05, top: '50%', left: '50%', marginTop: -450, marginLeft: -450, transform: `scale(${pulse})` }} />
        <div style={{ position: 'absolute', width: 0, height: 0, borderLeft: '250px solid transparent', borderRight: '250px solid transparent', borderBottom: '420px solid #F5F5F5', opacity: 0.04, bottom: 0, right: 100, transform: `rotate(${frame * 0.1}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — radiating spokes */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
        {Array.from({ length: 16 }).map((_, i) => (
          <div key={i} style={{ position: 'absolute', width: 3, height: 700, backgroundColor: '#F5F5F5', opacity: 0.05, transform: `rotate(${i * 22.5 + frame * 0.2}deg)`, transformOrigin: 'center' }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 14 }).map((_, i) => {
          const r = seededRandom(i + 17);
          const r2 = seededRandom(i + 66);
          const py = (r2 * 1080 - frame * (0.3 + r)) % 1080;
          return <div key={i} style={{ position: 'absolute', left: r * 1920, top: py < 0 ? py + 1080 : py, width: 5, height: 5, borderRadius: '50%', backgroundColor: '#303841', opacity: 0.25 }} />;
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `scale(${camZoom})` }}>
        <div style={{ fontFamily: "'Mont Heavy', 'Inter', sans-serif", fontWeight: 900, fontSize: 260, color: '#303841', lineHeight: 0.8, transform: `scale(${qMarkIn})`, textShadow: '0 8px 0 #F5F5F5' }}>
          ?
        </div>
        <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 700, fontSize: 52, color: '#303841', opacity: line1, marginTop: 10, transform: `translateY(${(1 - line1) * 20}px)` }}>
          If you tried to take delivery
        </div>
        <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 700, fontSize: 52, color: '#303841', opacity: line2, transform: `translateY(${(1 - line2) * 20}px)` }}>
          of the gold your account says you own...
        </div>
        <div
          style={{
            fontFamily: "'Mont Heavy', 'Inter', sans-serif",
            fontWeight: 900,
            fontSize: 84,
            color: '#F5F5F5',
            opacity: bigQ,
            marginTop: 24,
            backgroundColor: '#303841',
            padding: '16px 40px',
            borderRadius: 8,
            transform: `scale(${0.9 + bigQ * 0.1})`,
          }}
        >
          COULD YOU ACTUALLY GET IT?
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', sans-serif", fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default S06_CouldYouGetIt;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S07_CallYourBroker',
  durationInSeconds: 25,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S07_CallYourBroker: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const phoneIn = sp(20, { damping: 9 });
  const bubbleIn = sp(55);
  const doorIn = sp(90);
  const barDrop = interpolate(frame, [110, 140], [-200, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bounce });

  // ringing wiggle
  const ring = frame < 50 ? Math.sin(frame * 0.6) * 4 : 0;
  const camPan = interpolate(frame, [0, durationInFrames], [40, -40], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(120deg, #FF7043 0%, #FF5722 50%, #E64A19 100%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', width: 600, height: 600, borderRadius: '50%', border: '50px solid #F5F5F5', opacity: 0.05, top: 250, left: 80, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06})` }} />
        <div style={{ position: 'absolute', width: 500, height: 500, backgroundColor: '#303841', opacity: 0.05, top: -150, right: -100, transform: `rotate(${frame * 0.2}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — horizontal flow lines */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {Array.from({ length: 9 }).map((_, i) => (
          <div key={i} style={{ position: 'absolute', top: 80 + i * 120, left: -(frame * 2 + i * 100) % 400, width: 3000, height: 2, backgroundColor: '#303841' }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 14 }).map((_, i) => {
          const r = seededRandom(i + 19);
          const r2 = seededRandom(i + 77);
          const px = (r * 1920 - frame * (0.4 + r)) % 1920;
          return <div key={i} style={{ position: 'absolute', left: px < 0 ? px + 1920 : px, top: r2 * 1080, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#F5F5F5', opacity: 0.2 }} />;
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `translateX(${camPan}px)` }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 80 }}>
          {/* phone calling */}
          <div style={{ transform: `scale(${phoneIn}) rotate(${ring}deg)` }}>
            <svg width="180" height="260" viewBox="0 0 100 150">
              <rect x="10" y="5" width="80" height="140" rx="14" fill="#303841" stroke="#F5F5F5" strokeWidth="4" />
              <circle cx="50" cy="120" r="8" fill="#F5F5F5" />
              <path d="M30 40 Q34 30 44 34 L48 44 L42 50 Q48 62 60 68 L66 62 L76 66 Q80 76 70 80 Q45 80 30 55 Q26 46 30 40 Z" fill="#FF5722" />
            </svg>
            {/* speech bubble */}
            <div style={{ opacity: bubbleIn, transform: `translateY(${(1 - bubbleIn) * -20}px)`, marginTop: 16, backgroundColor: '#F5F5F5', borderRadius: 12, padding: '14px 22px', maxWidth: 260 }}>
              <span style={{ fontFamily: "'Inter', sans-serif", fontWeight: 700, fontSize: 32, color: '#303841' }}>"Send me my gold"</span>
            </div>
          </div>

          {/* arrow */}
          <div style={{ opacity: doorIn, fontFamily: "'Mont Heavy', sans-serif", fontWeight: 900, fontSize: 100, color: '#303841' }}>→</div>

          {/* door + bar */}
          <div style={{ transform: `scale(${doorIn})`, position: 'relative' }}>
            <svg width="200" height="280" viewBox="0 0 100 140">
              <rect x="15" y="10" width="70" height="130" rx="4" fill="#303841" stroke="#F5F5F5" strokeWidth="4" />
              <circle cx="72" cy="78" r="5" fill="#FF5722" />
            </svg>
            <div style={{ position: 'absolute', bottom: 20, left: 30, transform: `translateY(${barDrop}px)` }}>
              <svg width="140" height="70" viewBox="0 0 150 80">
                <polygon points="20,20 130,20 150,60 0,60" fill="#F5F5F5" stroke="#303841" strokeWidth="4" />
              </svg>
            </div>
          </div>
        </div>

        <div style={{ fontFamily: "'Mont Heavy', 'Inter', sans-serif", fontWeight: 900, fontSize: 70, color: '#F5F5F5', marginTop: 50, opacity: bubbleIn }}>
          I'M NOT BEING RHETORICAL.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', sans-serif", fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default S07_CallYourBroker;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S08_TheAnswerIsNo',
  durationInSeconds: 24,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S08_TheAnswerIsNo: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const topIn = sp(15);
  const noIn = sp(60, { damping: 7, stiffness: 140 });

  // impact screen shake when NO lands
  const shake = frame > 60 && frame < 72 ? Math.sin(frame * 4) * 8 : 0;

  // crowd dots — "vast majority"
  const crowd = Array.from({ length: 60 });
  const camZoom = interpolate(frame, [0, durationInFrames], [1.04, 1.0], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 80%, #FF7043 0%, #FF5722 50%, #D84315 100%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', width: 1100, height: 1100, borderRadius: '50%', border: '2px solid #303841', opacity: 0.05, top: -200, left: '50%', marginLeft: -550, transform: `scale(${1 + Math.sin(frame * 0.015) * 0.05})` }} />
        <div style={{ position: 'absolute', width: 400, height: 400, backgroundColor: '#F5F5F5', opacity: 0.04, bottom: 80, right: 120, transform: `rotate(${frame * 0.22}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — crowd grid of people dots */}
      <AbsoluteFill style={{ justifyContent: 'flex-start', alignItems: 'center', paddingTop: 120 }}>
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(12, 1fr)', gap: 22, width: 700, opacity: topIn }}>
          {crowd.map((_, i) => {
            const lit = i < 56; // vast majority
            const wob = Math.sin(frame * 0.05 + i) * 2;
            return (
              <svg key={i} width="34" height="44" viewBox="0 0 30 40" style={{ transform: `translateY(${wob}px)` }}>
                <circle cx="15" cy="10" r="7" fill={lit ? '#303841' : '#F5F5F5'} opacity={lit ? 0.85 : 1} />
                <path d="M5 38 Q5 22 15 22 Q25 22 25 38 Z" fill={lit ? '#303841' : '#F5F5F5'} opacity={lit ? 0.85 : 1} />
              </svg>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 12 }).map((_, i) => {
          const r = seededRandom(i + 23);
          const r2 = seededRandom(i + 88);
          const py = (r2 * 1080 - frame * (0.3 + r)) % 1080;
          return <div key={i} style={{ position: 'absolute', left: r * 1920, top: py < 0 ? py + 1080 : py, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#F5F5F5', opacity: 0.18 }} />;
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'flex-end', alignItems: 'center', paddingBottom: 130, transform: `scale(${camZoom}) translate(${shake}px, ${shake}px)` }}>
        <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 700, fontSize: 50, color: '#303841', opacity: topIn, marginBottom: 16 }}>
          The honest answer is...
        </div>
        <div
          style={{
            fontFamily: "'Mont Heavy', 'Inter', sans-serif",
            fontWeight: 900,
            fontSize: 260,
            color: '#F5F5F5',
            lineHeight: 0.85,
            transform: `scale(${noIn})`,
            textShadow: '0 10px 0 #303841',
            letterSpacing: -6,
          }}
        >
          NO.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', sans-serif", fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default S08_TheAnswerIsNo;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S09_TheGap',
  durationInSeconds: 25,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S09_TheGap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const leftIn = sp(20);
  const rightIn = sp(45);
  // the gap widens
  const gapWidth = interpolate(frame, [70, 120], [40, 300], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const labelIn = sp(120);
  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.05], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(90deg, #E64A19 0%, #FF5722 50%, #E64A19 100%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', width: 700, height: 700, borderRadius: '50%', backgroundColor: '#303841', opacity: 0.05, top: 200, left: -150, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06})` }} />
        <div style={{ position: 'absolute', width: 600, height: 600, border: '4px solid #F5F5F5', opacity: 0.05, bottom: -150, right: -100, transform: `rotate(${frame * 0.18}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — vertical bars */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <div key={i} style={{ position: 'absolute', left: i * 82, top: 0, width: 1, height: '100%', backgroundColor: '#303841' }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 12 }).map((_, i) => {
          const r = seededRandom(i + 27);
          const r2 = seededRandom(i + 99);
          const py = (r2 * 1080 + frame * (0.3 + r)) % 1080;
          return <div key={i} style={{ position: 'absolute', left: r * 1920, top: py, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#F5F5F5', opacity: 0.2 }} />;
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `scale(${camZoom})` }}>
        <div style={{ display: 'flex', alignItems: 'center' }}>
          {/* Left block — what you think you own */}
          <div style={{ opacity: leftIn, transform: `translateX(${(1 - leftIn) * -60 - gapWidth / 2}px)`, textAlign: 'center' }}>
            <div style={{ width: 320, height: 260, backgroundColor: '#303841', borderRadius: 14, display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center' }}>
              <svg width="90" height="90" viewBox="0 0 100 100">
                <rect x="20" y="30" width="60" height="42" rx="4" fill="#F5F5F5" />
                <line x1="28" y1="44" x2="72" y2="44" stroke="#303841" strokeWidth="3" />
              </svg>
              <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 800, fontSize: 32, color: '#F5F5F5', marginTop: 14 }}>WHAT YOU THINK</div>
              <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 600, fontSize: 26, color: '#FF7043' }}>you own</div>
            </div>
          </div>

          {/* THE GAP */}
          <div style={{ width: gapWidth, display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
            <div style={{ borderBottom: '6px dashed #F5F5F5', width: '80%', opacity: labelIn }} />
          </div>

          {/* Right block — what actually exists */}
          <div style={{ opacity: rightIn, transform: `translateX(${(1 - rightIn) * 60 + gapWidth / 2}px)`, textAlign: 'center' }}>
            <div style={{ width: 320, height: 260, backgroundColor: '#F5F5F5', borderRadius: 14, display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center', border: '4px solid #303841' }}>
              <svg width="90" height="90" viewBox="0 0 100 100">
                <rect x="18" y="22" width="64" height="58" rx="5" stroke="#303841" strokeWidth="5" fill="none" />
                <circle cx="50" cy="51" r="14" stroke="#303841" strokeWidth="5" fill="none" />
              </svg>
              <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 800, fontSize: 32, color: '#303841', marginTop: 14 }}>WHAT EXISTS</div>
              <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 600, fontSize: 26, color: '#D84315' }}>in the vault</div>
            </div>
          </div>
        </div>

        <div style={{ marginTop: 50, opacity: labelIn, transform: `translateY(${(1 - labelIn) * 25}px)`, textAlign: 'center' }}>
          <div style={{ fontFamily: "'Mont Heavy', 'Inter', sans-serif", fontWeight: 900, fontSize: 72, color: '#303841', letterSpacing: -2 }}>
            THE MOST DANGEROUS
          </div>
          <div style={{ fontFamily: "'Mont Heavy', 'Inter', sans-serif", fontWeight: 900, fontSize: 72, color: '#F5F5F5', letterSpacing: -2 }}>
            BLIND SPOT IN FINANCE
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', sans-serif", fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default S09_TheGap;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S10_MeetKunal',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S10_MeetKunal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const nameIn = sp(15, { damping: 10 });
  const brandIn = sp(45);
  const vsIn = sp(80);

  // toggle highlight between "how it works" vs "how it's marketed"
  const toggle = Math.sin(frame * 0.06) > 0;
  const camZoom = interpolate(frame, [0, durationInFrames], [1.05, 1.0], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 35%, #FF7043 0%, #FF5722 50%, #D84315 100%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', width: 900, height: 900, borderRadius: '50%', border: '70px solid #303841', opacity: 0.04, top: -300, right: -200, transform: `scale(${1 + Math.sin(frame * 0.018) * 0.05})` }} />
        <div style={{ position: 'absolute', width: 500, height: 500, backgroundColor: '#F5F5F5', opacity: 0.04, bottom: -180, left: -100, transform: `rotate(${frame * 0.2}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — orbiting dots */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
        {Array.from({ length: 10 }).map((_, i) => {
          const ang = (i / 10) * Math.PI * 2 + frame * 0.01;
          const rad = 520;
          return <div key={i} style={{ position: 'absolute', left: 960 + Math.cos(ang) * rad, top: 540 + Math.sin(ang) * rad, width: 8, height: 8, borderRadius: '50%', backgroundColor: '#F5F5F5', opacity: 0.18 }} />;
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 14 }).map((_, i) => {
          const r = seededRandom(i + 31);
          const r2 = seededRandom(i + 120);
          const py = (r2 * 1080 - frame * (0.3 + r)) % 1080;
          return <div key={i} style={{ position: 'absolute', left: r * 1920, top: py < 0 ? py + 1080 : py, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#303841', opacity: 0.22 }} />;
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `scale(${camZoom})` }}>
        <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 600, fontSize: 44, color: '#303841', opacity: nameIn }}>
          My name's
        </div>
        <div style={{ fontFamily: "'Mont Heavy', 'Inter', sans-serif", fontWeight: 900, fontSize: 200, color: '#F5F5F5', lineHeight: 0.9, transform: `scale(${nameIn})`, textShadow: '0 8px 0 #303841', letterSpacing: -4 }}>
          KUNAL
        </div>
        <div style={{ fontFamily: "'Inter', sans-serif", fontWeight: 700, fontSize: 42, color: '#303841', opacity: brandIn, marginTop: 6 }}>
          and I run <span style={{ color: '#F5F5F5', backgroundColor: '#303841', padding: '4px 16px', borderRadius: 6 }}>FAILED FINANCES</span>
        </div>

        {/* vs comparison */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 30, marginTop: 50, opacity: vsIn }}>
          <div style={{ backgroundColor: toggle ? '#303841' : 'transparent', border: '3px solid #303841', borderRadius: 10, padding: '16px 28px', transition: 'all 0.2s' }}>
            <span style={{ fontFamily: "'Inter', sans-serif", fontWeight: 800, fontSize: 38, color: toggle ? '#F5F5F5' : '#303841' }}>How wealth WORKS</span>
          </div>
          <div style={{ fontFamily: "'Mont Heavy', sans-serif", fontWeight: 900, fontSize: 48, color: '#F5F5F5' }}>VS</div>
          <div style={{ backgroundColor: !toggle ? '#303841' : 'transparent', border: '3px solid #303841', borderRadius: 10, padding: '16px 28px' }}>
            <span style={{ fontFamily: "'Inter', sans-serif", fontWeight: 800, fontSize: 38, color: !toggle ? '#F5F5F5' : '#303841' }}>How it's MARKETED</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', sans-serif", fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default S10_MeetKunal;
```

