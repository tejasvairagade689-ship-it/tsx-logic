```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S01_KarzKaChakra',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S01_KarzKaChakra: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#664343'; const DARK = '#4A2F2F'; const CREAM = '#EEEEEE'; const INK = '#000000'; const SAND = '#E8B984';

  const camZoom = interpolate(frame, [0, 900], [1, 1.08], { extrapolateRight: 'clamp' });
  const ringRot = frame * 0.25;

  const items = [
    { t: 'EMI पर फोन', a: -90 },
    { t: 'BNPL पर कपड़े', a: 0 },
    { t: 'क्रेडिट कार्ड पर खाना', a: 90 },
    { t: 'पर्सनल लोन से पुरानी EMI', a: 180 },
  ];

  const heroIn = sp(430, { damping: 14 });
  const lineIn = sp(620);

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `radial-gradient(circle at ${50 + Math.sin(frame * 0.01) * 10}% 45%, #7D5656 0%, ${BG} 55%, ${DARK} 100%)` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: -200 + Math.sin(frame * 0.008) * 40, top: -150, width: 700, height: 700, borderRadius: '50%', background: CREAM, opacity: 0.04, transform: `scale(${1 + Math.sin(frame * 0.012) * 0.06})` }} />
      <div style={{ position: 'absolute', right: -180, bottom: -220 + Math.cos(frame * 0.009) * 30, width: 640, height: 640, background: INK, opacity: 0.06, transform: `rotate(${frame * 0.05}deg)` }} />

      {/* Motion texture: concentric circles */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {[180, 320, 460, 600].map((r, i) => (
          <circle key={i} cx={960} cy={540} r={r + Math.sin(frame * 0.03 + i) * 8} fill="none" stroke={CREAM} strokeWidth={1.5} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const x = seededRandom(i + 1) * 1920;
        const y = ((seededRandom(i + 40) * 1080 + frame * (0.4 + seededRandom(i + 7))) % 1140) - 30;
        return <div key={i} style={{ position: 'absolute', left: x, top: 1080 - y, width: 5, height: 5, borderRadius: '50%', background: SAND, opacity: 0.25 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Rotating debt-cycle ring */}
        <svg width="820" height="820" style={{ position: 'absolute', transform: `rotate(${ringRot}deg)`, opacity: interpolate(frame, [10, 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          <circle cx={410} cy={410} r={330} fill="none" stroke={CREAM} strokeWidth={3} strokeDasharray="26 20" opacity={0.5} />
          <polygon points="410,58 396,90 424,90" fill={SAND} />
          <polygon points="410,762 396,730 424,730" fill={SAND} transform="rotate(180 410 410)" />
        </svg>

        {/* Four debt labels on cycle points */}
        {items.map((it, i) => {
          const s = sp(30 + i * 22);
          const rad = (it.a * Math.PI) / 180;
          const x = Math.cos(rad) * 460; const y = Math.sin(rad) * 320;
          return (
            <div key={i} style={{
              position: 'absolute', transform: `translate(${x}px, ${y}px) scale(${s})`,
              background: CREAM, color: INK, padding: '22px 44px', borderRadius: 14,
              fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 44,
              boxShadow: '0 14px 40px rgba(0,0,0,0.35)', border: `4px solid ${INK}`,
            }}>{it.t}</div>
          );
        })}

        {/* Hero payoff */}
        <div style={{ transform: `scale(${heroIn})`, textAlign: 'center' }}>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 150, color: CREAM, lineHeight: 1, textShadow: '0 10px 40px rgba(0,0,0,0.45)' }}>
            कर्ज़ का चक्र
          </div>
        </div>
        <div style={{ position: 'absolute', bottom: 130, opacity: lineIn, transform: `translateY(${(1 - lineIn) * 40}px)`, fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 46, fontWeight: 700, color: SAND }}>
          दोस्त, सहकर्मी, भाई, बहन... या तुम खुद?
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S01_KarzKaChakra;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S02_PehliTareekh',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S02_PehliTareekh: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#664343'; const DARK = '#4A2F2F'; const CREAM = '#EEEEEE'; const INK = '#000000'; const SAND = '#E8B984'; const WARN = '#D96B6B';

  const camPan = interpolate(frame, [0, 900], [-30, 30], { extrapolateRight: 'clamp' });
  const pulse = 1 + Math.sin(frame * 0.18) * 0.03;

  const notifs = ['आप पहले से योग्य हैं', 'केवल 2 मिनट में पैसा', 'शून्य लागत वाली EMI', 'अभी खरीदिये, बाद में चुकाइये'];

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${120 + Math.sin(frame * 0.01) * 15}deg, ${DARK} 0%, ${BG} 50%, #7D5656 100%)` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: 1300 + Math.sin(frame * 0.01) * 50, top: -200, width: 600, height: 600, borderRadius: 60, background: CREAM, opacity: 0.04, transform: `rotate(${25 + frame * 0.04}deg)` }} />
      <div style={{ position: 'absolute', left: -250, bottom: -180, width: 700, height: 700, borderRadius: '50%', background: INK, opacity: 0.07, transform: `scale(${1 + Math.cos(frame * 0.011) * 0.05})` }} />

      {/* Motion texture: grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 13 }).map((_, i) => (
          <line key={i} x1={i * 160 + (frame * 0.2) % 160} y1={0} x2={i * 160 + (frame * 0.2) % 160} y2={1080} stroke={CREAM} strokeWidth={1} />
        ))}
        {Array.from({ length: 8 }).map((_, i) => (
          <line key={'h' + i} x1={0} y1={i * 160} x2={1920} y2={i * 160} stroke={CREAM} strokeWidth={1} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const x = ((seededRandom(i + 3) * 1920 + frame * (0.3 + seededRandom(i + 9) * 0.5)) % 1960) - 20;
        const y = seededRandom(i + 60) * 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 4, height: 4, borderRadius: '50%', background: SAND, opacity: 0.3 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camPan}px)`, flexDirection: 'row', justifyContent: 'center', alignItems: 'center', gap: 120 }}>

        {/* Calendar block: the 1st = fear */}
        <div style={{ transform: `scale(${sp(15) * pulse})`, width: 480, borderRadius: 28, overflow: 'hidden', boxShadow: '0 30px 80px rgba(0,0,0,0.45)', border: `5px solid ${INK}` }}>
          <div style={{ background: WARN, color: INK, textAlign: 'center', padding: '20px 0', fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 44 }}>
            महीने की पहली तारीख
          </div>
          <div style={{ background: CREAM, textAlign: 'center', padding: '30px 0 40px' }}>
            <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 220, color: INK, lineHeight: 1 }}>1</div>
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 38, fontWeight: 700, color: WARN, opacity: interpolate(frame, [120, 150], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
              भीतर डर शुरू
            </div>
          </div>
        </div>

        {/* Phone with loan notifications */}
        <div style={{ transform: `translateY(${(1 - sp(60)) * 200}px) scale(${sp(60)})`, width: 420, height: 780, background: INK, borderRadius: 46, padding: 16, boxShadow: '0 30px 80px rgba(0,0,0,0.5)' }}>
          <div style={{ width: '100%', height: '100%', background: `linear-gradient(180deg, ${DARK}, ${BG})`, borderRadius: 34, overflow: 'hidden', padding: '70px 18px 0' }}>
            {notifs.map((n, i) => {
              const s = sp(150 + i * 55, { damping: 11 });
              const glow = 0.5 + Math.sin(frame * 0.15 + i) * 0.5;
              return (
                <div key={i} style={{
                  transform: `translateX(${(1 - s) * 320}px)`, opacity: s, marginBottom: 18,
                  background: CREAM, borderRadius: 14, padding: '18px 20px',
                  borderLeft: `10px solid ${SAND}`, boxShadow: `0 0 ${18 * glow}px rgba(232,185,132,0.5)`,
                }}>
                  <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 20, fontWeight: 700, color: '#8C5E5E' }}>LOAN APP</div>
                  <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 30, fontWeight: 800, color: INK }}>{n}</div>
                </div>
              );
            })}
          </div>
        </div>
      </AbsoluteFill>

      {/* Payoff line */}
      <div style={{ position: 'absolute', bottom: 90, width: '100%', textAlign: 'center', opacity: sp(560), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 52, fontWeight: 800, color: CREAM }}>
        कर्ज़ अब फाइल में नहीं — <span style={{ color: SAND }}>स्क्रीन पर चमकता है</span>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S02_PehliTareekh;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S03_TankhwahKisiAurKi',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S03_TankhwahKisiAurKi: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#664343'; const DARK = '#4A2F2F'; const CREAM = '#EEEEEE'; const INK = '#000000'; const SAND = '#E8B984'; const WARN = '#D96B6B';

  // Beat 1: salary bar splits (0-400). Beat 2: brand intro (430+)
  const sliceOut = sp(140, { damping: 15 });
  const beat2 = interpolate(frame, [400, 430], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const camZoom = interpolate(frame, [0, 900], [1.06, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `radial-gradient(ellipse at 30% ${40 + Math.sin(frame * 0.012) * 8}%, #7D5656 0%, ${BG} 50%, ${DARK} 100%)` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', right: -150, top: -150 + Math.sin(frame * 0.01) * 30, width: 560, height: 560, background: 'transparent', border: `70px solid ${CREAM}`, opacity: 0.03, borderRadius: '50%' }} />
      <div style={{ position: 'absolute', left: -120, bottom: -160, width: 520, height: 520, background: SAND, opacity: 0.05, transform: `rotate(${frame * 0.06}deg)` }} />

      {/* Motion texture: horizontal lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <line key={i} x1={0} y1={i * 120 + (frame * 0.3) % 120} x2={1920} y2={i * 120 + (frame * 0.3) % 120} stroke={CREAM} strokeWidth={1} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const x = seededRandom(i + 5) * 1920;
        const y = ((seededRandom(i + 25) * 1080 + frame * (0.35 + seededRandom(i + 13) * 0.4)) % 1140) - 30;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 5, height: 5, borderRadius: '50%', background: CREAM, opacity: 0.2 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, justifyContent: 'center', alignItems: 'center' }}>

        {/* BEAT 1: salary bar with a slice flying away */}
        <div style={{ opacity: 1 - beat2, position: 'absolute', textAlign: 'center' }}>
          <div style={{ transform: `translateY(${(1 - sp(10)) * -60}px)`, opacity: sp(10), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 68, fontWeight: 800, color: CREAM, marginBottom: 60 }}>
            आने वाली तनख़्वाह
          </div>
          <div style={{ position: 'relative', width: 1100, height: 150, margin: '0 auto' }}>
            <div style={{ position: 'absolute', left: 0, top: 0, width: 760, height: 150, background: CREAM, borderRadius: '18px 0 0 18px', border: `5px solid ${INK}`, display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 44, fontWeight: 800, color: INK, transform: `scaleX(${sp(40)})`, transformOrigin: 'left' }}>
              तुम्हारा हिस्सा
            </div>
            <div style={{
              position: 'absolute', left: 760, top: 0, width: 340, height: 150, background: WARN,
              borderRadius: '0 18px 18px 0', border: `5px solid ${INK}`, display: 'flex', alignItems: 'center', justifyContent: 'center',
              fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 38, fontWeight: 800, color: INK,
              transform: `translate(${sliceOut * 230}px, ${sliceOut * -130}px) rotate(${sliceOut * 10}deg)`,
            }}>
              किसी और के नाम
            </div>
          </div>
          <div style={{ marginTop: 70, opacity: sp(230), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 46, fontWeight: 700, color: SAND }}>
            "सुविधा" मिलने के उसी क्षण — हिस्सा जा चुका था
          </div>
        </div>

        {/* BEAT 2: creator intro card */}
        <div style={{ opacity: beat2, transform: `scale(${0.9 + beat2 * 0.1})`, textAlign: 'center' }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 56, fontWeight: 700, color: CREAM }}>मैं कुनाल हूँ</div>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 130, color: SAND, margin: '18px 0', textShadow: '0 10px 40px rgba(0,0,0,0.4)' }}>
            ProBuffet HQ
          </div>
          <div style={{ width: interpolate(frame, [470, 520], [0, 700], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }), height: 6, background: CREAM, margin: '0 auto 34px', borderRadius: 3 }} />
          <div style={{ opacity: sp(540), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 44, fontWeight: 700, color: CREAM, lineHeight: 1.6 }}>
            न डर बेचना है, न हर लोन को बुरा कहना है<br />
            <span style={{ color: SAND }}>उधार पाप नहीं — बिना समझ का उधार फंदा है</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S03_TankhwahKisiAurKi;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S04_AdhuraSach',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S04_AdhuraSach: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#664343'; const DARK = '#4A2F2F'; const CREAM = '#EEEEEE'; const INK = '#000000'; const SAND = '#E8B984'; const WARN = '#D96B6B';

  // Stamp slam with screen shake
  const stampAt = 300;
  const stamp = sp(stampAt, { damping: 9, stiffness: 200 });
  const shake = frame >= stampAt + 4 && frame < stampAt + 22 ? Math.sin((frame - stampAt) * 2.2) * 12 * (1 - (frame - stampAt - 4) / 18) : 0;
  const camTilt = interpolate(frame, [0, 900], [-1, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${160 + Math.sin(frame * 0.008) * 10}deg, ${BG} 0%, ${DARK} 70%, #3A2626 100%)` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: 200 + Math.sin(frame * 0.009) * 60, top: 700, width: 800, height: 800, borderRadius: '50%', background: CREAM, opacity: 0.03 }} />
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <polygon points={`${1500 + Math.sin(frame * 0.01) * 40},100 1800,500 1200,500`} fill={SAND} />
      </svg>

      {/* Motion texture: diagonal lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <line key={i} x1={i * 200 - 400 + (frame * 0.4) % 200} y1={0} x2={i * 200 + 200 + (frame * 0.4) % 200} y2={1080} stroke={CREAM} strokeWidth={1.5} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const x = seededRandom(i + 8) * 1920;
        const y = ((seededRandom(i + 33) * 1080 + frame * (0.3 + seededRandom(i + 17) * 0.5)) % 1140) - 30;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 4, height: 4, background: SAND, opacity: 0.25, transform: `rotate(${frame + i * 30}deg)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${shake}px) rotate(${camTilt}deg)`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Giant claim quote */}
        <div style={{ position: 'relative', textAlign: 'center', maxWidth: 1500 }}>
          <div style={{ position: 'absolute', left: -110, top: -120, fontFamily: "'Yantra One', sans-serif", fontSize: 260, color: SAND, opacity: 0.35 * sp(5) }}>"</div>
          <div style={{ transform: `translateY(${(1 - sp(15)) * 60}px)`, opacity: sp(15), fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 105, color: CREAM, lineHeight: 1.25 }}>
            युवा भारत — इतिहास की<br />सबसे <span style={{ color: WARN }}>कर्ज़दार पीढ़ी</span>
          </div>

          {/* Rubber stamp verdict */}
          <div style={{
            position: 'absolute', left: '50%', top: '32%',
            transform: `translate(-50%, -50%) rotate(-12deg) scale(${1.6 - stamp * 0.6})`,
            opacity: stamp, border: `10px solid ${SAND}`, borderRadius: 18, padding: '16px 50px',
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 72, fontWeight: 800, color: SAND, background: 'rgba(0,0,0,0.35)',
          }}>
            बिना संदर्भ — अधूरा सच
          </div>
        </div>

        {/* Context strip: Dec 2025 data */}
        <div style={{ position: 'absolute', bottom: 110, display: 'flex', gap: 30, opacity: sp(480), transform: `translateY(${(1 - sp(480)) * 60}px)` }}>
          <div style={{ background: CREAM, color: INK, borderRadius: 16, padding: '24px 44px', border: `4px solid ${INK}`, fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 38, fontWeight: 800 }}>
            दिसंबर 2025 डेटा
          </div>
          <div style={{ background: DARK, color: CREAM, borderRadius: 16, padding: '24px 44px', border: `4px solid ${SAND}`, fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 38, fontWeight: 700 }}>
            गंभीर बकाया दरें — नियंत्रण में, पिछले वर्ष से बेहतर
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S04_AdhuraSach;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S05_TootaCashflow',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S05_TootaCashflow: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#664343'; const DARK = '#4A2F2F'; const CREAM = '#EEEEEE'; const INK = '#000000'; const SAND = '#E8B984'; const WARN = '#D96B6B';

  const v1 = interpolate(frame, [80, 170], [0, 1.1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const v2 = interpolate(frame, [140, 230], [0, 1.9], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Crack reveal
  const crack = interpolate(frame, [330, 380], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad) });
  const crackShake = frame >= 335 && frame < 355 ? Math.sin((frame - 335) * 2) * 9 * (1 - (frame - 335) / 20) : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `radial-gradient(circle at ${60 + Math.cos(frame * 0.01) * 8}% 60%, #7D5656 0%, ${BG} 52%, ${DARK} 100%)` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: -200, top: 100 + Math.sin(frame * 0.011) * 40, width: 620, height: 620, borderRadius: '50%', border: `60px solid ${INK}`, opacity: 0.05 }} />
      <div style={{ position: 'absolute', right: -160, top: -180, width: 560, height: 560, background: SAND, opacity: 0.04, transform: `rotate(${frame * 0.05}deg)`, borderRadius: 70 }} />

      {/* Motion texture: grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.045 }}>
        {Array.from({ length: 13 }).map((_, i) => (
          <line key={i} x1={i * 160} y1={0} x2={i * 160} y2={1080} stroke={CREAM} strokeWidth={1} />
        ))}
        {Array.from({ length: 8 }).map((_, i) => (
          <line key={'h' + i} x1={0} y1={i * 160 + (frame * 0.25) % 160} x2={1920} y2={i * 160 + (frame * 0.25) % 160} stroke={CREAM} strokeWidth={1} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const x = seededRandom(i + 2) * 1920;
        const y = ((seededRandom(i + 44) * 1080 + frame * (0.35 + seededRandom(i + 21) * 0.4)) % 1140) - 30;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 5, height: 5, borderRadius: '50%', background: CREAM, opacity: 0.2 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${crackShake}px)`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Two big stat counters */}
        <div style={{ display: 'flex', gap: 90, marginBottom: 90 }}>
          {[{ label: 'पर्सनल लोन — 90+ दिन बकाया', val: v1, delay: 20 }, { label: 'क्रेडिट कार्ड — 90+ दिन बकाया', val: v2, delay: 70 }].map((s, i) => (
            <div key={i} style={{ transform: `scale(${sp(s.delay)})`, background: i === 0 ? CREAM : DARK, border: `5px solid ${i === 0 ? INK : SAND}`, borderRadius: 24, padding: '44px 70px', textAlign: 'center', boxShadow: '0 24px 60px rgba(0,0,0,0.4)' }}>
              <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 140, fontWeight: 800, color: i === 0 ? INK : SAND, lineHeight: 1 }}>
                {s.val.toFixed(1)}%
              </div>
              <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 34, fontWeight: 700, color: i === 0 ? '#8C5E5E' : CREAM, marginTop: 18 }}>{s.label}</div>
            </div>
          ))}
        </div>

        {/* Crack line across the screen */}
        <svg width="1920" height="1080" style={{ position: 'absolute', pointerEvents: 'none' }}>
          <path
            d="M 0 640 L 300 660 L 480 610 L 700 680 L 960 620 L 1200 690 L 1450 630 L 1700 670 L 1920 640"
            fill="none" stroke={WARN} strokeWidth={7} strokeLinejoin="round"
            strokeDasharray={2600} strokeDashoffset={2600 * (1 - crack)} opacity={0.9}
          />
        </svg>

        {/* Outside vs inside contrast */}
        <div style={{ display: 'flex', gap: 60, opacity: sp(420), transform: `translateY(${(1 - sp(420)) * 50}px)` }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 54, fontWeight: 800, color: CREAM }}>
            बाहर: सब सामान्य
          </div>
          <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 54, color: SAND }}>|</div>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 54, fontWeight: 800, color: WARN }}>
            भीतर: कैश फ्लो टूट चुका
          </div>
        </div>
        <div style={{ marginTop: 40, opacity: sp(560), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 38, fontWeight: 700, color: CREAM, textAlign: 'center', maxWidth: 1300 }}>
          किस्त समय पर — लेकिन बचत रुकी, माता-पिता से पैसा, कार्ड से किराना, या दूसरा लोन
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S05_TootaCashflow;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S06_RohanKaLedger',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S06_RohanKaLedger: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#664343'; const DARK = '#4A2F2F'; const CREAM = '#EEEEEE'; const INK = '#000000'; const SAND = '#E8B984'; const WARN = '#D96B6B';

  const rows = [
    { label: 'फोन EMI', amt: 4299 },
    { label: 'लैपटॉप EMI', amt: 5500 },
    { label: 'बाइक किस्त', amt: 6200 },
    { label: 'पर्सनल लोन EMI', amt: 7800 },
    { label: 'कार्ड न्यूनतम भुगतान', amt: 3500 },
    { label: 'BNPL किस्तें (2)', amt: 2000 },
  ];

  const camZoom = interpolate(frame, [0, 900], [1, 1.06], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${100 + Math.sin(frame * 0.009) * 12}deg, ${DARK} 0%, ${BG} 45%, #7D5656 100%)` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', right: -220 + Math.sin(frame * 0.01) * 30, top: 200, width: 680, height: 680, borderRadius: '50%', background: CREAM, opacity: 0.035 }} />
      <div style={{ position: 'absolute', left: -140, top: -180, width: 500, height: 500, background: INK, opacity: 0.06, transform: `rotate(${45 + frame * 0.04}deg)` }} />

      {/* Motion texture: ledger row lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <line key={i} x1={0} y1={i * 96} x2={1920} y2={i * 96} stroke={CREAM} strokeWidth={1} strokeDasharray="10 14" />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const x = seededRandom(i + 6) * 1920;
        const y = ((seededRandom(i + 55) * 1080 + frame * (0.3 + seededRandom(i + 15) * 0.4)) % 1140) - 30;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 4, height: 4, borderRadius: '50%', background: SAND, opacity: 0.28 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, flexDirection: 'row', justifyContent: 'center', alignItems: 'center', gap: 100 }}>

        {/* Profile card */}
        <div style={{ transform: `scale(${sp(15)}) rotate(-2deg)`, width: 460, background: CREAM, borderRadius: 26, border: `5px solid ${INK}`, boxShadow: '0 30px 70px rgba(0,0,0,0.45)', overflow: 'hidden' }}>
          <div style={{ background: INK, color: CREAM, padding: '26px 36px', fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 62 }}>रोहन</div>
          <div style={{ padding: '30px 36px' }}>
            {[['उम्र', '27 साल'], ['इन-हैंड', '₹45,000'], ['नौकरी', 'ठीक-ठाक'], ['परिवार की नज़र में', 'बेटा सेट है']].map((r, i) => (
              <div key={i} style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 20, opacity: sp(60 + i * 20), transform: `translateX(${(1 - sp(60 + i * 20)) * -40}px)` }}>
                <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 34, fontWeight: 700, color: '#8C5E5E' }}>{r[0]}</span>
                <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 34, fontWeight: 800, color: INK }}>{r[1]}</span>
              </div>
            ))}
          </div>
        </div>

        {/* EMI ledger */}
        <div style={{ width: 780 }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 48, fontWeight: 800, color: SAND, marginBottom: 26, opacity: sp(120) }}>
            हर महीने की देनदारियाँ
          </div>
          {rows.map((r, i) => {
            const d = 160 + i * 60;
            const s = sp(d, { damping: 11 });
            const amt = Math.round(interpolate(frame, [d + 6, d + 40], [0, r.amt], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
            return (
              <div key={i} style={{
                display: 'flex', justifyContent: 'space-between', alignItems: 'center',
                transform: `translateX(${(1 - s) * 400}px)`, opacity: s,
                background: i % 2 === 0 ? 'rgba(238,238,238,0.95)' : 'rgba(238,238,238,0.85)',
                borderLeft: `12px solid ${i >= 4 ? WARN : SAND}`, borderRadius: 12, padding: '18px 32px', marginBottom: 14,
              }}>
                <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 38, fontWeight: 800, color: INK }}>{r.label}</span>
                <span style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 42, fontWeight: 800, color: INK }}>₹{amt.toLocaleString('en-IN')}</span>
              </div>
            );
          })}
          <div style={{ opacity: sp(600), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 34, fontWeight: 700, color: CREAM, marginTop: 10 }}>
            अलग-अलग देखो — कोई रकम "खतरनाक" नहीं लगती...
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S06_RohanKaLedger;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S07_PainsathPratishat',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S07_PainsathPratishat: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#664343'; const DARK = '#4A2F2F'; const CREAM = '#EEEEEE'; const INK = '#000000'; const SAND = '#E8B984'; const WARN = '#D96B6B';

  const totalAt = 180;
  const total = Math.round(interpolate(frame, [totalAt, totalAt + 60], [0, 29299], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
  const slam = sp(totalAt, { damping: 9, stiffness: 220 });
  const shake = frame >= totalAt + 55 && frame < totalAt + 75 ? Math.sin((frame - totalAt - 55) * 2.4) * 11 * (1 - (frame - totalAt - 55) / 20) : 0;

  // Donut fills to 65%
  const pct = interpolate(frame, [340, 460], [0, 65], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const R = 190; const CIRC = 2 * Math.PI * R;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `radial-gradient(circle at 50% ${35 + Math.sin(frame * 0.011) * 8}%, #7D5656 0%, ${BG} 50%, ${DARK} 100%)` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: -180, top: -160, width: 620, height: 620, borderRadius: '50%', border: `80px solid ${SAND}`, opacity: 0.04, transform: `rotate(${frame * 0.06}deg)` }} />
      <div style={{ position: 'absolute', right: -200, bottom: -240 + Math.sin(frame * 0.01) * 30, width: 700, height: 700, background: INK, opacity: 0.06, borderRadius: 90 }} />

      {/* Motion texture: circles */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {[120, 260, 400].map((r, i) => (
          <circle key={i} cx={340} cy={860} r={r + Math.sin(frame * 0.04 + i) * 6} fill="none" stroke={CREAM} strokeWidth={1.5} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const x = seededRandom(i + 9) * 1920;
        const y = ((seededRandom(i + 71) * 1080 + frame * (0.35 + seededRandom(i + 23) * 0.4)) % 1140) - 30;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 5, height: 5, borderRadius: '50%', background: CREAM, opacity: 0.22 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${shake}px)`, flexDirection: 'row', justifyContent: 'center', alignItems: 'center', gap: 140 }}>

        {/* Sum stack -> slammed total */}
        <div style={{ textAlign: 'center' }}>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 40, color: CREAM, opacity: sp(20), lineHeight: 1.7 }}>
            4,299 + 5,500 + 6,200<br />+ 7,800 + 3,500 + 2,000
          </div>
          <div style={{ width: 520, height: 6, background: SAND, margin: '26px auto', transform: `scaleX(${sp(140)})` }} />
          <div style={{ transform: `scale(${slam})`, fontFamily: "'JetBrains Mono', monospace", fontSize: 150, fontWeight: 800, color: WARN, textShadow: '0 12px 44px rgba(0,0,0,0.5)', lineHeight: 1 }}>
            ₹{total.toLocaleString('en-IN')}
          </div>
          <div style={{ opacity: sp(totalAt + 70), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 40, fontWeight: 800, color: CREAM, marginTop: 20 }}>
            हर महीने — पक्की देनदारी
          </div>
        </div>

        {/* 65% donut of salary */}
        <div style={{ position: 'relative', transform: `scale(${sp(320)})` }}>
          <svg width="480" height="480">
            <circle cx={240} cy={240} r={R} fill="none" stroke={DARK} strokeWidth={54} />
            <circle
              cx={240} cy={240} r={R} fill="none" stroke={WARN} strokeWidth={54} strokeLinecap="round"
              strokeDasharray={CIRC} strokeDashoffset={CIRC * (1 - pct / 100)}
              transform="rotate(-90 240 240)"
            />
          </svg>
          <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center' }}>
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 105, fontWeight: 800, color: CREAM, lineHeight: 1 }}>{Math.round(pct)}%</div>
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 30, fontWeight: 700, color: SAND, marginTop: 8 }}>तनख़्वाह की EMI</div>
          </div>
          <div style={{ position: 'absolute', bottom: -80, width: '100%', textAlign: 'center', opacity: sp(500), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 34, fontWeight: 700, color: CREAM }}>
            महीने की शुरुआत में ही गया
          </div>
        </div>
      </AbsoluteFill>

      {/* Bottom squeeze line */}
      <div style={{ position: 'absolute', bottom: 70, width: '100%', textAlign: 'center', opacity: sp(600), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 38, fontWeight: 700, color: SAND }}>
        बचा हुआ: किराया + पेट्रोल + बिजली + खाना + मोबाइल... सबके लिए
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S07_PainsathPratishat;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S08_KeematKaJaadu',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S08_KeematKaJaadu: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#664343'; const DARK = '#4A2F2F'; const CREAM = '#EEEEEE'; const INK = '#000000'; const SAND = '#E8B984'; const WARN = '#D96B6B';

  // Beat 1: 50,000 shrinks away, 4,167 grows (0-380). Beat 2: 1,20,000 travel example (410+)
  const vanish = interpolate(frame, [120, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const easyIn = sp(190, { damping: 10 });
  const beat2 = interpolate(frame, [400, 435], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const camZoom = interpolate(frame, [0, 900], [1.05, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${200 + Math.sin(frame * 0.01) * 12}deg, #7D5656 0%, ${BG} 45%, ${DARK} 100%)` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: 760 + Math.sin(frame * 0.012) * 50, top: -260, width: 640, height: 640, borderRadius: '50%', background: CREAM, opacity: 0.035 }} />
      <div style={{ position: 'absolute', left: -200, bottom: -200, width: 560, height: 560, background: SAND, opacity: 0.05, transform: `rotate(${frame * 0.045}deg)`, borderRadius: 60 }} />

      {/* Motion texture: diagonal lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <line key={i} x1={i * 200 - 300 - (frame * 0.35) % 200} y1={0} x2={i * 200 - 100 - (frame * 0.35) % 200} y2={1080} stroke={CREAM} strokeWidth={1.5} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const x = seededRandom(i + 12) * 1920;
        const y = ((seededRandom(i + 81) * 1080 + frame * (0.3 + seededRandom(i + 27) * 0.5)) % 1140) - 30;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 4, height: 4, background: CREAM, opacity: 0.22, transform: `rotate(45deg)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, justifyContent: 'center', alignItems: 'center' }}>

        <div style={{ position: 'absolute', top: 110, opacity: sp(5), fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 80, color: CREAM, textAlign: 'center' }}>
          EMI का सबसे पुराना जादू
        </div>

        {/* BEAT 1: price swap trick */}
        <div style={{ opacity: 1 - beat2, position: 'absolute', display: 'flex', alignItems: 'center', gap: 120, marginTop: 40 }}>
          <div style={{ textAlign: 'center', transform: `scale(${1 - vanish * 0.45}) translateY(${vanish * -60}px)`, opacity: 1 - vanish * 0.65 }}>
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 120, fontWeight: 800, color: CREAM, textDecoration: vanish > 0.5 ? 'line-through' : 'none', textDecorationColor: WARN, textDecorationThickness: 10 }}>
              ₹50,000
            </div>
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 42, fontWeight: 800, color: WARN, marginTop: 12 }}>महंगा लगता है</div>
          </div>

          <svg width="140" height="90" style={{ opacity: sp(150) }}>
            <line x1={0} y1={45} x2={100} y2={45} stroke={SAND} strokeWidth={8} />
            <polygon points="100,25 140,45 100,65" fill={SAND} />
          </svg>

          <div style={{ textAlign: 'center', transform: `scale(${easyIn})` }}>
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 150, fontWeight: 800, color: SAND, textShadow: '0 12px 44px rgba(0,0,0,0.5)' }}>
              ₹4,167
            </div>
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 46, fontWeight: 800, color: CREAM, marginTop: 12 }}>
              "सिर्फ़" महीना — आसान लगता है
            </div>
          </div>
        </div>

        {/* BEAT 2: travel example */}
        <div style={{ opacity: beat2, transform: `translateY(${(1 - beat2) * 60}px)`, textAlign: 'center', marginTop: 60 }}>
          <div style={{ display: 'flex', gap: 50, justifyContent: 'center' }}>
            <div style={{ background: CREAM, color: INK, borderRadius: 20, border: `5px solid ${INK}`, padding: '36px 54px' }}>
              <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 74, fontWeight: 800 }}>₹1,20,000</div>
              <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 34, fontWeight: 800, color: WARN, marginTop: 10 }}>यात्रा — "ग़ैर-ज़िम्मेदार"</div>
            </div>
            <div style={{ background: DARK, color: CREAM, borderRadius: 20, border: `5px solid ${SAND}`, padding: '36px 54px' }}>
              <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 74, fontWeight: 800, color: SAND }}>₹10,000 × 12</div>
              <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 34, fontWeight: 800, marginTop: 10 }}>किस्तें — "संभालने योग्य"</div>
            </div>
          </div>
          <div style={{ marginTop: 50, opacity: sp(540), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 50, fontWeight: 800, color: CREAM }}>
            वस्तु वही, कीमत वही — <span style={{ color: SAND }}>सिर्फ़ प्रस्तुति बदली, निर्णय बदल गया</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S08_KeematKaJaadu;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S09_DoSawaal',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S09_DoSawaal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#664343'; const DARK = '#4A2F2F'; const CREAM = '#EEEEEE'; const INK = '#000000'; const SAND = '#E8B984'; const WARN = '#D96B6B';

  const cardL = sp(60, { damping: 13 });
  const cardR = sp(160, { damping: 13 });
  const vsPop = sp(260, { damping: 8, stiffness: 240 });
  const payoff = sp(480);
  const camOrbit = Math.sin(frame * 0.006) * 12;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `radial-gradient(ellipse at ${50 + Math.sin(frame * 0.009) * 12}% 50%, #7D5656 0%, ${BG} 48%, ${DARK} 100%)` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: 860, top: -200, width: 200, height: 1500, background: CREAM, opacity: 0.03, transform: `rotate(${18 + Math.sin(frame * 0.01) * 2}deg)` }} />
      <div style={{ position: 'absolute', right: -180, top: 640, width: 540, height: 540, borderRadius: '50%', border: `60px solid ${SAND}`, opacity: 0.04, transform: `scale(${1 + Math.sin(frame * 0.013) * 0.05})` }} />

      {/* Motion texture: vertical lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.045 }}>
        {Array.from({ length: 16 }).map((_, i) => (
          <line key={i} x1={i * 128 + (frame * 0.15) % 128} y1={0} x2={i * 128 + (frame * 0.15) % 128} y2={1080} stroke={CREAM} strokeWidth={1} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => {
        const x = seededRandom(i + 14) * 1920;
        const y = ((seededRandom(i + 91) * 1080 + frame * (0.3 + seededRandom(i + 31) * 0.4)) % 1140) - 30;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 5, height: 5, borderRadius: '50%', background: SAND, opacity: 0.25 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camOrbit}px)`, justifyContent: 'center', alignItems: 'center' }}>

        <div style={{ position: 'absolute', top: 100, opacity: sp(5), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 52, fontWeight: 800, color: CREAM }}>
          खरीदने से पहले — दो सवाल
        </div>

        <div style={{ display: 'flex', alignItems: 'center', gap: 70 }}>
          {/* Question 1 — the trap question */}
          <div style={{
            transform: `translateX(${(1 - cardL) * -500}px) rotate(-3deg)`, opacity: cardL,
            width: 620, background: CREAM, border: `6px solid ${INK}`, borderRadius: 26, padding: '54px 48px',
            boxShadow: '0 30px 70px rgba(0,0,0,0.45)', textAlign: 'center',
          }}>
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 30, fontWeight: 800, color: '#8C5E5E', marginBottom: 20 }}>SAWAAL 01</div>
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 56, fontWeight: 800, color: INK, lineHeight: 1.35 }}>
              क्या मैं इसकी<br />EMI भर सकता हूँ?
            </div>
            <div style={{ marginTop: 26, fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 32, fontWeight: 700, color: WARN }}>दिमाग का शॉर्टकट</div>
          </div>

          {/* VS badge */}
          <div style={{ transform: `scale(${vsPop}) rotate(${(1 - vsPop) * 180}deg)`, width: 130, height: 130, borderRadius: '50%', background: SAND, border: `6px solid ${INK}`, display: 'flex', justifyContent: 'center', alignItems: 'center', fontFamily: "'Yantra One', sans-serif", fontSize: 52, color: INK, boxShadow: '0 16px 40px rgba(0,0,0,0.4)' }}>
            VS
          </div>

          {/* Question 2 — the honest question */}
          <div style={{
            transform: `translateX(${(1 - cardR) * 500}px) rotate(3deg)`, opacity: cardR,
            width: 620, background: DARK, border: `6px solid ${SAND}`, borderRadius: 26, padding: '54px 48px',
            boxShadow: '0 30px 70px rgba(0,0,0,0.45)', textAlign: 'center',
          }}>
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 30, fontWeight: 800, color: SAND, marginBottom: 20 }}>SAWAAL 02</div>
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 56, fontWeight: 800, color: CREAM, lineHeight: 1.35 }}>
              पूरी कीमत अभी देनी पड़ती,<br />तो क्या खरीदता?
            </div>
            <div style={{ marginTop: 26, fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 32, fontWeight: 700, color: SAND }}>असली इम्तिहान</div>
          </div>
        </div>

        {/* Payoff */}
        <div style={{ position: 'absolute', bottom: 100, opacity: payoff, transform: `translateY(${(1 - payoff) * 50}px)`, textAlign: 'center' }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 48, fontWeight: 800, color: CREAM }}>
            दोनों जवाब अलग हैं? तो तुम ज़रूरत नहीं — <span style={{ color: WARN }}>भुगतान की सुविधा खरीद रहे हो</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S09_DoSawaal;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S10_BarahMahineLock',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S10_BarahMahineLock: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#664343'; const DARK = '#4A2F2F'; const CREAM = '#EEEEEE'; const INK = '#000000'; const SAND = '#E8B984'; const WARN = '#D96B6B';

  const months = ['जन', 'फ़र', 'मार्च', 'अप्रै', 'मई', 'जून', 'जुला', 'अग', 'सित', 'अक्टू', 'नव', 'दिस'];
  const camZoom = interpolate(frame, [0, 900], [1.07, 1], { extrapolateRight: 'clamp' });

  const LockIcon = ({ size, color }: { size: number; color: string }) => (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="none">
      <rect x="5" y="10" width="14" height="10" rx="2" fill={color} />
      <path d="M8 10V7a4 4 0 0 1 8 0v3" stroke={color} strokeWidth="2.4" fill="none" />
    </svg>
  );

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${180 + Math.sin(frame * 0.009) * 14}deg, ${DARK} 0%, ${BG} 55%, #7D5656 100%)` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: -220, top: 300 + Math.sin(frame * 0.01) * 40, width: 640, height: 640, borderRadius: '50%', background: CREAM, opacity: 0.035 }} />
      <div style={{ position: 'absolute', right: -160, top: -200, width: 520, height: 520, background: INK, opacity: 0.06, transform: `rotate(${frame * 0.05}deg)`, borderRadius: 50 }} />

      {/* Motion texture: grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.045 }}>
        {Array.from({ length: 13 }).map((_, i) => (
          <line key={i} x1={i * 160} y1={0} x2={i * 160} y2={1080} stroke={CREAM} strokeWidth={1} />
        ))}
        {Array.from({ length: 8 }).map((_, i) => (
          <line key={'h' + i} x1={0} y1={i * 160} x2={1920} y2={i * 160} stroke={CREAM} strokeWidth={1} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const x = seededRandom(i + 16) * 1920;
        const y = ((seededRandom(i + 101) * 1080 + frame * (0.32 + seededRandom(i + 37) * 0.4)) % 1140) - 30;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 5, height: 5, borderRadius: '50%', background: CREAM, opacity: 0.2 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, justifyContent: 'center', alignItems: 'center' }}>

        <div style={{ position: 'absolute', top: 100, textAlign: 'center', opacity: sp(5) }}>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 90, color: CREAM }}>
            EMI ने कीमत को <span style={{ color: SAND }}>समय में काट दिया</span>
          </div>
        </div>

        {/* 12-month timeline getting locked one by one */}
        <div style={{ display: 'flex', gap: 18, marginTop: 40 }}>
          {months.map((m, i) => {
            const d = 90 + i * 32;
            const s = sp(d, { damping: 10 });
            const locked = frame > d + 14;
            return (
              <div key={i} style={{
                transform: `translateY(${(1 - s) * 120}px) scale(${s})`,
                width: 128, height: 220, borderRadius: 16,
                background: locked ? DARK : CREAM,
                border: `4px solid ${locked ? WARN : INK}`,
                display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center', gap: 14,
                boxShadow: '0 18px 44px rgba(0,0,0,0.4)',
                transition: 'background 0.2s',
              }}>
                <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 30, fontWeight: 800, color: locked ? CREAM : INK }}>{m}</div>
                <div style={{ opacity: locked ? 1 : 0, transform: `scale(${locked ? 1 : 0.4})` }}>
                  <LockIcon size={44} color={WARN} />
                </div>
                <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 22, fontWeight: 700, color: locked ? SAND : '#8C5E5E' }}>
                  {locked ? 'EMI' : 'FREE'}
                </div>
              </div>
            );
          })}
        </div>

        {/* Payoff lines */}
        <div style={{ position: 'absolute', bottom: 150, textAlign: 'center', opacity: sp(540), transform: `translateY(${(1 - sp(540)) * 50}px)` }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 50, fontWeight: 800, color: CREAM }}>
            फोन खरीदा — साथ में <span style={{ color: WARN }}>12 महीनों का लचीलापन भी बेच दिया</span>
          </div>
          <div style={{ marginTop: 22, opacity: sp(640), fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 36, fontWeight: 700, color: SAND }}>
            छुट्टी मनाई = भविष्य की तनख़्वाह पर दावा | कपड़े लिए = अगले महीने की मजबूरी
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S10_BarahMahineLock;
```

