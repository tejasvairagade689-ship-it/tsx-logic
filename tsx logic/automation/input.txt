```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Question mark grand entrance
  const qScale = sp(20, { damping: 8, stiffness: 90 });
  const qRot = interpolate(sp(20), [0, 1], [-25, 0]);

  // Headline reveal
  const headlineY = interpolate(sp(50), [0, 1], [80, 0]);
  const headlineOp = sp(50);

  // Sub-text
  const subOp = sp(110);
  const subY = interpolate(sp(110), [0, 1], [40, 0]);

  // Money note slide
  const noteX = interpolate(sp(170), [0, 1], [-300, 0]);
  const noteOp = sp(170);

  // Arrow + amount
  const arrowScale = sp(220);
  const amountOp = sp(260);

  // Doubt cloud
  const cloudOp = sp(320);
  const cloudScale = sp(320, { damping: 10 });

  // Final hook line
  const hookOp = sp(380);
  const hookY = interpolate(sp(380), [0, 1], [30, 0]);

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  // Gradient breathing
  const gradShift = Math.sin(frame * 0.02) * 8;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at ${50 + gradShift}% ${40 - gradShift}%, #FFFFFF 0%, #EEEEEE 45%, #DCDCDC 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        <div
          style={{
            position: 'absolute',
            top: -200,
            right: -150,
            width: 700,
            height: 700,
            borderRadius: '50%',
            border: '3px solid #091413',
            opacity: 0.06,
            transform: `rotate(${frame * 0.15}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: -180,
            left: -120,
            width: 520,
            height: 520,
            backgroundColor: '#FF6D1F',
            opacity: 0.08,
            transform: `rotate(${45 + frame * 0.1}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            top: '40%',
            left: '8%',
            width: 280,
            height: 280,
            border: '2px solid #FF6D1F',
            opacity: 0.1,
            transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture - grid */}
      <svg
        width="100%"
        height="100%"
        style={{ position: 'absolute', opacity: 0.05, transform: `translateY(${Math.sin(frame * 0.03) * 10}px)` }}
      >
        <defs>
          <pattern id="grid01" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#091413" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid01)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 1);
        const r2 = seededRandom(i + 50);
        const xPos = r * 1920;
        const yPos = (r2 * 1080 + frame * (0.4 + r * 0.6)) % 1080;
        const size = 4 + r * 8;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: xPos,
              top: yPos,
              width: size,
              height: size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#FF6D1F' : '#091413',
              opacity: 0.18,
            }}
          />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Giant Question Mark */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 80,
            fontSize: 340,
            fontFamily: 'Yantra One, serif',
            color: '#FF6D1F',
            fontWeight: 900,
            lineHeight: 1,
            transform: `scale(${qScale}) rotate(${qRot}deg)`,
            transformOrigin: 'center',
            opacity: 0.95,
          }}
        >
          ?
        </div>

        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 180,
            left: 380,
            right: 100,
            opacity: headlineOp,
            transform: `translateY(${headlineY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 119,
              color: '#091413',
              fontWeight: 900,
              lineHeight: 1.05,
              letterSpacing: '-2px',
            }}
          >
            एक सवाल
          </div>
          <div
            style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 119,
              color: '#FF6D1F',
              fontWeight: 900,
              lineHeight: 1.05,
              letterSpacing: '-2px',
            }}
          >
            पूछता हूँ...
          </div>
        </div>

        {/* Sub line */}
        <div
          style={{
            position: 'absolute',
            top: 470,
            left: 100,
            right: 100,
            opacity: subOp,
            transform: `translateY(${subY}px)`,
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 60,
            color: '#091413',
            fontWeight: 600,
            lineHeight: 1.3,
          }}
        >
          हर महीने जेब से <span style={{ color: '#FF6D1F', fontWeight: 800 }}>₹2-3 हजार</span> कटते हैं...
        </div>

        {/* Money note visual */}
        <div
          style={{
            position: 'absolute',
            top: 600,
            left: 110,
            opacity: noteOp,
            transform: `translateX(${noteX}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 30,
          }}
        >
          <div
            style={{
              width: 240,
              height: 130,
              backgroundColor: '#FF6D1F',
              borderRadius: 12,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Yantra One, serif',
              fontSize: 76,
              color: '#091413',
              fontWeight: 900,
              boxShadow: '0 12px 30px rgba(9,20,19,0.25)',
              transform: `rotate(${-4 + Math.sin(frame * 0.05) * 1.5}deg)`,
            }}
          >
            ₹2000
          </div>

          {/* Arrow */}
          <div style={{ transform: `scale(${arrowScale})`, transformOrigin: 'left center' }}>
            <svg width="180" height="60">
              <line x1="0" y1="30" x2="150" y2="30" stroke="#091413" strokeWidth="6" strokeDasharray="12 8" />
              <polygon points="150,15 180,30 150,45" fill="#091413" />
            </svg>
          </div>

          {/* Question doubt */}
          <div
            style={{
              opacity: amountOp,
              fontFamily: 'Yantra One, serif',
              fontSize: 95,
              color: '#091413',
              fontWeight: 900,
            }}
          >
            ???
          </div>
        </div>

        {/* Doubt cloud / 6 months later */}
        <div
          style={{
            position: 'absolute',
            top: 780,
            right: 120,
            opacity: cloudOp,
            transform: `scale(${cloudScale})`,
            transformOrigin: 'center',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'flex-end',
            gap: 8,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins, sans-serif',
              fontSize: 36,
              color: '#091413',
              opacity: 0.6,
              fontWeight: 500,
            }}
          >
            6 महीने बाद...
          </div>
          <div
            style={{
              padding: '20px 36px',
              backgroundColor: '#091413',
              color: '#EEEEEE',
              fontFamily: 'Yantra One, serif',
              fontSize: 60,
              fontWeight: 700,
              borderRadius: 14,
              transform: `rotate(${2 + Math.sin(frame * 0.06) * 1}deg)`,
            }}
          >
            पैसा गड्ढे में?
          </div>
        </div>

        {/* Hook line bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 100,
            right: 100,
            opacity: hookOp,
            transform: `translateY(${hookY}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <div
            style={{
              width: 12,
              height: 80,
              backgroundColor: '#FF6D1F',
            }}
          />
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 51,
              color: '#091413',
              fontWeight: 700,
            }}
          >
            तो आप क्या <span style={{ color: '#FF6D1F' }}>करोगे?</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(9,20,19,0.18) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#091413',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene01;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Stamp "BAND KAR DOGE NA?"
  const stampScale = interpolate(sp(15, { damping: 7, stiffness: 100 }), [0, 1], [3, 1]);
  const stampOp = sp(15);
  const stampRot = interpolate(sp(15), [0, 1], [-15, -6]);

  // Map of India shape
  const mapOp = sp(80);
  const mapScale = sp(80, { damping: 10 });

  // Source bar
  const sourceX = interpolate(sp(150), [0, 1], [-500, 0]);
  const sourceOp = sp(150);

  // 100% giant
  const bigNumOp = sp(220);
  const bigNumScale = sp(220, { damping: 9, stiffness: 110 });

  // Shake on impact
  const shakeFrame = Math.max(0, frame - 220);
  const shake = shakeFrame < 20 ? Math.sin(shakeFrame * 2.5) * (1 - shakeFrame / 20) * 8 : 0;

  // Arrow up
  const arrowOp = sp(280);
  const arrowY = interpolate(sp(280), [0, 1], [60, 0]);

  // Months badge
  const monthOp = sp(340);

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [0, 0.5]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #EEEEEE 0%, #E2E2E2 50%, #D5D5D5 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '15%',
          right: '5%',
          width: 600,
          height: 600,
          backgroundColor: '#FF6D1F',
          opacity: 0.05,
          borderRadius: 40,
          transform: `rotate(${20 + frame * 0.08}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '-10%',
          left: '20%',
          width: 800,
          height: 400,
          border: '4px solid #091413',
          opacity: 0.05,
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />

      {/* Motion texture - diagonal lines */}
      <svg
        width="100%"
        height="100%"
        style={{ position: 'absolute', opacity: 0.06 }}
      >
        <defs>
          <pattern id="diag02" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform={`rotate(45) translate(${frame * 0.5}, 0)`}>
            <line x1="0" y1="0" x2="0" y2="40" stroke="#091413" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag02)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 10);
        const r2 = seededRandom(i + 88);
        const xPos = (r * 1920 + frame * 0.5) % 1920;
        const yPos = r2 * 1080;
        return (
          <div
            key={`p2-${i}`}
            style={{
              position: 'absolute',
              left: xPos,
              top: yPos,
              width: 6,
              height: 6,
              backgroundColor: '#FF6D1F',
              opacity: 0.2,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translate(${shake}px, ${shake * 0.5}px) rotate(${camRot}deg)` }}>
        {/* STAMP - बंद कर दोगे ना? */}
        <div
          style={{
            position: 'absolute',
            top: 100,
            left: 90,
            opacity: stampOp,
            transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
            transformOrigin: 'left top',
            padding: '24px 50px',
            border: '8px solid #FF6D1F',
            color: '#FF6D1F',
            fontFamily: 'Yantra One, serif',
            fontSize: 102,
            fontWeight: 900,
            letterSpacing: '-1px',
            backgroundColor: 'rgba(238,238,238,0.7)',
          }}
        >
          बंद कर दोगे ना?
        </div>

        {/* Map of India (stylized) */}
        <div
          style={{
            position: 'absolute',
            top: 290,
            left: 110,
            opacity: mapOp,
            transform: `scale(${mapScale})`,
            transformOrigin: 'left top',
            display: 'flex',
            alignItems: 'center',
            gap: 36,
          }}
        >
          <svg width="220" height="260" viewBox="0 0 220 260">
            <path
              d="M110 10 L160 40 L195 100 L185 160 L150 220 L110 250 L80 240 L60 200 L40 150 L30 100 L55 50 Z"
              fill="#091413"
              opacity="0.85"
            />
            <circle cx="110" cy="130" r="6" fill="#FF6D1F" />
            <circle cx="80" cy="80" r="4" fill="#FF6D1F" />
            <circle cx="150" cy="160" r="4" fill="#FF6D1F" />
            <circle cx="120" cy="200" r="4" fill="#FF6D1F" />
          </svg>
          <div>
            <div style={{ fontFamily: 'Yantra One, serif', fontSize: 76, color: '#091413', fontWeight: 900, lineHeight: 1 }}>
              पूरा
            </div>
            <div style={{ fontFamily: 'Yantra One, serif', fontSize: 102, color: '#FF6D1F', fontWeight: 900, lineHeight: 1 }}>
              हिंदुस्तान
            </div>
          </div>
        </div>

        {/* Source bar */}
        <div
          style={{
            position: 'absolute',
            top: 600,
            left: 110,
            opacity: sourceOp,
            transform: `translateX(${sourceX}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            padding: '14px 26px',
            backgroundColor: '#091413',
            borderRadius: 8,
          }}
        >
          <div style={{ width: 12, height: 12, backgroundColor: '#FF6D1F', borderRadius: '50%' }} />
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: '#EEEEEE', fontWeight: 600, letterSpacing: '1px' }}>
            SOURCE: MFI-AMFI DATA
          </div>
        </div>

        {/* Giant 100%+ */}
        <div
          style={{
            position: 'absolute',
            top: 700,
            right: 100,
            opacity: bigNumOp,
            transform: `scale(${bigNumScale})`,
            transformOrigin: 'right center',
            textAlign: 'right',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 38,
              color: '#091413',
              fontWeight: 600,
              opacity: 0.7,
              marginBottom: -10,
            }}
          >
            SIP STOPPAGE RATIO
          </div>
          <div
            style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 221,
              color: '#FF6D1F',
              fontWeight: 900,
              lineHeight: 1,
              letterSpacing: '-6px',
              textShadow: '6px 6px 0 #091413',
            }}
          >
            100%+
          </div>
        </div>

        {/* Arrow up indicator */}
        <div
          style={{
            position: 'absolute',
            bottom: 280,
            left: 200,
            opacity: arrowOp,
            transform: `translateY(${arrowY}px)`,
          }}
        >
          <svg width="100" height="180">
            <line x1="50" y1="170" x2="50" y2="30" stroke="#FF6D1F" strokeWidth="10" />
            <polygon points="20,50 50,10 80,50" fill="#FF6D1F" />
          </svg>
        </div>

        {/* Months badge */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: 90,
            opacity: monthOp,
            display: 'flex',
            gap: 18,
          }}
        >
          <div
            style={{
              padding: '18px 32px',
              backgroundColor: '#091413',
              color: '#EEEEEE',
              fontFamily: 'Yantra One, serif',
              fontSize: 55,
              fontWeight: 700,
              borderRadius: 10,
            }}
          >
            मार्च 2026
          </div>
          <div
            style={{
              padding: '18px 32px',
              backgroundColor: '#FF6D1F',
              color: '#091413',
              fontFamily: 'Yantra One, serif',
              fontSize: 55,
              fontWeight: 700,
              borderRadius: 10,
            }}
          >
            अप्रैल 2026
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(9,20,19,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#091413',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Left column (NEW SIP)
  const leftOp = sp(20);
  const leftX = interpolate(sp(20), [0, 1], [-200, 0]);

  // Right column (CLOSED SIP)
  const rightOp = sp(40);
  const rightX = interpolate(sp(40), [0, 1], [200, 0]);

  // Bars growing
  const leftBarH = interpolate(sp(80, { damping: 14 }), [0, 1], [0, 280]);
  const rightBarH = interpolate(sp(110, { damping: 14 }), [0, 1], [0, 360]);

  // VS badge
  const vsScale = sp(160, { damping: 8, stiffness: 100 });

  // Warning line
  const warnOp = sp(260);
  const warnY = interpolate(sp(260), [0, 1], [40, 0]);

  // Bottom hook
  const hookOp = sp(360);
  const hookScale = sp(360, { damping: 9 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, #F2F2F2 0%, #EEEEEE 50%, #DADADA 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: -100,
          left: '40%',
          width: 500,
          height: 500,
          border: '4px solid #FF6D1F',
          borderRadius: '50%',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -100,
          right: -100,
          width: 600,
          height: 600,
          backgroundColor: '#091413',
          opacity: 0.04,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Texture - dots */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="dots03" width="35" height="35" patternUnits="userSpaceOnUse">
            <circle cx="17.5" cy="17.5" r="2" fill="#091413" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots03)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const r = seededRandom(i + 20);
        const r2 = seededRandom(i + 200);
        return (
          <div
            key={`p3-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (r2 * 1080 + frame * 0.6) % 1080,
              width: 5,
              height: 5,
              backgroundColor: i % 2 === 0 ? '#FF6D1F' : '#091413',
              opacity: 0.25,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* Title */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          fontFamily: 'Yantra One, serif',
          fontSize: 76,
          color: '#091413',
          fontWeight: 900,
          opacity: sp(5),
        }}
      >
        SIP खाते का <span style={{ color: '#FF6D1F' }}>हिसाब</span>
      </div>

      {/* LEFT - NEW */}
      <div
        style={{
          position: 'absolute',
          top: 230,
          left: 200,
          opacity: leftOp,
          transform: `translateX(${leftX}px)`,
          textAlign: 'center',
        }}
      >
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 36, color: '#091413', opacity: 0.6, fontWeight: 600, marginBottom: 20 }}>
          NEW SIP खुले
        </div>
        <div
          style={{
            width: 220,
            height: leftBarH,
            backgroundColor: '#091413',
            margin: '0 auto',
            borderRadius: '12px 12px 0 0',
            display: 'flex',
            alignItems: 'flex-start',
            justifyContent: 'center',
            paddingTop: 30,
            position: 'relative',
            transition: 'none',
          }}
        >
          <div style={{ fontFamily: 'Yantra One, serif', fontSize: 60, color: '#EEEEEE', fontWeight: 900 }}>
            52.82L
          </div>
        </div>
        <div
          style={{
            width: 280,
            height: 8,
            backgroundColor: '#091413',
            margin: '0 auto',
            borderRadius: 4,
          }}
        />
      </div>

      {/* VS */}
      <div
        style={{
          position: 'absolute',
          top: 480,
          left: '50%',
          transform: `translateX(-50%) scale(${vsScale}) rotate(${-8 + Math.sin(frame * 0.08) * 3}deg)`,
          padding: '20px 40px',
          backgroundColor: '#FF6D1F',
          color: '#091413',
          fontFamily: 'Yantra One, serif',
          fontSize: 76,
          fontWeight: 900,
          borderRadius: 16,
          boxShadow: '0 12px 30px rgba(255,109,31,0.4)',
        }}
      >
        VS
      </div>

      {/* RIGHT - CLOSED */}
      <div
        style={{
          position: 'absolute',
          top: 230,
          right: 200,
          opacity: rightOp,
          transform: `translateX(${rightX}px)`,
          textAlign: 'center',
        }}
      >
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 36, color: '#091413', opacity: 0.6, fontWeight: 600, marginBottom: 20 }}>
          CLOSED SIP
        </div>
        <div
          style={{
            width: 220,
            height: rightBarH,
            backgroundColor: '#FF6D1F',
            margin: '0 auto',
            borderRadius: '12px 12px 0 0',
            display: 'flex',
            alignItems: 'flex-start',
            justifyContent: 'center',
            paddingTop: 30,
            boxShadow: '0 0 40px rgba(255,109,31,0.4)',
          }}
        >
          <div style={{ fontFamily: 'Yantra One, serif', fontSize: 60, color: '#091413', fontWeight: 900 }}>
            53.38L
          </div>
        </div>
        <div
          style={{
            width: 280,
            height: 8,
            backgroundColor: '#091413',
            margin: '0 auto',
            borderRadius: 4,
          }}
        />
      </div>

      {/* Warning line */}
      <div
        style={{
          position: 'absolute',
          bottom: 280,
          left: 100,
          right: 100,
          opacity: warnOp,
          transform: `translateY(${warnY}px)`,
          textAlign: 'center',
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 51,
          color: '#091413',
          fontWeight: 700,
          lineHeight: 1.3,
        }}
      >
        बंद ज्यादा... <span style={{ color: '#FF6D1F', fontWeight: 800 }}>नए कम</span>
      </div>

      {/* Bottom hook */}
      <div
        style={{
          position: 'absolute',
          bottom: 130,
          left: '50%',
          transform: `translateX(-50%) scale(${hookScale})`,
          opacity: hookOp,
          padding: '18px 40px',
          border: '5px solid #091413',
          backgroundColor: '#EEEEEE',
        }}
      >
        <div style={{ fontFamily: 'Yantra One, serif', fontSize: 60, color: '#091413', fontWeight: 900 }}>
          ये मामूली नहीं है!
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(9,20,19,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#091413',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Date stamp
  const dateOp = sp(10);
  const dateY = interpolate(sp(10), [0, 1], [-50, 0]);

  // Big number 1 - 53.38
  const num1Scale = sp(50, { damping: 9, stiffness: 110 });
  const num1Counter = Math.floor(interpolate(frame, [50, 130], [0, 53.38], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) * 100) / 100;

  // Label 1
  const label1Op = sp(130);

  // VS divider
  const dividerScale = sp(170);

  // Big number 2 - 52.82
  const num2Scale = sp(210, { damping: 9, stiffness: 110 });
  const num2Counter = Math.floor(interpolate(frame, [210, 290], [0, 52.82], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) * 100) / 100;

  // Label 2
  const label2Op = sp(290);

  // Crowd running graphic
  const crowdOp = sp(340);
  const crowdX = Math.sin(frame * 0.05) * 10;

  // Bottom warning
  const warnOp = sp(380);

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${135 + Math.sin(frame * 0.02) * 10}deg, #EEEEEE 0%, #E5E5E5 60%, #D8D8D8 100%)`,
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '20%',
          left: -100,
          width: 400,
          height: 400,
          backgroundColor: '#FF6D1F',
          opacity: 0.06,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '10%',
          right: -50,
          width: 350,
          height: 350,
          border: '3px solid #091413',
          opacity: 0.06,
          transform: `rotate(${45 + frame * 0.1}deg)`,
        }}
      />

      {/* Texture - vertical lines */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="vlines04" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="30" y1="0" x2="30" y2="60" stroke="#091413" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#vlines04)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 30);
        const r2 = seededRandom(i + 300);
        return (
          <div
            key={`p4-${i}`}
            style={{
              position: 'absolute',
              left: (r * 1920 - frame * 0.4 + 1920) % 1920,
              top: r2 * 1080,
              width: 6,
              height: 6,
              backgroundColor: '#FF6D1F',
              opacity: 0.2,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Date stamp */}
        <div
          style={{
            position: 'absolute',
            top: 50,
            left: '50%',
            transform: `translateX(-50%) translateY(${dateY}px)`,
            opacity: dateOp,
            padding: '14px 36px',
            backgroundColor: '#091413',
            color: '#FF6D1F',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 34,
            fontWeight: 700,
            letterSpacing: '2px',
            borderRadius: 6,
          }}
        >
          मार्च 2026 रिपोर्ट
        </div>

        {/* Number 1 block - LEFT */}
        <div
          style={{
            position: 'absolute',
            top: 180,
            left: 120,
            width: 780,
            opacity: num1Scale > 0 ? 1 : 0,
            transform: `scale(${num1Scale})`,
            transformOrigin: 'left center',
          }}
        >
          <div
            style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 187,
              color: '#FF6D1F',
              fontWeight: 900,
              lineHeight: 1,
              letterSpacing: '-4px',
            }}
          >
            {num1Counter.toFixed(2)}
            <span style={{ fontSize: 95, color: '#091413' }}> लाख</span>
          </div>
          <div
            style={{
              opacity: label1Op,
              marginTop: 10,
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 44,
              color: '#091413',
              fontWeight: 700,
            }}
          >
            SIP बंद / पूरी हुई
          </div>
          <div
            style={{
              opacity: label1Op,
              marginTop: 16,
              width: 200,
              height: 6,
              backgroundColor: '#FF6D1F',
            }}
          />
        </div>

        {/* VS divider */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: '50%',
            transform: `translateX(-50%) scale(${dividerScale})`,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <div style={{ width: 80, height: 4, backgroundColor: '#091413' }} />
          <div
            style={{
              padding: '12px 28px',
              backgroundColor: '#091413',
              color: '#EEEEEE',
              fontFamily: 'Yantra One, serif',
              fontSize: 50,
              fontWeight: 900,
              borderRadius: 100,
            }}
          >
            जबकि
          </div>
          <div style={{ width: 80, height: 4, backgroundColor: '#091413' }} />
        </div>

        {/* Number 2 block - RIGHT */}
        <div
          style={{
            position: 'absolute',
            top: 540,
            right: 120,
            textAlign: 'right',
            opacity: num2Scale > 0 ? 1 : 0,
            transform: `scale(${num2Scale})`,
            transformOrigin: 'right center',
          }}
        >
          <div
            style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 187,
              color: '#091413',
              fontWeight: 900,
              lineHeight: 1,
              letterSpacing: '-4px',
            }}
          >
            <span style={{ fontSize: 95, color: '#FF6D1F' }}>सिर्फ </span>
            {num2Counter.toFixed(2)}
            <span style={{ fontSize: 95, color: '#091413' }}> लाख</span>
          </div>
          <div
            style={{
              opacity: label2Op,
              marginTop: 10,
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 44,
              color: '#091413',
              fontWeight: 700,
            }}
          >
            नई SIP रजिस्टर हुई
          </div>
          <div
            style={{
              opacity: label2Op,
              marginTop: 16,
              marginLeft: 'auto',
              width: 200,
              height: 6,
              backgroundColor: '#091413',
            }}
          />
        </div>

        {/* Bottom alert bar */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: '50%',
            transform: `translateX(-50%) translateX(${crowdX}px)`,
            opacity: crowdOp,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            padding: '16px 36px',
            border: '4px solid #FF6D1F',
            backgroundColor: 'rgba(9,20,19,0.05)',
          }}
        >
          <svg width="60" height="60" viewBox="0 0 60 60">
            <polygon points="30,5 55,50 5,50" fill="none" stroke="#FF6D1F" strokeWidth="5" />
            <line x1="30" y1="22" x2="30" y2="38" stroke="#FF6D1F" strokeWidth="5" />
            <circle cx="30" cy="44" r="2.5" fill="#FF6D1F" />
          </svg>
          <div
            style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 47,
              color: '#091413',
              fontWeight: 900,
            }}
          >
            लोग बाजार से <span style={{ color: '#FF6D1F' }}>भाग रहे हैं?</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(9,20,19,0.18) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#091413',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Newspaper headline mock
  const paperOp = sp(20);
  const paperRot = interpolate(sp(20, { damping: 8 }), [0, 1], [-12, -4]);
  const paperScale = sp(20, { damping: 9 });

  // Red strike through headline
  const strikeW = interpolate(sp(120, { damping: 14 }), [0, 1], [0, 700]);

  // Big text overlay
  const mainOp = sp(180);
  const mainY = interpolate(sp(180), [0, 1], [60, 0]);

  // Decision graphic
  const decisionOp = sp(280);
  const decisionScale = sp(280, { damping: 9 });

  // Warning
  const warnScale = sp(360, { damping: 7, stiffness: 90 });
  const warnShake = sp(360) > 0.9 ? Math.sin(frame * 0.5) * 3 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 70%, #F0F0F0 0%, #EEEEEE 50%, #D5D5D5 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: '5%',
          right: '5%',
          width: 400,
          height: 400,
          backgroundColor: '#FF6D1F',
          opacity: 0.05,
          transform: `rotate(${frame * 0.12}deg)`,
          borderRadius: 30,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '5%',
          left: '5%',
          width: 350,
          height: 350,
          border: '4px solid #091413',
          opacity: 0.07,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.05) * 0.05})`,
        }}
      />

      {/* Texture - circles */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="circ05" width="80" height="80" patternUnits="userSpaceOnUse">
            <circle cx="40" cy="40" r="20" fill="none" stroke="#091413" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#circ05)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 40);
        const r2 = seededRandom(i + 400);
        return (
          <div
            key={`p5-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (r2 * 1080 - frame * 0.5 + 1080) % 1080,
              width: 5,
              height: 5,
              backgroundColor: '#091413',
              opacity: 0.2,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* Newspaper Mock */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          left: 100,
          opacity: paperOp,
          transform: `rotate(${paperRot}deg) scale(${paperScale})`,
          transformOrigin: 'left top',
          width: 760,
          padding: 40,
          backgroundColor: '#FFFFFF',
          border: '3px solid #091413',
          boxShadow: '12px 12px 0 #091413',
        }}
      >
        <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 22, color: '#091413', opacity: 0.6, letterSpacing: 2, marginBottom: 12 }}>
          BUSINESS NEWS · MARCH 2026
        </div>
        <div
          style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 64,
            color: '#091413',
            fontWeight: 900,
            lineHeight: 1.1,
            position: 'relative',
            display: 'inline-block',
          }}
        >
          SIP बंद होने का सैलाब!
          {/* Strike line */}
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              width: strikeW,
              height: 8,
              backgroundColor: '#FF6D1F',
              transform: 'translateY(-50%) rotate(-2deg)',
            }}
          />
        </div>
        <div style={{ marginTop: 24, fontFamily: 'Inter, sans-serif', fontSize: 28, color: '#091413', opacity: 0.6, fontWeight: 500 }}>
          ━━━━━━━━━━━━━━━━━
        </div>
      </div>

      {/* Big assertion */}
      <div
        style={{
          position: 'absolute',
          top: 600,
          left: 100,
          right: 100,
          opacity: mainOp,
          transform: `translateY(${mainY}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 110,
            color: '#091413',
            fontWeight: 900,
            lineHeight: 1.05,
            letterSpacing: '-2px',
          }}
        >
          असली कहानी
        </div>
        <div
          style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 110,
            color: '#FF6D1F',
            fontWeight: 900,
            lineHeight: 1.05,
            letterSpacing: '-2px',
            marginTop: -8,
          }}
        >
          इतनी सीधी नहीं!
        </div>
      </div>

      {/* Decision warning - right side */}
      <div
        style={{
          position: 'absolute',
          top: 180,
          right: 90,
          opacity: decisionOp,
          transform: `scale(${decisionScale})`,
          transformOrigin: 'right top',
          width: 480,
        }}
      >
        <div
          style={{
            padding: 30,
            backgroundColor: '#091413',
            color: '#EEEEEE',
            borderRadius: 16,
            borderLeft: '10px solid #FF6D1F',
          }}
        >
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 26, color: '#FF6D1F', fontWeight: 700, letterSpacing: 2, marginBottom: 14 }}>
            गलती ALERT
          </div>
          <div style={{ fontFamily: 'Yantra One, serif', fontSize: 55, fontWeight: 900, lineHeight: 1.1 }}>
            सिर्फ headline पढ़कर
          </div>
          <div style={{ fontFamily: 'Yantra One, serif', fontSize: 55, color: '#FF6D1F', fontWeight: 900, lineHeight: 1.1 }}>
            फैसला?
          </div>
        </div>
      </div>

      {/* Final stamp */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          right: 100,
          transform: `scale(${warnScale}) translate(${warnShake}px, ${warnShake * 0.5}px) rotate(-3deg)`,
          padding: '20px 40px',
          backgroundColor: '#FF6D1F',
          color: '#091413',
          fontFamily: 'Yantra One, serif',
          fontSize: 60,
          fontWeight: 900,
          border: '5px solid #091413',
        }}
      >
        जिंदगी की सबसे महंगी गलती!
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(9,20,19,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#091413',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Intro - main intro
  const helloOp = sp(15);
  const helloY = interpolate(sp(15), [0, 1], [50, 0]);

  // Name
  const nameScale = sp(60, { damping: 8 });

  // Brand logo
  const brandOp = sp(120);
  const brandX = interpolate(sp(120), [0, 1], [-100, 0]);

  // Truth badge
  const truthScale = sp(180, { damping: 7, stiffness: 100 });

  // No drama list
  const cross1Op = sp(240);
  const cross2Op = sp(290);

  // Bottom strip
  const stripOp = sp(360);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(ellipse at ${50 + Math.sin(frame * 0.02) * 10}% 50%, #F0F0F0 0%, #EEEEEE 50%, #D5D5D5 100%)`,
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: '15%',
          left: '5%',
          width: 350,
          height: 350,
          borderRadius: '50%',
          border: '5px solid #FF6D1F',
          opacity: 0.08,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '10%',
          right: '8%',
          width: 280,
          height: 280,
          backgroundColor: '#091413',
          opacity: 0.05,
          transform: `rotate(${45 + frame * 0.08}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '50%',
          right: '15%',
          width: 200,
          height: 200,
          borderRadius: '50%',
          backgroundColor: '#FF6D1F',
          opacity: 0.06,
          transform: `translate(${Math.sin(frame * 0.06) * 30}px, ${Math.cos(frame * 0.05) * 20}px)`,
        }}
      />

      {/* Texture - hexagon */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="hex06" width="60" height="52" patternUnits="userSpaceOnUse">
            <polygon points="30,2 56,17 56,37 30,52 4,37 4,17" fill="none" stroke="#091413" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hex06)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 50);
        const r2 = seededRandom(i + 500);
        return (
          <div
            key={`p6-${i}`}
            style={{
              position: 'absolute',
              left: (r * 1920 + frame * 0.3) % 1920,
              top: r2 * 1080,
              width: 4,
              height: 4,
              backgroundColor: '#FF6D1F',
              opacity: 0.3,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* "Hello" intro */}
      <div
        style={{
          position: 'absolute',
          top: 130,
          left: 100,
          opacity: helloOp,
          transform: `translateY(${helloY}px)`,
        }}
      >
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 38, color: '#091413', opacity: 0.6, fontWeight: 500, marginBottom: 4 }}>
          मैं हूं
        </div>
        <div
          style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 187,
            color: '#091413',
            fontWeight: 900,
            lineHeight: 1,
            letterSpacing: '-4px',
            transform: `scale(${nameScale})`,
            transformOrigin: 'left top',
          }}
        >
          <span style={{ color: '#FF6D1F' }}>कु</span>नाल
        </div>
      </div>

      {/* Brand badge */}
      <div
        style={{
          position: 'absolute',
          top: 400,
          left: 100,
          opacity: brandOp,
          transform: `translateX(${brandX}px)`,
          display: 'flex',
          alignItems: 'center',
          gap: 22,
        }}
      >
        <div
          style={{
            width: 80,
            height: 80,
            backgroundColor: '#FF6D1F',
            borderRadius: 16,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontFamily: 'Yantra One, serif',
            fontSize: 60,
            color: '#091413',
            fontWeight: 900,
            transform: `rotate(${Math.sin(frame * 0.05) * 4}deg)`,
          }}
        >
          P
        </div>
        <div>
          <div style={{ fontFamily: 'Yantra One, serif', fontSize: 60, color: '#091413', fontWeight: 900, lineHeight: 1 }}>
            ProBuffet HQ
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 30, color: '#FF6D1F', fontWeight: 600, marginTop: 4 }}>
            सच का प्लैटफॉर्म
          </div>
        </div>
      </div>

      {/* Truth badge - right side */}
      <div
        style={{
          position: 'absolute',
          top: 130,
          right: 100,
          transform: `scale(${truthScale}) rotate(${-6 + Math.sin(frame * 0.07) * 2}deg)`,
          padding: '36px 50px',
          backgroundColor: '#091413',
          color: '#EEEEEE',
          borderRadius: 20,
          border: '5px solid #FF6D1F',
          textAlign: 'center',
          boxShadow: '0 20px 40px rgba(9,20,19,0.25)',
        }}
      >
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 30, color: '#FF6D1F', fontWeight: 700, letterSpacing: 3 }}>
          PROMISE
        </div>
        <div style={{ fontFamily: 'Yantra One, serif', fontSize: 110, fontWeight: 900, lineHeight: 1, marginTop: 6 }}>
          सच!
        </div>
      </div>

      {/* What we DON'T do */}
      <div
        style={{
          position: 'absolute',
          bottom: 280,
          left: 100,
          right: 100,
          display: 'flex',
          gap: 30,
          justifyContent: 'center',
        }}
      >
        <div
          style={{
            opacity: cross1Op,
            transform: `scale(${cross1Op})`,
            flex: 1,
            padding: 24,
            backgroundColor: '#EEEEEE',
            border: '4px solid #091413',
            borderRadius: 14,
            position: 'relative',
          }}
        >
          <div style={{ fontFamily: 'Yantra One, serif', fontSize: 47, color: '#091413', fontWeight: 800, lineHeight: 1.1 }}>
            फैंसी बिक्री वाली बात
          </div>
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: 20,
              right: 20,
              height: 6,
              backgroundColor: '#FF6D1F',
              transform: `translateY(-50%) rotate(-3deg) scaleX(${cross1Op})`,
              transformOrigin: 'left',
            }}
          />
        </div>
        <div
          style={{
            opacity: cross2Op,
            transform: `scale(${cross2Op})`,
            flex: 1,
            padding: 24,
            backgroundColor: '#EEEEEE',
            border: '4px solid #091413',
            borderRadius: 14,
            position: 'relative',
          }}
        >
          <div style={{ fontFamily: 'Yantra One, serif', fontSize: 47, color: '#091413', fontWeight: 800, lineHeight: 1.1 }}>
            करोड़पती बनो वाला ड्रामा
          </div>
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: 20,
              right: 20,
              height: 6,
              backgroundColor: '#FF6D1F',
              transform: `translateY(-50%) rotate(-3deg) scaleX(${cross2Op})`,
              transformOrigin: 'left',
            }}
          />
        </div>
      </div>

      {/* Bottom strip */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: 0,
          right: 0,
          opacity: stripOp,
          padding: '24px 0',
          backgroundColor: '#FF6D1F',
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 30,
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 55,
            color: '#091413',
            fontWeight: 900,
            letterSpacing: '-1px',
          }}
        >
          सिर्फ DATA. सिर्फ FACTS. सिर्फ सच.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(9,20,19,0.18) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#091413',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Magnifying icon + headline
  const headlineOp = sp(15);
  const headlineY = interpolate(sp(15), [0, 1], [-40, 0]);
  const lensRot = Math.sin(frame * 0.04) * 8;

  // Digging visual - layers
  const layer1Op = sp(80);
  const layer1H = interpolate(sp(80, { damping: 14 }), [0, 1], [0, 80]);
  const layer2Op = sp(130);
  const layer2H = interpolate(sp(130, { damping: 14 }), [0, 1], [0, 80]);
  const layer3Op = sp(180);
  const layer3H = interpolate(sp(180, { damping: 14 }), [0, 1], [0, 80]);

  // Truth at bottom
  const truthOp = sp(240);
  const truthScale = sp(240, { damping: 8 });

  // Broker box (with cross)
  const brokerOp = sp(310);
  const brokerX = interpolate(sp(310), [0, 1], [200, 0]);

  // Bottom call
  const callOp = sp(380);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, #F2F2F2 0%, #EEEEEE 40%, #DADADA 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: '10%',
          right: '8%',
          width: 380,
          height: 380,
          backgroundColor: '#FF6D1F',
          opacity: 0.07,
          borderRadius: 30,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '15%',
          left: '3%',
          width: 280,
          height: 280,
          border: '4px solid #091413',
          opacity: 0.06,
          borderRadius: '50%',
        }}
      />

      {/* Texture - grid */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="grid07" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 50 0 L 0 0 0 50" fill="none" stroke="#091413" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid07)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const r = seededRandom(i + 60);
        const r2 = seededRandom(i + 600);
        return (
          <div
            key={`p7-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (r2 * 1080 + frame * 0.4) % 1080,
              width: 5,
              height: 5,
              backgroundColor: i % 2 === 0 ? '#FF6D1F' : '#091413',
              opacity: 0.2,
            }}
          />
        );
      })}

      {/* Headline */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 100,
          right: 100,
          opacity: headlineOp,
          transform: `translateY(${headlineY}px)`,
          display: 'flex',
          alignItems: 'center',
          gap: 28,
        }}
      >
        {/* Magnifying glass icon */}
        <svg width="100" height="100" viewBox="0 0 100 100" style={{ transform: `rotate(${lensRot}deg)` }}>
          <circle cx="40" cy="40" r="28" fill="none" stroke="#091413" strokeWidth="8" />
          <line x1="60" y1="60" x2="88" y2="88" stroke="#091413" strokeWidth="10" strokeLinecap="round" />
          <circle cx="40" cy="40" r="14" fill="#FF6D1F" opacity="0.5" />
        </svg>
        <div>
          <div style={{ fontFamily: 'Yantra One, serif', fontSize: 85, color: '#091413', fontWeight: 900, lineHeight: 1, letterSpacing: '-1px' }}>
            आंकड़ों की <span style={{ color: '#FF6D1F' }}>तह तक</span>
          </div>
        </div>
      </div>

      {/* Digging layers visualization */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          left: 120,
          width: 800,
        }}
      >
        <div
          style={{
            opacity: layer1Op,
            height: layer1H,
            backgroundColor: '#091413',
            marginBottom: 8,
            display: 'flex',
            alignItems: 'center',
            paddingLeft: 30,
            fontFamily: 'Inter, sans-serif',
            fontSize: 36,
            color: '#EEEEEE',
            fontWeight: 600,
            overflow: 'hidden',
          }}
        >
          सतह की News
        </div>
        <div
          style={{
            opacity: layer2Op,
            height: layer2H,
            backgroundColor: '#2D2D2D',
            marginBottom: 8,
            display: 'flex',
            alignItems: 'center',
            paddingLeft: 30,
            fontFamily: 'Inter, sans-serif',
            fontSize: 36,
            color: '#EEEEEE',
            fontWeight: 600,
            overflow: 'hidden',
          }}
        >
          ↓ Headlines के पीछे
        </div>
        <div
          style={{
            opacity: layer3Op,
            height: layer3H,
            backgroundColor: '#FF6D1F',
            display: 'flex',
            alignItems: 'center',
            paddingLeft: 30,
            fontFamily: 'Yantra One, serif',
            fontSize: 47,
            color: '#091413',
            fontWeight: 900,
            overflow: 'hidden',
          }}
        >
          ↓↓ असली DATA
        </div>
      </div>

      {/* Right side - Broker won't tell */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          right: 90,
          width: 450,
          opacity: brokerOp,
          transform: `translateX(${brokerX}px)`,
        }}
      >
        <div
          style={{
            padding: 28,
            backgroundColor: '#EEEEEE',
            border: '5px solid #091413',
            borderRadius: 16,
            position: 'relative',
          }}
        >
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 28, color: '#FF6D1F', fontWeight: 700, letterSpacing: 2 }}>
            BROKER बोले...
          </div>
          <div
            style={{
              marginTop: 14,
              fontFamily: 'Yantra One, serif',
              fontSize: 55,
              color: '#091413',
              fontWeight: 900,
              lineHeight: 1.05,
            }}
          >
            "सब अच्छा है... <br />SIP करो!"
          </div>
          {/* Cross overlay */}
          <svg
            width="100"
            height="100"
            style={{
              position: 'absolute',
              top: -20,
              right: -20,
              opacity: brokerOp,
            }}
          >
            <circle cx="50" cy="50" r="40" fill="#FF6D1F" />
            <line x1="30" y1="30" x2="70" y2="70" stroke="#091413" strokeWidth="8" strokeLinecap="round" />
            <line x1="70" y1="30" x2="30" y2="70" stroke="#091413" strokeWidth="8" strokeLinecap="round" />
          </svg>
        </div>
        <div
          style={{
            marginTop: 14,
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 30,
            color: '#091413',
            fontWeight: 600,
            opacity: 0.7,
          }}
        >
          वो ये नहीं बोलेगा...
        </div>
      </div>

      {/* Bottom truth call */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: '50%',
          transform: `translateX(-50%) scale(${truthScale})`,
          opacity: truthOp,
          padding: '24px 50px',
          backgroundColor: '#091413',
          color: '#EEEEEE',
          borderRadius: 14,
          textAlign: 'center',
          borderTop: '6px solid #FF6D1F',
          borderBottom: '6px solid #FF6D1F',
        }}
      >
        <div style={{ fontFamily: 'Yantra One, serif', fontSize: 72, fontWeight: 900, lineHeight: 1.1 }}>
          आज <span style={{ color: '#FF6D1F' }}>कड़वा सच</span> चबाते हैं
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(9,20,19,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#091413',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Slap impact
  const slapScale = interpolate(sp(15, { damping: 6, stiffness: 130 }), [0, 1], [4, 1]);
  const slapShake = sp(15) > 0.95 && frame < 50 ? Math.sin(frame * 1.8) * 8 : 0;

  // Retail investor word
  const retailOp = sp(80);
  const retailY = interpolate(sp(80), [0, 1], [40, 0]);

  // First point label
  const pointOp = sp(150);
  const pointX = interpolate(sp(150), [0, 1], [-150, 0]);

  // 100% giant with note
  const numScale = sp(220, { damping: 9 });

  // Cross out "सब भाग गए"
  const crossOp = sp(310);
  const crossW = interpolate(sp(310, { damping: 14 }), [0, 1], [0, 540]);

  // Reality reveal
  const realOp = sp(380);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 70% 30%, #F0F0F0 0%, #EEEEEE 50%, #D5D5D5 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: -100,
          left: -100,
          width: 500,
          height: 500,
          borderRadius: '50%',
          backgroundColor: '#FF6D1F',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '-15%',
          right: '10%',
          width: 600,
          height: 300,
          border: '4px solid #091413',
          opacity: 0.06,
          transform: `rotate(${frame * 0.08}deg)`,
        }}
      />

      {/* Texture - diagonal stripes */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="strp08" width="20" height="20" patternUnits="userSpaceOnUse" patternTransform="rotate(-45)">
            <line x1="0" y1="10" x2="20" y2="10" stroke="#091413" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#strp08)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 70);
        const r2 = seededRandom(i + 700);
        return (
          <div
            key={`p8-${i}`}
            style={{
              position: 'absolute',
              left: (r * 1920 - frame * 0.4 + 1920) % 1920,
              top: r2 * 1080,
              width: 4,
              height: 4,
              backgroundColor: '#FF6D1F',
              opacity: 0.3,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translate(${slapShake}px, 0)` }}>
        {/* "तमाचा" slap word */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 100,
            transform: `scale(${slapScale}) rotate(-5deg)`,
            transformOrigin: 'left top',
            padding: '14px 36px',
            backgroundColor: '#FF6D1F',
            color: '#091413',
            fontFamily: 'Yantra One, serif',
            fontSize: 110,
            fontWeight: 900,
            border: '6px solid #091413',
            letterSpacing: '-2px',
          }}
        >
          तमाचा!
        </div>

        {/* Retail investor label */}
        <div
          style={{
            position: 'absolute',
            top: 250,
            left: 110,
            opacity: retailOp,
            transform: `translateY(${retailY}px)`,
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 51,
            color: '#091413',
            fontWeight: 600,
          }}
        >
          भारतीय <span style={{ color: '#FF6D1F', fontWeight: 800 }}>रिटेल निवेशक</span> के चेहरे पर
        </div>

        {/* "पहली बात" label */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: 110,
            opacity: pointOp,
            transform: `translateX(${pointX}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
          }}
        >
          <div
            style={{
              width: 90,
              height: 90,
              borderRadius: '50%',
              backgroundColor: '#091413',
              color: '#FF6D1F',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Yantra One, serif',
              fontSize: 60,
              fontWeight: 900,
            }}
          >
            01
          </div>
          <div style={{ fontFamily: 'Yantra One, serif', fontSize: 60, color: '#091413', fontWeight: 800 }}>
            पहली बात...
          </div>
        </div>

        {/* 100%+ ratio with strikethrough idea */}
        <div
          style={{
            position: 'absolute',
            top: 500,
            right: 100,
            transform: `scale(${numScale})`,
            transformOrigin: 'right center',
            textAlign: 'right',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 32,
              color: '#091413',
              opacity: 0.6,
              fontWeight: 600,
              marginBottom: 4,
            }}
          >
            STOPPAGE RATIO
          </div>
          <div
            style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 195,
              color: '#FF6D1F',
              fontWeight: 900,
              lineHeight: 1,
              letterSpacing: '-6px',
              textShadow: '5px 5px 0 #091413',
            }}
          >
            100%+
          </div>
        </div>

        {/* Cross out "सब डर कर भाग गए" */}
        <div
          style={{
            position: 'absolute',
            bottom: 240,
            left: 110,
            opacity: crossOp,
            display: 'inline-block',
          }}
        >
          <div
            style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 64,
              color: '#091413',
              fontWeight: 800,
              position: 'relative',
              display: 'inline-block',
              padding: '4px 12px',
            }}
          >
            "सब डर कर भाग गए"
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                width: crossW,
                height: 8,
                backgroundColor: '#FF6D1F',
                transform: 'translateY(-50%) rotate(-3deg)',
              }}
            />
          </div>
        </div>

        {/* Reality */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 110,
            opacity: realOp,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            padding: '20px 36px',
            backgroundColor: '#091413',
            borderRadius: 12,
            borderLeft: '8px solid #FF6D1F',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 30,
              color: '#FF6D1F',
              fontWeight: 700,
              letterSpacing: 2,
            }}
          >
            असल में →
          </div>
          <div
            style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 64,
              color: '#EEEEEE',
              fontWeight: 900,
            }}
          >
            ये पूरा सच नहीं है!
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(9,20,19,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#091413',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleOp = sp(15);
  const titleY = interpolate(sp(15), [0, 1], [-40, 0]);

  // Timeline progress
  const tlOp = sp(80);
  const startDot = sp(100, { damping: 8 });
  const lineW = interpolate(sp(140, { damping: 14 }), [0, 1], [0, 1100]);
  const endDot = sp(200, { damping: 8 });

  // Year badges
  const yr3Op = sp(260);
  const yr3Scale = sp(260, { damping: 9 });
  const yr5Op = sp(310);
  const yr5Scale = sp(310, { damping: 9 });

  // Bottom message
  const msgOp = sp(380);
  const msgScale = sp(380, { damping: 8 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(45deg, #EEEEEE 0%, #E8E8E8 50%, #D5D5D5 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: '5%',
          right: -100,
          width: 400,
          height: 400,
          backgroundColor: '#FF6D1F',
          opacity: 0.06,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '8%',
          left: '10%',
          width: 300,
          height: 300,
          border: '3px solid #091413',
          opacity: 0.06,
          transform: `rotate(${30 + frame * 0.08}deg)`,
        }}
      />

      {/* Texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="d09" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#091413" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#d09)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 80);
        const r2 = seededRandom(i + 800);
        return (
          <div
            key={`p9-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (r2 * 1080 + frame * 0.5) % 1080,
              width: 5,
              height: 5,
              backgroundColor: '#FF6D1F',
              opacity: 0.25,
            }}
          />
        );
      })}

      {/* Title */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 100,
          right: 100,
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}
      >
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 36, color: '#FF6D1F', fontWeight: 700, letterSpacing: 3, marginBottom: 8 }}>
          असली वजह #1
        </div>
        <div
          style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 110,
            color: '#091413',
            fontWeight: 900,
            lineHeight: 1.05,
            letterSpacing: '-2px',
          }}
        >
          Tenure <span style={{ color: '#FF6D1F' }}>पूरा</span> हो गया
        </div>
      </div>

      {/* Timeline */}
      <div
        style={{
          position: 'absolute',
          top: 480,
          left: 200,
          right: 200,
          opacity: tlOp,
          display: 'flex',
          alignItems: 'center',
        }}
      >
        {/* Start dot */}
        <div
          style={{
            width: 50,
            height: 50,
            borderRadius: '50%',
            backgroundColor: '#091413',
            transform: `scale(${startDot})`,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            color: '#FF6D1F',
            fontFamily: 'Yantra One, serif',
            fontSize: 30,
            fontWeight: 900,
            flexShrink: 0,
          }}
        >
          ▶
        </div>

        {/* Line */}
        <div
          style={{
            height: 14,
            width: lineW,
            background: 'linear-gradient(90deg, #091413 0%, #FF6D1F 100%)',
            borderRadius: 7,
            marginLeft: -5,
            marginRight: -5,
          }}
        />

        {/* End dot */}
        <div
          style={{
            width: 60,
            height: 60,
            borderRadius: '50%',
            backgroundColor: '#FF6D1F',
            transform: `scale(${endDot})`,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            color: '#091413',
            fontFamily: 'Yantra One, serif',
            fontSize: 30,
            fontWeight: 900,
            flexShrink: 0,
            boxShadow: '0 0 30px rgba(255,109,31,0.5)',
          }}
        >
          ✓
        </div>
      </div>

      {/* Timeline labels */}
      <div
        style={{
          position: 'absolute',
          top: 420,
          left: 220,
          opacity: tlOp,
          fontFamily: 'Inter, sans-serif',
          fontSize: 30,
          color: '#091413',
          fontWeight: 600,
        }}
      >
        SIP शुरू
      </div>
      <div
        style={{
          position: 'absolute',
          top: 550,
          right: 220,
          opacity: tlOp,
          fontFamily: 'Inter, sans-serif',
          fontSize: 30,
          color: '#FF6D1F',
          fontWeight: 700,
        }}
      >
        Tenure खत्म ✓
      </div>

      {/* Year badges */}
      <div
        style={{
          position: 'absolute',
          top: 650,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          gap: 40,
        }}
      >
        <div
          style={{
            opacity: yr3Op,
            transform: `scale(${yr3Scale})`,
            padding: '24px 50px',
            backgroundColor: '#091413',
            color: '#EEEEEE',
            borderRadius: 16,
            textAlign: 'center',
            border: '4px solid #FF6D1F',
          }}
        >
          <div style={{ fontFamily: 'Yantra One, serif', fontSize: 110, fontWeight: 900, lineHeight: 1, color: '#FF6D1F' }}>
            3
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 34, fontWeight: 600, marginTop: 4 }}>
            साल का mandate
          </div>
        </div>
        <div
          style={{
            opacity: yr5Op,
            transform: `scale(${yr5Scale})`,
            padding: '24px 50px',
            backgroundColor: '#FF6D1F',
            color: '#091413',
            borderRadius: 16,
            textAlign: 'center',
            border: '4px solid #091413',
          }}
        >
          <div style={{ fontFamily: 'Yantra One, serif', fontSize: 110, fontWeight: 900, lineHeight: 1 }}>
            5
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 34, fontWeight: 700, marginTop: 4 }}>
            साल का mandate
          </div>
        </div>
      </div>

      {/* Bottom message */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          left: '50%',
          transform: `translateX(-50%) scale(${msgScale})`,
          opacity: msgOp,
          padding: '18px 40px',
          backgroundColor: '#EEEEEE',
          border: '5px solid #091413',
          borderRadius: 10,
        }}
      >
        <div style={{ fontFamily: 'Yantra One, serif', fontSize: 50, color: '#091413', fontWeight: 900 }}>
          ये बंद नहीं... <span style={{ color: '#FF6D1F' }}>ये PLAN पूरा हुआ है!</span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(9,20,19,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#091413',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Industry expert quote
  const quoteOp = sp(15);
  const quoteY = interpolate(sp(15), [0, 1], [-40, 0]);

  // INVESTOR CHURN big text
  const churnScale = sp(80, { damping: 8, stiffness: 100 });

  // Green tick - no panic
  const tickOp = sp(170);
  const tickScale = sp(170, { damping: 9 });

  // BUT divider
  const butOp = sp(240);
  const butScale = sp(240, { damping: 7, stiffness: 110 });

  // Second side - danger
  const dangerOp = sp(310);
  const dangerX = interpolate(sp(310), [0, 1], [200, 0]);

  // Correction warning
  const correctOp = sp(380);
  const correctShake = sp(380) > 0.9 ? Math.sin(frame * 0.6) * 3 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 20% 80%, #F2F2F2 0%, #EEEEEE 50%, #D8D8D8 100%)`,
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: '20%',
          left: -150,
          width: 500,
          height: 500,
          borderRadius: '50%',
          backgroundColor: '#FF6D1F',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '-10%',
          right: '5%',
          width: 450,
          height: 450,
          border: '5px solid #091413',
          opacity: 0.06,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Texture - waves */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="wv10" width="60" height="20" patternUnits="userSpaceOnUse">
            <path d="M0,10 Q15,0 30,10 T60,10" fill="none" stroke="#091413" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#wv10)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const r = seededRandom(i + 90);
        const r2 = seededRandom(i + 900);
        return (
          <div
            key={`p10-${i}`}
            style={{
              position: 'absolute',
              left: (r * 1920 + frame * 0.3) % 1920,
              top: r2 * 1080,
              width: 5,
              height: 5,
              backgroundColor: i % 2 === 0 ? '#FF6D1F' : '#091413',
              opacity: 0.2,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* Quote source */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 100,
          opacity: quoteOp,
          transform: `translateY(${quoteY}px)`,
          display: 'flex',
          alignItems: 'center',
          gap: 16,
        }}
      >
        <div
          style={{
            padding: '10px 24px',
            backgroundColor: '#091413',
            color: '#FF6D1F',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            fontWeight: 700,
            letterSpacing: 2,
            borderRadius: 6,
          }}
        >
          INDUSTRY EXPERTS कहते हैं
        </div>
      </div>

      {/* INVESTOR CHURN - big phrase */}
      <div
        style={{
          position: 'absolute',
          top: 160,
          left: 100,
          right: 100,
          transform: `scale(${churnScale})`,
          transformOrigin: 'left center',
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 160,
            color: '#091413',
            fontWeight: 900,
            lineHeight: 1,
            letterSpacing: '-3px',
          }}
        >
          "INVESTOR
        </div>
        <div
          style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 160,
            color: '#FF6D1F',
            fontWeight: 900,
            lineHeight: 1,
            letterSpacing: '-3px',
            marginTop: -10,
          }}
        >
          CHURN"
        </div>
      </div>

      {/* Green check - no panic */}
      <div
        style={{
          position: 'absolute',
          top: 200,
          right: 120,
          opacity: tickOp,
          transform: `scale(${tickScale}) rotate(${-8 + Math.sin(frame * 0.07) * 3}deg)`,
        }}
      >
        <div
          style={{
            padding: '20px 32px',
            border: '6px solid #091413',
            backgroundColor: '#EEEEEE',
            display: 'flex',
            alignItems: 'center',
            gap: 16,
            borderRadius: 12,
          }}
        >
          <svg width="60" height="60" viewBox="0 0 60 60">
            <circle cx="30" cy="30" r="28" fill="#091413" />
            <path d="M16,30 L26,40 L44,20" stroke="#FF6D1F" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div style={{ fontFamily: 'Yantra One, serif', fontSize: 50, color: '#091413', fontWeight: 900 }}>
            घबराहट नहीं
          </div>
        </div>
      </div>

      {/* BUT divider */}
      <div
        style={{
          position: 'absolute',
          top: 580,
          left: '50%',
          transform: `translateX(-50%) scale(${butScale}) rotate(-3deg)`,
          opacity: butOp,
          padding: '18px 50px',
          backgroundColor: '#FF6D1F',
          color: '#091413',
          fontFamily: 'Yantra One, serif',
          fontSize: 76,
          fontWeight: 900,
          letterSpacing: 4,
          border: '5px solid #091413',
          boxShadow: '0 14px 30px rgba(9,20,19,0.25)',
        }}
      >
        लेकिन...
      </div>

      {/* Danger side */}
      <div
        style={{
          position: 'absolute',
          bottom: 230,
          left: 100,
          right: 100,
          opacity: dangerOp,
          transform: `translateX(${dangerX}px)`,
        }}
      >
        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: 24,
            padding: '24px 40px',
            backgroundColor: '#091413',
            borderRadius: 14,
            borderLeft: '10px solid #FF6D1F',
          }}
        >
          <svg width="80" height="80" viewBox="0 0 80 80">
            <polygon points="40,8 75,68 5,68" fill="#FF6D1F" />
            <line x1="40" y1="28" x2="40" y2="52" stroke="#091413" strokeWidth="6" />
            <circle cx="40" cy="60" r="3.5" fill="#091413" />
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 28, color: '#FF6D1F', fontWeight: 700, letterSpacing: 2 }}>
              दूसरा पहलू
            </div>
            <div style={{ fontFamily: 'Yantra One, serif', fontSize: 64, color: '#EEEEEE', fontWeight: 900, lineHeight: 1.05 }}>
              बड़ा <span style={{ color: '#FF6D1F' }}>खतरनाक</span> है!
            </div>
          </div>
        </div>
      </div>

      {/* Bottom - correction time */}
      <div
        style={{
          position: 'absolute',
          bottom: 110,
          left: '50%',
          transform: `translateX(-50%) translate(${correctShake}px, 0)`,
          opacity: correctOp,
          fontFamily: 'Yantra One, serif',
          fontSize: 50,
          color: '#091413',
          fontWeight: 800,
          padding: '14px 36px',
          border: '4px dashed #FF6D1F',
          borderRadius: 12,
        }}
      >
        जब बाजार में CORRECTION हो...
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(9,20,19,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#091413',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10;
```

