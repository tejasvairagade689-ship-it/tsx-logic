```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_Ch1',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_Ch1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { extrapolateRight: 'clamp' });

  // Word reveal
  const wordOp = interpolate(frame, [10, 30], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Particle positions
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 3) * 1920,
    y: seededRandom(i * 3 + 1) * 1080,
    r: seededRandom(i * 3 + 2) * 4 + 2,
    speed: seededRandom(i * 7) * 0.4 + 0.2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at 20% 50%, #2a1a00 0%, #1C1C1C 60%, #0a0a0a 100%)',
          }}
        />

        {/* Shape Layer 1 — large circle drift */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          <circle
            cx={960 + Math.sin(frame * 0.008) * 30}
            cy={540 + Math.cos(frame * 0.006) * 20}
            r={420}
            fill="none"
            stroke="#e9d700"
            strokeWidth={1}
            opacity={0.04}
          />
          <circle
            cx={200 + Math.sin(frame * 0.01) * 15}
            cy={200}
            r={180}
            fill="#e9d700"
            opacity={0.03}
          />
          <rect
            x={1500 + Math.sin(frame * 0.007) * 10}
            y={600}
            width={300}
            height={300}
            fill="#FFC107"
            opacity={0.03}
            transform={`rotate(${frame * 0.04}, 1650, 750)`}
          />
        </svg>

        {/* Motion texture — horizontal scan lines */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          {Array.from({ length: 24 }, (_, i) => (
            <line
              key={i}
              x1={0}
              y1={i * 46}
              x2={1920}
              y2={i * 46}
              stroke="#F5F5F5"
              strokeWidth={0.4}
              opacity={0.03}
            />
          ))}
        </svg>

        {/* Floating Particles */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x}
              cy={(p.y - frame * p.speed * 0.6) % 1080}
              r={p.r}
              fill="#e9d700"
              opacity={0.12 + seededRandom(i * 11) * 0.1}
            />
          ))}
        </svg>

        {/* Left accent bar */}
        <div
          style={{
            position: 'absolute',
            left: 0,
            top: 0,
            width: 8,
            height: `${sp(0) * 100}%`,
            background: 'linear-gradient(180deg, #e9d700 0%, #FFC107 100%)',
          }}
        />

        {/* Main text block */}
        <div
          style={{
            position: 'absolute',
            left: 120,
            top: '50%',
            transform: `translateY(-50%) translateX(${(1 - sp(8)) * -60}px)`,
            opacity: sp(8),
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              color: '#e9d700',
              letterSpacing: 6,
              textTransform: 'uppercase',
              marginBottom: 24,
              opacity: wordOp,
            }}
          >
            ProBuffet HQ प्रस्तुत करता है
          </div>

          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 110,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1.1,
              maxWidth: 1100,
            }}
          >
            एक बात है
            <br />
            <span style={{ color: '#e9d700' }}>जो मैंने देखी है</span>
          </div>

          <div
            style={{
              marginTop: 32,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 48,
              color: '#9E9E9E',
              opacity: interpolate(frame, [30, 50], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            साल भर के अनुभव से
          </div>
        </div>

        {/* Stick Figure — Kunal */}
        <div
          style={{
            position: 'absolute',
            right: 160,
            bottom: 80,
            transform: `translateY(${Math.sin(frame * 0.05) * 3}px) translateX(${(1 - sp(40)) * 120}px)`,
            opacity: sp(40),
          }}
        >
          <svg width="120" height="200" viewBox="0 0 120 200">
            {/* Glow */}
            <circle cx="60" cy="100" r="70" fill="#e9d700" opacity="0.06" />
            {/* Head */}
            <circle
              cx="60"
              cy="30"
              r="22"
              fill="none"
              stroke="#F5F5F5"
              strokeWidth="3"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 30)`}
            />
            {/* Golden glasses */}
            <rect x="44" y="25" width="12" height="8" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <rect x="62" y="25" width="12" height="8" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="56" y1="29" x2="62" y2="29" stroke="#e9d700" strokeWidth="1.5" />
            {/* Body */}
            <line x1="60" y1="52" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arms */}
            <line x1="60" y1="70" x2="30" y2="95" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="60" y1="70" x2="90" y2="85" stroke="#F5F5F5" strokeWidth="3" />
            {/* Legs */}
            <line x1="60" y1="120" x2="38" y2="170" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="60" y1="120" x2="82" y2="170" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.65) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene1_Ch1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_Ch1',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_Ch1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Number "70" big counter slam
  const numScale = sp(5, { stiffness: 200, damping: 18 });

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 5) * 1920,
    y: seededRandom(i * 5 + 1) * 1080,
    r: seededRandom(i * 5 + 2) * 5 + 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Deep charcoal gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #2D2D2D 0%, #1C1C1C 40%, #0d0d0d 100%)',
        }}
      />

      {/* Shape layer — triangle */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        <polygon
          points={`1700,${100 + Math.sin(frame * 0.007) * 20} 1920,500 1600,700`}
          fill="#e9d700"
          opacity={0.04}
        />
        <circle
          cx={300 + Math.cos(frame * 0.009) * 20}
          cy={800}
          r={250}
          fill="none"
          stroke="#F5F5F5"
          strokeWidth={1}
          opacity={0.04}
        />
      </svg>

      {/* Grid texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {Array.from({ length: 20 }, (_, i) => (
          <line key={`v${i}`} x1={i * 100} y1={0} x2={i * 100} y2={1080} stroke="#F5F5F5" strokeWidth={0.3} opacity={0.04} />
        ))}
        {Array.from({ length: 12 }, (_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 90} x2={1920} y2={i * 90} stroke="#F5F5F5" strokeWidth={0.3} opacity={0.04} />
        ))}
      </svg>

      {/* Particles */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {particles.map((p, i) => (
          <circle
            key={i}
            cx={p.x + Math.sin(frame * 0.02 + i) * 8}
            cy={p.y}
            r={p.r}
            fill="#FFC107"
            opacity={0.1}
          />
        ))}
      </svg>

      {/* BIG 70 — center slam */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) scale(${numScale})`,
          fontFamily: 'Inter, Poppins',
          fontSize: 340,
          fontWeight: 900,
          color: '#e9d700',
          opacity: 0.08,
          lineHeight: 1,
          userSelect: 'none',
        }}
      >
        70
      </div>

      {/* Left column — text */}
      <div
        style={{
          position: 'absolute',
          left: 100,
          top: '50%',
          transform: `translateY(-50%) translateX(${(1 - sp(10)) * -80}px)`,
          opacity: sp(10),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 24,
            color: '#e9d700',
            letterSpacing: 5,
            marginBottom: 20,
            textTransform: 'uppercase',
          }}
        >
          The Silence After
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 96,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.15,
            maxWidth: 900,
          }}
        >
          सत्तर के पार
          <br />
          <span style={{ color: '#e9d700' }}>एक चुप्पी</span>
          <br />
          आ जाती है
        </div>

        {/* Silence indicator bars */}
        <div style={{ display: 'flex', gap: 8, marginTop: 40 }}>
          {Array.from({ length: 8 }, (_, i) => (
            <div
              key={i}
              style={{
                width: 6,
                height: 40 + seededRandom(i * 3) * 40,
                background: i < 3 ? '#e9d700' : '#2D2D2D',
                borderRadius: 3,
                opacity: interpolate(frame, [20 + i * 5, 35 + i * 5], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              }}
            />
          ))}
        </div>
        <div
          style={{
            marginTop: 16,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 36,
            color: '#9E9E9E',
            opacity: interpolate(frame, [40, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          न बात होती है, न तैयारी
        </div>
      </div>

      {/* Stick figure — hunched old pose */}
      <div
        style={{
          position: 'absolute',
          right: 180,
          top: '50%',
          transform: `translateY(-55%) translateX(${(1 - sp(45)) * 100}px)`,
          opacity: sp(45),
        }}
      >
        <svg width="130" height="220" viewBox="0 0 130 220">
          <circle cx="65" cy="65" r="70" fill="#e9d700" opacity="0.05" />
          {/* Head slightly bowed */}
          <circle
            cx="65"
            cy="32"
            r="22"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="3"
            transform={`rotate(${10 + Math.sin(frame * 0.04) * 2}, 65, 32)`}
          />
          {/* Glasses */}
          <rect x="49" y="27" width="12" height="8" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
          <rect x="67" y="27" width="12" height="8" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="61" y1="31" x2="67" y2="31" stroke="#e9d700" strokeWidth="1.5" />
          {/* Hunched body */}
          <path d="M65 54 Q58 90 62 125" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          {/* Arms down */}
          <line x1="63" y1="75" x2="36" y2="105" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="63" y1="75" x2="86" y2="100" stroke="#F5F5F5" strokeWidth="3" />
          {/* Legs */}
          <line x1="62" y1="125" x2="42" y2="175" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="62" y1="125" x2="80" y2="175" stroke="#F5F5F5" strokeWidth="3" />
          {/* Cane */}
          <line x1="36" y1="105" x2="22" y2="175" stroke="#9E9E9E" strokeWidth="2" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.7) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_Ch1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_Ch1',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_Ch1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 7) * 1920,
    y: seededRandom(i * 7 + 1) * 1080,
    r: seededRandom(i * 7 + 2) * 3 + 1.5,
    drift: seededRandom(i * 7 + 3) * 0.3 + 0.1,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Dark red tinted background — warning energy */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 80% 30%, #1a0808 0%, #1C1C1C 55%, #111 100%)',
        }}
      />

      {/* Shape layer — diagonal rectangles */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        <rect
          x={-100}
          y={300}
          width={500}
          height={600}
          fill="#ff0000"
          opacity={0.03}
          transform={`rotate(${-15 + Math.sin(frame * 0.006) * 2}, 150, 600)`}
        />
        <rect
          x={1600}
          y={-100}
          width={400}
          height={500}
          fill="#e9d700"
          opacity={0.04}
          transform={`rotate(${20 + Math.cos(frame * 0.007) * 2}, 1800, 150)`}
        />
        <circle cx={960} cy={540} r={380} fill="none" stroke="#ff0000" strokeWidth={1} opacity={0.05} />
      </svg>

      {/* Circle ring texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {Array.from({ length: 6 }, (_, i) => (
          <circle
            key={i}
            cx={960}
            cy={540}
            r={100 + i * 80}
            fill="none"
            stroke="#9E9E9E"
            strokeWidth={0.5}
            opacity={0.025}
          />
        ))}
      </svg>

      {/* Floating particles */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {particles.map((p, i) => (
          <circle
            key={i}
            cx={p.x + Math.sin(frame * p.drift + i) * 12}
            cy={(p.y - frame * p.drift * 0.5) % 1080}
            r={p.r}
            fill={i % 3 === 0 ? '#ff0000' : '#e9d700'}
            opacity={0.08}
          />
        ))}
      </svg>

      {/* Two-column split */}
      {/* Left — X marks (no one talks) */}
      <div
        style={{
          position: 'absolute',
          left: 80,
          top: '50%',
          transform: `translateY(-50%) translateX(${(1 - sp(5)) * -70}px)`,
          opacity: sp(5),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 22,
            color: '#ff0000',
            letterSpacing: 4,
            marginBottom: 28,
            textTransform: 'uppercase',
          }}
        >
          The Problem
        </div>

        {[
          'न कोई बात करता है',
          'न कोई तैयारी करता है',
        ].map((line, i) => (
          <div
            key={i}
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 20,
              marginBottom: 28,
              opacity: interpolate(frame, [15 + i * 18, 30 + i * 18], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              transform: `translateX(${interpolate(frame, [15 + i * 18, 30 + i * 18], [-40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
            }}
          >
            {/* X icon */}
            <svg width="48" height="48" viewBox="0 0 48 48">
              <circle cx="24" cy="24" r="22" fill="#ff0000" opacity={0.15} />
              <line x1="14" y1="14" x2="34" y2="34" stroke="#ff0000" strokeWidth="3.5" strokeLinecap="round" />
              <line x1="34" y1="14" x2="14" y2="34" stroke="#ff0000" strokeWidth="3.5" strokeLinecap="round" />
            </svg>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 58,
                fontWeight: 700,
                color: '#F5F5F5',
              }}
            >
              {line}
            </div>
          </div>
        ))}
      </div>

      {/* Right — THE MOST IMPORTANT turn */}
      <div
        style={{
          position: 'absolute',
          right: 80,
          top: '50%',
          width: 680,
          transform: `translateY(-50%) translateX(${(1 - sp(40)) * 80}px)`,
          opacity: sp(40),
        }}
      >
        <div
          style={{
            background: 'linear-gradient(135deg, #e9d700 0%, #FFC107 100%)',
            padding: '10px 28px',
            display: 'inline-block',
            marginBottom: 24,
            borderRadius: 4,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#1C1C1C',
              fontWeight: 700,
              letterSpacing: 3,
              textTransform: 'uppercase',
            }}
          >
            लेकिन यह है
          </div>
        </div>

        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 90,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
          }}
        >
          सबसे
          <br />
          <span style={{ color: '#e9d700' }}>महत्पूर्ण मोड</span>
        </div>

        <div
          style={{
            marginTop: 24,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 42,
            color: '#9E9E9E',
            opacity: interpolate(frame, [55, 75], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          तुम्हारा निवेश सब बदल जाता है
        </div>
      </div>

      {/* Stick figure — arms spread, alert pose */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          bottom: 60,
          transform: `translateX(-50%) translateY(${(1 - sp(55)) * 80}px)`,
          opacity: sp(55),
        }}
      >
        <svg width="110" height="190" viewBox="0 0 110 190">
          <circle cx="55" cy="95" r="65" fill="#ff0000" opacity="0.05" />
          <circle
            cx="55"
            cy="28"
            r="20"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.05) * 4}, 55, 28)`}
          />
          <rect x="41" y="23" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
          <rect x="57" y="23" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="51" y1="26" x2="57" y2="26" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="55" y1="48" x2="55" y2="115" stroke="#F5F5F5" strokeWidth="3" />
          {/* Arms raised — alert */}
          <line x1="55" y1="68" x2="18" y2="45" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="55" y1="68" x2="92" y2="45" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="55" y1="115" x2="35" y2="165" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="55" y1="115" x2="75" y2="165" stroke="#F5F5F5" strokeWidth="3" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 48%, rgba(0,0,0,0.68) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3_Ch1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4_Ch1',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_Ch1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 4) * 1920,
    y: seededRandom(i * 4 + 1) * 1080,
    r: seededRandom(i * 4 + 2) * 4 + 2,
  }));

  // Arrow path for "same strategy → destruction"
  const arrowProgress = interpolate(frame, [30, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient — split feel */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(to right, #1a1a2e 0%, #1C1C1C 50%, #1a0808 100%)',
        }}
      />

      {/* Shape layers */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {/* Diagonal slash */}
        <line
          x1={600 + Math.sin(frame * 0.005) * 10}
          y1={0}
          x2={700}
          y2={1080}
          stroke="#e9d700"
          strokeWidth={1.5}
          opacity={0.08}
        />
        <circle cx={200} cy={300} r={200} fill="#e9d700" opacity={0.03} />
        <circle cx={1700} cy={700} r={250} fill="#ff0000" opacity={0.04} />
      </svg>

      {/* Diagonal line texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {Array.from({ length: 16 }, (_, i) => (
          <line
            key={i}
            x1={i * 130 - 200}
            y1={0}
            x2={i * 130}
            y2={1080}
            stroke="#F5F5F5"
            strokeWidth={0.4}
            opacity={0.025}
          />
        ))}
      </svg>

      {/* Particles */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {particles.map((p, i) => (
          <circle key={i} cx={p.x} cy={p.y} r={p.r} fill="#e9d700" opacity={0.08} />
        ))}
      </svg>

      {/* LEFT SIDE — 40s/50s (worked) */}
      <div
        style={{
          position: 'absolute',
          left: 80,
          top: '50%',
          width: 560,
          transform: `translateY(-50%) translateX(${(1 - sp(5)) * -80}px)`,
          opacity: sp(5),
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 180,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 0.9,
            opacity: 0.9,
          }}
        >
          40s
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 180,
            fontWeight: 900,
            color: '#FFC107',
            lineHeight: 0.9,
            opacity: 0.7,
          }}
        >
          50s
        </div>

        <div
          style={{
            marginTop: 24,
            display: 'flex',
            alignItems: 'center',
            gap: 14,
            opacity: interpolate(frame, [20, 38], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <svg width="38" height="38" viewBox="0 0 38 38">
            <circle cx="19" cy="19" r="17" fill="#e9d700" opacity={0.2} />
            <polyline points="8,19 16,27 30,11" fill="none" stroke="#e9d700" strokeWidth="3" strokeLinecap="round" />
          </svg>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, color: '#F5F5F5', fontWeight: 600 }}>
            रणनीती काम करती थी
          </div>
        </div>
      </div>

      {/* CENTER ARROW — animated */}
      <svg
        style={{ position: 'absolute', left: 0, top: 0, width: '100%', height: '100%' }}
      >
        <defs>
          <marker id="arrowhead" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" fill="#ff0000" />
          </marker>
        </defs>
        <line
          x1={680}
          y1={540}
          x2={680 + arrowProgress * 560}
          y2={540}
          stroke="#ff0000"
          strokeWidth={3}
          markerEnd="url(#arrowhead)"
          opacity={0.8}
        />
        <text
          x={960}
          y={510}
          textAnchor="middle"
          fill="#ff0000"
          fontFamily="JetBrains Mono"
          fontSize={22}
          opacity={arrowProgress}
        >
          Same Strategy →
        </text>
      </svg>

      {/* RIGHT SIDE — 70+ (destruction) */}
      <div
        style={{
          position: 'absolute',
          right: 80,
          top: '50%',
          width: 560,
          textAlign: 'right',
          transform: `translateY(-50%) translateX(${(1 - sp(35)) * 80}px)`,
          opacity: sp(35),
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 180,
            fontWeight: 900,
            color: '#ff0000',
            lineHeight: 0.9,
            opacity: 0.85,
          }}
        >
          70+
        </div>

        <div
          style={{
            marginTop: 24,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'flex-end',
            gap: 14,
            opacity: interpolate(frame, [45, 62], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, color: '#F5F5F5', fontWeight: 600 }}>
            बरबाद कर सकती है
          </div>
          <svg width="38" height="38" viewBox="0 0 38 38">
            <circle cx="19" cy="19" r="17" fill="#ff0000" opacity={0.2} />
            <line x1="12" y1="12" x2="26" y2="26" stroke="#ff0000" strokeWidth="3" strokeLinecap="round" />
            <line x1="26" y1="12" x2="12" y2="26" stroke="#ff0000" strokeWidth="3" strokeLinecap="round" />
          </svg>
        </div>

        <div
          style={{
            marginTop: 36,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 52,
            fontWeight: 900,
            color: '#ff0000',
            opacity: interpolate(frame, [60, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          ये कोई छोटी बात नहीं
        </div>
      </div>

      {/* Stick figure — confused, looking left-right */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          bottom: 55,
          transform: `translateX(-50%) translateY(${(1 - sp(60)) * 80}px)`,
          opacity: sp(60),
        }}
      >
        <svg width="110" height="190" viewBox="0 0 110 190">
          <circle cx="55" cy="95" r="60" fill="#e9d700" opacity="0.05" />
          <circle
            cx="55"
            cy="27"
            r="20"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.06) * 12}, 55, 27)`}
          />
          <rect x="41" y="22" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
          <rect x="57" y="22" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="51" y1="25" x2="57" y2="25" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="55" y1="47" x2="55" y2="115" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="55" y1="68" x2="22" y2="80" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="55" y1="68" x2="88" y2="80" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="55" y1="115" x2="36" y2="165" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="55" y1="115" x2="74" y2="165" stroke="#F5F5F5" strokeWidth="3" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.65) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4_Ch1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5_Ch1',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_Ch1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on big number reveal
  const shakeX = frame > 40 && frame < 50
    ? (seededRandom(frame) - 0.5) * 14
    : 0;
  const shakeY = frame > 40 && frame < 50
    ? (seededRandom(frame + 100) - 0.5) * 10
    : 0;

  // Crash chart line points
  const chartPoints = [
    [0, 200], [80, 190], [160, 170], [240, 220], [320, 160],
    [400, 260], [480, 340], [560, 500], [640, 520], [720, 580],
  ];

  const chartProgress = interpolate(frame, [20, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const visiblePoints = Math.floor(chartProgress * chartPoints.length);

  const particles = Array.from({ length: 12 }, (_, i) => ({
    x: seededRandom(i * 6) * 1920,
    y: seededRandom(i * 6 + 1) * 1080,
    r: seededRandom(i * 6 + 2) * 4 + 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          transform: `translate(${shakeX}px, ${shakeY}px)`,
          background: 'linear-gradient(180deg, #0a0a0a 0%, #1C1C1C 40%, #1a0000 100%)',
        }}
      >
        {/* Shape layers */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          <circle cx={1700} cy={200} r={300} fill="#ff0000" opacity={0.04} />
          <rect
            x={-50}
            y={700}
            width={400}
            height={400}
            fill="#e9d700"
            opacity={0.03}
            transform={`rotate(${frame * 0.03}, 150, 900)`}
          />
        </svg>

        {/* Dot grid texture */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          {Array.from({ length: 20 }, (_, row) =>
            Array.from({ length: 36 }, (_, col) => (
              <circle
                key={`${row}-${col}`}
                cx={col * 55 + 20}
                cy={row * 55 + 20}
                r={1.5}
                fill="#F5F5F5"
                opacity={0.04}
              />
            ))
          )}
        </svg>

        {/* Particles */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          {particles.map((p, i) => (
            <circle key={i} cx={p.x} cy={p.y} r={p.r} fill="#ff0000" opacity={0.06} />
          ))}
        </svg>

        {/* Crash chart — left side */}
        <div
          style={{
            position: 'absolute',
            left: 80,
            top: 120,
            opacity: interpolate(frame, [10, 28], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#9E9E9E',
              letterSpacing: 4,
              marginBottom: 16,
              textTransform: 'uppercase',
            }}
          >
            Market Crash Scenario
          </div>
          <svg width="760" height="620" viewBox="0 0 760 620">
            {/* Grid lines */}
            {[100, 200, 300, 400, 500].map((y, i) => (
              <line key={i} x1={0} y1={y} x2={760} y2={y} stroke="#2D2D2D" strokeWidth={1} />
            ))}
            {/* Chart area fill */}
            {visiblePoints > 1 && (
              <polygon
                points={[
                  ...chartPoints.slice(0, visiblePoints).map(p => `${p[0]},${p[1]}`),
                  `${chartPoints[visiblePoints - 1][0]},620`,
                  '0,620',
                ].join(' ')}
                fill="#ff0000"
                opacity={0.08}
              />
            )}
            {/* Chart line */}
            {visiblePoints > 1 && (
              <polyline
                points={chartPoints.slice(0, visiblePoints).map(p => p.join(',')).join(' ')}
                fill="none"
                stroke="#ff0000"
                strokeWidth={3}
                strokeLinecap="round"
                strokeLinejoin="round"
              />
            )}
            {/* Axes */}
            <line x1={0} y1={0} x2={0} y2={620} stroke="#2D2D2D" strokeWidth={2} />
            <line x1={0} y1={620} x2={760} y2={620} stroke="#2D2D2D" strokeWidth={2} />
          </svg>
        </div>

        {/* Right side — big % numbers */}
        <div
          style={{
            position: 'absolute',
            right: 80,
            top: '50%',
            textAlign: 'right',
            transform: `translateY(-50%) translateX(${(1 - sp(35)) * 90}px)`,
            opacity: sp(35),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 240,
              fontWeight: 900,
              color: '#ff0000',
              lineHeight: 0.85,
              letterSpacing: -8,
            }}
          >
            30%
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 48,
              fontWeight: 700,
              color: '#9E9E9E',
              letterSpacing: 2,
            }}
          >
            से
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 240,
              fontWeight: 900,
              color: '#ff0000',
              lineHeight: 0.85,
              letterSpacing: -8,
              opacity: interpolate(frame, [48, 65], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            40%
          </div>

          <div
            style={{
              marginTop: 28,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 52,
              fontWeight: 700,
              color: '#F5F5F5',
              opacity: interpolate(frame, [70, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            ये केवल झटका नहीं है
            <br />
            <span style={{ color: '#e9d700' }}>ये जिंदगी बदल देता है</span>
          </div>
        </div>

        {/* Stick figure — falling/shocked */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            bottom: 50,
            transform: `translateX(-50%) translateY(${(1 - sp(65)) * 80}px) rotate(${Math.sin(frame * 0.08) * 5}deg)`,
            opacity: sp(65),
          }}
        >
          <svg width="110" height="190" viewBox="0 0 110 190">
            <circle cx="55" cy="95" r="65" fill="#ff0000" opacity="0.06" />
            <circle cx="55" cy="27" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <rect x="41" y="22" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <rect x="57" y="22" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="51" y1="25" x2="57" y2="25" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="55" y1="47" x2="55" y2="115" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arms out shocked */}
            <line x1="55" y1="65" x2="10" y2="55" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="55" y1="65" x2="100" y2="55" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="55" y1="115" x2="38" y2="165" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="55" y1="115" x2="72" y2="165" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at center, transparent 42%, rgba(0,0,0,0.7) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene5_Ch1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6_Ch1',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_Ch1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const dreams = [
    { icon: 'travel', label: 'घूमने की योजना', delay: 8 },
    { icon: 'family', label: 'बेटे-बहू की मदद', delay: 28 },
    { icon: 'peace', label: 'भीतर की शांती', delay: 48 },
  ];

  const particles = Array.from({ length: 15 }, (_, i) => ({
    x: seededRandom(i * 9) * 1920,
    y: seededRandom(i * 9 + 1) * 1080,
    r: seededRandom(i * 9 + 2) * 3 + 1,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Muted dark gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 60% 40%, #1e1520 0%, #1C1C1C 55%, #0e0e0e 100%)',
        }}
      />

      {/* Shape layers */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        <circle
          cx={960 + Math.sin(frame * 0.007) * 25}
          cy={540}
          r={480}
          fill="none"
          stroke="#9E9E9E"
          strokeWidth={1}
          opacity={0.05}
          strokeDasharray="12 8"
        />
        <rect x={50} y={50} width={200} height={200} fill="#FFC107" opacity={0.03} transform={`rotate(${frame * 0.03}, 150, 150)`} />
        <circle cx={1800} cy={900} r={200} fill="#e9d700" opacity={0.03} />
      </svg>

      {/* Fade diagonal lines texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {Array.from({ length: 18 }, (_, i) => (
          <line
            key={i}
            x1={0}
            y1={i * 62}
            x2={1920}
            y2={i * 62}
            stroke="#9E9E9E"
            strokeWidth={0.4}
            opacity={0.03}
          />
        ))}
      </svg>

      {/* Particles */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {particles.map((p, i) => (
          <circle
            key={i}
            cx={p.x + Math.sin(frame * 0.02 + i) * 6}
            cy={p.y}
            r={p.r}
            fill="#9E9E9E"
            opacity={0.07}
          />
        ))}
      </svg>

      {/* Top label */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(3)) * -40}px)`,
          opacity: sp(3),
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 24,
            color: '#9E9E9E',
            letterSpacing: 5,
            textTransform: 'uppercase',
          }}
        >
          जब बाजार गिरता है तो
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 72,
            fontWeight: 900,
            color: '#F5F5F5',
            marginTop: 8,
          }}
        >
          ये सपने <span style={{ color: '#ff0000' }}>खिसक जाते हैं</span>
        </div>
      </div>

      {/* Dream cards — fading out with X */}
      <div
        style={{
          position: 'absolute',
          top: 260,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 60,
        }}
      >
        {dreams.map((dream, i) => {
          const cardOp = sp(dream.delay);
          const strikeWidth = interpolate(
            frame,
            [dream.delay + 25, dream.delay + 45],
            [0, 100],
            { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
          );

          return (
            <div
              key={i}
              style={{
                width: 380,
                opacity: cardOp,
                transform: `translateY(${(1 - cardOp) * 50}px)`,
              }}
            >
              {/* Icon area */}
              <div
                style={{
                  background: '#2D2D2D',
                  borderRadius: 16,
                  padding: '40px 30px',
                  textAlign: 'center',
                  border: '1px solid #3a3a3a',
                  position: 'relative',
                  overflow: 'hidden',
                }}
              >
                {/* SVG Icon */}
                <svg width="80" height="80" viewBox="0 0 80 80" style={{ display: 'block', margin: '0 auto 20px' }}>
                  {dream.icon === 'travel' && (
                    <>
                      <circle cx="40" cy="40" r="36" fill="none" stroke="#e9d700" strokeWidth="2" opacity="0.3" />
                      <path d="M20 50 Q40 20 60 50" fill="none" stroke="#e9d700" strokeWidth="2.5" />
                      <circle cx="40" cy="38" r="6" fill="#e9d700" opacity="0.6" />
                    </>
                  )}
                  {dream.icon === 'family' && (
                    <>
                      <circle cx="28" cy="25" r="8" fill="none" stroke="#FFC107" strokeWidth="2.5" />
                      <circle cx="52" cy="25" r="8" fill="none" stroke="#FFC107" strokeWidth="2.5" />
                      <path d="M18 55 Q28 42 38 55 Q48 42 62 55" fill="none" stroke="#FFC107" strokeWidth="2.5" />
                    </>
                  )}
                  {dream.icon === 'peace' && (
                    <>
                      <circle cx="40" cy="40" r="28" fill="none" stroke="#9E9E9E" strokeWidth="2.5" />
                      <path d="M40 18 L40 62 M18 40 L62 40" stroke="#9E9E9E" strokeWidth="2" opacity="0.5" />
                    </>
                  )}
                </svg>

                <div
                  style={{
                    fontFamily: 'Noto Sans Devanagari',
                    fontSize: 46,
                    fontWeight: 700,
                    color: '#F5F5F5',
                  }}
                >
                  {dream.label}
                </div>

                {/* Strike-through line */}
                <div
                  style={{
                    position: 'absolute',
                    top: '50%',
                    left: 0,
                    height: 4,
                    width: `${strikeWidth}%`,
                    background: '#ff0000',
                    borderRadius: 2,
                    opacity: 0.85,
                  }}
                />

                {/* X overlay */}
                {strikeWidth > 80 && (
                  <div
                    style={{
                      position: 'absolute',
                      inset: 0,
                      background: 'rgba(255,0,0,0.07)',
                      borderRadius: 16,
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'center',
                    }}
                  >
                    <svg width="60" height="60">
                      <line x1="10" y1="10" x2="50" y2="50" stroke="#ff0000" strokeWidth="4" strokeLinecap="round" />
                      <line x1="50" y1="10" x2="10" y2="50" stroke="#ff0000" strokeWidth="4" strokeLinecap="round" />
                    </svg>
                  </div>
                )}
              </div>
            </div>
          );
        })}
      </div>

      {/* Bottom caption */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(70)) * 40}px)`,
          opacity: sp(70),
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 52,
            fontWeight: 700,
            color: '#e9d700',
          }}
        >
          सबसे ज्यादा — भीतर की शांती चली जाती है
        </div>
      </div>

      {/* Stick figure — slumped */}
      <div
        style={{
          position: 'absolute',
          right: 80,
          bottom: 60,
          transform: `translateX(${(1 - sp(60)) * 100}px) translateY(${Math.sin(frame * 0.04) * 3}px)`,
          opacity: sp(60),
        }}
      >
        <svg width="110" height="190" viewBox="0 0 110 190">
          <circle cx="55" cy="95" r="60" fill="#9E9E9E" opacity="0.05" />
          <circle
            cx="55"
            cy="28"
            r="20"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="3"
            transform="rotate(12, 55, 28)"
          />
          <rect x="41" y="23" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
          <rect x="57" y="23" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="51" y1="26" x2="57" y2="26" stroke="#e9d700" strokeWidth="1.5" />
          {/* Slumped body */}
          <path d="M55 48 Q48 85 50 120" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="52" y1="72" x2="25" y2="100" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="52" y1="72" x2="76" y2="95" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="50" y1="120" x2="32" y2="170" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="50" y1="120" x2="66" y2="170" stroke="#F5F5F5" strokeWidth="3" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.65) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6_Ch1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7_Ch1',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_Ch1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Night / moonlight feel
  const moonGlow = interpolate(frame, [0, 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Thought bubble pulse
  const thoughtPulse = 0.85 + Math.sin(frame * 0.08) * 0.12;

  const stars = Array.from({ length: 28 }, (_, i) => ({
    x: seededRandom(i * 11) * 1920,
    y: seededRandom(i * 11 + 1) * 600,
    r: seededRandom(i * 11 + 2) * 2.5 + 0.5,
    twinkle: seededRandom(i * 11 + 3),
  }));

  const particles = Array.from({ length: 10 }, (_, i) => ({
    x: seededRandom(i * 8) * 1920,
    y: seededRandom(i * 8 + 1) * 1080,
    r: seededRandom(i * 8 + 2) * 3 + 1,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Deep night gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, #050818 0%, #0d0d1a 40%, #1C1C1C 100%)',
        }}
      />

      {/* Stars */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {stars.map((s, i) => (
          <circle
            key={i}
            cx={s.x}
            cy={s.y}
            r={s.r}
            fill="#F5F5F5"
            opacity={(0.3 + Math.sin(frame * (0.03 + s.twinkle * 0.05) + i) * 0.25) * moonGlow}
          />
        ))}
      </svg>

      {/* Moon */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          right: 160,
          opacity: moonGlow * 0.7,
        }}
      >
        <svg width="120" height="120" viewBox="0 0 120 120">
          <circle cx="60" cy="60" r="50" fill="#FFC107" opacity={0.12} />
          <circle cx="60" cy="60" r="38" fill="#e9d700" opacity={0.1} />
          <circle cx="60" cy="60" r="28" fill="#FFC107" opacity={0.15} />
        </svg>
      </div>

      {/* Shape layers */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        <circle
          cx={1650}
          cy={200}
          r={280}
          fill="none"
          stroke="#FFC107"
          strokeWidth={1}
          opacity={0.05}
        />
        <rect x={-80} y={800} width={300} height={300} fill="#9E9E9E" opacity={0.03} transform={`rotate(${frame * 0.02}, 70, 950)`} />
      </svg>

      {/* Particles */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {particles.map((p, i) => (
          <circle key={i} cx={p.x} cy={p.y} r={p.r} fill="#9E9E9E" opacity={0.06} />
        ))}
      </svg>

      {/* Stick figure — lying in bed */}
      <div
        style={{
          position: 'absolute',
          left: 120,
          top: '55%',
          transform: `translateY(-50%) translateX(${(1 - sp(5)) * -100}px)`,
          opacity: sp(5),
        }}
      >
        <svg width="320" height="200" viewBox="0 0 320 200">
          {/* Glow */}
          <circle cx="80" cy="80" r="80" fill="#e9d700" opacity="0.04" />
          {/* Bed base */}
          <rect x="40" y="140" width="260" height="20" rx="5" fill="#2D2D2D" stroke="#3a3a3a" strokeWidth="1" />
          <rect x="40" y="120" width="260" height="22" rx="5" fill="#2D2D2D" opacity="0.6" />
          {/* Pillow */}
          <rect x="44" y="112" width="70" height="32" rx="10" fill="#3a3a3a" />
          {/* Figure lying down */}
          <circle
            cx="75"
            cy="100"
            r="18"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="2.5"
            transform={`rotate(${90 + Math.sin(frame * 0.04) * 3}, 75, 100)`}
          />
          {/* Glasses */}
          <rect x="62" y="96" width="9" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <rect x="74" y="96" width="9" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="71" y1="99" x2="74" y2="99" stroke="#e9d700" strokeWidth="1" />
          {/* Body horizontal */}
          <line x1="93" y1="118" x2="270" y2="118" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Arm up — awake */}
          <line x1="150" y1="118" x2="160" y2="100" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Legs */}
          <line x1="250" y1="118" x2="265" y2="138" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="250" y1="118" x2="240" y2="138" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </div>

      {/* Thought bubble — right side */}
      <div
        style={{
          position: 'absolute',
          right: 100,
          top: 140,
          transform: `scale(${thoughtPulse}) translateX(${(1 - sp(25)) * 80}px)`,
          opacity: sp(25),
        }}
      >
        <svg width="600" height="400" viewBox="0 0 600 400">
          {/* Bubble shape */}
          <ellipse cx="300" cy="180" rx="280" ry="160" fill="#2D2D2D" opacity={0.9} />
          <ellipse cx="300" cy="180" rx="280" ry="160" fill="none" stroke="#e9d700" strokeWidth="2" opacity={0.3} />
          {/* Bubble tail */}
          <circle cx="90" cy="330" r="18" fill="#2D2D2D" opacity={0.9} />
          <circle cx="60" cy="360" r="10" fill="#2D2D2D" opacity={0.7} />
          <circle cx="40" cy="380" r="6" fill="#2D2D2D" opacity={0.5} />
        </svg>

        {/* Thought text inside bubble */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 60,
            width: 480,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 52,
              fontWeight: 700,
              color: '#F5F5F5',
              lineHeight: 1.3,
            }}
          >
            कल क्या होगा?
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 36,
              color: '#9E9E9E',
              marginTop: 12,
              lineHeight: 1.4,
            }}
          >
            पैसा काफ़ी होगा?
          </div>
        </div>
      </div>

      {/* Bottom statement */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(65)) * 40}px)`,
          opacity: sp(65),
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 58,
            fontWeight: 900,
            color: '#F5F5F5',
          }}
        >
          ये <span style={{ color: '#e9d700' }}>वास्तविकता</span> है —
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 44,
            color: '#9E9E9E',
            marginTop: 8,
          }}
        >
          जिसे हम समझते ही नहीं
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 48%, rgba(0,0,0,0.72) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7_Ch1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8_Ch1',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_Ch1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Clock hand rotation
  const hourHandAngle = frame * 0.5;
  const minuteHandAngle = frame * 3;

  // Time bar draining
  const timeBarWidth = interpolate(frame, [20, durationInFrames - 20], [100, 15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 13) * 1920,
    y: seededRandom(i * 13 + 1) * 1080,
    r: seededRandom(i * 13 + 2) * 4 + 1.5,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Urgent warm gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #1a0800 0%, #1C1C1C 50%, #0a0a1a 100%)',
        }}
      />

      {/* Shape layers */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        <circle
          cx={960}
          cy={540}
          r={450}
          fill="none"
          stroke="#e9d700"
          strokeWidth={2}
          opacity={0.04}
          strokeDasharray={`${frame % 30} 20`}
        />
        <rect x={1500} y={100} width={350} height={350} fill="#FFC107" opacity={0.03} transform={`rotate(${frame * 0.025}, 1675, 275)`} />
      </svg>

      {/* Concentric circles texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {Array.from({ length: 8 }, (_, i) => (
          <circle
            key={i}
            cx={960}
            cy={540}
            r={80 + i * 70}
            fill="none"
            stroke="#9E9E9E"
            strokeWidth={0.4}
            opacity={0.03}
          />
        ))}
      </svg>

      {/* Particles */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {particles.map((p, i) => (
          <circle
            key={i}
            cx={p.x}
            cy={p.y}
            r={p.r}
            fill={i % 2 === 0 ? '#e9d700' : '#9E9E9E'}
            opacity={0.07}
          />
        ))}
      </svg>

      {/* LEFT — Big Clock SVG */}
      <div
        style={{
          position: 'absolute',
          left: 130,
          top: '50%',
          transform: `translateY(-50%) scale(${sp(5)})`,
          opacity: sp(5),
        }}
      >
        <svg width="420" height="420" viewBox="0 0 420 420">
          {/* Clock glow */}
          <circle cx="210" cy="210" r="200" fill="#e9d700" opacity="0.04" />
          {/* Clock face */}
          <circle cx="210" cy="210" r="185" fill="#2D2D2D" stroke="#e9d700" strokeWidth="3" opacity="0.9" />
          {/* Hour marks */}
          {Array.from({ length: 12 }, (_, i) => {
            const angle = (i * 30 - 90) * (Math.PI / 180);
            return (
              <line
                key={i}
                x1={210 + Math.cos(angle) * 155}
                y1={210 + Math.sin(angle) * 155}
                x2={210 + Math.cos(angle) * 175}
                y2={210 + Math.sin(angle) * 175}
                stroke="#F5F5F5"
                strokeWidth="3"
                opacity="0.5"
              />
            );
          })}
          {/* Hour hand */}
          <line
            x1="210"
            y1="210"
            x2={210 + Math.cos((hourHandAngle - 90) * Math.PI / 180) * 100}
            y2={210 + Math.sin((hourHandAngle - 90) * Math.PI / 180) * 100}
            stroke="#F5F5F5"
            strokeWidth="6"
            strokeLinecap="round"
          />
          {/* Minute hand */}
          <line
            x1="210"
            y1="210"
            x2={210 + Math.cos((minuteHandAngle - 90) * Math.PI / 180) * 145}
            y2={210 + Math.sin((minuteHandAngle - 90) * Math.PI / 180) * 145}
            stroke="#e9d700"
            strokeWidth="4"
            strokeLinecap="round"
          />
          {/* Center dot */}
          <circle cx="210" cy="210" r="8" fill="#e9d700" />
          {/* X mark — time is against you */}
          <line x1="160" y1="320" x2="260" y2="380" stroke="#ff0000" strokeWidth="3" opacity="0.6" />
          <line x1="260" y1="320" x2="160" y2="380" stroke="#ff0000" strokeWidth="3" opacity="0.6" />
        </svg>
      </div>

      {/* RIGHT — Text content */}
      <div
        style={{
          position: 'absolute',
          right: 100,
          top: '50%',
          width: 900,
          transform: `translateY(-50%) translateX(${(1 - sp(20)) * 80}px)`,
          opacity: sp(20),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 26,
            color: '#ff0000',
            letterSpacing: 5,
            marginBottom: 20,
            textTransform: 'uppercase',
          }}
        >
          The Real Problem
        </div>

        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 100,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
          }}
        >
          असली समस्या —
          <br />
          <span style={{ color: '#ff0000' }}>समय</span>{' '}
          <span style={{ color: '#F5F5F5' }}>अब</span>
          <br />
          <span style={{ color: '#9E9E9E' }}>तुम्हारे साथ</span>
          <br />
          <span style={{ color: '#ff0000' }}>नहीं है</span>
        </div>

        {/* Time bar draining */}
        <div
          style={{
            marginTop: 48,
            opacity: interpolate(frame, [40, 58], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#9E9E9E',
              marginBottom: 12,
              textTransform: 'uppercase',
              letterSpacing: 3,
            }}
          >
            Time Remaining
          </div>
          <div
            style={{
              width: 600,
              height: 16,
              background: '#2D2D2D',
              borderRadius: 8,
              overflow: 'hidden',
            }}
          >
            <div
              style={{
                width: `${timeBarWidth}%`,
                height: '100%',
                background: 'linear-gradient(90deg, #ff0000, #FFC107)',
                borderRadius: 8,
                transition: 'width 0.1s',
              }}
            />
          </div>
          <div
            style={{
              marginTop: 12,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 38,
              color: '#9E9E9E',
            }}
          >
            पुनः निर्माण का मौका नहीं है
          </div>
        </div>
      </div>

      {/* Stick figure — looking at clock */}
      <div
        style={{
          position: 'absolute',
          left: 580,
          bottom: 70,
          transform: `translateY(${(1 - sp(55)) * 80}px)`,
          opacity: sp(55),
        }}
      >
        <svg width="100" height="175" viewBox="0 0 100 175">
          <circle cx="50" cy="85" r="55" fill="#e9d700" opacity="0.04" />
          <circle
            cx="50"
            cy="25"
            r="18"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="2.5"
            transform={`rotate(${Math.sin(frame * 0.04) * -8}, 50, 25)`}
          />
          <rect x="38" y="20" width="9" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <rect x="51" y="20" width="9" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="47" y1="23" x2="51" y2="23" stroke="#e9d700" strokeWidth="1" />
          <line x1="50" y1="43" x2="50" y2="105" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Arm pointing at clock */}
          <line x1="50" y1="62" x2="18" y2="50" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="62" x2="78" y2="72" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="105" x2="34" y2="155" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="105" x2="66" y2="155" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 46%, rgba(0,0,0,0.68) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8_Ch1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9_Ch1',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_Ch1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Recovery chart — goes down then bounces back up
  const chartPoints = [
    [0, 150], [80, 180], [160, 220], [240, 300], [320, 380],
    [400, 340], [480, 260], [560, 200], [640, 160], [720, 120],
  ];

  const chartProgress = interpolate(frame, [15, 75], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const visiblePoints = Math.floor(chartProgress * chartPoints.length);

  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 15) * 1920,
    y: seededRandom(i * 15 + 1) * 1080,
    r: seededRandom(i * 15 + 2) * 3 + 1.5,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Hopeful green-tinted gradient for youth era */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #0a1a0a 0%, #1C1C1C 50%, #0d1520 100%)',
        }}
      />

      {/* Shape layers */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        <circle cx={300} cy={300} r={280} fill="#e9d700" opacity={0.03} />
        <polygon
          points={`1700,${800 + Math.sin(frame * 0.006) * 20} 1920,600 1600,900`}
          fill="#FFC107"
          opacity={0.03}
        />
      </svg>

      {/* Diagonal rising lines texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {Array.from({ length: 14 }, (_, i) => (
          <line
            key={i}
            x1={i * 140}
            y1={1080}
            x2={i * 140 + 200}
            y2={0}
            stroke="#e9d700"
            strokeWidth={0.5}
            opacity={0.025}
          />
        ))}
      </svg>

      {/* Particles */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {particles.map((p, i) => (
          <circle key={i} cx={p.x} cy={p.y} r={p.r} fill="#e9d700" opacity={0.07} />
        ))}
      </svg>

      {/* Left — young mindset */}
      <div
        style={{
          position: 'absolute',
          left: 80,
          top: 100,
          transform: `translateX(${(1 - sp(5)) * -80}px)`,
          opacity: sp(5),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 22,
            color: '#e9d700',
            letterSpacing: 5,
            textTransform: 'uppercase',
            marginBottom: 16,
          }}
        >
          When You Were Young
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 80,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
            maxWidth: 700,
          }}
        >
          बाजार गिरता था...
          <br />
          <span style={{ color: '#e9d700' }}>सोचते थे ठीक है!</span>
        </div>

        {/* Three reassurance points */}
        {[
          'अभी सालों बाकी हैं',
          'नौकरी चल रही है',
          'बचत कर सकता हूँ',
        ].map((line, i) => (
          <div
            key={i}
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 16,
              marginTop: 24,
              opacity: interpolate(frame, [30 + i * 18, 46 + i * 18], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              transform: `translateX(${interpolate(frame, [30 + i * 18, 46 + i * 18], [-30, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
            }}
          >
            <svg width="40" height="40" viewBox="0 0 40 40">
              <circle cx="20" cy="20" r="18" fill="#e9d700" opacity={0.2} />
              <polyline points="10,20 17,27 30,13" fill="none" stroke="#e9d700" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 48, color: '#F5F5F5', fontWeight: 600 }}>
              {line}
            </div>
          </div>
        ))}
      </div>

      {/* Right — recovery chart */}
      <div
        style={{
          position: 'absolute',
          right: 80,
          top: 120,
          opacity: interpolate(frame, [10, 28], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 22,
            color: '#9E9E9E',
            letterSpacing: 3,
            marginBottom: 16,
          }}
        >
          Recovery was possible ↑
        </div>
        <svg width="760" height="500" viewBox="0 0 760 500">
          {[100, 200, 300, 400].map((y, i) => (
            <line key={i} x1={0} y1={y} x2={760} y2={y} stroke="#2D2D2D" strokeWidth={1} />
          ))}
          {visiblePoints > 1 && (
            <>
              <polygon
                points={[
                  ...chartPoints.slice(0, visiblePoints).map(p => `${p[0]},${p[1]}`),
                  `${chartPoints[visiblePoints - 1][0]},500`,
                  '0,500',
                ].join(' ')}
                fill="#e9d700"
                opacity={0.06}
              />
              <polyline
                points={chartPoints.slice(0, visiblePoints).map(p => p.join(',')).join(' ')}
                fill="none"
                stroke="#e9d700"
                strokeWidth={3}
                strokeLinecap="round"
                strokeLinejoin="round"
              />
            </>
          )}
          <line x1={0} y1={500} x2={760} y2={500} stroke="#2D2D2D" strokeWidth={2} />
          <line x1={0} y1={0} x2={0} y2={500} stroke="#2D2D2D" strokeWidth={2} />
          {visiblePoints >= chartPoints.length && (
            <text x={640} y={100} fill="#e9d700" fontFamily="JetBrains Mono" fontSize={28} fontWeight="bold">
              ↑ वापस
            </text>
          )}
        </svg>
      </div>

      {/* Young stick figure — energetic pose */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          bottom: 60,
          transform: `translateX(-50%) translateY(${(1 - sp(70)) * 80}px)`,
          opacity: sp(70),
        }}
      >
        <svg width="120" height="200" viewBox="0 0 120 200">
          <circle cx="60" cy="100" r="70" fill="#e9d700" opacity="0.06" />
          <circle
            cx="60"
            cy="28"
            r="20"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.05) * 5}, 60, 28)`}
          />
          <rect x="46" y="23" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
          <rect x="62" y="23" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="56" y1="26" x2="62" y2="26" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="60" y1="48" x2="60" y2="118" stroke="#F5F5F5" strokeWidth="3" />
          {/* Energetic arms up */}
          <line x1="60" y1="70" x2="25" y2="45" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="60" y1="70" x2="95" y2="45" stroke="#F5F5F5" strokeWidth="3" />
          {/* Running legs */}
          <line x1="60" y1="118" x2="38" y2="170" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="60" y1="118" x2="85" y2="160" stroke="#F5F5F5" strokeWidth="3" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.65) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9_Ch1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_Ch1',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_Ch1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Bank balance number counting down
  const balanceStart = 2300000;
  const balanceEnd = 1150000;
  const balance = Math.round(
    interpolate(frame, [40, 100], [balanceStart, balanceEnd], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
      easing: Easing.out(Easing.quad),
    })
  );
  const formatNum = (n: number) => n.toLocaleString('en-IN');

  // Salary bar going to zero
  const salaryBar = interpolate(frame, [20, 75], [100, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 17) * 1920,
    y: seededRandom(i * 17 + 1) * 1080,
    r: seededRandom(i * 17 + 2) * 4 + 1.5,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Heavy serious background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, #0a0a0a 0%, #1C1C1C 45%, #1a1000 100%)',
        }}
      />

      {/* Shape layers */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        <rect
          x={1400}
          y={-100}
          width={600}
          height={600}
          fill="#e9d700"
          opacity={0.03}
          transform={`rotate(${frame * 0.02}, 1700, 200)`}
        />
        <circle cx={200} cy={800} r={300} fill="#2D2D2D" opacity={0.15} />
      </svg>

      {/* Cross-hatch texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {Array.from({ length: 12 }, (_, i) => (
          <React.Fragment key={i}>
            <line x1={i * 160} y1={0} x2={i * 160 + 200} y2={1080} stroke="#F5F5F5" strokeWidth={0.3} opacity={0.02} />
            <line x1={i * 160} y1={0} x2={i * 160 - 200} y2={1080} stroke="#F5F5F5" strokeWidth={0.3} opacity={0.02} />
          </React.Fragment>
        ))}
      </svg>

      {/* Particles */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
        {particles.map((p, i) => (
          <circle key={i} cx={p.x} cy={p.y} r={p.r} fill="#9E9E9E" opacity={0.06} />
        ))}
      </svg>

      {/* Left — salary going to zero */}
      <div
        style={{
          position: 'absolute',
          left: 100,
          top: '50%',
          transform: `translateY(-50%) translateX(${(1 - sp(5)) * -80}px)`,
          opacity: sp(5),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 24,
            color: '#9E9E9E',
            letterSpacing: 4,
            textTransform: 'uppercase',
            marginBottom: 20,
          }}
        >
          Monthly Income
        </div>

        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 72,
            fontWeight: 900,
            color: '#F5F5F5',
            marginBottom: 32,
          }}
        >
          वेतन — <span style={{ color: '#ff0000' }}>खत्म</span>
        </div>

        {/* Salary bar */}
        <div style={{ width: 600 }}>
          <div
            style={{
              display: 'flex',
              justifyContent: 'space-between',
              fontFamily: 'JetBrains Mono',
              fontSize: 20,
              color: '#9E9E9E',
              marginBottom: 10,
            }}
          >
            <span>₹0</span>
            <span>Monthly Salary</span>
          </div>
          <div
            style={{
              width: '100%',
              height: 20,
              background: '#2D2D2D',
              borderRadius: 10,
              overflow: 'hidden',
            }}
          >
            <div
              style={{
                width: `${salaryBar}%`,
                height: '100%',
                background: salaryBar > 30 ? '#e9d700' : '#ff0000',
                borderRadius: 10,
              }}
            />
          </div>
          <div
            style={{
              marginTop: 10,
              fontFamily: 'JetBrains Mono',
              fontSize: 20,
              color: '#ff0000',
              textAlign: 'right',
            }}
          >
            {Math.round(salaryBar)}% remaining
          </div>
        </div>

        <div
          style={{
            marginTop: 40,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 52,
            fontWeight: 700,
            color: '#F5F5F5',
            opacity: interpolate(frame, [50, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          बचाने के लिए
          <br />
          <span style={{ color: '#ff0000' }}>कमाई नहीं है</span>
        </div>
      </div>

      {/* Right — bank account = EVERYTHING */}
      <div
        style={{
          position: 'absolute',
          right: 80,
          top: '50%',
          textAlign: 'right',
          transform: `translateY(-50%) translateX(${(1 - sp(30)) * 90}px)`,
          opacity: sp(30),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 26,
            color: '#e9d700',
            letterSpacing: 4,
            textTransform: 'uppercase',
            marginBottom: 20,
          }}
        >
          Bank Account Balance
        </div>

        {/* Big balance number */}
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 88,
            fontWeight: 900,
            color: frame > 70 ? '#ff0000' : '#e9d700',
            letterSpacing: -2,
            lineHeight: 1,
          }}
        >
          ₹{formatNum(balance)}
        </div>

        <div
          style={{
            marginTop: 24,
            padding: '20px 32px',
            background: frame > 70 ? 'rgba(255,0,0,0.1)' : 'rgba(233,215,0,0.08)',
            border: `2px solid ${frame > 70 ? '#ff0000' : '#e9d700'}`,
            borderRadius: 12,
            display: 'inline-block',
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 52,
              fontWeight: 900,
              color: '#F5F5F5',
            }}
          >
            यही सब कुछ है
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 34,
              color: '#9E9E9E',
              marginTop: 8,
            }}
          >
            कोई दूसरा मौका नहीं
          </div>
        </div>

        {/* Chapter end label */}
        <div
          style={{
            marginTop: 36,
            fontFamily: 'JetBrains Mono',
            fontSize: 22,
            color: '#9E9E9E',
            letterSpacing: 3,
            opacity: interpolate(frame, [85, 105], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          → Chapter 2 coming next
        </div>
      </div>

      {/* Stick figure — empty pockets pose */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          bottom: 55,
          transform: `translateX(-50%) translateY(${(1 - sp(70)) * 80}px) translateY(${Math.sin(frame * 0.04) * 2}px)`,
          opacity: sp(70),
        }}
      >
        <svg width="120" height="200" viewBox="0 0 120 200">
          <circle cx="60" cy="100" r="70" fill="#9E9E9E" opacity="0.05" />
          <circle
            cx="60"
            cy="28"
            r="20"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 28)`}
          />
          <rect x="46" y="23" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
          <rect x="62" y="23" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="56" y1="26" x2="62" y2="26" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="60" y1="48" x2="60" y2="118" stroke="#F5F5F5" strokeWidth="3" />
          {/* Arms out — empty pockets */}
          <line x1="60" y1="70" x2="22" y2="90" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="60" y1="70" x2="98" y2="90" stroke="#F5F5F5" strokeWidth="3" />
          {/* Hands with question marks */}
          <text x="8" y="102" fill="#ff0000" fontSize="22" fontWeight="bold">?</text>
          <text x="100" y="102" fill="#ff0000" fontSize="22" fontWeight="bold">?</text>
          <line x1="60" y1="118" x2="40" y2="170" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="60" y1="118" x2="80" y2="170" stroke="#F5F5F5" strokeWidth="3" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 44%, rgba(0,0,0,0.72) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_Ch1;
```

