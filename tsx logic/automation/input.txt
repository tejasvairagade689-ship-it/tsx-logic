```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_TageRechner',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_TageRechner: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter animation: 0 → 365
  const counterVal = Math.floor(interpolate(frame, [20, 80], [0, 365], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  }));

  // Days "owned" counter
  const ownedDays = Math.floor(interpolate(frame, [60, 100], [0, 124], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  }));

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Hero number shake at frame 80
  const shakeX = frame >= 78 && frame <= 88 ? Math.sin(frame * 1.8) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient Background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 20% 50%, #2a1f00 0%, #1C1C1C 55%, #0d0d0d 100%)',
        transform: `scale(${camScale})`,
      }} />

      {/* Shape Layer 1 — Giant transparent circle drifting */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <div style={{
          position: 'absolute', width: 900, height: 900, borderRadius: '50%',
          border: '1.5px solid #e9d700',
          top: -200, left: -180,
          opacity: 0.04,
          transform: `rotate(${frame * 0.12}deg) scale(${1 + Math.sin(frame * 0.03) * 0.02})`,
        }} />
      </AbsoluteFill>

      {/* Shape Layer 2 — Rectangle top-right */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <div style={{
          position: 'absolute', width: 420, height: 420,
          border: '1px solid #F5F5F5',
          top: 60, right: 80,
          opacity: 0.05,
          transform: `rotate(${45 + frame * 0.08}deg)`,
        }} />
      </AbsoluteFill>

      {/* Shape Layer 3 — Small circle bottom */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', width: 300, height: 300, borderRadius: '50%',
          background: '#e9d700',
          bottom: -120, right: 350,
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
        }} />
      </AbsoluteFill>

      {/* Motion Texture — Horizontal grid lines */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.04 }}>
        {Array.from({ length: 18 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: '100%', height: 1,
            backgroundColor: '#F5F5F5',
            top: i * 62,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        {Array.from({ length: 22 }).map((_, i) => {
          const x = seededRandom(i * 3) * 1920;
          const y = seededRandom(i * 7) * 1080;
          const size = 2 + seededRandom(i * 11) * 4;
          const drift = Math.sin(frame * 0.02 + i) * 18;
          const op = 0.15 + seededRandom(i * 13) * 0.3;
          return (
            <div key={i} style={{
              position: 'absolute',
              width: size, height: size, borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#e9d700' : '#F5F5F5',
              left: x, top: y + drift,
              opacity: op,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main Content */}
      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'flex-start', paddingLeft: 110 }}>

        {/* Left block */}
        <div style={{ display: 'flex', flexDirection: 'column', maxWidth: 780 }}>

          {/* Label */}
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 700,
            color: '#e9d700', letterSpacing: 6, textTransform: 'uppercase',
            opacity: sp(0),
            marginBottom: 18,
          }}>
            DEINE JAHRESABRECHNUNG
          </div>

          {/* Hero Counter */}
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 220,
            color: '#F5F5F5',
            lineHeight: 1,
            transform: `translateX(${shakeX}px) translateY(${sp(10) * -2}px)`,
            opacity: sp(5),
          }}>
            <span style={{ color: '#e9d700' }}>{counterVal}</span>
          </div>

          {/* Label Days */}
          <div style={{
            fontFamily: 'Open Sans, sans-serif', fontSize: 52, fontWeight: 700,
            color: '#F5F5F5',
            opacity: sp(15),
            transform: `translateY(${(1 - sp(15)) * 30}px)`,
            marginTop: -10,
          }}>
            Tage im Jahr arbeitest du.
          </div>

          {/* Divider */}
          <div style={{
            width: interpolate(frame, [40, 80], [0, 560], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            height: 3, backgroundColor: '#e9d700', marginTop: 28, marginBottom: 28,
          }} />

          {/* Owned Days Row */}
          <div style={{
            display: 'flex', alignItems: 'center', gap: 24,
            opacity: sp(50),
            transform: `translateX(${(1 - sp(50)) * -40}px)`,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace', fontSize: 80, fontWeight: 700,
              color: '#ff0000',
            }}>
              {ownedDays}
            </div>
            <div style={{
              fontFamily: 'Open Sans, sans-serif', fontSize: 38, color: '#9E9E9E',
              lineHeight: 1.3,
            }}>
              Tage davon<br />
              <span style={{ color: '#F5F5F5', fontWeight: 700 }}>gehören wirklich dir.</span>
            </div>
          </div>

        </div>
      </AbsoluteFill>

      {/* Stick Figure — Right side */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <div style={{
          position: 'absolute', right: 160, bottom: 80,
          opacity: sp(40),
          transform: `translateY(${(1 - sp(40)) * 60}px)`,
        }}>
          {/* Golden glow */}
          <div style={{
            position: 'absolute', width: 120, height: 120, borderRadius: '50%',
            background: 'radial-gradient(circle, #FFC107 0%, transparent 70%)',
            top: -10, left: 10, opacity: 0.25,
            transform: `scale(${1 + Math.sin(frame * 0.05) * 0.1})`,
          }} />
          <svg width="140" height="240" viewBox="0 0 140 240"
            style={{ transform: `translateY(${Math.sin(frame * 0.05) * 2}px)` }}>
            {/* Head */}
            <circle cx="70" cy="38" r="28" fill="none" stroke="#F5F5F5" strokeWidth="4"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '70px 38px' }} />
            {/* Glasses */}
            <rect x="52" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <rect x="72" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="68" y1="37" x2="72" y2="37" stroke="#FFC107" strokeWidth="2" />
            {/* Body */}
            <line x1="70" y1="66" x2="70" y2="148" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Arms */}
            <line x1="70" y1="90" x2="30" y2="120" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round"
              style={{ transform: `rotate(${Math.sin(frame * 0.05) * 4}deg)`, transformOrigin: '70px 90px' }} />
            <line x1="70" y1="90" x2="110" y2="120" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Legs */}
            <line x1="70" y1="148" x2="44" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="96" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.72) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene1_TageRechner;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_MorgenRitual',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_MorgenRitual: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Items that appear sequentially
  const items = [
    { label: 'Zahnpasta', icon: 'M20 6H4v2h16V6zM4 12h10v2H4v-2zM4 18h7v2H4v-2z', delay: 10 },
    { label: 'Shampoo',   icon: 'M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2z', delay: 25 },
    { label: 'Waschmittel', icon: 'M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2z', delay: 40 },
  ];

  // Camera pan left→right
  const camX = interpolate(frame, [0, durationInFrames], [0, -30], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // MwSt reveal
  const mwstScale = sp(65, { damping: 8, stiffness: 200 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — warm amber tones */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 80% 20%, #1a1200 0%, #1C1C1C 60%, #0a0a0a 100%)',
        transform: `translateX(${camX}px)`,
      }} />

      {/* Shape Layer — Big diagonal rectangle */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          width: 700, height: 700,
          background: '#e9d700',
          opacity: 0.03,
          top: -200, right: -100,
          transform: `rotate(${30 + frame * 0.06}deg)`,
        }} />
      </AbsoluteFill>

      {/* Shape Layer — Thin rings */}
      {[0, 1, 2].map(i => (
        <AbsoluteFill key={i}>
          <div style={{
            position: 'absolute',
            width: 250 + i * 200, height: 250 + i * 200,
            borderRadius: '50%',
            border: `1px solid #FFC107`,
            opacity: 0.04 - i * 0.01,
            bottom: -100 - i * 80, left: -80 - i * 50,
            transform: `scale(${1 + Math.sin(frame * 0.03 + i) * 0.03})`,
          }} />
        </AbsoluteFill>
      ))}

      {/* Motion Texture — Diagonal dashes */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.035 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 80, height: 1.5,
            backgroundColor: '#F5F5F5',
            left: (i % 6) * 340 + 40,
            top: Math.floor(i / 6) * 280 + 60,
            transform: `rotate(-35deg)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {Array.from({ length: 18 }).map((_, i) => {
          const x = seededRandom(i * 5) * 1920;
          const baseY = seededRandom(i * 9) * 1080;
          const rise = interpolate(frame, [0, durationInFrames], [0, -60 * seededRandom(i)], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: baseY + rise,
              width: 3, height: 3, borderRadius: '50%',
              backgroundColor: '#FFC107',
              opacity: 0.2 + seededRandom(i * 3) * 0.25,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main Content */}
      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', justifyContent: 'center', paddingLeft: 120 }}>

        {/* Top label */}
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 5, textTransform: 'uppercase',
          opacity: sp(0), marginBottom: 32,
        }}>
          06:30 UHR — DEIN MORGEN
        </div>

        {/* Items row */}
        <div style={{ display: 'flex', gap: 48, marginBottom: 52 }}>
          {items.map((item, i) => (
            <div key={i} style={{
              display: 'flex', flexDirection: 'column', alignItems: 'center',
              opacity: sp(item.delay),
              transform: `translateY(${(1 - sp(item.delay)) * 50}px)`,
            }}>
              {/* Icon box */}
              <div style={{
                width: 110, height: 110,
                border: '2px solid #e9d700',
                borderRadius: 16,
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                backgroundColor: 'rgba(233, 215, 0, 0.06)',
                marginBottom: 16,
              }}>
                <svg width="52" height="52" viewBox="0 0 24 24" fill="#e9d700">
                  <path d={item.icon} />
                </svg>
              </div>
              <div style={{
                fontFamily: 'Open Sans, sans-serif', fontSize: 34,
                fontWeight: 700, color: '#F5F5F5',
              }}>
                {item.label}
              </div>
              {/* MwSt badge */}
              <div style={{
                marginTop: 10,
                fontFamily: 'JetBrains Mono, monospace', fontSize: 22,
                color: '#ff0000', fontWeight: 700,
                opacity: sp(item.delay + 20),
              }}>
                + 19% MwSt
              </div>
            </div>
          ))}
        </div>

        {/* MwSt highlight */}
        <div style={{
          transform: `scale(${mwstScale})`,
          transformOrigin: 'left center',
          opacity: mwstScale,
          display: 'flex', alignItems: 'center', gap: 24,
        }}>
          <div style={{
            width: 8, height: 80, backgroundColor: '#ff0000', borderRadius: 4,
          }} />
          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 72,
            color: '#F5F5F5', lineHeight: 1,
          }}>
            Natürlich steckt da{' '}
            <span style={{ color: '#ff0000' }}>Mehrwertsteuer</span> drin.
          </div>
        </div>

      </AbsoluteFill>

      {/* Stick Figure — bottom right */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 120, bottom: 60,
          opacity: sp(30),
          transform: `translateX(${(1 - sp(30)) * 80}px)`,
        }}>
          <div style={{
            position: 'absolute', width: 100, height: 100, borderRadius: '50%',
            background: 'radial-gradient(circle, #FFC107 0%, transparent 70%)',
            top: 5, left: 20, opacity: 0.22,
          }} />
          <svg width="120" height="210" viewBox="0 0 140 240"
            style={{ transform: `translateY(${Math.sin(frame * 0.05) * 2}px)` }}>
            <circle cx="70" cy="38" r="28" fill="none" stroke="#F5F5F5" strokeWidth="4"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '70px 38px' }} />
            <rect x="52" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <rect x="72" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="68" y1="37" x2="72" y2="37" stroke="#FFC107" strokeWidth="2" />
            <line x1="70" y1="66" x2="70" y2="148" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="90" x2="28" y2="116" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round"
              style={{ transform: `rotate(${Math.sin(frame * 0.06) * 5}deg)`, transformOrigin: '70px 90px' }} />
            <line x1="70" y1="90" x2="112" y2="116" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="44" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="96" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene2_MorgenRitual;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_SpritAbgaben',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_SpritAbgaben: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Backpack fill animation
  const rucksackItems = [
    { label: 'Energiesteuer', pct: '65%', color: '#ff0000', delay: 15 },
    { label: 'CO₂-Abgabe',   pct: '12%', color: '#FFC107', delay: 30 },
    { label: 'Mehrwertsteuer', pct: '23%', color: '#e9d700', delay: 45 },
  ];

  // Road scroll
  const roadX = interpolate(frame, [0, durationInFrames], [0, -120], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Price counter
  const priceVal = interpolate(frame, [10, 60], [0, 1.89], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  }).toFixed(2);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — deep charcoal with red tint */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #1C1C1C 0%, #2D0000 60%, #1C1C1C 100%)',
      }} />

      {/* Road stripes — motion texture */}
      <AbsoluteFill style={{ position: 'absolute', bottom: 0 }}>
        <div style={{
          position: 'absolute', bottom: 0, left: 0, right: 0, height: 120,
          backgroundColor: '#2D2D2D',
        }} />
        {Array.from({ length: 16 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', bottom: 55,
            left: (i * 160 + roadX) % 1920 - 80,
            width: 100, height: 10,
            backgroundColor: '#e9d700', borderRadius: 5,
            opacity: 0.7,
          }} />
        ))}
      </AbsoluteFill>

      {/* Shape Layer — large triangle */}
      <AbsoluteFill>
        <svg style={{ position: 'absolute', top: 0, right: 0, opacity: 0.04 }} width="600" height="600" viewBox="0 0 600 600">
          <polygon points="600,0 600,600 0,0" fill="#e9d700"
            style={{ transform: `rotate(${frame * 0.04}deg)`, transformOrigin: '300px 300px' }} />
        </svg>
      </AbsoluteFill>

      {/* Shape Layer — drifting circle left */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', width: 500, height: 500, borderRadius: '50%',
          border: '2px solid #ff0000',
          left: -200, top: 200,
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.04})`,
        }} />
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {Array.from({ length: 20 }).map((_, i) => {
          const x = seededRandom(i * 4) * 1920;
          const y = seededRandom(i * 8) * 900;
          const drift = Math.sin(frame * 0.03 + i * 0.5) * 15;
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: y + drift,
              width: 2.5, height: 2.5, borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#ff0000' : '#e9d700',
              opacity: 0.18 + seededRandom(i * 6) * 0.2,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main Content */}
      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', paddingLeft: 110, paddingRight: 100, paddingBottom: 120 }}>

        {/* Left: Price display */}
        <div style={{ display: 'flex', flexDirection: 'column' }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 700,
            color: '#9E9E9E', letterSpacing: 5, textTransform: 'uppercase',
            opacity: sp(0), marginBottom: 20,
          }}>
            1 LITER SPRIT
          </div>

          {/* Fuel icon + price */}
          <div style={{ display: 'flex', alignItems: 'center', gap: 30, marginBottom: 32 }}>
            <div style={{ opacity: sp(8), transform: `scale(${sp(8)})` }}>
              <svg width="80" height="80" viewBox="0 0 24 24" fill="#e9d700">
                <path d="M19.77 7.23l.01-.01-3.72-3.72L15 4.56l2.11 2.11c-.94.36-1.61 1.26-1.61 2.33 0 1.38 1.12 2.5 2.5 2.5.36 0 .69-.08 1-.21v7.21c0 .55-.45 1-1 1s-1-.45-1-1V14c0-1.1-.9-2-2-2h-1V5c0-1.1-.9-2-2-2H6C4.9 3 4 3.9 4 5v16h10V13.5h1.5v4c0 1.38 1.12 2.5 2.5 2.5s2.5-1.12 2.5-2.5V9c0-.69-.28-1.32-.73-1.77zM18 10c-.55 0-1-.45-1-1s.45-1 1-1 1 .45 1 1-.45 1-1 1zM6 11V5h6v6H6z" />
              </svg>
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 130, fontWeight: 700,
              color: '#F5F5F5',
              opacity: sp(5),
              lineHeight: 1,
            }}>
              {priceVal}<span style={{ fontSize: 60, color: '#9E9E9E' }}>€</span>
            </div>
          </div>

          {/* "davon echter Sprit" */}
          <div style={{
            fontFamily: 'Open Sans, sans-serif', fontSize: 38,
            color: '#9E9E9E',
            opacity: sp(35),
            transform: `translateY(${(1 - sp(35)) * 20}px)`,
          }}>
            Davon echter Kraftstoff:{' '}
            <span style={{ color: '#F5F5F5', fontWeight: 700 }}>~0,59€</span>
          </div>

          <div style={{ marginTop: 16, opacity: sp(50), transform: `translateX(${(1 - sp(50)) * -20}px)` }}>
            <div style={{
              fontFamily: 'Anton, sans-serif', fontSize: 52,
              color: '#ff0000',
            }}>
              1,30€ = reiner Rucksack aus Abgaben.
            </div>
          </div>
        </div>

        {/* Right: Abgaben breakdown bars */}
        <div style={{
          display: 'flex', flexDirection: 'column', gap: 22, width: 420,
          opacity: sp(45),
          transform: `translateX(${(1 - sp(45)) * 60}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 24, fontWeight: 700,
            color: '#9E9E9E', letterSpacing: 4, textTransform: 'uppercase',
            marginBottom: 10,
          }}>
            DER RUCKSACK
          </div>
          {rucksackItems.map((item, i) => {
            const barW = interpolate(frame, [item.delay + 40, item.delay + 80], [0, 420], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
              easing: Easing.out(Easing.quad),
            });
            return (
              <div key={i} style={{ opacity: sp(item.delay) }}>
                <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>
                  <span style={{ fontFamily: 'Open Sans, sans-serif', fontSize: 30, color: '#F5F5F5', fontWeight: 600 }}>
                    {item.label}
                  </span>
                  <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: item.color, fontWeight: 700 }}>
                    {item.pct}
                  </span>
                </div>
                <div style={{ width: 420, height: 14, backgroundColor: '#2D2D2D', borderRadius: 7, overflow: 'hidden' }}>
                  <div style={{ width: barW, height: '100%', backgroundColor: item.color, borderRadius: 7 }} />
                </div>
              </div>
            );
          })}
        </div>

      </AbsoluteFill>

      {/* Stick Figure on road */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 80, bottom: 110,
          opacity: sp(20),
          transform: `translateY(${(1 - sp(20)) * 40}px)`,
        }}>
          <div style={{
            position: 'absolute', width: 90, height: 90, borderRadius: '50%',
            background: 'radial-gradient(circle, #FFC107 0%, transparent 70%)',
            top: 5, left: 15, opacity: 0.2,
          }} />
          <svg width="110" height="195" viewBox="0 0 140 240"
            style={{ transform: `translateY(${Math.sin(frame * 0.05) * 2}px)` }}>
            <circle cx="70" cy="38" r="28" fill="none" stroke="#F5F5F5" strokeWidth="4"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '70px 38px' }} />
            <rect x="52" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <rect x="72" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="68" y1="37" x2="72" y2="37" stroke="#FFC107" strokeWidth="2" />
            <line x1="70" y1="66" x2="70" y2="148" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="90" x2="30" y2="118" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round"
              style={{ transform: `rotate(${Math.sin(frame * 0.07) * 8}deg)`, transformOrigin: '70px 90px' }} />
            <line x1="70" y1="90" x2="110" y2="118" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="44" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="96" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene3_SpritAbgaben;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4_Fleischwolf',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_Fleischwolf: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Brutto → Netto animation
  const bruttoAmt = 4000;
  const slices = [
    { label: 'Lohnsteuer', amount: 680, color: '#ff0000', delay: 30 },
    { label: 'Kirchensteuer', amount: 62, color: '#e9d700', delay: 45 },
    { label: 'Solidaritätszuschlag', amount: 28, color: '#FFC107', delay: 58 },
    { label: 'Rentenversicherung', amount: 372, color: '#9E9E9E', delay: 70 },
    { label: 'Krankenversicherung', amount: 314, color: '#9E9E9E', delay: 82 },
  ];
  const nettoAmt = bruttoAmt - slices.reduce((a, s) => a + s.amount, 0);

  // Camera tilt
  const camRot = interpolate(frame, [0, 60], [1.5, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Grinder spin
  const grindAngle = interpolate(frame, [0, durationInFrames], [0, 360 * 2], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — dramatic red-dark */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 0%, #300000 0%, #1C1C1C 55%, #0d0d0d 100%)',
        transform: `rotate(${camRot}deg)`,
      }} />

      {/* Shape Layer — spinning gear/cog suggestion */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', width: 600, height: 600,
          border: '2px dashed #ff0000',
          borderRadius: '50%',
          top: -250, right: -150,
          opacity: 0.05,
          transform: `rotate(${grindAngle}deg)`,
        }} />
      </AbsoluteFill>

      {/* Shape Layer — rectangle bars pattern */}
      <AbsoluteFill style={{ opacity: 0.035 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 8, height: 1080,
            backgroundColor: '#F5F5F5',
            left: 60 + i * 160,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {Array.from({ length: 16 }).map((_, i) => {
          const x = seededRandom(i * 7) * 1920;
          const y = seededRandom(i * 11) * 1080;
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: y + Math.sin(frame * 0.04 + i) * 12,
              width: 3, height: 3, borderRadius: '50%',
              backgroundColor: '#ff0000',
              opacity: 0.15 + seededRandom(i) * 0.2,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Main Content */}
      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', paddingLeft: 110, paddingRight: 100 }}>

        {/* LEFT: Brutto card */}
        <div style={{
          opacity: sp(0),
          transform: `translateY(${(1 - sp(0)) * -40}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 24, fontWeight: 700,
            color: '#9E9E9E', letterSpacing: 5, textTransform: 'uppercase', marginBottom: 12,
          }}>
            BRUTTOGEHALT
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace', fontSize: 110,
            color: '#F5F5F5', fontWeight: 700, lineHeight: 1,
          }}>
            {bruttoAmt}<span style={{ fontSize: 52, color: '#9E9E9E' }}>€</span>
          </div>
          <div style={{
            width: 320, height: 4, backgroundColor: '#2D2D2D',
            borderRadius: 2, marginTop: 18,
          }} />
        </div>

        {/* CENTER: Fleischwolf arrow / SVG grinder */}
        <div style={{
          display: 'flex', flexDirection: 'column', alignItems: 'center',
          opacity: sp(20),
        }}>
          <svg width="160" height="120" viewBox="0 0 160 120"
            style={{ transform: `rotate(${Math.sin(frame * 0.1) * 4}deg)` }}>
            {/* Arrow right with grind effect */}
            <defs>
              <marker id="ah" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto">
                <polygon points="0 0, 10 3.5, 0 7" fill="#ff0000" />
              </marker>
            </defs>
            <line x1="10" y1="60" x2="130" y2="60" stroke="#ff0000" strokeWidth="6"
              markerEnd="url(#ah)" strokeDasharray="14,8"
              strokeDashoffset={-frame * 2} />
            <text x="80" y="38" textAnchor="middle" fontFamily="Anton" fontSize="20" fill="#ff0000">
              FLEISCHWOLF
            </text>
          </svg>
          {/* Spinning gear */}
          <svg width="80" height="80" viewBox="0 0 24 24"
            style={{ transform: `rotate(${grindAngle}deg)`, opacity: 0.7 }}>
            <path d="M12 15.5A3.5 3.5 0 0 1 8.5 12 3.5 3.5 0 0 1 12 8.5a3.5 3.5 0 0 1 3.5 3.5 3.5 3.5 0 0 1-3.5 3.5m7.43-2.92c.04-.3.07-.62.07-.96s-.03-.66-.07-1l2.16-1.68c.19-.15.24-.42.12-.64l-2.04-3.54c-.12-.22-.39-.3-.61-.22l-2.55 1.02c-.52-.4-1.08-.73-1.69-.98L14.17 4.1c-.04-.24-.24-.41-.48-.41h-4.08c-.24 0-.43.17-.47.41L8.72 6.57c-.61.25-1.17.59-1.69.98L4.48 6.53c-.22-.08-.49 0-.61.22L1.83 10.3c-.12.22-.07.49.12.64L4.11 12.62c-.04.34-.07.67-.07 1s.03.66.07 1L1.95 16.3c-.19.15-.24.42-.12.64l2.04 3.54c.12.22.39.3.61.22l2.55-1.02c.52.4 1.08.73 1.69.98l.38 2.47c.04.24.24.41.48.41h4.08c.24 0 .44-.17.47-.41l.38-2.47c.61-.25 1.17-.58 1.69-.98l2.55 1.02c.22.08.49 0 .61-.22l2.04-3.54c.12-.22.07-.49-.12-.64l-2.16-1.68z"
              fill="#2D2D2D" />
          </svg>
        </div>

        {/* RIGHT: Abzüge list + Netto */}
        <div style={{ display: 'flex', flexDirection: 'column', width: 520 }}>
          {slices.map((s, i) => (
            <div key={i} style={{
              display: 'flex', justifyContent: 'space-between', alignItems: 'center',
              marginBottom: 14,
              opacity: sp(s.delay),
              transform: `translateX(${(1 - sp(s.delay)) * 40}px)`,
            }}>
              <div style={{
                display: 'flex', alignItems: 'center', gap: 12,
              }}>
                <div style={{ width: 10, height: 36, backgroundColor: s.color, borderRadius: 3 }} />
                <span style={{ fontFamily: 'Open Sans, sans-serif', fontSize: 30, color: '#F5F5F5', fontWeight: 600 }}>
                  {s.label}
                </span>
              </div>
              <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: s.color, fontWeight: 700 }}>
                -{s.amount}€
              </span>
            </div>
          ))}

          {/* Netto result */}
          <div style={{
            marginTop: 20,
            borderTop: '3px solid #e9d700',
            paddingTop: 20,
            opacity: sp(90),
            transform: `scale(${sp(90, { damping: 8, stiffness: 180 })})`,
            transformOrigin: 'left center',
          }}>
            <div style={{
              fontFamily: 'Inter, sans-serif', fontSize: 24,
              color: '#9E9E9E', letterSpacing: 4, textTransform: 'uppercase',
              marginBottom: 8,
            }}>
              NETTO (Realität)
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace', fontSize: 96,
              color: '#e9d700', fontWeight: 700, lineHeight: 1,
            }}>
              {nettoAmt}<span style={{ fontSize: 44, color: '#9E9E9E' }}>€</span>
            </div>
          </div>
        </div>

      </AbsoluteFill>

      {/* Stick Figure — shocked pose */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 80, bottom: 60,
          opacity: sp(25),
          transform: `translateX(${(1 - sp(25)) * -50}px)`,
        }}>
          <div style={{
            position: 'absolute', width: 90, height: 90, borderRadius: '50%',
            background: 'radial-gradient(circle, #FFC107 0%, transparent 70%)',
            top: 5, left: 15, opacity: 0.2,
          }} />
          <svg width="110" height="195" viewBox="0 0 140 240"
            style={{ transform: `translateY(${Math.sin(frame * 0.05) * 2}px)` }}>
            <circle cx="70" cy="38" r="28" fill="none" stroke="#F5F5F5" strokeWidth="4"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 5}deg)`, transformOrigin: '70px 38px' }} />
            <rect x="52" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <rect x="72" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="68" y1="37" x2="72" y2="37" stroke="#FFC107" strokeWidth="2" />
            <line x1="70" y1="66" x2="70" y2="148" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Arms raised (shocked) */}
            <line x1="70" y1="85" x2="22" y2="55" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="85" x2="118" y2="55" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="44" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="96" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.78) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene4_Fleischwolf;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5_NormalerMensch',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_NormalerMensch: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Traits that appear
  const traits = [
    { text: 'Pünktlich', icon: 'M11.99 2C6.47 2 2 6.48 2 12s4.47 10 9.99 10C17.52 22 22 17.52 22 12S17.52 2 11.99 2zM12 20c-4.42 0-8-3.58-8-8s3.58-8 8-8 8 3.58 8 8-3.58 8-8 8zm.5-13H11v6l5.25 3.15.75-1.23-4.5-2.67V7z', delay: 20 },
    { text: 'Fleißig', icon: 'M20 6h-2.18c.07-.44.18-.88.18-1.36C18 2.1 15.9 0 13.36 0c-1.33 0-2.43.5-3.23 1.34L9 3.5 7.87 1.34C7.07.5 5.97 0 4.64 0 2.1 0 0 2.1 0 4.64c0 .48.11.92.18 1.36H0v2h20V6z', delay: 35 },
    { text: 'Regelkonform', icon: 'M12 2L4 5v6.09c0 5.05 3.41 9.76 8 10.91 4.59-1.15 8-5.86 8-10.91V5l-8-3z', delay: 50 },
    { text: 'Familiäre Verantwortung', icon: 'M16 11c1.66 0 2.99-1.34 2.99-3S17.66 5 16 5c-1.66 0-3 1.34-3 3s1.34 3 3 3zm-8 0c1.66 0 2.99-1.34 2.99-3S9.66 5 8 5C6.34 5 5 6.34 5 8s1.34 3 3 3zm0 2c-2.33 0-7 1.17-7 3.5V19h14v-2.5c0-2.33-4.67-3.5-7-3.5zm8 0c-.29 0-.62.02-.97.05 1.16.84 1.97 1.97 1.97 3.45V19h6v-2.5c0-2.33-4.67-3.5-7-3.5z', delay: 65 },
  ];

  // Camera zoom-in
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Hurt text pulse
  const hurtPulse = 1 + Math.sin(frame * 0.15) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — muted blue-grey */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 70%, #0d1a2a 0%, #1C1C1C 55%, #0a0a0a 100%)',
        transform: `scale(${camScale})`,
      }} />

      {/* Shape Layer — large hexagon outline */}
      <AbsoluteFill>
        <svg style={{ position: 'absolute', right: -100, top: -100, opacity: 0.04 }} width="700" height="700" viewBox="0 0 700 700">
          <polygon points="350,0 700,175 700,525 350,700 0,525 0,175"
            fill="none" stroke="#F5F5F5" strokeWidth="2"
            style={{ transform: `rotate(${frame * 0.05}deg)`, transformOrigin: '350px 350px' }} />
        </svg>
      </AbsoluteFill>

      {/* Shape Layer — subtle corner rectangles */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <div style={{ position: 'absolute', top: 40, left: 40, width: 180, height: 180, border: '1px solid #F5F5F5' }} />
        <div style={{ position: 'absolute', bottom: 40, right: 40, width: 140, height: 140, border: '1px solid #e9d700', transform: `rotate(${frame * 0.07}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — dot grid */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        {Array.from({ length: 20 }).map((_, row) =>
          Array.from({ length: 36 }).map((_, col) => (
            <div key={`${row}-${col}`} style={{
              position: 'absolute',
              width: 3, height: 3, borderRadius: '50%',
              backgroundColor: '#F5F5F5',
              left: col * 54 + 20, top: row * 56 + 20,
            }} />
          ))
        )}
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {Array.from({ length: 14 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(i * 3) * 1920,
            top: seededRandom(i * 7) * 1080 + Math.sin(frame * 0.025 + i) * 20,
            width: 2, height: 2, borderRadius: '50%',
            backgroundColor: '#F5F5F5', opacity: 0.12 + seededRandom(i * 9) * 0.18,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main Content */}
      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', justifyContent: 'center', paddingLeft: 110, paddingRight: 100 }}>

        {/* Pain statement */}
        <div style={{
          opacity: sp(0),
          transform: `translateY(${(1 - sp(0)) * -30}px)`,
          marginBottom: 48,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 700,
            color: '#ff0000', letterSpacing: 5, textTransform: 'uppercase',
            marginBottom: 14,
          }}>
            DER Teil, DER WEHTUT
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: interpolate(frame, [0, 30], [60, 76], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            color: '#F5F5F5', lineHeight: 1.1,
            transform: `scale(${hurtPulse})`,
            transformOrigin: 'left center',
          }}>
            Selbst wenn du ein{' '}
            <span style={{ color: '#e9d700' }}>ganz normaler,</span>
            <br />
            fleißiger Mensch bist...
          </div>
        </div>

        {/* Traits grid */}
        <div style={{ display: 'flex', gap: 36, flexWrap: 'wrap' }}>
          {traits.map((t, i) => (
            <div key={i} style={{
              display: 'flex', alignItems: 'center', gap: 16,
              opacity: sp(t.delay),
              transform: `translateY(${(1 - sp(t.delay)) * 30}px)`,
              backgroundColor: 'rgba(245,245,245,0.05)',
              border: '1px solid rgba(245,245,245,0.12)',
              borderRadius: 14, padding: '14px 24px',
            }}>
              <svg width="36" height="36" viewBox="0 0 24 24" fill="#e9d700">
                <path d={t.icon} />
              </svg>
              <span style={{ fontFamily: 'Open Sans, sans-serif', fontSize: 34, fontWeight: 700, color: '#F5F5F5' }}>
                {t.text}
              </span>
            </div>
          ))}
        </div>

        {/* Bottom punchline */}
        <div style={{
          marginTop: 44,
          opacity: sp(78),
          transform: `translateX(${(1 - sp(78)) * -30}px)`,
        }}>
          <div style={{
            fontFamily: 'Open Sans, sans-serif', fontSize: 44, color: '#9E9E9E', lineHeight: 1.4,
          }}>
            ...und irgendwie versuchst, aus dem, was übrig bleibt,{' '}
            <span style={{ color: '#F5F5F5', fontWeight: 700 }}>ein Leben zu bauen.</span>
          </div>
        </div>

      </AbsoluteFill>

      {/* Stick Figure — walking */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 100, top: '50%',
          transform: `translateY(calc(-50% + ${Math.sin(frame * 0.05) * 2}px))`,
          opacity: sp(30),
        }}>
          <div style={{
            position: 'absolute', width: 100, height: 100, borderRadius: '50%',
            background: 'radial-gradient(circle, #FFC107 0%, transparent 70%)',
            top: 0, left: 15, opacity: 0.22,
          }} />
          <svg width="120" height="210" viewBox="0 0 140 240">
            <circle cx="70" cy="38" r="28" fill="none" stroke="#F5F5F5" strokeWidth="4"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '70px 38px' }} />
            <rect x="52" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <rect x="72" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="68" y1="37" x2="72" y2="37" stroke="#FFC107" strokeWidth="2" />
            <line x1="70" y1="66" x2="70" y2="148" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Walking arms */}
            <line x1="70" y1="90" x2="32" y2="120" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round"
              style={{ transform: `rotate(${Math.sin(frame * 0.12) * 18}deg)`, transformOrigin: '70px 90px' }} />
            <line x1="70" y1="90" x2="108" y2="120" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round"
              style={{ transform: `rotate(${-Math.sin(frame * 0.12) * 18}deg)`, transformOrigin: '70px 90px' }} />
            {/* Walking legs */}
            <line x1="70" y1="148" x2="48" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round"
              style={{ transform: `rotate(${Math.sin(frame * 0.12) * 14}deg)`, transformOrigin: '70px 148px' }} />
            <line x1="70" y1="148" x2="92" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round"
              style={{ transform: `rotate(${-Math.sin(frame * 0.12) * 14}deg)`, transformOrigin: '70px 148px' }} />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 48%, rgba(0,0,0,0.72) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene5_NormalerMensch;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6_ZigStellen',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_ZigStellen: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Many small payment dots appearing
  const paymentNodes = [
    { x: 280, y: 200, label: '–7€', delay: 10 },
    { x: 520, y: 340, label: '–23€', delay: 18 },
    { x: 820, y: 160, label: '–14€', delay: 26 },
    { x: 430, y: 520, label: '–39€', delay: 33 },
    { x: 1050, y: 390, label: '–11€', delay: 40 },
    { x: 750, y: 640, label: '–8€', delay: 47 },
    { x: 1220, y: 220, label: '–52€', delay: 54 },
    { x: 940, y: 730, label: '–19€', delay: 60 },
    { x: 1380, y: 480, label: '–4€', delay: 66 },
    { x: 620, y: 760, label: '–31€', delay: 72 },
    { x: 1500, y: 300, label: '–6€', delay: 77 },
    { x: 1100, y: 580, label: '–17€', delay: 82 },
  ];

  // "müde" word — slowly droops
  const muedeDrop = interpolate(frame, [70, durationInFrames - 20], [0, 12], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — very dark, low energy */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, #181818 0%, #0d0d0d 100%)',
      }} />

      {/* Shape Layer — big faint X pattern */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <div style={{
          position: 'absolute', width: '100%', height: 3,
          backgroundColor: '#9E9E9E', top: '50%',
          transform: 'rotate(-15deg)',
        }} />
        <div style={{
          position: 'absolute', width: '100%', height: 3,
          backgroundColor: '#9E9E9E', top: '50%',
          transform: 'rotate(15deg)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer — faint horizontal dashes */}
      <AbsoluteFill style={{ opacity: 0.035 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', width: '100%', height: 1,
            backgroundColor: '#F5F5F5',
            top: 120 + i * 120,
          }} />
        ))}
      </AbsoluteFill>

      {/* Payment nodes — scattered */}
      <AbsoluteFill>
        {paymentNodes.map((node, i) => {
          const show = sp(node.delay, { damping: 10, stiffness: 200 });
          return (
            <div key={i} style={{
              position: 'absolute',
              left: node.x, top: node.y,
              transform: `translate(-50%, -50%) scale(${show})`,
              opacity: show,
            }}>
              <div style={{
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                width: 90, height: 90, borderRadius: '50%',
                backgroundColor: 'rgba(255, 0, 0, 0.1)',
                border: `2px solid #ff0000`,
              }}>
                <span style={{
                  fontFamily: 'JetBrains Mono, monospace', fontSize: 26,
                  color: '#ff0000', fontWeight: 700,
                }}>
                  {node.label}
                </span>
              </div>
              {/* Connecting line to center */}
              <svg style={{ position: 'absolute', top: '50%', left: '50%', pointerEvents: 'none', opacity: 0.15 }}
                width="600" height="600" viewBox="-300 -300 600 600">
                <line x1="0" y1="0" x2={960 - node.x} y2={540 - node.y} stroke="#9E9E9E" strokeWidth="1" strokeDasharray="6,8" />
              </svg>
            </div>
          );
        })}
      </AbsoluteFill>

      {/* Floating Particles — slow drift */}
      <AbsoluteFill>
        {Array.from({ length: 12 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(i * 5) * 1920,
            top: seededRandom(i * 8) * 1080 + Math.sin(frame * 0.02 + i) * 25,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#9E9E9E', opacity: 0.12,
          }} />
        ))}
      </AbsoluteFill>

      {/* Center text overlay */}
      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{
          textAlign: 'center',
          opacity: sp(0),
          transform: `translateY(${(1 - sp(0)) * -20}px)`,
          marginBottom: 32,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 700,
            color: '#9E9E9E', letterSpacing: 6, textTransform: 'uppercase', marginBottom: 20,
          }}>
            VERTEILT — UNSICHTBAR — DAUERHAFT
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 96,
            color: '#F5F5F5', lineHeight: 1.05,
          }}>
            Du zahlst{' '}
            <span style={{ color: '#ff0000' }}>nicht einmal.</span>
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 96,
            color: '#F5F5F5', lineHeight: 1.05,
          }}>
            Du zahlst{' '}
            <span style={{ color: '#e9d700' }}>immer wieder.</span>
          </div>
        </div>

        {/* Müde punchline */}
        <div style={{
          opacity: sp(72),
          transform: `translateY(${muedeDrop + (1 - sp(72)) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Open Sans, sans-serif', fontSize: 52, color: '#9E9E9E',
            textAlign: 'center', fontStyle: 'italic',
          }}>
            So verteilt, dass du nicht wütest —
            <br />
            sondern{' '}
            <span style={{
              fontFamily: 'Anton, sans-serif', fontSize: 64,
              color: '#9E9E9E',
              opacity: 0.7,
            }}>
              müde.
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure — drooping, tired */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 80, bottom: 60,
          opacity: sp(35),
          transform: `translateY(${muedeDrop * 1.5}px) translateX(${(1 - sp(35)) * 60}px)`,
        }}>
          <svg width="110" height="195" viewBox="0 0 140 240">
            {/* Head drooped slightly */}
            <circle cx="70" cy="38" r="28" fill="none" stroke="#9E9E9E" strokeWidth="4"
              style={{ transform: `rotate(${8 + Math.sin(frame * 0.03) * 1}deg)`, transformOrigin: '70px 38px' }} />
            <rect x="52" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <rect x="72" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="68" y1="37" x2="72" y2="37" stroke="#FFC107" strokeWidth="2" />
            <line x1="70" y1="66" x2="70" y2="148" stroke="#9E9E9E" strokeWidth="4" strokeLinecap="round" />
            {/* Arms drooped */}
            <line x1="70" y1="100" x2="36" y2="140" stroke="#9E9E9E" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="100" x2="104" y2="140" stroke="#9E9E9E" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="50" y2="210" stroke="#9E9E9E" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="90" y2="210" stroke="#9E9E9E" strokeWidth="4" strokeLinecap="round" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.82) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene6_ZigStellen;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7_HostIntro',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_HostIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Golden spotlight pulse
  const spotPulse = 1 + Math.sin(frame * 0.06) * 0.08;

  // Name reveal letter by letter effect
  const nameReveal = interpolate(frame, [15, 50], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Two mission lines
  const line1Op = sp(45);
  const line2Op = sp(65);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — premium gold spotlight */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 35% 50%, #1a1200 0%, #1C1C1C 50%, #0d0d0d 100%)',
      }} />

      {/* Spotlight beam */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <div style={{
          position: 'absolute',
          width: 600, height: 900,
          background: 'radial-gradient(ellipse at center, rgba(233,215,0,0.08) 0%, transparent 70%)',
          left: 80, top: '50%',
          transform: `translateY(-50%) scale(${spotPulse})`,
          borderRadius: '50%',
        }} />
      </AbsoluteFill>

      {/* Shape Layer — large circle arcs */}
      <AbsoluteFill>
        {[0, 1, 2].map(i => (
          <div key={i} style={{
            position: 'absolute',
            width: 400 + i * 200, height: 400 + i * 200,
            borderRadius: '50%',
            border: `1.5px solid #FFC107`,
            opacity: 0.04 - i * 0.01,
            left: -100 - i * 80, top: '50%',
            transform: `translateY(-50%) rotate(${frame * (0.04 + i * 0.02)}deg)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Motion texture — vertical scan lines */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', width: 1, height: '100%',
            backgroundColor: '#F5F5F5', left: i * 64 + 32,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating Particles — gold */}
      <AbsoluteFill>
        {Array.from({ length: 20 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(i * 3) * 1920,
            top: seededRandom(i * 7) * 1080 + Math.sin(frame * 0.03 + i * 0.7) * 18,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#FFC107', opacity: 0.15 + seededRandom(i * 11) * 0.22,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main Content */}
      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', paddingLeft: 320, paddingRight: 100 }}>

        <div style={{ maxWidth: 900 }}>

          {/* Name reveal */}
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 700,
            color: '#FFC107', letterSpacing: 5, textTransform: 'uppercase',
            opacity: sp(0), marginBottom: 20,
          }}>
            DER MENSCH HINTER KUX LEGT AN
          </div>

          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 160,
            color: '#F5F5F5', lineHeight: 0.9,
            opacity: nameReveal,
            transform: `translateY(${(1 - nameReveal) * 30}px)`,
            marginBottom: 16,
          }}>
            ICH BIN<br />
            <span style={{ color: '#e9d700' }}>KUNAL.</span>
          </div>

          {/* Divider with glow */}
          <div style={{
            width: interpolate(frame, [30, 65], [0, 640], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            height: 4, backgroundColor: '#e9d700',
            borderRadius: 2, marginBottom: 36,
            boxShadow: '0 0 20px rgba(233,215,0,0.4)',
          }} />

          {/* Mission line 1 */}
          <div style={{
            fontFamily: 'Open Sans, sans-serif', fontSize: 42,
            color: '#9E9E9E', lineHeight: 1.4,
            opacity: line1Op,
            transform: `translateX(${(1 - line1Op) * -30}px)`,
            marginBottom: 20,
          }}>
            Nicht, um dir das Land{' '}
            <span style={{ color: '#ff0000', fontWeight: 700 }}>schlechtzureden.</span>
          </div>

          {/* Mission line 2 */}
          <div style={{
            fontFamily: 'Open Sans, sans-serif', fontSize: 42,
            color: '#F5F5F5', lineHeight: 1.4, fontWeight: 700,
            opacity: line2Op,
            transform: `translateX(${(1 - line2Op) * -30}px)`,
          }}>
            Sondern um dir die{' '}
            <span style={{ color: '#e9d700' }}>Mechanik zu zeigen,</span>
            <br />
            die auf deinem Konto wirkt.
          </div>

        </div>
      </AbsoluteFill>

      {/* Big Stick Figure — Host character, left side */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 80, top: '50%',
          transform: `translateY(calc(-50% + ${Math.sin(frame * 0.04) * 3}px))`,
          opacity: sp(5),
        }}>
          {/* Big golden glow */}
          <div style={{
            position: 'absolute', width: 200, height: 200, borderRadius: '50%',
            background: 'radial-gradient(circle, #FFC107 0%, transparent 70%)',
            top: -10, left: 20, opacity: 0.3 * spotPulse,
          }} />
          <svg width="220" height="380" viewBox="0 0 140 240">
            <circle cx="70" cy="38" r="28" fill="none" stroke="#F5F5F5" strokeWidth="4"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '70px 38px' }} />
            {/* Glasses */}
            <rect x="50" y="30" width="18" height="12" rx="6" fill="none" stroke="#FFC107" strokeWidth="3" />
            <rect x="72" y="30" width="18" height="12" rx="6" fill="none" stroke="#FFC107" strokeWidth="3" />
            <line x1="68" y1="36" x2="72" y2="36" stroke="#FFC107" strokeWidth="2.5" />
            {/* Body */}
            <line x1="70" y1="66" x2="70" y2="148" stroke="#F5F5F5" strokeWidth="4.5" strokeLinecap="round" />
            {/* Arms — confident */}
            <line x1="70" y1="88" x2="28" y2="118" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="88" x2="112" y2="118" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round"
              style={{ transform: `rotate(${Math.sin(frame * 0.05) * 4}deg)`, transformOrigin: '70px 88px' }} />
            {/* Legs */}
            <line x1="70" y1="148" x2="44" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="96" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Small sparkle */}
            <circle cx="105" cy="20" r="5" fill="#FFC107" opacity={0.7 + Math.sin(frame * 0.2) * 0.3} />
            <circle cx="115" cy="35" r="3" fill="#e9d700" opacity={0.5 + Math.sin(frame * 0.15 + 1) * 0.3} />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.68) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene7_HostIntro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8_Eintrittskarte',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_Eintrittskarte: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Ticket tear animation
  const ticketReveal = interpolate(frame, [10, 55], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Strike-through on "wird das schon"
  const strikeWidth = interpolate(frame, [50, 80], [0, 660], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  });

  // Camera pan right
  const camX = interpolate(frame, [0, durationInFrames], [-20, 20], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — split: yellow left, dark right */}
      <AbsoluteFill style={{
        background: 'linear-gradient(105deg, #1a1500 0%, #1C1C1C 40%, #0d0d0d 100%)',
        transform: `translateX(${camX}px)`,
      }} />

      {/* Shape Layer — torn ticket wavy edge */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg style={{ position: 'absolute', left: 0, top: 0 }} width="1920" height="1080">
          <path d="M 0 540 Q 100 500 200 540 Q 300 580 400 540 Q 500 500 600 540 Q 700 580 800 540 Q 900 500 1000 540 Q 1100 580 1200 540 Q 1300 500 1400 540 Q 1500 580 1600 540 Q 1700 500 1800 540 L 1920 540 L 1920 1080 L 0 1080 Z"
            fill="#e9d700" />
        </svg>
      </AbsoluteFill>

      {/* Shape Layer — corner triangles */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <div style={{ position: 'absolute', top: 0, left: 0, width: 0, height: 0,
          borderLeft: '260px solid #e9d700', borderBottom: '260px solid transparent' }} />
        <div style={{ position: 'absolute', bottom: 0, right: 0, width: 0, height: 0,
          borderRight: '200px solid #2D2D2D', borderTop: '200px solid transparent' }} />
      </AbsoluteFill>

      {/* Motion Texture — ticket perforation dots */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 18 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', width: 12, height: 12, borderRadius: '50%',
            backgroundColor: '#F5F5F5',
            left: 60 + i * 100, top: '50%',
            transform: 'translateY(-50%)',
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {Array.from({ length: 16 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(i * 4) * 1920,
            top: seededRandom(i * 9) * 1080 + Math.sin(frame * 0.03 + i) * 14,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#e9d700', opacity: 0.1 + seededRandom(i * 6) * 0.15,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main Content */}
      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', justifyContent: 'center', paddingLeft: 120, paddingRight: 120 }}>

        {/* The comforting story */}
        <div style={{
          opacity: sp(0), marginBottom: 40,
          transform: `translateY(${(1 - sp(0)) * -25}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 26,
            color: '#9E9E9E', letterSpacing: 5, textTransform: 'uppercase', marginBottom: 18,
          }}>
            DIE BERUHIGENDE GESCHICHTE
          </div>
          <div style={{ position: 'relative', display: 'inline-block' }}>
            <div style={{
              fontFamily: 'Anton, sans-serif', fontSize: 72,
              color: '#9E9E9E', lineHeight: 1.1, fontStyle: 'italic',
            }}>
              "Wenn ich nur ordentlich arbeite, wird das schon."
            </div>
            {/* Strikethrough */}
            <div style={{
              position: 'absolute', top: '50%', left: 0,
              width: strikeWidth, height: 6,
              backgroundColor: '#ff0000', borderRadius: 3,
              transform: 'translateY(-50%)',
            }} />
          </div>
        </div>

        {/* Ticket visual */}
        <div style={{
          transform: `scale(${ticketReveal}) translateY(${(1 - ticketReveal) * 40}px)`,
          opacity: ticketReveal,
          transformOrigin: 'left center',
          marginBottom: 44,
        }}>
          <div style={{
            display: 'inline-flex', alignItems: 'stretch',
            border: '3px solid #e9d700',
            borderRadius: 12, overflow: 'hidden',
          }}>
            {/* Stub */}
            <div style={{
              width: 180, backgroundColor: '#e9d700',
              display: 'flex', alignItems: 'center', justifyContent: 'center',
              padding: '20px 24px',
            }}>
              <div style={{
                fontFamily: 'Anton, sans-serif', fontSize: 36,
                color: '#1C1C1C', textAlign: 'center', lineHeight: 1.2,
              }}>
                EIN-<br />TRITT
              </div>
            </div>
            {/* Perforation */}
            <div style={{
              width: 24, display: 'flex', flexDirection: 'column',
              justifyContent: 'space-around', alignItems: 'center',
              backgroundColor: '#2D2D2D',
            }}>
              {Array.from({ length: 8 }).map((_, i) => (
                <div key={i} style={{ width: 8, height: 8, borderRadius: '50%', backgroundColor: '#1C1C1C' }} />
              ))}
            </div>
            {/* Main ticket body */}
            <div style={{ padding: '20px 40px', backgroundColor: '#1C1C1C' }}>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace', fontSize: 26,
                color: '#9E9E9E', marginBottom: 8,
              }}>
                GÜLTIG FÜR:
              </div>
              <div style={{
                fontFamily: 'Anton, sans-serif', fontSize: 58,
                color: '#F5F5F5',
              }}>
                Ordentliche Arbeit
              </div>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace', fontSize: 22,
                color: '#ff0000', marginTop: 8,
              }}>
                ≠ VERMÖGENSSTRATEGIE
              </div>
            </div>
          </div>
        </div>

        {/* Punchline */}
        <div style={{
          opacity: sp(70),
          transform: `translateX(${(1 - sp(70)) * -30}px)`,
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 80,
            color: '#F5F5F5', lineHeight: 1.05,
          }}>
            Ordentliche Arbeit =
            <span style={{ color: '#e9d700' }}> Eintrittskarte.</span>
            <br />
            <span style={{ color: '#9E9E9E', fontSize: 62 }}>Nicht der </span>
            <span style={{ color: '#ff0000' }}>Plan.</span>
          </div>
        </div>

      </AbsoluteFill>

      {/* Stick Figure */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 80, bottom: 60,
          opacity: sp(25),
          transform: `translateY(${(1 - sp(25)) * 50}px)`,
        }}>
          <div style={{
            position: 'absolute', width: 90, height: 90, borderRadius: '50%',
            background: 'radial-gradient(circle, #FFC107 0%, transparent 70%)',
            top: 5, left: 15, opacity: 0.22,
          }} />
          <svg width="110" height="195" viewBox="0 0 140 240"
            style={{ transform: `translateY(${Math.sin(frame * 0.05) * 2}px)` }}>
            <circle cx="70" cy="38" r="28" fill="none" stroke="#F5F5F5" strokeWidth="4"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '70px 38px' }} />
            <rect x="52" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <rect x="72" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="68" y1="37" x2="72" y2="37" stroke="#FFC107" strokeWidth="2" />
            <line x1="70" y1="66" x2="70" y2="148" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="90" x2="30" y2="120" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="90" x2="110" y2="120" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round"
              style={{ transform: `rotate(${Math.sin(frame * 0.05) * 4}deg)`, transformOrigin: '70px 90px' }} />
            <line x1="70" y1="148" x2="44" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="96" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.72) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene8_Eintrittskarte;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9_DerPlan',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_DerPlan: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const pillars = [
    {
      num: '01',
      title: 'Steuerlich klug',
      desc: 'Strukturell handeln,\nnicht reagieren',
      color: '#e9d700',
      delay: 15,
      icon: 'M11.8 10.9c-2.27-.59-3-1.2-3-2.15 0-1.09 1.01-1.85 2.7-1.85 1.78 0 2.44.85 2.5 2.1h2.21c-.07-1.72-1.12-3.3-3.21-3.81V3h-3v2.16c-1.94.42-3.5 1.68-3.5 3.61 0 2.31 1.91 3.46 4.7 4.13 2.5.6 3 1.48 3 2.41 0 .69-.49 1.79-2.7 1.79-2.06 0-2.87-.92-2.98-2.1h-2.2c.12 2.19 1.76 3.42 3.68 3.83V21h3v-2.15c1.95-.37 3.5-1.5 3.5-3.55 0-2.84-2.43-3.81-4.7-4.4z',
    },
    {
      num: '02',
      title: 'Inflation ernst nehmen',
      desc: 'Kaufkraft aktiv\nverteidigen',
      color: '#FFC107',
      delay: 35,
      icon: 'M16 6l2.29 2.29-4.88 4.88-4-4L2 16.59 3.41 18l6-6 4 4 6.3-6.29L22 12V6z',
    },
    {
      num: '03',
      title: 'Staat ≠ Berater',
      desc: 'Du brauchst\neinen eigenen Plan',
      color: '#ff0000',
      delay: 55,
      icon: 'M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z',
    },
  ];

  // Background shift
  const camY = interpolate(frame, [0, durationInFrames], [0, -20], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, #0d1a0d 0%, #1C1C1C 45%, #1a0d00 100%)',
        transform: `translateY(${camY}px)`,
      }} />

      {/* Shape Layer — blueprint grid */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <React.Fragment key={i}>
            <div style={{ position: 'absolute', width: '100%', height: 1, backgroundColor: '#F5F5F5', top: i * 56 }} />
            <div style={{ position: 'absolute', width: 1, height: '100%', backgroundColor: '#F5F5F5', left: i * 100 }} />
          </React.Fragment>
        ))}
      </AbsoluteFill>

      {/* Shape Layer — large rotating diamond */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', width: 500, height: 500,
          border: '2px solid #e9d700',
          top: '50%', left: '50%',
          transform: `translate(-50%, -50%) rotate(${45 + frame * 0.04}deg)`,
          opacity: 0.05,
        }} />
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        {Array.from({ length: 18 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(i * 6) * 1920,
            top: seededRandom(i * 10) * 1080 + Math.sin(frame * 0.025 + i) * 16,
            width: 2.5, height: 2.5, borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#e9d700' : '#FFC107',
            opacity: 0.12 + seededRandom(i * 4) * 0.18,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main Content */}
      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', justifyContent: 'center', paddingLeft: 110 }}>

        {/* Header */}
        <div style={{
          opacity: sp(0),
          transform: `translateY(${(1 - sp(0)) * -30}px)`,
          marginBottom: 52,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 700,
            color: '#9E9E9E', letterSpacing: 6, textTransform: 'uppercase', marginBottom: 14,
          }}>
            DER PLAN ENTSCHEIDET SICH HIER:
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 96,
            color: '#F5F5F5', lineHeight: 0.95,
          }}>
            DREI WEICHEN —<br />
            <span style={{ color: '#e9d700' }}>DEINE ZUKUNFT.</span>
          </div>
        </div>

        {/* Pillars */}
        <div style={{ display: 'flex', gap: 40 }}>
          {pillars.map((p, i) => (
            <div key={i} style={{
              flex: 1, maxWidth: 430,
              opacity: sp(p.delay),
              transform: `translateY(${(1 - sp(p.delay)) * 50}px)`,
              borderLeft: `5px solid ${p.color}`,
              paddingLeft: 28,
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace', fontSize: 52,
                color: p.color, fontWeight: 700, lineHeight: 1, marginBottom: 12,
              }}>
                {p.num}
              </div>
              <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 10 }}>
                <svg width="32" height="32" viewBox="0 0 24 24" fill={p.color}>
                  <path d={p.icon} />
                </svg>
                <span style={{ fontFamily: 'Open Sans, sans-serif', fontSize: 34, fontWeight: 700, color: '#F5F5F5' }}>
                  {p.title}
                </span>
              </div>
              <div style={{ fontFamily: 'Open Sans, sans-serif', fontSize: 28, color: '#9E9E9E', whiteSpace: 'pre-line' }}>
                {p.desc}
              </div>
            </div>
          ))}
        </div>

      </AbsoluteFill>

      {/* Stick Figure — right pointing */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 60, bottom: 50,
          opacity: sp(30),
          transform: `translateY(${(1 - sp(30)) * 60}px)`,
        }}>
          <div style={{
            position: 'absolute', width: 90, height: 90, borderRadius: '50%',
            background: 'radial-gradient(circle, #FFC107 0%, transparent 70%)',
            top: 5, left: 15, opacity: 0.2,
          }} />
          <svg width="110" height="195" viewBox="0 0 140 240"
            style={{ transform: `translateY(${Math.sin(frame * 0.05) * 2}px)` }}>
            <circle cx="70" cy="38" r="28" fill="none" stroke="#F5F5F5" strokeWidth="4"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '70px 38px' }} />
            <rect x="52" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <rect x="72" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="68" y1="37" x2="72" y2="37" stroke="#FFC107" strokeWidth="2" />
            <line x1="70" y1="66" x2="70" y2="148" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Pointing arm */}
            <line x1="70" y1="88" x2="20" y2="80" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round"
              style={{ transform: `rotate(${Math.sin(frame * 0.05) * 3}deg)`, transformOrigin: '70px 88px' }} />
            <line x1="70" y1="88" x2="112" y2="70" stroke="#e9d700" strokeWidth="5" strokeLinecap="round" />
            <line x1="70" y1="148" x2="44" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="96" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.73) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene9_DerPlan;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_EngWird',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_EngWird: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Two lines: "Funktioniert" vs "Aber trotzdem eng"
  const pressureItems = [
    { label: 'Wartezeiten', icon: 'M11.99 2C6.47 2 2 6.48 2 12s4.47 10 9.99 10C17.52 22 22 17.52 22 12S17.52 2 11.99 2zm4.24 16L11 13.5V6h1.5v6.89l4.74 2.82-1.01 1.69z', delay: 20 },
    { label: 'Mietpreise', icon: 'M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z', delay: 33 },
    { label: 'Infrastruktur', icon: 'M1 9l2 2c4.97-4.97 13.03-4.97 18 0l2-2C16.93 2.93 7.08 2.93 1 9zm8 8l3 3 3-3c-1.65-1.66-4.34-1.66-6 0zm-4-4l2 2c2.76-2.76 7.24-2.76 10 0l2-2C15.14 9.14 8.87 9.14 5 13z', delay: 46 },
    { label: 'Pflege & Kitas', icon: 'M12 21.593c-5.63-5.539-11-10.297-11-14.402 0-3.791 3.068-5.191 5.281-5.191 1.312 0 4.151.501 5.719 4.457 1.59-3.968 4.464-4.447 5.726-4.447 2.54 0 5.274 1.621 5.274 5.181 0 4.069-5.136 8.625-11 14.402z', delay: 59 },
  ];

  // Race bars: Kosten vs Netto
  const kostenBar = interpolate(frame, [65, 100], [0, 880], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });
  const nettoBar = interpolate(frame, [65, 100], [0, 560], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // Squeeze animation — walls closing
  const squeeze = interpolate(frame, [40, 90], [0, 60], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — compressed, tight feel */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #0d0d0d 0%, #1C1C1C 40%, #1a0000 100%)',
      }} />

      {/* Squeeze walls */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: 0, left: 0,
          width: squeeze, height: '100%',
          background: 'linear-gradient(to right, rgba(255,0,0,0.08), transparent)',
        }} />
        <div style={{
          position: 'absolute', top: 0, right: 0,
          width: squeeze, height: '100%',
          background: 'linear-gradient(to left, rgba(255,0,0,0.08), transparent)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer — tightening rectangle outline */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: squeeze, top: squeeze * 0.4,
          right: squeeze, bottom: squeeze * 0.4,
          border: '2px solid rgba(255,0,0,0.08)',
          borderRadius: 8,
        }} />
      </AbsoluteFill>

      {/* Motion Texture — pressure lines converging */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', width: '100%', height: 1.5,
            backgroundColor: '#ff0000',
            top: 80 + i * 100,
            transform: `scaleX(${1 - i * 0.04}) translateX(${squeeze * 0.3}px)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles — red, dense */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(i * 3) * 1920,
            top: seededRandom(i * 7) * 1080 + Math.sin(frame * 0.04 + i) * 12,
            width: 2, height: 2, borderRadius: '50%',
            backgroundColor: '#ff0000', opacity: 0.1 + seededRandom(i * 9) * 0.15,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main Content */}
      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', paddingLeft: 110, paddingRight: 100 }}>

        {/* Left: Problem list */}
        <div style={{ width: 560 }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 24, fontWeight: 700,
            color: '#ff0000', letterSpacing: 5, textTransform: 'uppercase',
            opacity: sp(0), marginBottom: 24,
          }}>
            TROTZDEM SPÜREN VIELE...
          </div>

          {pressureItems.map((item, i) => (
            <div key={i} style={{
              display: 'flex', alignItems: 'center', gap: 18, marginBottom: 24,
              opacity: sp(item.delay),
              transform: `translateX(${(1 - sp(item.delay)) * -40}px)`,
            }}>
              <svg width="38" height="38" viewBox="0 0 24 24" fill="#ff0000">
                <path d={item.icon} />
              </svg>
              <span style={{ fontFamily: 'Open Sans, sans-serif', fontSize: 38, fontWeight: 700, color: '#F5F5F5' }}>
                {item.label}
              </span>
            </div>
          ))}
        </div>

        {/* Right: Race chart — Kosten vs Netto */}
        <div style={{
          width: 680,
          opacity: sp(55),
          transform: `translateX(${(1 - sp(55)) * 60}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 24, fontWeight: 700,
            color: '#9E9E9E', letterSpacing: 4, textTransform: 'uppercase', marginBottom: 32,
          }}>
            DAS RENNEN
          </div>

          {/* Kosten bar */}
          <div style={{ marginBottom: 28 }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 10 }}>
              <span style={{ fontFamily: 'Open Sans, sans-serif', fontSize: 32, color: '#F5F5F5', fontWeight: 700 }}>
                Lebenshaltungskosten
              </span>
              <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 32, color: '#ff0000', fontWeight: 700 }}>
                ↑ Schnell
              </span>
            </div>
            <div style={{ width: 680, height: 22, backgroundColor: '#2D2D2D', borderRadius: 11, overflow: 'hidden' }}>
              <div style={{ width: kostenBar, height: '100%', backgroundColor: '#ff0000', borderRadius: 11 }} />
            </div>
          </div>

          {/* Netto bar */}
          <div style={{ marginBottom: 44 }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 10 }}>
              <span style={{ fontFamily: 'Open Sans, sans-serif', fontSize: 32, color: '#F5F5F5', fontWeight: 700 }}>
                Dein Nettolohn
              </span>
              <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 32, color: '#9E9E9E', fontWeight: 700 }}>
                ↑ Langsam
              </span>
            </div>
            <div style={{ width: 680, height: 22, backgroundColor: '#2D2D2D', borderRadius: 11, overflow: 'hidden' }}>
              <div style={{ width: nettoBar, height: '100%', backgroundColor: '#9E9E9E', borderRadius: 11 }} />
            </div>
          </div>

          {/* Gap callout */}
          <div style={{
            opacity: sp(90),
            transform: `scale(${sp(90, { damping: 8, stiffness: 200 })})`,
            transformOrigin: 'left center',
            backgroundColor: 'rgba(255,0,0,0.1)',
            border: '2px solid #ff0000',
            borderRadius: 12, padding: '16px 28px',
          }}>
            <div style={{
              fontFamily: 'Anton, sans-serif', fontSize: 52,
              color: '#ff0000', lineHeight: 1.1,
            }}>
              Lücke = stille Verarmung.
            </div>
          </div>
        </div>

      </AbsoluteFill>

      {/* Stick Figure — squeezed in middle */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: '50%', bottom: 50,
          transform: `translateX(-50%) translateY(${(1 - sp(20)) * 40}px)`,
          opacity: sp(20),
        }}>
          <div style={{
            position: 'absolute', width: 90, height: 90, borderRadius: '50%',
            background: 'radial-gradient(circle, #FFC107 0%, transparent 70%)',
            top: 5, left: 15, opacity: 0.2,
          }} />
          <svg width="100" height="178" viewBox="0 0 140 240"
            style={{
              transform: `translateY(${Math.sin(frame * 0.05) * 2}px) scaleX(${1 - squeeze * 0.003})`,
            }}>
            <circle cx="70" cy="38" r="28" fill="none" stroke="#F5F5F5" strokeWidth="4"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '70px 38px' }} />
            <rect x="52" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <rect x="72" y="32" width="16" height="10" rx="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="68" y1="37" x2="72" y2="37" stroke="#FFC107" strokeWidth="2" />
            <line x1="70" y1="66" x2="70" y2="148" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="90" x2="40" y2="110" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="90" x2="100" y2="110" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="50" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="70" y1="148" x2="90" y2="210" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene10_EngWird;
```

