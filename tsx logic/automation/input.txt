```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01Choc3400',
  durationInSeconds: 29,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01Choc3400: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Digit odometer for 3400
  const countProgress = interpolate(frame, [20, 90], [0, 3400], { extrapolateRight: 'clamp' });
  const displayNum = Math.floor(countProgress).toLocaleString('fr-FR');

  const impactShake = frame > 90 && frame < 105 ? Math.sin(frame * 3) * 6 : 0;
  const zoom = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  const subReveal = sp(110);
  const luca = sp(180);
  const line1 = sp(240);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFEA93', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 40%, rgba(255,255,255,0.35) 0%, transparent 65%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: '2px solid #000000', opacity: 0.05,
        top: '10%', left: '-15%',
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 600, height: 600,
        border: '2px solid #D90000', opacity: 0.06,
        bottom: '-10%', right: '-10%',
        transform: `rotate(${-frame * 0.2}deg)`,
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="g1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#g1)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 1);
        const y = (frame * (0.4 + s * 0.6) + s * 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`, top: y,
            width: 6, height: 6, borderRadius: '50%',
            background: i % 3 === 0 ? '#D90000' : '#000000',
            opacity: 0.25,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{
        transform: `scale(${zoom}) translateX(${impactShake}px)`,
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
      }}>
        {/* Number */}
        <div style={{
          fontFamily: 'Clash Display, Anton, sans-serif',
          fontSize: 240, fontWeight: 900,
          color: '#D90000',
          letterSpacing: -6,
          lineHeight: 1,
          opacity: interpolate(frame, [15, 30], [0, 1], { extrapolateRight: 'clamp' }),
          transform: `scale(${interpolate(frame, [15, 40], [0.7, 1], { extrapolateRight: 'clamp' })})`,
          textShadow: '6px 6px 0 #000000',
        }}>
          {displayNum}
        </div>

        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 90, fontWeight: 700,
          color: '#000000',
          marginTop: 10,
          letterSpacing: 2,
          opacity: subReveal,
          transform: `translateY(${(1 - subReveal) * 30}px)`,
        }}>
          MILLIARDS D'EUROS
        </div>

        {/* Divider */}
        <div style={{
          width: interpolate(sp(140), [0, 1], [0, 500]),
          height: 5, background: '#D90000',
          marginTop: 40, borderRadius: 4,
        }} />

        {/* Luca intro */}
        <div style={{
          fontFamily: 'Bricolage Grotesque, Montserrat, sans-serif',
          fontSize: 44, fontWeight: 500,
          color: '#000000',
          marginTop: 40,
          opacity: luca,
          transform: `translateY(${(1 - luca) * 20}px)`,
          textAlign: 'center', maxWidth: 1200,
        }}>
          Je m'appelle <b style={{ color: '#D90000' }}>Luca</b> — Matures Finance
        </div>

        <div style={{
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 38, fontWeight: 400, fontStyle: 'italic',
          color: '#000000',
          marginTop: 20,
          opacity: line1 * 0.85,
          transform: `translateY(${(1 - line1) * 20}px)`,
          textAlign: 'center', maxWidth: 1300,
        }}>
          « Les chiffres parlent tout seuls. »
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene01Choc3400;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02FoyerPart',
  durationInSeconds: 28,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02FoyerPart: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const questionOp = sp(15);
  const divideOp = sp(90);
  const numberProg = interpolate(frame, [140, 220], [0, 50000], { extrapolateRight: 'clamp' });
  const finalNum = Math.floor(numberProg).toLocaleString('fr-FR');
  const bitterOp = sp(240);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFEA93', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,255,255,0.2) 0%, transparent 50%, rgba(217,0,0,0.05) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 700, height: 700,
        background: '#000000', opacity: 0.04,
        top: '20%', right: '-20%',
        transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 400, height: 400, borderRadius: '50%',
        border: '3px solid #D90000', opacity: 0.06,
        bottom: '-10%', left: '5%',
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
      }} />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="diag2" width="30" height="30" patternUnits="userSpaceOnUse">
            <path d="M 0 30 L 30 0" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag2)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 7);
        const x = (frame * (0.3 + s * 0.5) + s * 1920) % 1920;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: `${s * 100}%`,
            width: 4, height: 4, borderRadius: '50%',
            background: '#000000', opacity: 0.3,
          }} />
        );
      })}

      {/* CONTENT */}
      <AbsoluteFill style={{
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
        padding: '0 100px',
      }}>
        {/* Question */}
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 82, fontWeight: 700,
          color: '#000000', lineHeight: 1.05,
          textAlign: 'center', maxWidth: 1500,
          opacity: questionOp,
          transform: `translateY(${(1 - questionOp) * 40}px)`,
          textTransform: 'uppercase',
          letterSpacing: 1,
        }}>
          Votre part <span style={{ color: '#D90000' }}>par foyer</span> ?
        </div>

        {/* Division equation */}
        <div style={{
          display: 'flex', alignItems: 'center', gap: 40,
          marginTop: 60,
          opacity: divideOp,
          transform: `translateY(${(1 - divideOp) * 30}px)`,
        }}>
          <div style={{
            padding: '20px 40px',
            background: '#000000', color: '#FFEA93',
            fontFamily: 'Space Mono, monospace',
            fontSize: 50, fontWeight: 700, borderRadius: 12,
          }}>
            3 460 Md€
          </div>
          <div style={{
            fontFamily: 'Anton', fontSize: 70, color: '#D90000',
          }}>÷</div>
          <div style={{
            padding: '20px 40px',
            background: '#FFEA93', color: '#000000',
            border: '4px solid #000000',
            fontFamily: 'Space Mono, monospace',
            fontSize: 50, fontWeight: 700, borderRadius: 12,
          }}>
            30M foyers
          </div>
          <div style={{
            fontFamily: 'Anton', fontSize: 70, color: '#000000',
          }}>=</div>
        </div>

        {/* Result number */}
        <div style={{
          marginTop: 40,
          padding: '30px 70px',
          background: '#D90000',
          borderRadius: 20,
          boxShadow: '10px 10px 0 #000000',
          opacity: interpolate(frame, [130, 150], [0, 1], { extrapolateRight: 'clamp' }),
          transform: `scale(${interpolate(frame, [130, 160], [0.5, 1], { extrapolateRight: 'clamp' })})`,
        }}>
          <div style={{
            fontFamily: 'Clash Display, Anton, sans-serif',
            fontSize: 130, fontWeight: 900,
            color: '#FFEA93', letterSpacing: -2,
          }}>
            ≈ {finalNum} €
          </div>
        </div>

        {/* Bitter taste line */}
        <div style={{
          marginTop: 40,
          fontFamily: 'Bricolage Grotesque, sans-serif',
          fontSize: 42, fontStyle: 'italic',
          color: '#000000', textAlign: 'center',
          opacity: bitterOp,
          transform: `translateY(${(1 - bitterOp) * 20}px)`,
        }}>
          « Un goût amer... un dimanche soir »
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene02FoyerPart;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03Maastricht',
  durationInSeconds: 29,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03Maastricht: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(10);
  const bar1 = interpolate(sp(60), [0, 1], [0, 1]);
  const bar2 = interpolate(sp(100), [0, 1], [0, 1]);
  const bar3 = interpolate(sp(140), [0, 1], [0, 1]);
  const totalOp = sp(200);
  const augOp = sp(260);

  const bars = [
    { year: '2023', pct: 109.5, prog: bar1 },
    { year: '2024', pct: 112.6, prog: bar2 },
    { year: '2025', pct: 115.6, prog: bar3, highlight: true },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFEA93', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(255,255,255,0.15) 0%, transparent 70%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 500, height: 500,
        border: '2px solid #000000', opacity: 0.06,
        top: '-15%', right: '10%',
        transform: `rotate(${frame * 0.3}deg)`,
      }} />

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots3" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="2" cy="2" r="1.5" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots3)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const s = seededRandom(i + 12);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`, top: `${(seededRandom(i + 30) * 100)}%`,
            width: 5, height: 5,
            background: '#D90000', opacity: 0.3,
            transform: `translateY(${Math.sin(frame * 0.03 + i) * 20}px)`,
          }} />
        );
      })}

      {/* CONTENT */}
      <AbsoluteFill style={{ padding: '80px 100px' }}>
        {/* Header label */}
        <div style={{
          display: 'flex', alignItems: 'center', gap: 20,
          opacity: titleOp,
          transform: `translateX(${(1 - titleOp) * -40}px)`,
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <circle cx="30" cy="30" r="26" fill="none" stroke="#D90000" strokeWidth="4" />
            <text x="30" y="40" textAnchor="middle" fontSize="30" fill="#D90000" fontWeight="900" fontFamily="Anton">!</text>
          </svg>
          <div style={{
            fontFamily: 'Space Mono, monospace',
            fontSize: 30, color: '#000000', letterSpacing: 3,
          }}>
            DETTE / PIB — TRAITÉ DE MAASTRICHT
          </div>
        </div>

        {/* Title */}
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 110, fontWeight: 700,
          color: '#000000', marginTop: 20, lineHeight: 1,
          opacity: titleOp,
        }}>
          UNE COURBE QUI GRIMPE
        </div>

        {/* Bars chart */}
        <div style={{
          display: 'flex', alignItems: 'flex-end',
          justifyContent: 'center', gap: 90,
          marginTop: 60, height: 500,
        }}>
          {bars.map((b, i) => {
            const barH = (b.pct - 100) * 22 * b.prog + 100 * b.prog;
            return (
              <div key={i} style={{
                display: 'flex', flexDirection: 'column',
                alignItems: 'center', width: 220,
              }}>
                <div style={{
                  fontFamily: 'Space Mono, monospace',
                  fontSize: 42, fontWeight: 700,
                  color: b.highlight ? '#D90000' : '#000000',
                  marginBottom: 10, opacity: b.prog,
                }}>
                  {b.pct}%
                </div>
                <div style={{
                  width: 180, height: barH,
                  background: b.highlight ? '#D90000' : '#000000',
                  borderRadius: '10px 10px 0 0',
                  boxShadow: b.highlight ? '6px -6px 0 #000000' : 'none',
                  position: 'relative',
                }}>
                  {b.highlight && (
                    <div style={{
                      position: 'absolute', top: -60, left: '50%',
                      transform: 'translateX(-50%)',
                      fontFamily: 'Anton', fontSize: 40,
                      color: '#D90000',
                      opacity: interpolate(frame, [180, 200], [0, 1], { extrapolateRight: 'clamp' }),
                    }}>▲</div>
                  )}
                </div>
                <div style={{
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 46, color: '#000000',
                  marginTop: 20, opacity: b.prog,
                }}>
                  {b.year}
                </div>
              </div>
            );
          })}
        </div>

        {/* Bottom summary */}
        <div style={{
          display: 'flex', gap: 40, marginTop: 40,
          justifyContent: 'center',
        }}>
          <div style={{
            padding: '18px 34px', background: '#000000', color: '#FFEA93',
            fontFamily: 'Space Mono', fontSize: 32, fontWeight: 700,
            borderRadius: 10, opacity: totalOp,
          }}>
            TOTAL: 3 460,5 Md€
          </div>
          <div style={{
            padding: '18px 34px', background: '#D90000', color: '#FFEA93',
            fontFamily: 'Space Mono', fontSize: 32, fontWeight: 700,
            borderRadius: 10, opacity: augOp,
          }}>
            +154,4 Md€ en 2025
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene03Maastricht;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04Trajectoire2027',
  durationInSeconds: 28,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04Trajectoire2027: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const lineProg = interpolate(frame, [40, 200], [0, 1], { extrapolateRight: 'clamp' });
  const titleOp = sp(10);
  const endLabel = sp(210);

  // Line chart path 2023 -> 2027
  const pts = [
    { x: 200, y: 720, v: 109.5, year: '2023' },
    { x: 500, y: 690, v: 112.6, year: '2024' },
    { x: 800, y: 660, v: 115.6, year: '2025' },
    { x: 1100, y: 580, v: 117.8, year: '2026' },
    { x: 1400, y: 460, v: 120, year: '2027' },
  ];

  const visibleCount = Math.floor(lineProg * pts.length);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFEA93', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 80% 20%, rgba(217,0,0,0.08) 0%, transparent 50%)',
      }} />

      {/* Big triangle shape */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <polygon points="1920,1080 1920,300 400,1080" fill="#D90000" />
      </svg>

      {/* Circles */}
      <div style={{
        position: 'absolute', width: 350, height: 350, borderRadius: '50%',
        border: '2px solid #000000', opacity: 0.08,
        top: '5%', left: '5%',
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
      }} />

      {/* Texture lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="hor4" width="100" height="60" patternUnits="userSpaceOnUse">
            <line x1="0" y1="30" x2="100" y2="30" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hor4)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const s = seededRandom(i + 20);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`, top: `${seededRandom(i + 44) * 100}%`,
            width: 5, height: 5, borderRadius: '50%',
            background: '#000000', opacity: 0.25,
            transform: `translateY(${Math.sin(frame * 0.05 + i) * 15}px)`,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 80, left: 100,
        opacity: titleOp,
        transform: `translateY(${(1 - titleOp) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'Space Mono, monospace',
          fontSize: 28, color: '#D90000', letterSpacing: 3,
        }}>
          PROJECTION — HORIZON 2027
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 100, fontWeight: 700,
          color: '#000000', lineHeight: 1, marginTop: 10,
        }}>
          LA COURBE <span style={{ color: '#D90000' }}>ACCÉLÈRE</span>
        </div>
      </div>

      {/* Chart area */}
      <svg width="1920" height="1080" style={{ position: 'absolute' }}>
        {/* Axis */}
        <line x1="150" y1="800" x2="1500" y2="800" stroke="#000000" strokeWidth="3" opacity="0.5" />
        <line x1="150" y1="800" x2="150" y2="380" stroke="#000000" strokeWidth="3" opacity="0.5" />

        {/* Threshold 120% line */}
        <line x1="150" y1="460" x2="1500" y2="460" stroke="#D90000" strokeWidth="2" strokeDasharray="10 8" opacity={lineProg} />
        <text x="1520" y="466" fill="#D90000" fontSize="26" fontFamily="Space Mono" fontWeight="700" opacity={lineProg}>120%</text>

        {/* Line segments */}
        {pts.slice(1).map((p, i) => {
          if (i + 1 > visibleCount) return null;
          const prev = pts[i];
          return (
            <line key={i} x1={prev.x} y1={prev.y} x2={p.x} y2={p.y}
              stroke={i >= 2 ? '#D90000' : '#000000'} strokeWidth="6" strokeLinecap="round" />
          );
        })}

        {/* Dots */}
        {pts.map((p, i) => {
          if (i >= visibleCount + 1) return null;
          return (
            <g key={i}>
              <circle cx={p.x} cy={p.y} r="12" fill={i === pts.length - 1 ? '#D90000' : '#000000'} />
              <text x={p.x} y={p.y - 25} textAnchor="middle" fontSize="24"
                fill="#000000" fontFamily="Space Mono" fontWeight="700">
                {p.v}%
              </text>
              <text x={p.x} y={p.y + 45} textAnchor="middle" fontSize="24"
                fill="#000000" fontFamily="Anton">
                {p.year}
              </text>
            </g>
          );
        })}
      </svg>

      {/* End label */}
      <div style={{
        position: 'absolute', top: 380, right: 60,
        padding: '20px 40px', background: '#D90000', color: '#FFEA93',
        fontFamily: 'Anton', fontSize: 42, fontWeight: 700,
        borderRadius: 12, boxShadow: '6px 6px 0 #000000',
        opacity: endLabel,
        transform: `scale(${0.7 + endLabel * 0.3})`,
      }}>
        ≈ 120% en 2027
      </div>

      {/* Bottom caption */}
      <div style={{
        position: 'absolute', bottom: 100, left: '50%',
        transform: 'translateX(-50%)',
        fontFamily: 'Bricolage Grotesque, sans-serif',
        fontSize: 40, fontStyle: 'italic', color: '#000000',
        opacity: sp(240),
      }}>
        Portée par des <b style={{ color: '#D90000' }}>déficits primaires importants</b>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene04Trajectoire2027;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05DeficitStruct',
  durationInSeconds: 29,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05DeficitStruct: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(10);
  const leftBox = sp(80);
  const rightBox = sp(140);
  const arrowOp = sp(200);
  const structOp = sp(260);

  // Bar heights: recettes vs dépenses
  const recetteH = 280 * leftBox;
  const depenseH = 420 * rightBox;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFEA93', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(0,0,0,0.05) 0%, transparent 30%, transparent 70%, rgba(217,0,0,0.08) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 800, height: 800, borderRadius: '50%',
        background: '#D90000', opacity: 0.04,
        bottom: '-30%', right: '-15%',
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 300, height: 300,
        border: '2px solid #000000', opacity: 0.08,
        top: '15%', left: '2%',
        transform: `rotate(${frame * 0.4}deg)`,
      }} />

      {/* Crosshatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="cross5" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 0 0 L 50 50 M 50 0 L 0 50" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross5)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const s = seededRandom(i + 55);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`, top: `${(frame + s * 500) % 1080}px`,
            width: 4, height: 4, background: '#000000', opacity: 0.3,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: titleOp,
        transform: `translateY(${(1 - titleOp) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'Space Mono', fontSize: 28, color: '#D90000', letterSpacing: 4,
        }}>
          DÉCRYPTAGE
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 96, fontWeight: 700, color: '#000000',
          lineHeight: 1, marginTop: 12,
        }}>
          RECETTES <span style={{ color: '#D90000' }}>&lt;</span> DÉPENSES
        </div>
      </div>

      {/* Comparison bars */}
      <div style={{
        position: 'absolute', top: 340, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', alignItems: 'flex-end',
        gap: 120,
      }}>
        {/* Recettes */}
        <div style={{
          display: 'flex', flexDirection: 'column', alignItems: 'center',
        }}>
          <div style={{
            fontFamily: 'Anton', fontSize: 40, color: '#000000',
            marginBottom: 12, opacity: leftBox,
          }}>RECETTES</div>
          <div style={{
            width: 260, height: recetteH,
            background: '#000000',
            borderRadius: '8px 8px 0 0',
          }} />
          <div style={{
            fontFamily: 'Space Mono', fontSize: 32,
            color: '#000000', marginTop: 16, opacity: leftBox,
          }}>
            €€€
          </div>
        </div>

        {/* Arrow */}
        <div style={{
          fontFamily: 'Anton', fontSize: 120, color: '#D90000',
          alignSelf: 'center',
          opacity: arrowOp,
          transform: `scale(${0.6 + arrowOp * 0.4})`,
        }}>
          &lt;
        </div>

        {/* Depenses */}
        <div style={{
          display: 'flex', flexDirection: 'column', alignItems: 'center',
        }}>
          <div style={{
            fontFamily: 'Anton', fontSize: 40, color: '#D90000',
            marginBottom: 12, opacity: rightBox,
          }}>DÉPENSES</div>
          <div style={{
            width: 260, height: depenseH,
            background: '#D90000',
            borderRadius: '8px 8px 0 0',
            boxShadow: '8px -8px 0 #000000',
          }} />
          <div style={{
            fontFamily: 'Space Mono', fontSize: 32,
            color: '#D90000', marginTop: 16, opacity: rightBox,
          }}>
            €€€€€€
          </div>
        </div>
      </div>

      {/* Verdict */}
      <div style={{
        position: 'absolute', bottom: 80, left: 0, right: 0,
        textAlign: 'center',
        opacity: structOp,
        transform: `translateY(${(1 - structOp) * 20}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          padding: '20px 50px',
          background: '#000000', color: '#FFEA93',
          fontFamily: 'Anton', fontSize: 46, fontWeight: 700,
          borderRadius: 10, letterSpacing: 2,
        }}>
          PROBLÈME <span style={{ color: '#D90000' }}>STRUCTUREL</span>, PAS CONJONCTUREL
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene05DeficitStruct;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06EducVsDette',
  durationInSeconds: 28,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06EducVsDette: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const qOp = sp(10);
  const leftCard = sp(80);
  const vsOp = sp(140);
  const rightCard = sp(180);
  const bascule = sp(260);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFEA93', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient split */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(0,0,0,0.06) 0%, transparent 50%, rgba(217,0,0,0.08) 100%)',
      }} />

      {/* Shape */}
      <div style={{
        position: 'absolute', width: 600, height: 600,
        border: '3px solid #000000', opacity: 0.05,
        top: '30%', left: '35%',
        transform: `rotate(${frame * 0.2}deg)`,
      }} />

      {/* Question mark texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        <text x="960" y="700" textAnchor="middle" fontSize="900" fontFamily="Anton" fontWeight="900" fill="#000000">?</text>
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 80);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`, top: `${seededRandom(i + 95) * 100}%`,
            width: 5, height: 5, background: i % 2 === 0 ? '#000000' : '#D90000',
            opacity: 0.25, borderRadius: '50%',
            transform: `translateY(${Math.sin(frame * 0.04 + i) * 20}px)`,
          }} />
        );
      })}

      {/* Question */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: qOp, transform: `translateY(${(1 - qOp) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'Space Mono', fontSize: 30, color: '#D90000', letterSpacing: 4,
        }}>QUESTION SIMPLE</div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 76, color: '#000000', marginTop: 10, lineHeight: 1.05,
        }}>
          QU'EST-CE QUI <span style={{ color: '#D90000' }}>COÛTE LE PLUS CHER</span> ?
        </div>
      </div>

      {/* Two cards */}
      <div style={{
        position: 'absolute', top: 340, left: 0, right: 0,
        display: 'flex', justifyContent: 'center',
        alignItems: 'center', gap: 60,
      }}>
        {/* Education */}
        <div style={{
          width: 520, padding: '40px 30px',
          background: '#FFEA93',
          border: '5px solid #000000',
          borderRadius: 20,
          boxShadow: '10px 10px 0 #000000',
          opacity: leftCard,
          transform: `translateX(${(1 - leftCard) * -80}px)`,
          textAlign: 'center',
        }}>
          <svg width="80" height="80" viewBox="0 0 80 80" style={{ display: 'block', margin: '0 auto 20px' }}>
            <path d="M40 15 L70 30 L40 45 L10 30 Z" fill="#000000" />
            <path d="M20 35 L20 55 Q40 65 60 55 L60 35" stroke="#000000" strokeWidth="4" fill="none" />
            <line x1="70" y1="30" x2="70" y2="55" stroke="#000000" strokeWidth="3" />
          </svg>
          <div style={{
            fontFamily: 'Anton', fontSize: 48, color: '#000000',
          }}>ÉDUCATION</div>
          <div style={{
            fontFamily: 'Space Mono', fontSize: 42, fontWeight: 700,
            color: '#000000', marginTop: 20,
          }}>
            ~ 60 Md€
          </div>
        </div>

        {/* VS */}
        <div style={{
          fontFamily: 'Anton', fontSize: 90, color: '#D90000',
          opacity: vsOp,
          transform: `scale(${0.5 + vsOp * 0.5}) rotate(${(1 - vsOp) * -30}deg)`,
        }}>
          VS
        </div>

        {/* Dette */}
        <div style={{
          width: 520, padding: '40px 30px',
          background: '#D90000',
          border: '5px solid #000000',
          borderRadius: 20,
          boxShadow: '10px 10px 0 #000000',
          opacity: rightCard,
          transform: `translateX(${(1 - rightCard) * 80}px)`,
          textAlign: 'center',
        }}>
          <svg width="80" height="80" viewBox="0 0 80 80" style={{ display: 'block', margin: '0 auto 20px' }}>
            <circle cx="40" cy="40" r="30" fill="none" stroke="#FFEA93" strokeWidth="6" />
            <text x="40" y="54" textAnchor="middle" fontSize="42" fontFamily="Anton" fill="#FFEA93">%</text>
          </svg>
          <div style={{
            fontFamily: 'Anton', fontSize: 48, color: '#FFEA93',
          }}>INTÉRÊTS DETTE</div>
          <div style={{
            fontFamily: 'Space Mono', fontSize: 42, fontWeight: 700,
            color: '#FFEA93', marginTop: 20,
          }}>
            59,3 Md€ →
          </div>
        </div>
      </div>

      {/* Bascule text */}
      <div style={{
        position: 'absolute', bottom: 90, left: 0, right: 0,
        textAlign: 'center',
        opacity: bascule,
      }}>
        <div style={{
          fontFamily: 'Bricolage Grotesque, sans-serif',
          fontSize: 44, fontStyle: 'italic', color: '#000000',
        }}>
          « La bascule se produit <b style={{ color: '#D90000' }}>silencieusement</b>... »
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene06EducVsDette;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07ServiceDette',
  durationInSeconds: 29,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07ServiceDette: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(10);
  const box2020 = sp(70);
  const box2026 = sp(170);
  const arrowOp = sp(240);

  const count2020 = Math.floor(interpolate(frame, [90, 140], [0, 36.2 * 10], { extrapolateRight: 'clamp' })) / 10;
  const count2026 = Math.floor(interpolate(frame, [190, 260], [0, 59.3 * 10], { extrapolateRight: 'clamp' })) / 10;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFEA93', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 20% 80%, rgba(217,0,0,0.1) 0%, transparent 50%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', width: 700, height: 700,
        background: '#000000', opacity: 0.04, borderRadius: 40,
        top: '-20%', left: '-15%',
        transform: `rotate(15deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="g7" width="60" height="60" patternUnits="userSpaceOnUse">
            <rect x="0" y="0" width="60" height="60" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#g7)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 111);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`, top: `${(frame * 0.5 + s * 900) % 1080}px`,
            width: 4, height: 4, background: '#D90000', opacity: 0.35,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: titleOp,
      }}>
        <div style={{
          fontFamily: 'Space Mono', fontSize: 28, letterSpacing: 4, color: '#D90000',
        }}>SERVICE DE LA DETTE</div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 100, color: '#000000', lineHeight: 1, marginTop: 8,
        }}>
          UNE EXPLOSION EN <span style={{ color: '#D90000' }}>6 ANS</span>
        </div>
      </div>

      {/* Two big boxes */}
      <div style={{
        position: 'absolute', top: 330, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 80,
      }}>
        {/* 2020 */}
        <div style={{
          width: 550, padding: '40px 30px',
          background: '#FFEA93', border: '5px solid #000000',
          borderRadius: 20, textAlign: 'center',
          opacity: box2020,
          transform: `translateY(${(1 - box2020) * 40}px)`,
        }}>
          <div style={{
            fontFamily: 'Anton', fontSize: 44, color: '#000000',
          }}>ANNÉE 2020</div>
          <div style={{
            fontFamily: 'Clash Display', fontSize: 140, color: '#000000',
            fontWeight: 900, letterSpacing: -3, marginTop: 15,
          }}>
            {count2020.toFixed(1).replace('.', ',')}
          </div>
          <div style={{
            fontFamily: 'Space Mono', fontSize: 32, color: '#000000',
          }}>MILLIARDS €</div>
        </div>

        {/* Arrow */}
        <svg width="120" height="60" style={{
          opacity: arrowOp,
          transform: `translateX(${(1 - arrowOp) * -40}px)`,
        }}>
          <line x1="0" y1="30" x2="100" y2="30" stroke="#D90000" strokeWidth="10" />
          <polygon points="80,10 120,30 80,50" fill="#D90000" />
        </svg>

        {/* 2026 */}
        <div style={{
          width: 550, padding: '40px 30px',
          background: '#D90000', border: '5px solid #000000',
          borderRadius: 20, textAlign: 'center',
          boxShadow: '12px 12px 0 #000000',
          opacity: box2026,
          transform: `translateY(${(1 - box2026) * 40}px) scale(${0.9 + box2026 * 0.1})`,
        }}>
          <div style={{
            fontFamily: 'Anton', fontSize: 44, color: '#FFEA93',
          }}>ANNÉE 2026</div>
          <div style={{
            fontFamily: 'Clash Display', fontSize: 140, color: '#FFEA93',
            fontWeight: 900, letterSpacing: -3, marginTop: 15,
          }}>
            {count2026.toFixed(1).replace('.', ',')}
          </div>
          <div style={{
            fontFamily: 'Space Mono', fontSize: 32, color: '#FFEA93',
          }}>MILLIARDS €</div>
        </div>
      </div>

      {/* Bottom insight */}
      <div style={{
        position: 'absolute', bottom: 90, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(300),
      }}>
        <div style={{
          display: 'inline-block',
          padding: '16px 40px', background: '#000000', color: '#FFEA93',
          fontFamily: 'Anton', fontSize: 40, borderRadius: 10,
        }}>
          × 1,64 EN 6 ANS — NON, PAS UNE LIGNE SECONDAIRE
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene07ServiceDette;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08Accelerateur',
  durationInSeconds: 28,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08Accelerateur: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(10);
  const gaugeOp = sp(60);
  // Needle angle from -120deg to +120deg
  const needleAngle = interpolate(frame, [80, 260], [-120, 120], { extrapolateRight: 'clamp' });
  const speed = Math.floor(interpolate(frame, [80, 260], [0, 220], { extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFEA93', overflow: 'hidden', opacity: exitOp }}>
      {/* Speed lines background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, transparent 0%, rgba(217,0,0,0.06) 50%, transparent 100%)',
      }} />

      {/* Motion lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.15 }}>
        {Array.from({ length: 25 }).map((_, i) => {
          const y = i * 45;
          const offset = (frame * (5 + i % 3) + i * 100) % 2000 - 200;
          return (
            <line key={i} x1={offset} y1={y} x2={offset + 200} y2={y}
              stroke="#000000" strokeWidth="2" />
          );
        })}
      </svg>

      {/* Shape */}
      <div style={{
        position: 'absolute', width: 500, height: 500, borderRadius: '50%',
        border: '2px solid #D90000', opacity: 0.08,
        top: '20%', right: '5%',
        transform: `scale(${1 + Math.sin(frame * 0.05) * 0.08})`,
      }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const s = seededRandom(i + 200);
        const x = (frame * 8 + i * 200) % 1920;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: `${s * 100}%`,
            width: 20, height: 3,
            background: '#000000', opacity: 0.3,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: titleOp,
      }}>
        <div style={{
          fontFamily: 'Space Mono', fontSize: 28, color: '#D90000', letterSpacing: 4,
        }}>MÉTAPHORE</div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 92, color: '#000000', lineHeight: 1, marginTop: 8,
        }}>
          LE COMPTEUR <span style={{ color: '#D90000' }}>S'EMBALLE</span>
        </div>
      </div>

      {/* Speedometer */}
      <div style={{
        position: 'absolute', top: 320, left: 0, right: 0,
        display: 'flex', justifyContent: 'center',
        opacity: gaugeOp,
      }}>
        <svg width="580" height="450" viewBox="0 0 580 450">
          {/* Outer ring */}
          <circle cx="290" cy="290" r="230" fill="#FFEA93" stroke="#000000" strokeWidth="10" />
          {/* Green zone arc */}
          <path d="M 90 290 A 200 200 0 0 1 200 120" stroke="#000000" strokeWidth="30" fill="none" />
          {/* Red zone arc */}
          <path d="M 380 120 A 200 200 0 0 1 490 290" stroke="#D90000" strokeWidth="30" fill="none" />
          {/* Middle arc */}
          <path d="M 200 120 A 200 200 0 0 1 380 120" stroke="#000000" strokeWidth="30" fill="none" opacity="0.4" />
          {/* Ticks */}
          {Array.from({ length: 13 }).map((_, i) => {
            const a = (-120 + i * 20) * Math.PI / 180;
            const x1 = 290 + Math.sin(a) * 200;
            const y1 = 290 - Math.cos(a) * 200;
            const x2 = 290 + Math.sin(a) * 170;
            const y2 = 290 - Math.cos(a) * 170;
            return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#000000" strokeWidth="3" />;
          })}
          {/* Needle */}
          <g transform={`rotate(${needleAngle} 290 290)`}>
            <polygon points="285,90 295,90 290,300" fill="#D90000" stroke="#000000" strokeWidth="2" />
            <circle cx="290" cy="290" r="20" fill="#000000" />
          </g>
          {/* Center speed */}
          <text x="290" y="400" textAnchor="middle"
            fontFamily="Space Mono" fontSize="60" fontWeight="900" fill="#000000">
            {speed} km/h
          </text>
        </svg>
      </div>

      {/* Right side text */}
      <div style={{
        position: 'absolute', top: 400, right: 100, width: 480,
        opacity: sp(200),
      }}>
        <div style={{
          fontFamily: 'Anton', fontSize: 90, color: '#D90000', lineHeight: 1,
        }}>×2</div>
        <div style={{
          fontFamily: 'Bricolage Grotesque', fontSize: 38,
          color: '#000000', marginTop: 15, lineHeight: 1.2,
        }}>
          Charge d'intérêt <b>quasiment doublée</b><br />en 6 ans
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene08Accelerateur;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09Refinancement',
  durationInSeconds: 29,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09Refinancement: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(10);
  const oldRate = sp(70);
  const arrowSlide = sp(160);
  const newRate = sp(220);
  const analogyOp = sp(290);

  const count1 = Math.floor(interpolate(frame, [70, 130], [0, 10], { extrapolateRight: 'clamp' })) / 10;
  const count35 = Math.floor(interpolate(frame, [220, 280], [0, 35], { extrapolateRight: 'clamp' })) / 10;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFEA93', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(120deg, transparent 0%, rgba(217,0,0,0.08) 100%)',
      }} />

      {/* Shape */}
      <div style={{
        position: 'absolute', width: 700, height: 700,
        border: '3px solid #000000', opacity: 0.05,
        top: '10%', left: '-10%',
        borderRadius: 40,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 400, height: 400, borderRadius: '50%',
        background: '#D90000', opacity: 0.05,
        bottom: '-10%', right: '-5%',
      }} />

      {/* Money symbols texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="dollars9" width="120" height="120" patternUnits="userSpaceOnUse">
            <text x="20" y="80" fontSize="60" fontFamily="Anton" fill="#000000">€</text>
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dollars9)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const s = seededRandom(i + 300);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`, top: `${seededRandom(i + 400) * 100}%`,
            width: 5, height: 5, borderRadius: '50%',
            background: '#000000', opacity: 0.3,
            transform: `translateY(${Math.sin(frame * 0.04 + i) * 25}px)`,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 60, left: 0, right: 0,
        textAlign: 'center',
        opacity: titleOp,
      }}>
        <div style={{
          fontFamily: 'Space Mono', fontSize: 28, color: '#D90000', letterSpacing: 4,
        }}>MÉCANISME DE REFINANCEMENT</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 90, color: '#000000', marginTop: 8, lineHeight: 1,
        }}>
          DE <span style={{ color: '#D90000' }}>ZÉRO</span> À <span style={{ color: '#D90000' }}>PLEIN TARIF</span>
        </div>
      </div>

      {/* Old bond */}
      <div style={{
        position: 'absolute', top: 320, left: 130, width: 620,
        padding: '35px', background: '#FFEA93',
        border: '5px dashed #000000', borderRadius: 20,
        opacity: oldRate,
        transform: `translateX(${(1 - oldRate) * -80}px)`,
      }}>
        <div style={{
          fontFamily: 'Space Mono', fontSize: 28, color: '#000000', letterSpacing: 2,
        }}>OBLIGATION 2019-2021</div>
        <div style={{
          fontFamily: 'Clash Display', fontSize: 180, color: '#000000',
          fontWeight: 900, letterSpacing: -4, lineHeight: 1,
        }}>
          {count1.toFixed(1).replace('.', ',')}%
        </div>
        <div style={{
          fontFamily: 'Anton', fontSize: 34, color: '#000000',
        }}>TAUX QUASI NUL</div>
      </div>

      {/* Arrow */}
      <div style={{
        position: 'absolute', top: 440, left: '50%',
        transform: `translateX(-50%) scale(${arrowSlide})`,
        opacity: arrowSlide,
      }}>
        <svg width="180" height="80" viewBox="0 0 180 80">
          <line x1="0" y1="40" x2="150" y2="40" stroke="#D90000" strokeWidth="12" />
          <polygon points="130,10 180,40 130,70" fill="#D90000" />
        </svg>
        <div style={{
          fontFamily: 'Anton', fontSize: 32, color: '#D90000',
          textAlign: 'center', marginTop: -5,
        }}>REFINANCEMENT</div>
      </div>

      {/* New bond */}
      <div style={{
        position: 'absolute', top: 320, right: 130, width: 620,
        padding: '35px', background: '#D90000',
        border: '5px solid #000000', borderRadius: 20,
        boxShadow: '10px 10px 0 #000000',
        opacity: newRate,
        transform: `translateX(${(1 - newRate) * 80}px)`,
      }}>
        <div style={{
          fontFamily: 'Space Mono', fontSize: 28, color: '#FFEA93', letterSpacing: 2,
        }}>OBLIGATION 2025+</div>
        <div style={{
          fontFamily: 'Clash Display', fontSize: 180, color: '#FFEA93',
          fontWeight: 900, letterSpacing: -4, lineHeight: 1,
        }}>
          {count35.toFixed(1).replace('.', ',')}%
        </div>
        <div style={{
          fontFamily: 'Anton', fontSize: 34, color: '#FFEA93',
        }}>TAUX ÉLEVÉS</div>
      </div>

      {/* Analogy */}
      <div style={{
        position: 'absolute', bottom: 80, left: 0, right: 0,
        textAlign: 'center',
        opacity: analogyOp,
      }}>
        <div style={{
          display: 'inline-block',
          padding: '18px 40px', background: '#000000', color: '#FFEA93',
          fontFamily: 'Bricolage Grotesque', fontSize: 38,
          fontStyle: 'italic', borderRadius: 10,
        }}>
          « Comme un crédit immo à 1 % refinancé soudain à 3,5 % »
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene09Refinancement;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10QuiEponge',
  durationInSeconds: 28,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10QuiEponge: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const qOp = sp(10);
  const answers = [
    { label: 'CONTRIBUABLE', delay: 60 },
    { label: 'ÉPARGNANT', delay: 100 },
    { label: 'VOUS', delay: 140 },
    { label: 'MOI', delay: 180 },
    { label: 'NOS ENFANTS', delay: 220 },
  ];
  const finalOp = sp(290);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFEA93', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(255,255,255,0.25) 0%, transparent 60%)',
      }} />

      {/* Shape */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: '3px solid #D90000', opacity: 0.06,
        top: '-20%', left: '-15%',
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500,
        border: '2px solid #000000', opacity: 0.08,
        bottom: '-15%', right: '-10%',
        transform: `rotate(${frame * 0.2}deg)`,
      }} />

      {/* Concentric rings */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {[1, 2, 3, 4].map(i => (
          <circle key={i} cx="960" cy="540" r={100 + i * 120}
            fill="none" stroke="#000000" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 500);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`, top: `${seededRandom(i + 600) * 100}%`,
            width: 5, height: 5, borderRadius: '50%',
            background: i % 3 === 0 ? '#D90000' : '#000000', opacity: 0.28,
            transform: `translateY(${Math.sin(frame * 0.04 + i) * 20}px)`,
          }} />
        );
      })}

      {/* Question big */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: qOp,
      }}>
        <div style={{
          fontFamily: 'Space Mono', fontSize: 30, color: '#D90000', letterSpacing: 4,
        }}>LA VRAIE QUESTION</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 130, color: '#000000',
          lineHeight: 1, marginTop: 10, letterSpacing: -2,
        }}>
          QUI <span style={{ color: '#D90000' }}>ÉPONGE</span> ?
        </div>
      </div>

      {/* Answer tags */}
      <div style={{
        position: 'absolute', top: 400, left: 0, right: 0,
        display: 'flex', justifyContent: 'center',
        flexWrap: 'wrap', gap: 25, padding: '0 100px',
      }}>
        {answers.map((a, i) => {
          const p = sp(a.delay);
          return (
            <div key={i} style={{
              padding: '22px 40px',
              background: i === answers.length - 1 ? '#D90000' : '#000000',
              color: '#FFEA93',
              fontFamily: 'Anton', fontSize: 52, fontWeight: 700,
              borderRadius: 60, letterSpacing: 2,
              boxShadow: i === answers.length - 1 ? '8px 8px 0 #000000' : '5px 5px 0 rgba(0,0,0,0.3)',
              opacity: p,
              transform: `translateY(${(1 - p) * 40}px) scale(${0.7 + p * 0.3})`,
            }}>
              {a.label}
            </div>
          );
        })}
      </div>

      {/* Payoff message */}
      <div style={{
        position: 'absolute', bottom: 120, left: 0, right: 0,
        textAlign: 'center',
        opacity: finalOp,
        transform: `translateY(${(1 - finalOp) * 30}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          padding: '25px 60px',
          background: '#FFEA93', border: '5px solid #000000',
          borderRadius: 20, boxShadow: '10px 10px 0 #D90000',
        }}>
          <div style={{
            fontFamily: 'Clash Display, Anton', fontSize: 62, fontWeight: 900,
            color: '#000000', letterSpacing: -1,
          }}>
            COMPRENDRE = <span style={{ color: '#D90000' }}>SE PROTÉGER</span>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene10QuiEponge;
```

