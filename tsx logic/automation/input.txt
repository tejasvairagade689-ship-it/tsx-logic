```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter01Scene01RateCutMyth',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BankIcon: React.FC = () => (
  <svg width="150" height="130" viewBox="0 0 150 130" fill="none">
    <path d="M15 48L75 14L135 48H15Z" fill="#FFF449" stroke="#25343F" strokeWidth="7" />
    <path d="M27 54H123V105H27V54Z" fill="#FFB56F" stroke="#25343F" strokeWidth="7" />
    {[42, 68, 94].map((x) => (
      <path key={x} d={`M${x} 60V99`} stroke="#25343F" strokeWidth="9" />
    ))}
    <path d="M15 106H135V120H15V106Z" fill="#FFF449" stroke="#25343F" strokeWidth="7" />
  </svg>
);

const HouseIcon: React.FC = () => (
  <svg width="220" height="190" viewBox="0 0 220 190" fill="none">
    <path d="M21 91L110 20L199 91" stroke="#25343F" strokeWidth="14" strokeLinejoin="round" />
    <path d="M43 80V169H177V80L110 27L43 80Z" fill="#FFF3E4" stroke="#25343F" strokeWidth="10" />
    <path d="M91 169V112H133V169" fill="#FFF449" stroke="#25343F" strokeWidth="9" />
    <path d="M145 93H166V118H145V93Z" fill="#FF9B51" stroke="#25343F" strokeWidth="7" />
  </svg>
);

const Chapter01Scene01RateCutMyth: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (
    delay: number,
    config?: {damping?: number; mass?: number; stiffness?: number},
  ) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {damping: 12, mass: 0.45, stiffness: 120, ...config},
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  const cameraScale = interpolate(
    frame,
    [0, durationInFrames],
    [1, 1.055],
    {
      easing: Easing.inOut(Easing.cubic),
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    },
  );

  const needleRotation = interpolate(
    frame,
    [50, 180, 300, 470],
    [-48, -35, -31, 32],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  const reportIn = sp(405, {damping: 10, stiffness: 150});
  const houseIn = sp(525, {damping: 13, stiffness: 105});
  const impact = Math.sin(frame * 0.4) * interpolate(
    frame,
    [455, 470, 500],
    [0, 7, 0],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  return (
    <AbsoluteFill style={{backgroundColor: '#FF9B51', overflow: 'hidden'}}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `scale(${cameraScale}) translate(${impact}px, ${-impact * 0.35}px)`,
          background:
            'radial-gradient(circle at 18% 20%, rgba(255,244,73,0.35), transparent 30%), linear-gradient(135deg, rgba(37,52,63,0.16), transparent 48%, rgba(37,52,63,0.27))',
          fontFamily: '"Noto Sans Devanagari", Inter, Poppins, sans-serif',
          color: '#25343F',
        }}
      >
        <AbsoluteFill
          style={{
            opacity: 0.22,
            backgroundImage:
              'linear-gradient(rgba(37,52,63,0.22) 2px, transparent 2px), linear-gradient(90deg, rgba(37,52,63,0.22) 2px, transparent 2px)',
            backgroundSize: '76px 76px',
            transform: `translate(${(frame * 0.3) % 76}px, ${(frame * 0.16) % 76}px)`,
          }}
        />

        {Array.from({length: 28}).map((_, i) => {
          const x = seededRandom(i + 1) * 1880;
          const speed = 0.25 + seededRandom(i + 30) * 0.55;
          const y = (seededRandom(i + 80) * 1200 + frame * speed) % 1200 - 60;
          const size = 5 + seededRandom(i + 120) * 13;

          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: y,
                width: size,
                height: size,
                backgroundColor: i % 3 === 0 ? '#FFF449' : '#25343F',
                opacity: 0.18 + seededRandom(i + 190) * 0.25,
                transform: `rotate(${frame * (i % 2 === 0 ? 0.3 : -0.2)}deg)`,
              }}
            />
          );
        })}

        <div
          style={{
            position: 'absolute',
            left: 72,
            top: 55,
            display: 'flex',
            gap: 18,
            alignItems: 'center',
            transform: `translateX(${interpolate(sp(0), [0, 1], [-280, 0])}px)`,
          }}
        >
          <div
            style={{
              background: '#25343F',
              color: '#FFF449',
              border: '5px solid #25343F',
              padding: '14px 24px',
              fontWeight: 950,
              fontSize: 30,
              letterSpacing: 2,
              boxShadow: '10px 10px 0 rgba(255,244,73,0.8)',
            }}
          >
            JULY 2026
          </div>
          <div
            style={{
              fontFamily: '"JetBrains Mono", monospace',
              fontSize: 25,
              fontWeight: 900,
              borderBottom: '5px solid #25343F',
            }}
          >
            HOME LOAN ALERT
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 105,
            top: 205,
            width: 390,
            height: 490,
            background: '#FFF3E4',
            border: '7px solid #25343F',
            boxShadow: '18px 18px 0 #25343F',
            transform: `rotate(-3deg) scale(${sp(28)})`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            paddingTop: 38,
          }}
        >
          <BankIcon />
          <div style={{fontSize: 42, fontWeight: 950, marginTop: 18}}>RBI RATE</div>

          <div
            style={{
              width: 260,
              height: 135,
              overflow: 'hidden',
              position: 'relative',
              marginTop: 18,
            }}
          >
            <div
              style={{
                width: 240,
                height: 240,
                borderRadius: '50%',
                border: '18px solid #25343F',
                borderBottomColor: '#FFF449',
                borderLeftColor: '#FFF449',
                position: 'absolute',
                left: 10,
                top: 5,
              }}
            />
            <div
              style={{
                width: 98,
                height: 13,
                background: '#D94B3D',
                border: '4px solid #25343F',
                position: 'absolute',
                left: 130,
                top: 116,
                transformOrigin: '0% 50%',
                transform: `rotate(${needleRotation}deg)`,
              }}
            />
            <div
              style={{
                position: 'absolute',
                left: 113,
                top: 99,
                width: 38,
                height: 38,
                borderRadius: '50%',
                background: '#FFF449',
                border: '6px solid #25343F',
              }}
            />
          </div>

          <div style={{fontSize: 54, lineHeight: 1, fontWeight: 1000}}>5.25%</div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 535,
            top: 205,
            width: 760,
            transform: `translateY(${interpolate(sp(80), [0, 1], [90, 0])}px)`,
            opacity: sp(80),
          }}
        >
          <div
            style={{
              display: 'flex',
              gap: 18,
              marginBottom: 26,
            }}
          >
            {['WAIT', 'WAIT', 'WAIT'].map((text, i) => (
              <div
                key={i}
                style={{
                  flex: 1,
                  background: i === 2 ? '#FFF449' : '#FFB56F',
                  border: '5px solid #25343F',
                  padding: '14px',
                  textAlign: 'center',
                  fontSize: 30,
                  fontWeight: 950,
                  transform: `translateY(${Math.sin(frame * 0.05 + i) * 8}px) rotate(${i - 1}deg)`,
                }}
              >
                {text}
              </div>
            ))}
          </div>

          <div
            style={{
              fontSize: 104,
              lineHeight: 0.92,
              fontWeight: 1000,
              letterSpacing: -6,
            }}
          >
            RATE CUT
            <br />
            <span style={{color: '#FFF449', textShadow: '6px 6px 0 #25343F'}}>
              कब आएगा?
            </span>
          </div>

          <div
            style={{
              marginTop: 38,
              height: 10,
              background: '#25343F',
              position: 'relative',
            }}
          >
            <div
              style={{
                position: 'absolute',
                left: `${interpolate(frame, [80, 370], [0, 92], {
                  extrapolateLeft: 'clamp',
                  extrapolateRight: 'clamp',
                })}%`,
                top: -15,
                width: 36,
                height: 36,
                background: '#FFF449',
                border: '6px solid #25343F',
                transform: 'rotate(45deg)',
              }}
            />
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 74,
            top: 114,
            width: 475,
            height: 320,
            padding: '34px 38px',
            background: '#D94B3D',
            color: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '18px 18px 0 #FFF449',
            transform: `rotate(2deg) scale(${reportIn})`,
          }}
        >
          <div
            style={{
              fontFamily: '"JetBrains Mono", monospace',
              fontSize: 25,
              color: '#FFF449',
              fontWeight: 900,
              marginBottom: 26,
            }}
          >
            FRESH REPORT
          </div>
          <div style={{fontSize: 64, lineHeight: 0.95, fontWeight: 1000}}>
            CUT
            <br />
            CANCELLED
          </div>
          <div
            style={{
              marginTop: 22,
              display: 'flex',
              alignItems: 'center',
              gap: 14,
              fontSize: 31,
              fontWeight: 900,
            }}
          >
            <svg width="52" height="52" viewBox="0 0 52 52">
              <path d="M9 39L39 9M23 9H39V25" fill="none" stroke="#FFF449" strokeWidth="8" />
            </svg>
            HIKE POSSIBLE
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 560,
            bottom: 84,
            width: 1190,
            height: 250,
            background: '#FFF3E4',
            border: '7px solid #25343F',
            boxShadow: '15px 15px 0 rgba(37,52,63,0.7)',
            transform: `translateY(${interpolate(houseIn, [0, 1], [310, 0])}px) rotate(-1deg)`,
            display: 'flex',
            alignItems: 'center',
            padding: '24px 48px',
            gap: 55,
          }}
        >
          <div style={{transform: `translateY(${Math.sin(frame * 0.06) * 4}px)`}}>
            <HouseIcon />
          </div>

          <div style={{flex: 1}}>
            <div
              style={{
                fontFamily: '"JetBrains Mono", monospace',
                fontSize: 25,
                fontWeight: 900,
                letterSpacing: 2,
              }}
            >
              BIGGEST COMMITMENT
            </div>
            <div style={{fontSize: 74, fontWeight: 1000, lineHeight: 1}}>
              HOME LOAN
            </div>
            <div
              style={{
                marginTop: 17,
                height: 28,
                border: '5px solid #25343F',
                background: '#FFB56F',
              }}
            >
              <div
                style={{
                  width: `${interpolate(houseIn, [0, 1], [0, 78])}%`,
                  height: '100%',
                  background: '#D94B3D',
                  borderRight: '5px solid #25343F',
                }}
              />
            </div>
          </div>

          <svg width="190" height="130" viewBox="0 0 190 130">
            <path
              d="M14 39H93V17L174 65L93 113V91H14V39Z"
              fill="#FFF449"
              stroke="#25343F"
              strokeWidth="8"
              strokeLinejoin="round"
            />
            <path d="M59 39V91" stroke="#25343F" strokeWidth="6" strokeDasharray="10 9" />
          </svg>
        </div>

        <AbsoluteFill
          style={{
            pointerEvents: 'none',
            background:
              'radial-gradient(circle, transparent 48%, rgba(37,52,63,0.44) 115%)',
          }}
        />

        <div
          style={{
            position: 'absolute',
            right: 45,
            bottom: 28,
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            fontWeight: 700,
            color: '#25343F',
            opacity: 0.38,
          }}
        >
          ProBuffet HQ
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Chapter01Scene01RateCutMyth;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter01Scene02InterestMachine',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const VaultIcon: React.FC<{rotation: number}> = ({rotation}) => (
  <svg width="210" height="210" viewBox="0 0 210 210" fill="none">
    <rect x="16" y="16" width="178" height="178" rx="16" fill="#FFF3E4" stroke="#25343F" strokeWidth="10" />
    <circle cx="105" cy="105" r="59" fill="#FF9B51" stroke="#25343F" strokeWidth="9" />
    <g transform={`rotate(${rotation} 105 105)`}>
      <circle cx="105" cy="105" r="20" fill="#FFF449" stroke="#25343F" strokeWidth="7" />
      {[0, 90, 180, 270].map((a) => (
        <path
          key={a}
          d="M105 50V83"
          stroke="#25343F"
          strokeWidth="10"
          strokeLinecap="round"
          transform={`rotate(${a} 105 105)`}
        />
      ))}
    </g>
  </svg>
);

const Chapter01Scene02InterestMachine: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (
    delay: number,
    config?: {damping?: number; mass?: number; stiffness?: number},
  ) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {damping: 13, mass: 0.42, stiffness: 115, ...config},
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  const pan = interpolate(frame, [0, durationInFrames], [-25, 35], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const interestWidth = interpolate(frame, [90, 390], [8, 86], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const revealTruth = sp(420, {damping: 10, stiffness: 145});
  const hostIn = sp(550, {damping: 11, stiffness: 135});

  return (
    <AbsoluteFill style={{backgroundColor: '#FF9B51', overflow: 'hidden'}}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `translateX(${pan}px) scale(1.035)`,
          background:
            'linear-gradient(120deg, rgba(37,52,63,0.32), transparent 33%, rgba(255,244,73,0.16) 62%, rgba(37,52,63,0.22))',
          color: '#25343F',
          fontFamily: '"Noto Sans Devanagari", Inter, Poppins, sans-serif',
        }}
      >
        <AbsoluteFill
          style={{
            opacity: 0.2,
            backgroundImage:
              'radial-gradient(circle, #25343F 2.5px, transparent 3px)',
            backgroundSize: '32px 32px',
            transform: `translateY(${(frame * 0.25) % 32}px)`,
          }}
        />

        {Array.from({length: 20}).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              width: 10 + seededRandom(i + 1) * 18,
              height: 10 + seededRandom(i + 1) * 18,
              left: seededRandom(i + 20) * 1900,
              top: seededRandom(i + 40) * 1080,
              border: '3px solid #25343F',
              opacity: 0.18,
              transform: `rotate(${frame * (i % 2 ? 0.25 : -0.18) + i * 19}deg)`,
            }}
          />
        ))}

        <div
          style={{
            position: 'absolute',
            left: 74,
            top: 64,
            background: '#FFF449',
            border: '6px solid #25343F',
            boxShadow: '12px 12px 0 #25343F',
            padding: '14px 28px',
            fontSize: 33,
            fontWeight: 1000,
            transform: `rotate(-2deg) scale(${sp(0)})`,
          }}
        >
          THE INTEREST MACHINE
        </div>

        <div
          style={{
            position: 'absolute',
            left: 82,
            top: 220,
            width: 365,
            height: 310,
            background: '#FFF3E4',
            border: '7px solid #25343F',
            boxShadow: '15px 15px 0 rgba(37,52,63,0.7)',
            transform: `translateX(${interpolate(sp(30), [0, 1], [-450, 0])}px) rotate(-2deg)`,
            padding: 30,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, fontWeight: 900}}>
            MONTHLY SALARY
          </div>
          <div style={{fontSize: 70, fontWeight: 1000, marginTop: 24}}>₹100</div>
          <div
            style={{
              display: 'grid',
              gridTemplateColumns: 'repeat(5, 1fr)',
              gap: 8,
              marginTop: 20,
            }}
          >
            {Array.from({length: 15}).map((_, i) => (
              <div
                key={i}
                style={{
                  height: 25,
                  background: i < 12 ? '#FFF449' : '#FF9B51',
                  border: '3px solid #25343F',
                  transform: `translateY(${Math.sin(frame * 0.08 + i) * 3}px)`,
                }}
              />
            ))}
          </div>
        </div>

        <svg
          width="1030"
          height="410"
          viewBox="0 0 1030 410"
          style={{position: 'absolute', left: 365, top: 300, overflow: 'visible'}}
        >
          <path
            d="M20 120C230 120 250 250 455 250C670 250 720 90 1000 90"
            fill="none"
            stroke="#25343F"
            strokeWidth="62"
            strokeLinecap="square"
          />
          <path
            d="M20 120C230 120 250 250 455 250C670 250 720 90 1000 90"
            fill="none"
            stroke="#FFF449"
            strokeWidth="36"
            strokeDasharray="26 18"
            strokeDashoffset={-frame * 2.8}
          />
        </svg>

        {Array.from({length: 8}).map((_, i) => {
          const progress = ((frame * 1.25 + i * 78) % 630) / 630;
          const x = interpolate(progress, [0, 1], [390, 1370]);
          const y = 390 + Math.sin(progress * Math.PI * 2) * 105;

          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: y,
                width: 54,
                height: 54,
                borderRadius: '50%',
                background: '#FFF449',
                border: '6px solid #25343F',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontFamily: '"JetBrains Mono", monospace',
                fontSize: 25,
                fontWeight: 1000,
                transform: `rotate(${frame * 2 + i * 35}deg)`,
              }}
            >
              ₹
            </div>
          );
        })}

        <div
          style={{
            position: 'absolute',
            left: 570,
            top: 650,
            width: 530,
            background: '#FFF3E4',
            border: '7px solid #25343F',
            padding: '28px 32px',
            transform: `rotate(1deg) scale(${sp(170)})`,
          }}
        >
          <div style={{display: 'flex', justifyContent: 'space-between', fontSize: 28, fontWeight: 950}}>
            <span>PRINCIPAL</span>
            <span>FROZEN</span>
          </div>
          <div style={{height: 38, border: '5px solid #25343F', marginTop: 13, background: '#FFB56F'}}>
            <div style={{width: '15%', height: '100%', background: '#FFF449', borderRight: '5px solid #25343F'}} />
          </div>

          <div style={{display: 'flex', justifyContent: 'space-between', fontSize: 28, fontWeight: 950, marginTop: 24}}>
            <span>INTEREST</span>
            <span>{Math.round(interestWidth)}%</span>
          </div>
          <div style={{height: 38, border: '5px solid #25343F', marginTop: 13, background: '#FFB56F'}}>
            <div
              style={{
                width: `${interestWidth}%`,
                height: '100%',
                background: '#D94B3D',
                borderRight: '5px solid #25343F',
              }}
            />
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 76,
            top: 172,
            width: 390,
            height: 415,
            background: '#D96E2D',
            border: '8px solid #25343F',
            boxShadow: '18px 18px 0 #FFF449',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            paddingTop: 30,
            transform: `translateX(${interpolate(sp(210), [0, 1], [500, 0])}px) rotate(2deg)`,
          }}
        >
          <VaultIcon rotation={frame * 0.65} />
          <div style={{fontSize: 35, fontWeight: 1000, marginTop: 12}}>BANK INCOME</div>
          <div style={{fontSize: 66, fontWeight: 1000, color: '#FFF449'}}>INTEREST</div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 85,
            bottom: 92,
            display: 'flex',
            gap: 16,
            transform: `scale(${revealTruth})`,
          }}
        >
          {['BANK', 'ADVISOR', 'NEWS'].map((label, i) => (
            <div
              key={label}
              style={{
                width: 190,
                height: 135,
                background: '#FFF3E4',
                border: '5px solid #25343F',
                padding: 18,
                transform: `rotate(${i === 1 ? 2 : -2}deg) translateY(${Math.sin(frame * 0.045 + i) * 6}px)`,
              }}
            >
              <div style={{fontSize: 25, fontWeight: 1000}}>{label}</div>
              <div
                style={{
                  marginTop: 17,
                  height: 26,
                  background: '#25343F',
                  transform: `scaleX(${interpolate(revealTruth, [0, 1], [0, 1])})`,
                  transformOrigin: 'left center',
                }}
              />
              <div style={{fontSize: 18, fontWeight: 900, marginTop: 12}}>NOT SHOWN</div>
            </div>
          ))}
        </div>

        <div
          style={{
            position: 'absolute',
            left: 82,
            bottom: 78,
            width: 405,
            background: '#25343F',
            color: '#FFF3E4',
            border: '7px solid #25343F',
            boxShadow: '14px 14px 0 #FFF449',
            padding: '26px 32px',
            transform: `translateY(${interpolate(hostIn, [0, 1], [220, 0])}px) rotate(-1deg)`,
          }}
        >
          <div style={{display: 'flex', alignItems: 'center', gap: 22}}>
            <svg width="68" height="68" viewBox="0 0 68 68">
              <rect x="16" y="7" width="36" height="42" rx="18" fill="#FFF449" stroke="#FFF3E4" strokeWidth="5" />
              <path d="M8 35V40C8 54 19 61 34 61C49 61 60 54 60 40V35" fill="none" stroke="#FFF3E4" strokeWidth="6" />
              <path d="M34 61V67" stroke="#FFF3E4" strokeWidth="6" />
            </svg>
            <div>
              <div style={{fontSize: 45, fontWeight: 1000, color: '#FFF449'}}>KUNAL</div>
              <div style={{fontSize: 23, fontWeight: 800}}>PROBUFFET HQ</div>
            </div>
          </div>
        </div>

        <AbsoluteFill
          style={{
            pointerEvents: 'none',
            background: 'radial-gradient(circle, transparent 48%, rgba(37,52,63,0.45) 118%)',
          }}
        />

        <div
          style={{
            position: 'absolute',
            right: 45,
            bottom: 28,
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            fontWeight: 700,
            opacity: 0.35,
          }}
        >
          ProBuffet HQ
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Chapter01Scene02InterestMachine;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter01Scene03ReutersPoll',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ReportIcon: React.FC = () => (
  <svg width="120" height="135" viewBox="0 0 120 135" fill="none">
    <path d="M13 9H82L108 35V126H13V9Z" fill="#FFF3E4" stroke="#25343F" strokeWidth="8" />
    <path d="M82 9V35H108" fill="#FFF449" stroke="#25343F" strokeWidth="8" />
    <path d="M31 59H89M31 80H89M31 101H72" stroke="#25343F" strokeWidth="8" />
  </svg>
);

const Chapter01Scene03ReutersPoll: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (
    delay: number,
    config?: {damping?: number; mass?: number; stiffness?: number},
  ) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {damping: 12, mass: 0.45, stiffness: 120, ...config},
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  const folderOpen = interpolate(frame, [35, 130], [88, 7], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const pollReveal = sp(190, {damping: 15, stiffness: 95});
  const answerIn = sp(430, {damping: 8, stiffness: 175});
  const rateIn = sp(550, {damping: 11, stiffness: 130});

  const shake = Math.sin(frame * 0.8) * interpolate(
    frame,
    [425, 445, 480],
    [0, 8, 0],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  return (
    <AbsoluteFill style={{backgroundColor: '#FF9B51', overflow: 'hidden'}}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `translate(${shake}px, ${-shake * 0.4}px) scale(1.025)`,
          background:
            'radial-gradient(circle at 82% 14%, rgba(255,244,73,0.32), transparent 27%), linear-gradient(160deg, rgba(37,52,63,0.4), transparent 54%, rgba(37,52,63,0.13))',
          color: '#25343F',
          fontFamily: '"Noto Sans Devanagari", Inter, Poppins, sans-serif',
        }}
      >
        <AbsoluteFill
          style={{
            opacity: 0.18,
            backgroundImage:
              'repeating-linear-gradient(115deg, #25343F 0px, #25343F 2px, transparent 2px, transparent 19px)',
            transform: `translateX(${(frame * 0.18) % 19}px)`,
          }}
        />

        {Array.from({length: 18}).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: seededRandom(i + 8) * 1900,
              top: seededRandom(i + 31) * 1080,
              width: 5 + seededRandom(i + 50) * 11,
              height: 35 + seededRandom(i + 60) * 80,
              background: i % 2 ? '#FFF449' : '#25343F',
              opacity: 0.14,
              transform: `rotate(${frame * 0.15 + i * 31}deg)`,
            }}
          />
        ))}

        <div
          style={{
            position: 'absolute',
            left: 74,
            top: 53,
            padding: '15px 27px',
            background: '#25343F',
            color: '#FFF449',
            border: '6px solid #25343F',
            boxShadow: '12px 12px 0 #FFF3E4',
            fontFamily: '"JetBrains Mono", monospace',
            fontSize: 28,
            fontWeight: 1000,
            transform: `rotate(-2deg) scale(${sp(0)})`,
          }}
        >
          UNCOMFORTABLE FILE
        </div>

        <div
          style={{
            position: 'absolute',
            left: 86,
            top: 205,
            width: 700,
            height: 670,
            perspective: 1200,
          }}
        >
          <div
            style={{
              position: 'absolute',
              inset: 0,
              background: '#D96E2D',
              border: '9px solid #25343F',
              boxShadow: '20px 20px 0 #25343F',
              transform: 'rotate(-2deg)',
            }}
          />

          <div
            style={{
              position: 'absolute',
              left: 35,
              top: 35,
              right: 35,
              bottom: 35,
              background: '#FFF3E4',
              border: '7px solid #25343F',
              padding: 38,
              transform: `scale(${sp(45)})`,
            }}
          >
            <div style={{display: 'flex', justifyContent: 'space-between', alignItems: 'center'}}>
              <ReportIcon />
              <div style={{textAlign: 'right'}}>
                <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, fontWeight: 900}}>
                  REUTERS POLL
                </div>
                <div style={{fontSize: 45, fontWeight: 1000}}>27 JUL 2026</div>
              </div>
            </div>

            <div
              style={{
                height: 7,
                background: '#25343F',
                margin: '26px 0',
              }}
            />

            <div style={{fontSize: 53, lineHeight: 1.08, fontWeight: 1000}}>
              AUGUST में
              <br />
              RATE बदलेगा?
            </div>

            <div
              style={{
                display: 'grid',
                gridTemplateColumns: 'repeat(12, 1fr)',
                gap: 9,
                marginTop: 38,
                opacity: pollReveal,
              }}
            >
              {Array.from({length: 72}).map((_, i) => {
                const dotIn = spring({
                  frame: Math.max(0, frame - 195 - i * 1.4),
                  fps,
                  config: {damping: 14, stiffness: 135},
                });

                return (
                  <div
                    key={i}
                    style={{
                      aspectRatio: '1',
                      borderRadius: '50%',
                      background: i % 6 === 0 ? '#FFF449' : '#FF9B51',
                      border: '3px solid #25343F',
                      transform: `scale(${dotIn}) translateY(${Math.sin(frame * 0.07 + i) * 2}px)`,
                    }}
                  />
                );
              })}
            </div>

            <div
              style={{
                fontFamily: '"JetBrains Mono", monospace',
                fontSize: 25,
                fontWeight: 900,
                marginTop: 22,
              }}
            >
              72 ECONOMISTS
            </div>
          </div>

          <div
            style={{
              position: 'absolute',
              left: -18,
              right: -18,
              bottom: -12,
              height: 340,
              background: '#FFF449',
              border: '9px solid #25343F',
              transformOrigin: 'bottom center',
              transform: `rotateX(${folderOpen}deg) rotate(1deg)`,
              boxShadow: '0 -12px 0 rgba(37,52,63,0.3)',
            }}
          >
            <div
              style={{
                width: 220,
                height: 55,
                background: '#FFF449',
                border: '8px solid #25343F',
                borderBottom: 0,
                position: 'absolute',
                left: 35,
                top: -61,
              }}
            />
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 855,
            top: 170,
            width: 930,
            height: 285,
            background: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '18px 18px 0 #FFF449',
            transform: `rotate(1.5deg) translateY(${interpolate(sp(280), [0, 1], [-220, 0])}px)`,
            padding: '38px 48px',
          }}
        >
          <div
            style={{
              fontFamily: '"JetBrains Mono", monospace',
              fontSize: 25,
              fontWeight: 900,
            }}
          >
            QUESTION
          </div>
          <div style={{fontSize: 76, fontWeight: 1000, lineHeight: 1, marginTop: 20}}>
            RBI RATE CHANGE?
          </div>
          <div style={{height: 9, background: '#25343F', marginTop: 28}}>
            <div
              style={{
                height: '100%',
                width: `${interpolate(sp(320), [0, 1], [0, 100])}%`,
                background: '#D94B3D',
              }}
            />
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 970,
            top: 490,
            width: 600,
            height: 275,
            background: '#D94B3D',
            color: '#FFF3E4',
            border: '10px solid #25343F',
            boxShadow: '20px 20px 0 #25343F',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            transform: `rotate(-5deg) scale(${answerIn})`,
          }}
        >
          <div style={{fontSize: 168, lineHeight: 1, fontWeight: 1000}}>नहीं</div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 82,
            bottom: 85,
            width: 570,
            height: 195,
            background: '#25343F',
            color: '#FFF449',
            border: '8px solid #25343F',
            boxShadow: '16px 16px 0 #FFF3E4',
            padding: '30px 40px',
            transform: `rotate(2deg) scale(${rateIn})`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 25, fontWeight: 900}}>
            REPO RATE HOLDS
          </div>
          <div style={{fontSize: 91, fontWeight: 1000, lineHeight: 1}}>5.25%</div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 750,
            bottom: 88,
            width: 260,
            background: '#FFF449',
            border: '6px solid #25343F',
            padding: '21px 24px',
            fontSize: 28,
            fontWeight: 1000,
            transform: `rotate(-8deg) scale(${sp(610)})`,
          }}
        >
          PLANNING
          <br />
          बचानी है
        </div>

        <AbsoluteFill
          style={{
            pointerEvents: 'none',
            background: 'radial-gradient(circle, transparent 48%, rgba(37,52,63,0.48) 118%)',
          }}
        />

        <div
          style={{
            position: 'absolute',
            right: 45,
            bottom: 28,
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            fontWeight: 700,
            opacity: 0.35,
          }}
        >
          ProBuffet HQ
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Chapter01Scene03ReutersPoll;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter01Scene04RateTransmissionBlocked',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ScissorsIcon: React.FC<{open: number}> = ({open}) => (
  <svg width="130" height="115" viewBox="0 0 130 115" fill="none">
    <circle cx="26" cy="83" r="19" fill="#FFF3E4" stroke="#25343F" strokeWidth="7" />
    <circle cx="65" cy="83" r="19" fill="#FFF3E4" stroke="#25343F" strokeWidth="7" />
    <path d="M39 69L102 18" stroke="#25343F" strokeWidth="9" strokeLinecap="round" transform={`rotate(${open} 39 69)`} />
    <path d="M54 69L104 34" stroke="#D94B3D" strokeWidth="9" strokeLinecap="round" transform={`rotate(${-open} 54 69)`} />
  </svg>
);

const Chapter01Scene04RateTransmissionBlocked: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (
    delay: number,
    config?: {damping?: number; mass?: number; stiffness?: number},
  ) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {damping: 13, mass: 0.44, stiffness: 118, ...config},
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  const pipeProgress = interpolate(frame, [250, 470], [0, 1], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const blockedIn = sp(455, {damping: 8, stiffness: 165});
  const noIn = sp(590, {damping: 9, stiffness: 160});
  const valveRotation = interpolate(frame, [330, 450], [0, 90], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{backgroundColor: '#FF9B51', overflow: 'hidden'}}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          opacity: exitOp,
          background:
            'linear-gradient(145deg, rgba(255,244,73,0.18), transparent 42%), radial-gradient(circle at 88% 72%, rgba(37,52,63,0.34), transparent 38%)',
          color: '#25343F',
          fontFamily: '"Noto Sans Devanagari", Inter, Poppins, sans-serif',
        }}
      >
        <AbsoluteFill
          style={{
            opacity: 0.18,
            backgroundImage:
              'linear-gradient(45deg, #25343F 25%, transparent 25%, transparent 75%, #25343F 75%), linear-gradient(45deg, #25343F 25%, transparent 25%, transparent 75%, #25343F 75%)',
            backgroundPosition: `0 0, ${24 + (frame * 0.08) % 48}px ${24 + (frame * 0.08) % 48}px`,
            backgroundSize: '48px 48px',
          }}
        />

        {Array.from({length: 16}).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: seededRandom(i + 2) * 1900,
              top: (seededRandom(i + 42) * 1200 + frame * 0.35) % 1200 - 80,
              width: 9,
              height: 9,
              background: i % 2 ? '#FFF449' : '#25343F',
              opacity: 0.25,
            }}
          />
        ))}

        <div
          style={{
            position: 'absolute',
            left: 70,
            top: 55,
            fontFamily: '"JetBrains Mono", monospace',
            fontSize: 27,
            fontWeight: 1000,
            padding: '14px 24px',
            border: '6px solid #25343F',
            background: '#FFF3E4',
            boxShadow: '12px 12px 0 #25343F',
            transform: `rotate(-2deg) scale(${sp(0)})`,
          }}
        >
          RATE CUT TRANSMISSION TEST
        </div>

        <div
          style={{
            position: 'absolute',
            left: 72,
            top: 185,
            width: 720,
            height: 430,
            background: '#25343F',
            color: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '18px 18px 0 #FFF449',
            padding: '34px 38px',
            transform: `rotate(-1deg) translateX(${interpolate(sp(35), [0, 1], [-780, 0])}px)`,
          }}
        >
          <div style={{display: 'flex', alignItems: 'flex-end', gap: 24}}>
            <div style={{fontSize: 130, lineHeight: 0.85, fontWeight: 1000, color: '#FFF449'}}>95%</div>
            <div style={{fontSize: 28, lineHeight: 1.15, fontWeight: 900}}>
              EXPECT
              <br />
              NO CHANGE
            </div>
          </div>

          <div
            style={{
              display: 'grid',
              gridTemplateColumns: 'repeat(12, 1fr)',
              gap: 8,
              marginTop: 30,
            }}
          >
            {Array.from({length: 72}).map((_, i) => {
              const dotIn = spring({
                frame: Math.max(0, frame - 75 - i * 1.15),
                fps,
                config: {damping: 14, stiffness: 130},
              });

              return (
                <div
                  key={i}
                  style={{
                    aspectRatio: '1',
                    borderRadius: '50%',
                    background: i < 68 ? '#FFF449' : '#FF9B51',
                    border: '3px solid #FFF3E4',
                    transform: `scale(${dotIn})`,
                  }}
                />
              );
            })}
          </div>

          <div
            style={{
              marginTop: 25,
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'center',
              fontFamily: '"JetBrains Mono", monospace',
              fontWeight: 900,
            }}
          >
            <span style={{fontSize: 32}}>68 / 72</span>
            <span style={{fontSize: 29, color: '#FFF449'}}>REPO 5.25%</span>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 82,
            top: 172,
            width: 800,
            height: 250,
            background: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '17px 17px 0 #25343F',
            padding: '29px 38px',
            transform: `rotate(1deg) translateY(${interpolate(sp(210), [0, 1], [-290, 0])}px)`,
          }}
        >
          <div style={{display: 'flex', alignItems: 'center', gap: 30}}>
            <ScissorsIcon open={Math.sin(frame * 0.11) * 8} />
            <div>
              <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, fontWeight: 900}}>
                RBI CUT DURING 2025
              </div>
              <div style={{fontSize: 83, lineHeight: 1, fontWeight: 1000}}>125 BPS</div>
            </div>
          </div>
        </div>

        <svg
          width="1280"
          height="360"
          viewBox="0 0 1280 360"
          style={{
            position: 'absolute',
            left: 390,
            top: 590,
            overflow: 'visible',
          }}
        >
          <path
            d="M25 115H520C625 115 650 235 760 235H1230"
            fill="none"
            stroke="#25343F"
            strokeWidth="72"
            strokeLinecap="square"
          />
          <path
            d="M25 115H520C625 115 650 235 760 235H1230"
            fill="none"
            stroke="#FFF3E4"
            strokeWidth="44"
            strokeDasharray="28 20"
            strokeDashoffset={-frame * 2.2}
          />
          <path
            d="M25 115H520C625 115 650 235 760 235H1230"
            fill="none"
            stroke="#FFF449"
            strokeWidth="28"
            strokeDasharray={`${pipeProgress * 1150} 1400`}
          />

          <g transform={`translate(690 175) rotate(${valveRotation})`}>
            <circle cx="0" cy="0" r="72" fill="#D94B3D" stroke="#25343F" strokeWidth="10" />
            <path d="M-43 0H43M0-43V43" stroke="#FFF3E4" strokeWidth="16" />
          </g>
        </svg>

        <div
          style={{
            position: 'absolute',
            left: 74,
            bottom: 80,
            width: 380,
            height: 220,
            background: '#FFF449',
            border: '8px solid #25343F',
            boxShadow: '15px 15px 0 #25343F',
            padding: '30px 34px',
            transform: `rotate(-3deg) scale(${sp(315)})`,
          }}
        >
          <div style={{fontSize: 31, fontWeight: 1000}}>RATE CUT</div>
          <div style={{fontSize: 72, lineHeight: 1, fontWeight: 1000}}>भेजा</div>
          <svg width="210" height="42" viewBox="0 0 210 42">
            <path d="M4 21H190M164 4L190 21L164 38" fill="none" stroke="#25343F" strokeWidth="8" />
          </svg>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 1000,
            bottom: 76,
            width: 740,
            height: 225,
            background: '#D94B3D',
            color: '#FFF3E4',
            border: '9px solid #25343F',
            boxShadow: '18px 18px 0 #FFF449',
            padding: '28px 38px',
            transform: `rotate(2deg) scale(${blockedIn})`,
          }}
        >
          <div style={{display: 'flex', alignItems: 'center', justifyContent: 'space-between'}}>
            <div>
              <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 25, color: '#FFF449', fontWeight: 900}}>
                YOUR MONTHLY OUTGO
              </div>
              <div style={{fontSize: 66, lineHeight: 1.05, fontWeight: 1000}}>EMI SAME</div>
            </div>

            <div
              style={{
                width: 145,
                height: 145,
                borderRadius: '50%',
                background: '#FFF3E4',
                border: '8px solid #25343F',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                transform: `scale(${noIn}) rotate(-9deg)`,
              }}
            >
              <div style={{fontSize: 75, fontWeight: 1000, color: '#D94B3D'}}>NO</div>
            </div>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 860,
            top: 530,
            background: '#25343F',
            color: '#FFF449',
            border: '6px solid #25343F',
            padding: '13px 23px',
            fontSize: 28,
            fontWeight: 1000,
            transform: `rotate(-8deg) scale(${blockedIn})`,
          }}
        >
          BLOCKED
        </div>

        <AbsoluteFill
          style={{
            pointerEvents: 'none',
            background: 'radial-gradient(circle, transparent 50%, rgba(37,52,63,0.45) 118%)',
          }}
        />

        <div
          style={{
            position: 'absolute',
            right: 45,
            bottom: 28,
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            fontWeight: 700,
            opacity: 0.35,
          }}
        >
          ProBuffet HQ
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Chapter01Scene04RateTransmissionBlocked;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter01Scene05TenureExtensionTrap',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const LockIcon: React.FC<{pulse: number}> = ({pulse}) => (
  <svg width="115" height="130" viewBox="0 0 115 130" fill="none" style={{transform: `scale(${pulse})`}}>
    <path d="M29 55V38C29 20 41 10 58 10C75 10 87 20 87 38V55" stroke="#25343F" strokeWidth="11" />
    <rect x="13" y="50" width="90" height="68" rx="8" fill="#FFF449" stroke="#25343F" strokeWidth="9" />
    <circle cx="58" cy="80" r="10" fill="#25343F" />
    <path d="M58 88V102" stroke="#25343F" strokeWidth="9" />
  </svg>
);

const Chapter01Scene05TenureExtensionTrap: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (
    delay: number,
    config?: {damping?: number; mass?: number; stiffness?: number},
  ) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {damping: 12, mass: 0.44, stiffness: 120, ...config},
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  const timelineWidth = interpolate(frame, [125, 500], [34, 100], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const twentyThreeIn = sp(310, {damping: 10, stiffness: 145});
  const twentyFiveIn = sp(430, {damping: 9, stiffness: 155});
  const receiptIn = sp(520, {damping: 11, stiffness: 130});
  const stealthScan = (frame * 3.1) % 780;

  return (
    <AbsoluteFill style={{backgroundColor: '#FF9B51', overflow: 'hidden'}}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          opacity: exitOp,
          background:
            'linear-gradient(125deg, rgba(37,52,63,0.28), transparent 46%, rgba(255,244,73,0.2)), radial-gradient(circle at 65% 82%, rgba(37,52,63,0.27), transparent 38%)',
          color: '#25343F',
          fontFamily: '"Noto Sans Devanagari", Inter, Poppins, sans-serif',
        }}
      >
        <AbsoluteFill
          style={{
            opacity: 0.22,
            backgroundImage:
              'repeating-linear-gradient(0deg, transparent 0, transparent 31px, rgba(37,52,63,0.28) 32px, transparent 34px)',
            transform: `translateY(${(frame * 0.25) % 34}px)`,
          }}
        />

        {Array.from({length: 14}).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: seededRandom(i + 9) * 1900,
              top: seededRandom(i + 39) * 1080,
              width: 30 + seededRandom(i + 79) * 90,
              height: 5,
              background: i % 3 === 0 ? '#FFF449' : '#25343F',
              opacity: 0.18,
              transform: `rotate(${seededRandom(i + 99) * 180}deg) translateX(${Math.sin(frame * 0.03 + i) * 18}px)`,
            }}
          />
        ))}

        <div
          style={{
            position: 'absolute',
            left: 73,
            top: 56,
            background: '#D94B3D',
            color: '#FFF3E4',
            border: '7px solid #25343F',
            boxShadow: '14px 14px 0 #FFF449',
            padding: '15px 28px',
            fontSize: 34,
            fontWeight: 1000,
            transform: `rotate(-2deg) scale(${sp(0)})`,
          }}
        >
          SILENT TENURE TRAP
        </div>

        <div
          style={{
            position: 'absolute',
            right: 77,
            top: 62,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            background: '#FFF3E4',
            border: '6px solid #25343F',
            padding: '14px 24px',
            transform: `rotate(2deg) scale(${sp(35)})`,
          }}
        >
          <LockIcon pulse={1 + Math.sin(frame * 0.07) * 0.03} />
          <div>
            <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 23, fontWeight: 900}}>
              EMI SETTING
            </div>
            <div style={{fontSize: 43, fontWeight: 1000}}>LOCKED</div>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 74,
            right: 74,
            top: 270,
            height: 420,
            background: '#FFF3E4',
            border: '9px solid #25343F',
            boxShadow: '20px 20px 0 rgba(37,52,63,0.75)',
            transform: `rotate(-0.7deg) scale(${sp(70)})`,
            padding: '50px 65px',
          }}
        >
          <div
            style={{
              position: 'absolute',
              left: 55,
              right: 55,
              top: 190,
              height: 46,
              border: '7px solid #25343F',
              background: '#FFB56F',
            }}
          >
            <div
              style={{
                height: '100%',
                width: `${timelineWidth}%`,
                background: '#FFF449',
                borderRight: '7px solid #25343F',
              }}
            />
          </div>

          {Array.from({length: 26}).map((_, i) => {
            const left = 68 + (i / 25) * 1650;
            const active = i <= interpolate(timelineWidth, [34, 100], [8, 25]);

            return (
              <div
                key={i}
                style={{
                  position: 'absolute',
                  left,
                  top: 241,
                  width: i % 5 === 0 ? 7 : 4,
                  height: i % 5 === 0 ? 45 : 27,
                  background: '#25343F',
                  opacity: active ? 1 : 0.35,
                }}
              />
            );
          })}

          <div
            style={{
              position: 'absolute',
              left: 75,
              top: 58,
              width: 280,
              padding: '19px 25px',
              background: '#25343F',
              color: '#FFF449',
              border: '6px solid #25343F',
              fontSize: 61,
              fontWeight: 1000,
              transform: 'rotate(-2deg)',
            }}
          >
            20 YEARS
          </div>

          <div
            style={{
              position: 'absolute',
              left: 890,
              top: 52,
              width: 285,
              padding: '19px 24px',
              background: '#FF9B51',
              border: '6px solid #25343F',
              fontSize: 61,
              fontWeight: 1000,
              transform: `rotate(2deg) scale(${twentyThreeIn})`,
            }}
          >
            23 YEARS
          </div>

          <div
            style={{
              position: 'absolute',
              right: 55,
              bottom: 53,
              width: 310,
              padding: '19px 24px',
              background: '#D94B3D',
              color: '#FFF3E4',
              border: '7px solid #25343F',
              boxShadow: '12px 12px 0 #FFF449',
              fontSize: 64,
              fontWeight: 1000,
              transform: `rotate(-3deg) scale(${twentyFiveIn})`,
            }}
          >
            25 YEARS
          </div>

          <svg
            width="600"
            height="150"
            viewBox="0 0 600 150"
            style={{position: 'absolute', left: 395, bottom: 16}}
          >
            <path
              d="M10 75H540"
              stroke="#25343F"
              strokeWidth="15"
              strokeDasharray="25 18"
              strokeDashoffset={-frame * 2}
            />
            <path d="M510 30L575 75L510 120" fill="#FFF449" stroke="#25343F" strokeWidth="10" />
          </svg>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 100,
            bottom: 72,
            width: 590,
            height: 250,
            background: '#25343F',
            color: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '17px 17px 0 #FFF449',
            padding: '32px 38px',
            transform: `rotate(2deg) translateY(${interpolate(receiptIn, [0, 1], [320, 0])}px)`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, fontWeight: 900, color: '#FFF449'}}>
            AUTO-ADJUST RECEIPT
          </div>
          <div style={{display: 'flex', justifyContent: 'space-between', marginTop: 26, fontSize: 31, fontWeight: 900}}>
            <span>EMI</span>
            <span>SAME</span>
          </div>
          <div style={{display: 'flex', justifyContent: 'space-between', marginTop: 17, fontSize: 31, fontWeight: 900}}>
            <span>TENURE</span>
            <span style={{color: '#FFF449'}}>EXTENDED</span>
          </div>
          <div
            style={{
              position: 'absolute',
              top: stealthScan % 240,
              left: 0,
              right: 0,
              height: 5,
              background: '#FFF449',
              opacity: 0.5,
            }}
          />
        </div>

        <div
          style={{
            position: 'absolute',
            left: 780,
            bottom: 87,
            width: 445,
            height: 205,
            background: '#FFF3E4',
            border: '7px solid #25343F',
            padding: '28px 33px',
            transform: `rotate(-2deg) scale(${sp(600)})`,
          }}
        >
          <svg width="100%" height="75" viewBox="0 0 390 75">
            <path d="M8 39C55 4 92 70 139 35C186 0 221 70 270 35C319 0 345 62 382 24" fill="none" stroke="#D94B3D" strokeWidth="9" />
          </svg>
          <div style={{fontSize: 44, fontWeight: 1000}}>NOTICE नहीं किया</div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 88,
            bottom: 77,
            width: 510,
            height: 225,
            background: '#FFF449',
            border: '8px solid #25343F',
            boxShadow: '16px 16px 0 #25343F',
            padding: '30px 36px',
            transform: `rotate(3deg) scale(${sp(480)})`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, fontWeight: 900}}>
            BANK ACTION
          </div>
          <div style={{fontSize: 55, lineHeight: 1.05, fontWeight: 1000, marginTop: 14}}>
            EMI नहीं,
            <br />
            YEARS बढ़े
          </div>
        </div>

        <AbsoluteFill
          style={{
            pointerEvents: 'none',
            background: 'radial-gradient(circle, transparent 50%, rgba(37,52,63,0.46) 118%)',
          }}
        />

        <div
          style={{
            position: 'absolute',
            right: 45,
            bottom: 28,
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            fontWeight: 700,
            opacity: 0.35,
          }}
        >
          ProBuffet HQ
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Chapter01Scene05TenureExtensionTrap;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter01Scene06ExtraYearsExtraInterest',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const CalendarIcon: React.FC<{year: number}> = ({year}) => (
  <svg width="190" height="180" viewBox="0 0 190 180" fill="none">
    <rect x="13" y="24" width="164" height="143" rx="10" fill="#FFF3E4" stroke="#25343F" strokeWidth="9" />
    <path d="M13 62H177" stroke="#25343F" strokeWidth="9" />
    <path d="M49 7V40M141 7V40" stroke="#25343F" strokeWidth="12" strokeLinecap="round" />
    <text x="95" y="126" textAnchor="middle" fill="#25343F" fontSize="58" fontWeight="1000">
      {year}
    </text>
  </svg>
);

const Chapter01Scene06ExtraYearsExtraInterest: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (
    delay: number,
    config?: {damping?: number; mass?: number; stiffness?: number},
  ) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {damping: 12, mass: 0.44, stiffness: 120, ...config},
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  const yearValue = Math.round(
    interpolate(frame, [80, 310], [20, 30], {
      easing: Easing.out(Easing.cubic),
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }),
  );

  const extraInterest = Math.round(
    interpolate(frame, [250, 520], [0, 1275000], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }),
  );

  const loanCardIn = sp(560, {damping: 10, stiffness: 145});
  const noReturnIn = sp(430, {damping: 9, stiffness: 160});

  return (
    <AbsoluteFill style={{backgroundColor: '#FF9B51', overflow: 'hidden'}}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          opacity: exitOp,
          background:
            'linear-gradient(130deg, rgba(37,52,63,0.34), transparent 44%), radial-gradient(circle at 74% 18%, rgba(255,244,73,0.35), transparent 30%)',
          color: '#25343F',
          fontFamily: '"Noto Sans Devanagari", Inter, Poppins, sans-serif',
        }}
      >
        <AbsoluteFill
          style={{
            opacity: 0.16,
            backgroundImage:
              'repeating-radial-gradient(circle at 0 0, #25343F 0, #25343F 2px, transparent 3px, transparent 23px)',
            transform: `rotate(${Math.sin(frame * 0.004) * 2}deg) scale(1.1)`,
          }}
        />

        {Array.from({length: 18}).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: seededRandom(i + 4) * 1900,
              top: (seededRandom(i + 44) * 1180 - frame * (0.2 + seededRandom(i + 70) * 0.4)) % 1180,
              width: 7 + seededRandom(i + 90) * 12,
              height: 7 + seededRandom(i + 90) * 12,
              background: i % 3 === 0 ? '#FFF449' : '#25343F',
              opacity: 0.22,
            }}
          />
        ))}

        <div
          style={{
            position: 'absolute',
            left: 72,
            top: 55,
            background: '#25343F',
            color: '#FFF449',
            border: '6px solid #25343F',
            boxShadow: '13px 13px 0 #FFF3E4',
            padding: '15px 26px',
            fontSize: 33,
            fontWeight: 1000,
            transform: `rotate(-2deg) scale(${sp(0)})`,
          }}
        >
          INVISIBLE COST METER
        </div>

        <div
          style={{
            position: 'absolute',
            left: 78,
            top: 195,
            width: 550,
            height: 440,
            background: '#FFF3E4',
            border: '9px solid #25343F',
            boxShadow: '20px 20px 0 #25343F',
            padding: '35px 42px',
            transform: `rotate(-1deg) translateX(${interpolate(sp(35), [0, 1], [-620, 0])}px)`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 25, fontWeight: 900}}>
            LOAN TENURE
          </div>
          <div style={{display: 'flex', alignItems: 'center', gap: 34, marginTop: 28}}>
            <CalendarIcon year={yearValue} />
            <div>
              <div style={{fontSize: 120, lineHeight: 0.9, fontWeight: 1000}}>{yearValue}</div>
              <div style={{fontSize: 42, fontWeight: 1000}}>YEARS</div>
            </div>
          </div>

          <div style={{height: 39, border: '6px solid #25343F', marginTop: 30, background: '#FFB56F'}}>
            <div
              style={{
                width: `${interpolate(yearValue, [20, 30], [20, 100])}%`,
                height: '100%',
                background: yearValue >= 27 ? '#D94B3D' : '#FFF449',
                borderRight: '6px solid #25343F',
              }}
            />
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 690,
            top: 170,
            width: 530,
            height: 290,
            background: '#FFF449',
            border: '8px solid #25343F',
            boxShadow: '17px 17px 0 rgba(37,52,63,0.75)',
            padding: '32px 38px',
            transform: `rotate(2deg) scale(${sp(170)})`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, fontWeight: 900}}>
            EMI DISPLAY
          </div>
          <div style={{fontSize: 78, lineHeight: 1, fontWeight: 1000, marginTop: 25}}>
            ₹40K–₹45K
          </div>
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 17,
              marginTop: 27,
              fontSize: 35,
              fontWeight: 1000,
            }}
          >
            <svg width="70" height="50" viewBox="0 0 70 50">
              <path d="M5 25H65" stroke="#25343F" strokeWidth="10" />
            </svg>
            SAME
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 75,
            top: 158,
            width: 570,
            height: 405,
            background: '#D94B3D',
            color: '#FFF3E4',
            border: '9px solid #25343F',
            boxShadow: '20px 20px 0 #FFF449',
            padding: '35px 40px',
            transform: `rotate(-2deg) translateX(${interpolate(sp(255), [0, 1], [650, 0])}px)`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 25, fontWeight: 900, color: '#FFF449'}}>
            EXTRA INTEREST
          </div>
          <div style={{fontSize: 75, lineHeight: 1, fontWeight: 1000, marginTop: 27}}>
            ₹{extraInterest.toLocaleString('en-IN')}
          </div>

          <div
            style={{
              height: 95,
              display: 'flex',
              alignItems: 'flex-end',
              gap: 9,
              marginTop: 34,
            }}
          >
            {Array.from({length: 13}).map((_, i) => {
              const bar = interpolate(frame, [260 + i * 13, 430 + i * 13], [8, 80], {
                extrapolateLeft: 'clamp',
                extrapolateRight: 'clamp',
              });

              return (
                <div
                  key={i}
                  style={{
                    flex: 1,
                    height: bar,
                    background: i > 8 ? '#FFF449' : '#FFF3E4',
                    border: '3px solid #25343F',
                  }}
                />
              );
            })}
          </div>

          <div style={{fontSize: 30, fontWeight: 900, marginTop: 20}}>3–5 EXTRA YEARS</div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 120,
            top: 520,
            background: '#25343F',
            color: '#FFF449',
            border: '6px solid #25343F',
            padding: '15px 27px',
            fontSize: 31,
            fontWeight: 1000,
            transform: `rotate(7deg) scale(${noReturnIn})`,
          }}
        >
          NO REFUND
        </div>

        <div
          style={{
            position: 'absolute',
            left: 80,
            bottom: 74,
            width: 700,
            height: 265,
            background: '#25343F',
            color: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '17px 17px 0 #FFF449',
            padding: '34px 42px',
            transform: `rotate(1.5deg) scale(${sp(385)})`,
          }}
        >
          <div style={{fontSize: 40, fontWeight: 1000}}>RATES घट भी जाएँ</div>
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 25,
              marginTop: 25,
            }}
          >
            <svg width="130" height="95" viewBox="0 0 130 95">
              <path d="M10 16L62 76L116 16" fill="none" stroke="#FFF449" strokeWidth="14" />
            </svg>
            <div style={{fontSize: 56, lineHeight: 1, fontWeight: 1000, color: '#FFF449'}}>
              COST वापस
              <br />
              नहीं आता
            </div>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 87,
            bottom: 72,
            width: 860,
            height: 275,
            background: '#FFF3E4',
            border: '9px solid #25343F',
            boxShadow: '19px 19px 0 #25343F',
            padding: '30px 43px',
            transform: `rotate(-1deg) translateY(${interpolate(loanCardIn, [0, 1], [340, 0])}px)`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, fontWeight: 900}}>
            EXAMPLE LOAN FILE
          </div>

          <div style={{display: 'flex', alignItems: 'center', gap: 35, marginTop: 23}}>
            <svg width="190" height="150" viewBox="0 0 190 150">
              <path d="M15 71L95 10L175 71" fill="#FFF449" stroke="#25343F" strokeWidth="10" />
              <path d="M35 65V137H155V65L95 20L35 65Z" fill="#FF9B51" stroke="#25343F" strokeWidth="9" />
              <path d="M77 137V91H113V137" fill="#FFF3E4" stroke="#25343F" strokeWidth="8" />
            </svg>

            <div>
              <div style={{fontSize: 87, lineHeight: 0.95, fontWeight: 1000}}>₹50 LAKH</div>
              <div style={{fontSize: 38, fontWeight: 900}}>HOME LOAN</div>
            </div>
          </div>
        </div>

        <AbsoluteFill
          style={{
            pointerEvents: 'none',
            background: 'radial-gradient(circle, transparent 50%, rgba(37,52,63,0.47) 118%)',
          }}
        />

        <div
          style={{
            position: 'absolute',
            right: 45,
            bottom: 28,
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            fontWeight: 700,
            opacity: 0.35,
          }}
        >
          ProBuffet HQ
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Chapter01Scene06ExtraYearsExtraInterest;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter01Scene07LoanAccordion',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const HouseBlueprint: React.FC<{breath: number}> = ({breath}) => (
  <svg
    width="450"
    height="360"
    viewBox="0 0 450 360"
    fill="none"
    style={{transform: `scale(${breath})`}}
  >
    <path d="M42 170L225 28L408 170" stroke="#FFF449" strokeWidth="16" strokeLinejoin="round" />
    <path d="M83 146V327H367V146L225 39L83 146Z" fill="#FF9B51" stroke="#25343F" strokeWidth="12" />
    <path d="M184 327V218H267V327" fill="#FFF3E4" stroke="#25343F" strokeWidth="11" />
    <path d="M294 173H339V225H294V173Z" fill="#FFF449" stroke="#25343F" strokeWidth="9" />
    <path d="M110 183H155V225H110V183Z" fill="#FFF449" stroke="#25343F" strokeWidth="9" />
    <path d="M57 342H393" stroke="#25343F" strokeWidth="13" />
  </svg>
);

const Chapter01Scene07LoanAccordion: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (
    delay: number,
    config?: {damping?: number; mass?: number; stiffness?: number},
  ) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {damping: 12, mass: 0.44, stiffness: 120, ...config},
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  const expandedYears = interpolate(
    frame,
    [220, 390, 470, 620],
    [20, 25, 25, 23],
    {
      easing: Easing.inOut(Easing.cubic),
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    },
  );

  const accordionWidth = interpolate(expandedYears, [20, 25], [560, 1000]);
  const emiIn = sp(160, {damping: 9, stiffness: 155});
  const warningIn = sp(360, {damping: 8, stiffness: 165});
  const finalIn = sp(575, {damping: 10, stiffness: 150});

  return (
    <AbsoluteFill style={{backgroundColor: '#FF9B51', overflow: 'hidden'}}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          opacity: exitOp,
          background:
            'radial-gradient(circle at 25% 80%, rgba(255,244,73,0.28), transparent 34%), linear-gradient(150deg, rgba(37,52,63,0.34), transparent 48%, rgba(37,52,63,0.18))',
          color: '#25343F',
          fontFamily: '"Noto Sans Devanagari", Inter, Poppins, sans-serif',
        }}
      >
        <AbsoluteFill
          style={{
            opacity: 0.18,
            backgroundImage:
              'linear-gradient(rgba(37,52,63,0.35) 2px, transparent 2px), linear-gradient(90deg, rgba(37,52,63,0.35) 2px, transparent 2px)',
            backgroundSize: '55px 55px',
            transform: `translate(${(frame * 0.12) % 55}px, ${(frame * -0.08) % 55}px)`,
          }}
        />

        {Array.from({length: 15}).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: seededRandom(i + 12) * 1900,
              top: seededRandom(i + 52) * 1080,
              width: 12,
              height: 12,
              border: '4px solid #25343F',
              opacity: 0.2,
              transform: `rotate(${frame * 0.22 + i * 15}deg)`,
            }}
          />
        ))}

        <div
          style={{
            position: 'absolute',
            left: 72,
            top: 55,
            background: '#FFF449',
            border: '7px solid #25343F',
            boxShadow: '14px 14px 0 #25343F',
            padding: '15px 28px',
            fontSize: 34,
            fontWeight: 1000,
            transform: `rotate(-2deg) scale(${sp(0)})`,
          }}
        >
          ₹50 LAKH LOAN BLUEPRINT
        </div>

        <div
          style={{
            position: 'absolute',
            left: 76,
            top: 185,
            width: 575,
            height: 600,
            background: '#FFF3E4',
            border: '9px solid #25343F',
            boxShadow: '20px 20px 0 #25343F',
            transform: `rotate(-1deg) translateX(${interpolate(sp(35), [0, 1], [-650, 0])}px)`,
            padding: '35px 45px',
          }}
        >
          <HouseBlueprint breath={1 + Math.sin(frame * 0.045) * 0.008} />

          <div
            style={{
              display: 'grid',
              gridTemplateColumns: '1fr 1fr',
              gap: 13,
              marginTop: 10,
            }}
          >
            {[
              ['LOAN', '₹50L'],
              ['RATE', '8.4–8.5%'],
              ['ORIGINAL', '20 YEARS'],
              ['TYPE', 'FLOATING'],
            ].map(([label, value], i) => (
              <div
                key={label}
                style={{
                  border: '5px solid #25343F',
                  background: i === 1 ? '#FFF449' : '#FFB56F',
                  padding: '13px 15px',
                  transform: `translateY(${Math.sin(frame * 0.05 + i) * 3}px)`,
                }}
              >
                <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 17, fontWeight: 900}}>{label}</div>
                <div style={{fontSize: 28, fontWeight: 1000}}>{value}</div>
              </div>
            ))}
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 78,
            top: 150,
            width: 1110,
            height: 250,
            background: '#25343F',
            color: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '18px 18px 0 #FFF449',
            padding: '30px 42px',
            transform: `rotate(1deg) scale(${emiIn})`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 25, fontWeight: 900, color: '#FFF449'}}>
            MONTHLY EMI
          </div>
          <div style={{display: 'flex', alignItems: 'center', justifyContent: 'space-between'}}>
            <div style={{fontSize: 110, lineHeight: 1, fontWeight: 1000}}>₹43,200</div>
            <svg width="210" height="105" viewBox="0 0 210 105">
              <path d="M12 52H198" stroke="#FFF449" strokeWidth="16" />
              <path d="M45 19V86M165 19V86" stroke="#FFF3E4" strokeWidth="9" />
            </svg>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 740,
            top: 470,
            width: accordionWidth,
            height: 280,
            background: '#FFF3E4',
            border: '9px solid #25343F',
            boxShadow: '18px 18px 0 rgba(37,52,63,0.7)',
            overflow: 'hidden',
            transform: 'rotate(-1deg)',
          }}
        >
          <div
            style={{
              position: 'absolute',
              inset: 0,
              display: 'flex',
            }}
          >
            {Array.from({length: 25}).map((_, i) => (
              <div
                key={i}
                style={{
                  flex: 1,
                  minWidth: 22,
                  borderRight: '4px solid #25343F',
                  background: i < 20 ? '#FFF449' : '#D94B3D',
                  opacity: i + 1 <= expandedYears ? 1 : 0.16,
                  transform: `translateY(${Math.sin(frame * 0.05 + i * 0.4) * 5}px)`,
                }}
              />
            ))}
          </div>

          <div
            style={{
              position: 'absolute',
              left: 25,
              top: 24,
              background: '#25343F',
              color: '#FFF449',
              border: '5px solid #25343F',
              padding: '10px 19px',
              fontSize: 29,
              fontWeight: 1000,
            }}
          >
            TENURE ACCORDION
          </div>

          <div
            style={{
              position: 'absolute',
              right: 28,
              bottom: 28,
              background: '#FFF3E4',
              border: '6px solid #25343F',
              padding: '13px 23px',
              fontSize: 55,
              fontWeight: 1000,
            }}
          >
            {expandedYears.toFixed(1)} YEARS
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 87,
            top: 410,
            background: '#D94B3D',
            color: '#FFF3E4',
            border: '7px solid #25343F',
            boxShadow: '13px 13px 0 #FFF449',
            padding: '18px 27px',
            fontSize: 37,
            fontWeight: 1000,
            transform: `rotate(5deg) scale(${warningIn})`,
          }}
        >
          RATE UP → 25 YEARS
        </div>

        <div
          style={{
            position: 'absolute',
            right: 95,
            bottom: 78,
            width: 790,
            height: 220,
            background: '#FFF449',
            border: '8px solid #25343F',
            boxShadow: '17px 17px 0 #25343F',
            padding: '29px 38px',
            transform: `rotate(2deg) scale(${finalIn})`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, fontWeight: 900}}>
            RATE DOWN
          </div>
          <div style={{fontSize: 61, lineHeight: 1.05, fontWeight: 1000, marginTop: 13}}>
            20 नहीं लौटा
            <br />
            अभी भी 22–23 YEARS
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 655,
            bottom: 80,
            width: 220,
            background: '#25343F',
            color: '#FFF449',
            border: '6px solid #25343F',
            padding: '18px',
            textAlign: 'center',
            fontSize: 30,
            fontWeight: 1000,
            transform: `rotate(-7deg) scale(${sp(625)})`,
          }}
        >
          NOT
          <br />
          ORIGINAL
        </div>

        <AbsoluteFill
          style={{
            pointerEvents: 'none',
            background: 'radial-gradient(circle, transparent 50%, rgba(37,52,63,0.46) 118%)',
          }}
        />

        <div
          style={{
            position: 'absolute',
            right: 45,
            bottom: 28,
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            fontWeight: 700,
            opacity: 0.35,
          }}
        >
          ProBuffet HQ
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Chapter01Scene07LoanAccordion;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter01Scene08OnePercentDifference',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const CoinStack: React.FC<{count: number; color: string; frame: number}> = ({
  count,
  color,
  frame,
}) => (
  <div style={{display: 'flex', alignItems: 'flex-end', gap: 7, height: 220}}>
    {Array.from({length: count}).map((_, i) => (
      <div
        key={i}
        style={{
          width: 47,
          height: 47 + i * 8,
          background: color,
          border: '5px solid #25343F',
          transform: `translateY(${Math.sin(frame * 0.06 + i) * 3}px)`,
        }}
      />
    ))}
  </div>
);

const Chapter01Scene08OnePercentDifference: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (
    delay: number,
    config?: {damping?: number; mass?: number; stiffness?: number},
  ) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {damping: 12, mass: 0.44, stiffness: 120, ...config},
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  const barOne = interpolate(frame, [75, 290], [0, 420], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const barTwo = interpolate(frame, [180, 400], [0, 515], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const differenceIn = sp(390, {damping: 8, stiffness: 170});
  const bonusIn = sp(540, {damping: 10, stiffness: 145});
  const disturbIn = sp(650, {damping: 9, stiffness: 155});

  return (
    <AbsoluteFill style={{backgroundColor: '#FF9B51', overflow: 'hidden'}}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          opacity: exitOp,
          background:
            'linear-gradient(118deg, rgba(37,52,63,0.37), transparent 42%), radial-gradient(circle at 84% 22%, rgba(255,244,73,0.34), transparent 28%)',
          color: '#25343F',
          fontFamily: '"Noto Sans Devanagari", Inter, Poppins, sans-serif',
        }}
      >
        <AbsoluteFill
          style={{
            opacity: 0.17,
            backgroundImage:
              'repeating-linear-gradient(90deg, transparent 0, transparent 69px, rgba(37,52,63,0.38) 70px, transparent 73px)',
            transform: `translateX(${(frame * 0.22) % 73}px)`,
          }}
        />

        {Array.from({length: 17}).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: seededRandom(i + 3) * 1900,
              top: seededRandom(i + 33) * 1080,
              width: 14,
              height: 14,
              borderRadius: i % 2 ? '50%' : 0,
              background: i % 3 === 0 ? '#FFF449' : '#25343F',
              opacity: 0.2,
              transform: `translateY(${Math.sin(frame * 0.04 + i) * 15}px)`,
            }}
          />
        ))}

        <div
          style={{
            position: 'absolute',
            left: 72,
            top: 55,
            background: '#FFF3E4',
            border: '7px solid #25343F',
            boxShadow: '14px 14px 0 #25343F',
            padding: '15px 27px',
            fontSize: 34,
            fontWeight: 1000,
            transform: `rotate(-2deg) scale(${sp(0)})`,
          }}
        >
          1% RATE GAP — REAL COST
        </div>

        <div
          style={{
            position: 'absolute',
            left: 76,
            top: 175,
            width: 1165,
            height: 705,
            background: '#FFF3E4',
            border: '9px solid #25343F',
            boxShadow: '20px 20px 0 #25343F',
            transform: `rotate(-1deg) scale(${sp(35)})`,
            overflow: 'hidden',
          }}
        >
          <div
            style={{
              position: 'absolute',
              left: 55,
              right: 55,
              bottom: 85,
              height: 10,
              background: '#25343F',
            }}
          />

          {[0, 1, 2, 3, 4].map((i) => (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: 55,
                right: 55,
                bottom: 85 + i * 120,
                borderTop: '3px dashed rgba(37,52,63,0.35)',
              }}
            />
          ))}

          <div
            style={{
              position: 'absolute',
              left: 125,
              bottom: 95,
              width: 340,
              height: barOne,
              background: '#FFF449',
              border: '8px solid #25343F',
              display: 'flex',
              alignItems: 'flex-start',
              justifyContent: 'center',
              paddingTop: 25,
            }}
          >
            <div style={{fontSize: 45, fontWeight: 1000}}>7.25%</div>
          </div>

          <div
            style={{
              position: 'absolute',
              right: 125,
              bottom: 95,
              width: 340,
              height: barTwo,
              background: '#D94B3D',
              color: '#FFF3E4',
              border: '8px solid #25343F',
              display: 'flex',
              alignItems: 'flex-start',
              justifyContent: 'center',
              paddingTop: 25,
            }}
          >
            <div style={{fontSize: 45, fontWeight: 1000}}>8.25%</div>
          </div>

          <div
            style={{
              position: 'absolute',
              left: 108,
              bottom: 540,
              width: 375,
              background: '#25343F',
              color: '#FFF449',
              border: '6px solid #25343F',
              padding: '16px 22px',
              textAlign: 'center',
              fontSize: 49,
              fontWeight: 1000,
              transform: `scale(${sp(215)})`,
            }}
          >
            ₹94.85 LAKH
          </div>

          <div
            style={{
              position: 'absolute',
              right: 105,
              top: 38,
              width: 385,
              background: '#25343F',
              color: '#FFF449',
              border: '6px solid #25343F',
              padding: '16px 22px',
              textAlign: 'center',
              fontSize: 49,
              fontWeight: 1000,
              transform: `scale(${sp(330)})`,
            }}
          >
            ₹1.02 CRORE
          </div>

          <svg
            width="250"
            height="360"
            viewBox="0 0 250 360"
            style={{position: 'absolute', left: 460, top: 190}}
          >
            <path
              d="M60 310C190 270 45 175 180 105"
              fill="none"
              stroke="#25343F"
              strokeWidth="13"
              strokeDasharray="20 15"
              strokeDashoffset={-frame * 2}
            />
            <path d="M147 81L203 91L181 145" fill="#FFF449" stroke="#25343F" strokeWidth="9" />
          </svg>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 75,
            top: 160,
            width: 540,
            height: 280,
            background: '#FFF449',
            border: '9px solid #25343F',
            boxShadow: '19px 19px 0 #25343F',
            padding: '30px 38px',
            transform: `rotate(3deg) scale(${differenceIn})`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, fontWeight: 900}}>
            JUST 1% DIFFERENCE
          </div>
          <div style={{fontSize: 91, lineHeight: 1, fontWeight: 1000, marginTop: 20}}>₹7.4L</div>
          <div style={{fontSize: 32, fontWeight: 1000, marginTop: 10}}>EXTRA REPAYMENT</div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 73,
            top: 500,
            width: 545,
            height: 290,
            background: '#25343F',
            color: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '18px 18px 0 #FFF449',
            padding: '26px 36px',
            transform: `rotate(-2deg) scale(${bonusIn})`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, color: '#FFF449', fontWeight: 900}}>
            BANK BONUS INCOME
          </div>
          <CoinStack count={8} color="#FFF449" frame={frame} />
        </div>

        <div
          style={{
            position: 'absolute',
            right: 95,
            bottom: 82,
            width: 510,
            background: '#D94B3D',
            color: '#FFF3E4',
            border: '7px solid #25343F',
            padding: '18px 28px',
            fontSize: 35,
            fontWeight: 1000,
            transform: `rotate(4deg) scale(${disturbIn})`,
          }}
        >
          THIS IS THE DISTURBING PART
        </div>

        <AbsoluteFill
          style={{
            pointerEvents: 'none',
            background: 'radial-gradient(circle, transparent 50%, rgba(37,52,63,0.47) 118%)',
          }}
        />

        <div
          style={{
            position: 'absolute',
            right: 45,
            bottom: 28,
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            fontWeight: 700,
            opacity: 0.35,
          }}
        >
          ProBuffet HQ
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Chapter01Scene08OnePercentDifference;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter01Scene09AmortizationReality',
  durationInSeconds: 26.111,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const DonutChart: React.FC<{
  percent: number;
  label: string;
  color: string;
  frame: number;
}> = ({percent, label, color, frame}) => {
  const radius = 125;
  const circumference = Math.PI * 2 * radius;
  const offset = circumference * (1 - percent / 100);

  return (
    <div style={{position: 'relative', width: 320, height: 320}}>
      <svg width="320" height="320" viewBox="0 0 320 320">
        <circle cx="160" cy="160" r={radius} fill="none" stroke="#FFB56F" strokeWidth="38" />
        <circle
          cx="160"
          cy="160"
          r={radius}
          fill="none"
          stroke={color}
          strokeWidth="38"
          strokeDasharray={circumference}
          strokeDashoffset={offset}
          strokeLinecap="square"
          transform="rotate(-90 160 160)"
          style={{filter: 'drop-shadow(7px 7px 0 rgba(37,52,63,0.25))'}}
        />
      </svg>
      <div
        style={{
          position: 'absolute',
          inset: 0,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          transform: `scale(${1 + Math.sin(frame * 0.045) * 0.008})`,
        }}
      >
        <div style={{fontSize: 78, lineHeight: 1, fontWeight: 1000}}>{percent}%</div>
        <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 22, fontWeight: 900}}>{label}</div>
      </div>
    </div>
  );
};

const Chapter01Scene09AmortizationReality: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (
    delay: number,
    config?: {damping?: number; mass?: number; stiffness?: number},
  ) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {damping: 12, mass: 0.44, stiffness: 120, ...config},
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  const paidYears = Math.floor(
    interpolate(frame, [55, 300], [1, 10], {
      easing: Easing.out(Easing.cubic),
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }),
  );

  const principalProgress = interpolate(frame, [160, 390], [0, 18], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const yearOneIn = sp(375, {damping: 9, stiffness: 155});
  const yearEightIn = sp(515, {damping: 10, stiffness: 145});
  const salaryIn = sp(620, {damping: 11, stiffness: 130});

  return (
    <AbsoluteFill style={{backgroundColor: '#FF9B51', overflow: 'hidden'}}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          opacity: exitOp,
          background:
            'radial-gradient(circle at 15% 14%, rgba(255,244,73,0.32), transparent 30%), linear-gradient(145deg, rgba(37,52,63,0.28), transparent 45%, rgba(37,52,63,0.27))',
          color: '#25343F',
          fontFamily: '"Noto Sans Devanagari", Inter, Poppins, sans-serif',
        }}
      >
        <AbsoluteFill
          style={{
            opacity: 0.19,
            backgroundImage:
              'repeating-linear-gradient(135deg, rgba(37,52,63,0.5) 0, rgba(37,52,63,0.5) 2px, transparent 2px, transparent 22px)',
            transform: `translate(${(frame * 0.16) % 22}px, ${(-frame * 0.1) % 22}px)`,
          }}
        />

        {Array.from({length: 18}).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: seededRandom(i + 13) * 1900,
              top: seededRandom(i + 63) * 1080,
              width: 8 + seededRandom(i + 93) * 15,
              height: 8 + seededRandom(i + 93) * 15,
              background: i % 2 ? '#FFF449' : '#25343F',
              opacity: 0.18,
              transform: `rotate(${frame * 0.2 + i * 27}deg)`,
            }}
          />
        ))}

        <div
          style={{
            position: 'absolute',
            left: 72,
            top: 54,
            background: '#25343F',
            color: '#FFF449',
            border: '7px solid #25343F',
            boxShadow: '14px 14px 0 #FFF3E4',
            padding: '15px 27px',
            fontSize: 34,
            fontWeight: 1000,
            transform: `rotate(-2deg) scale(${sp(0)})`,
          }}
        >
          10 YEARS OF EMI
        </div>

        <div
          style={{
            position: 'absolute',
            left: 75,
            top: 185,
            width: 635,
            height: 655,
            background: '#FFF3E4',
            border: '9px solid #25343F',
            boxShadow: '20px 20px 0 #25343F',
            padding: '35px 42px',
            transform: `rotate(-1deg) translateX(${interpolate(sp(35), [0, 1], [-700, 0])}px)`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, fontWeight: 900}}>
            PAYMENT HISTORY
          </div>

          <div
            style={{
              display: 'grid',
              gridTemplateColumns: 'repeat(5, 1fr)',
              gap: 15,
              marginTop: 30,
            }}
          >
            {Array.from({length: 10}).map((_, i) => (
              <div
                key={i}
                style={{
                  height: 110,
                  background: i + 1 <= paidYears ? '#FFF449' : '#FFB56F',
                  border: '6px solid #25343F',
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'center',
                  justifyContent: 'center',
                  transform: `scale(${i + 1 <= paidYears ? 1 : 0.88}) translateY(${Math.sin(frame * 0.05 + i) * 4}px)`,
                  opacity: i + 1 <= paidYears ? 1 : 0.35,
                }}
              >
                <div style={{fontSize: 36, fontWeight: 1000}}>{i + 1}</div>
                <div style={{fontSize: 16, fontWeight: 900}}>YEAR</div>
              </div>
            ))}
          </div>

          <div style={{fontSize: 36, fontWeight: 1000, marginTop: 32}}>PRINCIPAL REDUCED</div>
          <div
            style={{
              height: 52,
              border: '7px solid #25343F',
              background: '#FFB56F',
              marginTop: 16,
            }}
          >
            <div
              style={{
                width: `${principalProgress}%`,
                height: '100%',
                background: '#D94B3D',
                borderRight: '7px solid #25343F',
              }}
            />
          </div>
          <div style={{fontSize: 64, fontWeight: 1000, color: '#D94B3D', marginTop: 13}}>
            {principalProgress.toFixed(0)}%
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 770,
            top: 160,
            width: 1045,
            height: 250,
            background: '#FFF449',
            border: '8px solid #25343F',
            boxShadow: '18px 18px 0 #25343F',
            padding: '28px 40px',
            transform: `rotate(1deg) scale(${sp(190)})`,
          }}
        >
          <div style={{display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 18}}>
            {[
              ['LOAN', '₹60 LAKH'],
              ['TENURE', '20 YEARS'],
              ['RATE', '8.75%'],
            ].map(([label, value], i) => (
              <div
                key={label}
                style={{
                  background: i === 1 ? '#FFF3E4' : '#FF9B51',
                  border: '6px solid #25343F',
                  padding: '22px',
                  transform: `rotate(${i - 1}deg)`,
                }}
              >
                <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 20, fontWeight: 900}}>{label}</div>
                <div style={{fontSize: 42, fontWeight: 1000, marginTop: 10}}>{value}</div>
              </div>
            ))}
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 765,
            bottom: 70,
            width: 480,
            height: 480,
            background: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '17px 17px 0 #25343F',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            transform: `rotate(-2deg) scale(${yearOneIn})`,
          }}
        >
          <DonutChart percent={92} label="YEAR 1 INTEREST" color="#D94B3D" frame={frame} />
        </div>

        <div
          style={{
            position: 'absolute',
            right: 75,
            bottom: 70,
            width: 480,
            height: 480,
            background: '#25343F',
            color: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '17px 17px 0 #FFF449',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            transform: `rotate(2deg) scale(${yearEightIn})`,
          }}
        >
          <DonutChart percent={75} label="YEAR 8 INTEREST" color="#FFF449" frame={frame} />
        </div>

        <div
          style={{
            position: 'absolute',
            right: 102,
            top: 430,
            background: '#D94B3D',
            color: '#FFF3E4',
            border: '6px solid #25343F',
            padding: '14px 24px',
            fontSize: 29,
            fontWeight: 1000,
            transform: `rotate(6deg) scale(${salaryIn})`,
          }}
        >
          SALARY → INTEREST
        </div>

        <AbsoluteFill
          style={{
            pointerEvents: 'none',
            background: 'radial-gradient(circle, transparent 50%, rgba(37,52,63,0.47) 118%)',
          }}
        />

        <div
          style={{
            position: 'absolute',
            right: 45,
            bottom: 28,
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            fontWeight: 700,
            opacity: 0.35,
          }}
        >
          ProBuffet HQ
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Chapter01Scene09AmortizationReality;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter01Scene10OwnershipVsInterest',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const OwnershipHouse: React.FC<{fillPercent: number}> = ({fillPercent}) => (
  <svg width="470" height="420" viewBox="0 0 470 420" fill="none">
    <defs>
      <clipPath id="house-clip">
        <path d="M55 185L235 35L415 185V390H55V185Z" />
      </clipPath>
    </defs>
    <path d="M35 190L235 20L435 190" stroke="#25343F" strokeWidth="17" strokeLinejoin="round" />
    <path d="M55 185L235 35L415 185V390H55V185Z" fill="#FFB56F" stroke="#25343F" strokeWidth="13" />
    <rect
      x="55"
      y={390 - (355 * fillPercent) / 100}
      width="360"
      height={(355 * fillPercent) / 100}
      fill="#FFF449"
      clipPath="url(#house-clip)"
    />
    <path d="M55 185L235 35L415 185V390H55V185Z" stroke="#25343F" strokeWidth="13" />
    <path d="M190 390V270H280V390" fill="#FFF3E4" stroke="#25343F" strokeWidth="12" />
    <path d="M92 225H151V282H92V225Z" fill="#FFF3E4" stroke="#25343F" strokeWidth="10" />
    <path d="M320 225H379V282H320V225Z" fill="#FFF3E4" stroke="#25343F" strokeWidth="10" />
  </svg>
);

const Chapter01Scene10OwnershipVsInterest: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (
    delay: number,
    config?: {damping?: number; mass?: number; stiffness?: number},
  ) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {damping: 12, mass: 0.44, stiffness: 120, ...config},
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'},
  );

  const ownership = interpolate(frame, [120, 360], [0, 18], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const interestShare = interpolate(frame, [75, 350], [0, 75], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cashPaid = interpolate(frame, [160, 430], [0, 50], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const helplessIn = sp(440, {damping: 8, stiffness: 165});
  const scheduleIn = sp(545, {damping: 10, stiffness: 145});
  const conclusionIn = sp(665, {damping: 9, stiffness: 155});

  return (
    <AbsoluteFill style={{backgroundColor: '#FF9B51', overflow: 'hidden'}}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill
        style={{
          opacity: exitOp,
          background:
            'linear-gradient(135deg, rgba(37,52,63,0.38), transparent 44%), radial-gradient(circle at 78% 18%, rgba(255,244,73,0.32), transparent 29%)',
          color: '#25343F',
          fontFamily: '"Noto Sans Devanagari", Inter, Poppins, sans-serif',
        }}
      >
        <AbsoluteFill
          style={{
            opacity: 0.19,
            backgroundImage:
              'repeating-linear-gradient(35deg, transparent 0, transparent 35px, rgba(37,52,63,0.36) 36px, transparent 39px)',
            transform: `translateX(${(frame * 0.17) % 39}px)`,
          }}
        />

        {Array.from({length: 20}).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: seededRandom(i + 5) * 1900,
              top: (seededRandom(i + 45) * 1200 + frame * (0.18 + seededRandom(i + 75) * 0.3)) % 1200 - 70,
              width: 8 + seededRandom(i + 95) * 14,
              height: 8 + seededRandom(i + 95) * 14,
              background: i % 3 === 0 ? '#FFF449' : '#25343F',
              opacity: 0.2,
              transform: `rotate(${frame * 0.22 + i * 17}deg)`,
            }}
          />
        ))}

        <div
          style={{
            position: 'absolute',
            left: 72,
            top: 54,
            background: '#D94B3D',
            color: '#FFF3E4',
            border: '7px solid #25343F',
            boxShadow: '14px 14px 0 #FFF449',
            padding: '15px 28px',
            fontSize: 34,
            fontWeight: 1000,
            transform: `rotate(-2deg) scale(${sp(0)})`,
          }}
        >
          OWNERSHIP VS INTEREST
        </div>

        <div
          style={{
            position: 'absolute',
            left: 74,
            top: 180,
            width: 650,
            height: 690,
            background: '#FFF3E4',
            border: '9px solid #25343F',
            boxShadow: '20px 20px 0 #25343F',
            padding: '34px 45px',
            transform: `rotate(-1deg) translateX(${interpolate(sp(35), [0, 1], [-730, 0])}px)`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, fontWeight: 900}}>
            YOUR OWNERSHIP
          </div>

          <div style={{display: 'flex', justifyContent: 'center', marginTop: 12}}>
            <OwnershipHouse fillPercent={ownership} />
          </div>

          <div
            style={{
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'flex-end',
              marginTop: 8,
            }}
          >
            <div>
              <div style={{fontSize: 25, fontWeight: 900}}>PRINCIPAL REDUCED</div>
              <div style={{fontSize: 73, lineHeight: 1, fontWeight: 1000}}>₹8–10L</div>
            </div>
            <div
              style={{
                background: '#FFF449',
                border: '6px solid #25343F',
                padding: '13px 22px',
                fontSize: 42,
                fontWeight: 1000,
              }}
            >
              {ownership.toFixed(0)}%
            </div>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 780,
            top: 150,
            width: 970,
            height: 300,
            background: '#25343F',
            color: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '18px 18px 0 #FFF449',
            padding: '32px 42px',
            transform: `rotate(1deg) scale(${sp(180)})`,
          }}
        >
          <div style={{display: 'flex', justifyContent: 'space-between', alignItems: 'flex-end'}}>
            <div>
              <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 24, fontWeight: 900, color: '#FFF449'}}>
                8-YEAR PAYMENT FLOW
              </div>
              <div style={{fontSize: 75, lineHeight: 1, fontWeight: 1000, marginTop: 20}}>
                {interestShare.toFixed(0)}%
              </div>
              <div style={{fontSize: 35, fontWeight: 900}}>BANK INTEREST</div>
            </div>

            <svg width="370" height="165" viewBox="0 0 370 165">
              <path
                d="M15 85H300"
                stroke="#FFF449"
                strokeWidth="34"
                strokeDasharray="24 16"
                strokeDashoffset={-frame * 2.5}
              />
              <path d="M285 25L355 85L285 145" fill="#D94B3D" stroke="#FFF3E4" strokeWidth="10" />
            </svg>
          </div>

          <div style={{height: 35, border: '5px solid #FFF3E4', marginTop: 22, background: '#FF9B51'}}>
            <div
              style={{
                width: `${interestShare}%`,
                height: '100%',
                background: '#FFF449',
                borderRight: '5px solid #FFF3E4',
              }}
            />
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 805,
            top: 490,
            width: 500,
            height: 245,
            background: '#FFF449',
            border: '8px solid #25343F',
            boxShadow: '16px 16px 0 #25343F',
            padding: '28px 37px',
            transform: `rotate(-2deg) scale(${sp(300)})`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 23, fontWeight: 900}}>
            TOTAL PAID
          </div>
          <div style={{fontSize: 83, lineHeight: 1, fontWeight: 1000, marginTop: 18}}>
            ₹{cashPaid.toFixed(0)}L+
          </div>
          <div style={{fontSize: 29, fontWeight: 1000, marginTop: 15}}>AFTER 10 YEARS</div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 76,
            top: 490,
            width: 445,
            height: 250,
            background: '#D94B3D',
            color: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '16px 16px 0 #FFF449',
            padding: '28px 34px',
            transform: `rotate(3deg) scale(${helplessIn})`,
          }}
        >
          <svg width="100%" height="82" viewBox="0 0 370 82">
            <path
              d="M5 40H70L92 12L126 70L160 22L197 63L237 17L272 64L310 38H365"
              fill="none"
              stroke="#FFF449"
              strokeWidth="9"
            />
          </svg>
          <div style={{fontSize: 47, lineHeight: 1.05, fontWeight: 1000}}>
            HELPLESSNESS
            <br />
            IS RATIONAL
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 810,
            bottom: 75,
            width: 750,
            height: 225,
            background: '#FFF3E4',
            border: '8px solid #25343F',
            boxShadow: '17px 17px 0 #25343F',
            padding: '27px 35px',
            transform: `rotate(1deg) scale(${scheduleIn})`,
          }}
        >
          <div style={{fontFamily: '"JetBrains Mono", monospace', fontSize: 23, fontWeight: 900}}>
            AMORTIZATION ORDER
          </div>

          <div style={{display: 'flex', gap: 9, alignItems: 'flex-end', height: 105, marginTop: 18}}>
            {Array.from({length: 14}).map((_, i) => (
              <div
                key={i}
                style={{
                  flex: 1,
                  height: 95 - i * 4,
                  background: i < 9 ? '#D94B3D' : '#FFF449',
                  border: '3px solid #25343F',
                  transform: `translateY(${Math.sin(frame * 0.06 + i) * 3}px)`,
                }}
              />
            ))}
          </div>

          <div style={{display: 'flex', justifyContent: 'space-between', fontSize: 22, fontWeight: 1000}}>
            <span>INTEREST FIRST</span>
            <span>PRINCIPAL LATER</span>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 75,
            bottom: 92,
            width: 295,
            background: '#25343F',
            color: '#FFF449',
            border: '7px solid #25343F',
            padding: '21px 25px',
            fontSize: 35,
            lineHeight: 1.05,
            fontWeight: 1000,
            textAlign: 'center',
            transform: `rotate(-5deg) scale(${conclusionIn})`,
          }}
        >
          BANK GETS
          <br />
          PAID FIRST
        </div>

        <AbsoluteFill
          style={{
            pointerEvents: 'none',
            background: 'radial-gradient(circle, transparent 50%, rgba(37,52,63,0.49) 118%)',
          }}
        />

        <div
          style={{
            position: 'absolute',
            right: 45,
            bottom: 28,
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            fontWeight: 700,
            opacity: 0.35,
          }}
        >
          ProBuffet HQ
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Chapter01Scene10OwnershipVsInterest;
```

