```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_WhatBothersMe',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_WhatBothersMe: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.06], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Scanline reveal
  const lineReveal = interpolate(frame, [0, 25], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Text stagger
  const word1Op = interpolate(frame, [10, 22], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const word2Op = interpolate(frame, [22, 34], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const word3Op = interpolate(frame, [34, 46], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const word1Y = sp(10) * 0;
  const questionScale = sp(30, { damping: 8, stiffness: 80 });

  // Particles
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 3 + 1) * 1920,
    y: seededRandom(i * 3 + 2) * 1080,
    size: seededRandom(i * 3 + 3) * 4 + 1.5,
    speed: seededRandom(i * 3 + 4) * 0.4 + 0.1,
    op: seededRandom(i * 3 + 5) * 0.25 + 0.08,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 20% 80%, #2a2000 0%, transparent 55%), radial-gradient(ellipse at 80% 20%, #1a1a2e 0%, transparent 50%), #1C1C1C',
        transform: `scale(${camScale})`,
      }} />

      {/* Shape layer 1 — large drifting circle */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <circle
            cx={960 + Math.sin(frame * 0.012) * 30}
            cy={540 + Math.cos(frame * 0.009) * 20}
            r={520}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
            opacity={0.04}
            transform={`rotate(${frame * 0.08}, 960, 540)`}
          />
          <circle
            cx={300 + Math.sin(frame * 0.015) * 15}
            cy={200 + Math.cos(frame * 0.011) * 20}
            r={280}
            fill="#e9d700"
            opacity={0.03}
          />
        </svg>
      </AbsoluteFill>

      {/* Shape layer 2 — rotating rectangle */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <rect
            x={1400}
            y={600}
            width={400}
            height={400}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="1"
            opacity={0.05}
            transform={`rotate(${45 + frame * 0.06}, 1600, 800)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — diagonal grid lines */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 14 }, (_, i) => (
            <line
              key={i}
              x1={i * 150 - 100 + Math.sin(frame * 0.008 + i) * 8}
              y1={0}
              x2={i * 150 - 100 + 300 + Math.sin(frame * 0.008 + i) * 8}
              y2={1080}
              stroke="#e9d700"
              strokeWidth="0.5"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x}
              cy={(p.y - frame * p.speed * 0.8) % 1080}
              r={p.size}
              fill="#e9d700"
              opacity={p.op}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Horizontal scan line reveal */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <div style={{
          position: 'absolute',
          left: 0, top: '50%',
          width: `${lineReveal * 100}%`,
          height: '1px',
          backgroundColor: '#e9d700',
          opacity: 0.6,
          transform: 'translateY(-540px)',
          boxShadow: '0 0 12px #e9d700',
        }} />
        <div style={{
          position: 'absolute',
          left: 0, top: '50%',
          width: `${lineReveal * 60}%`,
          height: '1px',
          backgroundColor: '#e9d700',
          opacity: 0.3,
          transform: 'translateY(100px)',
        }} />
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
        gap: 0,
      }}>

        {/* Top label */}
        <div style={{
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: '22px',
          fontWeight: 700,
          color: '#e9d700',
          letterSpacing: '6px',
          textTransform: 'uppercase',
          opacity: word1Op,
          marginBottom: '32px',
        }}>
          Kux legt an — Finanzrealität
        </div>

        {/* WEISST DU */}
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '160px',
          fontWeight: 400,
          color: '#F5F5F5',
          lineHeight: 0.9,
          letterSpacing: '-2px',
          opacity: word2Op,
          transform: `translateY(${(1 - sp(22)) * 40}px)`,
        }}>
          WEISST DU,
        </div>

        {/* WAS MICH */}
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '160px',
          fontWeight: 400,
          color: '#F5F5F5',
          lineHeight: 0.9,
          letterSpacing: '-2px',
          opacity: word2Op,
          transform: `translateY(${(1 - sp(28)) * 40}px)`,
        }}>
          WAS MICH
        </div>

        {/* WIRKLICH — highlight */}
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '200px',
          fontWeight: 400,
          color: '#e9d700',
          lineHeight: 0.9,
          letterSpacing: '-3px',
          opacity: word3Op,
          transform: `translateY(${(1 - sp(34)) * 40}px) scale(${0.9 + questionScale * 0.1})`,
          textShadow: '0 0 60px rgba(233,215,0,0.3)',
        }}>
          WIRKLICH
        </div>

        {/* BESCHÄFTIGT? */}
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '160px',
          fontWeight: 400,
          color: '#F5F5F5',
          lineHeight: 0.9,
          letterSpacing: '-2px',
          opacity: word3Op,
          transform: `translateY(${(1 - sp(40)) * 40}px)`,
        }}>
          BESCHÄFTIGT?
        </div>

      </AbsoluteFill>

      {/* Stick figure */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg
          width="120" height="200"
          style={{
            position: 'absolute',
            bottom: 120,
            left: 120,
            opacity: sp(50),
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          {/* Golden glow */}
          <circle cx="60" cy="30" r="35" fill="#e9d700" opacity="0.08" />
          {/* Head */}
          <circle cx="60" cy="30" r="22"
            fill="none" stroke="#F5F5F5" strokeWidth="2.5"
            transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 30)`}
          />
          {/* Golden glasses */}
          <rect x="44" y="25" width="13" height="9" rx="4"
            fill="none" stroke="#e9d700" strokeWidth="2"
            transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 30)`}
          />
          <rect x="61" y="25" width="13" height="9" rx="4"
            fill="none" stroke="#e9d700" strokeWidth="2"
            transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 30)`}
          />
          <line x1="57" y1="29" x2="61" y2="29" stroke="#e9d700" strokeWidth="1.5"
            transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 30)`}
          />
          {/* Body */}
          <line x1="60" y1="52" x2="60" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Arms — thinking pose */}
          <line x1="60" y1="75" x2="30" y2="55" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="30" y1="55" x2="25" y2="48" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="60" y1="75" x2="90" y2="60" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Legs */}
          <line x1="60" y1="130" x2="40" y2="175" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="130" x2="80" y2="175" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Question mark thought bubble */}
          <text x="95" y="45" fontFamily="Anton" fontSize="28" fill="#e9d700" opacity={sp(55)}>?</text>
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: '22px',
        fontWeight: 600, color: '#9E9E9E',
        opacity: interpolate(frame, [0, 20], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene1_WhatBothersMe;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_FakePromises',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_FakePromises: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan left to right
  const camX = interpolate(frame, [0, durationInFrames], [0, -30], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const strikeWidth = interpolate(frame, [55, 80], [0, 100], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 4 + 1) * 1920,
    y: seededRandom(i * 4 + 2) * 1080,
    size: seededRandom(i * 4 + 3) * 3 + 1,
    op: seededRandom(i * 4 + 4) * 0.2 + 0.05,
    speed: seededRandom(i * 4 + 5) * 0.5 + 0.15,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 75% 25%, #200a0a 0%, transparent 50%), radial-gradient(ellipse at 25% 75%, #0d1a0d 0%, transparent 50%), #1C1C1C',
        transform: `translateX(${camX}px)`,
      }} />

      {/* Shape layer — large triangle */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          <polygon
            points={`960,${80 + Math.sin(frame * 0.01) * 10} ${1680 + Math.cos(frame * 0.008) * 10},${980} ${240 + Math.sin(frame * 0.012) * 10},${980}`}
            fill="none"
            stroke="#ff0000"
            strokeWidth="1"
            opacity={0.05}
          />
          <circle
            cx={1600} cy={200}
            r={350 + Math.sin(frame * 0.015) * 15}
            fill="#ff0000"
            opacity={0.025}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — horizontal scan lines */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.035 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 20 }, (_, i) => (
            <line
              key={i}
              x1={0} y1={i * 58 + (frame * 0.5) % 58}
              x2={1920} y2={i * 58 + (frame * 0.5) % 58}
              stroke="#F5F5F5"
              strokeWidth="0.5"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i}
              cx={p.x}
              cy={(p.y + frame * p.speed) % 1080}
              r={p.size}
              fill="#9E9E9E"
              opacity={p.op}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* NICHT label */}
      <div style={{
        position: 'absolute',
        top: 80, left: 120,
        fontFamily: 'Inter, sans-serif',
        fontSize: '18px', fontWeight: 700,
        color: '#ff0000',
        letterSpacing: '8px',
        textTransform: 'uppercase',
        opacity: sp(5),
      }}>
        ✕ Das interessiert mich NICHT
      </div>

      {/* Strike-through cards */}
      <AbsoluteFill style={{
        display: 'flex', flexDirection: 'column',
        justifyContent: 'center', alignItems: 'flex-start',
        paddingLeft: '140px', gap: '20px',
      }}>

        {/* Card 1 — Millionen */}
        <div style={{
          position: 'relative',
          opacity: sp(8),
          transform: `translateX(${(1 - sp(8)) * -80}px)`,
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: '100px',
            color: '#9E9E9E',
            letterSpacing: '-1px',
            lineHeight: 1,
          }}>
            MILLIONEN AUF PLAKATEN
          </div>
          {/* Red strike */}
          <div style={{
            position: 'absolute',
            top: '55%',
            left: 0,
            width: `${strikeWidth}%`,
            height: '5px',
            backgroundColor: '#ff0000',
            boxShadow: '0 0 10px rgba(255,0,0,0.6)',
          }} />
        </div>

        {/* Card 2 — YouTube Thumbnails */}
        <div style={{
          position: 'relative',
          opacity: sp(22),
          transform: `translateX(${(1 - sp(22)) * -80}px)`,
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: '100px',
            color: '#9E9E9E',
            letterSpacing: '-1px',
            lineHeight: 1,
          }}>
            YOUTUBE-THUMBNAILS
          </div>
          <div style={{
            position: 'absolute',
            top: '55%',
            left: 0,
            width: `${Math.max(0, strikeWidth - 20)}%`,
            height: '5px',
            backgroundColor: '#ff0000',
            boxShadow: '0 0 10px rgba(255,0,0,0.6)',
          }} />
        </div>

        {/* Card 3 — Ferraris */}
        <div style={{
          position: 'relative',
          opacity: sp(36),
          transform: `translateX(${(1 - sp(36)) * -80}px)`,
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: '100px',
            color: '#9E9E9E',
            letterSpacing: '-1px',
            lineHeight: 1,
          }}>
            FERRARIS & CHAMPAGNER
          </div>
          <div style={{
            position: 'absolute',
            top: '55%',
            left: 0,
            width: `${Math.max(0, strikeWidth - 35)}%`,
            height: '5px',
            backgroundColor: '#ff0000',
            boxShadow: '0 0 10px rgba(255,0,0,0.6)',
          }} />
        </div>

      </AbsoluteFill>

      {/* Right side — SVG icon reject symbol */}
      <div style={{
        position: 'absolute',
        right: 120, top: '50%',
        transform: `translateY(-50%) scale(${sp(45)})`,
        opacity: sp(45),
      }}>
        <svg width="200" height="200" viewBox="0 0 200 200">
          <circle cx="100" cy="100" r="90" fill="none" stroke="#ff0000" strokeWidth="6" opacity="0.7" />
          <line x1="40" y1="40" x2="160" y2="160" stroke="#ff0000" strokeWidth="6" opacity="0.7" />
          <circle cx="100" cy="100" r="90" fill="none" stroke="#ff0000" strokeWidth="1" opacity="0.15"
            transform={`rotate(${frame * 0.5}, 100, 100)`}
          />
        </svg>
      </div>

      {/* Stick figure — arms crossed (dismissive) */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="120" height="200" style={{
          position: 'absolute', bottom: 80, right: 380,
          opacity: sp(50),
          transform: `translateY(${Math.sin(frame * 0.05) * 2}px) scaleX(-1)`,
        }}>
          <circle cx="60" cy="30" r="35" fill="#ff0000" opacity="0.06" />
          <circle cx="60" cy="30" r="22" fill="none" stroke="#F5F5F5" strokeWidth="2.5"
            transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 30)`}
          />
          <rect x="44" y="25" width="13" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2"
            transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 30)`}
          />
          <rect x="61" y="25" width="13" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2"
            transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 30)`}
          />
          <line x1="57" y1="29" x2="61" y2="29" stroke="#e9d700" strokeWidth="1.5"
            transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 30)`}
          />
          <line x1="60" y1="52" x2="60" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Arms crossed */}
          <line x1="60" y1="70" x2="30" y2="90" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="70" x2="90" y2="90" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="130" x2="40" y2="175" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="130" x2="80" y2="175" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: '22px',
        fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene2_FakePromises;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_TheNumber',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_TheNumber: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pulse animation for the mystery number
  const pulse = Math.sin(frame * 0.08) * 0.02 + 1;

  // Number counter reveal — ??? then 20.000
  const numberRevealProgress = interpolate(frame, [60, 90], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const fogOpacity = interpolate(frame, [55, 80], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i * 3 + 7) * 1920,
    y: seededRandom(i * 3 + 8) * 1080,
    size: seededRandom(i * 3 + 9) * 5 + 1,
    op: seededRandom(i * 3 + 10) * 0.18 + 0.04,
    speed: seededRandom(i * 3 + 11) * 0.6 + 0.1,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#2D2D2D', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, #1a1500 0%, #2D2D2D 60%), radial-gradient(ellipse at 90% 90%, #0a0a1a 0%, transparent 40%)',
      }} />

      {/* Shape layer — concentric rings emanating */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {[1, 2, 3, 4].map((ring) => (
            <circle
              key={ring}
              cx={960} cy={540}
              r={ring * 160 + Math.sin(frame * 0.015 + ring) * 8}
              fill="none"
              stroke="#e9d700"
              strokeWidth="0.8"
              opacity={0.06 - ring * 0.01}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Motion texture — dot grid */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.06 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 18 }, (_, row) =>
            Array.from({ length: 32 }, (_, col) => (
              <circle
                key={`${row}-${col}`}
                cx={col * 62 + 10}
                cy={row * 62 + 10}
                r={1.5}
                fill="#e9d700"
              />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i}
              cx={p.x}
              cy={(p.y - frame * p.speed) % 1080}
              r={p.size}
              fill="#e9d700"
              opacity={p.op}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Top whisper label */}
      <div style={{
        position: 'absolute', top: 80, left: 0, right: 0,
        textAlign: 'center',
        fontFamily: 'Open Sans, sans-serif',
        fontSize: '28px', fontWeight: 300,
        color: '#9E9E9E',
        letterSpacing: '4px',
        opacity: sp(5),
      }}>
        Eine Zahl, über die kaum jemand spricht
      </div>

      {/* Central mystery reveal */}
      <AbsoluteFill style={{
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
        gap: '16px',
      }}>

        {/* EINE ZAHL */}
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '72px',
          color: '#9E9E9E',
          letterSpacing: '8px',
          opacity: sp(8),
          transform: `translateY(${(1 - sp(8)) * 30}px)`,
        }}>
          EINE ZAHL
        </div>

        {/* Mystery → Number */}
        <div style={{
          position: 'relative',
          transform: `scale(${pulse})`,
        }}>
          {/* Fog/mystery layer */}
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: '220px',
            fontWeight: 700,
            color: '#F5F5F5',
            letterSpacing: '-5px',
            lineHeight: 1,
            opacity: fogOpacity,
            position: 'relative', zIndex: 2,
            filter: `blur(${fogOpacity * 8}px)`,
          }}>
            ???.???
          </div>

          {/* Revealed number */}
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: '220px',
            fontWeight: 700,
            color: '#e9d700',
            letterSpacing: '-5px',
            lineHeight: 1,
            opacity: numberRevealProgress,
            position: 'absolute',
            top: 0, left: 0, right: 0,
            textAlign: 'center',
            textShadow: '0 0 80px rgba(233,215,0,0.4)',
          }}>
            20.000
          </div>
        </div>

        {/* EURO label */}
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '64px',
          color: '#e9d700',
          letterSpacing: '12px',
          opacity: interpolate(frame, [75, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateY(${(1 - sp(75)) * 20}px)`,
        }}>
          EURO
        </div>

        {/* Descriptor */}
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: '28px',
          color: '#9E9E9E',
          fontStyle: 'italic',
          opacity: interpolate(frame, [85, 105], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          marginTop: '8px',
        }}>
          unscheinbar · fast lächerlich klein · und doch entscheidend
        </div>

      </AbsoluteFill>

      {/* Stick figure — leaning in curious */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="130" height="210" style={{
          position: 'absolute', bottom: 100, right: 160,
          opacity: sp(50),
          transform: `translateY(${Math.sin(frame * 0.05) * 2}px) rotate(-8deg)`,
        }}>
          <circle cx="65" cy="32" r="38" fill="#e9d700" opacity="0.07" />
          <circle cx="65" cy="32" r="22" fill="none" stroke="#F5F5F5" strokeWidth="2.5"
            transform={`rotate(${Math.sin(frame * 0.04) * 3 - 5}, 65, 32)`}
          />
          <rect x="48" y="27" width="13" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2"
            transform={`rotate(${Math.sin(frame * 0.04) * 3 - 5}, 65, 32)`}
          />
          <rect x="65" y="27" width="13" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2"
            transform={`rotate(${Math.sin(frame * 0.04) * 3 - 5}, 65, 32)`}
          />
          <line x1="61" y1="31" x2="65" y2="31" stroke="#e9d700" strokeWidth="1.5"
            transform={`rotate(${Math.sin(frame * 0.04) * 3 - 5}, 65, 32)`}
          />
          <line x1="65" y1="54" x2="65" y2="135" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Leaning arm forward */}
          <line x1="65" y1="80" x2="30" y2="70" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="65" y1="80" x2="100" y2="65" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="65" y1="135" x2="45" y2="180" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="65" y1="135" x2="85" y2="180" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 30%, rgba(0,0,0,0.85) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: '22px',
        fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene3_TheNumber;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4_IchBinKunal',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_IchBinKunal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Tilt camera effect
  const camTilt = interpolate(frame, [0, durationInFrames], [0.5, -0.5], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const traits = ['LERNE', 'FEHLER', 'BEOBACHTE', 'RECHNE NACH'];
  const traitColors = ['#e9d700', '#F5F5F5', '#e9d700', '#F5F5F5'];

  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 5 + 1) * 1920,
    y: seededRandom(i * 5 + 2) * 1080,
    size: seededRandom(i * 5 + 3) * 4 + 1,
    op: seededRandom(i * 5 + 4) * 0.2 + 0.05,
    speed: seededRandom(i * 5 + 5) * 0.4 + 0.1,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #1C1C1C 0%, #252520 40%, #1C1C1C 100%)',
        transform: `rotate(${camTilt}deg) scale(1.01)`,
      }} />

      {/* Left vertical color bar */}
      <div style={{
        position: 'absolute',
        left: 0, top: 0,
        width: `${sp(5) * 8}px`,
        height: '100%',
        backgroundColor: '#e9d700',
        opacity: 0.9,
      }} />

      {/* Shape layers */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          <rect
            x={1200} y={-100}
            width={900} height={900}
            fill="none" stroke="#e9d700"
            strokeWidth="1" opacity={0.04}
            transform={`rotate(${15 + frame * 0.04}, 1650, 350)`}
          />
          <circle cx={200} cy={900} r={300}
            fill="#e9d700" opacity={0.03}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — vertical lines */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 30 }, (_, i) => (
            <line key={i}
              x1={i * 65 + ((frame * 0.3) % 65)}
              y1={0}
              x2={i * 65 + ((frame * 0.3) % 65)}
              y2={1080}
              stroke="#F5F5F5"
              strokeWidth="0.5"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i}
              cx={p.x}
              cy={(p.y - frame * p.speed) % 1080}
              r={p.size}
              fill="#e9d700"
              opacity={p.op}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Left — ICH BIN block */}
      <div style={{
        position: 'absolute',
        left: 80, top: '50%',
        transform: `translateY(-50%) translateX(${(1 - sp(8)) * -60}px)`,
        opacity: sp(8),
      }}>
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: '22px', fontWeight: 300,
          color: '#9E9E9E', letterSpacing: '5px',
          marginBottom: '12px',
        }}>
          KEIN GLASTURM · KEINE THEORIE
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '130px', fontWeight: 400,
          color: '#F5F5F5', lineHeight: 0.9,
          letterSpacing: '-2px',
        }}>
          ICH BIN
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '160px', fontWeight: 400,
          color: '#e9d700', lineHeight: 0.9,
          letterSpacing: '-3px',
          textShadow: '0 0 50px rgba(233,215,0,0.35)',
        }}>
          KUNAL
        </div>
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: '26px', fontWeight: 400,
          color: '#9E9E9E', marginTop: '20px',
          fontStyle: 'italic',
        }}>
          Mittendrin · nicht drüber
        </div>
      </div>

      {/* Right — Trait list */}
      <div style={{
        position: 'absolute',
        right: 120, top: '50%',
        transform: 'translateY(-50%)',
        display: 'flex', flexDirection: 'column',
        gap: '18px',
      }}>
        {traits.map((trait, i) => (
          <div key={i} style={{
            display: 'flex', alignItems: 'center', gap: '18px',
            opacity: sp(25 + i * 12),
            transform: `translateX(${(1 - sp(25 + i * 12)) * 60}px)`,
          }}>
            {/* Bullet */}
            <div style={{
              width: '12px', height: '12px',
              backgroundColor: '#e9d700',
              borderRadius: '50%',
              flexShrink: 0,
            }} />
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: '72px',
              color: traitColors[i],
              letterSpacing: '-1px',
            }}>
              {trait}
            </div>
          </div>
        ))}
      </div>

      {/* Large stick figure — center confident */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="160" height="260" style={{
          position: 'absolute',
          bottom: 60, left: '50%',
          transform: `translateX(-50%) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: sp(45),
        }}>
          {/* Golden aura */}
          <circle cx="80" cy="40" r="55" fill="#e9d700" opacity="0.10" />
          {/* Head */}
          <circle cx="80" cy="40" r="28" fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.04) * 2}, 80, 40)`}
          />
          {/* Glasses */}
          <rect x="58" y="33" width="16" height="11" rx="5" fill="none" stroke="#e9d700" strokeWidth="2.5"
            transform={`rotate(${Math.sin(frame * 0.04) * 2}, 80, 40)`}
          />
          <rect x="78" y="33" width="16" height="11" rx="5" fill="none" stroke="#e9d700" strokeWidth="2.5"
            transform={`rotate(${Math.sin(frame * 0.04) * 2}, 80, 40)`}
          />
          <line x1="74" y1="38" x2="78" y2="38" stroke="#e9d700" strokeWidth="2"
            transform={`rotate(${Math.sin(frame * 0.04) * 2}, 80, 40)`}
          />
          {/* Body */}
          <line x1="80" y1="68" x2="80" y2="170" stroke="#F5F5F5" strokeWidth="3" />
          {/* Arms — hands on hips confident */}
          <line x1="80" y1="95" x2="40" y2="125" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="40" y1="125" x2="35" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="80" y1="95" x2="120" y2="125" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="120" y1="125" x2="125" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Legs */}
          <line x1="80" y1="170" x2="55" y2="228" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="80" y1="170" x2="105" y2="228" stroke="#F5F5F5" strokeWidth="3" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: '22px',
        fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene4_IchBinKunal;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5_WieDasSpielFunktioniert',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_WieDasSpielFunktioniert: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Orbit zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1.0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const dividerW = interpolate(frame, [30, 55], [0, 780], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 6 + 2) * 1920,
    y: seededRandom(i * 6 + 3) * 1080,
    size: seededRandom(i * 6 + 4) * 3 + 1.5,
    op: seededRandom(i * 6 + 5) * 0.15 + 0.04,
    speed: seededRandom(i * 6 + 6) * 0.5 + 0.12,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 50%, #1a1800 0%, transparent 45%), radial-gradient(ellipse at 70% 50%, #0f0f1a 0%, transparent 50%), #1C1C1C',
        transform: `scale(${camScale})`,
      }} />

      {/* Shape layers — two half-circles split */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {/* Left half */}
          <path
            d={`M 500,100 A 400,400 0 0,0 500,980`}
            fill="none" stroke="#9E9E9E" strokeWidth="1" opacity={0.06}
          />
          {/* Right half */}
          <path
            d={`M 1420,100 A 400,400 0 0,1 1420,980`}
            fill="none" stroke="#e9d700" strokeWidth="1" opacity={0.06}
          />
          <rect x={850} y={200} width={220} height={680}
            fill="none" stroke="#e9d700" strokeWidth="0.5" opacity={0.04}
            transform={`rotate(${frame * 0.03}, 960, 540)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — circle burst */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.05 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 8 }, (_, i) => (
            <line key={i}
              x1={960} y1={540}
              x2={960 + Math.cos((i / 8) * Math.PI * 2) * 800}
              y2={540 + Math.sin((i / 8) * Math.PI * 2) * 600}
              stroke="#e9d700" strokeWidth="0.8"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i}
              cx={p.x}
              cy={(p.y + frame * p.speed) % 1080}
              r={p.size} fill="#e9d700" opacity={p.op}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* VS layout: Left = old Finanzberater / Right = Kux */}
      {/* Left side — CROSSED OUT */}
      <div style={{
        position: 'absolute',
        left: 0, top: 0, bottom: 0, width: '48%',
        display: 'flex', flexDirection: 'column',
        justifyContent: 'center', paddingLeft: '100px',
        opacity: sp(5),
        transform: `translateX(${(1 - sp(5)) * -50}px)`,
      }}>
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: '22px', fontWeight: 400,
          color: '#9E9E9E', letterSpacing: '3px',
          marginBottom: '16px',
        }}>
          NICHT DAS:
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '88px', color: '#9E9E9E',
          lineHeight: 0.95, letterSpacing: '-1px',
          position: 'relative',
        }}>
          HOCHGLANZ-<br />POLIERTER<br />FINANZ-<br />BERATER
          {/* Big X overlay */}
          <div style={{
            position: 'absolute', top: 0, left: 0, right: 0, bottom: 0,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            opacity: interpolate(frame, [40, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}>
            <svg width="340" height="340" viewBox="0 0 340 340">
              <line x1="20" y1="20" x2="320" y2="320" stroke="#ff0000" strokeWidth="8" opacity="0.7" />
              <line x1="320" y1="20" x2="20" y2="320" stroke="#ff0000" strokeWidth="8" opacity="0.7" />
            </svg>
          </div>
        </div>
      </div>

      {/* Divider */}
      <div style={{
        position: 'absolute',
        left: '50%', top: '50%',
        transform: 'translate(-50%, -50%)',
        width: '4px', height: `${sp(20) * 600}px`,
        backgroundColor: '#e9d700',
        boxShadow: '0 0 20px rgba(233,215,0,0.4)',
      }} />

      {/* Right side — THE REAL */}
      <div style={{
        position: 'absolute',
        right: 0, top: 0, bottom: 0, width: '48%',
        display: 'flex', flexDirection: 'column',
        justifyContent: 'center', paddingLeft: '60px',
        opacity: sp(25),
        transform: `translateX(${(1 - sp(25)) * 60}px)`,
      }}>
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: '22px', fontWeight: 700,
          color: '#e9d700', letterSpacing: '3px',
          marginBottom: '16px',
        }}>
          SONDERN DAS:
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '88px', color: '#F5F5F5',
          lineHeight: 0.95, letterSpacing: '-1px',
        }}>
          JEMAND,<br />DER MIT<br />DIR
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '96px', color: '#e9d700',
          lineHeight: 0.95, letterSpacing: '-1px',
          textShadow: '0 0 40px rgba(233,215,0,0.3)',
        }}>
          VERSTEHT
        </div>

        {/* Bottom divider line */}
        <div style={{
          marginTop: '24px',
          width: `${dividerW}px`,
          height: '3px',
          backgroundColor: '#e9d700',
          boxShadow: '0 0 10px rgba(233,215,0,0.5)',
        }} />

        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: '26px', fontStyle: 'italic',
          color: '#9E9E9E', marginTop: '16px',
          opacity: interpolate(frame, [65, 85], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          wie dieses Spiel wirklich funktioniert
        </div>
      </div>

      {/* Two stick figures — shaking hands at center */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="220" height="200" style={{
          position: 'absolute', bottom: 60, left: '50%',
          transform: `translateX(-50%) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: sp(55),
        }}>
          {/* Figure left */}
          <circle cx="60" cy="28" r="20" fill="none" stroke="#9E9E9E" strokeWidth="2" />
          <line x1="60" y1="48" x2="60" y2="110" stroke="#9E9E9E" strokeWidth="2" />
          <line x1="60" y1="68" x2="90" y2="85" stroke="#9E9E9E" strokeWidth="2" />
          <line x1="60" y1="110" x2="45" y2="155" stroke="#9E9E9E" strokeWidth="2" />
          <line x1="60" y1="110" x2="75" y2="155" stroke="#9E9E9E" strokeWidth="2" />
          {/* Figure right */}
          <circle cx="160" cy="28" r="20" fill="none" stroke="#F5F5F5" strokeWidth="2" />
          {/* Glasses on right */}
          <rect x="148" y="23" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <rect x="162" y="23" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="158" y1="26" x2="162" y2="26" stroke="#e9d700" strokeWidth="1" />
          <line x1="160" y1="48" x2="160" y2="110" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="160" y1="68" x2="130" y2="85" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="160" y1="110" x2="145" y2="155" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="160" y1="110" x2="175" y2="155" stroke="#F5F5F5" strokeWidth="2" />
          {/* Handshake */}
          <line x1="90" y1="85" x2="130" y2="85" stroke="#e9d700" strokeWidth="3" />
          <circle cx="110" cy="85" r="5" fill="#e9d700" opacity="0.8" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: '22px',
        fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene5_WieDasSpielFunktioniert;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6_WarumDieseZahl',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_WarumDieseZahl: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on the big reveal
  const shakeX = frame > 55 && frame < 65 ? Math.sin(frame * 8) * 6 : 0;
  const shakeY = frame > 55 && frame < 65 ? Math.cos(frame * 9) * 4 : 0;

  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 4 + 11) * 1920,
    y: seededRandom(i * 4 + 12) * 1080,
    size: seededRandom(i * 4 + 13) * 5 + 1,
    op: seededRandom(i * 4 + 14) * 0.2 + 0.05,
    speed: seededRandom(i * 4 + 15) * 0.5 + 0.1,
  }));

  // Number scale-in
  const heroScale = sp(50, { damping: 8, stiffness: 90, mass: 0.5 });

  return (
    <AbsoluteFill style={{
      backgroundColor: '#1C1C1C',
      overflow: 'hidden',
      opacity: exitOp,
      transform: `translate(${shakeX}px, ${shakeY}px)`,
    }}>

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 100%, #201800 0%, transparent 55%), radial-gradient(ellipse at 50% 0%, #0a0a0a 0%, transparent 40%), #1C1C1C',
      }} />

      {/* Shape layer — stacked horizontal bars */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          <rect x={0} y={480} width={1920} height={3} fill="#e9d700" opacity={0.08} />
          <rect x={0} y={600} width={1920} height={1} fill="#e9d700" opacity={0.04} />
          <circle cx={100} cy={540} r={400} fill="#e9d700" opacity={0.025} />
          <circle cx={1820} cy={540} r={400} fill="#e9d700" opacity={0.025} />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — radiating circles */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.05 }}>
        <svg width="1920" height="1080">
          {[200, 350, 500, 650].map((r, i) => (
            <circle key={i}
              cx={960} cy={480}
              r={r + (frame * 0.5) % 150}
              fill="none" stroke="#e9d700" strokeWidth="0.8"
              opacity={Math.max(0, 1 - ((r + (frame * 0.5) % 150) / 800))}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i}
              cx={p.x}
              cy={(p.y - frame * p.speed) % 1080}
              r={p.size} fill="#e9d700" opacity={p.op}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Rejected alternatives — top */}
      <div style={{
        position: 'absolute', top: 80,
        left: 0, right: 0,
        display: 'flex', justifyContent: 'center',
        gap: '80px',
        opacity: sp(5),
        transform: `translateY(${(1 - sp(5)) * -30}px)`,
      }}>
        {['1.000.000 €', '10.000 €', '5.000 €'].map((val, i) => (
          <div key={i} style={{ position: 'relative', textAlign: 'center' }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: '52px', fontWeight: 700,
              color: '#9E9E9E',
            }}>
              {val}
            </div>
            <div style={{
              position: 'absolute',
              top: '55%', left: 0,
              width: `${sp(30) * 100}%`,
              height: '3px',
              backgroundColor: '#ff0000',
              opacity: 0.7,
            }} />
          </div>
        ))}
      </div>

      {/* NEIN labels */}
      <div style={{
        position: 'absolute', top: 155,
        left: 0, right: 0,
        display: 'flex', justifyContent: 'center',
        gap: '170px',
        opacity: interpolate(frame, [35, 50], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        {['NEIN', 'NEIN', 'NEIN'].map((n, i) => (
          <div key={i} style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: '28px',
            color: '#ff0000',
            letterSpacing: '4px',
          }}>{n}</div>
        ))}
      </div>

      {/* HERO — 20.000 Euro */}
      <AbsoluteFill style={{
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: '28px', fontWeight: 400,
          color: '#9E9E9E', letterSpacing: '6px',
          textTransform: 'uppercase',
          opacity: sp(40),
          marginBottom: '8px',
        }}>
          Das Spiel beginnt mit
        </div>

        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: '260px', fontWeight: 700,
          color: '#e9d700',
          letterSpacing: '-8px',
          lineHeight: 1,
          transform: `scale(${0.7 + heroScale * 0.3})`,
          textShadow: '0 0 100px rgba(233,215,0,0.5), 0 0 200px rgba(233,215,0,0.2)',
          opacity: sp(50),
        }}>
          20.000
        </div>

        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '80px',
          color: '#F5F5F5',
          letterSpacing: '20px',
          opacity: sp(60),
          transform: `translateY(${(1 - sp(60)) * 20}px)`,
        }}>
          EURO
        </div>

        {/* WARUM label */}
        <div style={{
          marginTop: '32px',
          fontFamily: 'Open Sans, sans-serif',
          fontSize: '36px', fontWeight: 700,
          color: '#e9d700',
          letterSpacing: '2px',
          opacity: interpolate(frame, [90, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          fontStyle: 'italic',
        }}>
          Warum ausgerechnet diese Zahl?  →
        </div>
      </AbsoluteFill>

      {/* Stick figure — pointing up */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="120" height="200" style={{
          position: 'absolute', bottom: 80, left: 120,
          opacity: sp(55),
          transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
        }}>
          <circle cx="60" cy="30" r="35" fill="#e9d700" opacity="0.09" />
          <circle cx="60" cy="30" r="22" fill="none" stroke="#F5F5F5" strokeWidth="2.5"
            transform={`rotate(${Math.sin(frame * 0.04) * 2}, 60, 30)`}
          />
          <rect x="44" y="25" width="13" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2" />
          <rect x="61" y="25" width="13" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="57" y1="29" x2="61" y2="29" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="60" y1="52" x2="60" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Arm pointing up */}
          <line x1="60" y1="70" x2="30" y2="40" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="70" x2="90" y2="90" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="130" x2="40" y2="175" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="130" x2="80" y2="175" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: '22px',
        fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene6_WarumDieseZahl;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7_SteinInsRollen',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_SteinInsRollen: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Stone rolling animation — starts at left, accelerates
  const stoneX = interpolate(frame, [40, 160], [200, 1700], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.in(Easing.quad),
  });

  const stoneRotation = interpolate(frame, [40, 160], [0, 720], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.in(Easing.quad),
  });

  const stonePhase = frame < 40 ? 'static' : 'rolling';

  // Ground shake when stone is heavy/static
  const groundPulse = stonePhase === 'static'
    ? Math.sin(frame * 0.15) * 2
    : 0;

  // Push arrows fade out when rolling
  const arrowOp = interpolate(frame, [35, 55], [sp(10), 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Speed trail opacity
  const trailOp = interpolate(frame, [60, 80], [0, 0.6], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 12 }, (_, i) => ({
    x: seededRandom(i * 7 + 3) * 1920,
    y: seededRandom(i * 7 + 4) * 1080,
    size: seededRandom(i * 7 + 5) * 3 + 1,
    op: seededRandom(i * 7 + 6) * 0.15 + 0.04,
    speed: seededRandom(i * 7 + 7) * 0.4 + 0.1,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#2D2D2D', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #1a1a1a 0%, #2D2D2D 50%, #1a1200 100%)',
      }} />

      {/* Shape layers */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {/* Background mountain silhouette */}
          <path
            d="M0,800 L400,400 L700,600 L960,200 L1200,500 L1500,350 L1920,600 L1920,1080 L0,1080 Z"
            fill="#1C1C1C" opacity={0.4}
          />
          {/* Large gear shape */}
          <circle cx={1700} cy={200} r={180}
            fill="none" stroke="#9E9E9E" strokeWidth="1" opacity={0.04}
            transform={`rotate(${frame * 0.1}, 1700, 200)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Ground line */}
      <div style={{
        position: 'absolute', bottom: 220,
        left: 0, right: 0,
        height: '3px',
        backgroundColor: '#9E9E9E',
        opacity: 0.25,
      }} />

      {/* Motion texture — speed lines on right half */}
      <AbsoluteFill style={{ position: 'absolute', opacity: trailOp }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 12 }, (_, i) => (
            <line key={i}
              x1={stoneX - 80 - i * 25}
              y1={830 - 8 + i * 1.5}
              x2={stoneX + 20}
              y2={830 - 8 + i * 1.5}
              stroke="#e9d700"
              strokeWidth={Math.max(0.5, 3 - i * 0.2)}
              opacity={Math.max(0, 0.6 - i * 0.05)}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Push arrows — effort arrows before rolling */}
      <AbsoluteFill style={{ position: 'absolute', opacity: arrowOp }}>
        <svg width="1920" height="1080">
          {[0, 1, 2].map((i) => (
            <g key={i} transform={`translate(${80 + i * 35}, ${830 - 55})`}>
              <line x1="0" y1="0" x2="60" y2="0" stroke="#F5F5F5" strokeWidth="4" />
              <polygon points="60,0 45,-8 45,8" fill="#F5F5F5" />
            </g>
          ))}
          <text x="60" y="790" fontFamily="Open Sans" fontSize="26" fill="#9E9E9E" opacity="0.9">
            ENORME KRAFT NÖTIG
          </text>
        </svg>
      </AbsoluteFill>

      {/* THE STONE */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {/* Stone shadow */}
          <ellipse
            cx={stoneX}
            cy={855}
            rx={70 + (stoneX - 200) * 0.02}
            ry={12}
            fill="#000"
            opacity={0.35}
          />
          {/* Stone body */}
          <circle
            cx={stoneX}
            cy={830}
            r={80}
            fill="#9E9E9E"
            transform={`rotate(${stoneRotation}, ${stoneX}, 830)`}
          />
          {/* Stone texture crack */}
          <line
            x1={stoneX - 20} y1={810}
            x2={stoneX + 30} y2={850}
            stroke="#1C1C1C" strokeWidth="3" opacity="0.5"
            transform={`rotate(${stoneRotation}, ${stoneX}, 830)`}
          />
          <circle cx={stoneX} cy={830} r={80}
            fill="none" stroke="#F5F5F5" strokeWidth="1.5" opacity="0.12"
            transform={`rotate(${stoneRotation}, ${stoneX}, 830)`}
          />
          {/* Static weight indicator */}
          {stonePhase === 'static' && (
            <text x={stoneX - 30} y={750} fontFamily="Anton" fontSize="36" fill="#e9d700" opacity="0.9">
              SCHWER
            </text>
          )}
          {/* Speed label when rolling */}
          {frame > 80 && (
            <text x={stoneX - 40} y={750} fontFamily="Anton" fontSize="32" fill="#e9d700"
              opacity={interpolate(frame, [80, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
            >
              VON SELBST!
            </text>
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i}
              cx={p.x}
              cy={(p.y - frame * p.speed) % 1080}
              r={p.size} fill="#9E9E9E" opacity={p.op}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Text annotation */}
      <div style={{
        position: 'absolute',
        top: 80, left: 120, right: 120,
        display: 'flex', justifyContent: 'space-between',
        alignItems: 'flex-start',
      }}>
        <div style={{ opacity: sp(5), transform: `translateY(${(1 - sp(5)) * -20}px)` }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: '80px', color: '#F5F5F5',
            lineHeight: 1,
          }}>DER ERSTE</div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: '80px', color: '#e9d700',
            lineHeight: 1,
          }}>SCHUB</div>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: '28px', color: '#9E9E9E',
            marginTop: '12px',
          }}>
            brutale Kraft → einmalig nötig
          </div>
        </div>

        <div style={{
          opacity: interpolate(frame, [70, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateY(${(1 - sp(70)) * -20}px)`,
          textAlign: 'right',
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: '80px', color: '#F5F5F5',
            lineHeight: 1,
          }}>DANACH</div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: '80px', color: '#e9d700',
            lineHeight: 1,
          }}>LÄUFT ER</div>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: '28px', color: '#9E9E9E',
            marginTop: '12px',
          }}>
            Schwung → selbsttragend
          </div>
        </div>
      </div>

      {/* Stick figure pushing the stone */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="120" height="200" style={{
          position: 'absolute',
          bottom: 208,
          left: stonePhase === 'static' ? '90px' : `${Math.min(stoneX - 130, 1750)}px`,
          opacity: stonePhase === 'static' ? sp(5) : interpolate(frame, [40, 70], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateY(${groundPulse}px)`,
        }}>
          <circle cx="60" cy="30" r="22" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <rect x="44" y="25" width="13" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2" />
          <rect x="61" y="25" width="13" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="57" y1="29" x2="61" y2="29" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="60" y1="52" x2="60" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Arms pushing forward */}
          <line x1="60" y1="70" x2="95" y2="75" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="80" x2="92" y2="88" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Legs — pushing stance */}
          <line x1="60" y1="130" x2="35" y2="172" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="130" x2="75" y2="172" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: '22px',
        fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene7_SteinInsRollen;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8_Ueberlebenskampf',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_Ueberlebenskampf: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Red alert pulse
  const alertPulse = Math.sin(frame * 0.12) * 0.15 + 0.85;

  // Crack animation
  const crackProgress = interpolate(frame, [50, 90], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 4 + 20) * 1920,
    y: seededRandom(i * 4 + 21) * 1080,
    size: seededRandom(i * 4 + 22) * 4 + 1,
    op: seededRandom(i * 4 + 23) * 0.18 + 0.05,
    speed: seededRandom(i * 4 + 24) * 0.5 + 0.1,
  }));

  const threats = ['AUTOWERKSTATT', 'ZAHNARZT', 'WASCHMASCHINE'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Dark red gradient */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at 50% 50%, rgba(80,0,0,${alertPulse * 0.25}) 0%, #1C1C1C 60%)`,
      }} />

      {/* Shape layer — cracked shield */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {/* Shield outline */}
          <path
            d="M960,80 L1200,180 L1200,580 Q1200,750 960,850 Q720,750 720,580 L720,180 Z"
            fill="none" stroke="#9E9E9E" strokeWidth="2" opacity={0.08}
          />
          {/* Crack line */}
          <path
            d={`M960,80 L${960 + crackProgress * 80},${80 + crackProgress * 200} L${960 + crackProgress * 20},${80 + crackProgress * 400} L${960 + crackProgress * 100},${80 + crackProgress * 770}`}
            fill="none" stroke="#ff0000" strokeWidth="3" opacity={crackProgress * 0.8}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — falling debris dots */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.06 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 15 }, (_, i) => (
            <rect key={i}
              x={seededRandom(i * 3 + 50) * 1920}
              y={(seededRandom(i * 3 + 51) * 1080 + frame * (seededRandom(i * 3 + 52) * 2 + 0.5)) % 1080}
              width={seededRandom(i * 3 + 53) * 8 + 2}
              height={seededRandom(i * 3 + 54) * 8 + 2}
              fill="#9E9E9E"
              transform={`rotate(${frame * seededRandom(i + 60) * 3}, ${seededRandom(i * 3 + 50) * 1920}, ${(seededRandom(i * 3 + 51) * 1080 + frame * (seededRandom(i * 3 + 52) * 2 + 0.5)) % 1080})`}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i}
              cx={p.x}
              cy={(p.y + frame * p.speed) % 1080}
              r={p.size} fill="#ff0000" opacity={p.op * 0.4}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Main text — left column */}
      <div style={{
        position: 'absolute',
        left: 120, top: '50%',
        transform: `translateY(-50%) translateX(${(1 - sp(5)) * -60}px)`,
        opacity: sp(5),
        width: '800px',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '68px', color: '#9E9E9E',
          lineHeight: 1.05,
          letterSpacing: '-1px',
        }}>
          ALLES DARUNTER IST
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '120px', color: '#ff0000',
          lineHeight: 0.9, letterSpacing: '-2px',
          textShadow: `0 0 40px rgba(255,0,0,${alertPulse * 0.4})`,
          transform: `scale(${alertPulse})`,
          transformOrigin: 'left center',
        }}>
          KEIN AUFBAU
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '68px', color: '#9E9E9E',
          lineHeight: 1.05, letterSpacing: '-1px',
          marginTop: '8px',
        }}>
          ES IST
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '120px', color: '#ff0000',
          lineHeight: 0.9, letterSpacing: '-2px',
          textShadow: `0 0 40px rgba(255,0,0,${alertPulse * 0.4})`,
          opacity: sp(20),
          transform: `translateX(${(1 - sp(20)) * -30}px)`,
        }}>
          ÜBERLEBENSKAMPF
        </div>
      </div>

      {/* Right — threat icons */}
      <div style={{
        position: 'absolute',
        right: 120, top: '50%',
        transform: 'translateY(-50%)',
        display: 'flex', flexDirection: 'column',
        gap: '20px',
      }}>
        {threats.map((t, i) => (
          <div key={i} style={{
            display: 'flex', alignItems: 'center', gap: '20px',
            opacity: sp(35 + i * 15),
            transform: `translateX(${(1 - sp(35 + i * 15)) * 60}px)`,
          }}>
            {/* Warning triangle SVG */}
            <svg width="44" height="44" viewBox="0 0 44 44">
              <polygon points="22,4 42,40 2,40" fill="none" stroke="#ff0000" strokeWidth="3" opacity="0.85" />
              <text x="17" y="36" fontFamily="Anton" fontSize="22" fill="#ff0000">!</text>
            </svg>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: '56px',
              color: '#F5F5F5',
              letterSpacing: '-1px',
            }}>
              {t}
            </div>
          </div>
        ))}

        {/* EXISTENZIELLE KRISE label */}
        <div style={{
          marginTop: '8px',
          fontFamily: 'Open Sans, sans-serif',
          fontSize: '26px', fontStyle: 'italic',
          color: '#ff0000',
          opacity: interpolate(frame, [80, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          → keine Unannehmlichkeiten · existenzielle Krisen
        </div>
      </div>

      {/* Vulnerable stick figure — bent/weak */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="120" height="200" style={{
          position: 'absolute',
          bottom: 80, left: '50%',
          transform: `translateX(-50%) translateY(${Math.sin(frame * 0.08) * 4}px)`,
          opacity: sp(55),
        }}>
          <circle cx="60" cy="30" r="35" fill="#ff0000" opacity="0.07" />
          <circle cx="60" cy="30" r="22" fill="none" stroke="#F5F5F5" strokeWidth="2.5"
            transform={`rotate(${Math.sin(frame * 0.04) * 5}, 60, 30)`}
          />
          <rect x="44" y="25" width="13" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2" />
          <rect x="61" y="25" width="13" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="57" y1="29" x2="61" y2="29" stroke="#e9d700" strokeWidth="1.5" />
          {/* Bent body — weak posture */}
          <line x1="60" y1="52" x2="55" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Arms drooping */}
          <line x1="58" y1="75" x2="28" y2="100" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="58" y1="80" x2="82" y2="105" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Legs bent */}
          <line x1="55" y1="130" x2="38" y2="175" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="55" y1="130" x2="72" y2="175" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Crack mark on figure */}
          <line x1="52" y1="60" x2="65" y2="90" stroke="#ff0000" strokeWidth="2"
            opacity={crackProgress * 0.8}
          />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 30%, rgba(0,0,0,0.85) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: '22px',
        fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene8_Ueberlebenskampf;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9_DispoStrudel',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_DispoStrudel: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Spiral/whirlpool spin
  const spiralAngle = frame * 2.5;

  const particles = Array.from({ length: 16 }, (_, i) => ({
    angle: (i / 16) * 360 + frame * (1.5 + i * 0.1),
    radius: 80 + i * 22 + Math.sin(frame * 0.05 + i) * 10,
    size: 5 - i * 0.15,
    op: 0.5 - i * 0.025,
  }));

  const chainItems = [
    { label: 'KEIN ERSPARTES', color: '#9E9E9E' },
    { label: 'DISPO', color: '#ff0000' },
    { label: '12% ZINSEN', color: '#ff0000' },
    { label: 'NOCH WENIGER', color: '#9E9E9E' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Dark background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 50%, #150000 0%, transparent 45%), #1C1C1C',
      }} />

      {/* STRUDEL — spinning whirlpool in background right */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {[1, 2, 3, 4, 5, 6, 7, 8].map((ring) => (
            <ellipse key={ring}
              cx={1380} cy={540}
              rx={ring * 60}
              ry={ring * 35}
              fill="none"
              stroke="#ff0000"
              strokeWidth={Math.max(0.3, 2.5 - ring * 0.3)}
              opacity={Math.max(0.02, 0.18 - ring * 0.02)}
              transform={`rotate(${spiralAngle + ring * 15}, 1380, 540)`}
            />
          ))}
          {/* Center suck point */}
          <circle cx={1380} cy={540} r={18} fill="#ff0000" opacity="0.4" />
          <circle cx={1380} cy={540} r={8} fill="#ff0000" opacity="0.8" />
        </svg>
      </AbsoluteFill>

      {/* Strudel floating particles — orbiting center */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => {
            const rad = (p.angle * Math.PI) / 180;
            return (
              <circle key={i}
                cx={1380 + Math.cos(rad) * p.radius}
                cy={540 + Math.sin(rad) * p.radius * 0.55}
                r={Math.max(0.5, p.size)}
                fill="#ff0000"
                opacity={Math.max(0, p.op)}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Motion texture — downward arrows background */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 8 }, (_, i) => (
            <text key={i}
              x={80 + i * 110}
              y={(frame * 1.2 + i * 150) % 1200 - 50}
              fontFamily="Inter" fontSize="32"
              fill="#ff0000"
            >↓</text>
          ))}
        </svg>
      </AbsoluteFill>

      {/* Left side — chain of doom */}
      <div style={{
        position: 'absolute',
        left: 120, top: '50%',
        transform: 'translateY(-50%)',
        display: 'flex', flexDirection: 'column',
        gap: '8px',
      }}>
        {chainItems.map((item, i) => (
          <div key={i}>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: '88px',
              color: item.color,
              letterSpacing: '-1px',
              lineHeight: 1,
              opacity: sp(8 + i * 14),
              transform: `translateX(${(1 - sp(8 + i * 14)) * -50}px)`,
              textShadow: item.color === '#ff0000' ? '0 0 30px rgba(255,0,0,0.3)' : 'none',
            }}>
              {item.label}
            </div>
            {/* Chain link */}
            {i < chainItems.length - 1 && (
              <div style={{
                marginLeft: '20px',
                fontFamily: 'Inter, sans-serif',
                fontSize: '32px',
                color: '#9E9E9E',
                opacity: sp(20 + i * 14) * 0.6,
              }}>
                ↓
              </div>
            )}
          </div>
        ))}

        {/* Loop arrow */}
        <div style={{
          display: 'flex', alignItems: 'center', gap: '16px',
          opacity: interpolate(frame, [75, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          marginTop: '8px',
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <path d="M30,5 A25,25 0 1,1 8,45" fill="none" stroke="#ff0000" strokeWidth="3" />
            <polygon points="8,45 2,32 18,38" fill="#ff0000" />
          </svg>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: '28px', fontStyle: 'italic',
            color: '#ff0000',
          }}>
            wiederholt sich
          </div>
        </div>
      </div>

      {/* Right — STRUDEL label */}
      <div style={{
        position: 'absolute',
        right: 120, top: 80,
        textAlign: 'right',
        opacity: sp(60),
        transform: `translateY(${(1 - sp(60)) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: '24px', color: '#9E9E9E',
          letterSpacing: '4px',
        }}>
          KEIN KREISLAUF
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: '140px', color: '#ff0000',
          lineHeight: 0.9, letterSpacing: '-3px',
          textShadow: '0 0 60px rgba(255,0,0,0.4)',
        }}>
          STRUDEL
        </div>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: '80px', fontWeight: 700,
          color: '#F5F5F5',
          marginTop: '8px',
        }}>
          12% p.a.
        </div>
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: '24px', color: '#9E9E9E',
          marginTop: '8px',
        }}>
          Deutsche Bank · Dispokredit
        </div>
      </div>

      {/* Stick figure — being sucked into vortex */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="100" height="160" style={{
          position: 'absolute',
          bottom: 100,
          left: `${Math.min(1350, 700 + frame * 4.5)}px`,
          opacity: interpolate(frame, [60, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `scale(${Math.max(0.2, 1 - (frame - 60) * 0.005)}) rotate(${frame * 2}deg)`,
        }}>
          <circle cx="50" cy="25" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="50" y1="43" x2="50" y2="105" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="50" y1="65" x2="25" y2="80" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="50" y1="65" x2="75" y2="80" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="50" y1="105" x2="32" y2="145" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="50" y1="105" x2="68" y2="145" stroke="#F5F5F5" strokeWidth="2" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: '22px',
        fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene9_DispoStrudel;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_EiserneReserve',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_EiserneReserve: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom into center
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Iron vault door reveal
  const vaultOpenAngle = interpolate(frame, [30, 70], [0, 90], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Gold glow pulse
  const goldGlow = Math.sin(frame * 0.1) * 20 + 80;

  // Step sequence
  const steps = [
    { label: '① EISERNE RESERVE', sub: '20.000 € zuerst', color: '#e9d700' },
    { label: '② DANN ETF-SPARPLAN', sub: 'Investieren kommt danach', color: '#F5F5F5' },
    { label: '③ DANN STRATEGIE', sub: 'Aktien · Immobilien · Mehr', color: '#9E9E9E' },
  ];

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 5 + 30) * 1920,
    y: seededRandom(i * 5 + 31) * 1080,
    size: seededRandom(i * 5 + 32) * 4 + 1,
    op: seededRandom(i * 5 + 33) * 0.2 + 0.05,
    speed: seededRandom(i * 5 + 34) * 0.5 + 0.12,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gold gradient — triumphant */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at 50% 50%, rgba(${goldGlow},${Math.round(goldGlow * 0.85)},0,0.18) 0%, #1C1C1C 55%)`,
        transform: `scale(${camScale})`,
      }} />

      {/* Shape layer — vault circle + iron frame */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {/* Vault circle */}
          <circle cx={340} cy={540} r={260}
            fill="#2D2D2D" stroke="#e9d700"
            strokeWidth="6" opacity={sp(5) * 0.9}
          />
          {/* Bolt details */}
          {[0, 60, 120, 180, 240, 300].map((angle) => {
            const rad = (angle * Math.PI) / 180;
            return (
              <circle key={angle}
                cx={340 + Math.cos(rad) * 220}
                cy={540 + Math.sin(rad) * 220}
                r={12}
                fill="#9E9E9E"
                opacity={sp(10) * 0.6}
              />
            );
          })}
          {/* Vault door opening */}
          <rect
            x={80} y={280}
            width={520} height={520}
            rx={260}
            fill="#1C1C1C"
            opacity={0.85}
            transform={`rotate(${-vaultOpenAngle}, 340, 540)`}
            style={{ transformOrigin: '340px 540px' }}
          />
          {/* 20.000 inside vault */}
          <text x="340" y="530" textAnchor="middle"
            fontFamily="JetBrains Mono, monospace"
            fontSize="72" fontWeight="700"
            fill="#e9d700"
            opacity={interpolate(frame, [65, 85], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          >
            20.000
          </text>
          <text x="340" y="590" textAnchor="middle"
            fontFamily="Anton" fontSize="36"
            fill="#F5F5F5"
            opacity={interpolate(frame, [70, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          >
            EURO
          </text>
        </svg>
      </AbsoluteFill>

      {/* Motion texture — golden sparkles */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.06 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 12 }, (_, i) => {
            const sparkX = 200 + seededRandom(i * 3 + 80) * 400;
            const sparkY = 200 + seededRandom(i * 3 + 81) * 680;
            const sparkSize = seededRandom(i * 3 + 82) * 6 + 2;
            return (
              <polygon key={i}
                points={`${sparkX},${sparkY - sparkSize} ${sparkX + sparkSize * 0.3},${sparkY - sparkSize * 0.3} ${sparkX + sparkSize},${sparkY} ${sparkX + sparkSize * 0.3},${sparkY + sparkSize * 0.3} ${sparkX},${sparkY + sparkSize} ${sparkX - sparkSize * 0.3},${sparkY + sparkSize * 0.3} ${sparkX - sparkSize},${sparkY} ${sparkX - sparkSize * 0.3},${sparkY - sparkSize * 0.3}`}
                fill="#e9d700"
                transform={`rotate(${frame * seededRandom(i + 90) * 3}, ${sparkX}, ${sparkY})`}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i}
              cx={p.x}
              cy={(p.y - frame * p.speed) % 1080}
              r={p.size} fill="#e9d700" opacity={p.op}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Right side — ordered steps */}
      <div style={{
        position: 'absolute',
        right: 100, top: '50%',
        transform: 'translateY(-50%)',
        width: '900px',
        display: 'flex', flexDirection: 'column',
        gap: '24px',
      }}>
        {/* Header */}
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: '24px', fontWeight: 300,
          color: '#9E9E9E', letterSpacing: '5px',
          textTransform: 'uppercase',
          opacity: sp(8),
          marginBottom: '8px',
        }}>
          Die richtige Reihenfolge
        </div>

        {steps.map((step, i) => (
          <div key={i} style={{
            opacity: sp(20 + i * 18),
            transform: `translateX(${(1 - sp(20 + i * 18)) * 60}px)`,
            borderLeft: i === 0 ? '5px solid #e9d700' : '2px solid #2D2D2D',
            paddingLeft: '24px',
          }}>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: i === 0 ? '80px' : '64px',
              color: step.color,
              letterSpacing: '-1px',
              lineHeight: 1,
              textShadow: i === 0 ? '0 0 40px rgba(233,215,0,0.3)' : 'none',
            }}>
              {step.label}
            </div>
            <div style={{
              fontFamily: 'Open Sans, sans-serif',
              fontSize: '24px', color: '#9E9E9E',
              fontStyle: 'italic', marginTop: '4px',
            }}>
              {step.sub}
            </div>
          </div>
        ))}

        {/* NIEMALS VORHER */}
        <div style={{
          marginTop: '12px',
          opacity: interpolate(frame, [80, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          borderTop: '2px solid #e9d700',
          paddingTop: '16px',
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: '52px',
            color: '#e9d700',
            letterSpacing: '3px',
          }}>
            ALLES ANDERE: DANACH — NIEMALS VORHER
          </div>
        </div>
      </div>

      {/* Stick figure — triumphant arms up */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="130" height="220" style={{
          position: 'absolute',
          bottom: 60, left: 280,
          opacity: sp(70),
          transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
        }}>
          <circle cx="65" cy="32" r="42" fill="#e9d700" opacity="0.12" />
          <circle cx="65" cy="32" r="24" fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.04) * 2}, 65, 32)`}
          />
          <rect x="49" y="27" width="14" height="10" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <rect x="67" y="27" width="14" height="10" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="63" y1="32" x2="67" y2="32" stroke="#e9d700" strokeWidth="2" />
          <line x1="65" y1="56" x2="65" y2="145" stroke="#F5F5F5" strokeWidth="3" />
          {/* Arms raised — victory */}
          <line x1="65" y1="80" x2="28" y2="45" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="28" y1="45" x2="20" y2="35" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="65" y1="80" x2="102" y2="45" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="102" y1="45" x2="110" y2="35" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="65" y1="145" x2="45" y2="195" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="65" y1="145" x2="85" y2="195" stroke="#F5F5F5" strokeWidth="3" />
          {/* Star above head */}
          <text x="48" y="10" fontFamily="Anton" fontSize="22" fill="#e9d700"
            opacity={interpolate(frame, [80, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          >★</text>
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: '22px',
        fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene10_EiserneReserve;
```

