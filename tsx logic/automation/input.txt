```tsx
import React from 'react';
Audio: "So picture this, the President of the United States standing in the Oval Office on June 10th, cameras rolling, reporters leaning in,"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'OvalOfficeOpener',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const OvalOfficeOpener: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08]);
  const camY = interpolate(frame, [0, durationInFrames], [0, -20]);

  // Text reveals
  const labelOp = sp(5);
  const mainOp = sp(20);
  const subOp = sp(45);
  const dateOp = sp(70);
  const lineOp = sp(90);

  // Pulse on date stamp
  const pulse = 1 + Math.sin(frame * 0.15) * 0.03;

  // Floating particles
  const particles = Array.from({ length: 22 }, (_, i) => {
    const seed = i + 1;
    const x = seededRandom(seed) * 1920;
    const y = seededRandom(seed * 2) * 1080;
    const drift = Math.sin(frame * 0.02 + i) * 15;
    const op = 0.15 + seededRandom(seed * 3) * 0.25;
    const size = 3 + seededRandom(seed * 4) * 6;
    return { x, y: y + drift, op, size };
  });

  // Shape layer floats
  const shape1Rot = frame * 0.15;
  const shape2Rot = -frame * 0.1;
  const shape3Y = Math.sin(frame * 0.025) * 25;

  // Scan line sweep
  const scanY = interpolate(frame % 90, [0, 90], [-100, 1180]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 30% 40%, #EEEEEE 0%, #DDDDDD 60%, #C8C8C8 100%)',
        }}
      />

      {/* Shape layers — transparent geometric depth */}
      <div
        style={{
          position: 'absolute',
          top: 120,
          left: -100,
          width: 600,
          height: 600,
          border: '3px solid #06202B',
          opacity: 0.06,
          transform: `rotate(${shape1Rot}deg)`,
          borderRadius: 40,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -200,
          right: -150,
          width: 700,
          height: 700,
          background: '#06202B',
          opacity: 0.04,
          transform: `rotate(${shape2Rot}deg)`,
          borderRadius: '50%',
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: 300 + shape3Y,
          right: 200,
          width: 0,
          height: 0,
          borderLeft: '180px solid transparent',
          borderRight: '180px solid transparent',
          borderBottom: '320px solid #06202B',
          opacity: 0.05,
        }}
      />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#06202B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid1)" />
      </svg>

      {/* Floating particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            background: '#06202B',
            opacity: p.op,
          }}
        />
      ))}

      {/* Scan sweep */}
      <div
        style={{
          position: 'absolute',
          left: 0,
          top: scanY,
          width: '100%',
          height: 2,
          background: 'linear-gradient(90deg, transparent, #06202B, transparent)',
          opacity: 0.25,
        }}
      />

      {/* Main content */}
      <div
        style={{
          position: 'absolute',
          inset: 0,
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'flex-start',
          padding: '0 140px',
          transform: `scale(${camScale}) translateY(${camY}px)`,
        }}
      >
        {/* Top label tag */}
        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            opacity: labelOp,
            transform: `translateX(${interpolate(labelOp, [0, 1], [-40, 0])}px)`,
            marginBottom: 40,
          }}
        >
          <div style={{ width: 80, height: 4, background: '#06202B' }} />
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 30,
              color: '#06202B',
              letterSpacing: 6,
              fontWeight: 700,
            }}
          >
            BREAKING / WHITE HOUSE
          </div>
        </div>

        {/* Hero typography */}
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 200,
            color: '#06202B',
            fontWeight: 900,
            lineHeight: 0.92,
            letterSpacing: -4,
            opacity: mainOp,
            transform: `translateY(${interpolate(mainOp, [0, 1], [60, 0])}px)`,
          }}
        >
          THE OVAL
        </div>
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 200,
            color: '#000000',
            fontWeight: 900,
            lineHeight: 0.92,
            letterSpacing: -4,
            opacity: subOp,
            transform: `translateY(${interpolate(subOp, [0, 1], [60, 0])}px)`,
            position: 'relative',
          }}
        >
          OFFICE.
          <span
            style={{
              position: 'absolute',
              bottom: 20,
              left: 0,
              width: lineOp * 580,
              height: 14,
              background: '#06202B',
            }}
          />
        </div>

        {/* Date pill */}
        <div
          style={{
            marginTop: 50,
            display: 'flex',
            alignItems: 'center',
            gap: 30,
            opacity: dateOp,
            transform: `scale(${pulse})`,
          }}
        >
          <div
            style={{
              padding: '18px 36px',
              background: '#06202B',
              color: '#EEEEEE',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 38,
              fontWeight: 800,
              letterSpacing: 3,
            }}
          >
            JUNE 10 · CAMERAS ROLLING
          </div>
          <div
            style={{
              width: 22,
              height: 22,
              borderRadius: '50%',
              background: '#ff0000',
              opacity: 0.6 + Math.sin(frame * 0.3) * 0.4,
            }}
          />
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              color: '#06202B',
              fontWeight: 700,
              letterSpacing: 4,
            }}
          >
            LIVE
          </div>
        </div>
      </div>

      {/* Corner brackets */}
      <div style={{ position: 'absolute', top: 60, left: 60, width: 60, height: 60, borderTop: '5px solid #06202B', borderLeft: '5px solid #06202B', opacity: labelOp }} />
      <div style={{ position: 'absolute', top: 60, right: 60, width: 60, height: 60, borderTop: '5px solid #06202B', borderRight: '5px solid #06202B', opacity: labelOp }} />
      <div style={{ position: 'absolute', bottom: 60, left: 60, width: 60, height: 60, borderBottom: '5px solid #06202B', borderLeft: '5px solid #06202B', opacity: labelOp }} />

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(6,32,43,0.18) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default OvalOfficeOpener;
```

```tsx
import React from 'react';
Audio: "and out of his mouth comes a sentence that should make every single American who pays a grocery bill stop scrolling and pay attention."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'GroceryBillAttention',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const GroceryBillAttention: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Receipt slides in
  const receiptOp = sp(10);
  const receiptY = interpolate(receiptOp, [0, 1], [80, 0]);
  const receiptRot = interpolate(receiptOp, [0, 1], [-8, -3]);

  // Items reveal one by one
  const item1 = sp(35);
  const item2 = sp(50);
  const item3 = sp(65);
  const item4 = sp(80);
  const totalOp = sp(100);

  // Headline text
  const headOp = sp(20);
  const wordOp1 = sp(110);
  const wordOp2 = sp(125);
  const wordOp3 = sp(140);

  // Stamp impact
  const stampScale = interpolate(sp(150, { damping: 8, stiffness: 200 }), [0, 1], [3, 1]);
  const stampOp = sp(150);
  const stampRot = interpolate(sp(150), [0, 1], [-25, -12]);

  // Camera shake on stamp
  const shake = frame > 150 && frame < 170 ? Math.sin(frame * 2) * 6 : 0;

  // Shape drift
  const blob1 = Math.sin(frame * 0.02) * 30;
  const blob2 = Math.cos(frame * 0.025) * 40;

  // Particles
  const particles = Array.from({ length: 18 }, (_, i) => {
    const seed = i + 7;
    const x = seededRandom(seed) * 1920;
    const y = (seededRandom(seed * 2) * 1080 + frame * 0.5) % 1080;
    return { x, y, op: 0.12 + seededRandom(seed * 3) * 0.18, size: 3 + seededRandom(seed * 5) * 5 };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp, transform: `translateX(${shake}px)` }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #EEEEEE 0%, #E0E0E0 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: 100 + blob1,
          left: 1300,
          width: 500,
          height: 500,
          borderRadius: '50%',
          background: '#06202B',
          opacity: 0.05,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -100 + blob2,
          left: -150,
          width: 600,
          height: 600,
          background: '#06202B',
          opacity: 0.04,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Dotted grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.12 }}>
        <defs>
          <pattern id="dotgrid" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="2" cy="2" r="1.5" fill="#06202B" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dotgrid)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            background: '#06202B',
            opacity: p.op,
            borderRadius: '50%',
          }}
        />
      ))}

      {/* Headline left */}
      <div
        style={{
          position: 'absolute',
          top: 140,
          left: 120,
          opacity: headOp,
          transform: `translateX(${interpolate(headOp, [0, 1], [-50, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 28,
            color: '#06202B',
            letterSpacing: 5,
            fontWeight: 700,
            marginBottom: 24,
          }}
        >
          ── EVERY AMERICAN HOUSEHOLD ──
        </div>
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 150,
            color: '#06202B',
            fontWeight: 900,
            lineHeight: 0.95,
            letterSpacing: -3,
          }}
        >
          <div style={{ opacity: wordOp1, transform: `translateY(${interpolate(wordOp1, [0, 1], [40, 0])}px)` }}>
            STOP
          </div>
          <div style={{ opacity: wordOp2, transform: `translateY(${interpolate(wordOp2, [0, 1], [40, 0])}px)`, color: '#000000' }}>
            SCROLLING.
          </div>
          <div style={{ opacity: wordOp3, transform: `translateY(${interpolate(wordOp3, [0, 1], [40, 0])}px)`, color: '#06202B' }}>
            PAY ATTENTION.
          </div>
        </div>
      </div>

      {/* Receipt mockup right */}
      <div
        style={{
          position: 'absolute',
          top: 180,
          right: 140,
          width: 460,
          background: '#FFFFFF',
          padding: '40px 36px',
          opacity: receiptOp,
          transform: `translateY(${receiptY}px) rotate(${receiptRot}deg)`,
          boxShadow: '0 25px 60px rgba(6,32,43,0.25)',
          fontFamily: 'JetBrains Mono, monospace',
        }}
      >
        <div
          style={{
            textAlign: 'center',
            fontSize: 28,
            fontWeight: 800,
            color: '#06202B',
            marginBottom: 8,
            letterSpacing: 2,
          }}
        >
          GROCERY RECEIPT
        </div>
        <div
          style={{
            textAlign: 'center',
            fontSize: 18,
            color: '#06202B',
            opacity: 0.6,
            marginBottom: 24,
          }}
        >
          - - - - - - - - - - - - - - - - -
        </div>

        {[
          { name: 'EGGS (DOZEN)', price: '$6.49', op: item1 },
          { name: 'MILK GALLON', price: '$5.29', op: item2 },
          { name: 'BREAD LOAF', price: '$4.99', op: item3 },
          { name: 'GROUND BEEF', price: '$8.79', op: item4 },
        ].map((row, i) => (
          <div
            key={i}
            style={{
              display: 'flex',
              justifyContent: 'space-between',
              fontSize: 26,
              fontWeight: 700,
              color: '#06202B',
              marginBottom: 16,
              opacity: row.op,
              transform: `translateX(${interpolate(row.op, [0, 1], [30, 0])}px)`,
            }}
          >
            <span>{row.name}</span>
            <span>{row.price}</span>
          </div>
        ))}

        <div
          style={{
            borderTop: '3px dashed #06202B',
            marginTop: 20,
            paddingTop: 20,
            display: 'flex',
            justifyContent: 'space-between',
            fontSize: 34,
            fontWeight: 900,
            color: '#000000',
            opacity: totalOp,
          }}
        >
          <span>TOTAL</span>
          <span>$25.56</span>
        </div>
      </div>

      {/* Impact stamp */}
      <div
        style={{
          position: 'absolute',
          top: 720,
          right: 220,
          padding: '24px 50px',
          border: '6px solid #ff0000',
          color: '#ff0000',
          fontFamily: 'Mont Heavy, Impact, sans-serif',
          fontSize: 70,
          fontWeight: 900,
          letterSpacing: 4,
          opacity: stampOp,
          transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
        }}
      >
        HURTS
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(6,32,43,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default GroceryBillAttention;
```

```tsx
import React from 'react';
Audio: "He looks at the room and says, I love the inflation. Four words, said with a grin, said while consumer prices in this country just hit a three-year high."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'ILoveTheInflation',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ILoveTheInflation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Quote marks
  const quoteOp = sp(5);

  // Four words stagger
  const w1 = sp(15, { damping: 10, stiffness: 150 });
  const w2 = sp(28, { damping: 10, stiffness: 150 });
  const w3 = sp(41, { damping: 10, stiffness: 150 });
  const w4 = sp(54, { damping: 10, stiffness: 150 });

  // Chart reveal
  const chartOp = sp(85);
  const barH = interpolate(sp(95), [0, 1], [0, 1]);

  // Three year tag
  const tagOp = sp(140);

  // Shake on highlight
  const shake = frame > 54 && frame < 70 ? Math.sin(frame * 3) * 4 : 0;

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  // Particles drifting up
  const particles = Array.from({ length: 25 }, (_, i) => {
    const seed = i + 11;
    const x = seededRandom(seed) * 1920;
    const y = (1080 - (frame * 1.2 + seededRandom(seed * 2) * 1080) % 1080);
    return { x, y, op: 0.1 + seededRandom(seed * 3) * 0.2, size: 2 + seededRandom(seed * 5) * 5 };
  });

  // Bar values (3-year CPI)
  const bars = [
    { label: "'22", h: 0.55 },
    { label: "'23", h: 0.45 },
    { label: "'24", h: 0.6 },
    { label: "'25", h: 0.85 },
    { label: "'26", h: 1.0 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp, transform: `translate(${shake}px, ${shake / 2}px) scale(${camScale})` }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 30%, #F2F2F2, #DDDDDD)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: -100,
          right: -100,
          width: 500,
          height: 500,
          borderRadius: '50%',
          background: '#06202B',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: 80,
          width: 300,
          height: 300,
          border: '4px solid #06202B',
          opacity: 0.07,
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      />

      {/* Line texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 40} x2="1920" y2={i * 40 + 20} stroke="#06202B" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            background: '#06202B',
            opacity: p.op,
            borderRadius: '50%',
          }}
        />
      ))}

      {/* Quote opening */}
      <div
        style={{
          position: 'absolute',
          top: 130,
          left: 140,
          fontFamily: 'Mont Heavy, Impact, sans-serif',
          fontSize: 280,
          color: '#06202B',
          opacity: quoteOp * 0.25,
          lineHeight: 0.8,
          transform: `scale(${quoteOp})`,
        }}
      >
        "
      </div>

      {/* The four words */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'baseline',
          gap: 40,
          flexWrap: 'nowrap',
        }}
      >
        {[
          { txt: 'I', op: w1, color: '#06202B' },
          { txt: 'LOVE', op: w2, color: '#000000' },
          { txt: 'THE', op: w3, color: '#06202B' },
          { txt: 'INFLATION.', op: w4, color: '#ff0000' },
        ].map((w, i) => (
          <div
            key={i}
            style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: w.txt === 'INFLATION.' ? 180 : 220,
              fontWeight: 900,
              color: w.color,
              letterSpacing: -4,
              opacity: w.op,
              transform: `translateY(${interpolate(w.op, [0, 1], [80, 0])}px) scale(${interpolate(w.op, [0, 1], [0.6, 1])})`,
            }}
          >
            {w.txt}
          </div>
        ))}
      </div>

      {/* 3-year high chart */}
      <div
        style={{
          position: 'absolute',
          bottom: 120,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'flex-end',
          gap: 30,
          opacity: chartOp,
          transform: `translateY(${interpolate(chartOp, [0, 1], [50, 0])}px)`,
        }}
      >
        {bars.map((b, i) => {
          const localH = interpolate(sp(95 + i * 8), [0, 1], [0, b.h]);
          return (
            <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12 }}>
              <div
                style={{
                  width: 90,
                  height: 280 * localH,
                  background: i === bars.length - 1 ? '#ff0000' : '#06202B',
                  opacity: i === bars.length - 1 ? 1 : 0.75,
                }}
              />
              <div
                style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 28,
                  color: '#06202B',
                  fontWeight: 700,
                }}
              >
                {b.label}
              </div>
            </div>
          );
        })}
      </div>

      {/* 3-year high tag */}
      <div
        style={{
          position: 'absolute',
          bottom: 240,
          right: 180,
          padding: '20px 38px',
          background: '#06202B',
          color: '#EEEEEE',
          fontFamily: 'Mont Heavy, Impact, sans-serif',
          fontSize: 48,
          fontWeight: 900,
          letterSpacing: 2,
          opacity: tagOp,
          transform: `scale(${tagOp}) rotate(-4deg)`,
        }}
      >
        3-YEAR HIGH
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(6,32,43,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default ILoveTheInflation;
```

```tsx
import React from 'react';
Audio: "And I'm sitting here in my studio thinking, are we living in the same economy? Because the family I talked to last week in Ohio is rationing eggs."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'OhioRationingEggs',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const OhioRationingEggs: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Split panel reveal
  const leftOp = sp(5);
  const rightOp = sp(20);

  const qOp = sp(35);
  const locOp = sp(60);
  const cartonOp = sp(75);
  const stampOp = sp(115);

  // Egg counter animation
  const eggCount = Math.floor(interpolate(sp(90), [0, 1], [12, 4]));

  // Pulse
  const pulse = 1 + Math.sin(frame * 0.18) * 0.04;

  // Floating dots
  const particles = Array.from({ length: 20 }, (_, i) => {
    const seed = i + 19;
    const x = seededRandom(seed) * 1920;
    const y = seededRandom(seed * 2) * 1080;
    const drift = Math.sin(frame * 0.02 + i) * 20;
    return { x, y: y + drift, op: 0.1 + seededRandom(seed * 3) * 0.18, size: 3 + seededRandom(seed * 5) * 5 };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(120deg, #EEEEEE 0%, #E2E2E2 100%)',
        }}
      />

      {/* Split divider */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: 80,
          bottom: 80,
          width: 3,
          background: '#06202B',
          opacity: 0.15,
          transform: `translateX(-50%) scaleY(${sp(40)})`,
          transformOrigin: 'top',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: -150,
          left: -150,
          width: 600,
          height: 600,
          borderRadius: '50%',
          background: '#06202B',
          opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -200,
          right: -100,
          width: 700,
          height: 700,
          border: '4px solid #06202B',
          opacity: 0.05,
          transform: `rotate(${frame * 0.1}deg)`,
          borderRadius: 30,
        }}
      />

      {/* Texture grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="gridA" width="60" height="60" patternUnits="userSpaceOnUse">
            <circle cx="30" cy="30" r="1" fill="#06202B" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#gridA)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            background: '#06202B',
            opacity: p.op,
            borderRadius: '50%',
          }}
        />
      ))}

      {/* LEFT — Question */}
      <div
        style={{
          position: 'absolute',
          top: 180,
          left: 100,
          width: 760,
          opacity: leftOp,
          transform: `translateX(${interpolate(leftOp, [0, 1], [-60, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 30,
            color: '#06202B',
            letterSpacing: 5,
            fontWeight: 700,
            marginBottom: 30,
            opacity: qOp,
          }}
        >
          THE STUDIO QUESTION
        </div>
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 130,
            color: '#06202B',
            fontWeight: 900,
            lineHeight: 0.95,
            letterSpacing: -3,
            opacity: qOp,
          }}
        >
          ARE WE
        </div>
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 130,
            color: '#000000',
            fontWeight: 900,
            lineHeight: 0.95,
            letterSpacing: -3,
            opacity: qOp,
          }}
        >
          LIVING IN
        </div>
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 130,
            color: '#06202B',
            fontWeight: 900,
            lineHeight: 0.95,
            letterSpacing: -3,
            opacity: qOp,
          }}
        >
          THE SAME
        </div>
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 140,
            color: '#ff0000',
            fontWeight: 900,
            lineHeight: 0.95,
            letterSpacing: -3,
            opacity: qOp,
            position: 'relative',
            display: 'inline-block',
          }}
        >
          ECONOMY?
        </div>
      </div>

      {/* RIGHT — Ohio family case */}
      <div
        style={{
          position: 'absolute',
          top: 160,
          right: 100,
          width: 780,
          opacity: rightOp,
          transform: `translateX(${interpolate(rightOp, [0, 1], [60, 0])}px)`,
        }}
      >
        {/* Location badge */}
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 16,
            padding: '14px 30px',
            background: '#06202B',
            color: '#EEEEEE',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 28,
            fontWeight: 800,
            letterSpacing: 4,
            opacity: locOp,
            transform: `translateY(${interpolate(locOp, [0, 1], [-20, 0])}px)`,
          }}
        >
          OHIO · FAMILY OF 4
        </div>

        {/* Carton visualization */}
        <div
          style={{
            marginTop: 50,
            padding: 40,
            background: '#FFFFFF',
            border: '4px solid #06202B',
            opacity: cartonOp,
            transform: `translateY(${interpolate(cartonOp, [0, 1], [40, 0])}px) scale(${pulse})`,
            boxShadow: '0 20px 50px rgba(6,32,43,0.2)',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 24,
              color: '#06202B',
              fontWeight: 700,
              marginBottom: 20,
              letterSpacing: 3,
            }}
          >
            EGGS THIS WEEK
          </div>

          {/* Egg grid */}
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(6, 1fr)', gap: 14 }}>
            {Array.from({ length: 12 }).map((_, i) => {
              const used = i >= eggCount;
              return (
                <div
                  key={i}
                  style={{
                    width: 70,
                    height: 90,
                    borderRadius: '50% / 60%',
                    background: used ? 'transparent' : '#06202B',
                    border: '4px solid #06202B',
                    opacity: used ? 0.2 : 1,
                  }}
                />
              );
            })}
          </div>

          <div
            style={{
              marginTop: 28,
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'baseline',
              fontFamily: 'Mont Heavy, Impact, sans-serif',
            }}
          >
            <div style={{ fontSize: 44, color: '#06202B', fontWeight: 900 }}>{eggCount} LEFT</div>
            <div style={{ fontSize: 28, color: '#ff0000', fontWeight: 900, letterSpacing: 3 }}>RATIONING</div>
          </div>
        </div>
      </div>

      {/* Bottom stamp */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: '50%',
          transform: `translateX(-50%) scale(${stampOp})`,
          padding: '16px 40px',
          background: '#06202B',
          color: '#EEEEEE',
          fontFamily: 'Mont Heavy, Impact, sans-serif',
          fontSize: 38,
          fontWeight: 900,
          letterSpacing: 4,
          opacity: stampOp,
        }}
      >
        TWO AMERICAS · ONE ECONOMY
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(6,32,43,0.18) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default OhioRationingEggs;
```

```tsx
import React from 'react';
Audio: "The retired couple in Arizona just told me their property tax bill went up another 9%."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'ArizonaPropertyTax',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ArizonaPropertyTax: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headOp = sp(10);
  const houseOp = sp(30);
  const docOp = sp(55);
  const arrowOp = sp(95);
  const finalOp = sp(125);

  // Counter
  const counter = interpolate(sp(100), [0, 1], [0, 9]);

  // House tilt
  const houseTilt = Math.sin(frame * 0.04) * 1.5;

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  // Particles (money/dust falling)
  const particles = Array.from({ length: 30 }, (_, i) => {
    const seed = i + 23;
    const x = seededRandom(seed) * 1920;
    const fall = ((frame * 2 + seededRandom(seed * 2) * 1080) % 1200) - 100;
    return { x, y: fall, op: 0.12 + seededRandom(seed * 3) * 0.2, size: 4 + seededRandom(seed * 5) * 6 };
  });

  // Shake on final
  const shake = frame > 125 && frame < 145 ? Math.sin(frame * 2.5) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale}) translateX(${shake}px)` }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 70% 60%, #F0F0F0, #D8D8D8)',
        }}
      />

      {/* Shape layers — sun shapes for Arizona */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          right: 200,
          width: 400,
          height: 400,
          borderRadius: '50%',
          background: '#06202B',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -100,
          left: -100,
          width: 500,
          height: 500,
          background: '#06202B',
          opacity: 0.04,
          transform: `rotate(45deg) rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 40 }).map((_, i) => (
          <line key={i} x1={i * 60 - 200} y1="0" x2={i * 60 + 200} y2="1080" stroke="#06202B" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            background: '#06202B',
            opacity: p.op,
            borderRadius: '50%',
          }}
        />
      ))}

      {/* Top tag */}
      <div
        style={{
          position: 'absolute',
          top: 90,
          left: 140,
          display: 'flex',
          alignItems: 'center',
          gap: 20,
          opacity: headOp,
          transform: `translateX(${interpolate(headOp, [0, 1], [-40, 0])}px)`,
        }}
      >
        <div style={{ width: 80, height: 4, background: '#06202B' }} />
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 28,
            color: '#06202B',
            letterSpacing: 5,
            fontWeight: 700,
          }}
        >
          ARIZONA · RETIRED COUPLE
        </div>
      </div>

      {/* Headline */}
      <div
        style={{
          position: 'absolute',
          top: 160,
          left: 140,
          fontFamily: 'Mont Heavy, Impact, sans-serif',
          fontSize: 130,
          color: '#06202B',
          fontWeight: 900,
          lineHeight: 0.95,
          letterSpacing: -3,
          opacity: headOp,
          transform: `translateY(${interpolate(headOp, [0, 1], [40, 0])}px)`,
        }}
      >
        PROPERTY
        <br />
        <span style={{ color: '#000000' }}>TAX BILL</span>
      </div>

      {/* House icon SVG */}
      <div
        style={{
          position: 'absolute',
          left: 200,
          bottom: 140,
          opacity: houseOp,
          transform: `translateY(${interpolate(houseOp, [0, 1], [80, 0])}px) rotate(${houseTilt}deg)`,
        }}
      >
        <svg width="380" height="320" viewBox="0 0 380 320">
          {/* roof */}
          <polygon points="40,160 190,40 340,160" fill="#06202B" />
          {/* body */}
          <rect x="80" y="160" width="220" height="140" fill="#000000" />
          {/* door */}
          <rect x="160" y="210" width="60" height="90" fill="#06202B" />
          {/* windows */}
          <rect x="100" y="180" width="44" height="44" fill="#06202B" />
          <rect x="236" y="180" width="44" height="44" fill="#06202B" />
          {/* chimney */}
          <rect x="260" y="60" width="35" height="60" fill="#06202B" />
        </svg>
      </div>

      {/* Tax document */}
      <div
        style={{
          position: 'absolute',
          right: 160,
          top: 380,
          width: 540,
          background: '#FFFFFF',
          padding: 40,
          opacity: docOp,
          transform: `translateY(${interpolate(docOp, [0, 1], [60, 0])}px) rotate(2deg)`,
          boxShadow: '0 25px 60px rgba(6,32,43,0.25)',
          fontFamily: 'JetBrains Mono, monospace',
        }}
      >
        <div style={{ fontSize: 22, color: '#06202B', letterSpacing: 3, fontWeight: 700, marginBottom: 14 }}>
          ── TAX NOTICE 2026 ──
        </div>
        <div style={{ fontSize: 30, color: '#06202B', fontWeight: 800, marginBottom: 20 }}>
          PROPERTY ASSESSMENT
        </div>

        <div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 26, color: '#06202B', marginBottom: 12 }}>
          <span>Previous Year</span>
          <span style={{ fontWeight: 800 }}>$4,820</span>
        </div>
        <div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 26, color: '#06202B', marginBottom: 20 }}>
          <span>This Year</span>
          <span style={{ fontWeight: 800, color: '#ff0000' }}>$5,254</span>
        </div>

        <div
          style={{
            borderTop: '3px dashed #06202B',
            paddingTop: 20,
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
          }}
        >
          <span style={{ fontSize: 28, color: '#06202B', fontWeight: 800 }}>INCREASE</span>
          <span
            style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 70,
              color: '#ff0000',
              fontWeight: 900,
            }}
          >
            +{counter.toFixed(1)}%
          </span>
        </div>
      </div>

      {/* Arrow up */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: 540,
          transform: `translateX(-50%) scale(${arrowOp})`,
          opacity: arrowOp,
        }}
      >
        <svg width="100" height="160" viewBox="0 0 100 160">
          <path d="M 50 10 L 90 70 L 65 70 L 65 150 L 35 150 L 35 70 L 10 70 Z" fill="#ff0000" />
        </svg>
      </div>

      {/* Bottom stamp */}
      <div
        style={{
          position: 'absolute',
          bottom: 60,
          left: '50%',
          transform: `translateX(-50%) scale(${finalOp})`,
          opacity: finalOp,
          padding: '18px 50px',
          background: '#06202B',
          color: '#EEEEEE',
          fontFamily: 'Mont Heavy, Impact, sans-serif',
          fontSize: 38,
          fontWeight: 900,
          letterSpacing: 4,
        }}
      >
        RETIREMENT GETS HARDER
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(6,32,43,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default ArizonaPropertyTax;
```

```tsx
import React from 'react';
Audio: "And the 26-year-old kid who DM'd me on failed finance last night said his rent in Phoenix jumped 400 bucks in 18 months."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'PhoenixRentJump',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const PhoenixRentJump: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // DM bubble
  const dmOp = sp(10);

  // Chart line growth
  const chartOp = sp(40);
  const lineProgress = interpolate(sp(50), [0, 1], [0, 1]);

  // Numbers
  const startRent = 1450;
  const endRent = 1850;
  const counter = interpolate(sp(85), [0, 1], [startRent, endRent]);

  const labelOp = sp(110);

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.06]);

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => {
    const seed = i + 31;
    const x = seededRandom(seed) * 1920;
    const y = seededRandom(seed * 2) * 1080;
    const drift = Math.sin(frame * 0.02 + i) * 18;
    return { x, y: y + drift, op: 0.1 + seededRandom(seed * 3) * 0.2, size: 3 + seededRandom(seed * 5) * 5 };
  });

  // Chart values
  const chartW = 900;
  const chartH = 380;
  const chartX = 880;
  const chartY = 380;

  // Line path - upward curve over 18 months
  const points = Array.from({ length: 19 }, (_, i) => {
    const px = (i / 18) * chartW;
    const ratio = i / 18;
    const py = chartH - (ratio * ratio * 0.7 + ratio * 0.3) * chartH * 0.85;
    return { x: px, y: py };
  });

  const visiblePoints = Math.floor(points.length * lineProgress);
  const pathD = points
    .slice(0, visiblePoints + 1)
    .map((p, i) => `${i === 0 ? 'M' : 'L'} ${p.x} ${p.y}`)
    .join(' ');

  const currentPoint = points[Math.min(visiblePoints, points.length - 1)] || points[0];

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale})` }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(160deg, #EEEEEE, #DDDDDD)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: -150,
          left: 200,
          width: 500,
          height: 500,
          borderRadius: '50%',
          background: '#06202B',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.07})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -100,
          right: 100,
          width: 400,
          height: 400,
          border: '5px solid #06202B',
          opacity: 0.06,
          transform: `rotate(${frame * 0.12}deg)`,
          borderRadius: 30,
        }}
      />

      {/* Background dots */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="dotsP" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#06202B" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dotsP)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            background: '#06202B',
            opacity: p.op,
            borderRadius: '50%',
          }}
        />
      ))}

      {/* LEFT — DM bubble */}
      <div
        style={{
          position: 'absolute',
          top: 180,
          left: 110,
          width: 700,
          opacity: dmOp,
          transform: `translateX(${interpolate(dmOp, [0, 1], [-60, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#06202B',
            letterSpacing: 4,
            fontWeight: 700,
            marginBottom: 24,
          }}
        >
          ── DIRECT MESSAGE ──
        </div>

        <div
          style={{
            background: '#06202B',
            color: '#EEEEEE',
            padding: '40px 44px',
            borderRadius: '40px 40px 40px 8px',
            fontFamily: 'Inter, sans-serif',
            fontSize: 38,
            fontWeight: 600,
            lineHeight: 1.4,
            position: 'relative',
            boxShadow: '0 20px 50px rgba(6,32,43,0.3)',
          }}
        >
          "Bro my rent in Phoenix jumped 400 bucks in 18 months. I'm 26. I can't keep up."
        </div>

        <div
          style={{
            marginTop: 24,
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#06202B',
            opacity: 0.7,
            letterSpacing: 2,
          }}
        >
          @phoenix_kid · 2:47 AM
        </div>

        {/* Big number */}
        <div
          style={{
            marginTop: 40,
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 90,
            color: '#06202B',
            fontWeight: 900,
            letterSpacing: -2,
            lineHeight: 1,
          }}
        >
          AGE
          <span style={{ color: '#ff0000', marginLeft: 24 }}>26</span>
        </div>
      </div>

      {/* RIGHT — Chart */}
      <div
        style={{
          position: 'absolute',
          top: chartY - 80,
          left: chartX,
          width: chartW + 100,
          opacity: chartOp,
          transform: `translateY(${interpolate(chartOp, [0, 1], [40, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#06202B',
            letterSpacing: 3,
            fontWeight: 700,
            marginBottom: 14,
          }}
        >
          PHOENIX · MONTHLY RENT
        </div>

        <svg width={chartW} height={chartH} style={{ overflow: 'visible' }}>
          {/* Y axis labels */}
          <text x="-70" y="20" fill="#06202B" fontSize="22" fontFamily="JetBrains Mono">$1900</text>
          <text x="-70" y={chartH / 2} fill="#06202B" fontSize="22" fontFamily="JetBrains Mono">$1650</text>
          <text x="-70" y={chartH - 10} fill="#06202B" fontSize="22" fontFamily="JetBrains Mono">$1400</text>

          {/* Grid */}
          {[0, 1, 2, 3].map((i) => (
            <line key={i} x1="0" y1={(i / 3) * chartH} x2={chartW} y2={(i / 3) * chartH} stroke="#06202B" strokeWidth="1" opacity="0.15" />
          ))}

          {/* Area under line */}
          {visiblePoints > 0 && (
            <path
              d={`${pathD} L ${currentPoint.x} ${chartH} L 0 ${chartH} Z`}
              fill="#ff0000"
              opacity="0.15"
            />
          )}

          {/* Line */}
          <path d={pathD} fill="none" stroke="#ff0000" strokeWidth="7" strokeLinecap="round" strokeLinejoin="round" />

          {/* End dot */}
          <circle cx={currentPoint.x} cy={currentPoint.y} r="14" fill="#ff0000" />
          <circle cx={currentPoint.x} cy={currentPoint.y} r="22" fill="none" stroke="#ff0000" strokeWidth="3" opacity={0.6 + Math.sin(frame * 0.2) * 0.4} />

          {/* X axis */}
          <line x1="0" y1={chartH} x2={chartW} y2={chartH} stroke="#06202B" strokeWidth="3" />
        </svg>

        {/* Counter */}
        <div
          style={{
            marginTop: 30,
            display: 'flex',
            alignItems: 'baseline',
            gap: 30,
          }}
        >
          <div
            style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 130,
              color: '#06202B',
              fontWeight: 900,
              letterSpacing: -3,
            }}
          >
            ${Math.floor(counter)}
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 70,
              color: '#ff0000',
              fontWeight: 900,
              opacity: labelOp,
            }}
          >
            +$400 / 18 MO
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(6,32,43,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default PhoenixRentJump;
```

```tsx
import React from 'react';
Audio: "And the man at the top of the food chain is telling us he loves it."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'TopOfFoodChain',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const TopOfFoodChain: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pyramid layer reveals — bottom to top
  const layer1 = sp(10);
  const layer2 = sp(25);
  const layer3 = sp(40);
  const layer4 = sp(55);
  const layer5 = sp(70);
  const crownOp = sp(90);
  const quoteOp = sp(115);
  const finalOp = sp(150);

  // Camera pan up
  const camY = interpolate(frame, [0, durationInFrames], [40, -30]);

  // Pulse on crown
  const crownPulse = 1 + Math.sin(frame * 0.2) * 0.05;

  // Particles
  const particles = Array.from({ length: 24 }, (_, i) => {
    const seed = i + 37;
    const x = seededRandom(seed) * 1920;
    const y = seededRandom(seed * 2) * 1080;
    const drift = Math.sin(frame * 0.025 + i) * 22;
    return { x, y: y + drift, op: 0.1 + seededRandom(seed * 3) * 0.2, size: 3 + seededRandom(seed * 5) * 6 };
  });

  // Pyramid layer config
  const pyramidLayers = [
    { width: 800, label: 'WORKING FAMILIES', op: layer1, y: 0 },
    { width: 680, label: 'MIDDLE CLASS', op: layer2, y: -90 },
    { width: 540, label: 'UPPER MIDDLE', op: layer3, y: -180 },
    { width: 380, label: 'WEALTHY', op: layer4, y: -270 },
    { width: 220, label: 'ELITE', op: layer5, y: -360 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 80%, #EEEEEE 0%, #CDCDCD 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          left: -100,
          width: 500,
          height: 500,
          borderRadius: '50%',
          background: '#06202B',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -100,
          right: -100,
          width: 600,
          height: 600,
          border: '5px solid #06202B',
          opacity: 0.05,
          transform: `rotate(${frame * 0.1}deg)`,
          borderRadius: 30,
        }}
      />

      {/* Concentric circles texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <circle key={i} cx="960" cy="720" r={150 + i * 90} fill="none" stroke="#06202B" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            background: '#06202B',
            opacity: p.op,
            borderRadius: '50%',
          }}
        />
      ))}

      {/* Top label */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 30,
          color: '#06202B',
          letterSpacing: 6,
          fontWeight: 700,
          opacity: sp(5),
        }}
      >
        ── THE FOOD CHAIN ──
      </div>

      {/* Pyramid stack */}
      <div
        style={{
          position: 'absolute',
          left: 0,
          right: 0,
          bottom: 200,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          transform: `translateY(${camY}px)`,
        }}
      >
        {pyramidLayers.map((l, i) => (
          <div
            key={i}
            style={{
              width: l.width,
              height: 80,
              background: i === 4 ? '#06202B' : '#000000',
              opacity: l.op * (i === 4 ? 1 : 0.85 - i * 0.05),
              marginBottom: 6,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: i === 4 ? 32 : 36,
              fontWeight: 900,
              color: '#EEEEEE',
              letterSpacing: 3,
              transform: `translateY(${interpolate(l.op, [0, 1], [40, 0])}px)`,
              boxShadow: i === 4 ? '0 0 60px rgba(6,32,43,0.5)' : 'none',
            }}
          >
            {l.label}
          </div>
        ))}

        {/* Crown / pointer */}
        <div
          style={{
            marginTop: 20,
            opacity: crownOp,
            transform: `scale(${crownPulse}) translateY(${interpolate(crownOp, [0, 1], [-30, 0])}px)`,
          }}
        >
          <svg width="120" height="100" viewBox="0 0 120 100">
            <polygon points="60,5 80,40 115,40 88,62 98,95 60,75 22,95 32,62 5,40 40,40" fill="#ff0000" stroke="#06202B" strokeWidth="3" />
          </svg>
        </div>
      </div>

      {/* The quote — right side */}
      <div
        style={{
          position: 'absolute',
          top: 200,
          right: 100,
          width: 600,
          opacity: quoteOp,
          transform: `translateX(${interpolate(quoteOp, [0, 1], [60, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#06202B',
            letterSpacing: 4,
            fontWeight: 700,
            marginBottom: 24,
          }}
        >
          HE SAYS:
        </div>
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 140,
            color: '#06202B',
            fontWeight: 900,
            lineHeight: 0.92,
            letterSpacing: -3,
          }}
        >
          "I LOVE
        </div>
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 140,
            color: '#ff0000',
            fontWeight: 900,
            lineHeight: 0.92,
            letterSpacing: -3,
          }}
        >
          IT."
        </div>
      </div>

      {/* Bottom stamp */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${finalOp})`,
          opacity: finalOp,
          padding: '18px 48px',
          background: '#06202B',
          color: '#EEEEEE',
          fontFamily: 'Mont Heavy, Impact, sans-serif',
          fontSize: 36,
          fontWeight: 900,
          letterSpacing: 4,
        }}
      >
        WHO ACTUALLY BENEFITS?
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(6,32,43,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default TopOfFoodChain;
```

```tsx
import React from 'react';
Audio: "I'm Kunal, and if you've been around this channel for a while, you already know I don't do the cable news circus. I do the cold, hard math."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'ColdHardMath',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ColdHardMath: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Name card
  const nameOp = sp(5);

  // Cross out cable
  const cableOp = sp(35);
  const crossOp = interpolate(sp(60), [0, 1], [0, 1]);

  // Math reveal
  const mathOp = sp(85);

  // Equation parts
  const eq1 = sp(105);
  const eq2 = sp(120);
  const eq3 = sp(135);

  // Stamp
  const stampOp = sp(155);

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  // Particles — code-like floating
  const particles = Array.from({ length: 28 }, (_, i) => {
    const seed = i + 43;
    const x = seededRandom(seed) * 1920;
    const y = (seededRandom(seed * 2) * 1080 + frame * 0.4) % 1080;
    return { x, y, op: 0.1 + seededRandom(seed * 3) * 0.2, size: 4 + seededRandom(seed * 5) * 6 };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale})` }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(150deg, #EEEEEE 0%, #DEDEDE 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: -150,
          right: 150,
          width: 500,
          height: 500,
          background: '#06202B',
          opacity: 0.05,
          transform: `rotate(${frame * 0.1}deg) rotate(45deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -100,
          left: 100,
          width: 400,
          height: 400,
          borderRadius: '50%',
          border: '5px solid #06202B',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }}
      />

      {/* Mono code texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="codeGrid" width="100" height="50" patternUnits="userSpaceOnUse">
            <text x="0" y="35" fontFamily="JetBrains Mono" fontSize="16" fill="#06202B">01</text>
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#codeGrid)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            background: '#06202B',
            opacity: p.op,
            borderRadius: 2,
          }}
        />
      ))}

      {/* Name intro */}
      <div
        style={{
          position: 'absolute',
          top: 110,
          left: 140,
          opacity: nameOp,
          transform: `translateX(${interpolate(nameOp, [0, 1], [-50, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 30,
            color: '#06202B',
            letterSpacing: 6,
            fontWeight: 700,
            marginBottom: 20,
          }}
        >
          ── HOST ──
        </div>
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 220,
            color: '#06202B',
            fontWeight: 900,
            lineHeight: 0.9,
            letterSpacing: -5,
          }}
        >
          I'M KUNAL.
        </div>
      </div>

      {/* Cable news crossed out */}
      <div
        style={{
          position: 'absolute',
          top: 470,
          left: 140,
          opacity: cableOp,
          transform: `translateY(${interpolate(cableOp, [0, 1], [40, 0])}px)`,
        }}
      >
        <div style={{ position: 'relative', display: 'inline-block' }}>
          <div
            style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 130,
              color: '#06202B',
              fontWeight: 900,
              letterSpacing: -2,
              opacity: 0.45,
            }}
          >
            CABLE NEWS CIRCUS
          </div>
          {/* Cross line */}
          <div
            style={{
              position: 'absolute',
              left: 0,
              top: '50%',
              width: `${crossOp * 100}%`,
              height: 14,
              background: '#ff0000',
              transform: 'translateY(-50%) rotate(-3deg)',
              transformOrigin: 'left center',
            }}
          />
        </div>
      </div>

      {/* Cold hard math — equation strip */}
      <div
        style={{
          position: 'absolute',
          top: 660,
          left: 140,
          right: 140,
          opacity: mathOp,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 28,
            color: '#06202B',
            letterSpacing: 4,
            fontWeight: 700,
            marginBottom: 20,
          }}
        >
          INSTEAD ──▶ COLD HARD MATH
        </div>

        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: 30,
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 90,
            fontWeight: 800,
            color: '#06202B',
            flexWrap: 'wrap',
          }}
        >
          <div style={{ opacity: eq1, transform: `translateY(${interpolate(eq1, [0, 1], [30, 0])}px)`, background: '#06202B', color: '#EEEEEE', padding: '10px 30px' }}>
            DATA
          </div>
          <div style={{ opacity: eq1 }}>+</div>
          <div style={{ opacity: eq2, transform: `translateY(${interpolate(eq2, [0, 1], [30, 0])}px)`, background: '#000000', color: '#EEEEEE', padding: '10px 30px' }}>
            RECEIPTS
          </div>
          <div style={{ opacity: eq2 }}>=</div>
          <div style={{ opacity: eq3, transform: `translateY(${interpolate(eq3, [0, 1], [30, 0])}px)`, color: '#ff0000', borderBottom: '8px solid #ff0000' }}>
            TRUTH
          </div>
        </div>
      </div>

      {/* Stamp */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          right: 140,
          opacity: stampOp,
          transform: `scale(${stampOp}) rotate(-6deg)`,
          padding: '20px 50px',
          border: '5px solid #06202B',
          fontFamily: 'Mont Heavy, Impact, sans-serif',
          fontSize: 44,
          fontWeight: 900,
          color: '#06202B',
          letterSpacing: 3,
        }}
      >
        NO CIRCUS · ONLY MATH
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(6,32,43,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default ColdHardMath;
```

```tsx
import React from 'react';
Audio: "And today, the math is screaming. Because that single sentence from the Oval Office isn't just a soundbite."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'MathScreaming',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const MathScreaming: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Sound wave bars
  const wavesOp = sp(10);

  // Main word
  const screamOp = sp(25, { damping: 8, stiffness: 180 });

  // Soundbite vs window
  const sbOp = sp(85);
  const windowOp = sp(125);

  // Camera shake — math screaming
  const shake = frame > 25 && frame < 65 ? Math.sin(frame * 1.8) * 5 : 0;

  // Scream scale pulse
  const screamPulse = 1 + Math.sin(frame * 0.25) * 0.04;

  // Wave bar heights
  const waves = Array.from({ length: 50 }, (_, i) => {
    const h = 40 + Math.abs(Math.sin(frame * 0.15 + i * 0.4)) * 220;
    return h;
  });

  // Particles
  const particles = Array.from({ length: 30 }, (_, i) => {
    const seed = i + 49;
    const x = seededRandom(seed) * 1920;
    const y = seededRandom(seed * 2) * 1080;
    const drift = Math.sin(frame * 0.03 + i) * 25;
    return { x, y: y + drift, op: 0.1 + seededRandom(seed * 3) * 0.22, size: 3 + seededRandom(seed * 5) * 6 };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp, transform: `translate(${shake}px, ${shake / 2}px)` }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, #EEEEEE, #CCCCCC)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          left: 100,
          width: 400,
          height: 400,
          borderRadius: '50%',
          background: '#ff0000',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.15) * 0.1})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          right: 80,
          width: 500,
          height: 500,
          border: '6px solid #06202B',
          opacity: 0.06,
          transform: `rotate(${frame * 0.15}deg)`,
          borderRadius: 40,
        }}
      />

      {/* Concentric radial pulse */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        {[0, 1, 2, 3, 4].map((i) => {
          const r = (frame * 3 + i * 80) % 600;
          return <circle key={i} cx="960" cy="540" r={r} fill="none" stroke="#ff0000" strokeWidth="2" opacity={1 - r / 600} />;
        })}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            background: '#06202B',
            opacity: p.op,
            borderRadius: '50%',
          }}
        />
      ))}

      {/* Top label */}
      <div
        style={{
          position: 'absolute',
          top: 90,
          left: 0,
          right: 0,
          textAlign: 'center',
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 30,
          color: '#06202B',
          letterSpacing: 6,
          fontWeight: 700,
          opacity: sp(5),
        }}
      >
        TODAY, THE MATH IS . . .
      </div>

      {/* SCREAMING */}
      <div
        style={{
          position: 'absolute',
          top: 200,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: screamOp,
          transform: `scale(${screamOp * screamPulse})`,
        }}
      >
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 340,
            color: '#ff0000',
            fontWeight: 900,
            letterSpacing: -8,
            lineHeight: 0.9,
            WebkitTextStroke: '4px #06202B',
          }}
        >
          SCREAMING
        </div>
      </div>

      {/* Sound waves below */}
      <div
        style={{
          position: 'absolute',
          top: 560,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 8,
          height: 240,
          opacity: wavesOp,
        }}
      >
        {waves.map((h, i) => (
          <div
            key={i}
            style={{
              width: 10,
              height: h,
              background: i % 3 === 0 ? '#ff0000' : '#06202B',
              borderRadius: 4,
              opacity: 0.85,
            }}
          />
        ))}
      </div>

      {/* Comparison strip */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          gap: 50,
          padding: '0 100px',
        }}
      >
        {/* Not a soundbite */}
        <div
          style={{
            opacity: sbOp,
            transform: `translateX(${interpolate(sbOp, [0, 1], [-40, 0])}px)`,
            padding: '24px 40px',
            border: '5px solid #06202B',
            position: 'relative',
          }}
        >
          <div
            style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 56,
              color: '#06202B',
              fontWeight: 900,
              letterSpacing: 2,
              opacity: 0.5,
            }}
          >
            JUST A SOUNDBITE
          </div>
          <div
            style={{
              position: 'absolute',
              left: 0,
              top: '50%',
              width: '100%',
              height: 8,
              background: '#ff0000',
              transform: 'translateY(-50%) rotate(-4deg)',
            }}
          />
        </div>

        {/* It's a window */}
        <div
          style={{
            opacity: windowOp,
            transform: `translateX(${interpolate(windowOp, [0, 1], [40, 0])}px)`,
            padding: '24px 40px',
            background: '#06202B',
            color: '#EEEEEE',
          }}
        >
          <div
            style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 56,
              fontWeight: 900,
              letterSpacing: 2,
            }}
          >
            IT'S A WINDOW
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(6,32,43,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default MathScreaming;
```

```tsx
import React from 'react';
Audio: "It's a window into a strategy that almost nobody in mainstream finance is willing to talk about openly."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'WindowIntoStrategy',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WindowIntoStrategy: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Window frame reveal
  const windowOp = sp(5);
  const windowScale = interpolate(windowOp, [0, 1], [0.7, 1]);

  // Lock and reveal
  const lockOp = sp(35);
  const lockShake = frame > 70 && frame < 95 ? Math.sin(frame * 3) * 3 : 0;

  // Strategy reveal
  const stratOp = sp(95);

  // Silence elements
  const sil1 = sp(120);
  const sil2 = sp(135);
  const sil3 = sp(150);

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  // Particles
  const particles = Array.from({ length: 26 }, (_, i) => {
    const seed = i + 59;
    const x = seededRandom(seed) * 1920;
    const y = seededRandom(seed * 2) * 1080;
    const drift = Math.sin(frame * 0.025 + i) * 22;
    return { x, y: y + drift, op: 0.1 + seededRandom(seed * 3) * 0.22, size: 3 + seededRandom(seed * 5) * 6 };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale})` }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 50%, #EEEEEE 0%, #CFCFCF 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: -150,
          left: -100,
          width: 600,
          height: 600,
          background: '#06202B',
          opacity: 0.05,
          transform: `rotate(${frame * 0.08}deg)`,
          borderRadius: 50,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -200,
          right: -150,
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '6px solid #06202B',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
        }}
      />

      {/* Cross-hatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="hatchPat" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 0 40 L 40 0" stroke="#06202B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hatchPat)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            background: '#06202B',
            opacity: p.op,
            borderRadius: '50%',
          }}
        />
      ))}

      {/* Top label */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 30,
          color: '#06202B',
          letterSpacing: 6,
          fontWeight: 700,
          opacity: sp(5),
        }}
      >
        ── A WINDOW INTO ──
      </div>

      {/* Window frame on left */}
      <div
        style={{
          position: 'absolute',
          left: 140,
          top: 200,
          width: 620,
          height: 720,
          opacity: windowOp,
          transform: `scale(${windowScale})`,
          transformOrigin: 'center',
        }}
      >
        {/* Frame */}
        <div
          style={{
            position: 'absolute',
            inset: 0,
            border: '14px solid #06202B',
            background: '#000000',
          }}
        >
          {/* Cross frame */}
          <div
            style={{
              position: 'absolute',
              left: '50%',
              top: 0,
              bottom: 0,
              width: 14,
              background: '#06202B',
              transform: 'translateX(-50%)',
            }}
          />
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              right: 0,
              height: 14,
              background: '#06202B',
              transform: 'translateY(-50%)',
            }}
          />

          {/* Inside hint of strategy text */}
          <div
            style={{
              position: 'absolute',
              inset: 30,
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'space-around',
              padding: 20,
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 32,
              color: '#06202B',
              opacity: stratOp,
              fontWeight: 800,
              letterSpacing: 2,
            }}
          >
            <div style={{ textAlign: 'center' }}>OIL</div>
            <div style={{ textAlign: 'center' }}>DEBT</div>
            <div style={{ textAlign: 'center' }}>TARIFFS</div>
            <div style={{ textAlign: 'center' }}>CPI</div>
          </div>
        </div>

        {/* Lock icon overlay */}
        <div
          style={{
            position: 'absolute',
            top: '50%',
            left: '50%',
            transform: `translate(-50%, -50%) translate(${lockShake}px, 0)`,
            opacity: interpolate(stratOp, [0, 1], [lockOp, 0]),
          }}
        >
          <svg width="140" height="180" viewBox="0 0 140 180">
            <rect x="20" y="80" width="100" height="90" fill="#ff0000" rx="10" />
            <path d="M 35 80 L 35 50 Q 35 20, 70 20 Q 105 20, 105 50 L 105 80" fill="none" stroke="#ff0000" strokeWidth="14" strokeLinecap="round" />
            <circle cx="70" cy="120" r="10" fill="#06202B" />
            <rect x="65" y="125" width="10" height="25" fill="#06202B" />
          </svg>
        </div>
      </div>

      {/* RIGHT — Strategy */}
      <div
        style={{
          position: 'absolute',
          right: 100,
          top: 220,
          width: 800,
        }}
      >
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 130,
            color: '#06202B',
            fontWeight: 900,
            lineHeight: 0.9,
            letterSpacing: -3,
            opacity: stratOp,
            transform: `translateY(${interpolate(stratOp, [0, 1], [50, 0])}px)`,
          }}
        >
          A HIDDEN
        </div>
        <div
          style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 180,
            color: '#000000',
            fontWeight: 900,
            lineHeight: 0.9,
            letterSpacing: -4,
            opacity: stratOp,
            transform: `translateY(${interpolate(stratOp, [0, 1], [50, 0])}px)`,
            marginTop: 10,
          }}
        >
          STRATEGY.
        </div>

        {/* Silence list */}
        <div style={{ marginTop: 60, display: 'flex', flexDirection: 'column', gap: 18 }}>
          {[
            { txt: 'Wall Street → silent', op: sil1 },
            { txt: 'Cable News → silent', op: sil2 },
            { txt: 'Mainstream Finance → silent', op: sil3 },
          ].map((row, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 24,
                opacity: row.op,
                transform: `translateX(${interpolate(row.op, [0, 1], [40, 0])}px)`,
              }}
            >
              <div style={{ width: 22, height: 22, background: '#ff0000', borderRadius: '50%' }} />
              <div
                style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 42,
                  color: '#06202B',
                  fontWeight: 800,
                  letterSpacing: 1,
                }}
              >
                {row.txt}
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* Bottom stamp */}
      <div
        style={{
          position: 'absolute',
          bottom: 60,
          left: '50%',
          transform: `translateX(-50%) scale(${sil3})`,
          opacity: sil3,
          padding: '18px 50px',
          background: '#06202B',
          color: '#EEEEEE',
          fontFamily: 'Mont Heavy, Impact, sans-serif',
          fontSize: 38,
          fontWeight: 900,
          letterSpacing: 4,
        }}
      >
        WE'RE GOING TO OPEN IT
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(6,32,43,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default WindowIntoStrategy;
```

