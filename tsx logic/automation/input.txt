```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_Intro',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_Intro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.12]);

  // Greeting "नमस्कार"
  const namaskarY = interpolate(sp(5), [0, 1], [80, 0]);
  const namaskarOp = interpolate(sp(5), [0, 1], [0, 1]);

  // Name reveal
  const nameOp = interpolate(sp(35), [0, 1], [0, 1]);
  const nameY = interpolate(sp(35), [0, 1], [40, 0]);

  // Channel name reveal
  const brandOp = interpolate(sp(75), [0, 1], [0, 1]);
  const brandScale = sp(75, { damping: 10, stiffness: 150 });

  // Underline expand
  const underlineW = interpolate(sp(110), [0, 1], [0, 720]);

  // Pulsing dot
  const dotPulse = 1 + Math.sin(frame * 0.15) * 0.25;

  return (
    <AbsoluteFill style={{ backgroundColor: '#111844', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 30% 20%, #1f2960 0%, #111844 55%, #0a0f30 100%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid01" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#FFF9D2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid01)" />
      </svg>

      {/* Animated shape layers */}
      <div
        style={{
          position: 'absolute',
          top: -200,
          right: -150,
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '2px solid #FFF9D2',
          opacity: 0.05,
          transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -250,
          left: -200,
          width: 600,
          height: 600,
          borderRadius: 40,
          background: '#FFF9D2',
          opacity: 0.04,
          transform: `rotate(${-frame * 0.15}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '40%',
          left: '8%',
          width: 180,
          height: 180,
          background: '#EEEEEE',
          opacity: 0.05,
          transform: `rotate(45deg) translateY(${Math.sin(frame * 0.04) * 20}px)`,
        }}
      />

      {/* Floating particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const r = seededRandom(i + 1);
        const r2 = seededRandom(i + 50);
        const px = r * 1920;
        const py = (r2 * 1080 + frame * (0.5 + r)) % 1080;
        const size = 2 + r * 4;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: size,
              height: size,
              borderRadius: '50%',
              background: i % 3 === 0 ? '#FFF9D2' : '#EEEEEE',
              opacity: 0.3 + r * 0.4,
            }}
          />
        );
      })}

      {/* Diagonal accent lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.12 }}>
        <line x1="0" y1="950" x2="1920" y2="850" stroke="#FFF9D2" strokeWidth="2" strokeDasharray="20 15" />
        <line x1="0" y1="180" x2="1920" y2="100" stroke="#EEEEEE" strokeWidth="1" strokeDasharray="10 20" />
      </svg>

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `scale(${camScale})` }}>
        <div style={{ textAlign: 'center', position: 'relative' }}>
          {/* Top tag */}
          <div
            style={{
              opacity: namaskarOp,
              transform: `translateY(${namaskarY * 0.5}px)`,
              fontFamily: 'Inter, Poppins',
              fontSize: 36,
              fontWeight: 600,
              color: '#FFF9D2',
              letterSpacing: 8,
              marginBottom: 30,
            }}
          >
            ◆ EPISODE 01 ◆
          </div>

          {/* Hero greeting */}
          <div
            style={{
              opacity: namaskarOp,
              transform: `translateY(${namaskarY}px)`,
              fontFamily: 'Yantra One',
              fontSize: 221,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 1,
              letterSpacing: -3,
              textShadow: '0 0 60px rgba(255,249,210,0.2)',
            }}
          >
            नमस्कार दोस्तों
          </div>

          {/* Underline */}
          <div
            style={{
              width: underlineW,
              height: 6,
              background: '#FFF9D2',
              margin: '20px auto 30px',
              borderRadius: 3,
              boxShadow: '0 0 20px #FFF9D2',
            }}
          />

          {/* Name */}
          <div
            style={{
              opacity: nameOp,
              transform: `translateY(${nameY}px)`,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 64,
              fontWeight: 700,
              color: '#EEEEEE',
              marginBottom: 50,
            }}
          >
            मैं हूँ <span style={{ color: '#FFF9D2' }}>कुनाल</span>
          </div>

          {/* Brand box */}
          <div
            style={{
              opacity: brandOp,
              transform: `scale(${brandScale})`,
              display: 'inline-flex',
              alignItems: 'center',
              gap: 20,
              padding: '24px 60px',
              border: '3px solid #FFF9D2',
              borderRadius: 12,
              background: 'rgba(255,249,210,0.08)',
              backdropFilter: 'blur(6px)',
            }}
          >
            {/* Live dot SVG */}
            <svg width="28" height="28" style={{ transform: `scale(${dotPulse})` }}>
              <circle cx="14" cy="14" r="10" fill="#ff5252" />
              <circle cx="14" cy="14" r="10" fill="#ff5252" opacity="0.3">
                <animate attributeName="r" from="10" to="20" dur="1.2s" repeatCount="indefinite" />
                <animate attributeName="opacity" from="0.5" to="0" dur="1.2s" repeatCount="indefinite" />
              </circle>
            </svg>
            <span
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 56,
                fontWeight: 800,
                color: '#FFF9D2',
                letterSpacing: 2,
              }}
            >
              ProBuffet HQ
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Side corner brackets */}
      <svg width="120" height="120" style={{ position: 'absolute', top: 40, left: 40, opacity: brandOp }}>
        <path d="M 10 40 L 10 10 L 40 10" stroke="#FFF9D2" strokeWidth="3" fill="none" />
      </svg>
      <svg width="120" height="120" style={{ position: 'absolute', top: 40, right: 40, opacity: brandOp }}>
        <path d="M 80 10 L 110 10 L 110 40" stroke="#FFF9D2" strokeWidth="3" fill="none" />
      </svg>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_Intro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_Warning',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_Warning: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on warning impact
  const shakeX = frame < 20 ? Math.sin(frame * 2) * (1 - frame / 20) * 10 : 0;
  const shakeY = frame < 20 ? Math.cos(frame * 2.5) * (1 - frame / 20) * 6 : 0;

  // Warning sign rotation pulse
  const warnRot = Math.sin(frame * 0.15) * 4;
  const warnScale = sp(0, { damping: 8, stiffness: 100 });

  // Headline reveal
  const head1Op = interpolate(sp(20), [0, 1], [0, 1]);
  const head1Y = interpolate(sp(20), [0, 1], [60, 0]);

  const head2Op = interpolate(sp(55), [0, 1], [0, 1]);

  // Wallet emerge
  const walletScale = sp(90, { damping: 10, stiffness: 130 });
  const walletShake = Math.sin(frame * 0.4) * 3;

  // Bottom strip
  const stripW = interpolate(sp(140), [0, 1], [0, 1920]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#111844', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #111844 0%, #1a2050 50%, #0d1338 100%)',
        }}
      />

      {/* Diagonal stripes texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="stripes02" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#FFF9D2" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#stripes02)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '15%',
          left: '5%',
          width: 400,
          height: 400,
          borderRadius: '50%',
          background: '#ff5252',
          opacity: 0.05,
          filter: 'blur(40px)',
          transform: `scale(${1 + Math.sin(frame * 0.05) * 0.1})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '10%',
          right: '8%',
          width: 350,
          height: 350,
          borderRadius: 20,
          border: '3px solid #FFF9D2',
          opacity: 0.07,
          transform: `rotate(${frame * 0.3}deg)`,
        }}
      />

      {/* Particles - alert style */}
      {Array.from({ length: 25 }).map((_, i) => {
        const r = seededRandom(i + 5);
        const r2 = seededRandom(i + 80);
        const px = (r * 1920 + frame * 0.8) % 1920;
        const py = r2 * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FFF9D2',
              opacity: 0.4,
              boxShadow: '0 0 6px #FFF9D2',
            }}
          />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill
        style={{
          justifyContent: 'center',
          alignItems: 'center',
          transform: `translate(${shakeX}px, ${shakeY}px)`,
        }}
      >
        {/* Warning sign */}
        <div
          style={{
            transform: `scale(${warnScale}) rotate(${warnRot}deg)`,
            marginBottom: 30,
          }}
        >
          <svg width="160" height="160" viewBox="0 0 160 160">
            <polygon
              points="80,15 150,140 10,140"
              fill="none"
              stroke="#ff5252"
              strokeWidth="6"
              strokeLinejoin="round"
            />
            <polygon
              points="80,15 150,140 10,140"
              fill="#ff5252"
              opacity="0.15"
            />
            <line x1="80" y1="55" x2="80" y2="105" stroke="#ff5252" strokeWidth="8" strokeLinecap="round" />
            <circle cx="80" cy="123" r="6" fill="#ff5252" />
          </svg>
        </div>

        {/* Headline 1 */}
        <div
          style={{
            opacity: head1Op,
            transform: `translateY(${head1Y}px)`,
            fontFamily: 'Yantra One',
            fontSize: 119,
            fontWeight: 900,
            color: '#EEEEEE',
            textAlign: 'center',
            lineHeight: 1.05,
            letterSpacing: -1,
          }}
        >
          ये बात आपकी{' '}
          <span
            style={{
              color: '#FFF9D2',
              borderBottom: '6px solid #ff5252',
              paddingBottom: 8,
            }}
          >
            नींद उड़ा देगी
          </span>
        </div>

        {/* Subline */}
        <div
          style={{
            opacity: head2Op,
            marginTop: 50,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 60,
            fontWeight: 600,
            color: '#EEEEEE',
            opacity: 0.85 * head2Op,
            textAlign: 'center',
          }}
        >
          क्योंकि ये सीधे आपकी{' '}
          <span style={{ color: '#FFF9D2', fontWeight: 800 }}>जेब</span> से जुड़ी है
        </div>

        {/* Wallet illustration */}
        <div
          style={{
            transform: `scale(${walletScale}) translateY(${walletShake}px)`,
            marginTop: 50,
          }}
        >
          <svg width="280" height="180" viewBox="0 0 280 180">
            {/* wallet body */}
            <rect x="20" y="40" width="240" height="130" rx="14" fill="#FFF9D2" />
            <rect x="20" y="40" width="240" height="130" rx="14" fill="none" stroke="#111844" strokeWidth="4" />
            {/* flap */}
            <path d="M 20 60 L 140 20 L 260 60 Z" fill="#EEEEEE" stroke="#111844" strokeWidth="4" strokeLinejoin="round" />
            {/* coin/circle */}
            <circle cx="210" cy="110" r="22" fill="#111844" />
            <text x="210" y="120" fontSize="28" fontWeight="900" textAnchor="middle" fill="#FFF9D2" fontFamily="Inter">₹</text>
            {/* falling coins */}
            <circle cx="80" cy={170 + (frame * 4) % 60} r="10" fill="#FFF9D2" opacity={0.8} />
            <circle cx="120" cy={160 + (frame * 5) % 70} r="8" fill="#FFF9D2" opacity={0.7} />
            <circle cx="160" cy={175 + (frame * 6) % 50} r="11" fill="#FFF9D2" opacity={0.9} />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Bottom alert strip */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          width: stripW,
          height: 4,
          background: 'linear-gradient(90deg, #ff5252, #FFF9D2)',
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 95,
          left: 60,
          fontFamily: 'JetBrains Mono',
          fontSize: 28,
          fontWeight: 700,
          color: '#FFF9D2',
          letterSpacing: 4,
          opacity: head2Op,
        }}
      >
        ⚠ FINANCIAL ALERT — READ CAREFULLY
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_Warning;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_SalaryDrain',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_SalaryDrain: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // STOP sign entrance
  const stopScale = sp(0, { damping: 9, stiffness: 110 });
  const stopRot = Math.sin(frame * 0.1) * 2;

  // Title
  const titleOp = interpolate(sp(25), [0, 1], [0, 1]);
  const titleX = interpolate(sp(25), [0, 1], [-80, 0]);

  // Salary bar setup
  const barOp = interpolate(sp(60), [0, 1], [0, 1]);
  const fullBarW = 1300;
  const drainProgress = interpolate(frame, [110, 240], [1, 0.05], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const currentBarW = fullBarW * drainProgress;

  // Day counter
  const dayCount = Math.floor(interpolate(frame, [110, 240], [1, 7], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  }));

  // Amount counter
  const amount = Math.floor(interpolate(frame, [110, 240], [50000, 2500], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#111844', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 70% 30%, #1d2658 0%, #111844 60%, #0a0f30 100%)',
        }}
      />

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots03" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#FFF9D2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots03)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: -100,
          left: -100,
          width: 500,
          height: 500,
          borderRadius: '50%',
          border: '2px solid #FFF9D2',
          opacity: 0.06,
          transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -150,
          right: -100,
          width: 450,
          height: 450,
          background: '#EEEEEE',
          opacity: 0.04,
          borderRadius: 30,
          transform: `rotate(${-frame * 0.18}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 11);
        const r2 = seededRandom(i + 90);
        const px = r * 1920;
        const py = (r2 * 1080 + frame * (0.3 + r * 0.6)) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: '#FFF9D2',
              opacity: 0.35,
            }}
          />
        );
      })}

      {/* Top title row */}
      <div
        style={{
          position: 'absolute',
          top: 90,
          left: 0,
          width: '100%',
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 40,
        }}
      >
        {/* STOP hand SVG */}
        <div style={{ transform: `scale(${stopScale}) rotate(${stopRot}deg)` }}>
          <svg width="120" height="120" viewBox="0 0 120 120">
            <octagon />
            <polygon
              points="60,8 95,25 110,60 95,95 60,112 25,95 10,60 25,25"
              fill="#ff5252"
              stroke="#FFF9D2"
              strokeWidth="5"
            />
            <text x="60" y="72" textAnchor="middle" fontSize="28" fontWeight="900" fill="#FFF9D2" fontFamily="Inter">STOP</text>
          </svg>
        </div>

        <div
          style={{
            opacity: titleOp,
            transform: `translateX(${titleX}px)`,
            fontFamily: 'Yantra One',
            fontSize: 102,
            fontWeight: 900,
            color: '#EEEEEE',
            letterSpacing: -1,
          }}
        >
          एक पल <span style={{ color: '#FFF9D2' }}>रुकिए</span>
        </div>
      </div>

      {/* CENTER — Salary drain visualization */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', paddingTop: 100 }}>
        <div
          style={{
            opacity: barOp,
            textAlign: 'center',
            width: 1400,
          }}
        >
          {/* Label row */}
          <div
            style={{
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'center',
              marginBottom: 24,
              fontFamily: 'Inter, Poppins',
            }}
          >
            <div style={{ fontSize: 44, fontWeight: 700, color: '#EEEEEE' }}>
              आपकी <span style={{ color: '#FFF9D2' }}>सैलरी</span>
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 38,
                fontWeight: 700,
                color: '#FFF9D2',
                padding: '8px 24px',
                border: '2px solid #FFF9D2',
                borderRadius: 8,
              }}
            >
              DAY {dayCount} / 7
            </div>
          </div>

          {/* Track */}
          <div
            style={{
              width: fullBarW,
              height: 110,
              background: 'rgba(238,238,238,0.08)',
              border: '3px solid rgba(255,249,210,0.3)',
              borderRadius: 16,
              position: 'relative',
              overflow: 'hidden',
              margin: '0 auto',
            }}
          >
            {/* Fill */}
            <div
              style={{
                width: currentBarW,
                height: '100%',
                background: 'linear-gradient(90deg, #FFF9D2 0%, #ffb84d 60%, #ff5252 100%)',
                borderRadius: 13,
                transition: 'none',
                boxShadow: '0 0 30px rgba(255,249,210,0.4)',
              }}
            />
            {/* Amount overlay */}
            <div
              style={{
                position: 'absolute',
                top: 0,
                left: 0,
                width: '100%',
                height: '100%',
                display: 'flex',
                justifyContent: 'center',
                alignItems: 'center',
                fontFamily: 'JetBrains Mono',
                fontSize: 52,
                fontWeight: 900,
                color: '#111844',
                textShadow: '0 0 10px rgba(255,249,210,0.5)',
              }}
            >
              ₹ {amount.toLocaleString('en-IN')}
            </div>
          </div>

          {/* Tick marks */}
          <div
            style={{
              width: fullBarW,
              margin: '12px auto 0',
              display: 'flex',
              justifyContent: 'space-between',
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#EEEEEE',
              opacity: 0.6,
            }}
          >
            {['Day 1', 'Day 2', 'Day 3', 'Day 4', 'Day 5', 'Day 6', 'Day 7'].map((d) => (
              <span key={d}>{d}</span>
            ))}
          </div>

          {/* Question */}
          <div
            style={{
              marginTop: 80,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 64,
              fontWeight: 700,
              color: '#EEEEEE',
            }}
          >
            7 दिनों में{' '}
            <span style={{ color: '#FFF9D2', fontWeight: 900 }}>कितना बचा?</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_SalaryDrain;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_ExpenseList',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_ExpenseList: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const expenses = [
    { label: 'EMI', amount: '₹ 12,500', icon: 'bank', delay: 10 },
    { label: 'Credit Card Bill', amount: '₹ 8,200', icon: 'card', delay: 50 },
    { label: 'School Fees', amount: '₹ 9,800', icon: 'school', delay: 90 },
    { label: 'Ration / Groceries', amount: '₹ 7,500', icon: 'cart', delay: 130 },
    { label: 'Electricity Bill', amount: '₹ 3,400', icon: 'bolt', delay: 170 },
  ];

  // Title
  const titleOp = interpolate(sp(0), [0, 1], [0, 1]);

  // Running total
  const totals = [12500, 20700, 30500, 38000, 41400];
  const totalIdx = expenses.reduce((acc, e, i) => (frame >= e.delay + 20 ? i : acc), -1);
  const runningTotal = totalIdx >= 0 ? totals[totalIdx] : 0;

  const renderIcon = (icon: string) => {
    const s = 56;
    if (icon === 'bank')
      return (
        <svg width={s} height={s} viewBox="0 0 56 56">
          <polygon points="28,6 52,18 4,18" fill="#FFF9D2" />
          <rect x="8" y="22" width="6" height="22" fill="#FFF9D2" />
          <rect x="20" y="22" width="6" height="22" fill="#FFF9D2" />
          <rect x="32" y="22" width="6" height="22" fill="#FFF9D2" />
          <rect x="44" y="22" width="6" height="22" fill="#FFF9D2" />
          <rect x="4" y="46" width="48" height="6" fill="#FFF9D2" />
        </svg>
      );
    if (icon === 'card')
      return (
        <svg width={s} height={s} viewBox="0 0 56 56">
          <rect x="4" y="12" width="48" height="32" rx="4" fill="none" stroke="#FFF9D2" strokeWidth="3" />
          <rect x="4" y="18" width="48" height="6" fill="#FFF9D2" />
          <rect x="10" y="32" width="14" height="4" fill="#FFF9D2" />
        </svg>
      );
    if (icon === 'school')
      return (
        <svg width={s} height={s} viewBox="0 0 56 56">
          <polygon points="28,8 52,20 28,32 4,20" fill="#FFF9D2" />
          <path d="M 14 26 L 14 38 Q 28 46, 42 38 L 42 26" fill="none" stroke="#FFF9D2" strokeWidth="3" />
          <line x1="50" y1="20" x2="50" y2="36" stroke="#FFF9D2" strokeWidth="3" />
        </svg>
      );
    if (icon === 'cart')
      return (
        <svg width={s} height={s} viewBox="0 0 56 56">
          <path d="M 4 10 L 12 10 L 18 36 L 46 36 L 50 18 L 14 18" fill="none" stroke="#FFF9D2" strokeWidth="3" strokeLinejoin="round" />
          <circle cx="22" cy="46" r="4" fill="#FFF9D2" />
          <circle cx="42" cy="46" r="4" fill="#FFF9D2" />
        </svg>
      );
    if (icon === 'bolt')
      return (
        <svg width={s} height={s} viewBox="0 0 56 56">
          <polygon points="30,4 12,30 26,30 22,52 44,24 30,24" fill="#FFF9D2" />
        </svg>
      );
    return null;
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#111844', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(160deg, #111844 0%, #1c2354 50%, #0a0f30 100%)',
        }}
      />

      {/* Vertical line texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="vlines04" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="60" stroke="#FFF9D2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#vlines04)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '20%',
          right: -200,
          width: 600,
          height: 600,
          borderRadius: '50%',
          background: '#ff5252',
          opacity: 0.04,
          filter: 'blur(60px)',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.1})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -100,
          left: -100,
          width: 380,
          height: 380,
          background: '#FFF9D2',
          opacity: 0.04,
          transform: `rotate(${frame * 0.15}deg)`,
          borderRadius: 30,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 21);
        const r2 = seededRandom(i + 99);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (r2 * 1080 + frame * (0.4 + r * 0.5)) % 1080,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FFF9D2',
              opacity: 0.4,
            }}
          />
        );
      })}

      {/* TITLE */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: titleOp,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 28,
            color: '#FFF9D2',
            letterSpacing: 6,
            marginBottom: 12,
          }}
        >
          — MONTHLY OUTFLOW —
        </div>
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 96,
            fontWeight: 900,
            color: '#EEEEEE',
            letterSpacing: -1,
          }}
        >
          सब कुछ <span style={{ color: '#ff8585' }}>कट गया</span>
        </div>
      </div>

      {/* EXPENSE LIST */}
      <div
        style={{
          position: 'absolute',
          top: 290,
          left: 220,
          right: 220,
        }}
      >
        {expenses.map((e, i) => {
          const itemSp = sp(e.delay, { damping: 11, stiffness: 130 });
          const op = interpolate(itemSp, [0, 1], [0, 1]);
          const x = interpolate(itemSp, [0, 1], [-100, 0]);
          const strikeW = interpolate(
            frame,
            [e.delay + 20, e.delay + 40],
            [0, 100],
            { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
          );

          return (
            <div
              key={i}
              style={{
                opacity: op,
                transform: `translateX(${x}px)`,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'space-between',
                padding: '18px 36px',
                marginBottom: 14,
                background: 'rgba(255,249,210,0.05)',
                border: '2px solid rgba(255,249,210,0.2)',
                borderRadius: 14,
                backdropFilter: 'blur(6px)',
              }}
            >
              <div style={{ display: 'flex', alignItems: 'center', gap: 28 }}>
                <div
                  style={{
                    width: 76,
                    height: 76,
                    borderRadius: 12,
                    background: 'rgba(255,249,210,0.12)',
                    display: 'flex',
                    justifyContent: 'center',
                    alignItems: 'center',
                  }}
                >
                  {renderIcon(e.icon)}
                </div>
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontSize: 48,
                    fontWeight: 700,
                    color: '#EEEEEE',
                    position: 'relative',
                  }}
                >
                  {e.label}
                  <div
                    style={{
                      position: 'absolute',
                      top: '55%',
                      left: 0,
                      width: `${strikeW}%`,
                      height: 4,
                      background: '#ff5252',
                    }}
                  />
                </div>
              </div>
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 44,
                  fontWeight: 800,
                  color: '#ff8585',
                }}
              >
                − {e.amount}
              </div>
            </div>
          );
        })}

        {/* Running total bar */}
        <div
          style={{
            marginTop: 22,
            padding: '20px 36px',
            background: '#FFF9D2',
            borderRadius: 14,
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
            boxShadow: '0 0 40px rgba(255,249,210,0.3)',
          }}
        >
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 800, color: '#111844' }}>
            TOTAL OUT
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 52,
              fontWeight: 900,
              color: '#111844',
            }}
          >
            ₹ {runningTotal.toLocaleString('en-IN')}
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_ExpenseList;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_CardSwipeLoop',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_CardSwipeLoop: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Calendar date highlight (20th)
  const dateOp = interpolate(sp(0), [0, 1], [0, 1]);
  const dateScale = sp(40, { damping: 9, stiffness: 130 });

  // Loop arrow
  const loopOp = interpolate(sp(70), [0, 1], [0, 1]);
  const loopRot = (frame - 70) * 2;

  // Card swipe animation (recurring)
  const swipeCycle = (frame * 4) % 200;
  const cardX = swipeCycle < 100 ? interpolate(swipeCycle, [0, 100], [-200, 200]) : 200;
  const cardOpacity = swipeCycle < 100 ? 1 : 0;

  // Headline
  const headOp = interpolate(sp(100), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#111844', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 70%, #1e2860 0%, #111844 60%, #0a0f30 100%)',
        }}
      />

      {/* Circle grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="circs05" width="100" height="100" patternUnits="userSpaceOnUse">
            <circle cx="50" cy="50" r="30" fill="none" stroke="#FFF9D2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#circs05)" />
      </svg>

      {/* Shape layers - looping circles */}
      {[0, 1, 2].map((i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            top: '50%',
            left: '50%',
            width: 400 + i * 200,
            height: 400 + i * 200,
            marginLeft: -(200 + i * 100),
            marginTop: -(200 + i * 100),
            borderRadius: '50%',
            border: '2px solid #FFF9D2',
            opacity: 0.05,
            transform: `rotate(${frame * (0.2 + i * 0.1)}deg)`,
          }}
        />
      ))}

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 31);
        const r2 = seededRandom(i + 102);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (r2 * 1080 + frame * (0.4 + r * 0.5)) % 1080,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FFF9D2',
              opacity: 0.35,
            }}
          />
        );
      })}

      {/* LEFT — Calendar */}
      <div
        style={{
          position: 'absolute',
          left: 140,
          top: 200,
          opacity: dateOp,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 26,
            color: '#FFF9D2',
            letterSpacing: 4,
            marginBottom: 16,
          }}
        >
          DAY 20 OF THE MONTH
        </div>
        <div
          style={{
            width: 520,
            background: 'rgba(255,249,210,0.05)',
            border: '3px solid #FFF9D2',
            borderRadius: 18,
            padding: 28,
          }}
        >
          {/* Header */}
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 34,
              fontWeight: 700,
              color: '#FFF9D2',
              marginBottom: 18,
              textAlign: 'center',
            }}
          >
            DECEMBER 2025
          </div>
          {/* Days */}
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(7, 1fr)', gap: 6 }}>
            {['S', 'M', 'T', 'W', 'T', 'F', 'S'].map((d, i) => (
              <div
                key={`h${i}`}
                style={{
                  textAlign: 'center',
                  fontFamily: 'Inter',
                  fontSize: 20,
                  color: '#EEEEEE',
                  opacity: 0.6,
                  padding: 4,
                }}
              >
                {d}
              </div>
            ))}
            {Array.from({ length: 31 }).map((_, i) => {
              const day = i + 1;
              const is20 = day === 20;
              return (
                <div
                  key={day}
                  style={{
                    textAlign: 'center',
                    fontFamily: 'JetBrains Mono',
                    fontSize: 24,
                    fontWeight: is20 ? 900 : 500,
                    color: is20 ? '#111844' : '#EEEEEE',
                    padding: 8,
                    background: is20 ? '#FFF9D2' : 'transparent',
                    borderRadius: 6,
                    transform: is20 ? `scale(${dateScale})` : 'scale(1)',
                    boxShadow: is20 ? '0 0 25px rgba(255,249,210,0.6)' : 'none',
                  }}
                >
                  {day}
                </div>
              );
            })}
          </div>
        </div>
      </div>

      {/* RIGHT — Card swipe machine */}
      <div
        style={{
          position: 'absolute',
          right: 160,
          top: 240,
          width: 580,
          height: 520,
        }}
      >
        {/* POS terminal */}
        <div
          style={{
            width: 420,
            height: 340,
            background: '#1a2050',
            border: '4px solid #FFF9D2',
            borderRadius: 24,
            margin: '0 auto',
            position: 'relative',
            overflow: 'hidden',
          }}
        >
          {/* Screen */}
          <div
            style={{
              margin: 30,
              height: 140,
              background: '#0a0f30',
              borderRadius: 12,
              border: '2px solid rgba(255,249,210,0.3)',
              padding: 20,
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'center',
              alignItems: 'center',
            }}
          >
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#FFF9D2', opacity: 0.7 }}>SWIPE CARD</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 38, color: '#FFF9D2', fontWeight: 900, marginTop: 8 }}>
              ₹ 5,499
            </div>
          </div>
          {/* Card swipe slot */}
          <div
            style={{
              position: 'absolute',
              bottom: 60,
              left: 30,
              right: 30,
              height: 14,
              background: '#0a0f30',
              borderRadius: 4,
            }}
          />
          {/* Animated card */}
          <div
            style={{
              position: 'absolute',
              bottom: 50,
              left: '50%',
              width: 180,
              height: 30,
              marginLeft: -90,
              background: 'linear-gradient(135deg, #FFF9D2, #ffd966)',
              borderRadius: 4,
              transform: `translateX(${cardX}px)`,
              opacity: cardOpacity,
              boxShadow: '0 4px 12px rgba(0,0,0,0.5)',
            }}
          />
        </div>

        {/* Loop arrow circle */}
        <div
          style={{
            opacity: loopOp,
            marginTop: 40,
            display: 'flex',
            justifyContent: 'center',
          }}
        >
          <svg width="180" height="180" viewBox="0 0 180 180" style={{ transform: `rotate(${loopRot}deg)` }}>
            <circle cx="90" cy="90" r="65" fill="none" stroke="#FFF9D2" strokeWidth="6" strokeDasharray="200 100" />
            <polygon points="155,90 135,75 135,105" fill="#FFF9D2" />
          </svg>
        </div>
      </div>

      {/* HEADLINE BOTTOM */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: headOp,
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 76,
            fontWeight: 900,
            color: '#EEEEEE',
            letterSpacing: -1,
          }}
        >
          फिर वही कार्ड, फिर वही{' '}
          <span style={{ color: '#FFF9D2', borderBottom: '5px solid #ff5252', paddingBottom: 6 }}>
            स्वाइप
          </span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_CardSwipeLoop;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_TrappedCrowd',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_TrappedCrowd: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Headline split
  const head1Op = interpolate(sp(0), [0, 1], [0, 1]);
  const head1Y = interpolate(sp(0), [0, 1], [-60, 0]);

  // Stat block
  const statOp = interpolate(sp(60), [0, 1], [0, 1]);
  const statScale = sp(60, { damping: 10, stiffness: 130 });

  // Crowd grid reveal
  const crowdRevealStart = 100;

  // Net/trap drop
  const netOp = interpolate(sp(180), [0, 1], [0, 0.45]);
  const netY = interpolate(sp(180), [0, 1], [-200, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#111844', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, #111844 0%, #1a2050 60%, #0a0f30 100%)',
        }}
      />

      {/* Crosshatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="cross06" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 0 20 L 40 20 M 20 0 L 20 40" stroke="#FFF9D2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross06)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: -150,
          left: '40%',
          width: 500,
          height: 500,
          borderRadius: '50%',
          background: '#FFF9D2',
          opacity: 0.04,
          filter: 'blur(80px)',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.1})`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const r = seededRandom(i + 41);
        const r2 = seededRandom(i + 111);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (r2 * 1080 + frame * (0.3 + r * 0.6)) % 1080,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FFF9D2',
              opacity: 0.4,
            }}
          />
        );
      })}

      {/* TOP — Headline */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          width: '100%',
          textAlign: 'center',
          opacity: head1Op,
          transform: `translateY(${head1Y}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 102,
            fontWeight: 900,
            color: '#EEEEEE',
            letterSpacing: -1,
            lineHeight: 1.1,
          }}
        >
          आप <span style={{ color: '#FFF9D2' }}>अकेले नहीं</span> हैं
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 48,
            fontWeight: 600,
            color: '#EEEEEE',
            opacity: 0.75,
            marginTop: 14,
          }}
        >
          पूरा भारत का मिडल क्लास इसी जाल में है
        </div>
      </div>

      {/* CENTER — Crowd grid of figures */}
      <div
        style={{
          position: 'absolute',
          top: 340,
          left: '50%',
          marginLeft: -700,
          width: 1400,
          display: 'grid',
          gridTemplateColumns: 'repeat(20, 1fr)',
          gap: 8,
          rowGap: 16,
        }}
      >
        {Array.from({ length: 100 }).map((_, i) => {
          const delay = crowdRevealStart + i * 1.5;
          const figSp = sp(delay, { damping: 11, stiffness: 140 });
          const op = interpolate(figSp, [0, 1], [0, 1]);
          const scale = figSp;
          const isTrapped = i % 3 !== 0; // 67% trapped
          const color = isTrapped ? '#FFF9D2' : '#EEEEEE';
          const opMod = isTrapped ? 0.9 : 0.35;
          return (
            <svg
              key={i}
              width="50"
              height="60"
              viewBox="0 0 50 60"
              style={{ opacity: op * opMod, transform: `scale(${scale})` }}
            >
              <circle cx="25" cy="14" r="9" fill={color} />
              <rect x="14" y="24" width="22" height="28" rx="6" fill={color} />
            </svg>
          );
        })}
      </div>

      {/* Net/grid overlay (trap) */}
      <svg
        width="1400"
        height="280"
        style={{
          position: 'absolute',
          top: 340,
          left: '50%',
          marginLeft: -700,
          opacity: netOp,
          transform: `translateY(${netY}px)`,
        }}
      >
        <defs>
          <pattern id="net06" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform="rotate(20)">
            <path d="M 0 30 L 60 30 M 30 0 L 30 60" stroke="#ff5252" strokeWidth="2" />
            <circle cx="30" cy="30" r="3" fill="#ff5252" />
          </pattern>
        </defs>
        <rect width="1400" height="280" fill="url(#net06)" />
      </svg>

      {/* STAT bottom */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          width: '100%',
          textAlign: 'center',
          opacity: statOp,
          transform: `scale(${statScale})`,
        }}
      >
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'baseline',
            gap: 24,
            padding: '24px 60px',
            background: '#FFF9D2',
            borderRadius: 16,
            boxShadow: '0 0 50px rgba(255,249,210,0.35)',
          }}
        >
          <span style={{ fontFamily: 'JetBrains Mono', fontSize: 102, fontWeight: 900, color: '#111844' }}>67%</span>
          <span
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              fontWeight: 700,
              color: '#111844',
            }}
          >
            Middle Class is trapped in EMI cycle
          </span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_TrappedCrowd;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_Unaware',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_Unaware: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Eye blink
  const blinkPhase = (frame % 90);
  const eyeOpen = blinkPhase < 80 ? 1 : interpolate(blinkPhase, [80, 85, 90], [1, 0.05, 1]);

  // Camera slow push
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08]);

  // Big word reveal
  const wordOp = interpolate(sp(0), [0, 1], [0, 1]);
  const wordScale = sp(0, { damping: 10, stiffness: 110 });

  // Sub headline
  const subOp = interpolate(sp(50), [0, 1], [0, 1]);

  // Bottom donut chart
  const donutOp = interpolate(sp(100), [0, 1], [0, 1]);
  const arcProgress = interpolate(sp(110, { damping: 18, stiffness: 60 }), [0, 1], [0, 0.78]);
  const circumference = 2 * Math.PI * 90;

  // Percentage counter
  const pct = Math.floor(interpolate(frame, [110, 220], [0, 78], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#111844', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 35%, #1f2858 0%, #111844 55%, #08102e 100%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Wavy texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="wave07" width="100" height="50" patternUnits="userSpaceOnUse">
            <path d="M 0 25 Q 25 0, 50 25 T 100 25" fill="none" stroke="#FFF9D2" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#wave07)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '10%',
          left: '8%',
          width: 280,
          height: 280,
          borderRadius: '50%',
          border: '2px solid #FFF9D2',
          opacity: 0.07,
          transform: `rotate(${frame * 0.25}deg) scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '15%',
          right: '6%',
          width: 320,
          height: 320,
          background: '#EEEEEE',
          opacity: 0.04,
          borderRadius: 30,
          transform: `rotate(${-frame * 0.18}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 51);
        const r2 = seededRandom(i + 122);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (r2 * 1080 + frame * (0.4 + r * 0.5)) % 1080,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FFF9D2',
              opacity: 0.35,
            }}
          />
        );
      })}

      {/* Closed Eye SVG - top */}
      <div style={{ position: 'absolute', top: 130, width: '100%', display: 'flex', justifyContent: 'center' }}>
        <svg width="240" height="140" viewBox="0 0 240 140">
          <path d="M 20 70 Q 120 10, 220 70 Q 120 130, 20 70 Z" fill="none" stroke="#FFF9D2" strokeWidth="5" />
          {/* Iris that closes */}
          <ellipse cx="120" cy="70" rx="42" ry={42 * eyeOpen} fill="#FFF9D2" />
          <ellipse cx="120" cy="70" rx="20" ry={20 * eyeOpen} fill="#111844" />
        </svg>
      </div>

      {/* MAIN — Big DARAVNI word */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', paddingTop: 60 }}>
        <div
          style={{
            opacity: wordOp,
            transform: `scale(${wordScale})`,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              color: '#FFF9D2',
              letterSpacing: 8,
              marginBottom: 16,
            }}
          >
            — THE SCARY TRUTH —
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 187,
              fontWeight: 900,
              color: '#EEEEEE',
              letterSpacing: -3,
              lineHeight: 1,
              textShadow: '0 0 80px rgba(255,82,82,0.3)',
            }}
          >
            पता <span style={{ color: '#FFF9D2' }}>ही नहीं</span>
          </div>
          <div
            style={{
              opacity: subOp,
              marginTop: 30,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 51,
              fontWeight: 600,
              color: '#EEEEEE',
              opacity: 0.85 * subOp,
            }}
          >
            कि वो <span style={{ color: '#FFF9D2', fontWeight: 800 }}>फंस चुके</span> हैं
          </div>
        </div>
      </AbsoluteFill>

      {/* Bottom donut stat */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 100,
          display: 'flex',
          alignItems: 'center',
          gap: 32,
          opacity: donutOp,
        }}
      >
        <svg width="220" height="220" viewBox="0 0 220 220">
          <circle cx="110" cy="110" r="90" fill="none" stroke="rgba(255,249,210,0.15)" strokeWidth="20" />
          <circle
            cx="110"
            cy="110"
            r="90"
            fill="none"
            stroke="#FFF9D2"
            strokeWidth="20"
            strokeLinecap="round"
            strokeDasharray={`${circumference * arcProgress} ${circumference}`}
            transform="rotate(-90 110 110)"
          />
          <text
            x="110"
            y="118"
            textAnchor="middle"
            fontFamily="JetBrains Mono"
            fontSize="56"
            fontWeight="900"
            fill="#FFF9D2"
          >
            {pct}%
          </text>
        </svg>
        <div>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 800, color: '#EEEEEE' }}>
            of Indians
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 32,
              fontWeight: 600,
              color: '#FFF9D2',
              maxWidth: 480,
              marginTop: 6,
            }}
          >
            कर्ज में फंसे हैं — और उन्हें पता तक नहीं
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.65) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_Unaware;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_StanfordReport',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_StanfordReport: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Source tag
  const tagOp = interpolate(sp(0), [0, 1], [0, 1]);
  const tagY = interpolate(sp(0), [0, 1], [-40, 0]);

  // Document
  const docOp = interpolate(sp(30), [0, 1], [0, 1]);
  const docScale = sp(30, { damping: 11, stiffness: 130 });

  // Big number
  const numOp = interpolate(sp(85), [0, 1], [0, 1]);
  const num = interpolate(frame, [85, 200], [0, 77], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const numScale = sp(85, { damping: 10, stiffness: 110 });

  // Date stamp
  const dateOp = interpolate(sp(200), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#111844', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #111844 0%, #1d2560 50%, #0a0f30 100%)',
        }}
      />

      {/* Architectural grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="archgrid08" width="120" height="120" patternUnits="userSpaceOnUse">
            <path d="M 120 0 L 0 0 0 120" fill="none" stroke="#FFF9D2" strokeWidth="0.8" />
            <path d="M 60 0 L 60 120 M 0 60 L 120 60" fill="none" stroke="#FFF9D2" strokeWidth="0.4" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#archgrid08)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '-10%',
          right: '-10%',
          width: 500,
          height: 500,
          borderRadius: '50%',
          background: '#FFF9D2',
          opacity: 0.04,
          filter: 'blur(60px)',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.1})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '-15%',
          left: '-5%',
          width: 600,
          height: 600,
          borderRadius: 50,
          border: '3px solid #FFF9D2',
          opacity: 0.05,
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 61);
        const r2 = seededRandom(i + 133);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (r2 * 1080 + frame * (0.3 + r * 0.5)) % 1080,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FFF9D2',
              opacity: 0.35,
            }}
          />
        );
      })}

      {/* TOP — Source tag */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          width: '100%',
          textAlign: 'center',
          opacity: tagOp,
          transform: `translateY(${tagY}px)`,
        }}
      >
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 18,
            padding: '14px 36px',
            border: '2px solid #FFF9D2',
            borderRadius: 100,
            background: 'rgba(255,249,210,0.06)',
          }}
        >
          <svg width="32" height="32" viewBox="0 0 32 32">
            <path d="M 4 12 L 16 4 L 28 12 L 16 20 Z" fill="#FFF9D2" />
            <path d="M 8 16 L 8 24 L 16 28 L 24 24 L 24 16" fill="none" stroke="#FFF9D2" strokeWidth="2" />
          </svg>
          <span
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 30,
              fontWeight: 700,
              color: '#FFF9D2',
              letterSpacing: 4,
            }}
          >
            SOURCE: STANFORD ECONOMIC REVIEW
          </span>
        </div>
      </div>

      {/* LEFT — Document mock */}
      <div
        style={{
          position: 'absolute',
          top: 220,
          left: 130,
          opacity: docOp,
          transform: `scale(${docScale}) rotate(-4deg)`,
        }}
      >
        <div
          style={{
            width: 480,
            height: 600,
            background: '#EEEEEE',
            borderRadius: 8,
            padding: 36,
            boxShadow: '0 30px 70px rgba(0,0,0,0.5)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 20,
              fontWeight: 800,
              color: '#111844',
              letterSpacing: 2,
              borderBottom: '3px solid #111844',
              paddingBottom: 12,
              marginBottom: 24,
            }}
          >
            STANFORD ECONOMIC REVIEW
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 800,
              color: '#111844',
              marginBottom: 20,
              lineHeight: 1.3,
            }}
          >
            India Household Debt Report
          </div>
          {/* Lines simulating text */}
          {Array.from({ length: 8 }).map((_, i) => (
            <div
              key={i}
              style={{
                height: 8,
                background: '#9E9E9E',
                borderRadius: 4,
                marginBottom: 12,
                width: `${75 + ((i * 7) % 25)}%`,
                opacity: 0.6,
              }}
            />
          ))}
          {/* Highlight box */}
          <div
            style={{
              marginTop: 24,
              padding: '14px 18px',
              background: '#FFF9D2',
              border: '2px solid #111844',
              borderRadius: 6,
              fontFamily: 'JetBrains Mono',
              fontSize: 18,
              fontWeight: 700,
              color: '#111844',
            }}
          >
            "Indian household debt: ₹77 LCr (Jun 2021)"
          </div>
        </div>
        {/* Red stamp */}
        <div
          style={{
            position: 'absolute',
            top: 40,
            right: -20,
            transform: 'rotate(15deg)',
            border: '4px solid #ff5252',
            padding: '8px 20px',
            color: '#ff5252',
            fontFamily: 'Inter',
            fontWeight: 900,
            fontSize: 24,
            letterSpacing: 2,
            borderRadius: 4,
            background: 'rgba(255,255,255,0.9)',
          }}
        >
          OFFICIAL
        </div>
      </div>

      {/* RIGHT — Big number */}
      <div
        style={{
          position: 'absolute',
          top: 240,
          right: 140,
          width: 900,
          opacity: numOp,
          transform: `scale(${numScale})`,
          textAlign: 'right',
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 51,
            fontWeight: 700,
            color: '#EEEEEE',
            marginBottom: 10,
          }}
        >
          भारतीय परिवारों पर कुल कर्ज
        </div>
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 272,
            fontWeight: 900,
            color: '#FFF9D2',
            lineHeight: 0.95,
            letterSpacing: -6,
            textShadow: '0 0 50px rgba(255,249,210,0.3)',
          }}
        >
          ₹{num.toFixed(0)}
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 76,
            fontWeight: 800,
            color: '#EEEEEE',
            marginTop: -10,
          }}
        >
          लाख करोड़
        </div>
        <div
          style={{
            opacity: dateOp,
            marginTop: 30,
            display: 'inline-block',
            padding: '12px 28px',
            background: '#FFF9D2',
            borderRadius: 8,
            fontFamily: 'JetBrains Mono',
            fontSize: 32,
            fontWeight: 800,
            color: '#111844',
            letterSpacing: 3,
          }}
        >
          JUNE 2021
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_StanfordReport;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_DebtJump',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_DebtJump: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleOp = interpolate(sp(0), [0, 1], [0, 1]);

  // Bar 1 (77 LCr) - already present at start
  const bar1H = interpolate(sp(20, { damping: 15, stiffness: 80 }), [0, 1], [0, 240]);
  const bar1Op = interpolate(sp(20), [0, 1], [0, 1]);

  // Bar 2 (120 LCr) - dramatic rise
  const bar2H = interpolate(sp(80, { damping: 14, stiffness: 70 }), [0, 1], [0, 420]);
  const bar2Op = interpolate(sp(80), [0, 1], [0, 1]);

  // Arrow up
  const arrowOp = interpolate(sp(150), [0, 1], [0, 1]);
  const arrowY = Math.sin(frame * 0.15) * 8;

  // Final stat banner
  const bannerOp = interpolate(sp(180), [0, 1], [0, 1]);
  const bannerScale = sp(180, { damping: 9, stiffness: 130 });

  // Counters
  const num2 = Math.floor(interpolate(frame, [80, 180], [0, 120], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  }));

  // Shake on bar 2 land
  const shakeX = frame > 160 && frame < 180 ? Math.sin(frame * 3) * (180 - frame) * 0.4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#111844', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 100%, #1f2860 0%, #111844 50%, #08102e 100%)',
        }}
      />

      {/* Horizontal grid lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <line
            key={i}
            x1="0"
            y1={90 + i * 80}
            x2="1920"
            y2={90 + i * 80}
            stroke="#FFF9D2"
            strokeWidth="1"
            strokeDasharray="6 10"
          />
        ))}
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '15%',
          right: '5%',
          width: 320,
          height: 320,
          borderRadius: '50%',
          background: '#ff5252',
          opacity: 0.05,
          filter: 'blur(70px)',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.1})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '-10%',
          left: '-5%',
          width: 400,
          height: 400,
          background: '#FFF9D2',
          opacity: 0.04,
          borderRadius: 30,
          transform: `rotate(${frame * 0.18}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 71);
        const r2 = seededRandom(i + 144);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (r2 * 1080 + frame * (0.3 + r * 0.6)) % 1080,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FFF9D2',
              opacity: 0.4,
            }}
          />
        );
      })}

      {/* TITLE */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          width: '100%',
          textAlign: 'center',
          opacity: titleOp,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 26,
            color: '#FFF9D2',
            letterSpacing: 5,
            marginBottom: 12,
          }}
        >
          — INDIAN HOUSEHOLD DEBT —
        </div>
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 89,
            fontWeight: 900,
            color: '#EEEEEE',
            letterSpacing: -1,
          }}
        >
          सिर्फ 3 साल में <span style={{ color: '#FFF9D2' }}>विस्फोट</span>
        </div>
      </div>

      {/* CHART AREA */}
      <div
        style={{
          position: 'absolute',
          bottom: 200,
          left: 0,
          width: '100%',
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'flex-end',
          gap: 200,
          transform: `translateX(${shakeX}px)`,
        }}
      >
        {/* BAR 1 */}
        <div style={{ textAlign: 'center', opacity: bar1Op }}>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 38,
              fontWeight: 900,
              color: '#FFF9D2',
              marginBottom: 12,
            }}
          >
            ₹ 77 LCr
          </div>
          <div
            style={{
              width: 220,
              height: bar1H,
              background: 'linear-gradient(180deg, rgba(255,249,210,0.7) 0%, rgba(255,249,210,0.4) 100%)',
              border: '3px solid #FFF9D2',
              borderRadius: '8px 8px 0 0',
            }}
          />
          <div
            style={{
              marginTop: 18,
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 700,
              color: '#EEEEEE',
            }}
          >
            JUN 2021
          </div>
        </div>

        {/* ARROW between */}
        <div
          style={{
            opacity: arrowOp,
            transform: `translateY(${arrowY - 200}px)`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 64,
              fontWeight: 900,
              color: '#ff5252',
              letterSpacing: -2,
            }}
          >
            +56%
          </div>
          <svg width="100" height="120" viewBox="0 0 100 120">
            <line x1="50" y1="120" x2="50" y2="30" stroke="#ff5252" strokeWidth="6" />
            <polygon points="50,5 25,40 75,40" fill="#ff5252" />
          </svg>
        </div>

        {/* BAR 2 */}
        <div style={{ textAlign: 'center', opacity: bar2Op }}>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 51,
              fontWeight: 900,
              color: '#FFF9D2',
              marginBottom: 12,
              textShadow: '0 0 30px rgba(255,249,210,0.5)',
            }}
          >
            ₹ {num2} LCr
          </div>
          <div
            style={{
              width: 220,
              height: bar2H,
              background: 'linear-gradient(180deg, #FFF9D2 0%, #ffb84d 60%, #ff5252 100%)',
              border: '3px solid #FFF9D2',
              borderRadius: '8px 8px 0 0',
              boxShadow: '0 0 50px rgba(255,82,82,0.4)',
            }}
          />
          <div
            style={{
              marginTop: 18,
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 700,
              color: '#FFF9D2',
            }}
          >
            MAR 2024
          </div>
        </div>
      </div>

      {/* BOTTOM BANNER */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          width: '100%',
          textAlign: 'center',
          opacity: bannerOp,
          transform: `scale(${bannerScale})`,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            padding: '18px 50px',
            background: '#FFF9D2',
            borderRadius: 14,
            fontFamily: 'Inter, Poppins',
            fontSize: 42,
            fontWeight: 800,
            color: '#111844',
            boxShadow: '0 0 50px rgba(255,249,210,0.4)',
          }}
        >
          ₹ 43 LAKH CRORE ADDED IN JUST 3 YEARS
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_DebtJump;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_LeapAndDeny',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_LeapAndDeny: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 56% MEGA number
  const pctScale = sp(0, { damping: 9, stiffness: 120 });
  const pctOp = interpolate(sp(0), [0, 1], [0, 1]);
  const pctNum = Math.floor(interpolate(frame, [0, 70], [0, 56], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  }));

  // Subline "3 साल में छलांग"
  const subOp = interpolate(sp(50), [0, 1], [0, 1]);

  // Divider
  const dividerW = interpolate(sp(100), [0, 1], [0, 1700]);

  // Home loan ❌ block
  const homeOp = interpolate(sp(130), [0, 1], [0, 1]);
  const homeX = interpolate(sp(130), [0, 1], [-80, 0]);

  // Cross mark animation
  const crossDraw = interpolate(sp(170, { damping: 15, stiffness: 80 }), [0, 1], [0, 1]);

  // Camera mini zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#111844', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 50%, #1f2858 0%, #111844 60%, #0a0f30 100%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Mixed texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="mix10" width="80" height="80" patternUnits="userSpaceOnUse">
            <circle cx="40" cy="40" r="2" fill="#FFF9D2" />
            <path d="M 80 0 L 0 80" stroke="#FFF9D2" strokeWidth="0.6" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#mix10)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '-15%',
          left: '-5%',
          width: 600,
          height: 600,
          borderRadius: '50%',
          border: '3px solid #FFF9D2',
          opacity: 0.06,
          transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '-10%',
          right: '-5%',
          width: 500,
          height: 500,
          background: '#FFF9D2',
          opacity: 0.04,
          borderRadius: 40,
          transform: `rotate(${-frame * 0.18}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const r = seededRandom(i + 81);
        const r2 = seededRandom(i + 155);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (r2 * 1080 + frame * (0.3 + r * 0.7)) % 1080,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FFF9D2',
              opacity: 0.4,
            }}
          />
        );
      })}

      {/* TOP HALF — Big leap percentage */}
      <div
        style={{
          position: 'absolute',
          top: 120,
          width: '100%',
          textAlign: 'center',
          opacity: pctOp,
          transform: `scale(${pctScale})`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 30,
            color: '#FFF9D2',
            letterSpacing: 6,
            marginBottom: 14,
          }}
        >
          THE LEAP — 3 YEARS
        </div>
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 289,
            fontWeight: 900,
            color: '#FFF9D2',
            lineHeight: 0.9,
            letterSpacing: -8,
            textShadow: '0 0 80px rgba(255,249,210,0.4)',
          }}
        >
          {pctNum}%
        </div>
        <div
          style={{
            opacity: subOp,
            marginTop: 10,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 60,
            fontWeight: 700,
            color: '#EEEEEE',
          }}
        >
          की <span style={{ color: '#FFF9D2', fontWeight: 900 }}>छलांग</span>
        </div>
      </div>

      {/* Divider */}
      <div
        style={{
          position: 'absolute',
          bottom: 360,
          left: '50%',
          marginLeft: -dividerW / 2,
          width: dividerW,
          height: 4,
          background: 'linear-gradient(90deg, transparent, #FFF9D2, transparent)',
        }}
      />

      {/* BOTTOM — Home loan disclaimer */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          width: '100%',
          textAlign: 'center',
          opacity: homeOp,
          transform: `translateX(${homeX}px)`,
        }}
      >
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 40,
            padding: '30px 60px',
            background: 'rgba(255,82,82,0.1)',
            border: '3px solid #ff5252',
            borderRadius: 18,
          }}
        >
          {/* House icon with cross */}
          <div style={{ position: 'relative', width: 120, height: 120 }}>
            <svg width="120" height="120" viewBox="0 0 120 120">
              <polygon points="60,15 110,55 100,55 100,105 20,105 20,55 10,55" fill="#FFF9D2" />
              <rect x="50" y="70" width="20" height="35" fill="#111844" />
              <rect x="32" y="65" width="14" height="14" fill="#111844" />
              <rect x="74" y="65" width="14" height="14" fill="#111844" />
            </svg>
            {/* Cross overlay */}
            <svg width="120" height="120" style={{ position: 'absolute', top: 0, left: 0 }}>
              <line
                x1="15"
                y1="15"
                x2={15 + 90 * crossDraw}
                y2={15 + 90 * crossDraw}
                stroke="#ff5252"
                strokeWidth="10"
                strokeLinecap="round"
              />
              <line
                x1={105 - 90 * Math.max(0, crossDraw - 0.5) * 2}
                y1={15 + 90 * Math.max(0, crossDraw - 0.5) * 2}
                x2="105"
                y2="105"
                stroke="#ff5252"
                strokeWidth="10"
                strokeLinecap="round"
              />
            </svg>
          </div>

          <div style={{ textAlign: 'left' }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 30,
                fontWeight: 700,
                color: '#ff8585',
                letterSpacing: 3,
                marginBottom: 8,
              }}
            >
              NOT HOME LOAN GROWTH
            </div>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 64,
                fontWeight: 900,
                color: '#EEEEEE',
                letterSpacing: -1,
                lineHeight: 1.05,
              }}
            >
              ये बढ़त <span style={{ color: '#FFF9D2' }}>कुछ और</span> है
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_LeapAndDeny;
```

