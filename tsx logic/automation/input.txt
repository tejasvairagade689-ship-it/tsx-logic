```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_HelloFriends',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_HelloFriends: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.06], { extrapolateRight: 'clamp' });

  // Word reveals
  const helloIn = sp(8);
  const friendsIn = sp(20);
  const divorcedIn = sp(45);
  const retirementIn = sp(70);
  const subIn = sp(110);
  const lineIn = interpolate(frame, [30, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Alert ring pulse
  const ringPulse = 1 + Math.sin(frame * 0.12) * 0.04;
  const ringOpacity = 0.6 + Math.sin(frame * 0.15) * 0.2;

  // Particles
  const particles = Array.from({ length: 28 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 2) * 1080,
    s: 2 + seededRandom(i + 3) * 4,
    sp: 0.2 + seededRandom(i + 4) * 0.5,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 20%, rgba(254,236,65,0.18) 0%, rgba(159,161,255,0) 55%), radial-gradient(ellipse at 80% 90%, rgba(0,0,0,0.25) 0%, rgba(159,161,255,0) 60%)',
      }} />

      {/* Shape Layer — drifting geometric forms */}
      <div style={{
        position: 'absolute',
        width: 700, height: 700,
        left: -150 + Math.sin(frame * 0.01) * 30,
        top: -200 + Math.cos(frame * 0.012) * 25,
        borderRadius: '50%',
        background: '#FEEC41',
        opacity: 0.06,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 500, height: 500,
        right: -100 + Math.cos(frame * 0.008) * 40,
        bottom: -150,
        background: '#000000',
        opacity: 0.05,
        transform: `rotate(${45 + frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 300, height: 300,
        left: '60%',
        top: '15%',
        border: '4px solid #000000',
        opacity: 0.07,
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
      }} />

      {/* Motion Texture — diagonal grid lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <line key={i} x1={i * 90 - frame * 0.5} y1={0} x2={i * 90 - frame * 0.5 + 200} y2={1080} stroke="#000000" strokeWidth={1} />
        ))}
      </svg>

      {/* Floating Particles */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          width: p.s, height: p.s,
          left: p.x,
          top: (p.y - frame * p.sp) % 1080,
          backgroundColor: '#FEEC41',
          borderRadius: '50%',
          opacity: 0.5,
          boxShadow: '0 0 8px #FEEC41',
        }} />
      ))}

      {/* MAIN CAMERA WRAP */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center' }}>

        {/* Top label */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - helloIn) * -40}px)`,
          opacity: helloIn,
          display: 'flex',
          alignItems: 'center',
          gap: 18,
          padding: '14px 32px',
          background: '#000000',
          border: '3px solid #FEEC41',
          borderRadius: 50,
        }}>
          {/* Alert icon SVG */}
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
            <path d="M12 2L1 21h22L12 2z" stroke="#FEEC41" strokeWidth="2.5" strokeLinejoin="round" fill="#FEEC41" fillOpacity="0.2"/>
            <path d="M12 9v5" stroke="#FEEC41" strokeWidth="2.5" strokeLinecap="round"/>
            <circle cx="12" cy="17" r="1.2" fill="#FEEC41"/>
          </svg>
          <span style={{
            fontFamily: 'Inter, Poppins, sans-serif',
            fontWeight: 800,
            fontSize: 30,
            color: '#FEEC41',
            letterSpacing: 4,
          }}>URGENT FOR DIVORCED SENIORS</span>
        </div>

        {/* HERO TEXT — HELLO FRIENDS */}
        <div style={{
          position: 'absolute',
          top: 220,
          left: 0,
          right: 0,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif',
            fontWeight: 900,
            fontSize: 195,
            color: '#000000',
            lineHeight: 0.95,
            letterSpacing: -3,
            opacity: helloIn,
            transform: `translateY(${(1 - helloIn) * 60}px) scale(${0.85 + helloIn * 0.15})`,
          }}>
            HELLO
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif',
            fontWeight: 900,
            fontSize: 195,
            color: '#FEEC41',
            lineHeight: 0.95,
            letterSpacing: -3,
            textShadow: '6px 6px 0 #000000',
            opacity: friendsIn,
            transform: `translateY(${(1 - friendsIn) * 60}px) scale(${0.85 + friendsIn * 0.15})`,
            marginTop: -10,
          }}>
            FRIENDS.
          </div>
        </div>

        {/* Middle accent line */}
        <div style={{
          position: 'absolute',
          top: 640,
          left: '50%',
          width: lineIn * 600,
          height: 6,
          background: '#000000',
          transform: 'translateX(-50%)',
        }} />

        {/* SUBHEAD BLOCK */}
        <div style={{
          position: 'absolute',
          top: 685,
          left: 0,
          right: 0,
          textAlign: 'center',
          fontFamily: 'Inter, Poppins, sans-serif',
        }}>
          <div style={{
            fontWeight: 700,
            fontSize: 64,
            color: '#000000',
            opacity: divorcedIn,
            transform: `translateY(${(1 - divorcedIn) * 30}px)`,
            letterSpacing: -1,
          }}>
            If you're <span style={{
              background: '#FEEC41',
              padding: '2px 18px',
              display: 'inline-block',
              transform: `rotate(-1.5deg) scale(${0.9 + divorcedIn * 0.1})`,
            }}>DIVORCED</span>
          </div>
          <div style={{
            fontWeight: 700,
            fontSize: 64,
            color: '#000000',
            opacity: retirementIn,
            transform: `translateY(${(1 - retirementIn) * 30}px)`,
            marginTop: 14,
            letterSpacing: -1,
          }}>
            & approaching <span style={{ color: '#000000', borderBottom: '5px solid #FEEC41', paddingBottom: 2 }}>RETIREMENT...</span>
          </div>
        </div>

        {/* Bottom alert badge — STAY TILL END */}
        <div style={{
          position: 'absolute',
          bottom: 110,
          left: '50%',
          transform: `translateX(-50%) scale(${subIn * ringPulse})`,
          opacity: subIn,
          display: 'flex',
          alignItems: 'center',
          gap: 20,
          padding: '22px 48px',
          background: '#FEEC41',
          border: '4px solid #000000',
          boxShadow: '10px 10px 0 #000000',
        }}>
          {/* Eye icon */}
          <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
            <path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7-10-7-10-7z" stroke="#000000" strokeWidth="2.5"/>
            <circle cx="12" cy="12" r="3" fill="#000000"/>
          </svg>
          <span style={{
            fontFamily: 'Inter, Poppins, sans-serif',
            fontWeight: 900,
            fontSize: 46,
            color: '#000000',
            letterSpacing: 1,
          }}>WATCH TILL THE END</span>
        </div>

        {/* Decorative arrow pointing to title */}
        <svg
          width="120" height="120"
          style={{
            position: 'absolute',
            top: 280,
            left: 180,
            opacity: helloIn * 0.9,
            transform: `rotate(${-20 + Math.sin(frame * 0.08) * 5}deg)`,
          }}
          viewBox="0 0 100 100" fill="none"
        >
          <path d="M10 80 Q 50 20, 90 50" stroke="#000000" strokeWidth="5" strokeLinecap="round" strokeDasharray="6 6"/>
          <path d="M85 42 L 92 52 L 80 55" stroke="#000000" strokeWidth="5" strokeLinecap="round" strokeLinejoin="round" fill="none"/>
        </svg>

        {/* Right side stat chip */}
        <div style={{
          position: 'absolute',
          top: 320,
          right: 140,
          opacity: friendsIn,
          transform: `rotate(${5 - Math.sin(frame * 0.06) * 2}deg) scale(${friendsIn})`,
          padding: '18px 28px',
          background: '#000000',
          color: '#FEEC41',
          fontFamily: 'Inter, sans-serif',
          fontWeight: 900,
          fontSize: 38,
          border: '3px solid #FEEC41',
        }}>
          $$$ INSIDE
        </div>

      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 50%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene01_HelloFriends;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_HundredsThousands',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_HundredsThousands: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter animation $100 -> $3,000+
  const counter = Math.floor(interpolate(frame, [20, 130], [100, 3200], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const counterStr = `$${counter.toLocaleString()}`;

  // Bar chart growing
  const barProgress = interpolate(frame, [40, 150], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const titleIn = sp(5);
  const counterIn = sp(15);
  const barsIn = sp(40);
  const captionIn = sp(160);

  // Shake on payoff
  const shake = frame > 130 && frame < 160 ? Math.sin(frame * 1.8) * 4 : 0;

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.0], { extrapolateRight: 'clamp' });

  // Particles — money confetti
  const conf = Array.from({ length: 30 }, (_, i) => ({
    x: seededRandom(i + 11) * 1920,
    y: seededRandom(i + 22) * 1080,
    r: seededRandom(i + 33) * 360,
    sp: 0.3 + seededRandom(i + 44) * 0.6,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient bg */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 30%, rgba(254,236,65,0.22) 0%, rgba(159,161,255,0) 50%), radial-gradient(circle at 20% 80%, rgba(0,0,0,0.22) 0%, rgba(159,161,255,0) 55%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        width: 600, height: 600,
        left: '70%',
        top: -100,
        background: '#FEEC41',
        opacity: 0.07,
        transform: `rotate(${frame * 0.12}deg)`,
        borderRadius: 80,
      }} />
      <div style={{
        position: 'absolute',
        width: 400, height: 400,
        left: -100,
        bottom: 100,
        border: '5px solid #000000',
        opacity: 0.08,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.1})`,
      }} />

      {/* Texture — dots grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        {Array.from({ length: 20 }).map((_, r) =>
          Array.from({ length: 32 }).map((_, c) => (
            <circle key={`${r}-${c}`} cx={c * 60 + 30} cy={r * 60 + 30} r={1.5} fill="#000000" />
          ))
        )}
      </svg>

      {/* Confetti */}
      {conf.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          width: 14, height: 14,
          left: p.x,
          top: (p.y + frame * p.sp) % 1080,
          background: i % 2 === 0 ? '#FEEC41' : '#000000',
          transform: `rotate(${p.r + frame * 2}deg)`,
          opacity: 0.6,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)`, transformOrigin: 'center' }}>

        {/* TITLE */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0, right: 0,
          textAlign: 'center',
          fontFamily: 'Inter, sans-serif',
          fontWeight: 800,
          fontSize: 54,
          color: '#000000',
          opacity: titleIn,
          transform: `translateY(${(1 - titleIn) * -30}px)`,
          letterSpacing: 2,
        }}>
          EXTRA MONTHLY BENEFIT
        </div>

        {/* HUGE COUNTER */}
        <div style={{
          position: 'absolute',
          top: 180,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: counterIn,
          transform: `scale(${0.7 + counterIn * 0.3})`,
        }}>
          <div style={{
            display: 'inline-block',
            fontFamily: 'JetBrains Mono, monospace',
            fontWeight: 900,
            fontSize: 240,
            color: '#FEEC41',
            background: '#000000',
            padding: '10px 70px',
            border: '8px solid #000000',
            textShadow: '0 0 30px rgba(254,236,65,0.4)',
            letterSpacing: -4,
          }}>
            {counterStr}
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 700,
            fontSize: 42,
            color: '#000000',
            marginTop: 8,
            letterSpacing: 6,
          }}>EVERY SINGLE MONTH</div>
        </div>

        {/* BAR CHART — Hundreds vs Thousands */}
        <div style={{
          position: 'absolute',
          bottom: 180,
          left: '50%',
          transform: `translateX(-50%)`,
          display: 'flex',
          alignItems: 'flex-end',
          gap: 60,
          opacity: barsIn,
        }}>
          {[
            { label: 'HUNDREDS', val: 0.35, color: '#000000', txt: '#FEEC41' },
            { label: 'THOUSANDS', val: 1.0, color: '#FEEC41', txt: '#000000' },
          ].map((b, i) => {
            const h = b.val * barProgress * 320;
            return (
              <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 14 }}>
                <div style={{
                  fontFamily: 'Inter, sans-serif',
                  fontWeight: 900,
                  fontSize: 36,
                  color: '#000000',
                }}>{b.label}</div>
                <div style={{
                  width: 180,
                  height: h,
                  background: b.color,
                  border: '4px solid #000000',
                  display: 'flex',
                  alignItems: 'flex-start',
                  justifyContent: 'center',
                  paddingTop: 14,
                  boxShadow: '6px 6px 0 #000000',
                }}>
                  <span style={{
                    fontFamily: 'Inter, sans-serif',
                    fontWeight: 900,
                    fontSize: 32,
                    color: b.txt,
                  }}>{i === 0 ? '+' : '+++'}</span>
                </div>
              </div>
            );
          })}
        </div>

        {/* Arrow up icon between bars */}
        <svg
          width="100" height="160"
          viewBox="0 0 100 160"
          style={{
            position: 'absolute',
            bottom: 240,
            left: '50%',
            transform: `translateX(-50%) translateY(${-Math.sin(frame * 0.1) * 8}px)`,
            opacity: barsIn,
          }}
        >
          <path d="M50 140 L 50 30 M 20 60 L 50 25 L 80 60" stroke="#000000" strokeWidth="8" strokeLinecap="round" strokeLinejoin="round" fill="none"/>
        </svg>

        {/* BOTTOM CAPTION */}
        <div style={{
          position: 'absolute',
          bottom: 90,
          left: 0, right: 0,
          textAlign: 'center',
          fontFamily: 'Inter, sans-serif',
          fontWeight: 800,
          fontSize: 40,
          color: '#000000',
          opacity: captionIn,
          transform: `translateY(${(1 - captionIn) * 25}px)`,
        }}>
          Real money. <span style={{ background: '#FEEC41', padding: '2px 14px' }}>NOT</span> coupon savings.
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene02_HundredsThousands;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_FourLoopholes',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_FourLoopholes: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(5);
  const numberIn = sp(25);
  const card1 = sp(60);
  const card2 = sp(78);
  const card3 = sp(96);
  const card4 = sp(114);
  const footerIn = sp(170);

  // Camera pan
  const panX = interpolate(frame, [0, durationInFrames], [-15, 15], { extrapolateRight: 'clamp' });

  // Particles
  const particles = Array.from({ length: 24 }, (_, i) => ({
    x: seededRandom(i + 5) * 1920,
    y: seededRandom(i + 6) * 1080,
    s: 3 + seededRandom(i + 7) * 4,
    sp: 0.2 + seededRandom(i + 8) * 0.4,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(254,236,65,0.12) 0%, rgba(159,161,255,0) 50%, rgba(0,0,0,0.18) 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 800, height: 800,
        left: -200, top: -200,
        borderRadius: '50%',
        border: '6px solid #000000',
        opacity: 0.05,
        transform: `rotate(${frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 350, height: 350,
        right: 100, bottom: 100,
        background: '#FEEC41',
        opacity: 0.08,
        transform: `rotate(${15 + frame * 0.1}deg)`,
      }} />

      {/* Texture — vertical stripes */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <line key={i} x1={i * 70} y1={0} x2={i * 70} y2={1080} stroke="#000000" strokeWidth={1} />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          width: p.s, height: p.s,
          left: (p.x + frame * p.sp) % 1920,
          top: p.y,
          background: '#FEEC41',
          borderRadius: '50%',
          opacity: 0.45,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `translateX(${panX}px)` }}>

        {/* TOP TITLE */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: titleIn,
          transform: `translateY(${(1 - titleIn) * -25}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 700,
            fontSize: 38,
            color: '#000000',
            letterSpacing: 4,
          }}>SOCIAL SECURITY</div>
        </div>

        {/* MASSIVE 4 */}
        <div style={{
          position: 'absolute',
          top: 110,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: numberIn,
          transform: `scale(${0.6 + numberIn * 0.4})`,
        }}>
          <span style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 280,
            color: '#FEEC41',
            textShadow: '8px 8px 0 #000000',
            letterSpacing: -10,
          }}>4</span>
          <span style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 90,
            color: '#000000',
            marginLeft: 20,
            verticalAlign: 'top',
            marginTop: 70,
            display: 'inline-block',
          }}>LOOPHOLES</span>
        </div>

        {/* 4 mini cards */}
        <div style={{
          position: 'absolute',
          bottom: 200,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 26,
        }}>
          {[
            { n: '01', label: 'SPOUSAL', op: card1 },
            { n: '02', label: '2-YEAR', op: card2 },
            { n: '03', label: 'SURVIVOR', op: card3 },
            { n: '04', label: 'MULTI-EX', op: card4 },
          ].map((c, i) => (
            <div key={i} style={{
              width: 220,
              height: 180,
              background: '#000000',
              border: '4px solid #FEEC41',
              padding: 20,
              opacity: c.op,
              transform: `translateY(${(1 - c.op) * 60}px) scale(${0.85 + c.op * 0.15})`,
              boxShadow: '6px 6px 0 #FEEC41',
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'space-between',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 32,
                fontWeight: 900,
                color: '#FEEC41',
                opacity: 0.7,
              }}>#{c.n}</div>
              <div style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 36,
                fontWeight: 900,
                color: '#FEEC41',
                letterSpacing: 1,
              }}>{c.label}</div>
            </div>
          ))}
        </div>

        {/* Lock icon */}
        <svg
          width="90" height="90"
          viewBox="0 0 24 24"
          style={{
            position: 'absolute',
            top: 180,
            right: 240,
            opacity: numberIn,
            transform: `rotate(${-10 + Math.sin(frame * 0.08) * 4}deg)`,
          }}
        >
          <rect x="4" y="11" width="16" height="10" rx="2" fill="#000000" stroke="#000000" strokeWidth="1.5"/>
          <path d="M8 11V8a4 4 0 0 1 8 0v3" stroke="#000000" strokeWidth="2.5" fill="none"/>
          <circle cx="12" cy="16" r="1.5" fill="#FEEC41"/>
        </svg>

        {/* BOTTOM CAPTION */}
        <div style={{
          position: 'absolute',
          bottom: 95,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: footerIn,
          transform: `translateY(${(1 - footerIn) * 20}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            fontFamily: 'Inter, sans-serif',
            fontWeight: 800,
            fontSize: 38,
            color: '#000000',
            background: '#FEEC41',
            padding: '10px 30px',
            border: '4px solid #000000',
          }}>
            Based on your EX-SPOUSE's work record
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene03_FourLoopholes;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_HiddenRules',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_HiddenRules: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const tagIn = sp(5);
  const headlineIn = sp(25);
  const docIn = sp(70);
  const stamp = sp(110, { damping: 6, stiffness: 200 });
  const protectIn = sp(150);

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-0.3, 0.3], { extrapolateRight: 'clamp' });

  // Glitch shake on stamp
  const stampShake = frame > 110 && frame < 135 ? Math.sin(frame * 2) * 3 : 0;

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i + 15) * 1920,
    y: seededRandom(i + 25) * 1080,
    s: 2 + seededRandom(i + 35) * 4,
    sp: 0.3 + seededRandom(i + 45) * 0.4,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 20%, rgba(0,0,0,0.3) 0%, rgba(159,161,255,0) 60%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 600, height: 600,
        right: -150, top: -100,
        background: '#FEEC41',
        opacity: 0.06,
        borderRadius: 60,
        transform: `rotate(${30 + frame * 0.06}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 280, height: 280,
        left: 100, bottom: 80,
        borderRadius: '50%',
        border: '4px dashed #000000',
        opacity: 0.1,
        transform: `rotate(${frame * 0.5}deg)`,
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 18 }).map((_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 60} x2={1920} y2={i * 60} stroke="#000000" strokeWidth={1} />
        ))}
        {Array.from({ length: 32 }).map((_, i) => (
          <line key={`v${i}`} x1={i * 60} y1={0} x2={i * 60} y2={1080} stroke="#000000" strokeWidth={1} />
        ))}
      </svg>

      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          width: p.s, height: p.s,
          left: p.x,
          top: (p.y + frame * p.sp) % 1080,
          background: '#FEEC41', borderRadius: '50%', opacity: 0.45,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg) translateX(${stampShake}px)` }}>

        {/* TAG */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${tagIn})`,
          opacity: tagIn,
          background: '#000000',
          color: '#FEEC41',
          padding: '12px 32px',
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 800,
          fontSize: 28,
          letterSpacing: 6,
          border: '3px solid #FEEC41',
        }}>
          [ CLASSIFIED INFO ]
        </div>

        {/* HEADLINE */}
        <div style={{
          position: 'absolute',
          top: 175,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: headlineIn,
          transform: `translateY(${(1 - headlineIn) * 35}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 130,
            color: '#000000',
            lineHeight: 0.95,
            letterSpacing: -2,
          }}>
            THE SSA's
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 170,
            color: '#FEEC41',
            lineHeight: 0.95,
            textShadow: '6px 6px 0 #000000',
            letterSpacing: -3,
            marginTop: 6,
          }}>
            HIDDEN RULEBOOK
          </div>
        </div>

        {/* Document mock */}
        <div style={{
          position: 'absolute',
          bottom: 130,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - docIn) * 40}px) rotate(-2deg)`,
          opacity: docIn,
          width: 720,
          background: '#FEEC41',
          padding: '28px 38px',
          border: '5px solid #000000',
          boxShadow: '12px 12px 0 #000000',
          fontFamily: 'JetBrains Mono, monospace',
        }}>
          <div style={{
            fontSize: 22,
            color: '#000000',
            fontWeight: 700,
            letterSpacing: 2,
            opacity: 0.7,
          }}>SSA INTERNAL CODE §202</div>
          <div style={{ height: 3, background: '#000000', margin: '12px 0', opacity: 0.4 }} />
          {['Spousal benefit protection', 'Divorced-senior safeguards', 'Survivor income shield'].map((t, i) => (
            <div key={i} style={{
              display: 'flex',
              alignItems: 'center',
              gap: 14,
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              margin: '8px 0',
            }}>
              <svg width="22" height="22" viewBox="0 0 24 24">
                <path d="M4 12l5 5 11-12" stroke="#000000" strokeWidth="3" fill="none" strokeLinecap="round" strokeLinejoin="round"/>
              </svg>
              {t}
            </div>
          ))}
        </div>

        {/* Stamp — PROTECT YOU */}
        <div style={{
          position: 'absolute',
          bottom: 230,
          right: 180,
          transform: `rotate(-12deg) scale(${stamp})`,
          opacity: stamp,
          border: '6px solid #000000',
          padding: '14px 26px',
          background: 'transparent',
          fontFamily: 'Inter, sans-serif',
          fontWeight: 900,
          fontSize: 48,
          color: '#000000',
          letterSpacing: 3,
        }}>
          PROTECT YOU
        </div>

        {/* Bottom note */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: protectIn,
          fontFamily: 'Inter, sans-serif',
          fontWeight: 700,
          fontSize: 34,
          color: '#000000',
        }}>
          Designed to keep <span style={{ background: '#000000', color: '#FEEC41', padding: '2px 14px' }}>DIVORCED SENIORS</span> out of poverty.
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene04_HiddenRules;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_MoneyOnTable',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_MoneyOnTable: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const head1 = sp(5);
  const head2 = sp(25);
  const head3 = sp(50);
  const vaultIn = sp(75);
  const ticking = sp(140);

  // Vault breathing
  const vaultPulse = 1 + Math.sin(frame * 0.08) * 0.02;

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.05], { extrapolateRight: 'clamp' });

  // Cash bill drift
  const billDrift = (i: number) => Math.sin(frame * 0.05 + i) * 8;

  // Particles
  const particles = Array.from({ length: 26 }, (_, i) => ({
    x: seededRandom(i + 50) * 1920,
    y: seededRandom(i + 51) * 1080,
    s: 2 + seededRandom(i + 52) * 4,
    sp: 0.2 + seededRandom(i + 53) * 0.4,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(254,236,65,0.18) 0%, rgba(159,161,255,0) 40%, rgba(0,0,0,0.22) 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 500, height: 500,
        left: -150, top: 100,
        borderRadius: '50%',
        background: '#000000',
        opacity: 0.06,
      }} />
      <div style={{
        position: 'absolute',
        width: 400, height: 400,
        right: -100, bottom: -100,
        background: '#FEEC41',
        opacity: 0.07,
        transform: `rotate(${frame * 0.1}deg)`,
        borderRadius: 50,
      }} />

      {/* Texture rings */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {[200, 350, 500, 650].map((r, i) => (
          <circle key={i} cx={960} cy={540} r={r} stroke="#000000" strokeWidth={1} fill="none" strokeDasharray="6 8"/>
        ))}
      </svg>

      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          width: p.s, height: p.s,
          left: p.x,
          top: (p.y - frame * p.sp) % 1080,
          background: '#FEEC41', borderRadius: '50%', opacity: 0.5,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* LEFT — 3-line headline */}
        <div style={{
          position: 'absolute',
          top: 130,
          left: 100,
          fontFamily: 'Inter, sans-serif',
        }}>
          <div style={{
            fontWeight: 900, fontSize: 88, color: '#000000', lineHeight: 1.0,
            opacity: head1,
            transform: `translateX(${(1 - head1) * -40}px)`,
            letterSpacing: -2,
          }}>
            The money is
            <span style={{ background: '#FEEC41', padding: '0 14px', marginLeft: 12, color: '#000000' }}>SITTING THERE.</span>
          </div>
          <div style={{
            fontWeight: 900, fontSize: 88, color: '#000000', lineHeight: 1.0,
            opacity: head2,
            transform: `translateX(${(1 - head2) * -40}px)`,
            marginTop: 28,
            letterSpacing: -2,
          }}>
            The rights are
            <div style={{ color: '#FEEC41', textShadow: '5px 5px 0 #000000' }}>WRITTEN IN LAW.</div>
          </div>
          <div style={{
            fontWeight: 800, fontSize: 56, color: '#000000', lineHeight: 1.1,
            opacity: head3,
            transform: `translateY(${(1 - head3) * 25}px)`,
            marginTop: 40,
            maxWidth: 920,
          }}>
            But every month you wait =<br/>
            <span style={{ color: '#FF0000', fontWeight: 900 }}>another month LOST.</span>
          </div>
        </div>

        {/* RIGHT — Vault with cash */}
        <div style={{
          position: 'absolute',
          top: 200,
          right: 100,
          width: 480, height: 540,
          opacity: vaultIn,
          transform: `scale(${vaultPulse}) translateY(${(1 - vaultIn) * 50}px)`,
        }}>
          {/* Vault body */}
          <div style={{
            width: '100%', height: '100%',
            background: '#000000',
            border: '8px solid #FEEC41',
            borderRadius: 20,
            position: 'relative',
            boxShadow: '14px 14px 0 #FEEC41',
            padding: 30,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            gap: 20,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              color: '#FEEC41',
              fontWeight: 900,
              letterSpacing: 4,
            }}>FEDERAL VAULT</div>

            {/* Stacked cash */}
            {[0, 1, 2, 3].map((i) => (
              <div key={i} style={{
                width: 320,
                height: 70,
                background: '#FEEC41',
                border: '4px solid #000000',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'space-between',
                padding: '0 22px',
                transform: `translateX(${billDrift(i)}px) rotate(${-2 + i * 1.2}deg)`,
                boxShadow: '4px 4px 0 #000000',
              }}>
                <span style={{
                  fontFamily: 'Inter, sans-serif',
                  fontWeight: 900,
                  fontSize: 38,
                  color: '#000000',
                }}>$$$</span>
                <span style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontWeight: 700,
                  fontSize: 22,
                  color: '#000000',
                  opacity: 0.6,
                }}>YOURS</span>
              </div>
            ))}

            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontWeight: 900,
              fontSize: 36,
              color: '#FEEC41',
              textTransform: 'uppercase',
              letterSpacing: 2,
            }}>UNCLAIMED</div>
          </div>
        </div>

        {/* Ticking clock badge */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 100,
          opacity: ticking,
          transform: `translateY(${(1 - ticking) * 20}px)`,
          display: 'flex',
          alignItems: 'center',
          gap: 18,
        }}>
          <svg width="58" height="58" viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="10" stroke="#000000" strokeWidth="2.5" fill="#FEEC41"/>
            <path d="M12 7v5l3 2" stroke="#000000" strokeWidth="2.5" strokeLinecap="round" transform={`rotate(${frame * 6} 12 12)`}/>
          </svg>
          <span style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontWeight: 900,
            fontSize: 34,
            color: '#000000',
            letterSpacing: 3,
          }}>TIME = MONEY LOST</span>
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene05_MoneyOnTable;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_2026Guidelines',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_2026Guidelines: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const labelIn = sp(5);
  const yearIn = sp(20);
  const doc1 = sp(55);
  const doc2 = sp(80);
  const doc3 = sp(105);
  const stampIn = sp(150, { damping: 6, stiffness: 200 });

  // Camera orbit feel
  const orbitX = Math.sin(frame * 0.015) * 10;
  const orbitY = Math.cos(frame * 0.018) * 6;

  const particles = Array.from({ length: 24 }, (_, i) => ({
    x: seededRandom(i + 70) * 1920,
    y: seededRandom(i + 71) * 1080,
    s: 3 + seededRandom(i + 72) * 3,
    sp: 0.25 + seededRandom(i + 73) * 0.4,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, rgba(254,236,65,0.15) 0%, rgba(159,161,255,0) 60%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 700, height: 700,
        left: '50%', top: '50%',
        transform: `translate(-50%, -50%) rotate(${frame * 0.1}deg)`,
        border: '8px solid #000000',
        opacity: 0.05,
        borderRadius: 30,
      }} />
      <div style={{
        position: 'absolute',
        width: 400, height: 400,
        left: '50%', top: '50%',
        transform: `translate(-50%, -50%) rotate(${-frame * 0.15}deg)`,
        border: '6px solid #FEEC41',
        opacity: 0.12,
      }} />

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 22 }).map((_, r) =>
          Array.from({ length: 36 }).map((_, c) => (
            <circle key={`${r}-${c}`} cx={c * 55 + 20} cy={r * 55 + 20} r={1.2} fill="#000000" />
          ))
        )}
      </svg>

      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          width: p.s, height: p.s,
          left: p.x,
          top: (p.y + frame * p.sp) % 1080,
          background: '#FEEC41', borderRadius: '50%', opacity: 0.5,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `translate(${orbitX}px, ${orbitY}px)` }}>

        {/* TOP LABEL */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${labelIn})`,
          opacity: labelIn,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 30,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 6,
          padding: '12px 28px',
          border: '3px solid #000000',
          background: '#FEEC41',
        }}>
          OFFICIAL SOURCES
        </div>

        {/* HUGE 2026 */}
        <div style={{
          position: 'absolute',
          top: 145,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: yearIn,
          transform: `scale(${0.7 + yearIn * 0.3})`,
        }}>
          <span style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 250,
            color: '#FEEC41',
            textShadow: '8px 8px 0 #000000',
            letterSpacing: -8,
          }}>2026</span>
        </div>

        {/* 3 Documents row */}
        <div style={{
          position: 'absolute',
          bottom: 180,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 40,
        }}>
          {[
            { title: 'SSA', sub: 'GUIDELINES', op: doc1, rot: -3 },
            { title: 'SPOUSAL', sub: 'RULES', op: doc2, rot: 0 },
            { title: 'SURVIVOR', sub: 'LAWS', op: doc3, rot: 3 },
          ].map((d, i) => (
            <div key={i} style={{
              width: 280, height: 340,
              background: '#FEEC41',
              border: '5px solid #000000',
              padding: 24,
              opacity: d.op,
              transform: `translateY(${(1 - d.op) * 60}px) rotate(${d.rot}deg) scale(${0.85 + d.op * 0.15})`,
              boxShadow: '8px 8px 0 #000000',
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'space-between',
            }}>
              {/* Document icon */}
              <svg width="58" height="58" viewBox="0 0 24 24">
                <path d="M6 2h9l5 5v15H6V2z" fill="#000000"/>
                <path d="M15 2v5h5" stroke="#FEEC41" strokeWidth="1.5" fill="none"/>
                <line x1="9" y1="12" x2="17" y2="12" stroke="#FEEC41" strokeWidth="2"/>
                <line x1="9" y1="16" x2="17" y2="16" stroke="#FEEC41" strokeWidth="2"/>
                <line x1="9" y1="20" x2="14" y2="20" stroke="#FEEC41" strokeWidth="2"/>
              </svg>

              <div>
                <div style={{
                  fontFamily: 'Inter, sans-serif',
                  fontWeight: 900,
                  fontSize: 52,
                  color: '#000000',
                  lineHeight: 0.95,
                  letterSpacing: -1,
                }}>{d.title}</div>
                <div style={{
                  fontFamily: 'Inter, sans-serif',
                  fontWeight: 700,
                  fontSize: 32,
                  color: '#000000',
                  marginTop: 6,
                  letterSpacing: 1,
                }}>{d.sub}</div>
              </div>

              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.6,
                letterSpacing: 2,
              }}>VERIFIED ▸</div>
            </div>
          ))}
        </div>

        {/* APPROVED STAMP */}
        <div style={{
          position: 'absolute',
          top: 480,
          right: 240,
          transform: `rotate(-15deg) scale(${stampIn})`,
          opacity: stampIn,
          border: '6px double #FF0000',
          padding: '12px 24px',
          fontFamily: 'Inter, sans-serif',
          fontWeight: 900,
          fontSize: 38,
          color: '#FF0000',
          background: 'rgba(255,255,255,0.05)',
          letterSpacing: 4,
        }}>
          SCANNED
        </div>

        {/* Bottom strap */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 0, right: 0,
          textAlign: 'center',
          fontFamily: 'Inter, sans-serif',
          fontWeight: 800,
          fontSize: 38,
          color: '#000000',
          opacity: doc3,
        }}>
          Cross-referenced. <span style={{ color: '#FEEC41', background: '#000000', padding: '2px 14px' }}>4 Loopholes Found.</span>
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene06_2026Guidelines;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_MillionsIgnoring',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_MillionsIgnoring: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter — 0 to 12,400,000
  const counterRaw = interpolate(frame, [10, 120], [0, 12400000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const counter = Math.floor(counterRaw).toLocaleString();

  const headIn = sp(5);
  const counterIn = sp(15);
  const labelIn = sp(40);
  const peopleIn = sp(65);
  const lineIn = sp(140);

  // People row generated
  const peopleCount = 24;

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.04], { extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i + 90) * 1920,
    y: seededRandom(i + 91) * 1080,
    s: 2 + seededRandom(i + 92) * 3,
    sp: 0.2 + seededRandom(i + 93) * 0.4,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(0,0,0,0.2) 0%, rgba(159,161,255,0) 50%, rgba(254,236,65,0.18) 100%)',
      }} />

      <div style={{
        position: 'absolute',
        width: 700, height: 700,
        left: -200, top: -200,
        background: '#FEEC41', opacity: 0.05,
        borderRadius: '50%',
      }} />
      <div style={{
        position: 'absolute',
        width: 500, height: 500,
        right: -150, bottom: -150,
        border: '6px solid #000000', opacity: 0.07,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={i} x1={0} y1={i * 60 + frame * 0.3 % 60} x2={1920} y2={i * 60 + frame * 0.3 % 60} stroke="#000000" strokeWidth={1} />
        ))}
      </svg>

      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          width: p.s, height: p.s,
          left: p.x,
          top: (p.y - frame * p.sp) % 1080,
          background: '#FEEC41', borderRadius: '50%', opacity: 0.5,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* TOP HEADLINE */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: headIn,
          transform: `translateY(${(1 - headIn) * -30}px)`,
          fontFamily: 'Inter, sans-serif',
          fontWeight: 800,
          fontSize: 48,
          color: '#000000',
          letterSpacing: 2,
        }}>
          DIVORCED SENIORS WHO ARE
        </div>

        {/* COUNTER BOX */}
        <div style={{
          position: 'absolute',
          top: 160,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: counterIn,
          transform: `scale(${0.7 + counterIn * 0.3})`,
        }}>
          <div style={{
            display: 'inline-block',
            fontFamily: 'JetBrains Mono, monospace',
            fontWeight: 900,
            fontSize: 195,
            color: '#FEEC41',
            background: '#000000',
            padding: '12px 60px',
            border: '8px solid #FEEC41',
            letterSpacing: -2,
            boxShadow: '10px 10px 0 #FEEC41',
          }}>
            {counter}+
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 70,
            color: '#FF0000',
            marginTop: 14,
            textShadow: '4px 4px 0 #000000',
            letterSpacing: -1,
          }}>
            COMPLETELY IGNORING IT
          </div>
        </div>

        {/* People icons row */}
        <div style={{
          position: 'absolute',
          bottom: 240,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 14,
          opacity: peopleIn,
        }}>
          {Array.from({ length: peopleCount }).map((_, i) => {
            const ignored = i < peopleCount - 2;
            const delay = i * 4;
            const op = interpolate(frame - 65, [delay, delay + 12], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <svg key={i} width="42" height="62" viewBox="0 0 24 36" style={{ opacity: op }}>
                <circle cx="12" cy="8" r="6" fill={ignored ? '#000000' : '#FEEC41'} stroke="#000000" strokeWidth="1.5"/>
                <path d="M3 34 Q3 20 12 18 Q21 20 21 34 Z" fill={ignored ? '#000000' : '#FEEC41'} stroke="#000000" strokeWidth="1.5"/>
                {ignored && (
                  <line x1="2" y1="2" x2="22" y2="34" stroke="#FF0000" strokeWidth="3" />
                )}
              </svg>
            );
          })}
        </div>

        {/* LEGEND */}
        <div style={{
          position: 'absolute',
          bottom: 160,
          left: 0, right: 0,
          textAlign: 'center',
          display: 'flex',
          justifyContent: 'center',
          gap: 60,
          opacity: labelIn,
          fontFamily: 'Inter, sans-serif',
          fontSize: 28,
          fontWeight: 800,
        }}>
          <span style={{ color: '#000000', display: 'flex', alignItems: 'center', gap: 10 }}>
            <span style={{ width: 18, height: 18, background: '#000000', borderRadius: '50%' }}/> NOT CLAIMING
          </span>
          <span style={{ color: '#000000', display: 'flex', alignItems: 'center', gap: 10 }}>
            <span style={{ width: 18, height: 18, background: '#FEEC41', border: '2px solid #000000', borderRadius: '50%' }}/> CLAIMING
          </span>
        </div>

        {/* Bottom line */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: lineIn,
          transform: `translateY(${(1 - lineIn) * 20}px)`,
          fontFamily: 'Inter, sans-serif',
          fontWeight: 800,
          fontSize: 40,
          color: '#000000',
        }}>
          I'll walk through <span style={{ background: '#FEEC41', padding: '2px 14px' }}>EACH ONE.</span> Who qualifies. The numbers.
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene07_MillionsIgnoring;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_SurvivorTease',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_SurvivorTease: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const teaseIn = sp(5);
  const numIn = sp(25);
  const titleIn = sp(50);
  const beforeIn = sp(80);
  const arrowIn = sp(110);
  const afterIn = sp(130);

  // Counter before/after
  const before = 1500;
  const after = Math.floor(interpolate(frame, [130, 200], [1500, 3000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Shield pulse
  const shieldPulse = 1 + Math.sin(frame * 0.1) * 0.04;

  // Camera shake on number reveal
  const shake = frame > 130 && frame < 165 ? Math.sin(frame * 1.8) * 3 : 0;

  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i + 110) * 1920,
    y: seededRandom(i + 111) * 1080,
    s: 2 + seededRandom(i + 112) * 4,
    sp: 0.2 + seededRandom(i + 113) * 0.4,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, rgba(254,236,65,0.2) 0%, rgba(159,161,255,0) 60%)',
      }} />

      <div style={{
        position: 'absolute',
        width: 600, height: 600,
        left: '50%', top: '50%',
        transform: `translate(-50%, -50%) rotate(${frame * 0.08}deg)`,
        border: '8px dashed #000000',
        opacity: 0.08,
        borderRadius: 30,
      }} />
      <div style={{
        position: 'absolute',
        width: 350, height: 350,
        right: 80, top: 80,
        background: '#FEEC41', opacity: 0.07,
        borderRadius: '50%',
      }} />

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 26 }).map((_, i) => (
          <line key={i} x1={i * 80} y1={0} x2={i * 80 + 100} y2={1080} stroke="#000000" strokeWidth={1} />
        ))}
      </svg>

      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          width: p.s, height: p.s,
          left: (p.x + frame * p.sp) % 1920,
          top: p.y,
          background: '#FEEC41', borderRadius: '50%', opacity: 0.5,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `translateX(${shake}px)` }}>

        {/* TEASE TAG */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: '50%',
          transform: `translateX(-50%) scale(${teaseIn})`,
          opacity: teaseIn,
          background: '#FF0000',
          color: '#FEEC41',
          padding: '14px 36px',
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 900,
          fontSize: 28,
          letterSpacing: 6,
          border: '3px solid #000000',
        }}>
          PREVIEW · LOOPHOLE
        </div>

        {/* Big #3 */}
        <div style={{
          position: 'absolute',
          top: 140,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: numIn,
          transform: `scale(${0.6 + numIn * 0.4})`,
        }}>
          <span style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 240,
            color: '#FEEC41',
            textShadow: '8px 8px 0 #000000',
            letterSpacing: -8,
          }}>#3</span>
        </div>

        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 400,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: titleIn,
          transform: `translateY(${(1 - titleIn) * 20}px)`,
          fontFamily: 'Inter, sans-serif',
          fontWeight: 900,
          fontSize: 95,
          color: '#000000',
          letterSpacing: -2,
        }}>
          THE <span style={{ color: '#FEEC41', textShadow: '4px 4px 0 #000000' }}>SURVIVOR SHIELD</span>
        </div>

        {/* Before / After */}
        <div style={{
          position: 'absolute',
          bottom: 150,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          alignItems: 'center',
          gap: 50,
        }}>
          {/* Before */}
          <div style={{
            opacity: beforeIn,
            transform: `translateY(${(1 - beforeIn) * 40}px)`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 10,
          }}>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontWeight: 800,
              fontSize: 30,
              color: '#000000',
              letterSpacing: 3,
            }}>NOW</div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontWeight: 900,
              fontSize: 100,
              color: '#000000',
              background: '#FEEC41',
              padding: '10px 30px',
              border: '5px solid #000000',
              boxShadow: '6px 6px 0 #000000',
            }}>${before}</div>
          </div>

          {/* Arrow */}
          <svg
            width="140" height="80"
            viewBox="0 0 140 80"
            style={{
              opacity: arrowIn,
              transform: `scale(${arrowIn}) translateX(${Math.sin(frame * 0.12) * 5}px)`,
            }}
          >
            <path d="M5 40 L 110 40" stroke="#000000" strokeWidth="10" strokeLinecap="round"/>
            <path d="M95 18 L 130 40 L 95 62" stroke="#000000" strokeWidth="10" strokeLinecap="round" strokeLinejoin="round" fill="none"/>
            <text x="70" y="22" textAnchor="middle" fill="#FF0000" fontFamily="Inter" fontWeight="900" fontSize="26">2X</text>
          </svg>

          {/* After */}
          <div style={{
            opacity: afterIn,
            transform: `translateY(${(1 - afterIn) * 40}px) scale(${0.9 + afterIn * 0.1})`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 10,
          }}>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontWeight: 800,
              fontSize: 30,
              color: '#FF0000',
              letterSpacing: 3,
            }}>AFTER</div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontWeight: 900,
              fontSize: 100,
              color: '#FEEC41',
              background: '#000000',
              padding: '10px 30px',
              border: '5px solid #FEEC41',
              boxShadow: '6px 6px 0 #FEEC41',
              transform: `scale(${shieldPulse})`,
            }}>${after}</div>
          </div>
        </div>

        {/* Bottom line */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: 0, right: 0,
          textAlign: 'center',
          fontFamily: 'Inter, sans-serif',
          fontWeight: 800,
          fontSize: 34,
          color: '#000000',
          opacity: afterIn,
        }}>
          Literally <span style={{ background: '#000000', color: '#FEEC41', padding: '2px 14px' }}>DOUBLES</span> your monthly check.
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene08_SurvivorTease;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_MultiMarriedSub',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_MultiMarriedSub: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const numIn = sp(5);
  const titleIn = sp(25);
  const ringsIn = sp(55);
  const subIn = sp(110);
  const btnPulse = 1 + Math.sin(frame * 0.18) * 0.05;
  const btnIn = sp(140);

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.05], { extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 24 }, (_, i) => ({
    x: seededRandom(i + 130) * 1920,
    y: seededRandom(i + 131) * 1080,
    s: 2 + seededRandom(i + 132) * 4,
    sp: 0.25 + seededRandom(i + 133) * 0.4,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(45deg, rgba(254,236,65,0.15) 0%, rgba(159,161,255,0) 50%, rgba(0,0,0,0.2) 100%)',
      }} />

      <div style={{
        position: 'absolute',
        width: 500, height: 500,
        left: -100, bottom: -100,
        background: '#FEEC41', opacity: 0.07,
        borderRadius: '50%',
      }} />
      <div style={{
        position: 'absolute',
        width: 350, height: 350,
        right: 100, top: 100,
        border: '5px solid #000000', opacity: 0.08,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 22 }).map((_, r) =>
          Array.from({ length: 36 }).map((_, c) => (
            <circle key={`${r}-${c}`} cx={c * 55 + 20} cy={r * 55 + 20} r={1.5} fill="#000000" />
          ))
        )}
      </svg>

      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          width: p.s, height: p.s,
          left: p.x,
          top: (p.y + frame * p.sp) % 1080,
          background: '#FEEC41', borderRadius: '50%', opacity: 0.5,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* TOP — #4 + Title */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 100,
          opacity: numIn,
          transform: `scale(${0.7 + numIn * 0.3}) translateY(${(1 - numIn) * -20}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 220,
            color: '#FEEC41',
            textShadow: '6px 6px 0 #000000',
            lineHeight: 0.9,
            letterSpacing: -6,
          }}>#4</div>
        </div>

        <div style={{
          position: 'absolute',
          top: 110,
          left: 400,
          opacity: titleIn,
          transform: `translateX(${(1 - titleIn) * -30}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 800,
            fontSize: 40,
            color: '#000000',
            letterSpacing: 3,
          }}>LOOPHOLE FOR</div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 110,
            color: '#000000',
            lineHeight: 0.95,
            marginTop: 10,
            letterSpacing: -2,
          }}>
            MARRIED <span style={{ background: '#FEEC41', padding: '0 14px' }}>MORE</span>
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 110,
            color: '#000000',
            lineHeight: 0.95,
            letterSpacing: -2,
          }}>
            THAN <span style={{ color: '#FEEC41', textShadow: '4px 4px 0 #000000' }}>ONCE.</span>
          </div>
        </div>

        {/* Three interlocking rings */}
        <div style={{
          position: 'absolute',
          bottom: 240,
          left: '50%',
          transform: `translateX(-50%)`,
          opacity: ringsIn,
        }}>
          <svg width="600" height="200" viewBox="0 0 600 200">
            {[
              { cx: 130, color: '#000000', delay: 0 },
              { cx: 300, color: '#FEEC41', delay: 8 },
              { cx: 470, color: '#000000', delay: 16 },
            ].map((r, i) => {
              const local = Math.max(0, frame - 55 - r.delay);
              const scl = spring({ frame: local, fps: 30, config: { damping: 10, stiffness: 100 } });
              return (
                <circle key={i}
                  cx={r.cx}
                  cy={100 + Math.sin(frame * 0.06 + i) * 6}
                  r={80 * scl}
                  stroke={r.color}
                  strokeWidth="14"
                  fill="none"
                />
              );
            })}
            <text x="130" y="115" textAnchor="middle" fill="#000000" fontFamily="Inter" fontWeight="900" fontSize="48">1st</text>
            <text x="300" y="115" textAnchor="middle" fill="#000000" fontFamily="Inter" fontWeight="900" fontSize="48">2nd</text>
            <text x="470" y="115" textAnchor="middle" fill="#000000" fontFamily="Inter" fontWeight="900" fontSize="48">3rd</text>
          </svg>
        </div>

        {/* SUBSCRIBE STRIP */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${btnIn * btnPulse})`,
          opacity: btnIn,
          display: 'flex',
          alignItems: 'center',
          gap: 24,
          padding: '20px 44px',
          background: '#FF0000',
          border: '5px solid #000000',
          boxShadow: '10px 10px 0 #000000',
        }}>
          {/* Bell icon */}
          <svg width="50" height="50" viewBox="0 0 24 24" style={{
            transform: `rotate(${Math.sin(frame * 0.3) * 15}deg)`,
            transformOrigin: 'top center',
          }}>
            <path d="M12 2C8 2 6 5 6 9v5l-2 3h16l-2-3V9c0-4-2-7-6-7z" fill="#FEEC41" stroke="#000000" strokeWidth="2"/>
            <path d="M10 19a2 2 0 0 0 4 0" stroke="#000000" strokeWidth="2.5" fill="none"/>
          </svg>
          <span style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 50,
            color: '#FEEC41',
            letterSpacing: 3,
          }}>SUBSCRIBE NOW</span>
        </div>

        {/* Side mini chip */}
        <div style={{
          position: 'absolute',
          bottom: 220,
          right: 140,
          opacity: subIn,
          transform: `rotate(8deg) scale(${subIn})`,
          background: '#000000',
          color: '#FEEC41',
          padding: '14px 22px',
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 900,
          fontSize: 26,
          border: '3px solid #FEEC41',
        }}>
          + NOTIFICATIONS
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene09_MultiMarriedSub;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_RulesChangeYearly',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_RulesChangeYearly: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const tagIn = sp(5);
  const headIn = sp(25);
  const tl1 = sp(55);
  const tl2 = sp(70);
  const tl3 = sp(85);
  const tl4 = sp(100);
  const ctaIn = sp(150);

  // Camera pan
  const camPan = interpolate(frame, [0, durationInFrames], [15, -15], { extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i + 150) * 1920,
    y: seededRandom(i + 151) * 1080,
    s: 2 + seededRandom(i + 152) * 4,
    sp: 0.25 + seededRandom(i + 153) * 0.4,
  }));

  // Timeline line draw
  const lineDraw = interpolate(frame, [50, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 50%, rgba(254,236,65,0.18) 0%, rgba(159,161,255,0) 55%), radial-gradient(ellipse at 80% 80%, rgba(0,0,0,0.22) 0%, rgba(159,161,255,0) 60%)',
      }} />

      <div style={{
        position: 'absolute',
        width: 600, height: 600,
        right: -200, top: -150,
        background: '#FEEC41', opacity: 0.06,
        borderRadius: 40,
        transform: `rotate(${20 + frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 350, height: 350,
        left: -80, bottom: -80,
        border: '5px solid #000000', opacity: 0.1,
        borderRadius: '50%',
      }} />

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 18 }).map((_, i) => (
          <line key={i} x1={0} y1={i * 60} x2={1920} y2={i * 60 + 30} stroke="#000000" strokeWidth={1} />
        ))}
      </svg>

      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          width: p.s, height: p.s,
          left: (p.x - frame * p.sp) % 1920,
          top: p.y,
          background: '#FEEC41', borderRadius: '50%', opacity: 0.5,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>

        {/* Tag */}
        <div style={{
          position: 'absolute',
          top: 75,
          left: '50%',
          transform: `translateX(-50%) scale(${tagIn})`,
          opacity: tagIn,
          padding: '12px 30px',
          background: '#000000',
          color: '#FEEC41',
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 800,
          fontSize: 26,
          letterSpacing: 5,
          border: '3px solid #FEEC41',
        }}>
          RULES CHANGE · EVERY YEAR
        </div>

        {/* Headline */}
        <div style={{
          position: 'absolute',
          top: 150,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: headIn,
          transform: `translateY(${(1 - headIn) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 115,
            color: '#000000',
            lineHeight: 0.95,
            letterSpacing: -2,
          }}>
            Stay <span style={{ color: '#FEEC41', textShadow: '5px 5px 0 #000000' }}>AHEAD</span>
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontWeight: 800,
            fontSize: 52,
            color: '#000000',
            marginTop: 10,
            letterSpacing: 1,
          }}>
            of every social security update.
          </div>
        </div>

        {/* Timeline */}
        <div style={{
          position: 'absolute',
          bottom: 280,
          left: 0, right: 0,
          height: 180,
        }}>
          {/* Line */}
          <svg width="1920" height="180" style={{ position: 'absolute' }}>
            <line x1={200} y1={90} x2={200 + (1520 * lineDraw)} y2={90} stroke="#000000" strokeWidth="6" strokeLinecap="round"/>
          </svg>

          {/* Years */}
          {[
            { x: 280, year: '2024', label: 'OLD', op: tl1, color: '#000000', txt: '#FEEC41' },
            { x: 660, year: '2025', label: 'PREV', op: tl2, color: '#000000', txt: '#FEEC41' },
            { x: 1040, year: '2026', label: 'NOW', op: tl3, color: '#FEEC41', txt: '#000000' },
            { x: 1420, year: '2027', label: 'NEXT', op: tl4, color: '#000000', txt: '#FEEC41' },
          ].map((p, i) => (
            <div key={i} style={{
              position: 'absolute',
              left: p.x,
              top: 0,
              opacity: p.op,
              transform: `translateY(${(1 - p.op) * 40}px) scale(${0.8 + p.op * 0.2})`,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              gap: 6,
            }}>
              <div style={{
                width: 24, height: 24,
                background: p.color,
                border: '4px solid #000000',
                borderRadius: '50%',
                marginBottom: 4,
              }}/>
              <div style={{
                padding: '10px 20px',
                background: p.color,
                color: p.txt,
                fontFamily: 'JetBrains Mono, monospace',
                fontWeight: 900,
                fontSize: 42,
                border: '4px solid #000000',
                boxShadow: '4px 4px 0 #000000',
                letterSpacing: 1,
              }}>{p.year}</div>
              <div style={{
                fontFamily: 'Inter, sans-serif',
                fontWeight: 800,
                fontSize: 24,
                color: '#000000',
                letterSpacing: 2,
                marginTop: 4,
              }}>{p.label}</div>
            </div>
          ))}
        </div>

        {/* CTA bottom */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: ctaIn,
          transform: `translateY(${(1 - ctaIn) * 30}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            fontFamily: 'Inter, sans-serif',
            fontWeight: 900,
            fontSize: 44,
            color: '#000000',
            background: '#FEEC41',
            padding: '14px 36px',
            border: '5px solid #000000',
            boxShadow: '8px 8px 0 #000000',
          }}>
            Plain English. Before it's <span style={{ color: '#FF0000' }}>TOO LATE.</span>
          </div>
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene10_RulesChangeYearly;
```

