```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_MayMorning',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_MayMorning: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const cameraZoom = interpolate(frame, [0, durationInFrames], [1, 1.08], { extrapolateRight: 'clamp' });

  // Sun pulse
  const sunPulse = 1 + Math.sin(frame * 0.08) * 0.04;
  const sunRayRotate = frame * 0.3;

  // Heat wave
  const heatWave = Math.sin(frame * 0.1) * 8;

  // Calendar entry
  const calY = interpolate(sp(20), [0, 1], [200, 0]);
  const dateScale = sp(45);

  // Clock hands
  const minuteHand = frame * 1.2;
  const hourHand = frame * 0.1;

  // Particles
  const particles = Array.from({ length: 30 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#0D0B61', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 40%, rgba(180,229,13,0.12) 0%, transparent 60%), linear-gradient(135deg, #0D0B61 0%, #1a1880 100%)' }} />

      {/* Shape layers */}
      <div style={{ position: 'absolute', top: '10%', left: '5%', width: 400, height: 400, border: '3px solid #B4E50D', borderRadius: '50%', opacity: 0.06, transform: `rotate(${frame * 0.4}deg) scale(${1 + Math.sin(frame * 0.03) * 0.1})` }} />
      <div style={{ position: 'absolute', bottom: '8%', right: '8%', width: 500, height: 500, border: '2px solid #FFF9BD', opacity: 0.05, transform: `rotate(${-frame * 0.3}deg)` }} />
      <div style={{ position: 'absolute', top: '50%', left: '75%', width: 300, height: 300, background: '#B4E50D', opacity: 0.04, transform: `translate(-50%,-50%) rotate(${frame * 0.5}deg)`, clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)' }} />

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.06 }}>
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#FFF9BD" strokeWidth="1"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid1)" />
      </svg>

      {/* Particles */}
      {particles.map((i) => {
        const px = (seededRandom(i + 1) * 1920);
        const py = (seededRandom(i + 7) * 1080 + frame * (0.5 + seededRandom(i + 3) * 0.8)) % 1080;
        const size = 3 + seededRandom(i + 5) * 5;
        return <div key={i} style={{ position: 'absolute', left: px, top: py, width: size, height: size, borderRadius: '50%', background: '#FFF9BD', opacity: 0.4 }} />;
      })}

      {/* Main content - scaled by camera */}
      <AbsoluteFill style={{ transform: `scale(${cameraZoom})` }}>
        
        {/* Big sun visual on the right */}
        <div style={{ position: 'absolute', top: '20%', right: '12%', transform: `scale(${sp(10) * sunPulse})` }}>
          <svg width="380" height="380" viewBox="0 0 380 380">
            <g transform={`translate(190 190) rotate(${sunRayRotate})`}>
              {Array.from({ length: 12 }).map((_, i) => (
                <rect key={i} x="-4" y="-170" width="8" height="50" fill="#B4E50D" opacity={0.7} transform={`rotate(${i * 30})`} />
              ))}
            </g>
            <circle cx="190" cy="190" r="90" fill="#FFF9BD" />
            <circle cx="190" cy="190" r="90" fill="#B4E50D" opacity="0.3" />
          </svg>
          <div style={{ position: 'absolute', top: '50%', left: '50%', transform: 'translate(-50%,-50%)', fontFamily: 'Inter', fontSize: 28, fontWeight: 700, color: '#0D0B61' }}>42°C</div>
        </div>

        {/* Heat wave lines */}
        <svg style={{ position: 'absolute', top: '55%', right: '10%', width: 400, height: 200 }}>
          {Array.from({ length: 5 }).map((_, i) => (
            <path key={i} d={`M 0 ${30 + i * 30} Q 100 ${30 + i * 30 + heatWave}, 200 ${30 + i * 30} T 400 ${30 + i * 30}`} stroke="#FFF9BD" strokeWidth="2" fill="none" opacity={0.4} />
          ))}
        </svg>

        {/* Calendar card on left */}
        <div style={{ position: 'absolute', top: '25%', left: '8%', width: 520, transform: `translateY(${calY}px)`, opacity: sp(20) }}>
          <div style={{ background: '#FFF9BD', borderRadius: 24, padding: '24px 32px 32px', boxShadow: '0 30px 60px rgba(0,0,0,0.4)' }}>
            <div style={{ background: '#B4E50D', height: 12, borderRadius: 6, marginBottom: 18 }} />
            <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 700, color: '#0D0B61', letterSpacing: 2 }}>MAY 2026</div>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(7,1fr)', gap: 8, marginTop: 14 }}>
              {['M','T','W','T','F','S','S'].map((d, i) => (
                <div key={i} style={{ fontFamily: 'Inter', fontSize: 18, color: '#0D0B61', opacity: 0.6, textAlign: 'center' }}>{d}</div>
              ))}
              {Array.from({ length: 21 }).map((_, i) => {
                const isToday = i === 13;
                return (
                  <div key={i} style={{
                    fontFamily: 'Inter', fontSize: 22, fontWeight: isToday ? 800 : 500,
                    color: isToday ? '#FFF9BD' : '#0D0B61',
                    background: isToday ? '#0D0B61' : 'transparent',
                    borderRadius: 8, padding: '6px 0', textAlign: 'center',
                    transform: isToday ? `scale(${dateScale})` : 'none',
                  }}>{i + 1}</div>
                );
              })}
            </div>
          </div>
        </div>

        {/* Hindi label */}
        <div style={{ position: 'absolute', bottom: '12%', left: '8%', opacity: sp(60) }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 64, fontWeight: 800, color: '#FFF9BD', lineHeight: 1.1 }}>
            मई 2026 — गुरुवार
          </div>
          <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 38, color: '#B4E50D', marginTop: 8 }}>
            तप्ता हुआ दिन
          </div>
        </div>

        {/* Thermometer icon bottom right */}
        <div style={{ position: 'absolute', bottom: '20%', right: '15%', transform: `scale(${sp(70)})` }}>
          <svg width="80" height="200" viewBox="0 0 80 200">
            <rect x="30" y="10" width="20" height="140" rx="10" fill="none" stroke="#FFF9BD" strokeWidth="3" />
            <rect x="33" y={150 - Math.min(130, frame * 1.5)} width="14" height={Math.min(130, frame * 1.5)} fill="#B4E50D" />
            <circle cx="40" cy="170" r="22" fill="#B4E50D" />
          </svg>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1_MayMorning;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_CityProfile',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_CityProfile: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const cameraX = interpolate(frame, [0, durationInFrames], [0, -40], { extrapolateRight: 'clamp' });

  // Profile card pulse
  const profilePulse = 1 + Math.sin(frame * 0.06) * 0.02;

  // Age counter
  const ageCount = Math.min(55, Math.floor(interpolate(frame, [80, 140], [0, 55], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })));

  // City pin highlight rotates
  const activeCity = Math.floor((frame / 50) % 3);

  const cities = [
    { name: 'पुणे', en: 'PUNE', x: '20%', y: '55%' },
    { name: 'गुरुग्राम', en: 'GURUGRAM', x: '38%', y: '32%' },
    { name: 'अहमदाबाद', en: 'AHMEDABAD', x: '12%', y: '40%' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#0D0B61', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 30%, rgba(180,229,13,0.1) 0%, transparent 55%)' }} />

      {/* Diagonal stripes */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.05 }}>
        <defs>
          <pattern id="stripes2" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#B4E50D" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#stripes2)" />
      </svg>

      {/* Shape layer - hexagons */}
      <div style={{ position: 'absolute', top: '10%', right: '5%', width: 350, height: 350, opacity: 0.06, transform: `rotate(${frame * 0.4}deg)` }}>
        <svg viewBox="0 0 100 100"><polygon points="50,5 90,27 90,73 50,95 10,73 10,27" fill="none" stroke="#FFF9BD" strokeWidth="1"/></svg>
      </div>
      <div style={{ position: 'absolute', bottom: '15%', left: '40%', width: 200, height: 200, opacity: 0.05, transform: `rotate(${-frame * 0.5}deg) scale(${1 + Math.sin(frame * 0.05) * 0.15})` }}>
        <svg viewBox="0 0 100 100"><polygon points="50,5 90,27 90,73 50,95 10,73 10,27" fill="#B4E50D"/></svg>
      </div>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = (seededRandom(i + 11) * 1920 + frame * 0.3) % 1920;
        const py = seededRandom(i + 23) * 1080;
        return <div key={i} style={{ position: 'absolute', left: px, top: py, width: 4, height: 4, borderRadius: '50%', background: '#FFF9BD', opacity: 0.3 }} />;
      })}

      <AbsoluteFill style={{ transform: `translateX(${cameraX}px)` }}>
        
        {/* India map silhouette on left */}
        <div style={{ position: 'absolute', top: '15%', left: '4%', width: 700, height: 720, opacity: sp(10) }}>
          <svg viewBox="0 0 400 450" style={{ width: '100%', height: '100%' }}>
            {/* simplified india outline */}
            <path d="M 180 30 Q 240 40 270 80 Q 310 110 320 160 Q 340 200 320 250 Q 310 300 270 340 Q 240 380 200 410 Q 170 430 150 410 Q 120 380 100 340 Q 80 290 90 240 Q 80 190 100 140 Q 120 90 150 60 Q 165 40 180 30 Z" 
              fill="rgba(180,229,13,0.08)" stroke="#B4E50D" strokeWidth="2" />
            
            {cities.map((c, i) => {
              const isActive = i === activeCity;
              const x = 100 + i * 70;
              const y = 150 + i * 60;
              const px = i === 0 ? 130 : i === 1 ? 200 : 110;
              const py = i === 0 ? 240 : i === 1 ? 130 : 180;
              return (
                <g key={i}>
                  <circle cx={px} cy={py} r={isActive ? 18 : 8} fill={isActive ? '#B4E50D' : '#FFF9BD'} opacity={isActive ? 1 : 0.6}>
                  </circle>
                  {isActive && <circle cx={px} cy={py} r={18 + (frame % 30)} fill="none" stroke="#B4E50D" strokeWidth="2" opacity={1 - (frame % 30) / 30} />}
                  <text x={px + 25} y={py + 5} fontFamily="'Noto Sans Devanagari'" fontSize="22" fontWeight="700" fill={isActive ? '#B4E50D' : '#FFF9BD'}>{c.name}</text>
                </g>
              );
            })}
          </svg>
        </div>

        {/* Profile card on right */}
        <div style={{ position: 'absolute', top: '20%', right: '6%', width: 620, transform: `scale(${sp(40) * profilePulse})`, opacity: sp(40) }}>
          <div style={{ background: '#FFF9BD', borderRadius: 28, padding: 40, boxShadow: '0 30px 80px rgba(0,0,0,0.5)' }}>
            
            {/* Person silhouette icon */}
            <div style={{ display: 'flex', alignItems: 'center', gap: 20, marginBottom: 30 }}>
              <div style={{ width: 100, height: 100, background: '#0D0B61', borderRadius: '50%', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                <svg width="60" height="60" viewBox="0 0 60 60">
                  <circle cx="30" cy="20" r="12" fill="#B4E50D" />
                  <path d="M 10 55 Q 30 35 50 55 L 50 60 L 10 60 Z" fill="#B4E50D" />
                </svg>
              </div>
              <div>
                <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#0D0B61', opacity: 0.6, letterSpacing: 2 }}>PROFILE</div>
                <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 38, color: '#0D0B61', fontWeight: 800 }}>मिडल क्लास</div>
              </div>
            </div>

            {/* Age block */}
            <div style={{ background: '#0D0B61', borderRadius: 18, padding: 24, marginBottom: 20 }}>
              <div style={{ fontFamily: 'Inter', fontSize: 16, color: '#FFF9BD', opacity: 0.7, letterSpacing: 3 }}>उम्र / AGE</div>
              <div style={{ display: 'flex', alignItems: 'baseline', gap: 12 }}>
                <div style={{ fontFamily: "'Yantra One'", fontSize: 130, color: '#B4E50D', lineHeight: 1, fontWeight: 800 }}>{ageCount}</div>
                <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 32, color: '#FFF9BD' }}>साल</div>
              </div>
            </div>

            {/* Detail rows */}
            {[
              { label: 'शहर', val: 'पुणे / गुरुग्राम / अहमदाबाद', delay: 70 },
              { label: 'स्थिति', val: 'Salaried Professional', delay: 85 },
            ].map((row, i) => (
              <div key={i} style={{ borderTop: '2px dashed rgba(13,11,97,0.2)', padding: '14px 0', display: 'flex', justifyContent: 'space-between', alignItems: 'center', opacity: sp(row.delay) }}>
                <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 24, color: '#0D0B61', opacity: 0.7 }}>{row.label}</div>
                <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 24, color: '#0D0B61', fontWeight: 700 }}>{row.val}</div>
              </div>
            ))}
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_CityProfile;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_DiversifiedPortfolio',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_DiversifiedPortfolio: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Tilt camera
  const cameraTilt = interpolate(frame, [0, durationInFrames], [-1, 1], { extrapolateRight: 'clamp' });

  // Pie chart segments
  const segments = [
    { label: 'Equity', val: 35, color: '#B4E50D', delay: 30 },
    { label: 'Debt', val: 25, color: '#FFF9BD', delay: 45 },
    { label: 'Gold', val: 15, color: '#FFC107', delay: 60 },
    { label: 'Real Estate', val: 15, color: '#9E9E9E', delay: 75 },
    { label: 'Cash', val: 10, color: '#ff5577', delay: 90 },
  ];

  // Chart line going down then resilient
  const linePath = () => {
    const points: string[] = [];
    for (let i = 0; i <= Math.min(frame * 2, 200); i++) {
      const x = (i / 200) * 600;
      const y = 100 + Math.sin(i * 0.06) * 30 + (i > 100 ? -((i - 100) * 0.3) : 0);
      points.push(`${i === 0 ? 'M' : 'L'} ${x} ${y}`);
    }
    return points.join(' ');
  };

  let cumulative = 0;
  const drawProgress = sp(20, { damping: 14, stiffness: 80 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#0D0B61', overflow: 'hidden', opacity: exitOp, transform: `rotate(${cameraTilt * 0.3}deg)` }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 100%, rgba(180,229,13,0.12) 0%, transparent 60%)' }} />

      {/* Concentric circles texture */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.06 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <circle key={i} cx="960" cy="540" r={120 + i * 100} fill="none" stroke="#FFF9BD" strokeWidth="1" />
        ))}
      </svg>

      {/* Shape layers */}
      <div style={{ position: 'absolute', top: '5%', left: '3%', width: 280, height: 280, background: '#B4E50D', opacity: 0.05, borderRadius: '50%', transform: `scale(${1 + Math.sin(frame * 0.04) * 0.1})` }} />
      <div style={{ position: 'absolute', bottom: '5%', right: '5%', width: 350, height: 350, border: '4px solid #FFF9BD', opacity: 0.06, transform: `rotate(${frame * 0.3}deg)` }} />

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const px = seededRandom(i + 33) * 1920;
        const py = (seededRandom(i + 41) * 1080 - frame * 0.6) % 1080;
        const adj = py < 0 ? py + 1080 : py;
        return <div key={i} style={{ position: 'absolute', left: px, top: adj, width: 5, height: 5, borderRadius: '50%', background: '#B4E50D', opacity: 0.4 }} />;
      })}

      {/* Header */}
      <div style={{ position: 'absolute', top: 70, left: 0, right: 0, textAlign: 'center', opacity: sp(5) }}>
        <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#B4E50D', letterSpacing: 6, marginBottom: 8 }}>PORTFOLIO STRATEGY</div>
        <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 75, fontWeight: 800, color: '#FFF9BD' }}>
          डाइवर्सिफाइड पोर्टफोलियो
        </div>
      </div>

      {/* Donut chart on left */}
      <div style={{ position: 'absolute', top: '32%', left: '8%', width: 480, height: 480 }}>
        <svg viewBox="0 0 200 200" style={{ width: '100%', height: '100%', transform: `rotate(${frame * 0.3}deg)` }}>
          {segments.map((s, i) => {
            const start = cumulative;
            cumulative += s.val;
            const end = cumulative;
            const startAngle = (start / 100) * Math.PI * 2 - Math.PI / 2;
            const endAngle = (end / 100) * Math.PI * 2 - Math.PI / 2;
            const segProgress = sp(s.delay, { damping: 14, stiffness: 90 });
            const actualEnd = startAngle + (endAngle - startAngle) * segProgress;
            const largeArc = (end - start) > 50 ? 1 : 0;
            const x1 = 100 + 80 * Math.cos(startAngle);
            const y1 = 100 + 80 * Math.sin(startAngle);
            const x2 = 100 + 80 * Math.cos(actualEnd);
            const y2 = 100 + 80 * Math.sin(actualEnd);
            return (
              <path key={i} d={`M 100 100 L ${x1} ${y1} A 80 80 0 ${largeArc} 1 ${x2} ${y2} Z`} fill={s.color} stroke="#0D0B61" strokeWidth="2" />
            );
          })}
          <circle cx="100" cy="100" r="42" fill="#0D0B61" />
        </svg>
        <div style={{ position: 'absolute', top: '50%', left: '50%', transform: 'translate(-50%, -50%)', textAlign: 'center' }}>
          <div style={{ fontFamily: "'Yantra One'", fontSize: 60, color: '#B4E50D', fontWeight: 800 }}>100%</div>
          <div style={{ fontFamily: 'Inter', fontSize: 16, color: '#FFF9BD', letterSpacing: 2 }}>SPREAD</div>
        </div>
      </div>

      {/* Legend on right */}
      <div style={{ position: 'absolute', top: '32%', right: '8%', width: 540 }}>
        {segments.map((s, i) => (
          <div key={i} style={{
            display: 'flex', alignItems: 'center', gap: 18, marginBottom: 18,
            background: 'rgba(255,249,189,0.08)', padding: '14px 20px', borderRadius: 14,
            opacity: sp(s.delay + 10), transform: `translateX(${(1 - sp(s.delay + 10)) * 80}px)`,
          }}>
            <div style={{ width: 24, height: 60, background: s.color, borderRadius: 6 }} />
            <div style={{ flex: 1 }}>
              <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#FFF9BD', fontWeight: 700 }}>{s.label}</div>
            </div>
            <div style={{ fontFamily: "'Yantra One'", fontSize: 44, color: s.color, fontWeight: 800 }}>{s.val}%</div>
          </div>
        ))}
      </div>

      {/* Resilience line at bottom */}
      <div style={{ position: 'absolute', bottom: '5%', left: '50%', transform: 'translateX(-50%)', width: 600, opacity: sp(120) }}>
        <svg width="600" height="160" viewBox="0 0 600 160">
          <path d="M 0 80 L 600 80" stroke="#FFF9BD" strokeWidth="1" opacity="0.3" strokeDasharray="6 6" />
          <path d={linePath()} stroke="#B4E50D" strokeWidth="4" fill="none" />
          <text x="0" y="155" fontFamily="Inter" fontSize="18" fill="#FFF9BD" opacity="0.7">क्रैश के बावजूद हिम्मत बरकरार</text>
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3_DiversifiedPortfolio;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4_OpeningDemat',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_OpeningDemat: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom into phone
  const cameraScale = interpolate(frame, [0, 60, durationInFrames], [0.9, 1, 1.05], { extrapolateRight: 'clamp' });

  // Tap ripple
  const tapFrame = 50;
  const rippleProgress = Math.max(0, frame - tapFrame);
  const rippleScale = interpolate(rippleProgress, [0, 30], [0, 4], { extrapolateRight: 'clamp' });
  const rippleOp = interpolate(rippleProgress, [0, 30], [0.8, 0], { extrapolateRight: 'clamp' });

  // Loading dots
  const dot1 = (frame % 30) < 10 ? 1 : 0.3;
  const dot2 = (frame % 30) >= 10 && (frame % 30) < 20 ? 1 : 0.3;
  const dot3 = (frame % 30) >= 20 ? 1 : 0.3;

  // Number ticker for years
  const yearCount = Math.min(15, Math.floor(interpolate(frame, [10, 70], [0, 15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })));

  // Hourglass rotate
  const hourglassRotate = Math.floor(frame / 60) * 180;

  return (
    <AbsoluteFill style={{ backgroundColor: '#0D0B61', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 50%, rgba(180,229,13,0.1) 0%, transparent 55%)' }} />

      {/* Wave texture */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.05 }}>
        {Array.from({ length: 6 }).map((_, i) => (
          <path key={i} d={`M 0 ${200 + i * 130} Q 480 ${180 + i * 130 + Math.sin(frame * 0.04 + i) * 20} 960 ${200 + i * 130} T 1920 ${200 + i * 130}`} stroke="#B4E50D" strokeWidth="2" fill="none" />
        ))}
      </svg>

      {/* Shape layers */}
      <div style={{ position: 'absolute', top: '8%', right: '8%', width: 220, height: 220, opacity: 0.06 }}>
        <svg viewBox="0 0 100 100"><polygon points="50,5 95,50 50,95 5,50" fill="#FFF9BD" transform={`rotate(${frame * 0.5} 50 50)`} /></svg>
      </div>
      <div style={{ position: 'absolute', bottom: '15%', left: '5%', width: 320, height: 320, border: '3px solid #B4E50D', borderRadius: '50%', opacity: 0.06, transform: `scale(${1 + Math.sin(frame * 0.04) * 0.1})` }} />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 51) * 1920;
        const py = seededRandom(i + 67) * 1080;
        const float = Math.sin(frame * 0.04 + i) * 8;
        return <div key={i} style={{ position: 'absolute', left: px, top: py + float, width: 4, height: 4, borderRadius: '50%', background: '#FFF9BD', opacity: 0.4 }} />;
      })}

      <AbsoluteFill style={{ transform: `scale(${cameraScale})` }}>

        {/* Left side - Long term concept */}
        <div style={{ position: 'absolute', top: '20%', left: '5%', width: 600, opacity: sp(10) }}>
          <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#B4E50D', letterSpacing: 6, marginBottom: 14 }}>LONG TERM MINDSET</div>
          <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 68, fontWeight: 800, color: '#FFF9BD', lineHeight: 1.05 }}>
            लंबी अवधि का सोच
          </div>

          {/* Hourglass */}
          <div style={{ marginTop: 36, display: 'flex', alignItems: 'center', gap: 24 }}>
            <svg width="100" height="140" viewBox="0 0 100 140" style={{ transform: `rotate(${hourglassRotate}deg)`, transition: 'transform 0.6s' }}>
              <path d="M 15 10 L 85 10 L 85 25 L 55 65 L 85 105 L 85 130 L 15 130 L 15 105 L 45 65 L 15 25 Z" fill="none" stroke="#B4E50D" strokeWidth="3" />
              <path d="M 25 15 L 75 15 L 50 60 Z" fill="#FFF9BD" opacity={1 - (frame % 60) / 60} />
              <path d="M 25 125 L 75 125 L 50 80 Z" fill="#FFF9BD" opacity={(frame % 60) / 60} />
            </svg>
            <div>
              <div style={{ fontFamily: "'Yantra One'", fontSize: 110, color: '#B4E50D', lineHeight: 1, fontWeight: 800 }}>{yearCount}+</div>
              <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 28, color: '#FFF9BD', marginTop: -6 }}>साल निवेशित</div>
            </div>
          </div>
        </div>

        {/* Phone mockup on right with Demat app */}
        <div style={{ position: 'absolute', top: '12%', right: '10%', width: 380, height: 760, opacity: sp(40), transform: `scale(${sp(40)})` }}>
          {/* phone frame */}
          <div style={{ width: '100%', height: '100%', background: '#1a1a1a', borderRadius: 50, padding: 18, boxShadow: '0 30px 80px rgba(0,0,0,0.6)' }}>
            <div style={{ width: '100%', height: '100%', background: '#FFF9BD', borderRadius: 36, overflow: 'hidden', position: 'relative' }}>
              {/* status bar */}
              <div style={{ height: 40, background: '#0D0B61', display: 'flex', alignItems: 'center', justifyContent: 'space-between', padding: '0 24px' }}>
                <div style={{ color: '#FFF9BD', fontSize: 14, fontFamily: 'Inter' }}>9:41</div>
                <div style={{ color: '#FFF9BD', fontSize: 14, fontFamily: 'Inter' }}>5G</div>
              </div>
              
              {/* App header */}
              <div style={{ padding: 24, background: '#0D0B61', color: '#FFF9BD' }}>
                <div style={{ fontFamily: 'Inter', fontSize: 14, opacity: 0.7, letterSpacing: 2 }}>DEMAT ACCOUNT</div>
                <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 26, fontWeight: 700, marginTop: 4 }}>आपका पोर्टफोलियो</div>
              </div>

              {/* Login screen */}
              <div style={{ padding: 30 }}>
                <div style={{ background: '#0D0B61', borderRadius: 16, padding: 18, marginBottom: 14 }}>
                  <div style={{ fontSize: 12, color: '#FFF9BD', opacity: 0.6, fontFamily: 'Inter' }}>USER ID</div>
                  <div style={{ fontSize: 22, color: '#FFF9BD', fontFamily: 'Inter', fontWeight: 600 }}>RAJESH****</div>
                </div>
                <div style={{ background: '#0D0B61', borderRadius: 16, padding: 18, marginBottom: 24 }}>
                  <div style={{ fontSize: 12, color: '#FFF9BD', opacity: 0.6, fontFamily: 'Inter' }}>MPIN</div>
                  <div style={{ fontSize: 22, color: '#FFF9BD', fontFamily: 'Inter', letterSpacing: 8 }}>● ● ● ●</div>
                </div>

                {/* Login button with ripple */}
                <div style={{ position: 'relative', overflow: 'hidden', borderRadius: 16 }}>
                  <div style={{ background: '#B4E50D', padding: 22, borderRadius: 16, textAlign: 'center', position: 'relative' }}>
                    <div style={{ fontFamily: 'Inter', fontSize: 20, color: '#0D0B61', fontWeight: 800 }}>
                      {frame < tapFrame ? 'LOGIN' : (
                        <span>LOADING <span style={{ opacity: dot1 }}>.</span><span style={{ opacity: dot2 }}>.</span><span style={{ opacity: dot3 }}>.</span></span>
                      )}
                    </div>
                    {frame >= tapFrame && (
                      <div style={{ position: 'absolute', top: '50%', left: '50%', width: 80, height: 80, marginTop: -40, marginLeft: -40, borderRadius: '50%', background: '#0D0B61', transform: `scale(${rippleScale})`, opacity: rippleOp }} />
                    )}
                  </div>
                </div>

                {/* warning text */}
                <div style={{ marginTop: 30, fontFamily: "'Noto Sans Devanagari'", fontSize: 18, color: '#0D0B61', opacity: 0.5, textAlign: 'center', lineHeight: 1.4 }}>
                  आपके अंदर एक डर है...
                </div>
              </div>
            </div>
          </div>

          {/* Finger tap indicator */}
          {frame >= tapFrame - 10 && frame < tapFrame + 15 && (
            <div style={{ position: 'absolute', top: 580, left: 200, width: 60, height: 60 }}>
              <svg viewBox="0 0 60 60">
                <circle cx="30" cy="30" r="25" fill="none" stroke="#B4E50D" strokeWidth="3" />
                <circle cx="30" cy="30" r="10" fill="#B4E50D" />
              </svg>
            </div>
          )}
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4_OpeningDemat;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5_FourShockwaves',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_FourShockwaves: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on impact
  const shakeFrames = [40, 80, 120, 160];
  let shakeX = 0, shakeY = 0;
  shakeFrames.forEach((sf) => {
    if (frame > sf && frame < sf + 15) {
      const intensity = (15 - (frame - sf)) / 15;
      shakeX += (seededRandom(frame + sf) - 0.5) * 16 * intensity;
      shakeY += (seededRandom(frame + sf + 1) - 0.5) * 16 * intensity;
    }
  });

  const shocks = [
    { label: 'उथल-पुथल', en: 'VOLATILITY', val: '11M', icon: 'wave', delay: 30, color: '#ff5577' },
    { label: 'महंगाई', en: 'INFLATION', val: '↑12%', icon: 'flame', delay: 70, color: '#FFC107' },
    { label: 'सप्लाइ चेन', en: 'SUPPLY SHOCK', val: 'BREAK', icon: 'chain', delay: 110, color: '#FFF9BD' },
    { label: 'अस्थिरता', en: 'INSTABILITY', val: '⚡', icon: 'bolt', delay: 150, color: '#B4E50D' },
  ];

  // Portfolio crash bar
  const portfolioVal = Math.max(60, 100 - Math.min(40, (frame - 30) / 5));
  const lossPercent = Math.min(40, Math.max(0, Math.floor((frame - 30) / 4)));

  return (
    <AbsoluteFill style={{ backgroundColor: '#0D0B61', overflow: 'hidden', opacity: exitOp, transform: `translate(${shakeX}px, ${shakeY}px)` }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(255,85,119,0.15) 0%, transparent 60%)' }} />

      {/* Crosshatch texture */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.07 }}>
        <defs>
          <pattern id="cross5" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="0" y1="60" x2="60" y2="0" stroke="#FFF9BD" strokeWidth="1" />
            <line x1="0" y1="0" x2="60" y2="60" stroke="#FFF9BD" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#cross5)" />
      </svg>

      {/* Shapes */}
      <div style={{ position: 'absolute', top: '5%', left: '40%', width: 400, height: 400, opacity: 0.05 }}>
        <svg viewBox="0 0 100 100"><polygon points="50,10 90,90 10,90" fill="#ff5577" transform={`rotate(${frame * 0.4} 50 50)`} /></svg>
      </div>

      {/* Particles - debris */}
      {Array.from({ length: 35 }).map((_, i) => {
        const px = seededRandom(i + 71) * 1920;
        const py = (seededRandom(i + 83) * 1080 + frame * 1.2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: px, top: py, width: 3, height: 8, background: '#ff5577', opacity: 0.5, transform: `rotate(${frame + i * 30}deg)` }} />;
      })}

      {/* Header */}
      <div style={{ position: 'absolute', top: 60, left: 0, right: 0, textAlign: 'center', opacity: sp(0) }}>
        <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#ff5577', letterSpacing: 6 }}>4 SIMULTANEOUS SHOCKWAVES</div>
        <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 68, fontWeight: 800, color: '#FFF9BD', marginTop: 6 }}>
          11 महीने की तबाही
        </div>
      </div>

      {/* 4 shock cards */}
      <div style={{ position: 'absolute', top: '25%', left: 0, right: 0, display: 'flex', justifyContent: 'center', gap: 28 }}>
        {shocks.map((s, i) => {
          const cardSp = sp(s.delay);
          return (
            <div key={i} style={{
              width: 320, height: 380,
              background: 'rgba(255,249,189,0.06)',
              border: `3px solid ${s.color}`,
              borderRadius: 22,
              padding: 28,
              transform: `translateY(${(1 - cardSp) * 100}px) scale(${cardSp})`,
              opacity: cardSp,
              position: 'relative',
              overflow: 'hidden',
            }}>
              {/* Number badge */}
              <div style={{ position: 'absolute', top: -22, left: 24, background: s.color, color: '#0D0B61', width: 50, height: 50, borderRadius: '50%', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'Inter', fontSize: 26, fontWeight: 800 }}>{i + 1}</div>

              {/* Icon */}
              <div style={{ display: 'flex', justifyContent: 'center', marginTop: 20, marginBottom: 24 }}>
                <svg width="100" height="100" viewBox="0 0 100 100">
                  {s.icon === 'wave' && (
                    <g>
                      <path d={`M 5 50 Q 25 ${30 + Math.sin(frame * 0.15) * 8}, 50 50 T 95 50`} stroke={s.color} strokeWidth="5" fill="none" />
                      <path d={`M 5 70 Q 25 ${50 + Math.sin(frame * 0.15 + 1) * 8}, 50 70 T 95 70`} stroke={s.color} strokeWidth="5" fill="none" opacity="0.6" />
                    </g>
                  )}
                  {s.icon === 'flame' && (
                    <g>
                      <path d="M 50 15 Q 30 35 35 60 Q 30 80 50 88 Q 70 80 65 60 Q 70 35 50 15 Z" fill={s.color} transform={`scale(${1 + Math.sin(frame * 0.2) * 0.06}) translate(0 ${-Math.sin(frame * 0.2) * 3})`} style={{ transformOrigin: '50px 50px' }}/>
                      <path d="M 50 35 Q 42 50 45 65 Q 42 78 50 82 Q 58 78 55 65 Q 58 50 50 35 Z" fill="#0D0B61" />
                    </g>
                  )}
                  {s.icon === 'chain' && (
                    <g>
                      <ellipse cx="35" cy="35" rx="14" ry="10" fill="none" stroke={s.color} strokeWidth="5" />
                      <ellipse cx="65" cy="65" rx="14" ry="10" fill="none" stroke={s.color} strokeWidth="5" />
                      <line x1="50" y1="20" x2="50" y2="80" stroke="#ff5577" strokeWidth="5" strokeDasharray="6 6" />
                      <text x="50" y="55" fontSize="20" fill="#ff5577" textAnchor="middle" fontFamily="Inter" fontWeight="800">✕</text>
                    </g>
                  )}
                  {s.icon === 'bolt' && (
                    <g>
                      <path d="M 55 10 L 25 55 L 45 55 L 35 90 L 70 40 L 50 40 L 55 10 Z" fill={s.color} transform={`scale(${1 + Math.sin(frame * 0.3) * 0.08})`} style={{ transformOrigin: '50px 50px' }} />
                    </g>
                  )}
                </svg>
              </div>

              <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 38, fontWeight: 800, color: s.color, textAlign: 'center', lineHeight: 1.1 }}>{s.label}</div>
              <div style={{ fontFamily: 'Inter', fontSize: 16, color: '#FFF9BD', opacity: 0.6, textAlign: 'center', marginTop: 4, letterSpacing: 3 }}>{s.en}</div>
              <div style={{ fontFamily: "'Yantra One'", fontSize: 56, fontWeight: 800, color: '#FFF9BD', textAlign: 'center', marginTop: 18 }}>{s.val}</div>
            </div>
          );
        })}
      </div>

      {/* Bottom impact result */}
      <div style={{ position: 'absolute', bottom: '5%', left: '50%', transform: 'translateX(-50%)', opacity: sp(190) }}>
        <div style={{ background: '#ff5577', padding: '18px 50px', borderRadius: 16, display: 'flex', alignItems: 'center', gap: 24 }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 36, fontWeight: 800, color: '#FFF9BD' }}>पोर्टफोलियो साफ</div>
          <div style={{ fontFamily: "'Yantra One'", fontSize: 70, fontWeight: 800, color: '#FFF9BD' }}>−40%</div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.65) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5_FourShockwaves;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6_MoneyVanishing',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_MoneyVanishing: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter going DOWN dramatically
  const startVal = 5000000;
  const endVal = 3000000;
  const count = Math.floor(interpolate(frame, [20, 150], [startVal, endVal], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const formattedCount = count.toLocaleString('en-IN');

  // Camera shake
  const shake = (frame > 30 && frame < 150) ? Math.sin(frame * 0.8) * 3 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#0D0B61', overflow: 'hidden', opacity: exitOp, transform: `translateX(${shake}px)` }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, rgba(255,85,119,0.18) 0%, transparent 65%)' }} />

      {/* Vertical line texture */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.05 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <line key={i} x1={i * 70} y1="0" x2={i * 70} y2="1080" stroke="#FFF9BD" strokeWidth="1" />
        ))}
      </svg>

      {/* Big shape behind */}
      <div style={{ position: 'absolute', top: '15%', left: '15%', width: 800, height: 800, border: '4px solid #ff5577', borderRadius: '50%', opacity: 0.08, transform: `scale(${1 + Math.sin(frame * 0.04) * 0.1})` }} />

      {/* Money notes flying up */}
      {Array.from({ length: 18 }).map((_, i) => {
        const startX = 700 + (i % 6) * 80;
        const startY = 600 + Math.floor(i / 6) * 50;
        const fly = Math.max(0, frame - 30 - i * 4);
        const flyY = -fly * 6;
        const swayX = Math.sin(fly * 0.08 + i) * 30;
        const rotate = fly * 4 + i * 30;
        const op = Math.max(0, 1 - fly / 130);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: startX + swayX,
            top: startY + flyY,
            width: 90, height: 50,
            background: '#B4E50D',
            borderRadius: 6,
            transform: `rotate(${rotate}deg)`,
            opacity: op,
            boxShadow: '0 4px 12px rgba(0,0,0,0.4)',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Inter', fontSize: 14, fontWeight: 800, color: '#0D0B61',
          }}>
            ₹500
          </div>
        );
      })}

      {/* Particles - dust */}
      {Array.from({ length: 40 }).map((_, i) => {
        const px = seededRandom(i + 91) * 1920;
        const py = (seededRandom(i + 103) * 1080 - frame * 2) % 1080;
        const adj = py < 0 ? py + 1080 : py;
        return <div key={i} style={{ position: 'absolute', left: px, top: adj, width: 3, height: 3, borderRadius: '50%', background: '#FFF9BD', opacity: 0.5 }} />;
      })}

      {/* Big counter on left */}
      <div style={{ position: 'absolute', top: '30%', left: '7%', width: 800, opacity: sp(10) }}>
        <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#ff5577', letterSpacing: 6, marginBottom: 12 }}>आपकी गाढ़ी कमाई</div>
        <div style={{ fontFamily: "'Yantra One'", fontSize: 180, fontWeight: 800, color: '#FFF9BD', lineHeight: 0.9, letterSpacing: -2 }}>
          ₹{formattedCount}
        </div>
        <div style={{ marginTop: 18, display: 'flex', alignItems: 'center', gap: 14 }}>
          <svg width="50" height="50" viewBox="0 0 50 50">
            <path d="M 25 8 L 40 28 L 30 28 L 30 42 L 20 42 L 20 28 L 10 28 Z" fill="#ff5577" transform={`translate(0 ${Math.sin(frame * 0.2) * 4}) rotate(180 25 25)`} />
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 42, fontWeight: 800, color: '#ff5577' }}>−₹{(startVal - count).toLocaleString('en-IN')}</div>
        </div>
      </div>

      {/* Wallet/safe icon on right */}
      <div style={{ position: 'absolute', top: '32%', right: '8%', opacity: sp(40), transform: `scale(${sp(40)})` }}>
        <svg width="380" height="380" viewBox="0 0 200 200">
          <rect x="20" y="50" width="160" height="120" rx="12" fill="#FFF9BD" stroke="#0D0B61" strokeWidth="3" />
          <rect x="20" y="50" width="160" height="30" rx="12" fill="#B4E50D" />
          <rect x="80" y="60" width="40" height="10" fill="#0D0B61" rx="2" />
          {/* hole through wallet */}
          <ellipse cx="100" cy="120" rx="30" ry="20" fill="#0D0B61" />
          <ellipse cx="100" cy="120" rx="22" ry="14" fill="#1a1880" />
          {/* arrows showing money escaping */}
          {Array.from({ length: 3 }).map((_, i) => (
            <path key={i} d={`M 100 ${100 - (frame * 2 + i * 30) % 80} L 100 ${100 - (frame * 2 + i * 30) % 80 - 20}`} stroke="#ff5577" strokeWidth="3" markerEnd="url(#arr6)" />
          ))}
          <defs>
            <marker id="arr6" markerWidth="6" markerHeight="6" refX="3" refY="3" orient="auto">
              <path d="M0,0 L6,3 L0,6 Z" fill="#ff5577" />
            </marker>
          </defs>
        </svg>
        <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 32, color: '#FFF9BD', textAlign: 'center', marginTop: 12, fontWeight: 700 }}>
          हवा में उड़ चुके
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.7) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6_MoneyVanishing;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7_DreamsFading',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_DreamsFading: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Filmstrip pan
  const filmPan = -interpolate(frame, [0, durationInFrames], [0, 800], { extrapolateRight: 'clamp' });

  const dreams = [
    { icon: 'beach', label: 'समुद्र किनारा', en: 'BEACH RETREAT' },
    { icon: 'temple', label: 'तीर्थ यात्रा', en: 'PILGRIMAGE' },
    { icon: 'house', label: 'गांव का घर', en: 'VILLAGE HOME' },
    { icon: 'family', label: 'परिवार समय', en: 'FAMILY TIME' },
    { icon: 'beach', label: 'समुद्र किनारा', en: 'BEACH RETREAT' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#0D0B61', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 30%, rgba(180,229,13,0.1) 0%, transparent 60%)' }} />

      {/* Dot grid */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.06 }}>
        <defs>
          <pattern id="dots7" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#FFF9BD" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots7)" />
      </svg>

      {/* Big shape */}
      <div style={{ position: 'absolute', top: '50%', left: '50%', width: 1200, height: 1200, marginTop: -600, marginLeft: -600, border: '2px solid #B4E50D', borderRadius: '50%', opacity: 0.05, transform: `rotate(${frame * 0.2}deg)` }} />

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = seededRandom(i + 111) * 1920;
        const py = seededRandom(i + 121) * 1080 + Math.sin(frame * 0.04 + i) * 12;
        return <div key={i} style={{ position: 'absolute', left: px, top: py, width: 4, height: 4, borderRadius: '50%', background: '#B4E50D', opacity: 0.4 }} />;
      })}

      {/* Header */}
      <div style={{ position: 'absolute', top: 60, left: 0, right: 0, textAlign: 'center', opacity: sp(0) }}>
        <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#B4E50D', letterSpacing: 6 }}>RETIREMENT DREAMS — FADING</div>
        <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 68, fontWeight: 800, color: '#FFF9BD', marginTop: 8 }}>
          सपने जो टाल दिये थे
        </div>
      </div>

      {/* Filmstrip */}
      <div style={{ position: 'absolute', top: '32%', left: 0, width: '300%', display: 'flex', gap: 30, transform: `translateX(${filmPan}px)`, padding: '0 60px' }}>
        {/* Top film holes */}
        {dreams.map((d, i) => {
          const fadeIn = sp(20 + i * 12);
          const fadeOut = interpolate(frame, [180 + i * 10, 240 + i * 10], [1, 0.15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
          return (
            <div key={i} style={{
              flexShrink: 0,
              width: 460, height: 460,
              background: '#1a1880',
              border: '4px solid #FFF9BD',
              borderRadius: 14,
              padding: 24,
              position: 'relative',
              opacity: fadeIn * fadeOut,
              filter: `blur(${(1 - fadeOut) * 3}px) grayscale(${1 - fadeOut})`,
            }}>
              {/* Film holes top/bottom */}
              <div style={{ position: 'absolute', top: -2, left: 0, right: 0, height: 22, display: 'flex', justifyContent: 'space-around' }}>
                {Array.from({ length: 8 }).map((_, j) => (
                  <div key={j} style={{ width: 26, height: 16, background: '#0D0B61', marginTop: 3, borderRadius: 3 }} />
                ))}
              </div>
              <div style={{ position: 'absolute', bottom: -2, left: 0, right: 0, height: 22, display: 'flex', justifyContent: 'space-around' }}>
                {Array.from({ length: 8 }).map((_, j) => (
                  <div key={j} style={{ width: 26, height: 16, background: '#0D0B61', marginTop: 3, borderRadius: 3 }} />
                ))}
              </div>

              {/* Frame content */}
              <div style={{ width: '100%', height: '100%', display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center', gap: 20, padding: '40px 0' }}>
                <svg width="180" height="180" viewBox="0 0 180 180">
                  {d.icon === 'beach' && (
                    <g>
                      <circle cx="140" cy="50" r="22" fill="#FFC107" />
                      <path d="M 0 130 Q 90 110 180 130 L 180 180 L 0 180 Z" fill="#FFF9BD" />
                      <path d="M 50 130 L 60 100 L 70 130" stroke="#B4E50D" strokeWidth="3" fill="none" />
                      <ellipse cx="60" cy="100" rx="20" ry="8" fill="#B4E50D" />
                    </g>
                  )}
                  {d.icon === 'temple' && (
                    <g>
                      <polygon points="90,20 130,70 50,70" fill="#FFC107" />
                      <rect x="55" y="70" width="70" height="80" fill="#FFF9BD" />
                      <rect x="80" y="100" width="20" height="50" fill="#0D0B61" />
                      <circle cx="90" cy="35" r="6" fill="#B4E50D" />
                    </g>
                  )}
                  {d.icon === 'house' && (
                    <g>
                      <polygon points="90,30 30,80 150,80" fill="#B4E50D" />
                      <rect x="40" y="80" width="100" height="80" fill="#FFF9BD" />
                      <rect x="80" y="110" width="20" height="50" fill="#0D0B61" />
                      <rect x="105" y="100" width="20" height="20" fill="#FFC107" />
                      <rect x="55" y="100" width="20" height="20" fill="#FFC107" />
                    </g>
                  )}
                  {d.icon === 'family' && (
                    <g>
                      <circle cx="60" cy="60" r="14" fill="#FFF9BD" />
                      <rect x="48" y="74" width="24" height="40" rx="6" fill="#FFF9BD" />
                      <circle cx="120" cy="60" r="14" fill="#B4E50D" />
                      <rect x="108" y="74" width="24" height="40" rx="6" fill="#B4E50D" />
                      <circle cx="90" cy="100" r="10" fill="#FFC107" />
                      <rect x="82" y="110" width="16" height="28" rx="4" fill="#FFC107" />
                    </g>
                  )}
                </svg>
                <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 36, fontWeight: 800, color: '#FFF9BD', textAlign: 'center' }}>{d.label}</div>
                <div style={{ fontFamily: 'Inter', fontSize: 16, color: '#B4E50D', letterSpacing: 3 }}>{d.en}</div>
              </div>

              {/* CANCELLED stamp */}
              {frame > 200 && (
                <div style={{
                  position: 'absolute', top: '50%', left: '50%',
                  transform: `translate(-50%,-50%) rotate(-12deg) scale(${sp(200, { stiffness: 200 })})`,
                  border: '6px solid #ff5577', padding: '8px 30px',
                  fontFamily: 'Inter', fontSize: 38, fontWeight: 900, color: '#ff5577',
                  letterSpacing: 4,
                  opacity: 0.95,
                }}>
                  CANCELLED
                </div>
              )}
            </div>
          );
        })}
      </div>

      {/* Bottom caption */}
      <div style={{ position: 'absolute', bottom: '8%', left: 0, right: 0, textAlign: 'center', opacity: sp(160) }}>
        <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 42, fontWeight: 700, color: '#ff5577' }}>
          फिल्म की तरह घूम रहे... पर अब बस सपने
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.65) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7_DreamsFading;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8_NationalUnrest',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_NationalUnrest: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Three rising bars - inflation, taxes, unemployment
  const bars = [
    { label: 'महंगाई', en: 'INFLATION', val: '12.4%', height: 280, color: '#ff5577', delay: 30 },
    { label: 'टैक्स', en: 'TAXES', val: '+18%', height: 240, color: '#FFC107', delay: 60 },
    { label: 'बेरोजगारी', en: 'JOB LOSS', val: '8.7%', height: 320, color: '#B4E50D', delay: 90 },
  ];

  // Voice/sound waves being suppressed
  const suppressFrame = 130;
  const voiceOpacity = frame < suppressFrame ? 1 : Math.max(0.15, 1 - (frame - suppressFrame) / 40);

  return (
    <AbsoluteFill style={{ backgroundColor: '#0D0B61', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #0D0B61 0%, #1a1880 100%)' }} />

      {/* News ticker pattern in background */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.05 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <line key={i} x1={(i * 200 - frame * 2) % 1920} y1={i * 90} x2={(i * 200 - frame * 2) % 1920 + 100} y2={i * 90} stroke="#FFF9BD" strokeWidth="2" />
        ))}
      </svg>

      {/* Big circle */}
      <div style={{ position: 'absolute', top: '40%', right: '-10%', width: 700, height: 700, border: '3px solid #ff5577', borderRadius: '50%', opacity: 0.06, transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})` }} />

      {/* Header */}
      <div style={{ position: 'absolute', top: 60, left: 0, right: 0, textAlign: 'center', opacity: sp(0) }}>
        <div style={{ display: 'inline-block', background: '#ff5577', padding: '6px 22px', fontFamily: 'Inter', fontSize: 18, color: '#FFF9BD', letterSpacing: 4, fontWeight: 700, borderRadius: 4, marginBottom: 14 }}>● LIVE — MAY 2026</div>
          <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 64, fontWeight: 800, color: '#FFF9BD' }}>
            एक अजीब सी बेचैनी
          </div>
      </div>

      {/* 3 rising bars - left side */}
      <div style={{ position: 'absolute', bottom: '12%', left: '6%', width: 700, height: 400, display: 'flex', alignItems: 'flex-end', gap: 40 }}>
        {bars.map((b, i) => {
          const barSp = sp(b.delay, { damping: 14, stiffness: 100 });
          const realH = b.height * barSp;
          return (
            <div key={i} style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
              <div style={{ fontFamily: "'Yantra One'", fontSize: 50, fontWeight: 800, color: b.color, opacity: barSp, marginBottom: 8 }}>{b.val}</div>
              <div style={{ width: '100%', height: realH, background: b.color, borderRadius: '8px 8px 0 0', position: 'relative', boxShadow: `0 0 30px ${b.color}40` }}>
                {/* Up arrow */}
                {barSp > 0.7 && (
                  <div style={{ position: 'absolute', top: -40, left: '50%', transform: `translateX(-50%) translateY(${Math.sin(frame * 0.2 + i) * 5}px)` }}>
                    <svg width="36" height="36" viewBox="0 0 36 36">
                      <path d="M 18 4 L 32 20 L 24 20 L 24 32 L 12 32 L 12 20 L 4 20 Z" fill={b.color} />
                    </svg>
                  </div>
                )}
              </div>
              <div style={{ marginTop: 14, fontFamily: "'Noto Sans Devanagari'", fontSize: 30, fontWeight: 800, color: '#FFF9BD', textAlign: 'center' }}>{b.label}</div>
              <div style={{ fontFamily: 'Inter', fontSize: 14, color: b.color, letterSpacing: 2, marginTop: 2 }}>{b.en}</div>
            </div>
          );
        })}
      </div>

      {/* Right side - voice suppressed */}
      <div style={{ position: 'absolute', top: '28%', right: '5%', width: 580, opacity: sp(60) }}>
        <div style={{ background: 'rgba(255,249,189,0.06)', border: '2px solid rgba(255,249,189,0.2)', borderRadius: 18, padding: 32 }}>
          <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#B4E50D', letterSpacing: 4, marginBottom: 16 }}>आम आदमी की आवाज</div>
          
          {/* Megaphone with sound waves */}
          <div style={{ display: 'flex', alignItems: 'center', gap: 20, marginBottom: 24, opacity: voiceOpacity }}>
            <svg width="120" height="120" viewBox="0 0 120 120">
              <path d="M 20 45 L 50 45 L 90 25 L 90 95 L 50 75 L 20 75 Z" fill="#B4E50D" />
              <rect x="10" y="50" width="14" height="20" fill="#FFF9BD" />
            </svg>
            {/* sound waves */}
            <svg width="180" height="120" viewBox="0 0 180 120">
              {Array.from({ length: 4 }).map((_, j) => (
                <path key={j} d={`M ${20 + j * 30} 60 Q ${30 + j * 30} ${40 - Math.sin(frame * 0.3 + j) * 10}, ${40 + j * 30} 60 T ${60 + j * 30} 60`} stroke="#FFF9BD" strokeWidth="3" fill="none" opacity={0.7 - j * 0.15} />
              ))}
            </svg>
          </div>

          <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 28, fontWeight: 700, color: '#FFF9BD', lineHeight: 1.3, marginBottom: 18 }}>
            "महंगाई बहुत बढ़ गई है... नौकरी जा रही है..."
          </div>

          {/* Suppressed indicator */}
          {frame > suppressFrame && (
            <div style={{
              background: '#ff5577',
              padding: '14px 22px',
              borderRadius: 10,
              display: 'flex', alignItems: 'center', gap: 14,
              transform: `scale(${sp(suppressFrame, { stiffness: 200 })})`,
            }}>
              <svg width="36" height="36" viewBox="0 0 36 36">
                <circle cx="18" cy="18" r="14" fill="none" stroke="#FFF9BD" strokeWidth="3" />
                <line x1="8" y1="8" x2="28" y2="28" stroke="#FFF9BD" strokeWidth="3" />
              </svg>
              <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 24, fontWeight: 800, color: '#FFF9BD' }}>
                सिस्टम के शोर में दबा दी गई
              </div>
            </div>
          )}
        </div>
      </div>

      {/* Particles - dust */}
      {Array.from({ length: 30 }).map((_, i) => {
        const px = seededRandom(i + 131) * 1920;
        const py = (seededRandom(i + 141) * 1080 - frame * 0.4) % 1080;
        const adj = py < 0 ? py + 1080 : py;
        return <div key={i} style={{ position: 'absolute', left: px, top: adj, width: 3, height: 3, borderRadius: '50%', background: '#FFF9BD', opacity: 0.35 }} />;
      })}

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8_NationalUnrest;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9_TwoForcesCrushing',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_TwoForcesCrushing: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Forces closing in
  const forceClose = interpolate(frame, [30, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const leftX = -300 * (1 - forceClose);
  const rightX = 300 * (1 - forceClose);

  // Heart in middle compressing
  const heartScale = 1 - forceClose * 0.3;
  const heartShake = (frame > 100) ? Math.sin(frame * 1.5) * 4 : 0;

  // Crash chart line
  const chartProgress = Math.min(1, frame / 120);
  const chartPath = () => {
    const pts: string[] = ['M 0 80'];
    for (let i = 1; i <= 100; i++) {
      const x = (i / 100) * 300;
      const y = 80 + (i / 100) * 180 + Math.sin(i * 0.3) * 12;
      if (i / 100 < chartProgress) pts.push(`L ${x} ${y}`);
    }
    return pts.join(' ');
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#0D0B61', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at center, rgba(255,85,119,0.18) 0%, transparent 60%)' }} />

      {/* Diagonal warning stripes */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.05 }}>
        <defs>
          <pattern id="warn9" width="80" height="80" patternUnits="userSpaceOnUse" patternTransform="rotate(-45)">
            <rect width="40" height="80" fill="#FFC107" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#warn9)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const px = seededRandom(i + 151) * 1920;
        const py = seededRandom(i + 161) * 1080;
        const float = Math.sin(frame * 0.05 + i) * 10;
        return <div key={i} style={{ position: 'absolute', left: px, top: py + float, width: 4, height: 4, borderRadius: '50%', background: '#FFF9BD', opacity: 0.35 }} />;
      })}

      {/* Header */}
      <div style={{ position: 'absolute', top: 60, left: 0, right: 0, textAlign: 'center', opacity: sp(0) }}>
        <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#ff5577', letterSpacing: 6 }}>TWO FORCES — ONE PERSON</div>
        <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 64, fontWeight: 800, color: '#FFF9BD', marginTop: 8 }}>
          हिम्मत टूटने की वजह
        </div>
      </div>

      {/* LEFT FORCE — System apathy */}
      <div style={{ position: 'absolute', top: '32%', left: '4%', width: 480, height: 420, transform: `translateX(${leftX}px)`, opacity: sp(20) }}>
        <div style={{ background: 'rgba(255,85,119,0.15)', border: '3px solid #ff5577', borderRadius: 22, padding: 30, height: '100%' }}>
          <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#ff5577', letterSpacing: 4, marginBottom: 10 }}>FORCE 01</div>
          <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 52, fontWeight: 800, color: '#FFF9BD', lineHeight: 1.05 }}>
            सिस्टम की<br/>बेरूखी
          </div>
          
          {/* Building/system icon */}
          <div style={{ display: 'flex', justifyContent: 'center', marginTop: 24 }}>
            <svg width="220" height="180" viewBox="0 0 220 180">
              <rect x="20" y="60" width="180" height="110" fill="#FFF9BD" />
              <polygon points="20,60 110,20 200,60" fill="#ff5577" />
              {/* windows */}
              {Array.from({ length: 8 }).map((_, i) => (
                <rect key={i} x={40 + (i % 4) * 40} y={80 + Math.floor(i / 4) * 40} width="22" height="22" fill="#0D0B61" />
              ))}
              {/* closed door */}
              <rect x="95" y="130" width="30" height="40" fill="#0D0B61" />
              {/* X mark on door */}
              <text x="110" y="158" fontFamily="Inter" fontSize="24" fontWeight="900" fill="#ff5577" textAnchor="middle">✕</text>
            </svg>
          </div>
          <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 22, color: '#FFF9BD', opacity: 0.7, textAlign: 'center', marginTop: 12 }}>
            कोई जवाब नहीं, कोई सहारा नहीं
          </div>
        </div>
      </div>

      {/* RIGHT FORCE — Falling Market */}
      <div style={{ position: 'absolute', top: '32%', right: '4%', width: 480, height: 420, transform: `translateX(${rightX}px)`, opacity: sp(40) }}>
        <div style={{ background: 'rgba(255,85,119,0.15)', border: '3px solid #ff5577', borderRadius: 22, padding: 30, height: '100%' }}>
          <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#ff5577', letterSpacing: 4, marginBottom: 10, textAlign: 'right' }}>FORCE 02</div>
          <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 52, fontWeight: 800, color: '#FFF9BD', lineHeight: 1.05, textAlign: 'right' }}>
            गिरता<br/>शेयर बाजार
          </div>

          {/* Chart crashing */}
          <div style={{ display: 'flex', justifyContent: 'center', marginTop: 24 }}>
            <svg width="300" height="180" viewBox="0 0 300 280">
              {/* axis */}
              <line x1="0" y1="270" x2="300" y2="270" stroke="#FFF9BD" strokeWidth="2" opacity="0.4" />
              <line x1="0" y1="0" x2="0" y2="270" stroke="#FFF9BD" strokeWidth="2" opacity="0.4" />
              {/* crashing line */}
              <path d={chartPath()} stroke="#ff5577" strokeWidth="4" fill="none" />
              {/* end dot */}
              {chartProgress > 0.95 && (
                <circle cx="300" cy="260" r="8" fill="#ff5577">
                </circle>
              )}
              {/* arrow at end */}
              <path d="M 280 260 L 300 270 L 280 280" stroke="#ff5577" strokeWidth="3" fill="none" />
            </svg>
          </div>
          <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 22, color: '#FFF9BD', opacity: 0.7, textAlign: 'center', marginTop: 12 }}>
            हर दिन नया नुकसान
          </div>
        </div>
      </div>

      {/* CENTER — Heart being crushed */}
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        transform: `translate(-50%, -50%) translate(${heartShake}px, 0) scale(${heartScale})`,
        opacity: sp(60),
        zIndex: 5,
      }}>
        <svg width="280" height="280" viewBox="0 0 100 100">
          <path d="M 50 88 C 50 88, 12 65, 12 38 C 12 22, 25 12, 38 18 C 44 21, 50 28, 50 28 C 50 28, 56 21, 62 18 C 75 12, 88 22, 88 38 C 88 65, 50 88, 50 88 Z" fill="#B4E50D" stroke="#FFF9BD" strokeWidth="2" />
          {/* crack */}
          {frame > 130 && (
            <path d="M 50 25 L 45 45 L 55 60 L 48 80" stroke="#0D0B61" strokeWidth="3" fill="none" />
          )}
        </svg>
        <div style={{ marginTop: 12, fontFamily: "'Noto Sans Devanagari'", fontSize: 32, fontWeight: 800, color: '#FFF9BD', textAlign: 'center' }}>
          हिम्मत
        </div>
      </div>

      {/* Inward arrows */}
      <div style={{ position: 'absolute', top: '52%', left: '32%' }}>
        <svg width="100" height="40" viewBox="0 0 100 40">
          <path d={`M ${10 + forceClose * 60} 20 L 90 20`} stroke="#ff5577" strokeWidth="4" />
          <path d="M 75 10 L 90 20 L 75 30" stroke="#ff5577" strokeWidth="4" fill="none" />
        </svg>
      </div>
      <div style={{ position: 'absolute', top: '52%', right: '32%' }}>
        <svg width="100" height="40" viewBox="0 0 100 40">
          <path d={`M ${90 - forceClose * 60} 20 L 10 20`} stroke="#ff5577" strokeWidth="4" />
          <path d="M 25 10 L 10 20 L 25 30" stroke="#ff5577" strokeWidth="4" fill="none" />
        </svg>
      </div>

      {/* Bottom result */}
      <div style={{ position: 'absolute', bottom: '5%', left: 0, right: 0, textAlign: 'center', opacity: sp(160) }}>
        <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 38, fontWeight: 700, color: '#ff5577' }}>
          इंसान बेबस, हिम्मत टूटी
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.7) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9_TwoForcesCrushing;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_SelfDoubt',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_SelfDoubt: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Question marks floating
  const questions = Array.from({ length: 8 }, (_, i) => ({
    x: 200 + (i % 4) * 380,
    y: 200 + Math.floor(i / 4) * 350,
    delay: 30 + i * 10,
    size: 80 + (i % 3) * 30,
  }));

  // Camera slow zoom out
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 0.95], { extrapolateRight: 'clamp' });

  // Thought bubble breathe
  const bubbleScale = 1 + Math.sin(frame * 0.06) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#0D0B61', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(255,193,7,0.1) 0%, transparent 60%)' }} />

      {/* Spiral texture */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.06 }}>
        {Array.from({ length: 5 }).map((_, i) => (
          <circle key={i} cx="960" cy="540" r={150 + i * 130} fill="none" stroke="#FFF9BD" strokeWidth="1" strokeDasharray="8 12" transform={`rotate(${frame * (0.3 + i * 0.1)} 960 540)`} />
        ))}
      </svg>

      {/* Big shape */}
      <div style={{ position: 'absolute', top: '50%', left: '50%', width: 900, height: 900, marginTop: -450, marginLeft: -450, opacity: 0.04, transform: `rotate(${frame * 0.3}deg)` }}>
        <svg viewBox="0 0 100 100"><polygon points="50,5 95,35 80,90 20,90 5,35" fill="#FFC107" /></svg>
      </div>

      {/* Floating question marks */}
      {questions.map((q, i) => {
        const qSp = sp(q.delay);
        const float = Math.sin(frame * 0.05 + i) * 18;
        const rot = Math.sin(frame * 0.04 + i) * 12;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: q.x, top: q.y + float,
            opacity: qSp * 0.35,
            transform: `scale(${qSp}) rotate(${rot}deg)`,
            fontFamily: "'Yantra One'",
            fontSize: q.size,
            color: '#FFC107',
            fontWeight: 800,
          }}>?</div>
        );
      })}

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = seededRandom(i + 171) * 1920;
        const py = seededRandom(i + 181) * 1080;
        const dx = Math.sin(frame * 0.03 + i) * 15;
        return <div key={i} style={{ position: 'absolute', left: px + dx, top: py, width: 3, height: 3, borderRadius: '50%', background: '#FFF9BD', opacity: 0.4 }} />;
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Person silhouette - head + thought bubble */}
        <div style={{ position: 'absolute', bottom: '12%', left: '12%', opacity: sp(0) }}>
          <svg width="280" height="320" viewBox="0 0 200 240">
            {/* head */}
            <circle cx="100" cy="60" r="42" fill="none" stroke="#FFF9BD" strokeWidth="3" />
            {/* sad eyes */}
            <circle cx="85" cy="55" r="4" fill="#FFF9BD" />
            <circle cx="115" cy="55" r="4" fill="#FFF9BD" />
            {/* downturn mouth */}
            <path d="M 85 78 Q 100 70 115 78" stroke="#FFF9BD" strokeWidth="3" fill="none" />
            {/* shoulders dropping */}
            <path d="M 50 130 Q 100 110 150 130 L 150 230 L 50 230 Z" fill="none" stroke="#FFF9BD" strokeWidth="3" />
            {/* shrug arms */}
            <line x1="50" y1="135" x2="30" y2="170" stroke="#FFF9BD" strokeWidth="3" />
            <line x1="150" y1="135" x2="170" y2="170" stroke="#FFF9BD" strokeWidth="3" />
          </svg>
          <div style={{ marginTop: 12, fontFamily: "'Noto Sans Devanagari'", fontSize: 26, color: '#FFF9BD', opacity: 0.7, textAlign: 'center' }}>
            एक आम निवेशक
          </div>
        </div>

        {/* Thought bubble — big */}
        <div style={{
          position: 'absolute',
          top: '15%', right: '8%',
          width: 900, height: 580,
          transform: `scale(${sp(20) * bubbleScale})`,
          opacity: sp(20),
        }}>
          <div style={{
            background: '#FFF9BD',
            borderRadius: '60% 50% 55% 45% / 55% 60% 45% 50%',
            width: '100%', height: '100%',
            padding: '60px 70px',
            display: 'flex', flexDirection: 'column', justifyContent: 'center', gap: 26,
            boxShadow: '0 30px 80px rgba(0,0,0,0.4)',
            position: 'relative',
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#0D0B61', opacity: 0.5, letterSpacing: 4 }}>SELF DOUBT THOUGHTS</div>

            {/* Doubt option 1 */}
            <div style={{ display: 'flex', alignItems: 'center', gap: 22, opacity: sp(50) }}>
              <div style={{ width: 60, height: 60, background: '#ff5577', borderRadius: '50%', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: "'Yantra One'", fontSize: 40, fontWeight: 800, color: '#FFF9BD', flexShrink: 0 }}>?</div>
              <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 44, fontWeight: 800, color: '#0D0B61', lineHeight: 1.15 }}>
                क्या मैंने बड़ी गलती की?
              </div>
            </div>

            <div style={{ height: 2, background: 'rgba(13,11,97,0.15)', opacity: sp(70) }} />

            {/* Doubt option 2 */}
            <div style={{ display: 'flex', alignItems: 'center', gap: 22, opacity: sp(80) }}>
              <div style={{ width: 60, height: 60, background: '#ff5577', borderRadius: '50%', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: "'Yantra One'", fontSize: 40, fontWeight: 800, color: '#FFF9BD', flexShrink: 0 }}>?</div>
              <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 44, fontWeight: 800, color: '#0D0B61', lineHeight: 1.15 }}>
                या किस्मत ही खराब है?
              </div>
            </div>

            {/* Conclusion stamp */}
            {frame > 150 && (
              <div style={{
                marginTop: 14,
                padding: '14px 22px',
                background: '#0D0B61',
                borderRadius: 12,
                opacity: sp(150),
                transform: `translateY(${(1 - sp(150)) * 20}px)`,
              }}>
                <div style={{ fontFamily: 'Inter', fontSize: 16, color: '#B4E50D', letterSpacing: 3 }}>SPOILER</div>
                <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 32, fontWeight: 800, color: '#FFF9BD' }}>
                  गलती नहीं... समझ की कमी थी
                </div>
              </div>
            )}

            {/* Bubble tail */}
            <div style={{ position: 'absolute', bottom: -30, left: -10, width: 40, height: 40, background: '#FFF9BD', borderRadius: '50%' }} />
            <div style={{ position: 'absolute', bottom: -65, left: -50, width: 22, height: 22, background: '#FFF9BD', borderRadius: '50%' }} />
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_SelfDoubt;
```

