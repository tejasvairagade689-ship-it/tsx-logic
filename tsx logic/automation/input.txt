```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_PhoneGlitch',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_PhoneGlitch: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom-in slow
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.08], { extrapolateRight: 'clamp' });

  // Phone entrance
  const phoneScale = sp(10, { damping: 14, stiffness: 90 });
  const phoneRot = interpolate(frame, [0, 60, 120, 180, 240], [-4, 2, -2, 1, 0], { extrapolateRight: 'clamp' });

  // Glitch trigger between frame 120-180
  const glitchActive = frame > 120 && frame < 200;
  const glitchOffset = glitchActive ? (seededRandom(frame) - 0.5) * 18 : 0;
  const glitchOffset2 = glitchActive ? (seededRandom(frame + 7) - 0.5) * 12 : 0;

  // Screen shake on impact
  const shake = (frame > 140 && frame < 170) ? Math.sin(frame * 2) * 4 : 0;

  // Headline reveal
  const h1Op = interpolate(frame, [25, 55], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const h1Y = interpolate(sp(25), [0, 1], [40, 0]);

  // Subhead reveal
  const subOp = interpolate(frame, [200, 235], [0, 1], { extrapolateRight: 'clamp' });
  const subY = interpolate(sp(200), [0, 1], [30, 0]);

  // Portfolio number ticker
  const numProgress = interpolate(frame, [80, 200], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const portfolioVal = Math.floor(interpolate(numProgress, [0, 1], [847250, 412380]));
  const portfolioColor = frame < 130 ? '#EEEEEE' : (frame % 8 < 4 ? '#FF4444' : '#EEEEEE');

  // Down arrow pulse
  const arrowPulse = 1 + Math.sin(frame * 0.15) * 0.08;

  // Floating particles
  const particles = Array.from({ length: 28 }, (_, i) => i);

  // Shape layer drift
  const shape1X = Math.sin(frame * 0.01) * 30;
  const shape1Y = Math.cos(frame * 0.012) * 20;
  const shape2Rot = frame * 0.15;

  return (
    <AbsoluteFill style={{ backgroundColor: '#4BB8FA', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(255,255,255,0.18) 0%, transparent 55%), radial-gradient(circle at 75% 70%, rgba(0,0,0,0.25) 0%, transparent 60%)',
      }} />

      {/* Shape layer 1 — huge circle */}
      <div style={{
        position: 'absolute',
        left: -200 + shape1X,
        top: -150 + shape1Y,
        width: 700,
        height: 700,
        borderRadius: '50%',
        border: '3px solid rgba(238,238,238,0.08)',
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />

      {/* Shape layer 2 — rotating square */}
      <div style={{
        position: 'absolute',
        right: -100,
        bottom: -100,
        width: 500,
        height: 500,
        border: '2px solid rgba(0,0,0,0.08)',
        transform: `rotate(${shape2Rot}deg)`,
      }} />

      {/* Shape layer 3 — triangle */}
      <svg style={{ position: 'absolute', left: 1400, top: 150, opacity: 0.06 }} width="280" height="280">
        <polygon points="140,20 260,240 20,240" fill="none" stroke="#000000" strokeWidth="3" transform={`rotate(${-frame * 0.1} 140 140)`} />
      </svg>

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="grid01" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid01)" />
      </svg>

      {/* Floating particles */}
      {particles.map(i => {
        const r = seededRandom(i + 1);
        const r2 = seededRandom(i + 50);
        const x = r * 1920;
        const y = (r2 * 1080 + frame * 0.6) % 1080;
        const size = 3 + r * 6;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: size,
            height: size,
            borderRadius: '50%',
            backgroundColor: '#EEEEEE',
            opacity: 0.35 + r * 0.3,
          }} />
        );
      })}

      {/* Main content frame with camera */}
      <AbsoluteFill style={{ transform: `scale(${camZoom}) translateX(${shake}px)` }}>

        {/* Headline TOP */}
        <div style={{
          position: 'absolute',
          top: 100,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: h1Op,
          transform: `translateY(${h1Y}px)`,
        }}>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 110,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -2,
            lineHeight: 1,
          }}>
            कुछ घंटे पहले...
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 32,
            fontWeight: 600,
            color: '#000000',
            opacity: 0.5,
            marginTop: 12,
            letterSpacing: 8,
          }}>
            A FEW HOURS AGO
          </div>
        </div>

        {/* Phone mockup CENTER */}
        <div style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) scale(${phoneScale}) rotate(${phoneRot}deg)`,
        }}>
          {/* Phone body */}
          <div style={{
            width: 380,
            height: 720,
            borderRadius: 50,
            backgroundColor: '#000000',
            padding: 14,
            boxShadow: '0 30px 80px rgba(0,0,0,0.35)',
            position: 'relative',
          }}>
            {/* Screen */}
            <div style={{
              width: '100%',
              height: '100%',
              borderRadius: 38,
              backgroundColor: '#EEEEEE',
              overflow: 'hidden',
              position: 'relative',
              transform: `translate(${glitchOffset}px, 0)`,
            }}>
              {/* Notch */}
              <div style={{
                position: 'absolute',
                top: 14,
                left: '50%',
                transform: 'translateX(-50%)',
                width: 110,
                height: 28,
                borderRadius: 16,
                backgroundColor: '#000000',
              }} />

              {/* App header */}
              <div style={{
                paddingTop: 70,
                paddingLeft: 28,
                paddingRight: 28,
              }}>
                <div style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 14,
                  fontWeight: 600,
                  color: '#000000',
                  opacity: 0.5,
                  letterSpacing: 2,
                }}>
                  PORTFOLIO
                </div>
                <div style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 44,
                  fontWeight: 800,
                  color: portfolioColor,
                  marginTop: 6,
                  transform: `translate(${glitchOffset2}px, 0)`,
                }}>
                  ${portfolioVal.toLocaleString()}
                </div>
                <div style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 18,
                  fontWeight: 700,
                  color: '#FF2222',
                  marginTop: 4,
                }}>
                  ▼ -51.4% TODAY
                </div>
              </div>

              {/* Mini chart line going down */}
              <svg style={{ position: 'absolute', left: 28, top: 260, opacity: 0.9 }} width="296" height="180">
                <polyline
                  points="0,40 30,50 60,35 90,60 120,80 150,75 180,110 210,130 240,125 270,160 296,170"
                  fill="none"
                  stroke="#FF2222"
                  strokeWidth="4"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                />
                {/* Glow underneath */}
                <polyline
                  points="0,40 30,50 60,35 90,60 120,80 150,75 180,110 210,130 240,125 270,160 296,170"
                  fill="none"
                  stroke="#FF2222"
                  strokeWidth="10"
                  opacity="0.2"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                />
              </svg>

              {/* Coin rows */}
              <div style={{ position: 'absolute', bottom: 40, left: 28, right: 28 }}>
                {['BTC', 'ETH', 'SOL'].map((c, i) => (
                  <div key={c} style={{
                    display: 'flex',
                    justifyContent: 'space-between',
                    alignItems: 'center',
                    marginBottom: 14,
                    paddingBottom: 14,
                    borderBottom: '1px solid rgba(0,0,0,0.08)',
                  }}>
                    <div style={{
                      fontFamily: 'JetBrains Mono, monospace',
                      fontSize: 22,
                      fontWeight: 700,
                      color: '#000000',
                    }}>{c}</div>
                    <div style={{
                      fontFamily: 'JetBrains Mono, monospace',
                      fontSize: 20,
                      fontWeight: 700,
                      color: '#FF2222',
                    }}>-{42 + i * 3}%</div>
                  </div>
                ))}
              </div>

              {/* Glitch bars */}
              {glitchActive && (
                <>
                  <div style={{
                    position: 'absolute',
                    top: 200 + seededRandom(frame) * 300,
                    left: 0,
                    width: '100%',
                    height: 6,
                    backgroundColor: '#FF2222',
                    opacity: 0.5,
                  }} />
                  <div style={{
                    position: 'absolute',
                    top: 100 + seededRandom(frame + 3) * 400,
                    left: 0,
                    width: '100%',
                    height: 3,
                    backgroundColor: '#000000',
                    opacity: 0.4,
                  }} />
                </>
              )}
            </div>
          </div>

          {/* Red glow under phone on glitch */}
          {glitchActive && (
            <div style={{
              position: 'absolute',
              inset: -40,
              borderRadius: 70,
              boxShadow: '0 0 120px rgba(255,34,34,0.5)',
              pointerEvents: 'none',
            }} />
          )}
        </div>

        {/* Subhead BOTTOM */}
        <div style={{
          position: 'absolute',
          bottom: 110,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: subOp,
          transform: `translateY(${subY}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            padding: '14px 36px',
            backgroundColor: '#000000',
            borderRadius: 8,
          }}>
            <div style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 68,
              fontWeight: 800,
              color: '#EEEEEE',
              letterSpacing: 1,
            }}>
              क्या App ग्लिच कर रहा है?
            </div>
          </div>
        </div>

        {/* Left side decorative ticker */}
        <div style={{
          position: 'absolute',
          left: 60,
          top: '50%',
          transform: 'translateY(-50%) rotate(-90deg)',
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 18,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
          letterSpacing: 6,
        }}>
          LIVE / CRYPTO MARKET / CRASH
        </div>

        {/* Right side date */}
        <div style={{
          position: 'absolute',
          right: 70,
          top: '50%',
          transform: 'translateY(-50%) rotate(90deg)',
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 18,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
          letterSpacing: 6,
        }}>
          REPORT // 06.2026
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, transparent 50%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 1,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_PhoneGlitch;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_ThreeCoinsCrash',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_ThreeCoinsCrash: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [-15, 15], { extrapolateRight: 'clamp' });

  // Header
  const headOp = interpolate(frame, [10, 35], [0, 1], { extrapolateRight: 'clamp' });

  // Three card entries
  const card1 = sp(20);
  const card2 = sp(80);
  const card3 = sp(140);

  // Number animations
  const btcVal = Math.floor(interpolate(frame, [20, 80], [69000, 60000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
  const ethVal = Math.floor(interpolate(frame, [80, 140], [2400, 1700], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
  const solVal = Math.floor(interpolate(frame, [140, 200], [180, 117], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));

  // Shake card on impact
  const card1Shake = (frame > 60 && frame < 90) ? Math.sin(frame * 1.5) * 3 : 0;
  const card2Shake = (frame > 120 && frame < 150) ? Math.sin(frame * 1.5) * 3 : 0;
  const card3Shake = (frame > 180 && frame < 210) ? Math.sin(frame * 1.5) * 3 : 0;

  const particles = Array.from({ length: 32 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4BB8FA', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,255,255,0.12) 0%, transparent 50%, rgba(0,0,0,0.3) 100%)',
      }} />

      {/* Diagonal lines texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        <defs>
          <pattern id="diag02" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag02)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: -300 + Math.sin(frame * 0.01) * 40,
        top: 200,
        width: 800,
        height: 800,
        borderRadius: '50%',
        backgroundColor: '#EEEEEE',
        opacity: 0.04,
        transform: `scale(${1 + Math.sin(frame * 0.015) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute',
        right: -200,
        top: -200,
        width: 600,
        height: 600,
        border: '4px solid #000000',
        opacity: 0.06,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />

      {/* Particles */}
      {particles.map(i => {
        const r = seededRandom(i + 11);
        const r2 = seededRandom(i + 77);
        const x = r * 1920;
        const y = (r2 * 1080 - frame * 1.2 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 4 + r * 5,
            height: 4 + r * 5,
            backgroundColor: '#000000',
            opacity: 0.2 + r * 0.2,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>

        {/* Header */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: headOp,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 28,
            fontWeight: 700,
            color: '#000000',
            letterSpacing: 10,
            opacity: 0.6,
          }}>
            MARKET CRASH — LIVE
          </div>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 95,
            fontWeight: 900,
            color: '#000000',
            marginTop: 14,
            letterSpacing: -1,
          }}>
            खून बह रहा है
          </div>
        </div>

        {/* Three crypto cards */}
        <div style={{
          position: 'absolute',
          top: 360,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          gap: 50,
        }}>

          {/* BTC Card */}
          <div style={{
            width: 420,
            height: 540,
            backgroundColor: '#000000',
            borderRadius: 18,
            padding: 36,
            transform: `translateY(${interpolate(card1, [0, 1], [200, 0])}px) translateX(${card1Shake}px) scale(${card1})`,
            opacity: card1,
            boxShadow: '0 30px 60px rgba(0,0,0,0.3)',
          }}>
            <div style={{
              width: 70,
              height: 70,
              borderRadius: '50%',
              backgroundColor: '#F7931A',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Inter, sans-serif',
              fontSize: 42,
              fontWeight: 900,
              color: '#000000',
            }}>₿</div>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 26,
              fontWeight: 700,
              color: '#EEEEEE',
              opacity: 0.6,
              marginTop: 28,
              letterSpacing: 3,
            }}>BITCOIN</div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 72,
              fontWeight: 800,
              color: '#EEEEEE',
              marginTop: 16,
              lineHeight: 1,
            }}>
              ${btcVal.toLocaleString()}
            </div>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 30,
              fontWeight: 700,
              color: '#FF4444',
              marginTop: 18,
            }}>
              ▼ -13.0%
            </div>
            {/* Mini chart */}
            <svg style={{ marginTop: 30 }} width="350" height="110">
              <polyline points="0,20 40,30 80,18 120,45 160,40 200,70 240,85 280,75 320,95 350,105"
                fill="none" stroke="#FF4444" strokeWidth="5" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>

          {/* ETH Card */}
          <div style={{
            width: 420,
            height: 540,
            backgroundColor: '#EEEEEE',
            borderRadius: 18,
            padding: 36,
            transform: `translateY(${interpolate(card2, [0, 1], [200, 0])}px) translateX(${card2Shake}px) scale(${card2})`,
            opacity: card2,
            boxShadow: '0 30px 60px rgba(0,0,0,0.3)',
          }}>
            <div style={{
              width: 70,
              height: 70,
              transform: 'rotate(45deg)',
              backgroundColor: '#627EEA',
            }} />
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 26,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.6,
              marginTop: 28,
              letterSpacing: 3,
            }}>ETHEREUM</div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 72,
              fontWeight: 800,
              color: '#000000',
              marginTop: 16,
              lineHeight: 1,
            }}>
              ${ethVal.toLocaleString()}
            </div>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 30,
              fontWeight: 700,
              color: '#FF2222',
              marginTop: 18,
            }}>
              ▼ -29.1%
            </div>
            <svg style={{ marginTop: 30 }} width="350" height="110">
              <polyline points="0,15 40,25 80,40 120,30 160,55 200,75 240,68 280,90 320,95 350,108"
                fill="none" stroke="#FF2222" strokeWidth="5" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>

          {/* SOL Card */}
          <div style={{
            width: 420,
            height: 540,
            backgroundColor: '#000000',
            borderRadius: 18,
            padding: 36,
            transform: `translateY(${interpolate(card3, [0, 1], [200, 0])}px) translateX(${card3Shake}px) scale(${card3})`,
            opacity: card3,
            boxShadow: '0 30px 60px rgba(0,0,0,0.3)',
            border: '3px solid #FF2222',
          }}>
            <div style={{
              width: 70,
              height: 70,
              borderRadius: 14,
              background: 'linear-gradient(135deg, #14F195, #9945FF)',
            }} />
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 26,
              fontWeight: 700,
              color: '#EEEEEE',
              opacity: 0.6,
              marginTop: 28,
              letterSpacing: 3,
            }}>SOLANA</div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 72,
              fontWeight: 800,
              color: '#EEEEEE',
              marginTop: 16,
              lineHeight: 1,
            }}>
              ${solVal}
            </div>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 30,
              fontWeight: 700,
              color: '#FF2222',
              marginTop: 18,
            }}>
              ▼ -35.0% BROKEN
            </div>
            <svg style={{ marginTop: 30 }} width="350" height="110">
              <polyline points="0,10 40,20 80,15 120,40 160,55 200,50 240,80 280,95 320,100 350,108"
                fill="none" stroke="#FF2222" strokeWidth="5" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>
        </div>

        {/* Bottom strip */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [220, 250], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#000000',
            padding: '14px 44px',
            borderRadius: 4,
          }}>
            <span style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              fontWeight: 700,
              color: '#EEEEEE',
              letterSpacing: 4,
            }}>
              -$2 TRILLION WIPED IN 48 HOURS
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, transparent 60%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_ThreeCoinsCrash;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_HeadlinesStorm',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_HeadlinesStorm: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headlines = [
    { src: 'CRYPTO TIMES', txt: 'SAYLOR DUMPS BITCOIN — MARKET PANIC', delay: 15, x: 80, y: 140, rot: -4 },
    { src: 'COINTELEGRAPH', txt: 'STRATEGY SELLS BTC FOR FIRST TIME', delay: 35, x: 1050, y: 180, rot: 3 },
    { src: 'BLOOMBERG', txt: 'MICHAEL SAYLOR OFFLOADS HOLDINGS', delay: 55, x: 150, y: 460, rot: 2 },
    { src: 'DECRYPT', txt: 'IS THIS THE END OF THE BULL RUN?', delay: 75, x: 1100, y: 500, rot: -3 },
    { src: 'CNBC', txt: 'STRATEGY DUMPS — BTC CRASHES', delay: 95, x: 100, y: 780, rot: -2 },
    { src: 'THE BLOCK', txt: 'SAYLOR LOSES FAITH IN BITCOIN?', delay: 115, x: 1080, y: 800, rot: 4 },
  ];

  // Center reveal "SAYLOR"
  const centerOp = interpolate(frame, [180, 230], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const centerScale = sp(180, { damping: 10, stiffness: 100 });
  const centerShake = (frame > 200 && frame < 240) ? Math.sin(frame * 2) * 5 : 0;

  // Camera subtle zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  const particles = Array.from({ length: 40 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4BB8FA', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0.4) 0%, transparent 70%)',
      }} />

      {/* Concentric circles texture */}
      <svg style={{ position: 'absolute', left: '50%', top: '50%', transform: 'translate(-50%, -50%)', opacity: 0.07 }} width="1600" height="1600">
        {[100, 250, 400, 550, 700, 800].map((r, i) => (
          <circle key={i} cx="800" cy="800" r={r} fill="none" stroke="#000000" strokeWidth="2" />
        ))}
      </svg>

      {/* Shape layer */}
      <div style={{
        position: 'absolute',
        right: -100,
        bottom: -150,
        width: 500,
        height: 500,
        backgroundColor: '#EEEEEE',
        opacity: 0.04,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.08})`,
      }} />

      {/* Floating particles */}
      {particles.map(i => {
        const r = seededRandom(i + 5);
        const x = (r * 1920 + frame * 0.5) % 1920;
        const y = seededRandom(i + 33) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 3 + r * 4,
            height: 3 + r * 4,
            backgroundColor: '#EEEEEE',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>

        {/* Headlines flying in */}
        {headlines.map((h, i) => {
          const s = sp(h.delay, { damping: 11, stiffness: 95 });
          const fadeOut = frame > 200 ? interpolate(frame, [200, 240], [1, 0.15], { extrapolateRight: 'clamp' }) : 1;
          return (
            <div key={i} style={{
              position: 'absolute',
              left: h.x,
              top: h.y,
              width: 760,
              transform: `scale(${s}) rotate(${h.rot}deg)`,
              opacity: s * fadeOut,
              backgroundColor: '#EEEEEE',
              padding: '24px 32px',
              boxShadow: '0 20px 40px rgba(0,0,0,0.3)',
              borderLeft: '8px solid #FF2222',
            }}>
              <div style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 22,
                fontWeight: 800,
                color: '#FF2222',
                letterSpacing: 4,
              }}>{h.src}</div>
              <div style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 36,
                fontWeight: 900,
                color: '#000000',
                marginTop: 8,
                lineHeight: 1.1,
              }}>{h.txt}</div>
            </div>
          );
        })}

        {/* Center SAYLOR reveal */}
        <div style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) translateX(${centerShake}px) scale(${centerScale})`,
          opacity: centerOp,
          textAlign: 'center',
          zIndex: 10,
        }}>
          <div style={{
            backgroundColor: '#000000',
            padding: '40px 80px',
            border: '6px solid #FF2222',
            boxShadow: '0 0 100px rgba(255,34,34,0.4)',
          }}>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 32,
              fontWeight: 700,
              color: '#FF2222',
              letterSpacing: 10,
            }}>
              EK HI NAAM
            </div>
            <div style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 200,
              fontWeight: 900,
              color: '#EEEEEE',
              letterSpacing: -4,
              lineHeight: 1,
              marginTop: 10,
            }}>
              SAYLOR
            </div>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 600,
              color: '#EEEEEE',
              opacity: 0.6,
              marginTop: 12,
              letterSpacing: 6,
            }}>
              STRATEGY DUMPED BITCOIN
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_HeadlinesStorm;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_KunalIntro',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_KunalIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-0.5, 0.5]);

  // Left "MAIN" label
  const labelOp = interpolate(frame, [10, 35], [0, 1], { extrapolateRight: 'clamp' });

  // Name reveal
  const nameScale = sp(20, { damping: 11, stiffness: 100 });
  const nameY = interpolate(sp(20), [0, 1], [60, 0]);

  // Subtitle
  const subOp = interpolate(frame, [70, 100], [0, 1], { extrapolateRight: 'clamp' });

  // Timeline reveal
  const timelineOp = interpolate(frame, [130, 180], [0, 1], { extrapolateRight: 'clamp' });
  const timelineProgress = interpolate(frame, [150, 320], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  // Crash markers on timeline
  const markers = [
    { yr: '2018', label: 'CRYPTO -80%', xPct: 0.15, delay: 170 },
    { yr: '2020', label: 'COVID CRASH', xPct: 0.35, delay: 200 },
    { yr: '2022', label: 'LUNA + FTX', xPct: 0.6, delay: 230 },
    { yr: '2026', label: 'TODAY', xPct: 0.92, delay: 260 },
  ];

  const particles = Array.from({ length: 30 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4BB8FA', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(0,0,0,0.15) 0%, transparent 40%, rgba(0,0,0,0.25) 100%)',
      }} />

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="grid04" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M 100 0 L 0 0 0 100" fill="none" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid04)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: 200 + Math.sin(frame * 0.012) * 30,
        top: -100,
        width: 500,
        height: 500,
        border: '3px solid #EEEEEE',
        borderRadius: '50%',
        opacity: 0.1,
      }} />
      <div style={{
        position: 'absolute',
        right: 100,
        bottom: 100,
        width: 400,
        height: 400,
        backgroundColor: '#000000',
        opacity: 0.05,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Particles */}
      {particles.map(i => {
        const r = seededRandom(i + 22);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r * 1920 + frame * 0.4) % 1920,
            top: seededRandom(i + 88) * 1080,
            width: 3 + r * 4,
            height: 3 + r * 4,
            backgroundColor: '#EEEEEE',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)` }}>

        {/* Left tag */}
        <div style={{
          position: 'absolute',
          top: 120,
          left: 120,
          opacity: labelOp,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#000000',
            padding: '10px 24px',
            fontFamily: 'Inter, sans-serif',
            fontSize: 24,
            fontWeight: 800,
            color: '#EEEEEE',
            letterSpacing: 6,
          }}>
            HOST / NARRATOR
          </div>
        </div>

        {/* Big name */}
        <div style={{
          position: 'absolute',
          top: 200,
          left: 120,
          opacity: nameScale,
          transform: `translateY(${nameY}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 50,
            fontWeight: 700,
            color: '#000000',
            opacity: 0.7,
            letterSpacing: 2,
          }}>
            मैं हूँ
          </div>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 320,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -8,
            lineHeight: 0.9,
            marginTop: -10,
          }}>
            कुणाल
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 36,
            fontWeight: 800,
            color: '#EEEEEE',
            backgroundColor: '#000000',
            display: 'inline-block',
            padding: '8px 24px',
            letterSpacing: 8,
            marginTop: 10,
          }}>
            KUNAL
          </div>
        </div>

        {/* Subtitle */}
        <div style={{
          position: 'absolute',
          top: 750,
          left: 120,
          right: 120,
          opacity: subOp,
        }}>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 52,
            fontWeight: 700,
            color: '#000000',
            lineHeight: 1.2,
            maxWidth: 1000,
          }}>
            कई सालों से मार्केट का पैनिक देखता आ रहा हूँ
          </div>
        </div>

        {/* Right side — timeline of crashes */}
        <div style={{
          position: 'absolute',
          top: 250,
          right: 100,
          width: 700,
          opacity: timelineOp,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 24,
            fontWeight: 700,
            color: '#000000',
            opacity: 0.6,
            letterSpacing: 6,
            marginBottom: 30,
          }}>
            PANIC TIMELINE
          </div>

          {/* Vertical line */}
          <div style={{ position: 'relative', height: 400 }}>
            <div style={{
              position: 'absolute',
              left: 30,
              top: 0,
              width: 4,
              height: `${timelineProgress * 100}%`,
              backgroundColor: '#000000',
            }} />

            {markers.map((m, i) => {
              const mSp = sp(m.delay, { damping: 11 });
              return (
                <div key={i} style={{
                  position: 'absolute',
                  left: 0,
                  top: m.xPct * 400 - 25,
                  opacity: mSp,
                  transform: `translateX(${interpolate(mSp, [0, 1], [-40, 0])}px)`,
                }}>
                  <div style={{
                    display: 'flex',
                    alignItems: 'center',
                    gap: 24,
                  }}>
                    <div style={{
                      width: 28,
                      height: 28,
                      borderRadius: '50%',
                      backgroundColor: i === 3 ? '#FF2222' : '#000000',
                      border: '4px solid #EEEEEE',
                      marginLeft: 18,
                    }} />
                    <div>
                      <div style={{
                        fontFamily: 'JetBrains Mono, monospace',
                        fontSize: 28,
                        fontWeight: 800,
                        color: '#000000',
                      }}>{m.yr}</div>
                      <div style={{
                        fontFamily: 'Inter, sans-serif',
                        fontSize: 22,
                        fontWeight: 700,
                        color: i === 3 ? '#FF2222' : '#000000',
                        opacity: i === 3 ? 1 : 0.7,
                        letterSpacing: 2,
                      }}>{m.label}</div>
                    </div>
                  </div>
                </div>
              );
            })}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, transparent 60%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_KunalIntro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_NoiseVsData',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_NoiseVsData: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Left side noise — frame 0 to 200
  const noiseScale = interpolate(frame, [200, 280], [1, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const noiseOp = interpolate(frame, [200, 260], [1, 0.2], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const dataScale = sp(220, { damping: 11 });
  const dataOp = interpolate(frame, [220, 270], [0, 1], { extrapolateRight: 'clamp' });

  // VS divider
  const vsOp = interpolate(frame, [40, 70], [0, 1], { extrapolateRight: 'clamp' });

  // Title
  const titleOp = interpolate(frame, [10, 40], [0, 1], { extrapolateRight: 'clamp' });

  // Noise random words (shaking)
  const noiseWords = ['SELL!', 'PANIC!', 'CRASH!', 'DUMP!', 'END!', 'SCAM!', 'OVER!', 'BLOOD!', 'CRISIS!', 'FEAR!'];

  // Chart line drawing
  const chartProgress = interpolate(frame, [260, 380], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 35 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4BB8FA', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(255,34,34,0.12) 0%, transparent 50%, rgba(0,0,0,0.2) 100%)',
      }} />

      {/* Dotted texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="1920" height="1080">
        <defs>
          <pattern id="dots05" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots05)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: -150,
        top: 200,
        width: 500,
        height: 500,
        borderRadius: '50%',
        backgroundColor: '#FF2222',
        opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.08})`,
      }} />
      <div style={{
        position: 'absolute',
        right: -100,
        bottom: 100,
        width: 450,
        height: 450,
        borderRadius: '50%',
        backgroundColor: '#000000',
        opacity: 0.07,
      }} />

      {particles.map(i => {
        const r = seededRandom(i + 6);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r * 1920 + frame * 0.6) % 1920,
            top: seededRandom(i + 100) * 1080,
            width: 4,
            height: 4,
            backgroundColor: '#EEEEEE',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute',
        top: 70,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: titleOp,
      }}>
        <div style={{
          fontFamily: 'Yantra One, sans-serif',
          fontSize: 90,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -1,
        }}>
          शोर बनाम सच
        </div>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 30,
          fontWeight: 700,
          color: '#000000',
          opacity: 0.55,
          letterSpacing: 12,
          marginTop: 8,
        }}>
          NOISE VS DATA
        </div>
      </div>

      {/* LEFT SIDE — NOISE */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: 280,
        width: 800,
        height: 700,
        transform: `scale(${noiseScale})`,
        opacity: noiseOp,
        transformOrigin: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 38,
          fontWeight: 800,
          color: '#FF2222',
          letterSpacing: 8,
          textAlign: 'center',
          marginBottom: 30,
        }}>
          शोर / NOISE
        </div>

        {/* Chaotic words */}
        <div style={{ position: 'relative', height: 580 }}>
          {noiseWords.map((w, i) => {
            const r = seededRandom(i + 11);
            const r2 = seededRandom(i + 44);
            const shakeX = Math.sin(frame * 0.3 + i) * 6;
            const shakeY = Math.cos(frame * 0.35 + i) * 5;
            return (
              <div key={i} style={{
                position: 'absolute',
                left: r * 600,
                top: r2 * 500,
                fontFamily: 'Yantra One, sans-serif',
                fontSize: 50 + r * 50,
                fontWeight: 900,
                color: i % 2 === 0 ? '#FF2222' : '#000000',
                transform: `translate(${shakeX}px, ${shakeY}px) rotate(${(r - 0.5) * 25}deg)`,
                opacity: 0.7 + r * 0.3,
              }}>
                {w}
              </div>
            );
          })}
        </div>
      </div>

      {/* VS Divider */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: 480,
        transform: 'translateX(-50%)',
        opacity: vsOp,
      }}>
        <div style={{
          width: 4,
          height: 500,
          backgroundColor: '#000000',
          position: 'absolute',
          left: '50%',
          top: -150,
          transform: 'translateX(-50%)',
        }} />
        <div style={{
          width: 130,
          height: 130,
          borderRadius: '50%',
          backgroundColor: '#000000',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          fontFamily: 'Inter, sans-serif',
          fontSize: 48,
          fontWeight: 900,
          color: '#EEEEEE',
          letterSpacing: 2,
          position: 'relative',
        }}>
          VS
        </div>
      </div>

      {/* RIGHT SIDE — DATA */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: 280,
        width: 800,
        height: 700,
        transform: `scale(${dataScale})`,
        opacity: dataOp,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 38,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 8,
          textAlign: 'center',
          marginBottom: 30,
        }}>
          सच / DATA
        </div>

        {/* Data chart box */}
        <div style={{
          width: '100%',
          height: 550,
          backgroundColor: '#000000',
          padding: 40,
          borderRadius: 12,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            fontWeight: 700,
            color: '#EEEEEE',
            opacity: 0.5,
            letterSpacing: 4,
          }}>
            REAL DATA / VERIFIED
          </div>

          {/* Clean upward trending chart */}
          <svg style={{ marginTop: 30 }} width="700" height="380" viewBox="0 0 700 380">
            {/* Grid */}
            {[0, 1, 2, 3].map(i => (
              <line key={i} x1="0" y1={i * 95 + 20} x2="700" y2={i * 95 + 20} stroke="#EEEEEE" strokeWidth="1" opacity="0.15" />
            ))}
            {/* Volatile but uptrending line */}
            <polyline
              points="0,320 60,290 120,310 180,260 240,280 300,220 360,240 420,180 480,200 540,140 600,160 660,90 700,110"
              fill="none"
              stroke="#4BB8FA"
              strokeWidth="6"
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeDasharray="2000"
              strokeDashoffset={2000 - chartProgress * 2000}
            />
            {/* Endpoint */}
            <circle cx="700" cy="110" r="12" fill="#4BB8FA" opacity={chartProgress} />
          </svg>

          <div style={{
            marginTop: 20,
            display: 'flex',
            justifyContent: 'space-between',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#EEEEEE',
          }}>
            <div>
              <div style={{ opacity: 0.5, fontSize: 18 }}>VOLATILITY</div>
              <div style={{ fontWeight: 800, fontSize: 30 }}>NORMAL</div>
            </div>
            <div>
              <div style={{ opacity: 0.5, fontSize: 18 }}>LONG TERM</div>
              <div style={{ fontWeight: 800, fontSize: 30, color: '#4BB8FA' }}>↗ UP</div>
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, transparent 60%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_NoiseVsData;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_FilterTruth',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_FilterTruth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Funnel reveal
  const funnelOp = interpolate(frame, [20, 60], [0, 1], { extrapolateRight: 'clamp' });
  const funnelScale = sp(20, { damping: 11 });

  // Input chaos words falling
  const chaosWords = ['HYPE', 'FUD', 'PANIC', 'RUMOR', 'CLICKBAIT', 'NOISE', 'FOMO'];

  // Output words rising
  const outputs = ['DATA', 'FACTS', 'TRUTH'];

  // Bottom CTA
  const ctaOp = interpolate(frame, [240, 280], [0, 1], { extrapolateRight: 'clamp' });
  const ctaScale = sp(240, { damping: 10 });

  // Title
  const titleOp = interpolate(frame, [5, 30], [0, 1], { extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 30 }, (_, i) => i);

  // Camera zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4BB8FA', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, rgba(255,255,255,0.15) 0%, transparent 60%)',
      }} />

      {/* Cross-hatch texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        <defs>
          <pattern id="crosshatch06" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="0" y1="30" x2="60" y2="30" stroke="#000000" strokeWidth="1" />
            <line x1="30" y1="0" x2="30" y2="60" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#crosshatch06)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: 100,
        width: 500,
        height: 500,
        border: '4px solid #000000',
        opacity: 0.06,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute',
        right: 100,
        bottom: 80,
        width: 400,
        height: 400,
        backgroundColor: '#EEEEEE',
        opacity: 0.05,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />

      {particles.map(i => {
        const r = seededRandom(i + 9);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r * 1920 + frame * 0.5) % 1920,
            top: seededRandom(i + 60) * 1080,
            width: 3 + r * 4,
            height: 3 + r * 4,
            backgroundColor: '#EEEEEE',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>

        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
        }}>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 95,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -1,
          }}>
            शोर छानेंगे, सच निकालेंगे
          </div>
        </div>

        {/* Funnel diagram */}
        <div style={{
          position: 'absolute',
          left: '50%',
          top: 250,
          transform: `translateX(-50%) scale(${funnelScale})`,
          opacity: funnelOp,
        }}>
          {/* Falling chaos words above */}
          <div style={{
            position: 'absolute',
            top: -60,
            left: -300,
            width: 1100,
            height: 100,
          }}>
            {chaosWords.map((w, i) => {
              const delay = 40 + i * 18;
              const startY = -100;
              const endY = 100;
              const localFrame = frame - delay;
              const fallY = localFrame > 0 ? interpolate(localFrame, [0, 60], [startY, endY], { extrapolateRight: 'clamp', easing: Easing.in(Easing.cubic) }) : startY;
              const fadeOut = localFrame > 0 ? interpolate(localFrame, [50, 80], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) : 1;
              return (
                <div key={i} style={{
                  position: 'absolute',
                  left: i * 150 + 30,
                  top: fallY,
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 38,
                  fontWeight: 900,
                  color: '#FF2222',
                  opacity: fadeOut * 0.9,
                  transform: `rotate(${(seededRandom(i) - 0.5) * 30}deg)`,
                }}>
                  {w}
                </div>
              );
            })}
          </div>

          {/* Funnel shape */}
          <svg width="600" height="500" viewBox="0 0 600 500">
            {/* Outer */}
            <polygon
              points="50,50 550,50 380,300 380,480 220,480 220,300"
              fill="#000000"
              stroke="#000000"
              strokeWidth="4"
            />
            {/* Inner highlight */}
            <polygon
              points="80,80 520,80 360,290 360,460 240,460 240,290"
              fill="none"
              stroke="#4BB8FA"
              strokeWidth="3"
              opacity="0.6"
            />
            {/* Label */}
            <text x="300" y="200" textAnchor="middle" fontFamily="Inter" fontSize="46" fontWeight="900" fill="#EEEEEE">
              FILTER
            </text>
            <text x="300" y="380" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="24" fontWeight="700" fill="#4BB8FA" letterSpacing="4">
              PROCESSING
            </text>

            {/* Animated dots inside */}
            {[0, 1, 2].map(i => {
              const dotY = ((frame * 3 + i * 60) % 200) + 250;
              return (
                <circle key={i} cx="300" cy={dotY} r="6" fill="#4BB8FA" opacity="0.8" />
              );
            })}
          </svg>

          {/* Output truth coming out bottom */}
          <div style={{
            position: 'absolute',
            top: 500,
            left: 0,
            width: 600,
            textAlign: 'center',
          }}>
            {outputs.map((w, i) => {
              const delay = 150 + i * 25;
              const wSp = sp(delay, { damping: 11 });
              return (
                <div key={i} style={{
                  display: 'inline-block',
                  margin: '0 12px',
                  fontFamily: 'Yantra One, sans-serif',
                  fontSize: 60,
                  fontWeight: 900,
                  color: '#000000',
                  opacity: wSp,
                  transform: `scale(${wSp})`,
                }}>
                  {w}
                </div>
              );
            })}
          </div>
        </div>

        {/* CTA bottom */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: ctaOp,
          transform: `scale(${ctaScale})`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#000000',
            padding: '20px 60px',
            border: '4px solid #EEEEEE',
          }}>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 42,
              fontWeight: 900,
              color: '#EEEEEE',
              letterSpacing: 4,
            }}>
              आज सिर्फ DATA पर बात होगी
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, transparent 60%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_FilterTruth;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_ThreeQuestions',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_ThreeQuestions: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleOp = interpolate(frame, [10, 35], [0, 1], { extrapolateRight: 'clamp' });

  // 3 question boxes
  const q1 = sp(40);
  const q2 = sp(120);
  const q3 = sp(200);

  // Camera pan
  const camY = interpolate(frame, [0, durationInFrames], [-10, 10]);

  const particles = Array.from({ length: 30 }, (_, i) => i);

  const questions = [
    {
      no: '01',
      hi: 'क्रैश क्यों हुआ?',
      en: 'WHY THE CRASH?',
      sub: 'Real reasons behind 48% drop',
      sp: q1,
      icon: 'crash',
    },
    {
      no: '02',
      hi: 'सेलर का रोल?',
      en: "SAYLOR'S ROLE?",
      sub: 'Did he really dump Bitcoin?',
      sp: q2,
      icon: 'role',
    },
    {
      no: '03',
      hi: 'भारत का नज़रिया?',
      en: 'INDIAN ANGLE?',
      sub: 'What it means for you',
      sp: q3,
      icon: 'india',
    },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#4BB8FA', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(0,0,0,0.2) 0%, transparent 50%, rgba(0,0,0,0.25) 100%)',
      }} />

      {/* Grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        <defs>
          <pattern id="grid07" width="120" height="120" patternUnits="userSpaceOnUse">
            <path d="M 120 0 L 0 0 0 120" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid07)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: -100,
        top: -100,
        width: 600,
        height: 600,
        borderRadius: '50%',
        border: '3px solid #EEEEEE',
        opacity: 0.08,
        transform: `scale(${1 + Math.sin(frame * 0.015) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        right: -150,
        bottom: -150,
        width: 700,
        height: 700,
        backgroundColor: '#000000',
        opacity: 0.05,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {particles.map(i => {
        const r = seededRandom(i + 14);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r * 1920 + frame * 0.6) % 1920,
            top: seededRandom(i + 70) * 1080,
            width: 4,
            height: 4,
            backgroundColor: '#EEEEEE',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>

        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            fontWeight: 700,
            color: '#000000',
            opacity: 0.6,
            letterSpacing: 10,
          }}>
            TODAY'S AGENDA
          </div>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 110,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -2,
            lineHeight: 1,
            marginTop: 10,
          }}>
            तीन सवाल
          </div>
        </div>

        {/* Three columns */}
        <div style={{
          position: 'absolute',
          top: 360,
          left: 80,
          right: 80,
          display: 'flex',
          justifyContent: 'space-between',
          gap: 40,
        }}>
          {questions.map((q, i) => {
            const isMid = i === 1;
            const bg = isMid ? '#EEEEEE' : '#000000';
            const fg = isMid ? '#000000' : '#EEEEEE';
            const accent = i === 0 ? '#FF2222' : (i === 1 ? '#4BB8FA' : '#FFC107');
            return (
              <div key={i} style={{
                width: 540,
                height: 560,
                backgroundColor: bg,
                borderRadius: 18,
                padding: 44,
                transform: `translateY(${interpolate(q.sp, [0, 1], [200, 0])}px) scale(${q.sp})`,
                opacity: q.sp,
                boxShadow: '0 25px 50px rgba(0,0,0,0.3)',
                position: 'relative',
                overflow: 'hidden',
              }}>
                {/* Number watermark */}
                <div style={{
                  position: 'absolute',
                  right: -20,
                  top: -50,
                  fontFamily: 'Yantra One, sans-serif',
                  fontSize: 380,
                  fontWeight: 900,
                  color: fg,
                  opacity: 0.06,
                  lineHeight: 1,
                }}>
                  {q.no}
                </div>

                {/* Small number tag */}
                <div style={{
                  display: 'inline-block',
                  padding: '8px 18px',
                  backgroundColor: accent,
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 22,
                  fontWeight: 800,
                  color: '#000000',
                  letterSpacing: 3,
                }}>
                  QUESTION {q.no}
                </div>

                {/* Hindi main */}
                <div style={{
                  fontFamily: 'Yantra One, sans-serif',
                  fontSize: 68,
                  fontWeight: 900,
                  color: fg,
                  marginTop: 70,
                  lineHeight: 1.1,
                  letterSpacing: -1,
                }}>
                  {q.hi}
                </div>

                {/* English */}
                <div style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 32,
                  fontWeight: 800,
                  color: accent,
                  marginTop: 18,
                  letterSpacing: 3,
                }}>
                  {q.en}
                </div>

                {/* Subtitle */}
                <div style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 24,
                  fontWeight: 500,
                  color: fg,
                  opacity: 0.7,
                  marginTop: 24,
                  lineHeight: 1.3,
                }}>
                  {q.sub}
                </div>

                {/* Bottom accent bar */}
                <div style={{
                  position: 'absolute',
                  bottom: 0,
                  left: 0,
                  width: '100%',
                  height: 12,
                  backgroundColor: accent,
                }} />
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, transparent 60%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_ThreeQuestions;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_IndianInvestor',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_IndianInvestor: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleOp = interpolate(frame, [10, 40], [0, 1], { extrapolateRight: 'clamp' });
  const titleY = interpolate(sp(10), [0, 1], [40, 0]);

  // India map / flag colors
  const mapScale = sp(50, { damping: 11 });

  // Stats
  const s1 = sp(120);
  const s2 = sp(165);
  const s3 = sp(210);

  // Camera zoom out
  const camZoom = interpolate(frame, [0, durationInFrames], [1.06, 1]);

  const particles = Array.from({ length: 30 }, (_, i) => i);

  const stats = [
    { val: '30%', label: 'FLAT CRYPTO TAX', sub: 'no loss offset', sp: s1 },
    { val: '1%', label: 'TDS PER TRADE', sub: 'every transaction', sp: s2 },
    { val: '0', label: 'DEDUCTION ALLOWED', sub: 'pure pain', sp: s3 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#4BB8FA', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 50%, rgba(255,193,7,0.15) 0%, transparent 50%), linear-gradient(180deg, transparent 0%, rgba(0,0,0,0.25) 100%)',
      }} />

      {/* Dotted texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        <defs>
          <pattern id="dots08" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots08)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: -200,
        top: 200,
        width: 600,
        height: 600,
        borderRadius: '50%',
        backgroundColor: '#FFC107',
        opacity: 0.08,
        transform: `scale(${1 + Math.sin(frame * 0.018) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute',
        right: 100,
        bottom: -100,
        width: 500,
        height: 500,
        border: '4px solid #000000',
        opacity: 0.06,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {particles.map(i => {
        const r = seededRandom(i + 21);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r * 1920 + frame * 0.5) % 1920,
            top: seededRandom(i + 99) * 1080,
            width: 4,
            height: 4,
            backgroundColor: '#EEEEEE',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>

        {/* Top eyebrow */}
        <div style={{
          position: 'absolute',
          top: 100,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            padding: '12px 32px',
            backgroundColor: '#000000',
            fontFamily: 'Inter, sans-serif',
            fontSize: 26,
            fontWeight: 800,
            color: '#FFC107',
            letterSpacing: 8,
          }}>
            INDIAN INVESTOR PERSPECTIVE
          </div>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 115,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -2,
            marginTop: 20,
            lineHeight: 1,
          }}>
            भारतीय नज़रिया
          </div>
        </div>

        {/* India flag bars + map suggestion (left) */}
        <div style={{
          position: 'absolute',
          left: 150,
          top: 460,
          transform: `scale(${mapScale})`,
          opacity: mapScale,
        }}>
          {/* Stylized India representation — 3 horizontal bars */}
          <div style={{
            width: 480,
            height: 320,
            backgroundColor: '#000000',
            borderRadius: 14,
            padding: 28,
            position: 'relative',
            overflow: 'hidden',
          }}>
            {/* Three colored bars representing flag */}
            <div style={{ width: '100%', height: 80, backgroundColor: '#FF9933', borderRadius: 6 }} />
            <div style={{
              width: '100%',
              height: 80,
              backgroundColor: '#EEEEEE',
              borderRadius: 6,
              marginTop: 12,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}>
              {/* Ashoka chakra suggestion */}
              <div style={{
                width: 60,
                height: 60,
                borderRadius: '50%',
                border: '4px solid #000080',
                position: 'relative',
                transform: `rotate(${frame * 1.5}deg)`,
              }}>
                {Array.from({ length: 12 }).map((_, i) => (
                  <div key={i} style={{
                    position: 'absolute',
                    left: '50%',
                    top: '50%',
                    width: 2,
                    height: 26,
                    backgroundColor: '#000080',
                    transformOrigin: '50% 0%',
                    transform: `translateX(-50%) rotate(${i * 30}deg)`,
                  }} />
                ))}
              </div>
            </div>
            <div style={{ width: '100%', height: 80, backgroundColor: '#138808', borderRadius: 6, marginTop: 12 }} />

            <div style={{
              position: 'absolute',
              bottom: 30,
              left: 28,
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 32,
              fontWeight: 900,
              color: '#EEEEEE',
              letterSpacing: 4,
            }}>
              INDIA / भारत
            </div>
          </div>
        </div>

        {/* Right side — stats */}
        <div style={{
          position: 'absolute',
          right: 120,
          top: 440,
          width: 700,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 24,
            fontWeight: 700,
            color: '#000000',
            opacity: 0.6,
            letterSpacing: 6,
            marginBottom: 24,
          }}>
            WHAT MAKES INDIA DIFFERENT
          </div>

          {stats.map((s, i) => (
            <div key={i} style={{
              display: 'flex',
              alignItems: 'center',
              marginBottom: 24,
              transform: `translateX(${interpolate(s.sp, [0, 1], [-80, 0])}px)`,
              opacity: s.sp,
            }}>
              <div style={{
                minWidth: 160,
                height: 110,
                backgroundColor: '#000000',
                borderRadius: 10,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 56,
                fontWeight: 900,
                color: '#FFC107',
              }}>
                {s.val}
              </div>
              <div style={{ marginLeft: 26 }}>
                <div style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 34,
                  fontWeight: 800,
                  color: '#000000',
                  letterSpacing: 2,
                }}>
                  {s.label}
                </div>
                <div style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 22,
                  fontWeight: 500,
                  color: '#000000',
                  opacity: 0.6,
                  marginTop: 4,
                }}>
                  {s.sub}
                </div>
              </div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, transparent 60%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_IndianInvestor;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_SaylorSold',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_SaylorSold: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Newspaper entrance
  const paperScale = sp(20, { damping: 10, stiffness: 90 });
  const paperRot = interpolate(sp(20), [0, 1], [-15, -3]);

  // Stamp "EVERYWHERE"
  const stampScale = sp(140, { damping: 8, stiffness: 200 });
  const stampRot = interpolate(sp(140), [0, 1], [25, -8]);

  // Bottom big headline
  const headOp = interpolate(frame, [220, 260], [0, 1], { extrapolateRight: 'clamp' });
  const headScale = sp(220, { damping: 11 });

  // Camera
  const camY = interpolate(frame, [0, durationInFrames], [-8, 8]);

  const particles = Array.from({ length: 30 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4BB8FA', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, transparent 0%, rgba(0,0,0,0.25) 100%)',
      }} />

      {/* Halftone dots */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="1920" height="1080">
        <defs>
          <pattern id="halftone09" width="20" height="20" patternUnits="userSpaceOnUse">
            <circle cx="10" cy="10" r="2.5" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#halftone09)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: -100,
        bottom: -100,
        width: 600,
        height: 600,
        borderRadius: '50%',
        backgroundColor: '#000000',
        opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.018) * 0.07})`,
      }} />
      <div style={{
        position: 'absolute',
        right: -150,
        top: -100,
        width: 500,
        height: 500,
        border: '4px solid #EEEEEE',
        opacity: 0.1,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />

      {particles.map(i => {
        const r = seededRandom(i + 31);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r * 1920 + frame * 0.6) % 1920,
            top: seededRandom(i + 88) * 1080,
            width: 3 + r * 4,
            height: 3 + r * 4,
            backgroundColor: '#EEEEEE',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>

        {/* Top eyebrow */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [5, 30], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            display: 'inline-block',
            padding: '10px 30px',
            backgroundColor: '#000000',
            fontFamily: 'Inter, sans-serif',
            fontSize: 26,
            fontWeight: 800,
            color: '#EEEEEE',
            letterSpacing: 8,
          }}>
            EVERY WHERE / हर जगह
          </div>
        </div>

        {/* Newspaper mockup */}
        <div style={{
          position: 'absolute',
          left: 150,
          top: 220,
          width: 800,
          height: 720,
          backgroundColor: '#EEEEEE',
          padding: 50,
          transform: `scale(${paperScale}) rotate(${paperRot}deg)`,
          opacity: paperScale,
          boxShadow: '30px 30px 0px rgba(0,0,0,0.4)',
        }}>
          {/* Masthead */}
          <div style={{
            borderBottom: '4px solid #000000',
            paddingBottom: 14,
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
          }}>
            <div style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 48,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -1,
            }}>
              CRYPTO TIMES
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 18,
              color: '#000000',
              opacity: 0.6,
            }}>
              JUNE 2026 / VOL. 14
            </div>
          </div>

          {/* Sub head tag */}
          <div style={{
            marginTop: 24,
            fontFamily: 'Inter, sans-serif',
            fontSize: 20,
            fontWeight: 800,
            color: '#FF2222',
            letterSpacing: 6,
          }}>
            BREAKING / EXCLUSIVE
          </div>

          {/* Main headline */}
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 92,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 0.95,
            marginTop: 18,
            letterSpacing: -3,
          }}>
            SAYLOR SOLD BITCOIN
          </div>

          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 56,
            fontWeight: 800,
            color: '#000000',
            marginTop: 24,
            opacity: 0.85,
          }}>
            सेलर ने बेच दिया!
          </div>

          {/* Body lines mock */}
          <div style={{ marginTop: 30 }}>
            {[100, 90, 95, 80, 88, 70].map((w, i) => (
              <div key={i} style={{
                width: `${w}%`,
                height: 12,
                backgroundColor: '#000000',
                opacity: 0.5,
                marginBottom: 14,
              }} />
            ))}
          </div>
        </div>

        {/* EVERYWHERE stamp */}
        <div style={{
          position: 'absolute',
          right: 180,
          top: 360,
          transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
          opacity: stampScale,
        }}>
          <div style={{
            border: '8px solid #FF2222',
            padding: '30px 60px',
            backgroundColor: 'rgba(255,255,255,0.15)',
          }}>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 110,
              fontWeight: 900,
              color: '#FF2222',
              letterSpacing: 8,
              lineHeight: 1,
              textShadow: '3px 3px 0px rgba(0,0,0,0.2)',
            }}>
              EVERY
            </div>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 110,
              fontWeight: 900,
              color: '#FF2222',
              letterSpacing: 8,
              lineHeight: 1,
            }}>
              WHERE
            </div>
          </div>
        </div>

        {/* Bottom Big Claim */}
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: headOp,
          transform: `scale(${headScale})`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#000000',
            padding: '20px 50px',
            border: '4px solid #FF2222',
          }}>
            <span style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 56,
              fontWeight: 900,
              color: '#EEEEEE',
              letterSpacing: 1,
            }}>
              अब असली सवाल — कितना बेचा?
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, transparent 60%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_SaylorSold;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_ThirtyTwoBTC',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_ThirtyTwoBTC: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Question entry
  const qOp = interpolate(frame, [10, 40], [0, 1], { extrapolateRight: 'clamp' });
  const qY = interpolate(sp(10), [0, 1], [40, 0]);

  // Counter from 0 to 32
  const counterStart = 60;
  const counterEnd = 130;
  const counterVal = Math.floor(interpolate(frame, [counterStart, counterEnd], [0, 32], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic)
  }));

  // Big 32 shake on land
  const bigShake = (frame > 125 && frame < 160) ? Math.sin(frame * 2) * 6 : 0;
  const bigScale = sp(60, { damping: 9, stiffness: 100 });

  // "Yes you heard right" reveal
  const confirmOp = interpolate(frame, [180, 220], [0, 1], { extrapolateRight: 'clamp' });
  const confirmScale = sp(180, { damping: 11 });

  // BTC coins flying out (32 dots representing)
  const coinDelay = 70;

  // Camera zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.08]);

  const particles = Array.from({ length: 30 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4BB8FA', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 60%, rgba(255,193,7,0.15) 0%, transparent 55%), linear-gradient(180deg, transparent 0%, rgba(0,0,0,0.25) 100%)',
      }} />

      {/* Hex texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        <defs>
          <pattern id="hex10" width="60" height="52" patternUnits="userSpaceOnUse">
            <polygon points="30,1 58,15 58,40 30,52 1,40 1,15" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hex10)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: 200,
        top: 100,
        width: 400,
        height: 400,
        borderRadius: '50%',
        border: '4px solid #FFC107',
        opacity: 0.2,
        transform: `scale(${1 + Math.sin(frame * 0.015) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute',
        right: -100,
        bottom: 100,
        width: 500,
        height: 500,
        backgroundColor: '#000000',
        opacity: 0.05,
        transform: `rotate(${frame * 0.18}deg)`,
      }} />

      {particles.map(i => {
        const r = seededRandom(i + 17);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r * 1920 + frame * 0.5) % 1920,
            top: seededRandom(i + 91) * 1080,
            width: 4,
            height: 4,
            backgroundColor: '#EEEEEE',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>

        {/* Top question */}
        <div style={{
          position: 'absolute',
          top: 110,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: qOp,
          transform: `translateY(${qY}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 32,
            fontWeight: 700,
            color: '#000000',
            opacity: 0.6,
            letterSpacing: 10,
          }}>
            THE REAL QUESTION
          </div>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 110,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -2,
            marginTop: 12,
            lineHeight: 1,
          }}>
            कितना बेचा?
          </div>
        </div>

        {/* Big 32 number */}
        <div style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) translateX(${bigShake}px) scale(${bigScale})`,
        }}>
          <div style={{
            backgroundColor: '#000000',
            padding: '40px 100px',
            border: '8px solid #FFC107',
            position: 'relative',
            boxShadow: '0 0 100px rgba(255,193,7,0.3)',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              fontWeight: 700,
              color: '#FFC107',
              letterSpacing: 8,
              textAlign: 'center',
            }}>
              SAYLOR SOLD
            </div>
            <div style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 360,
              fontWeight: 900,
              color: '#EEEEEE',
              letterSpacing: -10,
              lineHeight: 0.9,
              textAlign: 'center',
              marginTop: 10,
            }}>
              {counterVal.toString().padStart(2, '0')}
            </div>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 50,
              fontWeight: 800,
              color: '#FFC107',
              letterSpacing: 12,
              textAlign: 'center',
              marginTop: 0,
            }}>
              BITCOIN
            </div>
            <div style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 38,
              fontWeight: 700,
              color: '#EEEEEE',
              opacity: 0.7,
              textAlign: 'center',
              marginTop: 6,
            }}>
              बत्तीस / केवल बत्तीस
            </div>
          </div>

          {/* Coins flying out — visual of 32 small BTCs */}
          {frame > coinDelay && Array.from({ length: 32 }).map((_, i) => {
            const r = seededRandom(i + 50);
            const r2 = seededRandom(i + 200);
            const angle = (i / 32) * Math.PI * 2;
            const dist = interpolate(frame, [coinDelay + i * 2, coinDelay + i * 2 + 50], [0, 350 + r * 100], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic)
            });
            const op = interpolate(frame, [coinDelay + i * 2, coinDelay + i * 2 + 50], [1, 0.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <div key={i} style={{
                position: 'absolute',
                left: '50%',
                top: '50%',
                width: 24,
                height: 24,
                borderRadius: '50%',
                backgroundColor: '#FFC107',
                border: '2px solid #000000',
                transform: `translate(-50%, -50%) translate(${Math.cos(angle) * dist}px, ${Math.sin(angle) * dist}px)`,
                opacity: op,
                fontFamily: 'Inter',
                fontSize: 14,
                fontWeight: 900,
                color: '#000000',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
              }}>₿</div>
            );
          })}
        </div>

        {/* Bottom confirm */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: confirmOp,
          transform: `scale(${confirmScale})`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#EEEEEE',
            padding: '18px 50px',
            border: '4px solid #000000',
          }}>
            <span style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 52,
              fontWeight: 800,
              color: '#000000',
              letterSpacing: 1,
            }}>
              हाँ, आपने सही सुना — सिर्फ बत्तीस!
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_ThirtyTwoBTC;
```

