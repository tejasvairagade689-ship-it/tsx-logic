```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_FixedIncomeHook',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_FixedIncomeHook: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  // Headline beats
  const word1 = sp(10);
  const word2 = sp(30);
  const word3 = sp(55);
  const subIn = sp(110);
  const wallet = sp(180);
  const dollarFlow = interpolate(frame, [200, 380], [0, 1], { extrapolateRight: 'clamp' });
  const finalLine = sp(330);

  // Falling dollar amount counter
  const counter = Math.floor(interpolate(frame, [220, 360], [847, 312], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp'
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 20%, rgba(96,153,102,0.18) 0%, rgba(57,62,70,0) 55%), radial-gradient(circle at 75% 80%, rgba(147,181,198,0.15) 0%, rgba(57,62,70,0) 60%)',
        }}
      />

      {/* Shape Layers - drifting geometric forms */}
      <div
        style={{
          position: 'absolute',
          top: '-200px',
          left: '-150px',
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '2px solid #609966',
          opacity: 0.06,
          transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '-180px',
          right: '-120px',
          width: 600,
          height: 600,
          border: '2px solid #93B5C6',
          opacity: 0.07,
          transform: `rotate(${-frame * 0.08}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '40%',
          right: '8%',
          width: 280,
          height: 280,
          background: '#609966',
          opacity: 0.04,
          borderRadius: 24,
          transform: `rotate(${frame * 0.15}deg)`,
        }}
      />

      {/* Motion texture - subtle grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#93B5C6" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid1)" transform={`translate(${frame * 0.3}, 0)`} />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const px = seededRandom(i + 1) * 1920;
        const py = (seededRandom(i + 50) * 1080 + frame * (0.4 + seededRandom(i + 9) * 0.5)) % 1080;
        const ps = 2 + seededRandom(i + 100) * 4;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: ps,
              height: ps,
              borderRadius: '50%',
              background: i % 2 === 0 ? '#609966' : '#93B5C6',
              opacity: 0.35,
            }}
          />
        );
      })}

      {/* Main content area with camera zoom */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center center' }}>
        {/* Top tag line */}
        <div
          style={{
            position: 'absolute',
            top: 110,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: word1,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '14px 36px',
              border: '2px solid #609966',
              borderRadius: 6,
              color: '#609966',
              fontFamily: 'Inter, Poppins, sans-serif',
              fontSize: 32,
              fontWeight: 700,
              letterSpacing: 8,
              transform: `translateY(${(1 - word1) * 40}px)`,
            }}
          >
            FIXED INCOME ALERT
          </div>
        </div>

        {/* Mega headline */}
        <div
          style={{
            position: 'absolute',
            top: 230,
            left: 0,
            right: 0,
            textAlign: 'center',
            fontFamily: 'Inter, Poppins, sans-serif',
            color: '#F5F5F5',
          }}
        >
          <div
            style={{
              fontSize: 178,
              fontWeight: 900,
              lineHeight: 1,
              letterSpacing: -3,
              opacity: word2,
              transform: `translateY(${(1 - word2) * 60}px)`,
            }}
          >
            YOUR <span style={{ color: '#609966' }}>WALLET</span>
          </div>
          <div
            style={{
              fontSize: 178,
              fontWeight: 900,
              lineHeight: 1,
              letterSpacing: -3,
              opacity: word3,
              transform: `translateY(${(1 - word3) * 60}px)`,
              marginTop: 10,
            }}
          >
            IS <span style={{ color: '#ff0000', textDecoration: 'line-through', textDecorationThickness: 10 }}>BLEEDING</span>
          </div>
        </div>

        {/* Visual: Wallet with descending dollar bar chart */}
        <div
          style={{
            position: 'absolute',
            bottom: 200,
            left: '50%',
            transform: `translateX(-50%) scale(${wallet})`,
            display: 'flex',
            alignItems: 'flex-end',
            gap: 18,
            opacity: wallet,
          }}
        >
          {[1, 0.85, 0.72, 0.58, 0.45, 0.33, 0.22].map((h, i) => {
            const barH = 220 * h * Math.min(1, dollarFlow * 1.5);
            return (
              <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 10 }}>
                <div
                  style={{
                    width: 60,
                    height: barH,
                    background: i < 3 ? '#609966' : i < 5 ? '#FFC107' : '#ff0000',
                    borderRadius: '4px 4px 0 0',
                    transition: 'background 0.3s',
                  }}
                />
                <div
                  style={{
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: 22,
                    color: '#93B5C6',
                    fontWeight: 600,
                  }}
                >
                  {['JAN', 'FEB', 'MAR', 'APR', 'MAY', 'JUN', 'JUL'][i]}
                </div>
              </div>
            );
          })}
        </div>

        {/* Live counter */}
        <div
          style={{
            position: 'absolute',
            bottom: 200,
            right: 140,
            opacity: dollarFlow,
            textAlign: 'right',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 24,
              color: '#93B5C6',
              letterSpacing: 4,
              fontWeight: 600,
            }}
          >
            BANK BALANCE
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 92,
              color: '#ff0000',
              fontWeight: 900,
              letterSpacing: -2,
            }}
          >
            ${counter}
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 26,
              color: '#FFC107',
              fontWeight: 700,
              marginTop: -8,
            }}
          >
            ▼ DROPPING FAST
          </div>
        </div>

        {/* Bottom CTA line */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: finalLine,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins, sans-serif',
              fontSize: 48,
              fontWeight: 700,
              color: '#F5F5F5',
              letterSpacing: 1,
              transform: `translateY(${(1 - finalLine) * 30}px)`,
            }}
          >
            Stay with me — <span style={{ color: '#609966' }}>hundreds back</span> in your pocket
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 2,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_FixedIncomeHook;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_RealMoneyVsCoupons',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_RealMoneyVsCoupons: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Split screen reveal: LEFT = tiny coupon | RIGHT = big real money
  const leftSlide = sp(15);
  const rightSlide = sp(60);
  const vsPop = sp(110);
  const fiveProgramsIn = sp(170);
  const cardsStagger = (i: number) => sp(200 + i * 18);

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [-15, 15]);

  const programs = [
    { label: 'GROCERIES', icon: 'cart' },
    { label: 'RX MEDS', icon: 'pill' },
    { label: 'UTILITIES', icon: 'bolt' },
    { label: 'MEDICAL', icon: 'cross' },
    { label: 'HOME REPAIR', icon: 'home' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Diagonal gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(96,153,102,0.12) 0%, rgba(57,62,70,0) 40%, rgba(147,181,198,0.12) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '20%',
          left: '5%',
          width: 380,
          height: 380,
          border: '3px solid #93B5C6',
          opacity: 0.05,
          transform: `rotate(${frame * 0.12}deg)`,
          borderRadius: 30,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '10%',
          right: '8%',
          width: 0,
          height: 0,
          borderLeft: '180px solid transparent',
          borderRight: '180px solid transparent',
          borderBottom: '320px solid #609966',
          opacity: 0.05,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        {Array.from({ length: 25 }).map((_, i) => (
          <line
            key={i}
            x1={-200 + i * 100}
            y1={0}
            x2={i * 100}
            y2={1080}
            stroke="#93B5C6"
            strokeWidth="1"
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = (seededRandom(i + 2) * 1920 + frame * 0.4) % 1920;
        const py = seededRandom(i + 70) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 3,
              height: 3,
              background: '#609966',
              opacity: 0.4,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            fontFamily: 'Inter, sans-serif',
            fontSize: 36,
            fontWeight: 700,
            color: '#93B5C6',
            letterSpacing: 12,
            opacity: sp(5),
          }}
        >
          THIS — NOT THAT
        </div>

        {/* SPLIT: LEFT = Coupon (small/dim) */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: 100,
            width: 700,
            opacity: leftSlide * 0.55,
            transform: `translateX(${(1 - leftSlide) * -100}px) scale(0.85)`,
          }}
        >
          <div
            style={{
              padding: 40,
              background: 'rgba(245,245,245,0.06)',
              border: '2px dashed #9E9E9E',
              borderRadius: 16,
              filter: 'grayscale(60%)',
            }}
          >
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 28, color: '#9E9E9E', letterSpacing: 4, fontWeight: 600 }}>
              OLD WAY
            </div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 96, color: '#9E9E9E', fontWeight: 900, marginTop: 10, textDecoration: 'line-through', textDecorationThickness: 6 }}>
              50¢ OFF
            </div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 38, color: '#9E9E9E', fontWeight: 600, marginTop: 6 }}>
              clipping coupons
            </div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 30, color: '#9E9E9E', fontWeight: 500, marginTop: 20 }}>
              one can of soup
            </div>
          </div>
        </div>

        {/* VS badge */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: '50%',
            transform: `translateX(-50%) scale(${vsPop}) rotate(${vsPop * 360}deg)`,
            width: 130,
            height: 130,
            borderRadius: '50%',
            background: '#ff0000',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontFamily: 'Inter, sans-serif',
            fontSize: 54,
            fontWeight: 900,
            color: '#F5F5F5',
            boxShadow: '0 10px 40px rgba(255,0,0,0.5)',
            zIndex: 5,
          }}
        >
          VS
        </div>

        {/* RIGHT = Real money (big/bright) */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            right: 100,
            width: 700,
            opacity: rightSlide,
            transform: `translateX(${(1 - rightSlide) * 100}px)`,
          }}
        >
          <div
            style={{
              padding: 40,
              background: 'rgba(96,153,102,0.15)',
              border: '4px solid #609966',
              borderRadius: 16,
              boxShadow: '0 20px 60px rgba(96,153,102,0.25)',
            }}
          >
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 32, color: '#609966', letterSpacing: 4, fontWeight: 700 }}>
              REAL MONEY
            </div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 150, color: '#F5F5F5', fontWeight: 900, marginTop: 10, letterSpacing: -3, lineHeight: 1 }}>
              $100s
            </div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 42, color: '#93B5C6', fontWeight: 600, marginTop: 14 }}>
              every single month
            </div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 32, color: '#FFC107', fontWeight: 700, marginTop: 20 }}>
              ★ government-funded
            </div>
          </div>
        </div>

        {/* 5 PROGRAMS strip */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: 0,
            right: 0,
            opacity: fiveProgramsIn,
          }}
        >
          <div
            style={{
              textAlign: 'center',
              fontFamily: 'Inter, sans-serif',
              fontSize: 44,
              fontWeight: 800,
              color: '#F5F5F5',
              marginBottom: 22,
              letterSpacing: 2,
            }}
          >
            <span style={{ color: '#609966', fontSize: 64 }}>5</span> FREE GOVERNMENT PROGRAMS
          </div>
          <div
            style={{
              display: 'flex',
              justifyContent: 'center',
              gap: 22,
            }}
          >
            {programs.map((p, i) => {
              const cs = cardsStagger(i);
              return (
                <div
                  key={i}
                  style={{
                    width: 220,
                    padding: '22px 12px',
                    background: '#2D2D2D',
                    border: '2px solid #609966',
                    borderRadius: 12,
                    textAlign: 'center',
                    opacity: cs,
                    transform: `translateY(${(1 - cs) * 40}px) scale(${0.85 + cs * 0.15})`,
                  }}
                >
                  <svg width="48" height="48" viewBox="0 0 48 48" style={{ marginBottom: 10 }}>
                    {p.icon === 'cart' && (
                      <g fill="none" stroke="#93B5C6" strokeWidth="2.5" strokeLinecap="round">
                        <path d="M6 8 L12 8 L16 30 L38 30 L42 14 L14 14" />
                        <circle cx="18" cy="38" r="3" fill="#93B5C6" />
                        <circle cx="36" cy="38" r="3" fill="#93B5C6" />
                      </g>
                    )}
                    {p.icon === 'pill' && (
                      <g fill="none" stroke="#93B5C6" strokeWidth="2.5">
                        <rect x="8" y="18" width="32" height="14" rx="7" />
                        <line x1="24" y1="18" x2="24" y2="32" />
                      </g>
                    )}
                    {p.icon === 'bolt' && (
                      <path d="M26 6 L12 28 L22 28 L20 42 L36 18 L26 18 Z" fill="#FFC107" stroke="#FFC107" strokeWidth="1.5" strokeLinejoin="round" />
                    )}
                    {p.icon === 'cross' && (
                      <g fill="#ff0000">
                        <rect x="20" y="8" width="8" height="32" rx="2" />
                        <rect x="8" y="20" width="32" height="8" rx="2" />
                      </g>
                    )}
                    {p.icon === 'home' && (
                      <g fill="none" stroke="#609966" strokeWidth="2.5" strokeLinejoin="round">
                        <path d="M8 24 L24 10 L40 24 L40 40 L8 40 Z" />
                        <rect x="20" y="28" width="8" height="12" fill="#609966" />
                      </g>
                    )}
                  </svg>
                  <div
                    style={{
                      fontFamily: 'Inter, sans-serif',
                      fontSize: 22,
                      fontWeight: 800,
                      color: '#F5F5F5',
                      letterSpacing: 1.5,
                    }}
                  >
                    {p.label}
                  </div>
                </div>
              );
            })}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 2,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_RealMoneyVsCoupons;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_UnclaimedBillions',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_UnclaimedBillions: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(10);
  const vaultIn = sp(50);
  const billionsCount = Math.floor(interpolate(frame, [80, 220], [0, 47], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp'
  }));
  const billionsLabel = sp(80);
  const letterIn = sp(220);
  const letterCross = sp(280);
  const askLineIn = sp(330);

  // Vault door rotation
  const vaultOpen = interpolate(frame, [60, 180], [0, 35], { extrapolateRight: 'clamp' });
  const glow = 0.5 + Math.sin(frame * 0.08) * 0.3;

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 40%, rgba(96,153,102,0.2) 0%, rgba(57,62,70,0) 50%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '15%',
          left: '10%',
          width: 500,
          height: 500,
          border: '2px solid #609966',
          opacity: 0.05,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.04})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '10%',
          right: '12%',
          width: 320,
          height: 320,
          border: '2px solid #93B5C6',
          opacity: 0.06,
          borderRadius: 20,
          transform: `rotate(${frame * 0.18}deg)`,
        }}
      />

      {/* Concentric circles texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {[120, 220, 320, 420, 520].map((r, i) => (
          <circle key={i} cx="960" cy="540" r={r + (frame * 0.2) % 100} fill="none" stroke="#93B5C6" strokeWidth="1" />
        ))}
      </svg>

      {/* Money particles falling */}
      {Array.from({ length: 30 }).map((_, i) => {
        const px = seededRandom(i + 3) * 1920;
        const py = (seededRandom(i + 80) * 600 + frame * (1 + seededRandom(i + 12) * 1.5)) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              fontFamily: 'Inter, sans-serif',
              fontSize: 16 + seededRandom(i + 33) * 14,
              color: '#609966',
              opacity: 0.3,
              fontWeight: 700,
              transform: `rotate(${frame * 2 + i * 30}deg)`,
            }}
          >
            $
          </div>
        );
      })}

      {/* Title */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleIn,
          transform: `translateY(${(1 - titleIn) * 30}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 32,
            color: '#93B5C6',
            letterSpacing: 8,
            fontWeight: 700,
            marginBottom: 10,
          }}
        >
          WHAT NOBODY TELLS YOU
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 78,
            color: '#F5F5F5',
            fontWeight: 900,
            letterSpacing: -1,
            lineHeight: 1.05,
          }}
        >
          The Government Is Sitting On
        </div>
      </div>

      {/* MASSIVE BILLIONS counter center */}
      <div
        style={{
          position: 'absolute',
          top: 290,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: billionsLabel,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 280,
            fontWeight: 900,
            color: '#609966',
            lineHeight: 1,
            letterSpacing: -8,
            textShadow: `0 0 ${40 * glow}px rgba(96,153,102,${glow})`,
          }}
        >
          ${billionsCount}B
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 48,
            color: '#F5F5F5',
            fontWeight: 700,
            letterSpacing: 4,
            marginTop: -10,
          }}>
          IN UNCLAIMED SENIOR BENEFITS
        </div>
      </div>

      {/* Bottom split: NO LETTER + YOU MUST ASK */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          gap: 60,
          alignItems: 'center',
        }}
      >
        {/* Letter with X */}
        <div
          style={{
            position: 'relative',
            opacity: letterIn,
            transform: `translateX(${(1 - letterIn) * -50}px)`,
          }}
        >
          <svg width="260" height="180" viewBox="0 0 260 180">
            <rect x="10" y="20" width="240" height="140" rx="6" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="3" />
            <path d="M10 20 L130 110 L250 20" fill="none" stroke="#9E9E9E" strokeWidth="3" />
            <text x="130" y="155" textAnchor="middle" fontFamily="Inter" fontSize="20" fill="#9E9E9E" fontWeight="700">NO LETTER COMING</text>
          </svg>
          {/* Big red X */}
          <svg
            width="260"
            height="180"
            viewBox="0 0 260 180"
            style={{
              position: 'absolute',
              top: 0,
              left: 0,
              opacity: letterCross,
              transform: `scale(${letterCross})`,
            }}
          >
            <line x1="30" y1="30" x2="230" y2="150" stroke="#ff0000" strokeWidth="14" strokeLinecap="round" />
            <line x1="230" y1="30" x2="30" y2="150" stroke="#ff0000" strokeWidth="14" strokeLinecap="round" />
          </svg>
        </div>

        {/* Arrow */}
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 80,
            color: '#FFC107',
            opacity: askLineIn,
            transform: `translateX(${Math.sin(frame * 0.15) * 8}px)`,
          }}
        >
          →
        </div>

        {/* YOU MUST ASK */}
        <div
          style={{
            opacity: askLineIn,
            transform: `translateX(${(1 - askLineIn) * 50}px) scale(${0.9 + askLineIn * 0.1})`,
            padding: '30px 50px',
            background: '#FFC107',
            borderRadius: 12,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              color: '#1C1C1C',
              fontWeight: 700,
              letterSpacing: 4,
            }}
          >
            THE RULE
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 72,
              color: '#1C1C1C',
              fontWeight: 900,
              letterSpacing: -1,
              lineHeight: 1,
            }}
          >
            YOU MUST ASK
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 2,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_UnclaimedBillions;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_NotCharity',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_NotCharity: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const word1 = sp(10);
  const strikeIn = sp(45);
  const replaceIn = sp(80);
  const taxTimelineIn = sp(140);
  const timelineProgress = interpolate(frame, [160, 320], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const roiBadge = sp(310);

  const years = [1985, 1995, 2005, 2015, 2025];

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(96,153,102,0.1) 0%, rgba(57,62,70,0) 50%, rgba(255,193,7,0.08) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '5%',
          right: '5%',
          width: 450,
          height: 450,
          background: '#609966',
          opacity: 0.04,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '8%',
          left: '6%',
          width: 380,
          height: 380,
          border: '3px solid #93B5C6',
          opacity: 0.06,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Dotted grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="dots4" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#93B5C6" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots4)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 4) * 1920;
        const py = (1080 - (seededRandom(i + 90) * 1080 + frame * 0.6) % 1080);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 4,
              height: 4,
              background: '#609966',
              opacity: 0.35,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* TOP: NOT CHARITY → THIS IS YOURS */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          left: 0,
          right: 0,
          textAlign: 'center',
        }}
      >
        <div style={{ position: 'relative', display: 'inline-block' }}>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 140,
              fontWeight: 900,
              color: '#9E9E9E',
              letterSpacing: -2,
              lineHeight: 1,
              opacity: word1,
              transform: `scale(${word1})`,
            }}
          >
            CHARITY
          </div>
          {/* Strike-through */}
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              right: 0,
              height: 10,
              background: '#ff0000',
              transformOrigin: 'left center',
              transform: `scaleX(${strikeIn}) translateY(-50%)`,
              borderRadius: 5,
            }}
          />
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 140,
            fontWeight: 900,
            color: '#609966',
            letterSpacing: -2,
            lineHeight: 1,
            marginTop: 10,
            opacity: replaceIn,
            transform: `translateY(${(1 - replaceIn) * 40}px)`,
          }}
        >
          IT'S <span style={{ color: '#FFC107' }}>YOURS.</span>
        </div>
      </div>

      {/* MIDDLE: Tax timeline */}
      <div
        style={{
          position: 'absolute',
          top: 540,
          left: 120,
          right: 120,
          opacity: taxTimelineIn,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            color: '#93B5C6',
            letterSpacing: 4,
            fontWeight: 600,
            textAlign: 'center',
            marginBottom: 18,
          }}
        >
          YOU PAID TAXES — 40 YEARS
        </div>

        {/* Timeline bar */}
        <div
          style={{
            position: 'relative',
            height: 50,
            background: '#2D2D2D',
            borderRadius: 25,
            border: '2px solid #93B5C6',
            overflow: 'hidden',
          }}
        >
          <div
            style={{
              position: 'absolute',
              top: 0,
              left: 0,
              bottom: 0,
              width: `${timelineProgress * 100}%`,
              background: 'linear-gradient(90deg, #609966 0%, #FFC107 100%)',
              borderRadius: 25,
            }}
          />
          {/* Year markers */}
          {years.map((y, i) => (
            <div
              key={i}
              style={{
                position: 'absolute',
                top: -42,
                left: `${(i / (years.length - 1)) * 100}%`,
                transform: 'translateX(-50%)',
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22,
                color: '#F5F5F5',
                fontWeight: 700,
              }}
            >
              {y}
            </div>
          ))}
          {/* Year tick dots */}
          {years.map((_, i) => (
            <div
              key={`d${i}`}
              style={{
                position: 'absolute',
                top: '50%',
                left: `${(i / (years.length - 1)) * 100}%`,
                transform: 'translate(-50%, -50%)',
                width: 14,
                height: 14,
                borderRadius: '50%',
                background: '#F5F5F5',
                border: '3px solid #2D2D2D',
              }}
            />
          ))}
        </div>

        {/* Sub line */}
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 38,
            color: '#F5F5F5',
            fontWeight: 700,
            textAlign: 'center',
            marginTop: 35,
          }}
        >
          This is <span style={{ color: '#FFC107' }}>RETURN ON YOUR INVESTMENT</span>
        </div>
      </div>

      {/* ROI badge corner */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          right: 100,
          opacity: roiBadge,
          transform: `scale(${roiBadge}) rotate(${(1 - roiBadge) * -20}deg)`,
        }}
      >
        <div
          style={{
            padding: '20px 36px',
            background: '#609966',
            borderRadius: 12,
            boxShadow: '0 14px 40px rgba(96,153,102,0.4)',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 26,
              color: '#1C1C1C',
              fontWeight: 800,
              letterSpacing: 3,
            }}
          >
            2026 LIMITS
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 32,
              color: '#F5F5F5',
              fontWeight: 800,
              marginTop: 4,
            }}
          >
            VERIFIED ✓
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 2,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_NotCharity;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_FiveProgramsTeaser',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_FiveProgramsTeaser: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 5 numbered cards stagger in
  const card = (i: number) => sp(20 + i * 18);
  // Highlight #5 (last)
  const highlight5 = sp(180);
  const teaser = sp(260);

  const items = [
    { num: '01', label: 'SNAP' },
    { num: '02', label: 'EXTRA HELP' },
    { num: '03', label: 'QMB' },
    { num: '04', label: 'LIHEAP' },
    { num: '05', label: 'HOME REPAIR' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 80% 50%, rgba(255,193,7,0.12) 0%, rgba(57,62,70,0) 50%), linear-gradient(180deg, rgba(96,153,102,0.08) 0%, rgba(57,62,70,0) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: '8%',
          left: '4%',
          width: 280,
          height: 280,
          border: '2px solid #609966',
          opacity: 0.06,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.05) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '5%',
          right: '6%',
          width: 0,
          height: 0,
          borderLeft: '160px solid transparent',
          borderRight: '160px solid transparent',
          borderBottom: '280px solid #93B5C6',
          opacity: 0.05,
          transform: `rotate(${frame * 0.08}deg)`,
        }}
      />

      {/* Horizontal line texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        {Array.from({ length: 15 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 80} x2="1920" y2={i * 80} stroke="#93B5C6" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = (seededRandom(i + 5) * 1920 + frame * 0.5) % 1920;
        const py = seededRandom(i + 110) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 3,
              height: 3,
              background: i % 3 === 0 ? '#FFC107' : '#609966',
              opacity: 0.4,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* Top label */}
      <div
        style={{
          position: 'absolute',
          top: 90,
          left: 0,
          right: 0,
          textAlign: 'center',
          fontFamily: 'Inter, sans-serif',
          fontSize: 34,
          color: '#93B5C6',
          letterSpacing: 10,
          fontWeight: 700,
          opacity: sp(5),
        }}
      >
        MILLIONS ARE IGNORING THESE
      </div>

      {/* 5 numbered cards row */}
      <div
        style={{
          position: 'absolute',
          top: 250,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          gap: 26,
        }}
      >
        {items.map((it, i) => {
          const c = card(i);
          const isFive = i === 4;
          const fivePulse = isFive ? 1 + Math.sin(frame * 0.12) * 0.04 : 1;
          return (
            <div
              key={i}
              style={{
                width: 280,
                height: 380,
                background: isFive ? '#FFC107' : '#2D2D2D',
                border: `4px solid ${isFive ? '#FFC107' : '#609966'}`,
                borderRadius: 16,
                opacity: c,
                transform: `translateY(${(1 - c) * 80}px) scale(${(0.85 + c * 0.15) * (isFive ? fivePulse : 1)})`,
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
                padding: 20,
                boxShadow: isFive ? `0 0 ${30 + Math.sin(frame * 0.15) * 20}px rgba(255,193,7,0.5)` : '0 10px 30px rgba(0,0,0,0.3)',
              }}
            >
              <div
                style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 130,
                  fontWeight: 900,
                  color: isFive ? '#1C1C1C' : '#609966',
                  lineHeight: 1,
                  letterSpacing: -4,
                }}
              >
                {it.num}
              </div>
              <div
                style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 30,
                  fontWeight: 800,
                  color: isFive ? '#1C1C1C' : '#F5F5F5',
                  letterSpacing: 2,
                  marginTop: 16,
                  textAlign: 'center',
                }}
              >
                {it.label}
              </div>
              {isFive && (
                <div
                  style={{
                    marginTop: 20,
                    padding: '8px 18px',
                    background: '#ff0000',
                    borderRadius: 6,
                    fontFamily: 'Inter, sans-serif',
                    fontSize: 18,
                    fontWeight: 800,
                    color: '#F5F5F5',
                    letterSpacing: 3,
                    opacity: highlight5,
                  }}
                >
                  ★ MASSIVE GRANT
                </div>
              )}
            </div>
          );
        })}
      </div>

      {/* Bottom teaser */}
      <div
        style={{
          position: 'absolute',
          bottom: 110,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: teaser,
          transform: `translateY(${(1 - teaser) * 30}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 64,
            color: '#F5F5F5',
            fontWeight: 900,
            letterSpacing: -1,
          }}
        >
          STAY TIL THE END FOR <span style={{ color: '#FFC107' }}>#5</span>
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 34,
            color: '#93B5C6',
            fontWeight: 600,
            marginTop: 10,
          }}
        >
          A massive home repair grant
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 2,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_FiveProgramsTeaser;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_SubscribeCTA',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_SubscribeCTA: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const roofIn = sp(10);
  const zeroIn = sp(70);
  const zeroPulse = 1 + Math.sin(frame * 0.12) * 0.06;
  const subBtn = sp(160);
  const bellIn = sp(220);
  const bellShake = Math.sin(frame * 0.4) * 8;
  const rulesIn = sp(290);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 30%, rgba(96,153,102,0.15) 0%, rgba(57,62,70,0) 50%), radial-gradient(circle at 70% 70%, rgba(255,0,0,0.12) 0%, rgba(57,62,70,0) 50%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: '10%',
          right: '8%',
          width: 500,
          height: 500,
          borderRadius: '50%',
          border: '3px solid #93B5C6',
          opacity: 0.05,
          transform: `rotate(${frame * 0.08}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '12%',
          left: '5%',
          width: 320,
          height: 320,
          background: '#609966',
          opacity: 0.05,
          borderRadius: 20,
          transform: `rotate(${-frame * 0.1}deg)`,
        }}
      />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="grid6" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#93B5C6" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid6)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const px = seededRandom(i + 6) * 1920;
        const py = (seededRandom(i + 130) * 1080 + frame * 0.7) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 3,
              height: 3,
              background: '#609966',
              opacity: 0.4,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* LEFT side: Leaking roof + plumbing visual */}
      <div
        style={{
          position: 'absolute',
          top: 200,
          left: 120,
          width: 700,
          opacity: roofIn,
          transform: `translateX(${(1 - roofIn) * -60}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 32,
            color: '#ff0000',
            letterSpacing: 6,
            fontWeight: 700,
            marginBottom: 18,
          }}
        >
          THE PROBLEM
        </div>

        {/* House SVG */}
        <svg width="500" height="380" viewBox="0 0 500 380">
          {/* House */}
          <path d="M250 60 L60 200 L60 360 L440 360 L440 200 Z" fill="#2D2D2D" stroke="#93B5C6" strokeWidth="4" />
          {/* Roof (red broken) */}
          <path d="M250 60 L60 200 L440 200 Z" fill="#ff0000" opacity="0.7" stroke="#ff0000" strokeWidth="4" />
          {/* Crack on roof */}
          <path d="M250 60 L200 130 L260 170 L220 200" stroke="#1C1C1C" strokeWidth="5" fill="none" strokeLinecap="round" />
          {/* Door */}
          <rect x="220" y="260" width="60" height="100" fill="#609966" />
          {/* Windows */}
          <rect x="100" y="230" width="70" height="70" fill="#93B5C6" opacity="0.5" />
          <rect x="330" y="230" width="70" height="70" fill="#93B5C6" opacity="0.5" />

          {/* Water drops animated */}
          {[0, 1, 2].map((d) => {
            const dropY = ((frame * 3 + d * 30) % 80) + 180;
            return (
              <circle key={d} cx={150 + d * 100} cy={dropY} r="6" fill="#93B5C6" opacity={1 - (dropY - 180) / 80} />
            );
          })}
        </svg>

        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 38,
            color: '#F5F5F5',
            fontWeight: 700,
            marginTop: 10,
          }}
        >
          Leaking roof. Broken pipes.
        </div>
      </div>

      {/* RIGHT side: ZERO DOLLARS */}
      <div
        style={{
          position: 'absolute',
          top: 200,
          right: 120,
          width: 700,
          textAlign: 'center',
          opacity: zeroIn,
          transform: `scale(${zeroIn * zeroPulse})`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 32,
            color: '#609966',
            letterSpacing: 6,
            fontWeight: 700,
            marginBottom: 14,
          }}
        >
          YOUR COST
        </div>
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 360,
            color: '#609966',
            fontWeight: 900,
            lineHeight: 0.9,
            letterSpacing: -10,
            textShadow: `0 0 ${30 + Math.sin(frame * 0.15) * 20}px rgba(96,153,102,0.5)`,
          }}
        >
          $0
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 38,
            color: '#FFC107',
            fontWeight: 800,
            marginTop: 6,
            letterSpacing: 2,
          }}
        >
          ZERO DOLLARS
        </div>
      </div>

      {/* BOTTOM BAR: Subscribe CTA */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 30,
        }}
      >
        {/* Subscribe button */}
        <div
          style={{
            padding: '24px 50px',
            background: '#ff0000',
            borderRadius: 50,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            opacity: subBtn,
            transform: `scale(${subBtn * (1 + Math.sin(frame * 0.1) * 0.03)})`,
            boxShadow: '0 14px 40px rgba(255,0,0,0.5)',
          }}
        >
          <svg width="42" height="32" viewBox="0 0 42 32">
            <rect x="2" y="2" width="38" height="28" rx="8" fill="#F5F5F5" />
            <polygon points="16,10 16,22 28,16" fill="#ff0000" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 42,
              color: '#F5F5F5',
              fontWeight: 900,
              letterSpacing: 2,
            }}
          >
            SUBSCRIBE NOW
          </div>
        </div>

        {/* Bell */}
        <div
          style={{
            opacity: bellIn,
            transform: `scale(${bellIn}) rotate(${bellShake}deg)`,
          }}
        >
          <svg width="80" height="80" viewBox="0 0 80 80">
            <path d="M40 10 C28 10 20 20 20 35 L20 50 L15 58 L65 58 L60 50 L60 35 C60 20 52 10 40 10 Z" fill="#FFC107" stroke="#1C1C1C" strokeWidth="3" />
            <circle cx="40" cy="68" r="6" fill="#FFC107" stroke="#1C1C1C" strokeWidth="3" />
          </svg>
        </div>
      </div>

      {/* Top right: Rules change tag */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: rulesIn,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            padding: '12px 30px',
            background: '#2D2D2D',
            border: '2px solid #FFC107',
            borderRadius: 8,
            fontFamily: 'Inter, sans-serif',
            fontSize: 28,
            color: '#FFC107',
            fontWeight: 700,
            letterSpacing: 4,
          }}
        >
          RULES CHANGE EVERY YEAR
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 2,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_SubscribeCTA;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_PlainEnglishAdvantage',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_PlainEnglishAdvantage: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const head = sp(8);
  const limitsRow = sp(50);
  // Income limit animated counter
  const limitVal = Math.floor(interpolate(frame, [60, 200], [18500, 22950], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp'
  }));
  const newProgramTag = sp(160);
  const arrowIn = sp(220);
  const winnerIn = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(96,153,102,0.1) 0%, rgba(57,62,70,0) 60%, rgba(147,181,198,0.12) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: '15%',
          left: '8%',
          width: 280,
          height: 280,
          border: '2px solid #609966',
          opacity: 0.06,
          transform: `rotate(${frame * 0.12}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '15%',
          right: '6%',
          width: 360,
          height: 360,
          borderRadius: '50%',
          background: '#93B5C6',
          opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />

      {/* Vertical line texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <line key={i} x1={i * 70} y1="0" x2={i * 70} y2="1080" stroke="#93B5C6" strokeWidth="0.8" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = (seededRandom(i + 7) * 1920 + frame * 0.6) % 1920;
        const py = seededRandom(i + 150) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 3,
              height: 3,
              background: '#609966',
              opacity: 0.4,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* Headline */}
      <div
        style={{
          position: 'absolute',
          top: 90,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: head,
          transform: `translateY(${(1 - head) * 30}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            color: '#93B5C6',
            letterSpacing: 8,
            fontWeight: 700,
          }}
        >
          THE RULES KEEP MOVING
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 110,
            color: '#F5F5F5',
            fontWeight: 900,
            letterSpacing: -2,
            marginTop: 8,
            lineHeight: 1,
          }}
        >
          PLAIN ENGLISH. <span style={{ color: '#609966' }}>NO JARGON.</span>
        </div>
      </div>

      {/* Income limit panel */}
      <div
        style={{
          position: 'absolute',
          top: 360,
          left: 120,
          width: 760,
          padding: 36,
          background: '#2D2D2D',
          borderRadius: 16,
          border: '2px solid #93B5C6',
          opacity: limitsRow,
          transform: `translateX(${(1 - limitsRow) * -50}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#93B5C6',
            letterSpacing: 3,
            fontWeight: 700,
          }}
        >
          INCOME LIMIT — 2026
        </div>
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 130,
            color: '#609966',
            fontWeight: 900,
            lineHeight: 1.1,
            letterSpacing: -3,
            marginTop: 6,
          }}
        >
          ${limitVal.toLocaleString()}
        </div>
        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: 12,
            marginTop: 10,
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            color: '#FFC107',
            fontWeight: 700,
          }}
        >
          <span>▲ shifted up from 2025</span>
        </div>
      </div>

      {/* New programs tag */}
      <div
        style={{
          position: 'absolute',
          top: 360,
          right: 120,
          width: 760,
          padding: 36,
          background: '#2D2D2D',
          borderRadius: 16,
          border: '2px solid #609966',
          opacity: newProgramTag,
          transform: `translateX(${(1 - newProgramTag) * 50}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#609966',
            letterSpacing: 3,
            fontWeight: 700,
          }}
        >
          NEW PROGRAMS — OPEN
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 90,
            color: '#F5F5F5',
            fontWeight: 900,
            lineHeight: 1.1,
            letterSpacing: -2,
            marginTop: 6,
          }}
        >
          + STATE GRANTS
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            color: '#93B5C6',
            fontWeight: 600,
            marginTop: 10,
          }}
        >
          decoded for you ✓
        </div>
      </div>

      {/* Center arrow + winner badge */}
      <div
        style={{
          position: 'absolute',
          bottom: 120,
          left: 0,
          right: 0,
          textAlign: 'center',
        }}
      >
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 42,
              color: '#F5F5F5',
              fontWeight: 700,
              opacity: arrowIn,
            }}
          >
            Subscribers
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 60,
              color: '#FFC107',
              fontWeight: 900,
              opacity: arrowIn,
              transform: `translateX(${Math.sin(frame * 0.15) * 6}px)`,
            }}
          >
            →
          </div>
          <div
            style={{
              padding: '20px 40px',
              background: '#609966',
              borderRadius: 12,
              fontFamily: 'Inter, sans-serif',
              fontSize: 48,
              color: '#1C1C1C',
              fontWeight: 900,
              letterSpacing: 2,
              opacity: winnerIn,
              transform: `scale(${winnerIn})`,
              boxShadow: '0 14px 40px rgba(96,153,102,0.4)',
            }}
          >
            STAY AHEAD
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 2,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_PlainEnglishAdvantage;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_COLAIntro',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_COLAIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(10);
  const squeezeIn = sp(60);
  // Squeeze animation (pinch the wallet)
  const squeezePulse = 1 - Math.abs(Math.sin(frame * 0.05)) * 0.08;
  const colaBoxIn = sp(160);
  const ssCheckIn = sp(230);
  const arrowFlow = interpolate(frame, [260, 360], [0, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 30%, rgba(255,193,7,0.1) 0%, rgba(57,62,70,0) 50%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: '20%',
          right: '5%',
          width: 400,
          height: 400,
          border: '3px solid #609966',
          opacity: 0.05,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '8%',
          left: '5%',
          width: 320,
          height: 320,
          background: '#93B5C6',
          opacity: 0.04,
          borderRadius: 20,
          transform: `rotate(${frame * 0.12}deg)`,
        }}
      />

      {/* Wave texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <path
            key={i}
            d={`M0 ${200 + i * 100} Q 480 ${200 + i * 100 + Math.sin(frame * 0.04 + i) * 30} 960 ${200 + i * 100} T 1920 ${200 + i * 100}`}
            stroke="#93B5C6"
            strokeWidth="1"
            fill="none"
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 8) * 1920;
        const py = (seededRandom(i + 170) * 1080 + frame * 0.6) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 3,
              height: 3,
              background: '#609966',
              opacity: 0.35,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* Title */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: title,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            color: '#93B5C6',
            letterSpacing: 10,
            fontWeight: 700,
          }}
        >
          THE FIXED INCOME
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 200,
            color: '#FFC107',
            fontWeight: 900,
            letterSpacing: -4,
            lineHeight: 1,
            marginTop: 4,
          }}
        >
          SQUEEZE
        </div>
      </div>

      {/* Center squeeze visual: two hands compressing wallet */}
      <div
        style={{
          position: 'absolute',
          top: 380,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 0,
          opacity: squeezeIn,
        }}
      >
        {/* Left arrow */}
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 140,
            color: '#ff0000',
            fontWeight: 900,
            transform: `translateX(${Math.sin(frame * 0.1) * 14}px)`,
          }}
        >
          ▶
        </div>
        {/* Wallet getting squeezed */}
        <div
          style={{
            transform: `scaleX(${squeezePulse})`,
            margin: '0 -20px',
          }}
        >
          <svg width="320" height="220" viewBox="0 0 320 220">
            <rect x="20" y="40" width="280" height="160" rx="14" fill="#609966" stroke="#1C1C1C" strokeWidth="4" />
            <rect x="20" y="80" width="280" height="30" fill="#1C1C1C" opacity="0.4" />
            <circle cx="240" cy="130" r="22" fill="#FFC107" stroke="#1C1C1C" strokeWidth="3" />
            <text x="240" y="140" textAnchor="middle" fontSize="28" fill="#1C1C1C" fontWeight="900" fontFamily="Inter">$</text>
            <text x="40" y="180" fontSize="22" fill="#F5F5F5" fontWeight="700" fontFamily="Inter">FIXED</text>
          </svg>
        </div>
        {/* Right arrow */}
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 140,
            color: '#ff0000',
            fontWeight: 900,
            transform: `translateX(${-Math.sin(frame * 0.1) * 14}px)`,
          }}
        >
          ◀
        </div>
      </div>

      {/* Bottom: COLA → SS Check flow */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 40,
        }}
      >
        {/* COLA box */}
        <div
          style={{
            padding: '24px 40px',
            background: '#2D2D2D',
            border: '3px solid #609966',
            borderRadius: 12,
            opacity: colaBoxIn,
            transform: `scale(${colaBoxIn})`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 26,
              color: '#609966',
              fontWeight: 700,
              letterSpacing: 3,
            }}
          >
            C.O.L.A
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              color: '#F5F5F5',
              fontWeight: 700,
              marginTop: 4,
            }}
          >
            Cost of Living Adjustment
          </div>
        </div>

        {/* Animated arrow */}
        <div style={{ position: 'relative', width: 180, height: 60 }}>
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              height: 4,
              width: 180 * arrowFlow,
              background: '#FFC107',
              transform: 'translateY(-50%)',
              borderRadius: 2,
            }}
          />
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: 180 * arrowFlow - 10,
              transform: 'translateY(-50%)',
              fontSize: 50,
              color: '#FFC107',
              opacity: arrowFlow,
              fontWeight: 900,
              lineHeight: 0.5,
            }}
          >
            ▶
          </div>
        </div>

        {/* SS Check */}
        <div
          style={{
            padding: '20px 36px',
            background: '#FFC107',
            borderRadius: 12,
            opacity: ssCheckIn,
            transform: `scale(${ssCheckIn})`,
            boxShadow: '0 14px 40px rgba(255,193,7,0.4)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 22,
              color: '#1C1C1C',
              fontWeight: 700,
              letterSpacing: 3,
            }}
          >
            SOCIAL SECURITY CHECK
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 56,
              color: '#1C1C1C',
              fontWeight: 900,
              marginTop: 4,
            }}
          >
            +2.5%
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 2,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_COLAIntro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_COLAvsReality',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_COLAvsReality: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const head = sp(8);
  const chartIn = sp(50);
  const chartProgress = interpolate(frame, [70, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const receiptsIn = sp(180);
  const truthIn = sp(300);

  // Two lines: COLA (slow) and REALITY (steep)
  const buildPath = (points: number[], height: number, progress: number) => {
    const w = 700;
    const step = w / (points.length - 1);
    const visible = Math.ceil(points.length * progress);
    return points.slice(0, visible).map((p, i) => {
      const x = i * step;
      const y = height - p;
      return `${i === 0 ? 'M' : 'L'} ${x} ${y}`;
    }).join(' ');
  };

  const colaPts = [20, 30, 42, 55, 68, 82, 95];
  const realPts = [20, 60, 110, 170, 230, 280, 340];

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(255,0,0,0.08) 0%, rgba(57,62,70,0) 50%, rgba(96,153,102,0.06) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: '8%',
          left: '6%',
          width: 360,
          height: 360,
          borderRadius: '50%',
          background: '#ff0000',
          opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '10%',
          right: '5%',
          width: 320,
          height: 320,
          border: '2px solid #93B5C6',
          opacity: 0.06,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="grid9" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 40 0 L 0 0 0 40" fill="none" stroke="#93B5C6" strokeWidth="0.6" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid9)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = (seededRandom(i + 9) * 1920 + frame * 0.5) % 1920;
        const py = seededRandom(i + 190) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 3,
              height: 3,
              background: '#ff0000',
              opacity: 0.3,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* Headline */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: head,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 32,
            color: '#93B5C6',
            letterSpacing: 8,
            fontWeight: 700,
          }}
        >
          THEY SAY vs THE TRUTH
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 90,
            color: '#F5F5F5',
            fontWeight: 900,
            letterSpacing: -1,
            marginTop: 6,
            lineHeight: 1,
          }}
        >
          The Gap Is <span style={{ color: '#ff0000' }}>WIDENING</span>
        </div>
      </div>

      {/* Chart center */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          left: 120,
          width: 800,
          opacity: chartIn,
        }}
      >
        <svg width="800" height="400" viewBox="0 0 800 400">
          {/* Axes */}
          <line x1="60" y1="20" x2="60" y2="360" stroke="#93B5C6" strokeWidth="2" opacity="0.5" />
          <line x1="60" y1="360" x2="780" y2="360" stroke="#93B5C6" strokeWidth="2" opacity="0.5" />

          {/* Y label */}
          <text x="20" y="40" fill="#93B5C6" fontFamily="Inter" fontSize="16" fontWeight="700">COST</text>

          {/* COLA line - flat */}
          <path
            d={buildPath(colaPts, 360, chartProgress).replace(/(\d+(?:\.\d+)?) /g, (m, n) => `${parseFloat(n) + 60} `)}
            stroke="#609966"
            strokeWidth="5"
            fill="none"
            strokeLinecap="round"
          />
          {/* REALITY line - steep */}
          <path
            d={buildPath(realPts, 360, chartProgress).replace(/(\d+(?:\.\d+)?) /g, (m, n) => `${parseFloat(n) + 60} `)}
            stroke="#ff0000"
            strokeWidth="6"
            fill="none"
            strokeLinecap="round"
          />

          {/* Labels at end */}
          {chartProgress > 0.9 && (
            <>
              <text x="700" y="280" fill="#609966" fontFamily="Inter" fontSize="22" fontWeight="800">COLA</text>
              <text x="700" y="40" fill="#ff0000" fontFamily="Inter" fontSize="26" fontWeight="900">REAL COST</text>
            </>
          )}

          {/* X axis labels */}
          {['2020', '2021', '2022', '2023', '2024', '2025', '2026'].map((y, i) => (
            <text key={i} x={60 + (i * 720) / 6} y="385" textAnchor="middle" fill="#93B5C6" fontFamily="JetBrains Mono" fontSize="14" fontWeight="600">{y}</text>
          ))}
        </svg>
      </div>

      {/* Right: 3 stacked receipts */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          right: 100,
          width: 460,
          opacity: receiptsIn,
          transform: `translateX(${(1 - receiptsIn) * 50}px)`,
        }}
      >
        <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 26, color: '#93B5C6', letterSpacing: 4, fontWeight: 700, marginBottom: 14 }}>
          YOUR REAL BILLS
        </div>
        {[
          { label: 'GROCERY', val: '+34%', icon: 'cart' },
          { label: 'ELECTRIC', val: '+28%', icon: 'bolt' },
          { label: 'PHARMACY', val: '+41%', icon: 'pill' },
        ].map((r, i) => {
          const delay = receiptsIn * (i + 1) * 0.5;
          return (
            <div
              key={i}
              style={{
                padding: '18px 22px',
                background: '#2D2D2D',
                border: '2px solid #ff0000',
                borderRadius: 10,
                marginBottom: 14,
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'center',
                transform: `translateX(${(1 - Math.min(1, delay)) * 30}px)`,
                opacity: Math.min(1, delay),
              }}
            >
              <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 30, color: '#F5F5F5', fontWeight: 800, letterSpacing: 2 }}>
                {r.label}
              </div>
              <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 42, color: '#ff0000', fontWeight: 900 }}>
                {r.val}
              </div>
            </div>
          );
        })}
      </div>

      {/* Bottom truth bar */}
      <div
        style={{
          position: 'absolute',
          bottom: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: truthIn,
          transform: `translateY(${(1 - truthIn) * 30}px)`,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            padding: '16px 40px',
            background: '#ff0000',
            borderRadius: 8,
            fontFamily: 'Inter, sans-serif',
            fontSize: 40,
            color: '#F5F5F5',
            fontWeight: 900,
            letterSpacing: 3,
          }}
        >
          COLA NEVER KEEPS UP
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 2,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_COLAvsReality;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_HousePoorTransition',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_HousePoorTransition: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const houseIn = sp(15);
  const equityTagIn = sp(70);
  const walletIn = sp(130);
  const emptyTagIn = sp(180);
  const safetyNetIn = sp(250);
  const ctaIn = sp(320);

  // Camera slight pan
  const camY = interpolate(frame, [0, durationInFrames], [0, -10]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 25% 50%, rgba(96,153,102,0.12) 0%, rgba(57,62,70,0) 50%), radial-gradient(circle at 75% 50%, rgba(255,0,0,0.1) 0%, rgba(57,62,70,0) 50%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: '10%',
          left: '50%',
          width: 600,
          height: 600,
          borderRadius: '50%',
          border: '2px solid #93B5C6',
          opacity: 0.05,
          transform: `translateX(-50%) scale(${1 + Math.sin(frame * 0.04) * 0.04})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '8%',
          left: '8%',
          width: 280,
          height: 280,
          background: '#609966',
          opacity: 0.05,
          borderRadius: 16,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '8%',
          right: '8%',
          width: 280,
          height: 280,
          border: '2px solid #ff0000',
          opacity: 0.06,
          transform: `rotate(${-frame * 0.1}deg)`,
        }}
      />

      {/* Cross-hatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={`a${i}`} x1={-100 + i * 110} y1="0" x2={i * 110} y2="1080" stroke="#93B5C6" strokeWidth="0.8" />
        ))}
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={`b${i}`} x1={i * 110} y1="0" x2={-100 + i * 110} y2="1080" stroke="#93B5C6" strokeWidth="0.8" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 10) * 1920;
        const py = (seededRandom(i + 210) * 1080 + frame * 0.5) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 3,
              height: 3,
              background: '#93B5C6',
              opacity: 0.35,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* Top title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            color: '#FFC107',
            letterSpacing: 10,
            fontWeight: 700,
            opacity: sp(5),
          }}
        >
          THE CRUEL PARADOX
        </div>
        <div
          style={{
            position: 'absolute',
            top: 110,
            left: 0,
            right: 0,
            textAlign: 'center',
            fontFamily: 'Inter, sans-serif',
            fontSize: 80,
            color: '#F5F5F5',
            fontWeight: 900,
            letterSpacing: -1,
            lineHeight: 1,
          }}
        >
          HOUSE RICH. <span style={{ color: '#ff0000' }}>CASH POOR.</span>
        </div>

        {/* LEFT: House with equity badge */}
        <div
          style={{
            position: 'absolute',
            top: 290,
            left: 200,
            textAlign: 'center',
            opacity: houseIn,
            transform: `scale(${houseIn})`,
          }}
        >
          <svg width="380" height="320" viewBox="0 0 380 320">
            <path d="M190 30 L40 160 L40 290 L340 290 L340 160 Z" fill="#609966" stroke="#1C1C1C" strokeWidth="4" />
            <path d="M190 30 L40 160 L340 160 Z" fill="#2D2D2D" stroke="#1C1C1C" strokeWidth="4" />
            <rect x="160" y="200" width="60" height="90" fill="#1C1C1C" />
            <rect x="70" y="180" width="60" height="60" fill="#93B5C6" opacity="0.7" />
            <rect x="250" y="180" width="60" height="60" fill="#93B5C6" opacity="0.7" />
            {/* Equity stack inside */}
            <text x="190" y="170" textAnchor="middle" fontFamily="Inter" fontSize="40" fill="#FFC107" fontWeight="900">$$$</text>
          </svg>
          <div
            style={{
              marginTop: 14,
              padding: '14px 26px',
              background: '#609966',
              borderRadius: 10,
              display: 'inline-block',
              opacity: equityTagIn,
              transform: `translateY(${(1 - equityTagIn) * 20}px)`,
            }}
          >
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 22, color: '#1C1C1C', letterSpacing: 3, fontWeight: 700 }}>EQUITY</div>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 44, color: '#1C1C1C', fontWeight: 900 }}>$240K</div>
          </div>
        </div>

        {/* Big arrow vs */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: 0,
            right: 0,
            textAlign: 'center',
            fontFamily: 'Inter, sans-serif',
            fontSize: 80,
            color: '#FFC107',
            fontWeight: 900,
            opacity: walletIn,
          }}
        >
          ≠
        </div>

        {/* RIGHT: Empty wallet */}
        <div
          style={{
            position: 'absolute',
            top: 290,
            right: 200,
            textAlign: 'center',
            opacity: walletIn,
            transform: `scale(${walletIn})`,
          }}
        >
          <svg width="380" height="320" viewBox="0 0 380 320">
            <rect x="40" y="100" width="300" height="180" rx="14" fill="#2D2D2D" stroke="#ff0000" strokeWidth="4" />
            <rect x="40" y="140" width="300" height="30" fill="#1C1C1C" opacity="0.6" />
            <circle cx="280" cy="200" r="26" fill="#ff0000" opacity="0.3" stroke="#ff0000" strokeWidth="3" />
            <text x="280" y="212" textAnchor="middle" fontFamily="Inter" fontSize="32" fill="#ff0000" fontWeight="900">$</text>
            {/* Empty marks */}
            <line x1="60" y1="240" x2="240" y2="240" stroke="#9E9E9E" strokeWidth="2" strokeDasharray="6,6" />
            <line x1="60" y1="260" x2="200" y2="260" stroke="#9E9E9E" strokeWidth="2" strokeDasharray="6,6" />
          </svg>
          <div
            style={{
              marginTop: 14,
              padding: '14px 26px',
              background: '#ff0000',
              borderRadius: 10,
              display: 'inline-block',
              opacity: emptyTagIn,
              transform: `translateY(${(1 - emptyTagIn) * 20}px)`,
            }}
          >
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 22, color: '#F5F5F5', letterSpacing: 3, fontWeight: 700 }}>CASH</div>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 44, color: '#F5F5F5', fontWeight: 900 }}>EMPTY</div>
          </div>
        </div>

        {/* Bottom safety net + CTA */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 40,
              color: '#93B5C6',
              fontWeight: 600,
              opacity: safetyNetIn,
              transform: `translateY(${(1 - safetyNetIn) * 20}px)`,
            }}
          >
            Use the <span style={{ color: '#609966', fontWeight: 900 }}>SAFETY NETS</span> that exist
          </div>
          <div
            style={{
              marginTop: 14,
              fontFamily: 'Inter, sans-serif',
              fontSize: 58,
              color: '#FFC107',
              fontWeight: 900,
              letterSpacing: 2,
              opacity: ctaIn,
              transform: `scale(${ctaIn})`,
            }}
          >
            ▸ PROGRAM #1 — NEXT
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 2,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_HousePoorTransition;
```

