```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1Intro',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1Intro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  // Greeting reveal
  const greetY = interpolate(sp(5), [0, 1], [80, 0]);
  const greetOp = interpolate(sp(5), [0, 1], [0, 1]);

  // Name underline
  const underlineW = interpolate(sp(25, { damping: 18 }), [0, 1], [0, 520]);

  // Channel badge
  const badgeScale = sp(40);

  // Question mark giant
  const qMarkScale = sp(70, { damping: 9, stiffness: 100 });
  const qMarkRot = Math.sin(frame * 0.04) * 4;

  // Subtext typewriter feel
  const subOp = interpolate(sp(95), [0, 1], [0, 1]);
  const subY = interpolate(sp(95), [0, 1], [40, 0]);

  // Warning bar
  const warnW = interpolate(sp(140, { damping: 16 }), [0, 1], [0, 900]);
  const warnPulse = 1 + Math.sin(frame * 0.15) * 0.03;

  // Shape drift
  const shape1Y = Math.sin(frame * 0.02) * 20;
  const shape2X = Math.cos(frame * 0.018) * 30;
  const shape3Rot = frame * 0.2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A27B5C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(255,193,7,0.18) 0%, transparent 55%), linear-gradient(135deg, rgba(22,48,32,0.35) 0%, transparent 70%)',
      }} />

      {/* Shape Layer — large transparent geometry */}
      <div style={{
        position: 'absolute', top: -200, right: -150,
        width: 700, height: 700, borderRadius: '50%',
        border: '3px solid #163020', opacity: 0.06,
        transform: `translate(${shape2X}px, ${shape1Y}px) scale(${camZoom})`,
      }} />
      <div style={{
        position: 'absolute', bottom: -250, left: -200,
        width: 800, height: 800,
        border: '2px solid #163020', opacity: 0.05,
        transform: `rotate(${shape3Rot}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '20%', left: '60%',
        width: 300, height: 300,
        background: '#FFC107', opacity: 0.04,
        transform: `rotate(45deg) translateY(${shape1Y}px)`,
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#163020" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid1)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = seededRandom(i + 1) * 1920;
        const py = seededRandom(i + 2) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 15;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + drift,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FFC107', opacity: 0.4,
          }} />
        );
      })}

      {/* Main content container */}
      <div style={{
        position: 'absolute', top: 0, left: 0, width: '100%', height: '100%',
        display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center',
        transform: `scale(${camZoom})`,
      }}>
        {/* Greeting */}
        <div style={{
          fontFamily: 'Yantra One', fontSize: 102, color: '#163020',
          opacity: greetOp, transform: `translateY(${greetY}px)`,
          letterSpacing: '-2px', marginBottom: 10,
        }}>
          नमस्कार
        </div>

        {/* Name with underline */}
        <div style={{ position: 'relative', marginBottom: 30 }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 153, color: '#163020',
            fontWeight: 900, letterSpacing: '-3px',
            transform: `scale(${sp(15)})`,
          }}>
            मैं <span style={{ color: '#FFC107' }}>कुनाल</span> हूं
          </div>
          <div style={{
            position: 'absolute', bottom: -10, left: '50%',
            transform: 'translateX(-50%)',
            width: underlineW, height: 6, background: '#FFC107',
          }} />
        </div>

        {/* Channel Badge */}
        <div style={{
          transform: `scale(${badgeScale})`,
          background: '#163020', padding: '14px 38px',
          borderRadius: 8, display: 'flex', alignItems: 'center', gap: 14,
          border: '2px solid #FFC107',
          marginBottom: 50,
        }}>
          <div style={{ width: 14, height: 14, borderRadius: '50%', background: '#ff0000' }} />
          <div style={{
            fontFamily: 'Inter', fontSize: 38, color: '#F5F5F5', fontWeight: 700,
            letterSpacing: '2px',
          }}>
            PROBUFFET HQ
          </div>
        </div>

        {/* Giant Question Mark + Text */}
        <div style={{
          display: 'flex', alignItems: 'center', gap: 40,
          transform: `scale(${qMarkScale}) rotate(${qMarkRot * 0.3}deg)`,
        }}>
          <svg width="140" height="180" viewBox="0 0 100 130">
            <path d="M 30 35 Q 30 15 50 15 Q 70 15 70 35 Q 70 50 50 60 L 50 80" 
              stroke="#FFC107" strokeWidth="10" fill="none" strokeLinecap="round" />
            <circle cx="50" cy="105" r="7" fill="#FFC107" />
          </svg>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 76, color: '#163020',
            fontWeight: 900, lineHeight: 1.1, letterSpacing: '-1px',
          }}>
            एक सवाल<br />
            <span style={{ color: '#ff0000' }}>आपके लिए</span>
          </div>
        </div>

        {/* Warning Bar */}
        <div style={{
          marginTop: 60,
          transform: `scaleX(${warnPulse})`,
          opacity: subOp, transform: `translateY(${subY}px) scaleX(${warnPulse})`,
        }}>
          <div style={{
            width: warnW, height: 65, background: '#163020',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            border: '2px solid #FFC107',
            overflow: 'hidden',
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 37,
              color: '#FFC107', fontWeight: 700, letterSpacing: '1px',
              whiteSpace: 'nowrap',
            }}>
              ⚡ असहज कर देगा — लेकिन जरूरी है ⚡
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1Intro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2BankBalance',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2BankBalance: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Phone tilt
  const phoneRot = interpolate(sp(10, { damping: 14 }), [0, 1], [-15, -3]);
  const phoneScale = sp(10);

  // Counter animation
  const counterProgress = interpolate(frame, [40, 110], [0, 2547893], { extrapolateRight: 'clamp' });
  const counterVal = Math.floor(counterProgress);

  // Question reveal
  const qOp = interpolate(sp(115), [0, 1], [0, 1]);
  const qY = interpolate(sp(115), [0, 1], [50, 0]);

  // Percent breakdown bars
  const yoursW = interpolate(sp(170, { damping: 18 }), [0, 1], [0, 180]);
  const banksW = interpolate(sp(190, { damping: 18 }), [0, 1], [0, 620]);

  // Shake on reveal
  const shake = frame > 165 && frame < 180 ? Math.sin(frame * 2) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A27B5C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 70% 30%, rgba(255,193,7,0.15) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '10%', left: '5%',
        width: 400, height: 400,
        border: '3px solid #163020', opacity: 0.05,
        borderRadius: 40,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', right: '10%',
        width: 500, height: 500, borderRadius: '50%',
        background: '#163020', opacity: 0.04,
      }} />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="lines2" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#163020" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#lines2)" />
      </svg>

      {/* Particles - rupee symbols */}
      {Array.from({ length: 15 }).map((_, i) => {
        const px = seededRandom(i + 10) * 1920;
        const py = (seededRandom(i + 20) * 1080 + frame * 0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            fontSize: 26, color: '#163020', opacity: 0.15,
            fontFamily: 'Inter', fontWeight: 700,
          }}>₹</div>
        );
      })}

      {/* Main layout - split */}
      <div style={{
        position: 'absolute', top: 0, left: 0, width: '100%', height: '100%',
        display: 'flex', alignItems: 'center', padding: '0 120px', gap: 80,
        transform: `translateX(${shake}px)`,
      }}>
        {/* LEFT - Phone Mockup */}
        <div style={{
          transform: `scale(${phoneScale}) rotate(${phoneRot}deg)`,
          width: 480, height: 720,
          background: '#163020', borderRadius: 50,
          border: '8px solid #2D2D2D',
          padding: 30, position: 'relative',
          boxShadow: '0 30px 60px rgba(0,0,0,0.4)',
        }}>
          {/* Notch */}
          <div style={{
            position: 'absolute', top: 15, left: '50%',
            transform: 'translateX(-50%)',
            width: 140, height: 30, background: '#1C1C1C', borderRadius: 20,
          }} />

          {/* App header */}
          <div style={{
            marginTop: 40, padding: '20px 10px',
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 24, color: '#FFC107', fontWeight: 700,
            }}>YOUR BANK</div>
            <div style={{ width: 32, height: 32, borderRadius: '50%', background: '#FFC107' }} />
          </div>

          {/* Balance Card */}
          <div style={{
            marginTop: 30, padding: 30,
            background: 'rgba(255,193,7,0.1)', borderRadius: 20,
            border: '2px solid #FFC107',
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 26,
              color: '#9E9E9E', marginBottom: 15,
            }}>उपलब्ध बैलेंस</div>
            <div style={{
              fontFamily: 'Inter', fontSize: 64, color: '#F5F5F5',
              fontWeight: 800, letterSpacing: '-2px',
            }}>
              ₹{counterVal.toLocaleString('en-IN')}
            </div>
            <div style={{
              marginTop: 20, height: 2, background: '#FFC107', opacity: 0.4,
            }} />
            <div style={{
              marginTop: 15, fontFamily: 'Inter', fontSize: 18, color: '#9E9E9E',
            }}>A/C ••••4521</div>
          </div>

          {/* Action rows */}
          {['जमा', 'निकासी', 'ट्रांस्फर'].map((t, i) => (
            <div key={i} style={{
              marginTop: 20, padding: '18px 20px',
              background: 'rgba(245,245,245,0.05)', borderRadius: 12,
              display: 'flex', justifyContent: 'space-between',
              fontFamily: 'Noto Sans Devanagari', fontSize: 24, color: '#F5F5F5',
            }}>
              <span>{t}</span>
              <span style={{ color: '#9E9E9E' }}>→</span>
            </div>
          ))}
        </div>

        {/* RIGHT - Question */}
        <div style={{ flex: 1, opacity: qOp, transform: `translateY(${qY}px)` }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 32, color: '#FFC107',
            fontWeight: 700, letterSpacing: '4px', marginBottom: 20,
          }}>THE BIG QUESTION</div>

          <div style={{
            fontFamily: 'Yantra One', fontSize: 110, color: '#163020',
            fontWeight: 900, lineHeight: 1.05, letterSpacing: '-2px',
          }}>
            असल में<br />
            कितने <span style={{ color: '#ff0000' }}>आपके</span><br />
            हैं?
          </div>

          {/* Split bar visualization */}
          <div style={{ marginTop: 50 }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 15, height: 60 }}>
              <div style={{
                width: yoursW, height: '100%', background: '#FFC107',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                fontFamily: 'Inter', fontSize: 28, color: '#163020', fontWeight: 800,
              }}>~22%</div>
              <div style={{
                width: banksW, height: '100%', background: '#163020',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                fontFamily: 'Inter', fontSize: 28, color: '#F5F5F5', fontWeight: 800,
              }}>~78% बैंक के पास</div>
            </div>
            <div style={{
              marginTop: 15, fontFamily: 'Noto Sans Devanagari', fontSize: 26,
              color: '#163020', opacity: 0.7,
            }}>* RBI रिज़र्व रेश्यो के आधार पर</div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2BankBalance;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3Belief',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3Belief: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title slide
  const titleX = interpolate(sp(5), [0, 1], [-200, 0]);
  const titleOp = sp(5);

  // Generations cards
  const gen1 = sp(30);
  const gen2 = sp(50);
  const gen3 = sp(70);

  // Bank fortress
  const bankScale = sp(100, { damping: 10 });
  const bankBreath = 1 + Math.sin(frame * 0.04) * 0.015;

  // Belief text
  const beliefOp = interpolate(sp(150), [0, 1], [0, 1]);

  // Government stamp
  const stampScale = sp(180, { damping: 7, stiffness: 90 });
  const stampRot = interpolate(sp(180), [0, 1], [-30, -12]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#A27B5C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(22,48,32,0.2) 0%, transparent 50%, rgba(255,193,7,0.1) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '5%', right: '8%',
        width: 0, height: 0,
        borderLeft: '200px solid transparent',
        borderRight: '200px solid transparent',
        borderBottom: '350px solid #163020',
        opacity: 0.04,
        transform: `rotate(${Math.sin(frame * 0.02) * 5}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', left: '5%',
        width: 350, height: 350,
        background: '#FFC107', opacity: 0.05,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots3" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#163020" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots3)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 30) * 1920;
        const py = seededRandom(i + 40) * 1080;
        const drift = Math.sin(frame * 0.025 + i) * 12;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + drift,
            width: 3, height: 3, borderRadius: '50%',
            background: '#163020', opacity: 0.3,
          }} />
        );
      })}

      {/* Title top */}
      <div style={{
        position: 'absolute', top: 80, left: 100,
        opacity: titleOp, transform: `translateX(${titleX}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 28, color: '#FFC107',
          letterSpacing: '6px', fontWeight: 700, marginBottom: 12,
        }}>// PURANI SOCH</div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 92, color: '#163020',
          fontWeight: 900, lineHeight: 1, letterSpacing: '-2px',
        }}>
          हम भारतीय — एक<br />
          <span style={{ color: '#ff0000' }}>विश्वास</span> के साथ बड़े हुए
        </div>
      </div>

      {/* Generation cards - timeline */}
      <div style={{
        position: 'absolute', top: 380, left: 100,
        display: 'flex', gap: 30,
      }}>
        {[
          { y: '1960s', label: 'दादा-दादी', op: gen1 },
          { y: '1990s', label: 'माता-पिता', op: gen2 },
          { y: '2020s', label: 'आप', op: gen3 },
        ].map((g, i) => (
          <div key={i} style={{
            width: 240, height: 140,
            background: '#163020', borderRadius: 12,
            padding: '20px 25px',
            border: '2px solid #FFC107',
            opacity: g.op, transform: `translateY(${(1 - g.op) * 40}px)`,
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 24, color: '#FFC107',
              fontWeight: 700, letterSpacing: '2px',
            }}>{g.y}</div>
            <div style={{
              fontFamily: 'Yantra One', fontSize: 38, color: '#F5F5F5',
              marginTop: 8, letterSpacing: '-1px',
            }}>{g.label}</div>
            <div style={{
              marginTop: 12, fontFamily: 'Noto Sans Devanagari', fontSize: 22,
              color: '#9E9E9E',
            }}>"बैंक = सुरक्षा"</div>
          </div>
        ))}
      </div>

      {/* Bank fortress illustration RIGHT */}
      <div style={{
        position: 'absolute', top: 380, right: 120,
        transform: `scale(${bankScale * bankBreath})`,
        transformOrigin: 'center bottom',
      }}>
        <svg width="420" height="420" viewBox="0 0 200 200">
          {/* Base */}
          <rect x="20" y="160" width="160" height="20" fill="#163020" />
          {/* Pillars */}
          {[40, 75, 110, 145].map((x, i) => (
            <rect key={i} x={x - 8} y="80" width="16" height="80" fill="#163020" />
          ))}
          {/* Roof */}
          <polygon points="10,80 100,30 190,80" fill="#163020" />
          {/* Roof line */}
          <rect x="10" y="78" width="180" height="8" fill="#FFC107" />
          {/* Top symbol */}
          <circle cx="100" cy="55" r="6" fill="#FFC107" />
          {/* Door */}
          <rect x="85" y="115" width="30" height="45" fill="#FFC107" opacity="0.3" />
          {/* Sign */}
          <rect x="55" y="92" width="90" height="14" fill="#FFC107" />
        </svg>
        <div style={{
          textAlign: 'center', marginTop: -20,
          fontFamily: 'Inter', fontSize: 26, color: '#163020',
          fontWeight: 800, letterSpacing: '3px',
        }}>SARKARI BANK</div>
      </div>

      {/* Bottom belief banner */}
      <div style={{
        position: 'absolute', bottom: 90, left: 100, right: 100,
        opacity: beliefOp,
        background: '#163020', padding: '28px 50px',
        border: '3px solid #FFC107',
        display: 'flex', alignItems: 'center', justifyContent: 'space-between',
      }}>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 58, color: '#F5F5F5',
          letterSpacing: '-1px',
        }}>
          "बैंक मतलब <span style={{ color: '#FFC107' }}>सुरक्षा की गारंटी</span>"
        </div>
        {/* Government stamp */}
        <div style={{
          transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
          width: 140, height: 140, borderRadius: '50%',
          border: '4px solid #ff0000',
          display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
          background: 'rgba(255,0,0,0.1)',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 14, color: '#ff0000', fontWeight: 800, letterSpacing: '1px' }}>GOVT</div>
          <div style={{ fontFamily: 'Inter', fontSize: 20, color: '#ff0000', fontWeight: 900 }}>BHARAT</div>
          <div style={{ fontFamily: 'Inter', fontSize: 14, color: '#ff0000', fontWeight: 800, letterSpacing: '1px' }}>BACKED</div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3Belief;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4Vault',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4Vault: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Item drops
  const item1 = sp(20, { damping: 9 });
  const item2 = sp(45, { damping: 9 });
  const item3 = sp(70, { damping: 9 });
  const item4 = sp(95, { damping: 9 });

  // Vault scale
  const vaultScale = sp(120, { damping: 11 });
  const vaultWheelRot = interpolate(frame, [140, 240], [0, 540], { extrapolateRight: 'clamp' });

  // Lock label
  const lockOp = interpolate(sp(220), [0, 1], [0, 1]);

  // Thought bubble
  const thoughtOp = interpolate(sp(260), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#A27B5C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 60%, rgba(22,48,32,0.3) 0%, transparent 70%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '8%', left: '8%',
        width: 250, height: 250, borderRadius: '50%',
        border: '3px solid #FFC107', opacity: 0.06,
      }} />
      <div style={{
        position: 'absolute', top: '15%', right: '12%',
        width: 200, height: 200,
        background: '#163020', opacity: 0.05,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', left: '20%',
        width: 600, height: 100,
        background: '#163020', opacity: 0.04,
        transform: 'skewX(-20deg)',
      }} />

      {/* Crosshatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="hatch4" width="20" height="20" patternUnits="userSpaceOnUse">
            <path d="M 0 20 L 20 0" stroke="#163020" strokeWidth="1" />
            <path d="M 0 0 L 20 20" stroke="#163020" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hatch4)" />
      </svg>

      {/* Falling rupee particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i + 50) * 1920;
        const startY = -100;
        const py = startY + ((frame * 2 + seededRandom(i + 60) * 1080) % 1280);
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            fontFamily: 'Inter', fontSize: 22, color: '#FFC107', opacity: 0.3,
            fontWeight: 700, transform: `rotate(${frame + i * 30}deg)`,
          }}>₹</div>
        );
      })}

      {/* LEFT - Life savings flowing into vault */}
      <div style={{
        position: 'absolute', top: 0, left: 0, width: '50%', height: '100%',
        display: 'flex', flexDirection: 'column', justifyContent: 'center',
        padding: '0 80px', gap: 28,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 26, color: '#FFC107',
          letterSpacing: '5px', fontWeight: 700,
        }}>// JEEVAN BHAR KI KAMAI</div>

        {[
          { icon: '💼', text: 'पूरी जिंदगी की कमाई', amt: '₹ 45,00,000', op: item1, color: '#FFC107' },
          { icon: '🔨', text: 'मेहनत का फल', amt: '₹ 22,00,000', op: item2, color: '#F5F5F5' },
          { icon: '🏖', text: 'रिटायरमेंट फंड', amt: '₹ 38,00,000', op: item3, color: '#FFC107' },
          { icon: '🎓', text: 'बच्चों का भविष्य', amt: '₹ 18,00,000', op: item4, color: '#F5F5F5' },
        ].map((it, i) => (
          <div key={i} style={{
            opacity: it.op,
            transform: `translateX(${(1 - it.op) * -100}px)`,
            background: '#163020', padding: '22px 30px',
            border: `2px solid ${it.color}`,
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
          }}>
            <div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari', fontSize: 30, color: '#F5F5F5', fontWeight: 600,
              }}>{it.text}</div>
            </div>
            <div style={{
              fontFamily: 'Inter', fontSize: 32, color: it.color, fontWeight: 800,
              letterSpacing: '-1px',
            }}>{it.amt}</div>
          </div>
        ))}

        {/* Arrow */}
        <div style={{
          marginTop: 10, fontFamily: 'Inter', fontSize: 50,
          color: '#163020', textAlign: 'right', opacity: lockOp,
        }}>━━━━━▶</div>
      </div>

      {/* RIGHT - Vault */}
      <div style={{
        position: 'absolute', top: '50%', right: 100,
        transform: `translateY(-50%) scale(${vaultScale})`,
      }}>
        <svg width="500" height="600" viewBox="0 0 200 240">
          {/* Vault body */}
          <rect x="10" y="10" width="180" height="220" rx="8" fill="#163020" stroke="#FFC107" strokeWidth="3" />
          {/* Inner door */}
          <rect x="25" y="25" width="150" height="190" rx="4" fill="#2D2D2D" stroke="#FFC107" strokeWidth="2" />
          {/* Bolts */}
          {[[35,35],[165,35],[35,205],[165,205]].map(([cx,cy],i)=>(
            <circle key={i} cx={cx} cy={cy} r="4" fill="#FFC107" />
          ))}
          {/* Wheel */}
          <g transform={`translate(100 120) rotate(${vaultWheelRot})`}>
            <circle r="38" fill="none" stroke="#FFC107" strokeWidth="4" />
            <circle r="28" fill="#163020" stroke="#FFC107" strokeWidth="2" />
            {[0,60,120,180,240,300].map((a,i)=>(
              <rect key={i} x="-3" y="-44" width="6" height="14" fill="#FFC107" transform={`rotate(${a})`} />
            ))}
            <circle r="8" fill="#FFC107" />
          </g>
          {/* Handle */}
          <rect x="155" y="115" width="20" height="10" rx="2" fill="#FFC107" />
        </svg>
        <div style={{
          marginTop: -10, textAlign: 'center',
          fontFamily: 'Yantra One', fontSize: 56, color: '#163020',
          letterSpacing: '-1px',
        }}>तिजोरी</div>
        <div style={{
          marginTop: 8, textAlign: 'center',
          fontFamily: 'Inter', fontSize: 22, color: '#163020',
          letterSpacing: '4px', fontWeight: 700, opacity: 0.6,
        }}>SAFE • SECURED • LOCKED</div>
      </div>

      {/* Thought bubble - belief */}
      <div style={{
        position: 'absolute', top: 80, right: 80,
        opacity: thoughtOp,
        transform: `scale(${0.8 + thoughtOp * 0.2})`,
      }}>
        <div style={{
          background: '#F5F5F5', padding: '20px 32px',
          borderRadius: 30, position: 'relative',
          border: '3px solid #163020',
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 44, color: '#163020',
            letterSpacing: '-1px',
          }}>
            "पैसा <span style={{ color: '#ff0000' }}>सुरक्षित</span> है"
          </div>
          {/* Tail */}
          <div style={{
            position: 'absolute', bottom: -20, left: 60,
            width: 0, height: 0,
            borderLeft: '15px solid transparent',
            borderRight: '15px solid transparent',
            borderTop: '20px solid #F5F5F5',
          }} />
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4Vault;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5Illusion',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5Illusion: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // BUT impact
  const butScale = sp(5, { damping: 7, stiffness: 80 });
  const shake = frame < 30 ? Math.sin(frame * 2.5) * 6 : 0;

  // Crack reveal
  const crackOp = interpolate(sp(50, { damping: 18 }), [0, 1], [0, 1]);

  // Illusion word
  const illuOp = interpolate(sp(90), [0, 1], [0, 1]);
  const illuScale = sp(90, { damping: 8 });

  // System pillars
  const p1 = sp(140);
  const p2 = sp(155);
  const p3 = sp(170);
  const p4 = sp(185);

  // System label
  const sysOp = interpolate(sp(210), [0, 1], [0, 1]);

  // Wobble pillars
  const wobble = frame > 200 ? Math.sin(frame * 0.3) * 2 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A27B5C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 30%, rgba(255,0,0,0.12) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '20%', left: '5%',
        width: 500, height: 500, borderRadius: '50%',
        background: '#ff0000', opacity: 0.03,
        transform: `scale(${1 + Math.sin(frame * 0.05) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', right: '8%',
        width: 400, height: 400,
        border: '4px solid #163020', opacity: 0.05,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Concentric circles texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {[100, 200, 300, 400, 500, 600, 700].map((r, i) => (
          <circle key={i} cx="960" cy="540" r={r} fill="none" stroke="#163020" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 70) * 1920;
        const py = seededRandom(i + 80) * 1080;
        const drift = Math.cos(frame * 0.03 + i) * 15;
        return (
          <div key={i} style={{
            position: 'absolute', left: px + drift, top: py,
            width: 4, height: 4,
            background: '#FFC107', opacity: 0.35,
          }} />
        );
      })}

      {/* Main content */}
      <div style={{
        position: 'absolute', top: 0, left: 0, width: '100%', height: '100%',
        display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center',
        transform: `translateX(${shake}px)`,
      }}>
        {/* BUT - giant */}
        <div style={{
          transform: `scale(${butScale})`,
          fontFamily: 'Inter', fontSize: 68, color: '#ff0000',
          fontWeight: 900, letterSpacing: '12px',
        }}>
          ━━ LEKIN ━━
        </div>

        {/* Crack with text overlay */}
        <div style={{
          position: 'relative', marginTop: 30, opacity: crackOp,
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 119, color: '#163020',
            fontWeight: 900, lineHeight: 1, letterSpacing: '-3px',
            textAlign: 'center',
          }}>
            सिस्टम की सबसे बड़ी
          </div>
          {/* SVG crack overlay */}
          <svg width="900" height="20" style={{ position: 'absolute', bottom: -10, left: '50%', transform: 'translateX(-50%)' }}>
            <path d="M 0 10 L 150 8 L 200 14 L 350 6 L 450 12 L 600 8 L 750 14 L 900 10"
              stroke="#ff0000" strokeWidth="3" fill="none" strokeDasharray="900"
              strokeDashoffset={interpolate(sp(70), [0, 1], [900, 0])} />
          </svg>
        </div>

        {/* Illusion mega word */}
        <div style={{
          marginTop: 40,
          transform: `scale(${illuScale})`,
          opacity: illuOp,
          position: 'relative',
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 221, color: '#ff0000',
            fontWeight: 900, letterSpacing: '-6px', lineHeight: 1,
            textShadow: '6px 6px 0px #163020',
          }}>
            भ्रम
          </div>
          <div style={{
            position: 'absolute', top: 30, right: -180,
            fontFamily: 'Inter', fontSize: 28, color: '#163020',
            fontWeight: 700, letterSpacing: '4px',
            transform: 'rotate(8deg)',
          }}>= ILLUSION</div>
        </div>

        {/* System pillars */}
        <div style={{
          marginTop: 30, display: 'flex', gap: 16, alignItems: 'flex-end',
          opacity: sysOp,
        }}>
          {[
            { h: 110, label: 'TRUST', s: p1 },
            { h: 140, label: 'BANKING', s: p2 },
            { h: 125, label: 'SYSTEM', s: p3 },
            { h: 100, label: 'FAITH', s: p4 },
          ].map((p, i) => (
            <div key={i} style={{
              width: 110, height: p.h, background: '#163020',
              border: '2px solid #FFC107',
              display: 'flex', alignItems: 'flex-end', justifyContent: 'center',
              padding: '0 0 12px 0',
              transform: `scaleY(${p.s}) translateX(${wobble * (i % 2 ? 1 : -1)}px)`,
              transformOrigin: 'bottom',
            }}>
              <div style={{
                fontFamily: 'Inter', fontSize: 16, color: '#FFC107',
                fontWeight: 800, letterSpacing: '1.5px',
                writingMode: 'vertical-rl', textOrientation: 'mixed',
                transform: 'rotate(180deg)',
              }}>{p.label}</div>
            </div>
          ))}
        </div>

        {/* Caption */}
        <div style={{
          marginTop: 20, opacity: sysOp,
          fontFamily: 'Noto Sans Devanagari', fontSize: 32,
          color: '#163020', fontWeight: 600, letterSpacing: '2px',
        }}>
          ━ पूरी व्यवस्था इस पर टिकी है ━
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5Illusion;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6Breaking',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6Breaking: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Glass shatter
  const glassOp = sp(5);
  const crackDash = interpolate(sp(40, { damping: 16 }), [0, 1], [1500, 0]);

  // Impact shake
  const shake = frame > 30 && frame < 55 ? Math.sin(frame * 3) * 8 : 0;

  // Pyramid build
  const pyTop = sp(80);
  const pyMid = sp(100);
  const pyBot = sp(120);

  // Loss arrow
  const arrowDraw = interpolate(sp(160, { damping: 18 }), [0, 1], [0, 1]);

  // Stats
  const statOp = interpolate(sp(200), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#A27B5C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, rgba(255,0,0,0.15) 0%, transparent 65%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: -100, left: -100,
        width: 500, height: 500,
        background: '#163020', opacity: 0.05,
        transform: `rotate(${45 + frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: -150, right: -150,
        width: 600, height: 600, borderRadius: '50%',
        border: '4px solid #FFC107', opacity: 0.05,
      }} />

      {/* Texture - radial */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <radialGradient id="rg6">
            <stop offset="0%" stopColor="#163020" stopOpacity="0.4" />
            <stop offset="100%" stopColor="#163020" stopOpacity="0" />
          </radialGradient>
        </defs>
        {Array.from({ length: 8 }).map((_, i) => (
          <line key={i} x1="960" y1="540"
            x2={960 + Math.cos(i * Math.PI / 4) * 1000}
            y2={540 + Math.sin(i * Math.PI / 4) * 1000}
            stroke="#163020" strokeWidth="1" />
        ))}
      </svg>

      {/* Shatter particles */}
      {Array.from({ length: 35 }).map((_, i) => {
        const angle = seededRandom(i + 100) * Math.PI * 2;
        const dist = Math.min(frame * 4, 600) * (0.5 + seededRandom(i + 110));
        const px = 960 + Math.cos(angle) * dist;
        const py = 540 + Math.sin(angle) * dist;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 6, height: 6, background: '#FFC107',
            opacity: interpolate(frame, [30, 100], [0.8, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            transform: `rotate(${frame * 5 + i * 30}deg)`,
          }} />
        );
      })}

      {/* Main layout */}
      <div style={{
        position: 'absolute', top: 0, left: 0, width: '100%', height: '100%',
        display: 'flex', padding: '60px 100px',
        transform: `translateX(${shake}px)`,
      }}>
        {/* LEFT - Breaking Illusion */}
        <div style={{ flex: 1, display: 'flex', flexDirection: 'column', justifyContent: 'center' }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 26, color: '#ff0000',
            letterSpacing: '5px', fontWeight: 800, marginBottom: 15, opacity: glassOp,
          }}>// JAB BHARAM TOOTA</div>

          <div style={{ position: 'relative', display: 'inline-block' }}>
            <div style={{
              fontFamily: 'Yantra One', fontSize: 145, color: '#163020',
              fontWeight: 900, lineHeight: 1, letterSpacing: '-3px',
              opacity: glassOp,
            }}>
              भ्रम<br />
              <span style={{ color: '#ff0000' }}>टूटता</span> है
            </div>
            {/* Crack SVG */}
            <svg width="700" height="350" style={{ position: 'absolute', top: 0, left: 0, pointerEvents: 'none' }}>
              <path
                d="M 50 80 L 200 60 L 180 130 L 350 150 L 320 220 L 500 200 L 470 280 L 600 260"
                stroke="#ff0000" strokeWidth="4" fill="none"
                strokeDasharray="1500"
                strokeDashoffset={crackDash}
              />
              <path
                d="M 200 60 L 250 30 M 350 150 L 400 100 M 500 200 L 550 170"
                stroke="#ff0000" strokeWidth="3" fill="none"
                strokeDasharray="1500"
                strokeDashoffset={crackDash}
              />
            </svg>
          </div>

          {/* Loss arrow */}
          <div style={{ marginTop: 50, display: 'flex', alignItems: 'center', gap: 20 }}>
            <svg width="320" height="80" viewBox="0 0 320 80">
              <line x1="10" y1="40" x2={10 + arrowDraw * 280} y2="40"
                stroke="#ff0000" strokeWidth="6" />
              <polygon points={`${10 + arrowDraw * 280},40 ${arrowDraw * 280 - 10},25 ${arrowDraw * 280 - 10},55`}
                fill="#ff0000" opacity={arrowDraw} />
            </svg>
            <div style={{
              fontFamily: 'Inter', fontSize: 42, color: '#ff0000',
              fontWeight: 900, opacity: arrowDraw, letterSpacing: '-1px',
            }}>NUKSAAN</div>
          </div>
        </div>

        {/* RIGHT - Victim Pyramid */}
        <div style={{
          flex: 1, display: 'flex', flexDirection: 'column',
          alignItems: 'center', justifyContent: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 22, color: '#FFC107',
            letterSpacing: '4px', fontWeight: 700, marginBottom: 25,
          }}>WHO LOSES FIRST</div>

          {/* Pyramid */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 6 }}>
            {/* Top - Insiders SAFE */}
            <div style={{
              width: 200, height: 70,
              background: '#163020',
              border: '2px solid #FFC107',
              display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
              transform: `scale(${pyTop})`,
            }}>
              <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#FFC107', fontWeight: 800, letterSpacing: '2px' }}>INSIDERS</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 17, color: '#9E9E9E' }}>सुरक्षित ✓</div>
            </div>
            {/* Mid - Institutions */}
            <div style={{
              width: 360, height: 75,
              background: '#163020',
              border: '2px solid #F5F5F5',
              display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
              transform: `scale(${pyMid})`,
            }}>
              <div style={{ fontFamily: 'Inter', fontSize: 20, color: '#F5F5F5', fontWeight: 800, letterSpacing: '2px' }}>BIG INSTITUTIONS</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 18, color: '#9E9E9E' }}>थोड़ा नुकसान</div>
            </div>
            {/* Bottom - Common Depositors */}
            <div style={{
              width: 540, height: 110,
              background: '#ff0000',
              display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
              transform: `scale(${pyBot})`,
              border: '3px solid #163020',
            }}>
              <div style={{ fontFamily: 'Yantra One', fontSize: 38, color: '#F5F5F5', letterSpacing: '-1px' }}>आम जमाकर्ता</div>
              <div style={{ fontFamily: 'Inter', fontSize: 19, color: '#F5F5F5', fontWeight: 700, letterSpacing: '2px', marginTop: 4 }}>YOU & ME — MAXIMUM LOSS</div>
            </div>
          </div>

          {/* Stat */}
          <div style={{
            marginTop: 35, opacity: statOp,
            background: '#163020', padding: '15px 35px',
            border: '2px solid #ff0000',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 32, color: '#ff0000',
              fontWeight: 900, letterSpacing: '-1px',
            }}>100% BHAR <span style={{ color: '#F5F5F5' }}>→ AAM AADMI PAR</span></div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6Breaking;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7Depth',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7Depth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Dive arrow drawing
  const diveDraw = interpolate(sp(5, { damping: 18 }), [0, 1], [0, 1]);

  // Layers
  const l1 = sp(40);
  const l2 = sp(60);
  const l3 = sp(80);
  const l4 = sp(100);

  // Bank statement reveal
  const stmtOp = interpolate(sp(150), [0, 1], [0, 1]);
  const stmtScale = sp(150);

  // Eye/before-after
  const eyeOp = interpolate(sp(210), [0, 1], [0, 1]);

  // Statement transformation
  const transformProgress = interpolate(frame, [220, 280], [0, 1], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A27B5C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient - deep dive feel */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, transparent 0%, rgba(22,48,32,0.4) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '30%', left: '15%',
        width: 400, height: 400, borderRadius: '50%',
        border: '3px solid #FFC107', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '15%', right: '20%',
        width: 300, height: 300,
        background: '#163020', opacity: 0.05,
        borderRadius: 30,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Texture - depth lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 15 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 72} x2="1920" y2={i * 72}
            stroke="#163020" strokeWidth="1" opacity={1 - i / 15} />
        ))}
      </svg>

      {/* Sinking particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 200) * 1920;
        const py = (frame * 1.2 + seededRandom(i + 210) * 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 5, height: 5, borderRadius: '50%',
            background: '#FFC107', opacity: 0.25,
          }} />
        );
      })}

      {/* LEFT - Diving in arrow + layers */}
      <div style={{
        position: 'absolute', top: 80, left: 80, width: 760,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 28, color: '#FFC107',
          letterSpacing: '5px', fontWeight: 800,
        }}>// CHALIYE GEHRAI MEIN</div>

        <div style={{
          fontFamily: 'Yantra One', fontSize: 102, color: '#163020',
          fontWeight: 900, lineHeight: 1, letterSpacing: '-2px', marginTop: 15,
        }}>
          कहानी की<br />
          <span style={{ color: '#ff0000' }}>गहराई</span> में
        </div>

        {/* Depth layers */}
        <div style={{ marginTop: 40, display: 'flex', flexDirection: 'column', gap: 12 }}>
          {[
            { lvl: 'L1', txt: 'दिखावे की सच्चाई', op: l1 },
            { lvl: 'L2', txt: 'कानूनी हकीकत', op: l2 },
            { lvl: 'L3', txt: 'सिस्टम का खेल', op: l3 },
            { lvl: 'L4', txt: 'असली नुकसान', op: l4 },
          ].map((d, i) => (
            <div key={i} style={{
              opacity: d.op,
              transform: `translateX(${(1 - d.op) * -50}px)`,
              display: 'flex', alignItems: 'center', gap: 20,
              padding: '18px 25px',
              background: i === 3 ? '#ff0000' : '#163020',
              border: '2px solid #FFC107',
              marginLeft: i * 30,
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 26, color: '#FFC107',
                fontWeight: 800, letterSpacing: '2px',
              }}>{d.lvl}</div>
              <div style={{
                width: 2, height: 26, background: '#FFC107', opacity: 0.5,
              }} />
              <div style={{
                fontFamily: 'Noto Sans Devanagari', fontSize: 32, color: '#F5F5F5',
                fontWeight: 700,
              }}>{d.txt}</div>
            </div>
          ))}
        </div>

        {/* Arrow down */}
        <svg width="80" height="80" style={{ marginTop: 10, marginLeft: 30 }}>
          <line x1="40" y1="0" x2="40" y2={diveDraw * 60} stroke="#ff0000" strokeWidth="5" />
          <polygon points={`40,${diveDraw * 80} 25,${diveDraw * 65} 55,${diveDraw * 65}`} fill="#ff0000" opacity={diveDraw} />
        </svg>
      </div>

      {/* RIGHT - Bank Statement transforming */}
      <div style={{
        position: 'absolute', top: 130, right: 100,
        opacity: stmtOp,
        transform: `scale(${stmtScale})`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 22, color: '#163020',
          letterSpacing: '4px', fontWeight: 700, marginBottom: 15,
          textAlign: 'center',
        }}>BEFORE → AFTER</div>

        {/* Statement card */}
        <div style={{
          width: 580, background: '#F5F5F5',
          border: '4px solid #163020', padding: 30,
        }}>
          <div style={{
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
            borderBottom: '2px solid #163020', paddingBottom: 15, marginBottom: 20,
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 24, color: '#163020', fontWeight: 800,
              letterSpacing: '2px',
            }}>BANK STATEMENT</div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 18, color: '#163020', opacity: 0.6,
            }}>25/05/2026</div>
          </div>

          {/* Account number */}
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 22, color: '#163020',
            marginBottom: 18, fontWeight: 600,
          }}>A/C: XXXX-XXXX-4521</div>

          {/* Balance row */}
          <div style={{
            background: '#163020', padding: '20px 25px',
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#F5F5F5',
            }}>बैलेंस:</div>
            <div style={{ position: 'relative' }}>
              <div style={{
                fontFamily: 'Inter', fontSize: 51, color: '#FFC107',
                fontWeight: 900, letterSpacing: '-2px',
                opacity: 1 - transformProgress,
              }}>₹ 12,45,000</div>
              <div style={{
                position: 'absolute', top: 0, right: 0,
                fontFamily: 'Inter', fontSize: 51, color: '#ff0000',
                fontWeight: 900, letterSpacing: '-2px',
                opacity: transformProgress,
                textDecoration: 'line-through',
              }}>₹ 12,45,000</div>
            </div>
          </div>

          {/* Truth reveal */}
          <div style={{
            marginTop: 18, padding: '15px 20px',
            background: '#ff0000', opacity: eyeOp,
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 24, color: '#F5F5F5',
              fontWeight: 700,
            }}>असली कैश:</div>
            <div style={{
              fontFamily: 'Inter', fontSize: 38, color: '#F5F5F5',
              fontWeight: 900, letterSpacing: '-1px',
            }}>~ ₹ 2,80,000</div>
          </div>

          {/* Caption */}
          <div style={{
            marginTop: 18, fontFamily: 'Noto Sans Devanagari', fontSize: 22,
            color: '#163020', opacity: eyeOp * 0.7, fontStyle: 'italic',
            textAlign: 'center',
          }}>"अब आप इसे पहले की तरह नहीं देख पाएंगे"</div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7Depth;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8LegalTruth',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8LegalTruth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Truth tag
  const tagScale = sp(5, { damping: 9 });

  // Step labels
  const step1Op = interpolate(sp(35), [0, 1], [0, 1]);
  const step2Op = interpolate(sp(80), [0, 1], [0, 1]);
  const step3Op = interpolate(sp(125), [0, 1], [0, 1]);

  // Money flow
  const moneyX = interpolate(sp(60, { damping: 18 }), [0, 1], [0, 1]);

  // Owner flip
  const flipProgress = interpolate(frame, [140, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Legal stamp
  const stampScale = sp(200, { damping: 7, stiffness: 90 });
  const stampRot = interpolate(sp(200), [0, 1], [-25, -8]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#A27B5C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 40%, rgba(255,193,7,0.12) 0%, transparent 65%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '5%', right: '5%',
        width: 350, height: 350,
        border: '4px solid #163020', opacity: 0.06,
        borderRadius: 20,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '8%', left: '8%',
        width: 300, height: 300, borderRadius: '50%',
        background: '#FFC107', opacity: 0.05,
      }} />

      {/* Paper texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="paper8" width="100" height="100" patternUnits="userSpaceOnUse">
            <line x1="0" y1="50" x2="100" y2="50" stroke="#163020" strokeWidth="0.5" />
            <line x1="50" y1="0" x2="50" y2="100" stroke="#163020" strokeWidth="0.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#paper8)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i + 300) * 1920;
        const py = seededRandom(i + 310) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 12;
        return (
          <div key={i} style={{
            position: 'absolute', left: px + drift, top: py,
            width: 3, height: 3, borderRadius: '50%',
            background: '#163020', opacity: 0.3,
          }} />
        );
      })}

      {/* TOP - Title tag */}
      <div style={{
        position: 'absolute', top: 70, left: '50%',
        transform: `translateX(-50%) scale(${tagScale})`,
      }}>
        <div style={{
          background: '#163020', padding: '14px 50px',
          border: '3px solid #FFC107',
          display: 'flex', alignItems: 'center', gap: 18,
        }}>
          <div style={{
            width: 16, height: 16, borderRadius: '50%', background: '#ff0000',
            transform: `scale(${1 + Math.sin(frame * 0.2) * 0.2})`,
          }} />
          <div style={{
            fontFamily: 'Inter', fontSize: 30, color: '#FFC107',
            fontWeight: 800, letterSpacing: '5px',
          }}>BUNIYADI SACH — TRUTH #1</div>
        </div>
      </div>

      {/* Main 3-step flow */}
      <div style={{
        position: 'absolute', top: 220, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 30,
      }}>
        {/* Step 1 - You */}
        <div style={{
          opacity: step1Op,
          transform: `scale(${0.85 + step1Op * 0.15})`,
          width: 320, textAlign: 'center',
        }}>
          <div style={{
            width: 150, height: 150, borderRadius: '50%',
            background: '#163020', margin: '0 auto',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            border: '4px solid #FFC107',
          }}>
            <svg width="80" height="80" viewBox="0 0 24 24">
              <circle cx="12" cy="7" r="4" fill="#FFC107" />
              <path d="M 4 22 Q 12 14 20 22" fill="#FFC107" />
            </svg>
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 48, color: '#163020',
            marginTop: 18, letterSpacing: '-1px',
          }}>आप</div>
          <div style={{
            fontFamily: 'Inter', fontSize: 22, color: '#163020',
            letterSpacing: '2px', fontWeight: 700, opacity: 0.7, marginTop: 4,
          }}>DEPOSITOR</div>
        </div>

        {/* Arrow + Money */}
        <div style={{ position: 'relative', width: 280, height: 150 }}>
          <svg width="280" height="150">
            <line x1="20" y1="75" x2="260" y2="75" stroke="#163020" strokeWidth="5" strokeDasharray="8 6" />
            <polygon points="260,75 240,60 240,90" fill="#163020" />
          </svg>
          {/* Moving money */}
          <div style={{
            position: 'absolute', top: 30,
            left: 20 + moneyX * 220,
            background: '#FFC107', padding: '8px 18px',
            border: '3px solid #163020',
            fontFamily: 'Inter', fontSize: 26, color: '#163020',
            fontWeight: 900,
          }}>₹ ₹ ₹</div>
        </div>

        {/* Step 2 - Bank */}
        <div style={{
          opacity: step2Op,
          transform: `scale(${0.85 + step2Op * 0.15})`,
          width: 320, textAlign: 'center',
        }}>
          <div style={{
            width: 200, height: 150,
            background: '#163020',
            margin: '0 auto', position: 'relative',
            border: '4px solid #FFC107',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
          }}>
            <svg width="120" height="100" viewBox="0 0 100 80">
              <polygon points="10,30 50,10 90,30" fill="#FFC107" />
              <rect x="10" y="30" width="80" height="6" fill="#FFC107" />
              <rect x="20" y="36" width="8" height="35" fill="#FFC107" />
              <rect x="38" y="36" width="8" height="35" fill="#FFC107" />
              <rect x="54" y="36" width="8" height="35" fill="#FFC107" />
              <rect x="72" y="36" width="8" height="35" fill="#FFC107" />
              <rect x="10" y="71" width="80" height="5" fill="#FFC107" />
            </svg>
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 48, color: '#163020',
            marginTop: 18, letterSpacing: '-1px',
          }}>बैंक</div>
          <div style={{
            fontFamily: 'Inter', fontSize: 22, color: '#163020',
            letterSpacing: '2px', fontWeight: 700, opacity: 0.7, marginTop: 4,
          }}>NEW OWNER</div>
        </div>
      </div>

      {/* Ownership flip card */}
      <div style={{
        position: 'absolute', bottom: 180, left: '50%',
        transform: `translateX(-50%)`,
        display: 'flex', gap: 30, alignItems: 'center',
        opacity: step3Op,
      }}>
        {/* Before */}
        <div style={{
          background: 'rgba(22,48,32,0.15)', padding: '20px 35px',
          border: '2px dashed #163020',
          textDecoration: flipProgress > 0.3 ? 'line-through' : 'none',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 16, color: '#163020', letterSpacing: '3px', fontWeight: 700, opacity: 0.6 }}>PEHLE</div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 50, color: '#163020', letterSpacing: '-1px' }}>आपका पैसा</div>
        </div>

        {/* Arrow */}
        <div style={{
          fontFamily: 'Inter', fontSize: 60, color: '#ff0000', fontWeight: 900,
          transform: `scale(${1 + flipProgress * 0.3})`,
        }}>→</div>

        {/* After */}
        <div style={{
          background: '#ff0000', padding: '20px 35px',
          border: '3px solid #163020',
          opacity: flipProgress,
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 16, color: '#F5F5F5', letterSpacing: '3px', fontWeight: 700 }}>AB</div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 50, color: '#F5F5F5', letterSpacing: '-1px' }}>बैंक की संपत्ति</div>
        </div>
      </div>

      {/* Legal stamp */}
      <div style={{
        position: 'absolute', bottom: 80, right: 100,
        transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
        width: 200, height: 200, borderRadius: '50%',
        border: '5px solid #163020',
        display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
        background: 'rgba(22,48,32,0.1)',
      }}>
        <div style={{ fontFamily: 'Inter', fontSize: 16, color: '#163020', fontWeight: 900, letterSpacing: '2px' }}>SECTION 5(b)</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 32, color: '#163020', lineHeight: 1, marginTop: 4 }}>कानूनी</div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 32, color: '#163020', lineHeight: 1, marginTop: 2 }}>हकीकत</div>
        <div style={{ fontFamily: 'Inter', fontSize: 14, color: '#163020', fontWeight: 800, letterSpacing: '1.5px', marginTop: 6 }}>BR ACT 1949</div>
      </div>

      {/* Bottom statement */}
      <div style={{
        position: 'absolute', bottom: 90, left: 100,
        opacity: interpolate(sp(170), [0, 1], [0, 1]),
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 32, color: '#163020',
          fontWeight: 700, lineHeight: 1.2, maxWidth: 700,
        }}>
          "जमा करते ही पैसा <span style={{ color: '#ff0000', fontWeight: 900 }}>आपका नहीं रहता</span>"
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8LegalTruth;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9BankProperty',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9BankProperty: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Mega text impact
  const megaScale = sp(5, { damping: 8, stiffness: 90 });
  const shake = frame < 30 ? Math.sin(frame * 3) * 5 : 0;

  // Tag flip
  const tagFlip = interpolate(sp(50, { damping: 12 }), [0, 1], [0, 180]);

  // Receipt slide
  const receiptY = interpolate(sp(110, { damping: 14 }), [0, 1], [200, 0]);
  const receiptOp = sp(110);

  // Status changes
  const statusOp = interpolate(sp(170), [0, 1], [0, 1]);

  // Warning blink
  const blink = Math.sin(frame * 0.4) > 0 ? 1 : 0.5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A27B5C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, rgba(255,0,0,0.18) 0%, transparent 70%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '10%', left: '10%',
        width: 600, height: 600, borderRadius: '50%',
        border: '6px solid #163020', opacity: 0.04,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', right: '5%',
        width: 450, height: 450,
        background: '#FFC107', opacity: 0.06,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '40%', right: '15%',
        width: 250, height: 250, borderRadius: '50%',
        border: '3px solid #FFC107', opacity: 0.08,
        transform: `scale(${1 + Math.sin(frame * 0.06) * 0.08})`,
      }} />

      {/* Diagonal grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="diag9" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform="rotate(30)">
            <line x1="0" y1="0" x2="60" y2="0" stroke="#163020" strokeWidth="1" />
            <line x1="0" y1="30" x2="60" y2="30" stroke="#163020" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag9)" />
      </svg>

      {/* Explosion particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const angle = (i / 28) * Math.PI * 2;
        const dist = Math.min(frame * 6, 700);
        const px = 960 + Math.cos(angle) * dist;
        const py = 540 + Math.sin(angle) * dist;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 8, height: 8,
            background: i % 2 ? '#FFC107' : '#ff0000',
            opacity: interpolate(frame, [10, 80], [0.9, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            transform: `rotate(${frame * 8}deg)`,
          }} />
        );
      })}

      {/* Mega text - center */}
      <div style={{
        position: 'absolute', top: 0, left: 0, width: '100%', height: '100%',
        display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center',
        transform: `translateX(${shake}px)`,
      }}>
        {/* Pre label */}
        <div style={{
          fontFamily: 'Inter', fontSize: 30, color: '#ff0000',
          letterSpacing: '8px', fontWeight: 800, marginBottom: 20,
          opacity: blink,
        }}>━━ LEGAL FACT ━━</div>

        {/* Mega phrase */}
        <div style={{
          transform: `scale(${megaScale})`,
          textAlign: 'center', position: 'relative',
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 196, color: '#163020',
            fontWeight: 900, lineHeight: 0.95, letterSpacing: '-5px',
          }}>
            बैंक की
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 230, color: '#ff0000',
            fontWeight: 900, lineHeight: 0.95, letterSpacing: '-6px',
            textShadow: '8px 8px 0px #163020',
          }}>
            संपत्ति
          </div>
        </div>

        {/* Owner tag - flipping */}
        <div style={{
          marginTop: 35, perspective: 800,
        }}>
          <div style={{
            transform: `rotateY(${tagFlip}deg)`,
            transformStyle: 'preserve-3d',
            position: 'relative', width: 480, height: 70,
          }}>
            {/* Front - You */}
            <div style={{
              position: 'absolute', width: '100%', height: '100%',
              backfaceVisibility: 'hidden',
              background: '#163020', display: 'flex', alignItems: 'center', justifyContent: 'center',
              border: '3px solid #FFC107',
              fontFamily: 'Inter', fontSize: 32, color: '#FFC107', fontWeight: 800,
              letterSpacing: '4px',
            }}>OWNER: YOU</div>
            {/* Back - Bank */}
            <div style={{
              position: 'absolute', width: '100%', height: '100%',
              backfaceVisibility: 'hidden',
              transform: 'rotateY(180deg)',
              background: '#ff0000', display: 'flex', alignItems: 'center', justifyContent: 'center',
              border: '3px solid #163020',
              fontFamily: 'Inter', fontSize: 32, color: '#F5F5F5', fontWeight: 800,
              letterSpacing: '4px',
            }}>OWNER: BANK</div>
          </div>
        </div>
      </div>

      {/* Receipt LEFT */}
      <div style={{
        position: 'absolute', bottom: 60, left: 80,
        opacity: receiptOp, transform: `translateY(${receiptY}px) rotate(-6deg)`,
        background: '#F5F5F5', padding: 25, width: 380,
        border: '3px solid #163020',
        boxShadow: '8px 8px 0 #163020',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 16, color: '#163020',
          letterSpacing: '2px', fontWeight: 700, opacity: 0.7,
        }}>OWNERSHIP TRANSFER</div>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 14, color: '#163020',
          marginTop: 6, opacity: 0.5,
        }}>TXN ID: #DPST-{Math.floor(seededRandom(1) * 100000)}</div>
        <div style={{ height: 1, background: '#163020', opacity: 0.3, margin: '12px 0' }} />
        <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>
          <div style={{ fontFamily: 'Inter', fontSize: 17, color: '#163020' }}>FROM:</div>
          <div style={{ fontFamily: 'Inter', fontSize: 17, color: '#163020', fontWeight: 700 }}>आप (DEPOSITOR)</div>
        </div>
        <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>
          <div style={{ fontFamily: 'Inter', fontSize: 17, color: '#163020' }}>TO:</div>
          <div style={{ fontFamily: 'Inter', fontSize: 17, color: '#ff0000', fontWeight: 800 }}>BANK</div>
        </div>
        <div style={{ display: 'flex', justifyContent: 'space-between' }}>
          <div style={{ fontFamily: 'Inter', fontSize: 17, color: '#163020' }}>STATUS:</div>
          <div style={{ fontFamily: 'Inter', fontSize: 17, color: '#ff0000', fontWeight: 800 }}>TRANSFERRED ✓</div>
        </div>
      </div>

      {/* Status box RIGHT */}
      <div style={{
        position: 'absolute', bottom: 100, right: 80,
        opacity: statusOp,
        display: 'flex', flexDirection: 'column', gap: 12,
      }}>
        <div style={{
          background: '#163020', padding: '14px 26px',
          border: '2px solid #FFC107',
          display: 'flex', justifyContent: 'space-between', gap: 30, alignItems: 'center',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 19, color: '#9E9E9E', letterSpacing: '2px' }}>YOU</div>
          <div style={{ fontFamily: 'Inter', fontSize: 24, color: '#FFC107', fontWeight: 800 }}>CREDITOR</div>
        </div>
        <div style={{
          background: '#ff0000', padding: '14px 26px',
          border: '2px solid #163020',
          display: 'flex', justifyContent: 'space-between', gap: 30, alignItems: 'center',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 19, color: '#F5F5F5', letterSpacing: '2px' }}>BANK</div>
          <div style={{ fontFamily: 'Inter', fontSize: 24, color: '#F5F5F5', fontWeight: 800 }}>DEBTOR</div>
        </div>
        <div style={{
          marginTop: 8, fontFamily: 'Noto Sans Devanagari', fontSize: 22, color: '#163020',
          fontWeight: 600, textAlign: 'center',
        }}>आपका दर्जा बदल चुका है</div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 45%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9BankProperty;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10HundredRupee',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10HundredRupee: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title slide
  const titleX = interpolate(sp(5), [0, 1], [-200, 0]);
  const titleOp = sp(5);

  // ₹100 note appear
  const noteScale = sp(40, { damping: 9 });
  const noteRot = interpolate(sp(40), [0, 1], [-12, -3]);

  // Split happens
  const splitProgress = interpolate(frame, [110, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Pie chart fill
  const pieFill = interpolate(sp(170, { damping: 18 }), [0, 1], [0, 1]);

  // Donut counters
  const reserveCount = Math.floor(interpolate(frame, [180, 240], [0, 22], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const loanCount = Math.floor(interpolate(frame, [180, 240], [0, 78], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Loan flow
  const loanFlow = interpolate(sp(260), [0, 1], [0, 1]);

  // Calculation reveal
  const calcOp = interpolate(sp(310), [0, 1], [0, 1]);

  // Pie geometry
  const pieRadius = 130;
  const pieCx = 200;
  const pieCy = 200;
  const reserveAngle = 22 / 100 * 360 * pieFill;
  const reserveLargeArc = reserveAngle > 180 ? 1 : 0;
  const reserveX2 = pieCx + pieRadius * Math.sin(reserveAngle * Math.PI / 180);
  const reserveY2 = pieCy - pieRadius * Math.cos(reserveAngle * Math.PI / 180);

  return (
    <AbsoluteFill style={{ backgroundColor: '#A27B5C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(22,48,32,0.2) 0%, transparent 50%, rgba(255,193,7,0.1) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '5%', right: '5%',
        width: 350, height: 350, borderRadius: '50%',
        border: '3px solid #FFC107', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '8%', left: '5%',
        width: 400, height: 400,
        background: '#163020', opacity: 0.04,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Money grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="rupee10" width="120" height="120" patternUnits="userSpaceOnUse">
            <text x="60" y="70" fontFamily="Inter" fontSize="40" fill="#163020" textAnchor="middle">₹</text>
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#rupee10)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 400) * 1920;
        const py = seededRandom(i + 410) * 1080;
        const drift = Math.cos(frame * 0.025 + i) * 14;
        return (
          <div key={i} style={{
            position: 'absolute', left: px + drift, top: py,
            width: 3, height: 3, borderRadius: '50%',
            background: '#FFC107', opacity: 0.35,
          }} />
        );
      })}

      {/* Title TOP */}
      <div style={{
        position: 'absolute', top: 60, left: 80,
        opacity: titleOp, transform: `translateX(${titleX}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 26, color: '#FFC107',
          letterSpacing: '5px', fontWeight: 800,
        }}>// MATLAB KYA HAI</div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 76, color: '#163020',
          fontWeight: 900, lineHeight: 1, letterSpacing: '-2px', marginTop: 8,
        }}>
          हर ₹100 का <span style={{ color: '#ff0000' }}>बड़ा हिस्सा</span> लोन पर
        </div>
      </div>

      {/* LEFT - ₹100 Note */}
      <div style={{
        position: 'absolute', top: 260, left: 100,
        transform: `scale(${noteScale}) rotate(${noteRot}deg) translateX(${splitProgress * -100}px)`,
      }}>
        <div style={{
          width: 460, height: 220,
          background: 'linear-gradient(135deg, #163020 0%, #2D2D2D 100%)',
          border: '4px solid #FFC107',
          padding: 25, position: 'relative',
          boxShadow: '8px 8px 0 #163020',
        }}>
          {/* Corner symbols */}
          <div style={{
            position: 'absolute', top: 12, left: 18,
            fontFamily: 'Inter', fontSize: 28, color: '#FFC107', fontWeight: 800,
          }}>₹100</div>
          <div style={{
            position: 'absolute', top: 12, right: 18,
            fontFamily: 'Inter', fontSize: 28, color: '#FFC107', fontWeight: 800,
          }}>₹100</div>
          {/* Center */}
          <div style={{
            position: 'absolute', top: '50%', left: '50%',
            transform: 'translate(-50%, -50%)', textAlign: 'center',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 108, color: '#FFC107',
              fontWeight: 900, lineHeight: 1,
            }}>₹100</div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 22, color: '#F5F5F5',
              letterSpacing: '3px', marginTop: 4,
            }}>आपकी जमा राशि</div>
          </div>
          {/* Bottom corners */}
          <div style={{
            position: 'absolute', bottom: 12, left: 18,
            fontFamily: 'Inter', fontSize: 28, color: '#FFC107', fontWeight: 800,
          }}>₹100</div>
          <div style={{
            position: 'absolute', bottom: 12, right: 18,
            fontFamily: 'Inter', fontSize: 28, color: '#FFC107', fontWeight: 800,
          }}>₹100</div>
        </div>

        {/* Split arrow */}
        <svg width="200" height="80" style={{ marginTop: 30, marginLeft: 130, opacity: splitProgress }}>
          <line x1="100" y1="0" x2="100" y2={splitProgress * 50} stroke="#ff0000" strokeWidth="5" />
          <polygon points={`100,${splitProgress * 70} 80,${splitProgress * 50} 120,${splitProgress * 50}`} fill="#ff0000" />
        </svg>

        {/* Calculation */}
        <div style={{
          marginTop: 10, marginLeft: 30, opacity: calcOp,
          background: '#163020', padding: '16px 25px',
          border: '2px solid #FFC107', width: 400,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 28, color: '#FFC107',
            fontWeight: 800, letterSpacing: '1px',
          }}>₹100 = ₹22 + ₹78</div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 18, color: '#9E9E9E',
            marginTop: 6,
          }}>(रिजर्व + लोन में घुमाया गया पैसा)</div>
        </div>
      </div>

      {/* RIGHT - Pie Chart Breakdown */}
      <div style={{
        position: 'absolute', top: 230, right: 120,
        transform: `translateX(${(1 - pieFill) * 100}px)`,
        opacity: pieFill,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 22, color: '#163020',
          letterSpacing: '4px', fontWeight: 800, marginBottom: 15, textAlign: 'center',
        }}>BANK KA KHEL</div>

        <svg width="400" height="400" viewBox="0 0 400 400">
          {/* Background circle - loan portion */}
          <circle cx={pieCx} cy={pieCy} r={pieRadius} fill="#ff0000" />
          {/* Reserve slice */}
          {reserveAngle > 0 && (
            <path
              d={`M ${pieCx} ${pieCy} L ${pieCx} ${pieCy - pieRadius} A ${pieRadius} ${pieRadius} 0 ${reserveLargeArc} 1 ${reserveX2} ${reserveY2} Z`}
              fill="#FFC107"
            />
          )}
          {/* Inner circle - donut */}
          <circle cx={pieCx} cy={pieCy} r="55" fill="#A27B5C" />
          {/* Center label */}
          <text x={pieCx} y={pieCy - 4} fontFamily="Inter" fontSize="32" fill="#163020"
            fontWeight="900" textAnchor="middle">₹100</text>
          <text x={pieCx} y={pieCy + 22} fontFamily="Inter" fontSize="14" fill="#163020"
            fontWeight="700" textAnchor="middle" letterSpacing="2">DEPOSIT</text>
        </svg>

        {/* Legend */}
        <div style={{ marginTop: 20, display: 'flex', flexDirection: 'column', gap: 12 }}>
          <div style={{
            display: 'flex', alignItems: 'center', gap: 15,
            background: '#163020', padding: '14px 22px',
            border: '2px solid #FFC107',
          }}>
            <div style={{ width: 22, height: 22, background: '#FFC107' }} />
            <div style={{ flex: 1, fontFamily: 'Inter', fontSize: 22, color: '#F5F5F5', fontWeight: 700 }}>रिजर्व</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#FFC107', fontWeight: 900 }}>₹{reserveCount}</div>
          </div>
          <div style={{
            display: 'flex', alignItems: 'center', gap: 15,
            background: '#163020', padding: '14px 22px',
            border: '2px solid #ff0000',
          }}>
            <div style={{ width: 22, height: 22, background: '#ff0000' }} />
            <div style={{ flex: 1, fontFamily: 'Inter', fontSize: 22, color: '#F5F5F5', fontWeight: 700 }}>लोन पर</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#ff0000', fontWeight: 900 }}>₹{loanCount}</div>
          </div>
        </div>
      </div>

      {/* Bottom flow - loan distribution */}
      <div style={{
        position: 'absolute', bottom: 60, left: '50%',
        transform: 'translateX(-50%)',
        opacity: loanFlow,
        display: 'flex', alignItems: 'center', gap: 20,
      }}>
        <div style={{
          background: '#FFC107', padding: '12px 22px',
          fontFamily: 'Inter', fontSize: 22, color: '#163020', fontWeight: 800,
          letterSpacing: '2px',
        }}>₹78 OUT</div>
        <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#163020', fontWeight: 900 }}>→</div>
        <div style={{
          background: '#163020', padding: '12px 22px',
          fontFamily: 'Noto Sans Devanagari', fontSize: 22, color: '#F5F5F5', fontWeight: 700,
        }}>होम लोन</div>
        <div style={{
          background: '#163020', padding: '12px 22px',
          fontFamily: 'Noto Sans Devanagari', fontSize: 22, color: '#F5F5F5', fontWeight: 700,
        }}>कार लोन</div>
        <div style={{
          background: '#163020', padding: '12px 22px',
          fontFamily: 'Noto Sans Devanagari', fontSize: 22, color: '#F5F5F5', fontWeight: 700,
        }}>बिज़नेस लोन</div>
        <div style={{
          background: '#163020', padding: '12px 22px',
          fontFamily: 'Noto Sans Devanagari', fontSize: 22, color: '#F5F5F5', fontWeight: 700,
        }}>क्रेडिट कार्ड</div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10HundredRupee;
```

