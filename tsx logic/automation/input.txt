```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01Hook32',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01Hook32: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera push-in + micro drift
  const camScale = interpolate(frame, [0, durationInFrames], [1.03, 1.11], { easing: Easing.inOut(Easing.ease) });
  const camX = Math.sin(frame * 0.013) * 12;

  // Counter 0 -> 32
  const pct = Math.round(interpolate(sp(12), [0, 1], [0, 32]));
  const taxFill = interpolate(sp(46), [0, 1], [0, 33]);
  const impact = frame > 58 && frame < 72 ? Math.sin(frame * 2.9) * 7 : 0;

  const particles = new Array(26).fill(0).map((_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: (seededRandom(i + 9) * 1080 + frame * (0.5 + seededRandom(i + 3))) % 1080,
    r: 3 + seededRandom(i + 5) * 7,
    o: 0.12 + seededRandom(i + 7) * 0.22,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#00ADB5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 28% 22%, #14C6CE 0%, #00ADB5 46%, #00777D 100%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 120, top: 90, width: 760, height: 760, borderRadius: '50%',
          background: '#EEEEEE', opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06}) rotate(${frame * 0.08}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: -160, bottom: -180, width: 900, height: 900,
          background: '#222831', opacity: 0.06,
          transform: `rotate(${18 + Math.sin(frame * 0.015) * 5}deg)`,
        }} />
        <div style={{
          position: 'absolute', left: 780, top: 520, width: 0, height: 0,
          borderLeft: '210px solid transparent', borderRight: '210px solid transparent',
          borderBottom: '360px solid #EEEEEE', opacity: 0.04,
          transform: `rotate(${-frame * 0.12}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — halftone dot grid (punk print feel) */}
      <AbsoluteFill style={{ opacity: 0.16 }}>
        <svg width="1920" height="1080">
          {new Array(16).fill(0).map((_, r) =>
            new Array(28).fill(0).map((_, c) => (
              <circle
                key={`d-${r}-${c}`}
                cx={30 + c * 70}
                cy={30 + r * 70}
                r={2 + Math.sin((r + c) * 0.6 + frame * 0.05) * 1.8}
                fill="#222831"
              />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {particles.map((p, i) => (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: p.x, top: p.y, width: p.r, height: p.r,
            borderRadius: '50%', background: '#EEEEEE', opacity: p.o,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX + impact}px)` }}>

        {/* Torn collage strip behind the hero number */}
        <div style={{
          position: 'absolute', left: 100, top: 300, width: 820, height: 250,
          background: '#222831', opacity: interpolate(sp(6), [0, 1], [0, 0.92]),
          transform: `rotate(-3deg) scaleX(${sp(6)})`, transformOrigin: 'left center',
        }} />

        {/* Hero number — xerox offset print */}
        <div style={{ position: 'absolute', left: 140, top: 250, transform: `translateY(${interpolate(sp(12), [0, 1], [80, 0])}px)` }}>
          <div style={{
            position: 'absolute', left: 10, top: 12, fontFamily: 'Yantra One', fontSize: 221,
            color: '#00777D', lineHeight: 1, letterSpacing: -6,
          }}>{pct}%</div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 221, color: '#EEEEEE', lineHeight: 1, letterSpacing: -6,
          }}>{pct}%</div>
        </div>

        <div style={{
          position: 'absolute', left: 150, top: 545, fontFamily: 'Noto Sans Devanagari', fontWeight: 700,
          fontSize: 63, color: '#222831', opacity: sp(34),
          transform: `translateX(${interpolate(sp(34), [0, 1], [-60, 0])}px)`,
        }}>
          हर खरीद में छुपा हिस्सा
        </div>

        <div style={{
          position: 'absolute', left: 152, top: 640, width: interpolate(sp(44), [0, 1], [0, 520]),
          height: 8, background: '#222831',
        }} />

        {/* TV icon card with tax fill */}
        <div style={{
          position: 'absolute', right: 130, top: 230,
          transform: `scale(${sp(26)}) rotate(${2 + Math.sin(frame * 0.03) * 1.2}deg)`,
          transformOrigin: 'center',
        }}>
          <div style={{
            width: 620, height: 500, background: '#EEEEEE', borderRadius: 18,
            border: '10px solid #222831', boxShadow: '18px 18px 0px #00777D', padding: 26,
          }}>
            <svg width="560" height="360" viewBox="0 0 560 360">
              <rect x="20" y="14" width="520" height="290" rx="14" fill="#222831" />
              <rect x="42" y="36" width="476" height="246" rx="8" fill="#00ADB5" />
              <rect
                x="42" y={282 - (246 * taxFill) / 33} width="476" height={(246 * taxFill) / 33}
                fill="#222831" opacity={0.85}
              />
              <rect x="240" y="304" width="80" height="26" fill="#222831" />
              <rect x="180" y="330" width="200" height="18" rx="9" fill="#222831" />
            </svg>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 34, color: '#222831',
              marginTop: 4, textAlign: 'center', letterSpacing: 2,
            }}>
              PRICE = PRODUCT + TAX
            </div>
          </div>
          <div style={{
            position: 'absolute', top: -34, right: -26, background: '#222831', color: '#EEEEEE',
            fontFamily: 'Inter', fontWeight: 700, fontSize: 30, padding: '10px 22px',
            transform: `rotate(${6 + Math.sin(frame * 0.06) * 2}deg)`,
          }}>
            1/3 = GST
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 400px rgba(34,40,49,0.5)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 19,
        fontWeight: 600, color: '#EEEEEE', opacity: 0.35,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene01Hook32;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02BillSplit',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02BillSplit: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan left -> right
  const camX = interpolate(frame, [0, durationInFrames], [40, -40], { easing: Easing.inOut(Easing.ease) });
  const camScale = interpolate(frame, [0, durationInFrames], [1.08, 1.0]);

  const total = Math.round(interpolate(sp(10), [0, 1], [0, 24000]));
  const goods = Math.round(interpolate(sp(70), [0, 1], [0, 18500]));
  const tax = Math.round(interpolate(sp(90), [0, 1], [0, 5500]));

  // Card swipe travel
  const swipe = interpolate(frame, [150, 190], [-420, 460], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  const flakes = new Array(22).fill(0).map((_, i) => ({
    x: (seededRandom(i + 2) * 1920 + frame * 0.6) % 1920,
    y: seededRandom(i + 6) * 1080,
    s: 6 + seededRandom(i + 11) * 12,
    rot: frame * (0.4 + seededRandom(i + 4)),
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#00ADB5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #00C2CB 0%, #00ADB5 52%, #006F75 100%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: -140, bottom: -120, width: 720, height: 720, borderRadius: 60,
          background: '#EEEEEE', opacity: 0.05, transform: `rotate(${25 + frame * 0.05}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: 200, top: -200, width: 620, height: 620, borderRadius: '50%',
          border: '60px solid #222831', opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.024) * 0.07})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — diagonal stripe field */}
      <AbsoluteFill style={{
        opacity: 0.1,
        backgroundImage: 'repeating-linear-gradient(45deg, #222831 0px, #222831 6px, transparent 6px, transparent 34px)',
        transform: `translateX(${(frame * 0.8) % 40}px)`,
      }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {flakes.map((f, i) => (
          <div key={`f-${i}`} style={{
            position: 'absolute', left: f.x, top: f.y, width: f.s, height: f.s,
            background: '#EEEEEE', opacity: 0.18, transform: `rotate(${f.rot}deg)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px) scale(${camScale})` }}>

        {/* Receipt card */}
        <div style={{
          position: 'absolute', left: 150, top: 190, width: 600,
          background: '#EEEEEE', border: '9px solid #222831', boxShadow: '20px 20px 0 #006F75',
          padding: '34px 38px',
          transform: `rotate(-2.5deg) translateY(${interpolate(sp(8), [0, 1], [120, 0])}px)`,
          opacity: sp(8),
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 30, color: '#00777D', letterSpacing: 4 }}>INVOICE / बिल</div>
          <div style={{ height: 5, background: '#222831', margin: '18px 0 26px' }} />
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 51, color: '#222831' }}>
            एयर कंडीशनर
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 119, color: '#222831', lineHeight: 1.1, marginTop: 10,
          }}>
            ₹{total.toLocaleString('en-IN')}
          </div>
          <div style={{
            marginTop: 22, borderTop: '4px dashed #222831', paddingTop: 18,
            fontFamily: 'JetBrains Mono', fontSize: 28, color: '#222831', opacity: 0.75,
          }}>
            GST INCLUDED — 28%
          </div>
        </div>

        {/* Split arrow */}
        <svg width="180" height="120" style={{ position: 'absolute', left: 790, top: 470, opacity: sp(60) }} viewBox="0 0 180 120">
          <path d="M0 60 H120" stroke="#222831" strokeWidth="12" />
          <path d="M110 30 L165 60 L110 90 Z" fill="#222831" />
        </svg>

        {/* Split cards */}
        <div style={{
          position: 'absolute', right: 170, top: 200, width: 560, background: '#222831',
          border: '8px solid #EEEEEE', padding: '30px 34px',
          transform: `translateX(${interpolate(sp(70), [0, 1], [200, 0])}px) rotate(2deg)`, opacity: sp(70),
        }}>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 44, color: '#00ADB5' }}>असली सामान</div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 98, color: '#EEEEEE', lineHeight: 1.15 }}>
            ₹{goods.toLocaleString('en-IN')}
          </div>
        </div>

        <div style={{
          position: 'absolute', right: 170, top: 470, width: 560, background: '#EEEEEE',
          border: '8px solid #222831', padding: '30px 34px',
          transform: `translateX(${interpolate(sp(90), [0, 1], [200, 0])}px) rotate(-1.5deg)`, opacity: sp(90),
        }}>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 44, color: '#00777D' }}>सरकार का हिस्सा</div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 98, color: '#222831', lineHeight: 1.15 }}>
            ₹{tax.toLocaleString('en-IN')}
          </div>
        </div>

        {/* Card swipe machine — micro animation */}
        <div style={{ position: 'absolute', left: 620, bottom: 90, opacity: sp(140) }}>
          <svg width="420" height="200" viewBox="0 0 420 200">
            <rect x="60" y="60" width="300" height="130" rx="16" fill="#222831" />
            <rect x="90" y="86" width="240" height="10" rx="5" fill="#00ADB5" />
            <rect x="90" y="112" width="150" height="10" rx="5" fill="#EEEEEE" opacity="0.5" />
            <g transform={`translate(${swipe}, 0)`}>
              <rect x="120" y="0" width="200" height="120" rx="12" fill="#EEEEEE" stroke="#222831" strokeWidth="7" />
              <rect x="138" y="26" width="52" height="38" rx="6" fill="#00ADB5" />
              <rect x="138" y="80" width="150" height="10" rx="5" fill="#222831" opacity="0.6" />
            </g>
          </svg>
        </div>

        <div style={{
          position: 'absolute', left: 150, bottom: 70, fontFamily: 'Noto Sans Devanagari',
          fontWeight: 700, fontSize: 46, color: '#EEEEEE', opacity: sp(160),
        }}>
          और हम चुपचाप स्वाइप कर देते थे
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 380px rgba(34,40,49,0.5)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 19,
        fontWeight: 600, color: '#EEEEEE', opacity: 0.35,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02BillSplit;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03HiddenTrap',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03HiddenTrap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt-orbit
  const camRot = Math.sin(frame * 0.01) * 1.4;
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.1], { easing: Easing.inOut(Easing.ease) });

  // Beat 1: STOP  |  Beat 2: budget house  |  Beat 3: 40% stamp
  const stopOut = interpolate(frame, [100, 125], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const crack = interpolate(sp(150), [0, 1], [0, 1]);
  const stampRot = interpolate(sp(250, { damping: 8, stiffness: 90 }), [0, 1], [-28, -9]);
  const shake = frame > 250 && frame < 266 ? Math.sin(frame * 3.4) * 9 : 0;

  const dust = new Array(30).fill(0).map((_, i) => ({
    x: seededRandom(i + 13) * 1920,
    y: (seededRandom(i + 17) * 1080 - frame * (0.7 + seededRandom(i + 21))) % 1080,
    r: 2 + seededRandom(i + 31) * 6,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#00ADB5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 70%, #00C7D0 0%, #00ADB5 40%, #00666B 100%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 620, top: 120, width: 800, height: 800,
          border: '90px solid #222831', borderRadius: '50%', opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.018) * 0.08})`,
        }} />
        <div style={{
          position: 'absolute', left: -120, top: 380, width: 640, height: 380,
          background: '#EEEEEE', opacity: 0.04, transform: `rotate(${-12 + frame * 0.04}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — concentric warning rings */}
      <AbsoluteFill style={{ opacity: 0.12 }}>
        <svg width="1920" height="1080">
          {new Array(7).fill(0).map((_, i) => (
            <circle
              key={`ring-${i}`}
              cx={960} cy={540}
              r={((frame * 2.2 + i * 130) % 900) + 60}
              stroke="#222831" strokeWidth="5" fill="none"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {dust.map((d, i) => (
          <div key={`d-${i}`} style={{
            position: 'absolute', left: d.x, top: d.y, width: d.r, height: d.r,
            borderRadius: '50%', background: '#EEEEEE', opacity: 0.22,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg) translateX(${shake}px)` }}>

        {/* Beat 1 — STOP block */}
        <div style={{
          position: 'absolute', left: 0, right: 0, top: 330, textAlign: 'center',
          opacity: stopOut, transform: `scale(${interpolate(sp(4), [0, 1], [0.6, 1])})`,
        }}>
          <div style={{
            display: 'inline-block', background: '#222831', padding: '26px 70px',
            transform: 'rotate(-2deg)', boxShadow: '16px 16px 0 #EEEEEE',
          }}>
            <span style={{ fontFamily: 'Yantra One', fontSize: 187, color: '#EEEEEE', lineHeight: 1 }}>
              अब रुकिये
            </span>
          </div>
        </div>

        {/* Beat 2 — Household budget card with crack */}
        <div style={{
          position: 'absolute', left: 170, top: 250, opacity: sp(130),
          transform: `translateY(${interpolate(sp(130), [0, 1], [90, 0])}px) rotate(-2deg)`,
        }}>
          <div style={{
            width: 640, height: 520, background: '#EEEEEE', border: '10px solid #222831',
            boxShadow: '18px 18px 0 #00666B', padding: 30,
          }}>
            <svg width="580" height="330" viewBox="0 0 580 330">
              <path d="M290 30 L540 190 H40 Z" fill="#222831" />
              <rect x="90" y="185" width="400" height="130" fill="#00ADB5" stroke="#222831" strokeWidth="10" />
              <rect x="250" y="230" width="80" height="85" fill="#222831" />
              <path
                d={`M290 30 L${290 - 40 * crack} 200 L${290 + 30 * crack} 250 L${290 - 20 * crack} 315`}
                stroke="#EEEEEE" strokeWidth={10 * crack} fill="none" strokeLinecap="round"
              />
            </svg>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 51,
              color: '#222831', textAlign: 'center', marginTop: 12,
            }}>
              घर का बजट
            </div>
          </div>
        </div>

        {/* Beat 3 — the 40% trap stamp */}
        <div style={{
          position: 'absolute', right: 150, top: 300, opacity: sp(240),
          transform: `scale(${interpolate(sp(240), [0, 1], [1.9, 1])}) rotate(${stampRot}deg)`,
        }}>
          <div style={{
            border: '14px solid #222831', background: '#00ADB5', padding: '30px 54px',
            boxShadow: '0 0 0 12px #EEEEEE',
          }}>
            <div style={{ fontFamily: 'Yantra One', fontSize: 204, color: '#EEEEEE', lineHeight: 1 }}>40%</div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 55,
              color: '#222831', textAlign: 'center', letterSpacing: 2,
            }}>
              नया स्लैब
            </div>
          </div>
          <div style={{
            position: 'absolute', bottom: -44, left: 20, fontFamily: 'JetBrains Mono',
            fontSize: 30, color: '#EEEEEE', letterSpacing: 4, opacity: 0.8,
          }}>
            NOBODY IS TALKING ABOUT THIS
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 420px rgba(34,40,49,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 19,
        fontWeight: 600, color: '#EEEEEE', opacity: 0.35,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03HiddenTrap;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04LedgerOpen',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04LedgerOpen: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow tilt-down reveal
  const camY = interpolate(frame, [0, durationInFrames], [-24, 18], { easing: Easing.inOut(Easing.ease) });
  const camScale = interpolate(frame, [0, durationInFrames], [1.09, 1.02]);

  // Ledger cover open (rotateY)
  const openDeg = interpolate(sp(10), [0, 1], [-88, 0]);
  const ticker = -((frame * 3.2) % 1200);

  const rows = ['TV', 'AC', 'CAR', 'MEDICINE', 'INSURANCE'];

  const bits = new Array(24).fill(0).map((_, i) => ({
    x: seededRandom(i + 41) * 1920,
    y: (seededRandom(i + 43) * 1080 + frame * (0.4 + seededRandom(i + 47) * 1.2)) % 1080,
    s: 4 + seededRandom(i + 53) * 9,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#00ADB5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(200deg, #00CBD4 0%, #00ADB5 55%, #00747A 100%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: -100, top: 60, width: 700, height: 700,
          background: '#222831', opacity: 0.06, borderRadius: 80,
          transform: `rotate(${-14 + Math.sin(frame * 0.02) * 4}deg)`,
        }} />
        <div style={{
          position: 'absolute', left: 60, bottom: -220, width: 620, height: 620, borderRadius: '50%',
          background: '#EEEEEE', opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — ledger grid */}
      <AbsoluteFill style={{
        opacity: 0.12,
        backgroundImage:
          'linear-gradient(#222831 2px, transparent 2px), linear-gradient(90deg, #222831 2px, transparent 2px)',
        backgroundSize: '90px 90px',
        transform: `translateY(${(frame * 0.5) % 90}px)`,
      }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {bits.map((b, i) => (
          <div key={`b-${i}`} style={{
            position: 'absolute', left: b.x, top: b.y, width: b.s, height: b.s,
            background: '#EEEEEE', opacity: 0.2, borderRadius: 2,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateY(${camY}px) scale(${camScale})`, perspective: 1400 }}>

        {/* Opening ledger */}
        <div style={{
          position: 'absolute', left: 140, top: 190, width: 760, height: 620,
          background: '#EEEEEE', border: '10px solid #222831', boxShadow: '20px 20px 0 #00747A',
          transformOrigin: 'left center', transform: `rotateY(${openDeg}deg)`, padding: 36,
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 30, letterSpacing: 5, color: '#00777D' }}>
            HISAAB — KITAAB
          </div>
          <div style={{ height: 6, background: '#222831', margin: '16px 0 20px' }} />
          {rows.map((r, i) => (
            <div key={`r-${i}`} style={{
              display: 'flex', justifyContent: 'space-between', alignItems: 'center',
              borderBottom: '3px dashed #222831', padding: '14px 0',
              opacity: sp(40 + i * 12),
              transform: `translateX(${interpolate(sp(40 + i * 12), [0, 1], [-50, 0])}px)`,
            }}>
              <span style={{ fontFamily: 'Inter', fontWeight: 700, fontSize: 46, color: '#222831' }}>{r}</span>
              <span style={{ fontFamily: 'JetBrains Mono', fontSize: 40, color: '#00777D' }}>%</span>
            </div>
          ))}
        </div>

        {/* Name + channel lockup */}
        <div style={{ position: 'absolute', right: 130, top: 250, textAlign: 'right' }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 63, color: '#222831',
            opacity: sp(60), transform: `translateY(${interpolate(sp(60), [0, 1], [40, 0])}px)`,
          }}>
            मैं कुनाल
          </div>
          <div style={{
            display: 'inline-block', background: '#222831', padding: '18px 34px', marginTop: 18,
            transform: `rotate(1.5deg) scale(${sp(80)})`,
          }}>
            <span style={{ fontFamily: 'Yantra One', fontSize: 102, color: '#EEEEEE', lineHeight: 1 }}>
              प्रो-बफेट HQ
            </span>
          </div>

          {/* Percent badge with breathing micro-motion */}
          <div style={{
            marginTop: 44, display: 'inline-block', border: '9px solid #EEEEEE', padding: '18px 40px',
            transform: `scale(${1 + Math.sin(frame * 0.08) * 0.03}) rotate(-2deg)`, opacity: sp(110),
          }}>
            <span style={{ fontFamily: 'Yantra One', fontSize: 90, color: '#EEEEEE' }}>हर 1% = आपकी जेब</span>
          </div>
        </div>

        {/* Bottom ticker strip */}
        <div style={{
          position: 'absolute', left: 0, right: 0, bottom: 66, height: 80, background: '#222831',
          overflow: 'hidden', display: 'flex', alignItems: 'center', opacity: sp(30),
        }}>
          <div style={{
            whiteSpace: 'nowrap', transform: `translateX(${ticker}px)`,
            fontFamily: 'JetBrains Mono', fontSize: 36, color: '#00ADB5', letterSpacing: 6,
          }}>
            GST 2.0 // 5% // 18% // 40% // GST 2.0 // 5% // 18% // 40% // GST 2.0 // 5% // 18% // 40% //
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 400px rgba(34,40,49,0.5)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 19,
        fontWeight: 600, color: '#EEEEEE', opacity: 0.35,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04LedgerOpen;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05ThreeRules',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05ThreeRules: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: orbit-ish sway
  const camRot = Math.sin(frame * 0.015) * 1.8;
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1.0]);

  const cards = [
    { title: 'बिना लाग-लपेट', tag: 'NO FLUFF', delay: 20 },
    { title: 'बिना डराए', tag: 'NO FEAR', delay: 45 },
    { title: 'सीधे-सीधे', tag: 'STRAIGHT UP', delay: 70 },
  ];

  const spark = new Array(20).fill(0).map((_, i) => ({
    x: seededRandom(i + 61) * 1920,
    y: (seededRandom(i + 67) * 1080 - frame * (0.6 + seededRandom(i + 71))) % 1080,
    r: 3 + seededRandom(i + 73) * 6,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#00ADB5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at 50% 110%, #00D2DB 0%, #00ADB5 45%, #006B71 100%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        {new Array(3).fill(0).map((_, i) => (
          <div key={`shape-${i}`} style={{
            position: 'absolute',
            left: 120 + i * 640,
            top: 120 + Math.sin(frame * 0.02 + i) * 30,
            width: 460, height: 460,
            background: i % 2 === 0 ? '#EEEEEE' : '#222831',
            opacity: 0.05,
            borderRadius: i === 1 ? '50%' : 40,
            transform: `rotate(${frame * 0.06 * (i + 1)}deg)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Motion texture — vertical scanlines */}
      <AbsoluteFill style={{
        opacity: 0.1,
        backgroundImage: 'repeating-linear-gradient(90deg, #222831 0px, #222831 3px, transparent 3px, transparent 26px)',
        transform: `translateX(${-(frame * 0.6) % 26}px)`,
      }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {spark.map((s, i) => (
          <div key={`s-${i}`} style={{
            position: 'absolute', left: s.x, top: s.y, width: s.r, height: s.r,
            borderRadius: '50%', background: '#EEEEEE', opacity: 0.22,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>

        <div style={{
          position: 'absolute', left: 0, right: 0, top: 120, textAlign: 'center',
          opacity: sp(4), transform: `translateY(${interpolate(sp(4), [0, 1], [-50, 0])}px)`,
        }}>
          <span style={{
            fontFamily: 'Yantra One', fontSize: 111, color: '#222831',
            background: '#EEEEEE', padding: '12px 40px', display: 'inline-block', transform: 'rotate(-1deg)',
          }}>
            मेरी एक ही जिद
          </span>
        </div>

        {/* Flip cards */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 340, display: 'flex', justifyContent: 'center', gap: 60, perspective: 1600 }}>
          {cards.map((c, i) => {
            const flip = interpolate(sp(c.delay), [0, 1], [92, 0]);
            const bob = Math.sin(frame * 0.05 + i) * 8;
            return (
              <div key={`c-${i}`} style={{
                width: 480, height: 520, background: i === 1 ? '#222831' : '#EEEEEE',
                border: `10px solid ${i === 1 ? '#EEEEEE' : '#222831'}`,
                boxShadow: '16px 16px 0 #006B71',
                transform: `rotateY(${flip}deg) translateY(${bob}px)`,
                opacity: sp(c.delay),
                padding: 34, display: 'flex', flexDirection: 'column', justifyContent: 'space-between',
              }}>
                <svg width="150" height="150" viewBox="0 0 100 100">
                  {i === 0 && (
                    <g stroke={i === 1 ? '#00ADB5' : '#222831'} strokeWidth="8" fill="none" strokeLinecap="round">
                      <path d="M15 50 H70" />
                      <path d="M55 32 L75 50 L55 68" />
                      <circle cx="86" cy="50" r="7" fill="#00ADB5" stroke="none" />
                    </g>
                  )}
                  {i === 1 && (
                    <g>
                      <path d="M50 10 L86 26 V52 C86 74 68 86 50 92 C32 86 14 74 14 52 V26 Z"
                        fill="none" stroke="#00ADB5" strokeWidth="8" />
                      <path d="M34 50 L46 62 L68 38" fill="none" stroke="#EEEEEE" strokeWidth="8" strokeLinecap="round" />
                    </g>
                  )}
                  {i === 2 && (
                    <g stroke="#222831" strokeWidth="8" fill="none" strokeLinecap="round">
                      <path d="M12 78 L88 22" />
                      <path d="M62 22 H88 V48" />
                    </g>
                  )}
                </svg>
                <div>
                  <div style={{
                    fontFamily: 'JetBrains Mono', fontSize: 28, letterSpacing: 4,
                    color: i === 1 ? '#00ADB5' : '#00777D', marginBottom: 12,
                  }}>{c.tag}</div>
                  <div style={{
                    fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 68,
                    color: i === 1 ? '#EEEEEE' : '#222831', lineHeight: 1.15,
                  }}>{c.title}</div>
                </div>
              </div>
            );
          })}
        </div>

        <div style={{
          position: 'absolute', left: 0, right: 0, bottom: 78, textAlign: 'center',
          fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 51, color: '#EEEEEE',
          opacity: sp(105),
        }}>
          जो चीज आपके पैसे से जुड़ी हो — वही सीधा आपके सामने
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 380px rgba(34,40,49,0.5)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 19,
        fontWeight: 600, color: '#EEEEEE', opacity: 0.35,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05ThreeRules;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06BalanceTilt',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06BalanceTilt: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom-out reveal
  const camScale = interpolate(frame, [0, durationInFrames], [1.16, 1.0], { easing: Easing.out(Easing.cubic) });

  // Scale tilts toward the buyer at frame ~90
  const tilt = interpolate(sp(90, { damping: 9, stiffness: 70 }), [0, 1], [12, -13]);
  const idle = Math.sin(frame * 0.05) * 1.2;

  const coins = new Array(18).fill(0).map((_, i) => ({
    x: seededRandom(i + 83) * 1920,
    y: (seededRandom(i + 89) * 1080 + frame * (0.8 + seededRandom(i + 97))) % 1080,
    r: 8 + seededRandom(i + 101) * 12,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#00ADB5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(0deg, #006F75 0%, #00ADB5 50%, #00C9D2 100%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 300, top: 100, width: 1320, height: 820,
          border: '70px solid #EEEEEE', opacity: 0.04, borderRadius: 90,
          transform: `rotate(${Math.sin(frame * 0.012) * 3}deg)`,
        }} />
        <div style={{
          position: 'absolute', left: -180, top: 240, width: 520, height: 520,
          background: '#222831', opacity: 0.06, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — moving crosshatch */}
      <AbsoluteFill style={{
        opacity: 0.1,
        backgroundImage:
          'repeating-linear-gradient(60deg, #222831 0 3px, transparent 3px 30px), repeating-linear-gradient(-60deg, #222831 0 3px, transparent 3px 30px)',
        transform: `translate(${(frame * 0.4) % 30}px, ${(frame * 0.3) % 30}px)`,
      }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {coins.map((c, i) => (
          <div key={`c-${i}`} style={{
            position: 'absolute', left: c.x, top: c.y, width: c.r, height: c.r,
            borderRadius: '50%', border: '2px solid #EEEEEE', opacity: 0.2,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        <div style={{
          position: 'absolute', left: 0, right: 0, top: 90, textAlign: 'center', opacity: sp(6),
        }}>
          <span style={{
            fontFamily: 'Yantra One', fontSize: 119, color: '#EEEEEE',
            textShadow: '8px 8px 0 #00666B',
          }}>
            नंबर किसकी तरफ?
          </span>
        </div>

        {/* Balance scale */}
        <div style={{
          position: 'absolute', left: 0, right: 0, top: 290, display: 'flex', justifyContent: 'center',
          opacity: sp(16),
        }}>
          <svg width="1200" height="520" viewBox="0 0 1200 520">
            <rect x="580" y="150" width="40" height="300" fill="#222831" />
            <rect x="470" y="450" width="260" height="34" rx="12" fill="#222831" />
            <g transform={`rotate(${tilt + idle}, 600, 150)`}>
              <rect x="180" y="134" width="840" height="32" rx="16" fill="#EEEEEE" />
              <line x1="240" y1="166" x2="240" y2="270" stroke="#EEEEEE" strokeWidth="10" />
              <line x1="960" y1="166" x2="960" y2="270" stroke="#EEEEEE" strokeWidth="10" />
              <rect x="90" y="270" width="300" height="26" rx="13" fill="#222831" />
              <rect x="810" y="270" width="300" height="26" rx="13" fill="#222831" />
              <text x="240" y="360" textAnchor="middle" fontFamily="Yantra One" fontSize="76" fill="#222831">आप</text>
              <text x="960" y="360" textAnchor="middle" fontFamily="Yantra One" fontSize="76" fill="#EEEEEE" opacity="0.55">दुकानदार</text>
            </g>
            <circle cx="600" cy="150" r="26" fill="#EEEEEE" />
          </svg>
        </div>

        {/* Verdict cards */}
        <div style={{
          position: 'absolute', left: 200, bottom: 90, background: '#EEEEEE', border: '9px solid #222831',
          padding: '22px 40px', transform: `rotate(-2deg) scale(${sp(110)})`, boxShadow: '14px 14px 0 #006F75',
        }}>
          <span style={{ fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 55, color: '#222831' }}>
            फायदा आपका
          </span>
        </div>
        <div style={{
          position: 'absolute', right: 200, bottom: 90, background: '#222831', border: '9px solid #EEEEEE',
          padding: '22px 40px', transform: `rotate(2deg) scale(${sp(125)})`, opacity: 0.85,
        }}>
          <span style={{ fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 55, color: '#EEEEEE' }}>
            बेचने वाले का नहीं
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 400px rgba(34,40,49,0.5)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 19,
        fontWeight: 600, color: '#EEEEEE', opacity: 0.35,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06BalanceTilt;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07StructureFlip',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07StructureFlip: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: whole board rotates as the structure "flips"
  const boardRot = interpolate(sp(60, { damping: 10, stiffness: 60 }), [0, 1], [0, 180]);
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.1], { easing: Easing.inOut(Easing.ease) });
  const jolt = frame > 60 && frame < 76 ? Math.sin(frame * 3.1) * 8 : 0;

  const tiles = new Array(12).fill(0).map((_, i) => ({
    delay: 10 + i * 4,
    label: ['5', '12', '18', '28', 'CESS', 'ITC', 'HSN', 'SLAB', 'RATE', 'GST', 'CGST', 'IGST'][i],
  }));

  const dots = new Array(24).fill(0).map((_, i) => ({
    x: (seededRandom(i + 111) * 1920 + frame * 0.9) % 1920,
    y: seededRandom(i + 113) * 1080,
    r: 3 + seededRandom(i + 127) * 7,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#00ADB5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 40%, #00D6DF 0%, #00ADB5 45%, #00646A 100%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 700, top: 200, width: 560, height: 560,
          background: '#EEEEEE', opacity: 0.05, transform: `rotate(${frame * 0.2}deg)`,
        }} />
        <div style={{
          position: 'absolute', left: 140, bottom: 60, width: 420, height: 420, borderRadius: '50%',
          background: '#222831', opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.035) * 0.09})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — rotating radial spokes */}
      <AbsoluteFill style={{ opacity: 0.1 }}>
        <svg width="1920" height="1080">
          <g transform={`rotate(${frame * 0.25}, 960, 540)`}>
            {new Array(24).fill(0).map((_, i) => (
              <line
                key={`sp-${i}`}
                x1="960" y1="540"
                x2={960 + Math.cos((i * Math.PI) / 12) * 1400}
                y2={540 + Math.sin((i * Math.PI) / 12) * 1400}
                stroke="#222831" strokeWidth="6"
              />
            ))}
          </g>
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {dots.map((d, i) => (
          <div key={`dt-${i}`} style={{
            position: 'absolute', left: d.x, top: d.y, width: d.r, height: d.r,
            background: '#EEEEEE', opacity: 0.2, borderRadius: 2,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${jolt}px)` }}>

        {/* Flipping tile board */}
        <div style={{
          position: 'absolute', left: 0, right: 0, top: 230,
          display: 'flex', flexWrap: 'wrap', justifyContent: 'center', gap: 22, width: 1180, margin: '0 auto',
          transform: `rotate(${boardRot}deg)`, transformOrigin: 'center',
        }}>
          {tiles.map((t, i) => (
            <div key={`t-${i}`} style={{
              width: 270, height: 150, background: i % 3 === 0 ? '#222831' : '#EEEEEE',
              border: `8px solid ${i % 3 === 0 ? '#EEEEEE' : '#222831'}`,
              display: 'flex', alignItems: 'center', justifyContent: 'center',
              transform: `scale(${sp(t.delay)}) rotate(${Math.sin(frame * 0.04 + i) * 2}deg)`,
              opacity: sp(t.delay),
            }}>
              <span style={{
                fontFamily: 'JetBrains Mono', fontSize: 63, fontWeight: 700,
                color: i % 3 === 0 ? '#00ADB5' : '#222831',
              }}>{t.label}</span>
            </div>
          ))}
        </div>

        {/* Headline stamp */}
        <div style={{
          position: 'absolute', left: 0, right: 0, top: 430, textAlign: 'center',
          opacity: sp(80), transform: `scale(${interpolate(sp(80), [0, 1], [1.6, 1])})`,
        }}>
          <span style={{
            fontFamily: 'Yantra One', fontSize: 170, color: '#EEEEEE', background: '#222831',
            padding: '16px 56px', display: 'inline-block', transform: 'rotate(-2deg)',
            boxShadow: '20px 20px 0 rgba(0,0,0,0.25)',
          }}>
            उलट-पुलट
          </span>
        </div>

        {/* Loop arrow icon */}
        <svg width="200" height="200" viewBox="0 0 100 100" style={{
          position: 'absolute', right: 190, bottom: 130, opacity: sp(100),
          transform: `rotate(${frame * 1.6}deg)`,
        }}>
          <path d="M50 12 A38 38 0 1 1 12 50" stroke="#EEEEEE" strokeWidth="10" fill="none" strokeLinecap="round" />
          <path d="M50 0 L50 26 L28 13 Z" fill="#EEEEEE" />
        </svg>

        <div style={{
          position: 'absolute', left: 160, bottom: 100, fontFamily: 'Noto Sans Devanagari',
          fontWeight: 700, fontSize: 55, color: '#222831', opacity: sp(110),
        }}>
          पूरे देश का टैक्स ढांचा बदल गया
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 400px rgba(34,40,49,0.5)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 19,
        fontWeight: 600, color: '#EEEEEE', opacity: 0.35,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07StructureFlip;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08FourSlabs',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08FourSlabs: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan up along the bar chart
  const camY = interpolate(frame, [0, durationInFrames], [30, -26], { easing: Easing.inOut(Easing.ease) });
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.01]);

  const bars = [
    { val: 5, h: 180, delay: 24 },
    { val: 12, h: 320, delay: 44 },
    { val: 18, h: 460, delay: 64 },
    { val: 28, h: 620, delay: 84 },
  ];

  const motes = new Array(22).fill(0).map((_, i) => ({
    x: seededRandom(i + 131) * 1920,
    y: (seededRandom(i + 137) * 1080 - frame * (0.5 + seededRandom(i + 139))) % 1080,
    r: 4 + seededRandom(i + 149) * 8,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#00ADB5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #00C6CF 0%, #00ADB5 48%, #00696F 100%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 120, top: 80, width: 620, height: 620, borderRadius: 40,
          border: '70px solid #EEEEEE', opacity: 0.04, transform: `rotate(${12 + frame * 0.05}deg)`,
        }} />
        <div style={{
          position: 'absolute', left: -200, bottom: -150, width: 700, height: 700, borderRadius: '50%',
          background: '#222831', opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.022) * 0.06})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — chart baseline grid */}
      <AbsoluteFill style={{
        opacity: 0.14,
        backgroundImage: 'linear-gradient(#222831 2px, transparent 2px)',
        backgroundSize: '100% 110px',
        transform: `translateY(${(frame * 0.35) % 110}px)`,
      }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {motes.map((m, i) => (
          <div key={`m-${i}`} style={{
            position: 'absolute', left: m.x, top: m.y, width: m.r, height: m.r,
            background: '#EEEEEE', opacity: 0.18, borderRadius: '50%',
          }} />
        ))}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateY(${camY}px) scale(${camScale})` }}>

        <div style={{
          position: 'absolute', left: 130, top: 90, opacity: sp(4),
          transform: `translateX(${interpolate(sp(4), [0, 1], [-70, 0])}px)`,
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 32, letterSpacing: 6, color: '#222831' }}>
            OLD STRUCTURE — 4 SLABS
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 128, color: '#EEEEEE', lineHeight: 1.1 }}>
            चार स्लैब
          </div>
        </div>

        {/* Bar chart */}
        <div style={{
          position: 'absolute', left: 0, right: 0, bottom: 150,
          display: 'flex', justifyContent: 'center', alignItems: 'flex-end', gap: 74,
        }}>
          {bars.map((b, i) => {
            const grow = sp(b.delay);
            const h = b.h * grow;
            return (
              <div key={`bar-${i}`} style={{ width: 250, textAlign: 'center' }}>
                <div style={{
                  fontFamily: 'Yantra One', fontSize: 102, color: '#EEEEEE', lineHeight: 1,
                  opacity: grow, transform: `translateY(${Math.sin(frame * 0.06 + i) * 5}px)`,
                }}>
                  {b.val}%
                </div>
                <div style={{
                  marginTop: 16, height: h, background: i === 3 ? '#222831' : '#EEEEEE',
                  border: `8px solid ${i === 3 ? '#EEEEEE' : '#222831'}`,
                  boxShadow: '12px 12px 0 rgba(0,0,0,0.18)',
                }} />
              </div>
            );
          })}
        </div>

        <div style={{ position: 'absolute', left: 200, right: 200, bottom: 132, height: 10, background: '#222831', opacity: sp(20) }} />

        <div style={{
          position: 'absolute', left: 0, right: 0, bottom: 58, textAlign: 'center',
          fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 51, color: '#222831',
          opacity: sp(100),
        }}>
          सालों से यही चार खाने चल रहे थे
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 400px rgba(34,40,49,0.5)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 19,
        fontWeight: 600, color: '#EEEEEE', opacity: 0.35,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08FourSlabs;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09ConfusionMaze',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09ConfusionMaze: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow orbit sway
  const camRot = Math.sin(frame * 0.017) * 2.2;
  const camScale = interpolate(frame, [0, durationInFrames], [1.1, 1.0], { easing: Easing.out(Easing.cubic) });

  const actors = [
    { label: 'दुकानदार', tag: 'SHOP', delay: 26 },
    { label: 'CA', tag: 'AUDIT', delay: 50 },
    { label: 'कारोबारी', tag: 'MSME', delay: 74 },
  ];

  const marks = new Array(9).fill(0).map((_, i) => ({
    x: 120 + seededRandom(i + 151) * 1600,
    y: 120 + seededRandom(i + 157) * 820,
    s: 40 + seededRandom(i + 163) * 50,
    d: 30 + i * 8,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#00ADB5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 20% 80%, #00CDD6 0%, #00ADB5 42%, #005F65 100%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 520, top: 180, width: 880, height: 720,
          background: '#222831', opacity: 0.06, borderRadius: 60,
          transform: `rotate(${-6 + Math.sin(frame * 0.02) * 3}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: -140, top: 60, width: 480, height: 480,
          background: '#EEEEEE', opacity: 0.05, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.07})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — maze path lines */}
      <AbsoluteFill style={{ opacity: 0.15 }}>
        <svg width="1920" height="1080">
          {new Array(11).fill(0).map((_, i) => (
            <path
              key={`mz-${i}`}
              d={`M${-100 + i * 190} 1080 V${420 + Math.sin(i + frame * 0.02) * 160} H${40 + i * 190} V0`}
              stroke="#222831" strokeWidth="7" fill="none"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles — question marks as typographic motes */}
      <AbsoluteFill>
        {marks.map((q, i) => (
          <div key={`q-${i}`} style={{
            position: 'absolute', left: q.x, top: q.y + Math.sin(frame * 0.05 + i) * 14,
            fontFamily: 'Yantra One', fontSize: q.s, color: '#EEEEEE',
            opacity: 0.28 * sp(q.d), transform: `rotate(${Math.sin(frame * 0.03 + i) * 12}deg)`,
          }}>
            ?
          </div>
        ))}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>

        <div style={{
          position: 'absolute', left: 0, right: 0, top: 110, textAlign: 'center', opacity: sp(6),
        }}>
          <span style={{
            fontFamily: 'Yantra One', fontSize: 128, color: '#222831', background: '#EEEEEE',
            padding: '14px 46px', display: 'inline-block', transform: 'rotate(1deg)',
          }}>
            फलाना सामान किस खाने में?
          </span>
        </div>

        {/* Actor cards */}
        <div style={{
          position: 'absolute', left: 0, right: 0, top: 350,
          display: 'flex', justifyContent: 'center', gap: 70,
        }}>
          {actors.map((a, i) => (
            <div key={`a-${i}`} style={{
              width: 440, height: 460, background: '#EEEEEE', border: '10px solid #222831',
              boxShadow: '16px 16px 0 #005F65', padding: 30,
              transform: `translateY(${interpolate(sp(a.delay), [0, 1], [140, 0])}px) rotate(${(i - 1) * 2.2}deg)`,
              opacity: sp(a.delay),
            }}>
              <svg width="140" height="140" viewBox="0 0 100 100">
                {i === 0 && (
                  <g fill="none" stroke="#222831" strokeWidth="8">
                    <path d="M14 40 L50 14 L86 40" />
                    <rect x="22" y="40" width="56" height="46" />
                    <rect x="40" y="58" width="20" height="28" fill="#00ADB5" stroke="none" />
                  </g>
                )}
                {i === 1 && (
                  <g fill="none" stroke="#222831" strokeWidth="8">
                    <rect x="24" y="12" width="52" height="76" />
                    <path d="M36 34 H64 M36 50 H64 M36 66 H54" />
                    <circle cx="72" cy="72" r="16" fill="#00ADB5" stroke="#222831" />
                  </g>
                )}
                {i === 2 && (
                  <g fill="none" stroke="#222831" strokeWidth="8">
                    <rect x="18" y="34" width="64" height="52" />
                    <path d="M38 34 V22 H62 V34" />
                    <path d="M18 56 H82" />
                    <rect x="44" y="48" width="14" height="16" fill="#00ADB5" stroke="none" />
                  </g>
                )}
              </svg>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 27, letterSpacing: 4, color: '#00777D', marginTop: 18,
              }}>{a.tag}</div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 68, color: '#222831', lineHeight: 1.1,
              }}>{a.label}</div>
              <div style={{
                marginTop: 22, height: 12, width: interpolate(sp(a.delay + 20), [0, 1], [0, 340]),
                background: '#00ADB5', border: '3px solid #222831',
              }} />
            </div>
          ))}
        </div>

        <div style={{
          position: 'absolute', left: 0, right: 0, bottom: 66, textAlign: 'center',
          fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 51, color: '#EEEEEE',
          opacity: sp(110),
        }}>
          सब इसी उलझन में फंसे रहते थे
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 400px rgba(34,40,49,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 19,
        fontWeight: 600, color: '#EEEEEE', opacity: 0.35,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09ConfusionMaze;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10CouncilBreak',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10CouncilBreak: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: impact zoom then settle
  const camScale = interpolate(frame, [0, 60, 90, durationInFrames], [1.14, 1.06, 1.12, 1.02], {
    extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });
  const hit = frame > 60 && frame < 78 ? Math.sin(frame * 3.6) * 11 : 0;

  // Gavel swing
  const gavel = interpolate(sp(38, { damping: 8, stiffness: 140 }), [0, 1], [-52, 6]);

  // Maze shards fly apart after the strike
  const shards = new Array(14).fill(0).map((_, i) => {
    const ang = seededRandom(i + 171) * Math.PI * 2;
    const dist = interpolate(sp(62), [0, 1], [0, 420 + seededRandom(i + 173) * 380]);
    return {
      x: 960 + Math.cos(ang) * dist,
      y: 540 + Math.sin(ang) * dist,
      w: 40 + seededRandom(i + 179) * 120,
      h: 16 + seededRandom(i + 181) * 40,
      rot: seededRandom(i + 191) * 360 + frame * 1.4,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#00ADB5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 30%, #00DCE6 0%, #00ADB5 44%, #005C62 100%)' }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 260, top: 60, width: 1400, height: 960,
          border: '80px solid #222831', opacity: 0.05, borderRadius: 120,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
        }} />
        <div style={{
          position: 'absolute', right: 140, bottom: 40, width: 460, height: 460,
          background: '#EEEEEE', opacity: 0.05, transform: `rotate(${frame * 0.1}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — impact burst rings */}
      <AbsoluteFill style={{ opacity: 0.13 }}>
        <svg width="1920" height="1080">
          {new Array(5).fill(0).map((_, i) => (
            <circle
              key={`br-${i}`}
              cx="960" cy="540"
              r={interpolate(sp(62), [0, 1], [0, 520 + i * 150])}
              stroke="#EEEEEE" strokeWidth="9" fill="none"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles — broken maze shards */}
      <AbsoluteFill>
        {shards.map((s, i) => (
          <div key={`sh-${i}`} style={{
            position: 'absolute', left: s.x, top: s.y, width: s.w, height: s.h,
            background: i % 2 === 0 ? '#222831' : '#EEEEEE', opacity: 0.55,
            transform: `rotate(${s.rot}deg)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${hit}px)` }}>

        {/* Council plate */}
        <div style={{
          position: 'absolute', left: 0, right: 0, top: 140, textAlign: 'center',
          opacity: sp(10), transform: `translateY(${interpolate(sp(10), [0, 1], [-70, 0])}px)`,
        }}>
          <div style={{
            display: 'inline-block', background: '#EEEEEE', border: '10px solid #222831',
            padding: '20px 60px', boxShadow: '18px 18px 0 #005C62', transform: 'rotate(-1.2deg)',
          }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 32, letterSpacing: 7, color: '#00777D' }}>
              GST COUNCIL MEETING
            </div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 187, color: '#222831', lineHeight: 1 }}>
              56वीं बैठक
            </div>
          </div>
        </div>

        {/* Gavel strike icon */}
        <div style={{
          position: 'absolute', left: 250, bottom: 150, transformOrigin: '80% 90%',
          transform: `rotate(${gavel}deg)`, opacity: sp(30),
        }}>
          <svg width="360" height="300" viewBox="0 0 180 150">
            <rect x="20" y="20" width="96" height="46" rx="10" fill="#222831" transform="rotate(-18 68 43)" />
            <rect x="70" y="52" width="100" height="18" rx="9" fill="#EEEEEE" transform="rotate(-18 120 61)" />
            <rect x="20" y="118" width="140" height="20" rx="10" fill="#222831" />
          </svg>
        </div>

        {/* Broken maze headline */}
        <div style={{
          position: 'absolute', right: 170, bottom: 210, textAlign: 'right',
          opacity: sp(80), transform: `translateX(${interpolate(sp(80), [0, 1], [120, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 128, color: '#EEEEEE', lineHeight: 1.05,
            textShadow: '10px 10px 0 #005C62',
          }}>
            भूल-भुलैया<br />टूट गई
          </div>
          <div style={{
            marginTop: 20, display: 'inline-block', background: '#222831', color: '#00ADB5',
            fontFamily: 'JetBrains Mono', fontSize: 34, letterSpacing: 5, padding: '12px 26px',
          }}>
            SIMPLIFIED STRUCTURE
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 420px rgba(34,40,49,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 19,
        fontWeight: 600, color: '#EEEEEE', opacity: 0.35,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10CouncilBreak;
```

