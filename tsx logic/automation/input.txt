```tsx
import React from 'react';
Audio: "If you have recently lost your spouse, or you are helping a parent navigate the devastating aftermath of losing a partner, I need you to stay with me until the very end of this video."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_OpeningHook',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_OpeningHook: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { extrapolateRight: 'clamp' });

  // Word reveals
  const word1 = sp(10);
  const word2 = sp(35);
  const word3 = sp(70);
  const urgentPulse = 1 + Math.sin(frame * 0.18) * 0.04;

  // Heartbeat line scan
  const scanY = interpolate(frame % 90, [0, 90], [-100, 1180]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(147,181,198,0.25) 0%, rgba(244,206,20,0) 60%)',
      }} />

      {/* Shape Layers - drifting geometric forms */}
      <div style={{
        position: 'absolute', top: -200, right: -150,
        width: 700, height: 700, borderRadius: '50%',
        background: '#93B5C6', opacity: 0.08,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05}) rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: -250, left: -200,
        width: 600, height: 600,
        background: '#000000', opacity: 0.05,
        transform: `rotate(${45 + frame * 0.05}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: 200, left: 100,
        width: 300, height: 300, borderRadius: '50%',
        border: '4px solid #000000', opacity: 0.06,
        transform: `scale(${1 + Math.cos(frame * 0.03) * 0.1})`,
      }} />

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        <defs>
          <pattern id="grid01" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid01)" />
      </svg>

      {/* Scan line - urgency */}
      <div style={{
        position: 'absolute', top: scanY, left: 0, right: 0, height: 2,
        background: 'linear-gradient(90deg, transparent, #000000, transparent)', opacity: 0.3,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i + 1) * 1920;
        const py = seededRandom(i + 50) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 25;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + drift,
            width: 6, height: 6, borderRadius: '50%',
            background: '#000000', opacity: 0.25,
          }} />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{
        transform: `scale(${camScale})`,
        display: 'flex', alignItems: 'center', justifyContent: 'center', flexDirection: 'column',
      }}>
        {/* Top urgent label */}
        <div style={{
          opacity: word1, transform: `translateY(${(1 - word1) * 30}px) scale(${urgentPulse})`,
          background: '#000000', color: '#F4CE14',
          padding: '14px 40px', borderRadius: 8,
          fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 800, letterSpacing: 4,
          marginBottom: 50,
        }}>
          URGENT FOR WIDOWS &amp; FAMILIES
        </div>

        {/* SVG Icon - broken heart with shield */}
        <div style={{
          opacity: word2, transform: `scale(${word2})`,
          marginBottom: 40,
        }}>
          <svg width="180" height="180" viewBox="0 0 180 180">
            <circle cx="90" cy="90" r="85" fill="none" stroke="#000000" strokeWidth="6" opacity="0.9" />
            <path d="M 90 60 L 90 130 M 60 90 L 120 90" stroke="#000000" strokeWidth="0" />
            <path d="M 55 75 C 55 60, 75 55, 90 75 C 105 55, 125 60, 125 75 C 125 100, 90 125, 90 125 C 90 125, 55 100, 55 75 Z"
              fill="#93B5C6" stroke="#000000" strokeWidth="4" />
            <path d="M 80 80 L 100 105 M 100 80 L 80 105" stroke="#000000" strokeWidth="4" strokeLinecap="round" />
          </svg>
        </div>

        {/* HERO TEXT */}
        <div style={{
          opacity: word3, transform: `translateY(${(1 - word3) * 40}px)`,
          fontFamily: 'Inter, Poppins', fontSize: 200, fontWeight: 900,
          color: '#000000', lineHeight: 0.95, textAlign: 'center',
          letterSpacing: -4,
        }}>
          STAY WITH<br />ME.
        </div>

        {/* Subhead */}
        <div style={{
          opacity: interpolate(frame, [110, 140], [0, 1], { extrapolateRight: 'clamp' }),
          marginTop: 30,
          fontFamily: 'Inter', fontSize: 50, fontWeight: 600,
          color: '#000000', textAlign: 'center', maxWidth: 1400,
          background: '#93B5C6', padding: '12px 35px', borderRadius: 4,
        }}>
          A spouse just passed. Don't sign anything yet.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_OpeningHook;
```

```tsx
import React from 'react';
Audio: "Because what I'm about to share could save you from making a permanent, irreversible mistake that costs you tens of thousands of dollars. And I mean real money. Not the kind of savings where you clip coupons and feel good about 50 cents off a can of soup."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_RealMoney',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_RealMoney: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pan camera
  const camX = interpolate(frame, [0, durationInFrames], [-20, 20]);

  // Counter from 0 -> 50,000
  const counter = Math.floor(interpolate(frame, [60, 200], [0, 50000], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));

  const headIn = sp(15);
  const cardIn = sp(60);
  const couponIn = sp(220);
  const vsIn = sp(260);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(147,181,198,0.2) 0%, rgba(244,206,20,0) 50%, rgba(0,0,0,0.1) 100%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: 100, right: 80,
        width: 500, height: 500,
        background: '#000000', opacity: 0.04,
        clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
        transform: `rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: 50, left: 200,
        width: 400, height: 400, borderRadius: '50%',
        border: '6px solid #93B5C6', opacity: 0.15,
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.08})`,
      }} />

      {/* Diagonal lines texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="diag02" width="40" height="40" patternUnits="userSpaceOnUse">
            <line x1="0" y1="40" x2="40" y2="0" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag02)" />
      </svg>

      {/* Particles - falling money dots */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 100) * 1920;
        const py = (seededRandom(i + 200) * 1080 + frame * 1.2) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 4, height: 4, borderRadius: '50%',
            background: '#000000', opacity: 0.2,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)`, padding: '60px 100px' }}>
        {/* Headline */}
        <div style={{
          opacity: headIn, transform: `translateY(${(1 - headIn) * 40}px)`,
          fontFamily: 'Inter', fontSize: 110, fontWeight: 900,
          color: '#000000', lineHeight: 0.95, letterSpacing: -3,
        }}>
          REAL MONEY.<br />
          <span style={{ color: '#93B5C6', WebkitTextStroke: '3px #000000' }}>NOT COUPON CLIPPING.</span>
        </div>

        {/* Counter Card */}
        <div style={{
          opacity: cardIn, transform: `translateY(${(1 - cardIn) * 60}px) scale(${cardIn})`,
          marginTop: 70, padding: '50px 70px',
          background: '#000000', borderRadius: 18,
          display: 'inline-block', boxShadow: '0 20px 60px rgba(0,0,0,0.3)',
          border: '4px solid #93B5C6',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 36, fontWeight: 700,
            color: '#93B5C6', letterSpacing: 3, marginBottom: 10,
          }}>
            POTENTIAL LIFETIME LOSS
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 200, fontWeight: 900,
            color: '#F4CE14', lineHeight: 1, letterSpacing: -4,
          }}>
            ${counter.toLocaleString()}+
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 32, fontWeight: 600,
            color: '#F4CE14', opacity: 0.7, marginTop: 8,
          }}>
            from one wrong signature
          </div>
        </div>

        {/* VS comparison row */}
        <div style={{
          marginTop: 60, display: 'flex', alignItems: 'center', gap: 40,
          opacity: couponIn,
        }}>
          {/* Coupon (small) */}
          <div style={{
            transform: `translateX(${(1 - couponIn) * -40}px)`,
            background: '#93B5C6', padding: '18px 28px', borderRadius: 10,
            border: '3px dashed #000000',
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 700, color: '#000000' }}>50¢ OFF SOUP</div>
          </div>

          <div style={{
            opacity: vsIn, transform: `scale(${vsIn})`,
            fontFamily: 'Inter', fontSize: 60, fontWeight: 900, color: '#000000',
          }}>
            VS
          </div>

          {/* Big money */}
          <div style={{
            transform: `translateX(${(1 - couponIn) * 40}px)`,
            background: '#000000', padding: '20px 36px', borderRadius: 10,
            border: '3px solid #F4CE14',
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 44, fontWeight: 900, color: '#F4CE14' }}>
              $50,000 SAVED
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_RealMoney;
```

```tsx
import React from 'react';
Audio: "I'm talking about the exact, step-by-step roadmap to maximize your Social Security Survivor Benefit. Here's the thing most people never find out. The day your spouse passes away, the Social Security Administration does not combine your checks. The lower check simply vanishes."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_CheckVanishes',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_CheckVanishes: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(10);
  const check1In = sp(80);
  const check2In = sp(110);
  const plusIn = sp(140);

  // Vanish animation for lower check after frame 240
  const vanishStart = 240;
  const vanishProgress = interpolate(frame, [vanishStart, vanishStart + 45], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const check1Op = 1 - vanishProgress;
  const check1Scale = 1 - vanishProgress * 0.5;
  const check1Rotate = vanishProgress * 25;

  const stampIn = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(244,206,20,1) 30%, rgba(147,181,198,0.3) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: -100, left: -100,
        width: 500, height: 500,
        background: '#93B5C6', opacity: 0.1,
        clipPath: 'polygon(0% 0%, 100% 0%, 50% 100%)',
        transform: `rotate(${frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: -150, right: -100,
        width: 450, height: 450, borderRadius: '50%',
        background: '#000000', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
      }} />

      {/* Dotted texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="1920" height="1080">
        <defs>
          <pattern id="dots03" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots03)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const px = seededRandom(i + 30) * 1920;
        const py = seededRandom(i + 70) * 1080;
        const float = Math.sin(frame * 0.025 + i) * 15;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + float,
            width: 5, height: 5,
            background: '#93B5C6', opacity: 0.5, borderRadius: '50%',
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 80, left: 0, right: 0, textAlign: 'center',
        opacity: titleIn, transform: `translateY(${(1 - titleIn) * -30}px)`,
      }}>
        <div style={{
          display: 'inline-block', background: '#000000', color: '#F4CE14',
          padding: '14px 50px', borderRadius: 8,
          fontFamily: 'Inter', fontSize: 32, fontWeight: 700, letterSpacing: 4,
        }}>
          THE BRUTAL SSA RULE
        </div>
        <div style={{
          marginTop: 25, fontFamily: 'Inter', fontSize: 95, fontWeight: 900,
          color: '#000000', lineHeight: 1, letterSpacing: -2,
        }}>
          1 + 1 ≠ 2
        </div>
      </div>

      {/* Two Checks */}
      <div style={{
        position: 'absolute', top: 380, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 60,
      }}>
        {/* Lower check - vanishes */}
        <div style={{
          opacity: check1In * check1Op,
          transform: `translateY(${(1 - check1In) * 30}px) scale(${check1Scale}) rotate(${check1Rotate}deg)`,
          background: '#FFFFFF', padding: '40px 60px',
          border: '4px solid #000000', borderRadius: 12,
          boxShadow: '8px 8px 0px #000000',
          width: 480,
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#93B5C6', letterSpacing: 2 }}>YOUR CHECK</div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 100, fontWeight: 900, color: '#000000', marginTop: 10 }}>$1,500</div>
          <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 600, color: '#000000', opacity: 0.6 }}>/ month</div>
          {vanishProgress > 0.3 && (
            <div style={{
              position: 'absolute', top: '50%', left: '50%',
              transform: 'translate(-50%, -50%) rotate(-15deg)',
              fontFamily: 'Inter', fontSize: 60, fontWeight: 900, color: '#ff0000',
              border: '5px solid #ff0000', padding: '8px 30px',
            }}>
              GONE
            </div>
          )}
        </div>

        {/* Plus sign */}
        <div style={{
          opacity: plusIn * check1Op,
          fontFamily: 'Inter', fontSize: 100, fontWeight: 900, color: '#000000',
        }}>
          +
        </div>

        {/* Higher check - survives */}
        <div style={{
          opacity: check2In,
          transform: `translateY(${(1 - check2In) * 30}px) scale(${check2In + (vanishProgress * 0.05)})`,
          background: '#000000', padding: '40px 60px',
          borderRadius: 12,
          boxShadow: '8px 8px 0px #93B5C6',
          width: 480,
          border: vanishProgress > 0.5 ? '4px solid #F4CE14' : '4px solid #000000',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#93B5C6', letterSpacing: 2 }}>SPOUSE CHECK</div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 100, fontWeight: 900, color: '#F4CE14', marginTop: 10 }}>$2,500</div>
          <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 600, color: '#F4CE14', opacity: 0.7 }}>/ month</div>
        </div>
      </div>

      {/* Bottom stamp */}
      <div style={{
        position: 'absolute', bottom: 100, left: 0, right: 0, textAlign: 'center',
        opacity: stampIn, transform: `scale(${stampIn})`,
      }}>
        <div style={{
          display: 'inline-block', background: '#93B5C6',
          padding: '20px 50px', borderRadius: 10,
          border: '4px solid #000000',
          fontFamily: 'Inter', fontSize: 48, fontWeight: 900, color: '#000000',
        }}>
          THE LOWER CHECK SIMPLY VANISHES.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_CheckVanishes;
```

```tsx
import React from 'react';
Audio: "You are left standing on a financial cliff, drowning in grief, and suddenly facing a massive drop in household income. The SSA is not going to send you a guide explaining how to protect yourself."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_FinancialCliff',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_FinancialCliff: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camTilt = interpolate(frame, [0, durationInFrames], [0, -2]);

  // Bar chart drop animation
  const bar1H = interpolate(sp(40), [0, 1], [0, 380]);
  const bar2H = interpolate(sp(120), [0, 1], [0, 140]);
  const arrowIn = sp(180);
  const dropPctIn = sp(220);
  const warningIn = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(244,206,20,1) 0%, rgba(147,181,198,0.4) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: 50, right: 50,
        width: 600, height: 600,
        background: '#000000', opacity: 0.05,
        clipPath: 'polygon(0% 100%, 100% 100%, 100% 0%)',
      }} />
      <div style={{
        position: 'absolute', top: 200, left: -80,
        width: 350, height: 350, borderRadius: '50%',
        border: '5px solid #000000', opacity: 0.08,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Cliff lines texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        <defs>
          <pattern id="vert04" width="100" height="100" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="100" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#vert04)" />
      </svg>

      {/* Particles drifting down (grief) */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 400) * 1920;
        const py = (seededRandom(i + 500) * 1080 + frame * 0.8) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 3, height: 12,
            background: '#93B5C6', opacity: 0.4,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camTilt}deg)`, padding: '60px 100px' }}>
        {/* Left side - title */}
        <div style={{ width: '50%' }}>
          <div style={{
            opacity: sp(0), transform: `translateX(${(1 - sp(0)) * -30}px)`,
            background: '#000000', color: '#F4CE14', display: 'inline-block',
            padding: '10px 30px', borderRadius: 6,
            fontFamily: 'Inter', fontSize: 28, fontWeight: 700, letterSpacing: 3,
            marginBottom: 30,
          }}>
            HOUSEHOLD INCOME
          </div>
          <div style={{
            opacity: sp(20), transform: `translateX(${(1 - sp(20)) * -40}px)`,
            fontFamily: 'Inter', fontSize: 140, fontWeight: 900,
            color: '#000000', lineHeight: 0.9, letterSpacing: -4,
          }}>
            FINANCIAL<br />
            <span style={{ color: '#93B5C6', WebkitTextStroke: '4px #000000' }}>CLIFF.</span>
          </div>
          <div style={{
            marginTop: 40, opacity: warningIn,
            transform: `translateX(${(1 - warningIn) * -40}px)`,
            background: '#000000', padding: '20px 32px', borderRadius: 8,
            display: 'inline-block', maxWidth: 700,
            borderLeft: '8px solid #F4CE14',
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 700, color: '#F4CE14', lineHeight: 1.2 }}>
              The SSA won't send a guide.
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 500, color: '#93B5C6', marginTop: 8 }}>
              They process whatever you sign.
            </div>
          </div>
        </div>

        {/* Right side - bar chart drop */}
        <div style={{
          position: 'absolute', right: 120, bottom: 120,
          width: 600, height: 500, display: 'flex', alignItems: 'flex-end', gap: 60,
        }}>
          {/* Before bar */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 44, fontWeight: 900,
              color: '#000000', marginBottom: 12,
            }}>
              $4,000
            </div>
            <div style={{
              width: 180, height: bar1H,
              background: '#000000', borderRadius: '8px 8px 0 0',
              border: '4px solid #000000',
            }} />
            <div style={{
              fontFamily: 'Inter', fontSize: 28, fontWeight: 700, color: '#000000', marginTop: 12,
            }}>BEFORE</div>
          </div>

          {/* Arrow */}
          <div style={{
            opacity: arrowIn, transform: `scale(${arrowIn})`,
            position: 'absolute', top: 100, left: 200, zIndex: 10,
          }}>
            <svg width="180" height="120" viewBox="0 0 180 120">
              <path d="M 10 20 Q 90 20 90 80 L 70 60 M 90 80 L 110 60"
                stroke="#ff0000" strokeWidth="8" fill="none" strokeLinecap="round" />
            </svg>
          </div>

          {/* After bar */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 44, fontWeight: 900,
              color: '#ff0000', marginBottom: 12,
            }}>
              $2,500
            </div>
            <div style={{
              width: 180, height: bar2H,
              background: '#93B5C6', borderRadius: '8px 8px 0 0',
              border: '4px solid #000000',
            }} />
            <div style={{
              fontFamily: 'Inter', fontSize: 28, fontWeight: 700, color: '#000000', marginTop: 12,
            }}>AFTER</div>
          </div>

          {/* Drop % badge */}
          <div style={{
            opacity: dropPctIn, transform: `scale(${dropPctIn}) rotate(-8deg)`,
            position: 'absolute', top: -60, right: -40,
            background: '#ff0000', color: '#F4CE14',
            padding: '14px 28px', borderRadius: 12,
            fontFamily: 'Inter', fontSize: 42, fontWeight: 900,
            border: '4px solid #000000',
          }}>
            -37.5%
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_FinancialCliff;
```

```tsx
import React from 'react';
Audio: "They just process whatever form you hand them. I went through the latest SSA Survivor Guidelines, the 2026 Widow Benefit Rules, and the Hidden Claiming Strategies. And I found four exact steps you must take to maximize your monthly income."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_FourSteps',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_FourSteps: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera orbit
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1]);

  const headIn = sp(10);
  const docsIn = sp(70);
  const step1 = sp(160);
  const step2 = sp(195);
  const step3 = sp(230);
  const step4 = sp(265);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at top right, rgba(147,181,198,0.3) 0%, rgba(244,206,20,1) 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: -100, left: -100,
        width: 500, height: 500, borderRadius: '50%',
        background: '#93B5C6', opacity: 0.08,
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', bottom: -100, right: -100,
        width: 600, height: 600,
        background: '#000000', opacity: 0.04,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
        transform: `rotate(${frame * 0.05}deg)`,
      }} />

      {/* Hex grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="1920" height="1080">
        <defs>
          <pattern id="cross05" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="30" y1="0" x2="30" y2="60" stroke="#000000" strokeWidth="1" />
            <line x1="0" y1="30" x2="60" y2="30" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross05)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const px = seededRandom(i + 800) * 1920;
        const py = seededRandom(i + 900) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 20;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + drift,
            width: 4, height: 4, background: '#000000', opacity: 0.2, borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, padding: '60px 80px' }}>
        {/* Top heading */}
        <div style={{
          opacity: headIn, transform: `translateY(${(1 - headIn) * -30}px)`,
          textAlign: 'center', marginBottom: 30,
        }}>
          <div style={{
            display: 'inline-block', background: '#000000', color: '#F4CE14',
            padding: '12px 40px', borderRadius: 6,
            fontFamily: 'Inter', fontSize: 28, fontWeight: 700, letterSpacing: 4,
            marginBottom: 18,
          }}>
            2026 SSA WIDOW RULES — DECODED
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 110, fontWeight: 900,
            color: '#000000', lineHeight: 0.95, letterSpacing: -3,
          }}>
            4 EXACT STEPS.
          </div>
        </div>

        {/* Documents row icons */}
        <div style={{
          opacity: docsIn, transform: `translateY(${(1 - docsIn) * 30}px)`,
          display: 'flex', justifyContent: 'center', gap: 30, marginBottom: 50,
        }}>
          {['SSA GUIDELINES', '2026 RULES', 'HIDDEN STRATEGIES'].map((label, i) => (
            <div key={i} style={{
              background: '#93B5C6', padding: '18px 28px', borderRadius: 8,
              border: '3px solid #000000',
              fontFamily: 'Inter', fontSize: 26, fontWeight: 800, color: '#000000',
              letterSpacing: 2,
            }}>
              {label}
            </div>
          ))}
        </div>

        {/* 4 Step boxes */}
        <div style={{
          display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 28,
          padding: '0 40px',
        }}>
          {[
            { num: '01', title: 'AGE 60 LOOPHOLE', anim: step1 },
            { num: '02', title: 'STAGGERED CLAIM', anim: step2 },
            { num: '03', title: 'WORKING PENALTY', anim: step3 },
            { num: '04', title: 'MAXIMIZE & SWITCH', anim: step4 },
          ].map((s, i) => (
            <div key={i} style={{
              opacity: s.anim,
              transform: `translateY(${(1 - s.anim) * 50}px) scale(${s.anim})`,
              background: i === 3 ? '#000000' : '#F4CE14',
              border: '5px solid #000000', borderRadius: 14,
              padding: '30px 22px', textAlign: 'center',
              boxShadow: '6px 6px 0 #000000',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 80, fontWeight: 900,
                color: i === 3 ? '#F4CE14' : '#000000', lineHeight: 1,
              }}>
                {s.num}
              </div>
              <div style={{
                marginTop: 12,
                fontFamily: 'Inter', fontSize: 28, fontWeight: 800,
                color: i === 3 ? '#93B5C6' : '#000000', letterSpacing: 1,
              }}>
                {s.title}
              </div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_FourSteps;
```

```tsx
import React from 'react';
Audio: "Stay until the end, because step number four is the Staggered Switch Loophole, a massive strategy that SSA clerks almost never explain, which lets you collect one benefit early while the other grows by 8% a year."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_EightPercent',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_EightPercent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter 0 -> 8
  const counter = interpolate(frame, [60, 150], [0, 8], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const counterDisplay = counter.toFixed(1);

  // Growing chart bars
  const bars = Array.from({ length: 5 }).map((_, i) => {
    const delay = 100 + i * 25;
    const a = sp(delay);
    return interpolate(a, [0, 1], [0, 60 + i * 70]);
  });

  const headIn = sp(10);
  const stampIn = sp(220);

  // Tick marks on side
  const tickPulse = Math.sin(frame * 0.15) * 0.1 + 1;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(45deg, rgba(244,206,20,1) 0%, rgba(147,181,198,0.3) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: 100, left: 100,
        width: 400, height: 400, borderRadius: '50%',
        border: '8px solid #000000', opacity: 0.08,
        transform: `scale(${tickPulse})`,
      }} />
      <div style={{
        position: 'absolute', bottom: 0, right: 0,
        width: 700, height: 700,
        background: '#93B5C6', opacity: 0.1,
        clipPath: 'polygon(100% 0%, 100% 100%, 0% 100%)',
      }} />

      {/* Graph paper texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        <defs>
          <pattern id="grid06" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 40 0 L 0 0 0 40" fill="none" stroke="#000000" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid06)" />
      </svg>

      {/* Floating + symbols (compounding) */}
      {Array.from({ length: 12 }).map((_, i) => {
        const px = seededRandom(i + 1000) * 1920;
        const py = (seededRandom(i + 1100) * 1080 - frame * 0.6 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            fontFamily: 'Inter', fontSize: 28, fontWeight: 900,
            color: '#000000', opacity: 0.2,
          }}>
            +
          </div>
        );
      })}

      <AbsoluteFill style={{ padding: '60px 100px' }}>
        {/* Left side - giant percent */}
        <div style={{
          opacity: headIn, transform: `translateY(${(1 - headIn) * -30}px)`,
          width: '50%',
        }}>
          <div style={{
            background: '#000000', color: '#F4CE14',
            padding: '12px 30px', borderRadius: 6, display: 'inline-block',
            fontFamily: 'Inter', fontSize: 28, fontWeight: 700, letterSpacing: 4,
            marginBottom: 30,
          }}>
            STEP 04 PREVIEW
          </div>

          <div style={{
            fontFamily: 'Inter', fontSize: 70, fontWeight: 900,
            color: '#000000', lineHeight: 0.95, letterSpacing: -2,
            marginBottom: 20,
          }}>
            ONE GROWS BY
          </div>

          {/* Massive number */}
          <div style={{
            display: 'flex', alignItems: 'flex-start',
            fontFamily: 'JetBrains Mono', fontWeight: 900,
            color: '#000000', lineHeight: 0.9,
          }}>
            <span style={{ fontSize: 380, letterSpacing: -10 }}>{counterDisplay}</span>
            <span style={{ fontSize: 180, marginTop: 60, color: '#93B5C6', WebkitTextStroke: '4px #000000' }}>%</span>
          </div>

          <div style={{
            fontFamily: 'Inter', fontSize: 44, fontWeight: 700, color: '#000000',
            marginTop: -10,
          }}>
            every single year.
          </div>
        </div>

        {/* Right side - growing bar chart */}
        <div style={{
          position: 'absolute', right: 120, top: 200,
          width: 600, height: 600,
        }}>
          <div style={{
            display: 'flex', alignItems: 'flex-end', gap: 20, height: 480,
            borderBottom: '5px solid #000000', borderLeft: '5px solid #000000',
            padding: '0 20px 0 20px',
          }}>
            {bars.map((h, i) => (
              <div key={i} style={{
                width: 90, height: h,
                background: i === bars.length - 1 ? '#000000' : '#93B5C6',
                border: '3px solid #000000', borderRadius: '6px 6px 0 0',
                display: 'flex', alignItems: 'flex-start', justifyContent: 'center',
                paddingTop: 8,
              }}>
                <span style={{
                  fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 900,
                  color: i === bars.length - 1 ? '#F4CE14' : '#000000',
                }}>
                  +8%
                </span>
              </div>
            ))}
          </div>
          <div style={{
            display: 'flex', justifyContent: 'space-around',
            fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#000000',
            marginTop: 10,
          }}>
            {['Y1', 'Y2', 'Y3', 'Y4', 'Y5'].map(y => <span key={y}>{y}</span>)}
          </div>
        </div>

        {/* Bottom stamp */}
        <div style={{
          position: 'absolute', bottom: 70, left: 100,
          opacity: stampIn, transform: `scale(${stampIn})`,
          background: '#000000', color: '#F4CE14',
          padding: '18px 36px', borderRadius: 10,
          fontFamily: 'Inter', fontSize: 38, fontWeight: 800,
          borderLeft: '8px solid #93B5C6',
        }}>
          SSA clerks almost never explain this.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_EightPercent;
```

```tsx
import React from 'react';
Audio: "Real quick, before we get into it, if you're not subscribed to this channel yet, hit that button right now and turn on notifications. The rules around Social Security Survivor Benefits are incredibly complex, and one wrong signature can cost you a fortune."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_Subscribe',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_Subscribe: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(10);
  const btnIn = sp(60);
  const bellIn = sp(150);
  const bellShake = Math.sin(frame * 0.5) * (frame > 150 && frame < 240 ? 8 : 0);
  const warnIn = sp(240);

  // Button pulse
  const btnPulse = 1 + Math.sin(frame * 0.12) * 0.03;

  // Cursor pointer
  const cursorX = interpolate(frame, [80, 130], [1400, 1050], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const cursorY = interpolate(frame, [80, 130], [200, 450], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, rgba(244,206,20,1) 30%, rgba(147,181,198,0.4) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: -100, right: -100,
        width: 500, height: 500, borderRadius: '50%',
        background: '#000000', opacity: 0.05,
      }} />
      <div style={{
        position: 'absolute', bottom: -150, left: -50,
        width: 600, height: 600, borderRadius: '50%',
        border: '6px solid #93B5C6', opacity: 0.2,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
      }} />

      {/* Diagonal texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="diag07" width="30" height="30" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="30" y2="30" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag07)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i + 200) * 1920;
        const py = seededRandom(i + 300) * 1080;
        const float = Math.sin(frame * 0.03 + i) * 18;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + float,
            width: 5, height: 5, background: '#000000', opacity: 0.18, borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ padding: '60px 100px' }}>
        {/* Left title */}
        <div style={{ width: '55%' }}>
          <div style={{
            opacity: titleIn, transform: `translateY(${(1 - titleIn) * -30}px)`,
            background: '#000000', color: '#F4CE14', display: 'inline-block',
            padding: '12px 30px', borderRadius: 6,
            fontFamily: 'Inter', fontSize: 26, fontWeight: 700, letterSpacing: 3,
            marginBottom: 25,
          }}>
            PROTECT YOURSELF FIRST
          </div>
          <div style={{
            opacity: titleIn, transform: `translateY(${(1 - titleIn) * 30}px)`,
            fontFamily: 'Inter', fontSize: 130, fontWeight: 900,
            color: '#000000', lineHeight: 0.92, letterSpacing: -3,
          }}>
            HIT<br />
            <span style={{ color: '#93B5C6', WebkitTextStroke: '4px #000000' }}>SUBSCRIBE.</span>
          </div>
          <div style={{
            opacity: bellIn, marginTop: 30,
            fontFamily: 'Inter', fontSize: 44, fontWeight: 700, color: '#000000',
          }}>
            Turn on the bell.
          </div>

          {/* Warning callout */}
          <div style={{
            marginTop: 50, opacity: warnIn,
            transform: `translateX(${(1 - warnIn) * -40}px)`,
            background: '#000000', borderLeft: '8px solid #F4CE14',
            padding: '22px 32px', borderRadius: 6, maxWidth: 800,
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 800, color: '#F4CE14' }}>
              ONE wrong signature →
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 900, color: '#93B5C6', marginTop: 4 }}>
              costs you a FORTUNE.
            </div>
          </div>
        </div>

        {/* Right - Subscribe button */}
        <div style={{
          position: 'absolute', right: 150, top: 350,
          opacity: btnIn, transform: `scale(${btnIn * btnPulse})`,
        }}>
          <div style={{
            background: '#ff0000', color: '#F5F5F5',
            padding: '36px 80px', borderRadius: 16,
            fontFamily: 'Inter', fontSize: 70, fontWeight: 900,
            letterSpacing: 2, border: '6px solid #000000',
            boxShadow: '12px 12px 0 #000000',
          }}>
            SUBSCRIBE
          </div>

          {/* Bell icon below */}
          <div style={{
            opacity: bellIn, transform: `translate(${(1 - bellIn) * 30}px, ${(1 - bellIn) * 30}px) rotate(${bellShake}deg)`,
            marginTop: 40, display: 'flex', justifyContent: 'center',
          }}>
            <svg width="120" height="120" viewBox="0 0 120 120">
              <path d="M 60 20 Q 35 20 35 50 L 35 75 L 25 90 L 95 90 L 85 75 L 85 50 Q 85 20 60 20 Z"
                fill="#000000" stroke="#000000" strokeWidth="4" />
              <circle cx="60" cy="100" r="10" fill="#000000" />
              <circle cx="60" cy="14" r="6" fill="#93B5C6" />
            </svg>
          </div>
        </div>

        {/* Cursor */}
        {frame > 80 && frame < 200 && (
          <div style={{
            position: 'absolute', left: cursorX, top: cursorY,
            width: 50, height: 50, zIndex: 50,
          }}>
            <svg width="50" height="50" viewBox="0 0 50 50">
              <path d="M 5 5 L 5 35 L 15 28 L 22 45 L 28 42 L 22 25 L 35 25 Z"
                fill="#F4CE14" stroke="#000000" strokeWidth="3" />
            </svg>
          </div>
        )}
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_Subscribe;
```

```tsx
import React from 'react';
Audio: "I cover all of that in plain English, so you always know what you're actually entitled to. The people who are subscribed stay protected. The people who aren't tend to find out too late. So subscribe now, and let's get into it."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_ProtectedVsLate',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_ProtectedVsLate: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(10);
  const leftIn = sp(60);
  const rightIn = sp(100);
  const vsIn = sp(140);
  const ctaIn = sp(340);

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [10, -10]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient split */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(147,181,198,0.25) 0%, rgba(244,206,20,1) 50%, rgba(0,0,0,0.1) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: 100, left: 200,
        width: 350, height: 350, borderRadius: '50%',
        border: '5px solid #000000', opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
      }} />
      <div style={{
        position: 'absolute', bottom: 80, right: 200,
        width: 400, height: 400,
        background: '#000000', opacity: 0.05,
        clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
        transform: `rotate(${frame * 0.08}deg)`,
      }} />

      {/* Texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="lines08" width="20" height="20" patternUnits="userSpaceOnUse">
            <line x1="0" y1="20" x2="20" y2="0" stroke="#000000" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#lines08)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i + 700) * 1920;
        const py = seededRandom(i + 600) * 1080;
        const f = Math.cos(frame * 0.025 + i) * 15;
        return (
          <div key={i} style={{
            position: 'absolute', left: px + f, top: py,
            width: 4, height: 4, background: '#93B5C6', opacity: 0.5, borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)`, padding: '60px 100px' }}>
        {/* Title */}
        <div style={{
          opacity: titleIn, transform: `translateY(${(1 - titleIn) * -30}px)`,
          textAlign: 'center', marginBottom: 50,
        }}>
          <div style={{
            display: 'inline-block', background: '#000000', color: '#F4CE14',
            padding: '12px 36px', borderRadius: 6,
            fontFamily: 'Inter', fontSize: 28, fontWeight: 700, letterSpacing: 3,
            marginBottom: 20,
          }}>
            TWO PATHS
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 115, fontWeight: 900,
            color: '#000000', lineHeight: 0.95, letterSpacing: -3,
          }}>
            PROTECTED <span style={{ fontSize: 70, color: '#93B5C6', WebkitTextStroke: '3px #000000' }}>OR</span> TOO LATE.
          </div>
        </div>

        {/* Two columns */}
        <div style={{ display: 'flex', justifyContent: 'center', gap: 50, marginTop: 30 }}>
          {/* Protected */}
          <div style={{
            opacity: leftIn, transform: `translateX(${(1 - leftIn) * -60}px) scale(${leftIn})`,
            width: 580, background: '#000000', borderRadius: 16,
            border: '5px solid #000000', padding: '40px',
            boxShadow: '10px 10px 0 #93B5C6',
          }}>
            <div style={{
              display: 'flex', alignItems: 'center', gap: 16, marginBottom: 24,
            }}>
              <svg width="60" height="60" viewBox="0 0 60 60">
                <path d="M 30 5 L 50 15 L 50 30 Q 50 50 30 55 Q 10 50 10 30 L 10 15 Z"
                  fill="#93B5C6" stroke="#F4CE14" strokeWidth="3" />
                <path d="M 20 30 L 27 38 L 42 22" stroke="#000000" strokeWidth="5" fill="none" strokeLinecap="round" />
              </svg>
              <div style={{
                fontFamily: 'Inter', fontSize: 46, fontWeight: 900, color: '#F4CE14',
              }}>
                SUBSCRIBED
              </div>
            </div>
            {['Knows the rules', 'Maxes the benefit', 'Keeps the money'].map((t, i) => (
              <div key={i} style={{
                fontFamily: 'Inter', fontSize: 32, fontWeight: 700, color: '#93B5C6',
                padding: '10px 0', borderBottom: i < 2 ? '2px dashed rgba(147,181,198,0.3)' : 'none',
                display: 'flex', alignItems: 'center', gap: 12,
              }}>
                <span style={{ color: '#F4CE14' }}>›</span> {t}
              </div>
            ))}
          </div>

          {/* VS */}
          <div style={{
            opacity: vsIn, transform: `scale(${vsIn})`,
            alignSelf: 'center',
            fontFamily: 'Inter', fontSize: 90, fontWeight: 900, color: '#000000',
          }}>
            VS
          </div>

          {/* Too Late */}
          <div style={{
            opacity: rightIn, transform: `translateX(${(1 - rightIn) * 60}px) scale(${rightIn})`,
            width: 580, background: '#F4CE14', borderRadius: 16,
            border: '5px solid #000000', padding: '40px',
            boxShadow: '10px 10px 0 #000000',
          }}>
            <div style={{
              display: 'flex', alignItems: 'center', gap: 16, marginBottom: 24,
            }}>
              <svg width="60" height="60" viewBox="0 0 60 60">
                <circle cx="30" cy="30" r="25" fill="#ff0000" stroke="#000000" strokeWidth="3" />
                <path d="M 18 18 L 42 42 M 42 18 L 18 42" stroke="#F4CE14" strokeWidth="6" strokeLinecap="round" />
              </svg>
              <div style={{
                fontFamily: 'Inter', fontSize: 46, fontWeight: 900, color: '#000000',
              }}>
                NOT SUBSCRIBED
              </div>
            </div>
            {['Finds out too late', 'Signs wrong form', 'Loses thousands'].map((t, i) => (
              <div key={i} style={{
                fontFamily: 'Inter', fontSize: 32, fontWeight: 700, color: '#000000',
                padding: '10px 0', borderBottom: i < 2 ? '2px dashed rgba(0,0,0,0.3)' : 'none',
                display: 'flex', alignItems: 'center', gap: 12,
              }}>
                <span style={{ color: '#ff0000' }}>×</span> {t}
              </div>
            ))}
          </div>
        </div>

        {/* CTA bottom */}
        <div style={{
          position: 'absolute', bottom: 50, left: 0, right: 0, textAlign: 'center',
          opacity: ctaIn, transform: `translateY(${(1 - ctaIn) * 30}px)`,
        }}>
          <div style={{
            display: 'inline-block', background: '#000000', color: '#F4CE14',
            padding: '20px 60px', borderRadius: 10,
            fontFamily: 'Inter', fontSize: 44, fontWeight: 900, letterSpacing: 2,
            borderLeft: '8px solid #93B5C6',
          }}>
            SUBSCRIBE NOW → Let's get into it.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_ProtectedVsLate;
```

```tsx
import React from 'react';
Audio: "To understand how to protect your future, you have to understand the brutal reality of the one-check rule. When you and your spouse are both alive, you both collect your own Social Security checks. Let's say you get $1,500 a month, and your spouse gets $2,500 a month. Your household brings in $4,000."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_HouseholdMath',
  durationInSeconds: 27,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_HouseholdMath: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(10);
  const check1 = sp(120);
  const plusIn = sp(220);
  const check2 = sp(280);
  const equalsIn = sp(420);
  const totalIn = sp(480);

  // Counter for total
  const total = Math.floor(interpolate(frame, [480, 600], [0, 4000], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(244,206,20,1) 0%, rgba(147,181,198,0.3) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: -100, right: -100,
        width: 550, height: 550, borderRadius: '50%',
        background: '#93B5C6', opacity: 0.1,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: -100, left: -50,
        width: 450, height: 450,
        background: '#000000', opacity: 0.04,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
        transform: `rotate(${frame * 0.06}deg)`,
      }} />

      {/* Texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="grid09" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid09)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const px = seededRandom(i + 50) * 1920;
        const py = seededRandom(i + 150) * 1080;
        const drift = Math.sin(frame * 0.022 + i) * 18;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + drift,
            width: 5, height: 5, background: '#000000', opacity: 0.2, borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ padding: '50px 80px' }}>
        {/* Title */}
        <div style={{
          opacity: titleIn, transform: `translateY(${(1 - titleIn) * -30}px)`,
          textAlign: 'center', marginBottom: 30,
        }}>
          <div style={{
            display: 'inline-block', background: '#000000', color: '#F4CE14',
            padding: '12px 40px', borderRadius: 6,
            fontFamily: 'Inter', fontSize: 28, fontWeight: 700, letterSpacing: 4,
            marginBottom: 18,
          }}>
            BOTH SPOUSES ALIVE — THE BASELINE
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 90, fontWeight: 900,
            color: '#000000', lineHeight: 0.95, letterSpacing: -3,
          }}>
            THE HOUSEHOLD MATH
          </div>
        </div>

        {/* Math row */}
        <div style={{
          display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 30,
          marginTop: 50,
        }}>
          {/* You */}
          <div style={{
            opacity: check1, transform: `translateY(${(1 - check1) * 40}px) scale(${check1})`,
            background: '#F4CE14', border: '5px solid #000000',
            padding: '32px 40px', borderRadius: 14, width: 380, textAlign: 'center',
            boxShadow: '8px 8px 0 #000000',
          }}>
            <div style={{ display: 'flex', justifyContent: 'center', marginBottom: 12 }}>
              <svg width="70" height="70" viewBox="0 0 70 70">
                <circle cx="35" cy="22" r="14" fill="#000000" />
                <path d="M 12 60 Q 35 38 58 60 Z" fill="#000000" />
              </svg>
            </div>
            <div style={{
              fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#000000',
              letterSpacing: 2, marginBottom: 8,
            }}>
              YOU
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 90, fontWeight: 900,
              color: '#000000', lineHeight: 1,
            }}>
              $1,500
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#000000', opacity: 0.6 }}>
              / month
            </div>
          </div>

          {/* Plus */}
          <div style={{
            opacity: plusIn, transform: `scale(${plusIn})`,
            fontFamily: 'Inter', fontSize: 130, fontWeight: 900, color: '#000000',
          }}>
            +
          </div>

          {/* Spouse */}
          <div style={{
            opacity: check2, transform: `translateY(${(1 - check2) * 40}px) scale(${check2})`,
            background: '#93B5C6', border: '5px solid #000000',
            padding: '32px 40px', borderRadius: 14, width: 380, textAlign: 'center',
            boxShadow: '8px 8px 0 #000000',
          }}>
            <div style={{ display: 'flex', justifyContent: 'center', marginBottom: 12 }}>
              <svg width="70" height="70" viewBox="0 0 70 70">
                <circle cx="35" cy="22" r="14" fill="#000000" />
                <path d="M 12 60 Q 35 38 58 60 Z" fill="#000000" />
              </svg>
            </div>
            <div style={{
              fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#000000',
              letterSpacing: 2, marginBottom: 8,
            }}>
              SPOUSE
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 90, fontWeight: 900,
              color: '#000000', lineHeight: 1,
            }}>
              $2,500
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#000000', opacity: 0.6 }}>
              / month
            </div>
          </div>

          {/* Equals */}
          <div style={{
            opacity: equalsIn, transform: `scale(${equalsIn})`,
            fontFamily: 'Inter', fontSize: 130, fontWeight: 900, color: '#000000',
          }}>
            =
          </div>
        </div>

        {/* Total box */}
        <div style={{
          opacity: totalIn, transform: `translateY(${(1 - totalIn) * 50}px) scale(${totalIn})`,
          marginTop: 60, marginLeft: 'auto', marginRight: 'auto',
          background: '#000000', border: '6px solid #F4CE14',
          padding: '32px 60px', borderRadius: 16, maxWidth: 800, textAlign: 'center',
          boxShadow: '0 20px 50px rgba(0,0,0,0.4)',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 30, fontWeight: 700,
            color: '#93B5C6', letterSpacing: 3, marginBottom: 6,
          }}>
            HOUSEHOLD INCOME
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 180, fontWeight: 900,
            color: '#F4CE14', lineHeight: 1, letterSpacing: -4,
          }}>
            ${total.toLocaleString()}
          </div>
          <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#F4CE14', opacity: 0.7 }}>
            per month — while both alive
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_HouseholdMath;
```

```tsx
import React from 'react';
Audio: "But the moment your spouse passes away, the SSA stops sending their check. They do not add them together. Instead, they offer you a Survivor Benefit. And the rule is simple. You get the higher of the two checks. In this example, your $1,500 check disappears, and you step up to collect your spouse's $2,500 check. You just lost a massive chunk of your household income overnight."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_OvernightLoss',
  durationInSeconds: 28,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_OvernightLoss: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(10);

  // Timeline: BEFORE -> EVENT -> AFTER
  const beforeIn = sp(60);
  const eventIn = sp(200);
  const afterIn = sp(360);
  const dropIn = sp(540);
  const ruleIn = sp(660);

  // Shake on event
  const shake = (frame > 200 && frame < 260) ? Math.sin(frame * 1.2) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(244,206,20,1) 0%, rgba(0,0,0,0.15) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: 50, left: 50,
        width: 400, height: 400,
        background: '#93B5C6', opacity: 0.08,
        clipPath: 'polygon(0% 0%, 100% 0%, 100% 100%, 30% 100%, 30% 70%, 0% 70%)',
        transform: `rotate(${frame * 0.04}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: -100, right: -100,
        width: 500, height: 500, borderRadius: '50%',
        border: '6px solid #000000', opacity: 0.07,
        transform: `scale(${1 + Math.cos(frame * 0.025) * 0.06})`,
      }} />

      {/* Texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="cross10" width="80" height="80" patternUnits="userSpaceOnUse">
            <line x1="40" y1="20" x2="40" y2="60" stroke="#000000" strokeWidth="1" />
            <line x1="20" y1="40" x2="60" y2="40" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross10)" />
      </svg>

      {/* Particles falling */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 333) * 1920;
        const py = (seededRandom(i + 444) * 1080 + frame * 1.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 3, height: 14, background: '#000000', opacity: 0.2,
          }} />
        );
      })}

      <AbsoluteFill style={{ padding: '50px 80px', transform: `translateX(${shake}px) translateY(${shake * 0.5}px)` }}>
        {/* Title */}
        <div style={{
          opacity: titleIn, transform: `translateY(${(1 - titleIn) * -30}px)`,
          textAlign: 'center', marginBottom: 30,
        }}>
          <div style={{
            display: 'inline-block', background: '#ff0000', color: '#F5F5F5',
            padding: '12px 40px', borderRadius: 6,
            fontFamily: 'Inter', fontSize: 28, fontWeight: 800, letterSpacing: 4,
            marginBottom: 18, border: '3px solid #000000',
          }}>
            OVERNIGHT — THE ONE-CHECK RULE HITS
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 95, fontWeight: 900,
            color: '#000000', lineHeight: 0.95, letterSpacing: -3,
          }}>
            YOU GET THE <span style={{ color: '#93B5C6', WebkitTextStroke: '4px #000000' }}>HIGHER</span> CHECK.
          </div>
        </div>

        {/* Timeline 3 columns */}
        <div style={{
          display: 'grid', gridTemplateColumns: '1fr auto 1fr', gap: 50, alignItems: 'center',
          padding: '0 60px', marginTop: 40,
        }}>
          {/* BEFORE */}
          <div style={{
            opacity: beforeIn, transform: `translateX(${(1 - beforeIn) * -40}px)`,
            background: '#000000', border: '5px solid #000000',
            padding: '32px', borderRadius: 14,
            boxShadow: '8px 8px 0 #93B5C6',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#93B5C6',
              letterSpacing: 3, marginBottom: 12,
            }}>
              BEFORE
            </div>
            <div style={{ display: 'flex', gap: 14, alignItems: 'center', marginBottom: 12 }}>
              <div style={{
                background: '#F4CE14', padding: '8px 16px', borderRadius: 6,
                fontFamily: 'JetBrains Mono', fontSize: 32, fontWeight: 900, color: '#000000',
              }}>$1,500</div>
              <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 900, color: '#F4CE14' }}>+</div>
              <div style={{
                background: '#93B5C6', padding: '8px 16px', borderRadius: 6,
                fontFamily: 'JetBrains Mono', fontSize: 32, fontWeight: 900, color: '#000000',
              }}>$2,500</div>
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 70, fontWeight: 900,
              color: '#F4CE14', lineHeight: 1,
            }}>
              = $4,000
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#93B5C6', opacity: 0.7, marginTop: 4 }}>
              per month
            </div>
          </div>

          {/* EVENT - lightning arrow */}
          <div style={{
            opacity: eventIn, transform: `scale(${eventIn})`,
            display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12,
          }}>
            <svg width="140" height="140" viewBox="0 0 140 140">
              <path d="M 70 5 L 35 75 L 60 75 L 45 135 L 105 55 L 80 55 L 95 5 Z"
                fill="#ff0000" stroke="#000000" strokeWidth="5" strokeLinejoin="round" />
            </svg>
            <div style={{
              background: '#ff0000', color: '#F5F5F5',
              padding: '8px 18px', borderRadius: 6,
              fontFamily: 'Inter', fontSize: 24, fontWeight: 900, letterSpacing: 2,
              border: '3px solid #000000',
            }}>
              SPOUSE PASSES
            </div>
          </div>

          {/* AFTER */}
          <div style={{
            opacity: afterIn, transform: `translateX(${(1 - afterIn) * 40}px)`,
            background: '#F4CE14', border: '5px solid #000000',
            padding: '32px', borderRadius: 14,
            boxShadow: '8px 8px 0 #000000',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#000000',
              letterSpacing: 3, marginBottom: 12,
            }}>
              AFTER
            </div>
            <div style={{ display: 'flex', gap: 14, alignItems: 'center', marginBottom: 12 }}>
              <div style={{
                background: '#000000', padding: '8px 16px', borderRadius: 6,
                fontFamily: 'JetBrains Mono', fontSize: 32, fontWeight: 900, color: '#ff0000',
                textDecoration: 'line-through',
              }}>$1,500</div>
              <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 900, color: '#000000' }}>→</div>
              <div style={{
                background: '#93B5C6', padding: '8px 16px', borderRadius: 6,
                fontFamily: 'JetBrains Mono', fontSize: 32, fontWeight: 900, color: '#000000',
                border: '3px solid #000000',
              }}>$2,500</div>
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 70, fontWeight: 900,
              color: '#000000', lineHeight: 1,
            }}>
              = $2,500
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#000000', opacity: 0.6, marginTop: 4 }}>
              per month
            </div>
          </div>
        </div>

        {/* Drop badge */}
        <div style={{
          opacity: dropIn, transform: `scale(${dropIn}) rotate(-5deg)`,
          position: 'absolute', top: 130, right: 100,
          background: '#ff0000', color: '#F4CE14',
          padding: '14px 28px', borderRadius: 12,
          fontFamily: 'Inter', fontSize: 40, fontWeight: 900,
          border: '4px solid #000000',
        }}>
          -$1,500 / mo LOST
        </div>

        {/* Rule banner bottom */}
        <div style={{
          position: 'absolute', bottom: 60, left: 0, right: 0, textAlign: 'center',
          opacity: ruleIn, transform: `translateY(${(1 - ruleIn) * 30}px)`,
        }}>
          <div style={{
            display: 'inline-block', background: '#000000', color: '#F4CE14',
            padding: '18px 50px', borderRadius: 10,
            fontFamily: 'Inter', fontSize: 38, fontWeight: 800, letterSpacing: 1,
            borderLeft: '8px solid #ff0000',
          }}>
            SSA RULE: They do NOT combine checks. The lower one is gone.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_OvernightLoss;
```

