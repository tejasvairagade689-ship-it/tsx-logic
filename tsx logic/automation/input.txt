```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1FamilyMemory2019',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed  9999)  10000;
  return x - Math.floor(x);
};

const Scene1FamilyMemory2019: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06], { easing: Easing.inOut(Easing.ease) });

  // Year reveal
  const yearScale = sp(10, { damping: 10, stiffness: 90 });
  const yearOp = interpolate(frame, [10, 30], [0, 1], { extrapolateRight: 'clamp' });

  // Headline
  const headlineOp = interpolate(frame, [35, 55], [0, 1], { extrapolateRight: 'clamp' });
  const headlineY = interpolate(sp(35), [0, 1], [40, 0]);

  // Sub line
  const subOp = interpolate(frame, [70, 90], [0, 1], { extrapolateRight: 'clamp' });

  // Underline draw
  const underline = interpolate(frame, [90, 130], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#E8E6E1', overflow: 'hidden', opacity: exitOp }}>
      {/ Soft gradient base /}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 30%, #F2F0EB 0%, #E0DED8 60%, #D4D2CC 100%)',
      }} />

      {/ Shape layers — drifting geometric /}
      <div style={{
        position: 'absolute', top: -200, left: -200, width: 700, height: 700, borderRadius: '50%',
        border: '2px solid #3B2F2F', opacity: 0.06,
        transform: translate(${Math.sin(frame  0.01)  20}px, ${Math.cos(frame  0.012)  15}px) rotate(${frame  0.1}deg),
      }} />
      <div style={{
        position: 'absolute', bottom: -250, right: -150, width: 600, height: 600,
        border: '2px solid #3B2F2F', opacity: 0.05,
        transform: rotate(${frame  0.15}deg) scale(${1 + Math.sin(frame  0.02)  0.05}),
      }} />
      <div style={{
        position: 'absolute', top: 150, right: 200, width: 300, height: 300,
        background: '#3B2F2F', opacity: 0.03,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
        transform: rotate(${frame  0.2}deg),
      }} />

      {/ Grid texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid1)" />
      </svg>

      {/ Floating particles /}
      {Array.from({ length: 25 }).map((, i) => {
        const r = seededRandom(i + 1);
        const x = r  1920;
        const y = (seededRandom(i + 50)  1080 + frame  0.3) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y, width: 4, height: 4, borderRadius: '50%',
            background: '#3B2F2F', opacity: 0.15,
          }} />
        );
      })}

      {/ Main content /}
      <AbsoluteFill style={{ transform: scale(${camZoom}), transformOrigin: 'center' }}>
        <div style={{
          position: 'absolute', top: 80, left: 100,
          fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 600,
          color: '#3B2F2F', opacity: yearOp, letterSpacing: 4,
        }}>
          ▎FLASHBACK
        </div>

        {/ Big Year /}
        <div style={{
          position: 'absolute', top: 160, left: 100,
          fontFamily: 'Yantra One', fontSize: 320, fontWeight: 900,
          color: '#1C1C1C', lineHeight: 1,
          transform: scale(${yearScale}), opacity: yearOp,
          textShadow: '6px 6px 0px rgba(59,47,47,0.12)',
        }}>
          2019
        </div>

        {/ Headline Hindi /}
        <div style={{
          position: 'absolute', top: 540, left: 100, right: 100,
          fontFamily: 'Yantra One', fontSize: 120, fontWeight: 800,
          color: '#1C1C1C', lineHeight: 1.1,
          opacity: headlineOp, transform: translateY(${headlineY}px),
        }}>
          एक आम भारतीय परिवार
        </div>

        {/ Underline /}
        <div style={{
          position: 'absolute', top: 690, left: 100, height: 10,
          width: ${underline  7}px, background: '#3B2F2F', borderRadius: 5,
        }} />

        {/ Sub line /}
        <div style={{
          position: 'absolute', top: 760, left: 100, right: 100,
          fontFamily: 'Noto Sans Devanagari', fontSize: 58, fontWeight: 500,
          color: '#3B2F2F', opacity: subOp, lineHeight: 1.4,
        }}>
          दिल्ली का एक मध्यवर्गीय घर • रविवार की किराना खरीद
        </div>
      </AbsoluteFill>

      {/ Stick figure — family man with bag /}
      <div style={{
        position: 'absolute', bottom: 60, right: 280,
        transform: translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(50)}),
        opacity: sp(50),
      }}>
        <svg width="180" height="260" viewBox="0 0 180 260">
          <defs>
            <radialGradient id="glow1">
              <stop offset="0%" stopColor="#FFC107" stopOpacity="0.4" />
              <stop offset="100%" stopColor="#FFC107" stopOpacity="0" />
            </radialGradient>
          </defs>
          <circle cx="90" cy="80" r="90" fill="url(#glow1)" />
          <g transform={rotate(${Math.sin(frame  0.04)  3} 90 60)}>
            <circle cx="90" cy="60" r="30" fill="none" stroke="#1C1C1C" strokeWidth="4" />
            <rect x="68" y="55" width="20" height="12" rx="2" fill="none" stroke="#FFC107" strokeWidth="3" />
            <rect x="92" y="55" width="20" height="12" rx="2" fill="none" stroke="#FFC107" strokeWidth="3" />
            <line x1="88" y1="61" x2="92" y2="61" stroke="#FFC107" strokeWidth="3" />
            <path d="M 80 75 Q 90 80 100 75" fill="none" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          </g>
          <line x1="90" y1="90" x2="90" y2="180" stroke="#1C1C1C" strokeWidth="4" />
          <line x1="90" y1="110" x2="55" y2="150" stroke="#1C1C1C" strokeWidth="4" strokeLinecap="round" />
          <line x1="90" y1="110" x2="130" y2="155" stroke="#1C1C1C" strokeWidth="4" strokeLinecap="round" />
          {/ grocery bag /}
          <rect x="120" y="155" width="35" height="40" fill="#3B2F2F" />
          <path d="M 125 155 Q 130 145 137 155 M 138 155 Q 143 145 150 155" fill="none" stroke="#3B2F2F" strokeWidth="2" />
          <line x1="90" y1="180" x2="70" y2="240" stroke="#1C1C1C" strokeWidth="4" strokeLinecap="round" />
          <line x1="90" y1="180" x2="110" y2="240" stroke="#1C1C1C" strokeWidth="4" strokeLinecap="round" />
        </svg>
      </div>

      {/ Vignette /}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(28,28,28,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/ Watermark /}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0, 0.3]),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1FamilyMemory2019;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2GroceryItems5000',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed  9999)  10000;
  return x - Math.floor(x);
};

const Scene2GroceryItems5000: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const items = [
    { name: 'रोटी', icon: 'roti' },
    { name: 'दाल', icon: 'dal' },
    { name: 'सब्ज़ी', icon: 'veg' },
    { name: 'दूध', icon: 'milk' },
    { name: 'अंडे', icon: 'egg' },
    { name: 'चावल', icon: 'rice' },
    { name: 'तेल', icon: 'oil' },
    { name: 'जरूरी', icon: 'basket' },
  ];

  const totalOp = interpolate(frame, [200, 230], [0, 1], { extrapolateRight: 'clamp' });
  const totalScale = sp(200, { damping: 8, stiffness: 100 });

  const Icon = ({ type, color }: { type: string, color: string }) => {
    const s = { width: 80, height: 80, fill: 'none', stroke: color, strokeWidth: 4 };
    if (type === 'roti') return <svg style={s} viewBox="0 0 80 80"><circle cx="40" cy="40" r="30" /><circle cx="32" cy="35" r="2" fill={color} /><circle cx="48" cy="38" r="2" fill={color} /><circle cx="40" cy="48" r="2" fill={color} /></svg>;
    if (type === 'dal') return <svg style={s} viewBox="0 0 80 80"><path d="M 15 35 Q 40 15 65 35 L 60 60 Q 40 70 20 60 Z" /><ellipse cx="35" cy="50" rx="4" ry="2" fill={color} /><ellipse cx="48" cy="52" rx="4" ry="2" fill={color} /></svg>;
    if (type === 'veg') return <svg style={s} viewBox="0 0 80 80"><circle cx="40" cy="45" r="22" /><path d="M 40 23 L 36 15 L 44 15 Z" fill={color} /></svg>;
    if (type === 'milk') return <svg style={s} viewBox="0 0 80 80"><path d="M 28 18 L 52 18 L 56 28 L 56 65 L 24 65 L 24 28 Z" /><line x1="32" y1="40" x2="48" y2="40" /></svg>;
    if (type === 'egg') return <svg style={s} viewBox="0 0 80 80"><ellipse cx="40" cy="42" rx="22" ry="28" /></svg>;
    if (type === 'rice') return <svg style={s} viewBox="0 0 80 80"><path d="M 20 50 Q 40 30 60 50 L 55 65 L 25 65 Z" /><ellipse cx="35" cy="55" rx="3" ry="1.5" fill={color} /><ellipse cx="45" cy="58" rx="3" ry="1.5" fill={color} /></svg>;
    if (type === 'oil') return <svg style={s} viewBox="0 0 80 80"><path d="M 32 15 L 48 15 L 48 25 L 55 30 L 55 65 L 25 65 L 25 30 L 32 25 Z" /></svg>;
    return <svg style={s} viewBox="0 0 80 80"><path d="M 15 30 L 65 30 L 60 65 L 20 65 Z" /><path d="M 25 30 Q 40 10 55 30" /></svg>;
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#E8E6E1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 40%, #F2F0EB 0%, #DDDBD5 100%)',
      }} />

      {/ Shape layers /}
      <div style={{
        position: 'absolute', top: 100, left: -100, width: 500, height: 500,
        background: '#3B2F2F', opacity: 0.04, borderRadius: '50%',
        transform: translate(${Math.cos(frame  0.015)  30}px, ${Math.sin(frame  0.013)  20}px),
      }} />
      <div style={{
        position: 'absolute', bottom: -100, right: -100, width: 450, height: 450,
        border: '3px solid #1C1C1C', opacity: 0.05,
        transform: rotate(${frame  0.12}deg),
      }} />

      {/ Dotted texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="dots2" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#1C1C1C" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots2)" />
      </svg>

      {/ Particles /}
      {Array.from({ length: 20 }).map((, i) => {
        const r = seededRandom(i + 10);
        const x = (r  1920 + frame  0.4) % 1920;
        const y = seededRandom(i + 99)  1080;
        return <div key={i} style={{
          position: 'absolute', left: x, top: y, width: 5, height: 5,
          background: '#3B2F2F', opacity: 0.12, borderRadius: '50%',
        }} />;
      })}

      {/ Title /}
      <div style={{
        position: 'absolute', top: 60, left: 100,
        fontFamily: 'Yantra One', fontSize: 96, color: '#1C1C1C', fontWeight: 800,
        opacity: sp(5), transform: translateY(${interpolate(sp(5), [0, 1], [-30, 0])}px),
      }}>
        रविवार की <span style={{ color: '#3B2F2F' }}>थैली</span>
      </div>
      <div style={{
        position: 'absolute', top: 175, left: 100,
        fontFamily: 'Noto Sans Devanagari', fontSize: 42, color: '#3B2F2F', fontWeight: 500,
        opacity: interpolate(frame, [25, 50], [0, 1], { extrapolateRight: 'clamp' }),
      }}>
        2019 का मध्यमवर्गीय घर
      </div>

      {/ Items grid 4x2 /}
      <div style={{
        position: 'absolute', top: 290, left: 100, right: 100,
        display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 30,
      }}>
        {items.map((item, i) => {
          const delay = 50 + i  18;
          const s = sp(delay, { damping: 9, stiffness: 110 });
          const op = interpolate(frame, [delay, delay + 20], [0, 1], { extrapolateRight: 'clamp' });
          return (
            <div key={i} style={{
              background: '#F5F3EE', border: '3px solid #1C1C1C', borderRadius: 18,
              padding: 24, display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12,
              transform: scale(${s}) translateY(${(1 - s)  30}px), opacity: op,
              boxShadow: '6px 6px 0px #3B2F2F',
            }}>
              <Icon type={item.icon} color="#1C1C1C" />
              <div style={{ fontFamily: 'Yantra One', fontSize: 44, color: '#1C1C1C', fontWeight: 700 }}>
                {item.name}
              </div>
            </div>
          );
        })}
      </div>

      {/ Total bill /}
      <div style={{
        position: 'absolute', bottom: 60, left: 100,
        opacity: totalOp, transform: scale(${totalScale}), transformOrigin: 'left',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 38, color: '#3B2F2F', fontWeight: 600, letterSpacing: 2,
        }}>
          कुल खर्च →
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 200, color: '#1C1C1C', fontWeight: 900, lineHeight: 1,
        }}>
          ₹5,000
        </div>
      </div>

      {/ Stick figure /}
      <div style={{
        position: 'absolute', bottom: 80, right: 120,
        transform: translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(60)}),
        opacity: sp(60),
      }}>
        <svg width="160" height="240" viewBox="0 0 160 240">
          <defs>
            <radialGradient id="glow2">
              <stop offset="0%" stopColor="#FFC107" stopOpacity="0.35" />
              <stop offset="100%" stopColor="#FFC107" stopOpacity="0" />
            </radialGradient>
          </defs>
          <circle cx="80" cy="70" r="80" fill="url(#glow2)" />
          <g transform={rotate(${Math.sin(frame  0.04)  3} 80 55)}>
            <circle cx="80" cy="55" r="28" fill="none" stroke="#1C1C1C" strokeWidth="4" />
            <rect x="60" y="50" width="18" height="11" rx="2" fill="none" stroke="#FFC107" strokeWidth="3" />
            <rect x="82" y="50" width="18" height="11" rx="2" fill="none" stroke="#FFC107" strokeWidth="3" />
            <path d="M 72 68 Q 80 73 88 68" fill="none" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          </g>
          <line x1="80" y1="83" x2="80" y2="165" stroke="#1C1C1C" strokeWidth="4" />
          <line x1="80" y1="100" x2="48" y2="135" stroke="#1C1C1C" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="100" x2="115" y2="135" stroke="#1C1C1C" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="165" x2="62" y2="220" stroke="#1C1C1C" strokeWidth="4" strokeLinecap="round" />
          <line x1="80" y1="165" x2="98" y2="220" stroke="#1C1C1C" strokeWidth="4" strokeLinecap="round" />
        </svg>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(28,28,28,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2GroceryItems5000;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3StableNumber',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed  9999)  10000;
  return x - Math.floor(x);
};

const Scene3StableNumber: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // flat line graph
  const lineDraw = interpolate(frame, [40, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const billCards = ['बिजली बिल', 'किराया', 'किराना'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#E8E6E1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #EFEDE7 0%, #DAD8D2 100%)',
      }} />

      {/ Drifting shapes /}
      <div style={{
        position: 'absolute', top: 50, right: 80, width: 380, height: 380,
        border: '3px solid #3B2F2F', borderRadius: 30, opacity: 0.05,
        transform: rotate(${frame  0.1 + 15}deg) scale(${1 + Math.sin(frame  0.02)  0.04}),
      }} />
      <div style={{
        position: 'absolute', bottom: -150, left: -150, width: 500, height: 500, borderRadius: '50%',
        background: '#1C1C1C', opacity: 0.04,
        transform: translate(${Math.sin(frame  0.01)  15}px, 0),
      }} />

      {/ Diagonal line texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="diag3" width="30" height="30" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="30" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag3)" />
      </svg>

      {/ Particles /}
      {Array.from({ length: 18 }).map((, i) => {
        const r = seededRandom(i + 30);
        const x = r  1920;
        const y = (seededRandom(i + 70)  1080 - frame  0.25 + 1080) % 1080;
        return <div key={i} style={{
          position: 'absolute', left: x, top: y, width: 3, height: 3,
          background: '#3B2F2F', opacity: 0.18, borderRadius: '50%',
        }} />;
      })}

      {/ Headline /}
      <div style={{
        position: 'absolute', top: 80, left: 100, right: 100,
        opacity: sp(5),
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 36, color: '#3B2F2F', fontWeight: 600, letterSpacing: 4,
        }}>
          ▎एक स्थिर नंबर
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 130, color: '#1C1C1C', fontWeight: 900, lineHeight: 1.05, marginTop: 12,
        }}>
          जिंदगी का <span style={{
            background: '#3B2F2F', color: '#E8E6E1', padding: '0 18px',
          }}>हिस्सा</span>
        </div>
      </div>

      {/ Flat line chart /}
      <div style={{
        position: 'absolute', top: 430, left: 100, right: 100, height: 280,
        background: '#F5F3EE', border: '3px solid #1C1C1C', borderRadius: 20, padding: 30,
        boxShadow: '8px 8px 0px #3B2F2F',
        opacity: interpolate(frame, [30, 55], [0, 1], { extrapolateRight: 'clamp' }),
      }}>
        <div style={{ fontFamily: 'Inter, Poppins', fontSize: 28, color: '#3B2F2F', fontWeight: 600 }}>
          MONTHLY GROCERY • 2015 – 2019
        </div>
        <svg width="100%" height="180" viewBox="0 0 1600 180" style={{ marginTop: 12 }}>
          {/ horizontal guides /}
          {[40, 90, 140].map((y, i) => (
            <line key={i} x1="0" y1={y} x2="1600" y2={y} stroke="#9E9E9E" strokeWidth="1" strokeDasharray="6 6" opacity="0.4" />
          ))}
          {/ flat line /}
          <line
            x1="20" y1="90"
            x2={20 + 1560  lineDraw} y2="90"
            stroke="#1C1C1C" strokeWidth="8" strokeLinecap="round"
          />
          {/ dots /}
          {[0, 0.25, 0.5, 0.75, 1].map((p, i) => (
            lineDraw > p && (
              <circle key={i} cx={20 + 1560  p} cy={90} r="10" fill="#3B2F2F" stroke="#1C1C1C" strokeWidth="3" />
            )
          ))}
          {/ labels /}
          <text x="20" y="170" fontFamily="Inter" fontSize="22" fill="#3B2F2F" fontWeight="600">2015</text>
          <text x="1540" y="170" fontFamily="Inter" fontSize="22" fill="#3B2F2F" fontWeight="600" textAnchor="end">2019</text>
          <text x="800" y="70" fontFamily="Yantra One" fontSize="32" fill="#1C1C1C" fontWeight="800" textAnchor="middle">
            ₹5,000 / month
          </text>
        </svg>
      </div>

      {/ Bill cards /}
      <div style={{
        position: 'absolute', bottom: 80, left: 100, right: 100,
        display: 'flex', gap: 24,
      }}>
        {billCards.map((b, i) => {
          const d = 140 + i  20;
          const s = sp(d, { damping: 10, stiffness: 110 });
          return (
            <div key={i} style={{
              flex: 1, background: i === 2 ? '#1C1C1C' : '#F5F3EE',
              color: i === 2 ? '#E8E6E1' : '#1C1C1C',
              border: '3px solid #1C1C1C', borderRadius: 14,
              padding: '22px 26px',
              fontFamily: 'Yantra One', fontSize: 52, fontWeight: 700,
              transform: scale(${s}) translateX(${(1 - s)  -50}px),
              opacity: s, textAlign: 'center',
              boxShadow: i === 2 ? '6px 6px 0px #FFC107' : '6px 6px 0px #3B2F2F',
            }}>
              {b}
            </div>
          );
        })}
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(28,28,28,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3StableNumber;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4May2026Arrived',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed  9999)  10000;
  return x - Math.floor(x);
};

const Scene4May2026Arrived: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // year flip 2019 -> 2026
  const flipProgress = interpolate(frame, [25, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.7, 0, 0.3, 1) });
  const oldYearY = -200  flipProgress;
  const newYearY = 200  (1 - flipProgress);

  const items = ['वही दुकान', 'वही सामान', 'वही रूटीन'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#E8E6E1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at top, #F2F0EB 0%, #D5D3CD 100%)',
      }} />

      {/ Hex shape /}
      <div style={{
        position: 'absolute', top: 100, left: 80, width: 280, height: 280,
        background: '#3B2F2F', opacity: 0.05,
        clipPath: 'polygon(50% 0%, 93% 25%, 93% 75%, 50% 100%, 7% 75%, 7% 25%)',
        transform: rotate(${frame  0.15}deg),
      }} />
      <div style={{
        position: 'absolute', bottom: 60, right: 100, width: 220, height: 220,
        border: '3px solid #1C1C1C', opacity: 0.06,
        clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
        transform: rotate(${-frame  0.2}deg) scale(${1 + Math.sin(frame  0.03)  0.06}),
      }} />

      {/ Concentric circles texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {[100, 200, 300, 400, 500].map((r, i) => (
          <circle key={i} cx={960 + Math.sin(frame  0.01)  20} cy={540} r={r} fill="none" stroke="#1C1C1C" strokeWidth="1" />
        ))}
      </svg>

      {/ Particles /}
      {Array.from({ length: 22 }).map((, i) => {
        const r = seededRandom(i + 5);
        const x = (r  1920 + frame  0.5) % 1920;
        const y = seededRandom(i + 88)  1080;
        return <div key={i} style={{
          position: 'absolute', left: x, top: y, width: 4, height: 4,
          background: '#3B2F2F', opacity: 0.14, borderRadius: '50%',
        }} />;
      })}

      {/ TOP — "लेकिन अब" badge /}
      <div style={{
        position: 'absolute', top: 90, left: 100,
        background: '#1C1C1C', color: '#E8E6E1',
        padding: '14px 32px', borderRadius: 40,
        fontFamily: 'Yantra One', fontSize: 44, fontWeight: 700,
        transform: scale(${sp(5)}), opacity: sp(5),
        boxShadow: '5px 5px 0px #3B2F2F',
      }}>
        लेकिन अब...
      </div>

      {/ Year flip stage /}
      <div style={{
        position: 'absolute', top: 200, left: 100, right: 100, height: 360,
        overflow: 'hidden',
      }}>
        <div style={{
          position: 'absolute', top: oldYearY, left: 0, width: '100%',
          fontFamily: 'Yantra One', fontSize: 360, color: '#9E9E9E',
          fontWeight: 900, lineHeight: 1, opacity: 1 - flipProgress,
          textDecoration: 'line-through', textDecorationColor: '#3B2F2F', textDecorationThickness: 12,
        }}>
          2019
        </div>
        <div style={{
          position: 'absolute', top: newYearY, left: 0, width: '100%',
          fontFamily: 'Yantra One', fontSize: 380, color: '#1C1C1C',
          fontWeight: 900, lineHeight: 1, opacity: flipProgress,
          textShadow: '8px 8px 0px rgba(59,47,47,0.2)',
        }}>
          मई 2026
        </div>
      </div>

      {/ Three "वही" cards /}
      <div style={{
        position: 'absolute', bottom: 150, left: 100, right: 100,
        display: 'flex', gap: 28, justifyContent: 'space-between',
      }}>
        {items.map((it, i) => {
          const d = 80 + i  22;
          const s = sp(d, { damping: 9, stiffness: 110 });
          return (
            <div key={i} style={{
              flex: 1, background: '#F5F3EE', border: '4px solid #1C1C1C',
              borderRadius: 18, padding: '28px 24px', textAlign: 'center',
              fontFamily: 'Yantra One', fontSize: 64, fontWeight: 800, color: '#1C1C1C',
              transform: scale(${s}) translateY(${(1 - s)  60}px), opacity: s,
              boxShadow: '8px 8px 0px #3B2F2F',
            }}>
              {it}
            </div>
          );
        })}
      </div>

      {/ Stick figure looking surprised /}
      <div style={{
        position: 'absolute', top: 240, right: 140,
        transform: translateY(${Math.sin(frame  0.05)  2}px) scale(${sp(80)}),
        opacity: sp(80),
      }}>
        <svg width="140" height="200" viewBox="0 0 140 200">
          <defs>
            <radialGradient id="glow4">
              <stop offset="0%" stopColor="#FFC107" stopOpacity="0.35" />
              <stop offset="100%" stopColor="#FFC107" stopOpacity="0" />
            </radialGradient>
          </defs>
          <circle cx="70" cy="60" r="70" fill="url(#glow4)" />
          <g transform={rotate(${Math.sin(frame  0.04)  4} 70 50)}>
            <circle cx="70" cy="50" r="26" fill="none" stroke="#1C1C1C" strokeWidth="4" />
            <rect x="51" y="46" width="17" height="11" rx="2" fill="none" stroke="#FFC107" strokeWidth="3" />
            <rect x="72" y="46" width="17" height="11" rx="2" fill="none" stroke="#FFC107" strokeWidth="3" />
            <circle cx="70" cy="68" r="3" fill="none" stroke="#1C1C1C" strokeWidth="2.5" />
          </g>
          <line x1="70" y1="76" x2="70" y2="140" stroke="#1C1C1C" strokeWidth="4" />
          <line x1="70" y1="90" x2="40" y2="115" stroke="#1C1C1C" strokeWidth="4" strokeLinecap="round" />
          <line x1="70" y1="90" x2="100" y2="115" stroke="#1C1C1C" strokeWidth="4" strokeLinecap="round" />
          <line x1="70" y1="140" x2="55" y2="190" stroke="#1C1C1C" strokeWidth="4" strokeLinecap="round" />
          <line x1="70" y1="140" x2="85" y2="190" stroke="#1C1C1C" strokeWidth="4" strokeLinecap="round" />
        </svg>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(28,28,28,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4May2026Arrived;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5Bill9000Shock',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed  9999)  10000;
  return x - Math.floor(x);
};

const Scene5Bill9000Shock: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // counter 5000 → 9000
  const counterProgress = interpolate(frame, [40, 140], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.3, 0, 0.2, 1) });
  const counterValue = Math.round(5000 + 4000  counterProgress);

  // screen shake on impact
  const shakeFrames = frame >= 145 && frame <= 165;
  const shakeX = shakeFrames ? Math.sin(frame  3)  8  (1 - (frame - 145) / 20) : 0;
  const shakeY = shakeFrames ? Math.cos(frame  4)  6  (1 - (frame - 145) / 20) : 0;

  // arrow draw
  const arrowDraw = interpolate(frame, [160, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#E8E6E1', overflow: 'hidden', opacity: exitOp, transform: translate(${shakeX}px, ${shakeY}px) }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 60%, #F0EEE9 0%, #D2D0CA 100%)',
      }} />

      {/ Impact shapes /}
      <div style={{
        position: 'absolute', top: -120, left: -120, width: 600, height: 600, borderRadius: '50%',
        background: '#3B2F2F', opacity: 0.05 + (frame > 140 ? 0.04 : 0),
        transform: scale(${1 + Math.sin(frame  0.02)  0.05}),
      }} />
      <div style={{
        position: 'absolute', bottom: -100, right: -100, width: 500, height: 500,
        border: '4px solid #1C1C1C', opacity: 0.06, borderRadius: 40,
        transform: rotate(${frame  0.15}deg),
      }} />

      {/ Vertical line texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        <defs>
          <pattern id="vlines5" width="20" height="20" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="20" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#vlines5)" />
      </svg>

      {/ Particles — impact burst after frame 145 /}
      {Array.from({ length: 30 }).map((, i) => {
        const r = seededRandom(i + 200);
        const burst = frame > 145 ? Math.min(1, (frame - 145) / 30) : 0;
        const angle = r  Math.PI  2;
        const dist = burst  400;
        const x = 960 + Math.cos(angle)  dist;
        const y = 540 + Math.sin(angle)  dist;
        return <div key={i} style={{
          position: 'absolute', left: x, top: y, width: 6, height: 6,
          background: '#3B2F2F', opacity: 0.5  (1 - burst), borderRadius: '50%',
        }} />;
      })}

      {/ Receipt card centered /}
      <div style={{
        position: 'absolute', top: 90, left: '50%',
        transform: translateX(-50%) scale(${sp(5, { damping: 10, stiffness: 90 })}),
        opacity: sp(5),
        background: '#F5F3EE', border: '4px solid #1C1C1C',
        borderRadius: 20, padding: '40px 80px', minWidth: 1100,
        boxShadow: '12px 12px 0px #3B2F2F',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 32, color: '#3B2F2F',
          fontWeight: 600, letterSpacing: 4, textAlign: 'center',
        }}>
          ▎NEW GROCERY BILL — MAY 2026
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 70, color: '#1C1C1C',
          fontWeight: 800, textAlign: 'center', marginTop: 16,
        }}>
          वही सामान • वही दुकान
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 320, color: '#1C1C1C',
          fontWeight: 900, textAlign: 'center', lineHeight: 1, marginTop: 20,
          textShadow: '8px 8px 0px rgba(59,47,47,0.15)',
        }}>
          ₹{counterValue.toLocaleString('en-IN')}
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 36, color: '#3B2F2F',
          fontWeight: 600, textAlign: 'center', marginTop: 6,
        }}>
          से ऊपर जा चुका है
        </div>
      </div>

      {/ Arrow showing jump 5K → 9K /}
      <div style={{
        position: 'absolute', bottom: 80, left: 100, right: 100,
        display: 'flex', alignItems: 'center', justifyContent: 'space-between',
        opacity: arrowDraw,
      }}>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 80, fontWeight: 800, color: '#9E9E9E',
          textDecoration: 'line-through',
        }}>
          ₹5,000
        </div>
        <svg width={600  arrowDraw} height="60" viewBox="0 0 600 60" style={{ flexShrink: 0 }}>
          <line x1="10" y1="30" x2="560" y2="30" stroke="#1C1C1C" strokeWidth="8" strokeLinecap="round" />
          <path d="M 540 10 L 580 30 L 540 50" fill="none" stroke="#1C1C1C" strokeWidth="8" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 100, fontWeight: 900, color: '#1C1C1C',
          background: '#FFC107', padding: '8px 28px', border: '4px solid #1C1C1C',
          boxShadow: '6px 6px 0px #3B2F2F',
        }}>
          ₹9,000+
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(28,28,28,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5Bill9000Shock;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6BrandSwitch',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed  9999)  10000;
  return x - Math.floor(x);
};

const Scene6BrandSwitch: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // brand cross-out
  const cross = interpolate(frame, [60, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const arrowOp = interpolate(frame, [110, 140], [0, 1], { extrapolateRight: 'clamp' });
  const localScale = sp(140, { damping: 9, stiffness: 110 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#E8E6E1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #F0EEE9 0%, #D8D6D0 100%)',
      }} />

      {/ Tilted rect shape /}
      <div style={{
        position: 'absolute', top: -100, right: -50, width: 500, height: 500,
        background: '#3B2F2F', opacity: 0.04,
        transform: rotate(${15 + frame  0.08}deg),
      }} />
      <div style={{
        position: 'absolute', bottom: -80, left: 100, width: 300, height: 300, borderRadius: '50%',
        border: '3px solid #1C1C1C', opacity: 0.06,
        transform: scale(${1 + Math.sin(frame  0.025)  0.05}),
      }} />

      {/ Cross-hatch texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        <defs>
          <pattern id="cross6" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 0 40 L 40 0 M -10 10 L 10 -10 M 30 50 L 50 30" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross6)" />
      </svg>

      {/ Particles /}
      {Array.from({ length: 18 }).map((, i) => {
        const r = seededRandom(i + 60);
        const x = (r  1920 - frame  0.3 + 1920) % 1920;
        const y = seededRandom(i + 120)  1080;
        return <div key={i} style={{
          position: 'absolute', left: x, top: y, width: 4, height: 4,
          background: '#3B2F2F', opacity: 0.13, borderRadius: '50%',
        }} />;
      })}

      {/ Title /}
      <div style={{
        position: 'absolute', top: 70, left: 100, opacity: sp(5),
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 36, color: '#3B2F2F',
          fontWeight: 600, letterSpacing: 4,
        }}>
          ▎कंजूसी की कोशिश
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 130, color: '#1C1C1C', fontWeight: 900, lineHeight: 1.05, marginTop: 10,
        }}>
          ब्रांड बदल लिए...
        </div>
      </div>

      {/ Switch visualization /}
      <div style={{
        position: 'absolute', top: 370, left: 100, right: 100,
        display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 40,
      }}>
        {/ Branded /}
        <div style={{
          flex: 1, background: '#F5F3EE', border: '4px solid #1C1C1C',
          borderRadius: 18, padding: '36px 30px', position: 'relative',
          transform: scale(${sp(20)}), opacity: sp(20),
          boxShadow: '8px 8px 0px #3B2F2F',
        }}>
          <svg width="80" height="80" viewBox="0 0 80 80" style={{ display: 'block', margin: '0 auto 16px' }}>
            <rect x="15" y="15" width="50" height="50" rx="8" fill="none" stroke="#1C1C1C" strokeWidth="4" />
            <text x="40" y="48" fontFamily="Yantra One" fontSize="24" fontWeight="800" fill="#1C1C1C" textAnchor="middle">★</text>
          </svg>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 64, color: '#1C1C1C',
            fontWeight: 800, textAlign: 'center',
          }}>
            ब्रांडेड
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 28, color: '#3B2F2F',
            textAlign: 'center', marginTop: 8, fontWeight: 500,
          }}>
            Premium FMCG
          </div>
          {/ cross line /}
          <svg style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%', pointerEvents: 'none' }} viewBox="0 0 100 100" preserveAspectRatio="none">
            <line x1="5" y1="5" x2={5 + 90  cross} y2={5 + 90  cross} stroke="#ff0000" strokeWidth="2.5" />
          </svg>
        </div>

        {/ Arrow /}
        <div style={{ opacity: arrowOp }}>
          <svg width="120" height="80" viewBox="0 0 120 80">
            <line x1="10" y1="40" x2="100" y2="40" stroke="#1C1C1C" strokeWidth="8" strokeLinecap="round" />
            <path d="M 85 20 L 115 40 L 85 60" fill="none" stroke="#1C1C1C" strokeWidth="8" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/ Local /}
        <div style={{
          flex: 1, background: '#1C1C1C', color: '#E8E6E1',
          border: '4px solid #1C1C1C', borderRadius: 18, padding: '36px 30px',
          transform: scale(${localScale}), opacity: localScale,
          boxShadow: '8px 8px 0px #FFC107',
        }}>
          <svg width="80" height="80" viewBox="0 0 80 80" style={{ display: 'block', margin: '0 auto 16px' }}>
            <path d="M 15 25 L 65 25 L 60 65 L 20 65 Z" fill="none" stroke="#E8E6E1" strokeWidth="4" />
            <path d="M 25 25 Q 40 10 55 25" fill="none" stroke="#E8E6E1" strokeWidth="4" />
          </svg>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 64, fontWeight: 800, textAlign: 'center',
          }}>
            लोकल ब्रांड
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 28, color: '#FFC107',
            textAlign: 'center', marginTop: 8, fontWeight: 500,
          }}>
            सस्ता विकल्प
          </div>
        </div>
      </div>

      {/ Bottom note /}
      <div style={{
        position: 'absolute', bottom: 80, left: 100, right: 100,
        textAlign: 'center', opacity: interpolate(frame, [180, 220], [0, 1], { extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 50, color: '#3B2F2F', fontWeight: 600,
        }}>
          फिर भी... कुछ बदला नहीं
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(28,28,28,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6BrandSwitch;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7BillDoubled',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed  9999)  10000;
  return x - Math.floor(x);
};

const Scene7BillDoubled: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // bar grow
  const barL = interpolate(frame, [50, 100], [0, 1], { extrapolateRight: 'clamp', easing: Easing.bezier(0.3, 0, 0.2, 1) });
  const barR = interpolate(frame, [110, 180], [0, 1], { extrapolateRight: 'clamp', easing: Easing.bezier(0.3, 0, 0.2, 1) });

  const x2Scale = sp(200, { damping: 6, stiffness: 90 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#E8E6E1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at bottom, #F0EEE9 0%, #D4D2CC 100%)',
      }} />

      {/ Triangle shape /}
      <div style={{
        position: 'absolute', top: 80, right: 60, width: 350, height: 350,
        background: '#3B2F2F', opacity: 0.04,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
        transform: rotate(${frame  0.12}deg),
      }} />
      <div style={{
        position: 'absolute', bottom: -100, left: -100, width: 450, height: 450,
        border: '3px solid #1C1C1C', opacity: 0.05, borderRadius: 30,
        transform: rotate(${-frame  0.1}deg),
      }} />

      {/ Dot grid texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="dotgrid7" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#1C1C1C" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dotgrid7)" />
      </svg>

      {/ Particles /}
      {Array.from({ length: 20 }).map((, i) => {
        const r = seededRandom(i + 77);
        const x = r  1920;
        const y = (seededRandom(i + 22)  1080 + frame  0.4) % 1080;
        return <div key={i} style={{
          position: 'absolute', left: x, top: y, width: 4, height: 4,
          background: '#3B2F2F', opacity: 0.15, borderRadius: '50%',
        }} />;
      })}

      {/ Headline /}
      <div style={{
        position: 'absolute', top: 70, left: 100, opacity: sp(5),
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 36, color: '#3B2F2F',
          fontWeight: 600, letterSpacing: 4,
        }}>
          ▎कम खरीदा • ज़्यादा दिया
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 120, color: '#1C1C1C',
          fontWeight: 900, lineHeight: 1.05, marginTop: 8,
        }}>
          बिल <span style={{ background: '#FFC107', padding: '0 16px' }}>दुगना</span>
        </div>
      </div>

      {/ Two bars comparison /}
      <div style={{
        position: 'absolute', bottom: 130, left: 120, right: 120, height: 460,
        display: 'flex', alignItems: 'flex-end', justifyContent: 'space-around', gap: 80,
      }}>
        {/ Old bar /}
        <div style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 90, color: '#1C1C1C', fontWeight: 900,
            marginBottom: 12, opacity: interpolate(frame, [50, 80], [0, 1], { extrapolateRight: 'clamp' }),
          }}>
            ₹5K
          </div>
          <div style={{
            width: '100%', height: 220  barL,
            background: '#9E9E9E', border: '4px solid #1C1C1C', borderRadius: '10px 10px 0 0',
            boxShadow: '6px 6px 0px #3B2F2F',
          }} />
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 34, color: '#3B2F2F',
            fontWeight: 600, marginTop: 16,
          }}>
            2019
          </div>
        </div>

        {/ X2 badge /}
        <div style={{
          transform: scale(${x2Scale}) rotate(${-10 + Math.sin(frame  0.1)  4}deg),
          opacity: x2Scale,
          background: '#ff0000', color: '#F5F5F5',
          padding: '20px 36px', borderRadius: 20,
          fontFamily: 'Yantra One', fontSize: 100, fontWeight: 900,
          border: '5px solid #1C1C1C', boxShadow: '8px 8px 0px #1C1C1C',
        }}>
          ×2
        </div>

        {/ New bar /}
        <div style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 110, color: '#1C1C1C', fontWeight: 900,
            marginBottom: 12, opacity: interpolate(frame, [110, 140], [0, 1], { extrapolateRight: 'clamp' }),
          }}>
            ₹9K+
          </div>
          <div style={{
            width: '100%', height: 420  barR,
            background: '#1C1C1C', border: '4px solid #1C1C1C', borderRadius: '10px 10px 0 0',
            boxShadow: '6px 6px 0px #3B2F2F',
          }} />
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 34, color: '#3B2F2F',
            fontWeight: 600, marginTop: 16,
          }}>
            2026
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(28,28,28,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7BillDoubled;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8PatientReport',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed  9999)  10000;
  return x - Math.floor(x);
};

const Scene8PatientReport: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // ECG line draw
  const ecg = interpolate(frame, [50, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const reportRows = [
    { label: 'Monthly Spend', val: '↑ 80%', critical: true },
    { label: 'Rupee Strength', val: '↓ 26%', critical: true },
    { label: 'Food Inflation', val: 'ABNORMAL', critical: true },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#E8E6E1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #F0EEE9 0%, #D6D4CE 100%)',
      }} />

      {/ Plus shapes (medical) /}
      <div style={{
        position: 'absolute', top: 100, left: 70, width: 160, height: 160, opacity: 0.05,
        transform: rotate(${frame  0.15}deg),
      }}>
        <div style={{ position: 'absolute', top: '40%', left: 0, width: '100%', height: '20%', background: '#1C1C1C' }} />
        <div style={{ position: 'absolute', left: '40%', top: 0, height: '100%', width: '20%', background: '#1C1C1C' }} />
      </div>
      <div style={{
        position: 'absolute', bottom: 80, right: 80, width: 350, height: 350, borderRadius: '50%',
        border: '3px solid #1C1C1C', opacity: 0.05,
        transform: scale(${1 + Math.sin(frame  0.03)  0.05}),
      }} />

      {/ Horizontal lines texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        <defs>
          <pattern id="hlines8" width="20" height="20" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="20" y2="0" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hlines8)" />
      </svg>

      {/ Particles /}
      {Array.from({ length: 18 }).map((, i) => {
        const r = seededRandom(i + 130);
        const x = (r  1920 + frame  0.3) % 1920;
        const y = seededRandom(i + 55)  1080;
        return <div key={i} style={{
          position: 'absolute', left: x, top: y, width: 3, height: 3,
          background: '#3B2F2F', opacity: 0.15, borderRadius: '50%',
        }} />;
      })}

      {/ Title /}
      <div style={{
        position: 'absolute', top: 60, left: 100, right: 100, opacity: sp(5),
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 36, color: '#3B2F2F',
          fontWeight: 600, letterSpacing: 4,
        }}>
          ▎एक मरीज़ की रिपोर्ट
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 120, color: '#1C1C1C',
          fontWeight: 900, lineHeight: 1.05, marginTop: 8,
        }}>
          कुछ बुनियादी बदल गया
        </div>
      </div>

      {/ Report Card /}
      <div style={{
        position: 'absolute', top: 340, left: 100, width: 1100,
        background: '#F5F3EE', border: '4px solid #1C1C1C', borderRadius: 18,
        padding: '30px 40px', boxShadow: '10px 10px 0px #3B2F2F',
        transform: scale(${sp(30, { damping: 10, stiffness: 100 })}),
        opacity: sp(30), transformOrigin: 'top left',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 30, color: '#3B2F2F',
          fontWeight: 700, letterSpacing: 3, marginBottom: 18,
        }}>
          ECONOMIC LAB REPORT — PATIENT: AAM AADMI
        </div>
        <div style={{ borderTop: '2px dashed #3B2F2F', paddingTop: 18 }}>
          {reportRows.map((r, i) => {
            const d = 70 + i  25;
            const op = interpolate(frame, [d, d + 20], [0, 1], { extrapolateRight: 'clamp' });
            return (
              <div key={i} style={{
                display: 'flex', justifyContent: 'space-between', alignItems: 'center',
                padding: '14px 0', borderBottom: '1px solid #9E9E9E',
                opacity: op, transform: translateX(${(1 - op)  -30}px),
              }}>
                <div style={{ fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 600, color: '#1C1C1C' }}>
                  {r.label}
                </div>
                <div style={{
                  fontFamily: 'Yantra One', fontSize: 56, fontWeight: 800,
                  color: '#ff0000',
                  background: '#1C1C1C', padding: '4px 20px', borderRadius: 8,
                }}>
                  {r.val}
                </div>
              </div>
            );
          })}
        </div>
      </div>

      {/ ECG line on right /}
      <div style={{
        position: 'absolute', top: 380, right: 80, width: 580, height: 350,
        background: '#1C1C1C', border: '4px solid #1C1C1C', borderRadius: 18,
        padding: 24, transform: scale(${sp(40)}), opacity: sp(40),
        boxShadow: '8px 8px 0px #FFC107',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 24, color: '#FFC107',
          fontWeight: 600, letterSpacing: 3,
        }}>
          ▎LIVE ECG
        </div>
        <svg width="100%" height="260" viewBox="0 0 540 260" style={{ marginTop: 8 }}>
          {[60, 130, 200].map((y, i) => (
            <line key={i} x1="0" y1={y} x2="540" y2={y} stroke="#9E9E9E" strokeWidth="1" opacity="0.3" />
          ))}
          <path
            d={M 0 130 L ${100  ecg} 130 L ${110  ecg} 130 L ${120  ecg} 60 L ${130  ecg} 200 L ${145  ecg} 130 L ${250  ecg} 130 L ${260  ecg} 60 L ${270  ecg} 200 L ${285  ecg} 130 L ${400  ecg} 130 L ${410  ecg} 60 L ${420  ecg} 220 L ${440  ecg} 130 L ${540  ecg} 130}
            fill="none" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round"
          />
        </svg>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(28,28,28,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8PatientReport;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9StructuralChange',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed  9999)  10000;
  return x - Math.floor(x);
};

const Scene9StructuralChange: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // cross out "temporary"
  const cross = interpolate(frame, [70, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#E8E6E1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 70%, #F0EEE9 0%, #D6D4CE 100%)',
      }} />

      {/ Big circle /}
      <div style={{
        position: 'absolute', top: -200, right: -200, width: 700, height: 700, borderRadius: '50%',
        background: '#3B2F2F', opacity: 0.04,
        transform: scale(${1 + Math.sin(frame  0.02)  0.04}),
      }} />
      <div style={{
        position: 'absolute', bottom: -150, left: -150, width: 500, height: 500,
        border: '4px solid #1C1C1C', opacity: 0.06,
        clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
        transform: rotate(${frame  0.13}deg),
      }} />

      {/ Brick texture /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="brick9" width="80" height="40" patternUnits="userSpaceOnUse">
            <rect x="0" y="0" width="80" height="40" fill="none" stroke="#1C1C1C" strokeWidth="1" />
            <rect x="40" y="20" width="80" height="40" fill="none" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#brick9)" />
      </svg>

      {/ Particles /}
      {Array.from({ length: 22 }).map((, i) => {
        const r = seededRandom(i + 88);
        const x = r  1920;
        const y = (seededRandom(i + 33)  1080 - frame  0.35 + 1080) % 1080;
        return <div key={i} style={{
          position: 'absolute', left: x, top: y, width: 4, height: 4,
          background: '#3B2F2F', opacity: 0.14, borderRadius: '50%',
        }} />;
      })}

      {/ "Temporary" crossed out /}
      <div style={{
        position: 'absolute', top: 140, left: '50%',
        transform: translateX(-50%) scale(${sp(5)}), opacity: sp(5),
      }}>
        <div style={{ position: 'relative', display: 'inline-block' }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 130, color: '#9E9E9E', fontWeight: 800, lineHeight: 1,
          }}>
            अस्थाई उछाल
          </div>
          <svg style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%' }} viewBox="0 0 100 30" preserveAspectRatio="none">
            <line x1="2" y1="15" x2={2 + 96  cross} y2="15" stroke="#ff0000" strokeWidth="3" />
          </svg>
        </div>
      </div>

      {/ Big statement /}
      <div style={{
        position: 'absolute', top: 360, left: 100, right: 100, textAlign: 'center',
        opacity: interpolate(frame, [120, 150], [0, 1], { extrapolateRight: 'clamp' }),
        transform: scale(${sp(120, { damping: 10, stiffness: 100 })}),
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 42, color: '#3B2F2F',
          fontWeight: 600, letterSpacing: 6,
        }}>
          ये है असली सच
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 220, color: '#1C1C1C',
          fontWeight: 900, lineHeight: 1, marginTop: 18,
          textShadow: '8px 8px 0px rgba(59,47,47,0.2)',
        }}>
          ढाँचागत
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 100, color: '#E8E6E1',
          background: '#1C1C1C', display: 'inline-block', padding: '8px 36px',
          fontWeight: 800, marginTop: 8,
          boxShadow: '8px 8px 0px #FFC107',
        }}>
          स्थाई बदलाव
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(28,28,28,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9StructuralChange;
`
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10NeverComingBack',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed  9999)  10000;
  return x - Math.floor(x);
};

const Scene10NeverComingBack: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // path draw — staircase up showing one-way
  const path = interpolate(frame, [40, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const lockScale = sp(180, { damping: 8, stiffness: 100 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#E8E6E1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, #F0EEE9 0%, #D2D0CA 100%)',
      }} />

      {/ Triangle drift /}
      <div style={{
        position: 'absolute', top: 60, right: 60, width: 280, height: 280,
        background: '#3B2F2F', opacity: 0.05,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
        transform: rotate(${frame  0.18}deg),
      }} />
      <div style={{
        position: 'absolute', bottom: -100, left: -100, width: 450, height: 450, borderRadius: '50%',
        border: '3px solid #1C1C1C', opacity: 0.05,
      }} />

      {/ Grid + diag combo /}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        <defs>
          <pattern id="combo10" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#1C1C1C" strokeWidth="1" />
            <line x1="0" y1="60" x2="60" y2="0" stroke="#1C1C1C" strokeWidth="0.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#combo10)" />
      </svg>

      {/ Particles /}
      {Array.from({ length: 20 }).map((, i) => {
        const r = seededRandom(i + 200);
        const x = (r  1920 + frame  0.35) % 1920;
        const y = seededRandom(i + 11)  1080;
        return <div key={i} style={{
          position: 'absolute', left: x, top: y, width: 4, height: 4,
          background: '#3B2F2F', opacity: 0.14, borderRadius: '50%',
        }} />;
      })}

      {/ Title /}
      <div style={{
        position: 'absolute', top: 70, left: 100, opacity: sp(5),
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 36, color: '#3B2F2F',
          fontWeight: 600, letterSpacing: 4,
        }}>
          ▎दशकों का नतीजा
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 110, color: '#1C1C1C',
          fontWeight: 900, lineHeight: 1.05, marginTop: 8,
        }}>
          एक तरफ़ा रास्ता
        </div>
      </div>

      {/ Staircase chart /}
      <div style={{
        position: 'absolute', top: 320, left: 100, right: 100, height: 480,
        background: '#F5F3EE', border: '4px solid #1C1C1C', borderRadius: 18,
        padding: 28, boxShadow: '10px 10px 0px #3B2F2F',
        opacity: interpolate(frame, [30, 55], [0, 1], { extrapolateRight: 'clamp' }),
      }}>
        <svg width="100%" height="100%" viewBox="0 0 1600 400">
          {/ x axis /}
          <line x1="60" y1="360" x2="1540" y2="360" stroke="#1C1C1C" strokeWidth="3" />
          <line x1="60" y1="40" x2="60" y2="360" stroke="#1C1C1C" strokeWidth="3" />

          {/ dashed "expected return" line /}
          <line x1="60" y1="320" x2={60 + 1480  path} y2="320" stroke="#9E9E9E" strokeWidth="3" strokeDasharray="14 10" />

          {/ one-way staircase up /}
          {[
            [60, 320, 360, 320],
            [360, 320, 360, 260],
            [360, 260, 660, 260],
            [660, 260, 660, 200],
            [660, 200, 960, 200],
            [960, 200, 960, 140],
            [960, 140, 1260, 140],
            [1260, 140, 1260, 80],
            [1260, 80, 1540, 80],
          ].map(([x1, y1, x2, y2], i) => {
            const segStart = i / 9;
            const segEnd = (i + 1) / 9;
            if (path < segStart) return null;
            const p = Math.min(1, (path - segStart) / (segEnd - segStart));
            const nx = x1 + (x2 - x1)  p;
            const ny = y1 + (y2 - y1)  p;
            return <line key={i} x1={x1} y1={y1} x2={nx} y2={ny} stroke="#1C1C1C" strokeWidth="8" strokeLinecap="square" />;
          })}

          {/ labels /}
          <text x="80" y="395" fontFamily="Inter" fontSize="26" fill="#3B2F2F" fontWeight="600">2010</text>
          <text x="660" y="395" fontFamily="Inter" fontSize="26" fill="#3B2F2F" fontWeight="600">2019</text>
          <text x="1520" y="395" fontFamily="Inter" fontSize="26" fill="#3B2F2F" fontWeight="600" textAnchor="end">2026</text>
          <text x="60" y="335" fontFamily="Inter" fontSize="22" fill="#9E9E9E" fontWeight="500">expected return →</text>
        </svg>
      </div>

      {/ Lock badge /}
      <div style={{
        position: 'absolute', top: 360, right: 160,
        transform: scale(${lockScale}) rotate(${-8}deg), opacity: lockScale,
        background: '#1C1C1C', color: '#FFC107',
        padding: '20px 28px', borderRadius: 18,
        border: '5px solid #1C1C1C', boxShadow: '8px 8px 0px #FFC107',
        display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 10,
      }}>
        <svg width="80" height="80" viewBox="0 0 80 80">
          <rect x="18" y="38" width="44" height="34" rx="4" fill="none" stroke="#FFC107" strokeWidth="5" />
          <path d="M 28 38 L 28 26 Q 28 14 40 14 Q 52 14 52 26 L 52 38" fill="none" stroke="#FFC107" strokeWidth="5" />
        </svg>
        <div style={{ fontFamily: 'Yantra One', fontSize: 38, fontWeight: 800 }}>
          NO RETURN
        </div>
      </div>

      {/ Bottom callout /}
      <div style={{
        position: 'absolute', bottom: 40, left: 100, right: 100, textAlign: 'center',
        opacity: interpolate(frame, [200, 230], [0, 1], { extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 52, color: '#1C1C1C', fontWeight: 700,
          background: '#FFC107', display: 'inline-block', padding: '10px 32px',
          border: '4px solid #1C1C1C', boxShadow: '6px 6px 0px #3B2F2F',
        }}>
          2019 के लेवल पर • कभी नहीं
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(28,28,28,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10NeverComingBack;
``
```

