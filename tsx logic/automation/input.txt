```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_Hook65',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_Hook65: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06], { extrapolateRight: 'clamp' });

  // Label entrance
  const labelIn = sp(8);
  // "65" mega number entrance
  const sixtyFiveIn = sp(20, { damping: 9, stiffness: 100 });
  const sixtyFivePulse = 1 + Math.sin(frame * 0.08) * 0.015;

  // Subhead
  const subIn = sp(55);

  // Ring rotation
  const ringRot = frame * 0.4;

  // Bottom bar
  const barIn = interpolate(frame, [70, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Particles
  const particles = Array.from({ length: 28 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 2) * 1080,
    r: 2 + seededRandom(i + 3) * 4,
    sp: 0.3 + seededRandom(i + 4) * 0.6,
  }));

  // Drift shapes
  const shapeDrift = (s: number) => Math.sin(frame * 0.01 + s) * 30;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5BAFF', overflow: 'hidden', opacity: exitOp }}>
      {/* gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 45%, rgba(255,255,255,0.35) 0%, rgba(181,186,255,0) 60%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: -200 + shapeDrift(1), left: -180,
        width: 720, height: 720, borderRadius: '50%',
        background: '#000000', opacity: 0.05,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: -240 + shapeDrift(2), right: -200,
        width: 820, height: 820,
        background: '#EEEEEE', opacity: 0.08,
        transform: `rotate(${45 + frame * 0.05}deg)`,
        borderRadius: 80,
      }} />
      <div style={{
        position: 'absolute', top: 200 + shapeDrift(3), right: 250,
        width: 0, height: 0,
        borderLeft: '180px solid transparent',
        borderRight: '180px solid transparent',
        borderBottom: '300px solid #000000',
        opacity: 0.04,
        transform: `rotate(${-frame * 0.2}deg)`,
      }} />

      {/* Motion texture — grid */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.12 }}>
        <defs>
          <pattern id="grid01" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid01)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x,
          top: (p.y + frame * p.sp) % 1080,
          width: p.r, height: p.r,
          borderRadius: '50%',
          background: '#000000',
          opacity: 0.25,
        }} />
      ))}

      {/* Main content wrapper with camera zoom */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: '50% 50%' }}>

        {/* Top Label */}
        <div style={{
          position: 'absolute',
          top: 130,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - labelIn) * -40}px)`,
          opacity: labelIn,
          display: 'flex', alignItems: 'center', gap: 18,
        }}>
          <div style={{ width: 60, height: 4, background: '#000000', borderRadius: 2 }} />
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 700,
            color: '#000000',
            letterSpacing: 8,
            textTransform: 'uppercase',
          }}>
            ATTENTION SENIORS
          </div>
          <div style={{ width: 60, height: 4, background: '#000000', borderRadius: 2 }} />
        </div>

        {/* Rotating ring behind 65 */}
        <svg
          width="900" height="900"
          style={{
            position: 'absolute',
            top: '50%', left: '50%',
            transform: `translate(-50%, -50%) rotate(${ringRot}deg) scale(${sixtyFiveIn})`,
            opacity: 0.9 * sixtyFiveIn,
          }}
        >
          <circle cx="450" cy="450" r="380" fill="none" stroke="#000000" strokeWidth="3" strokeDasharray="22 14" opacity="0.4" />
          <circle cx="450" cy="450" r="320" fill="none" stroke="#EEEEEE" strokeWidth="6" opacity="0.55" />
        </svg>

        {/* Massive 65 */}
        <div style={{
          position: 'absolute',
          top: '50%', left: '50%',
          transform: `translate(-50%, -50%) scale(${sixtyFiveIn * sixtyFivePulse})`,
          fontFamily: 'Yantra One, Inter',
          fontSize: 612,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 1,
          letterSpacing: -10,
          textShadow: '8px 8px 0 #EEEEEE',
        }}>
          65
        </div>

        {/* Subheadline */}
        <div style={{
          position: 'absolute',
          bottom: 230,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - subIn) * 30}px)`,
          opacity: subIn,
          textAlign: 'center',
          fontFamily: 'Inter, Poppins',
        }}>
          <div style={{
            fontSize: 64,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: -1,
            lineHeight: 1.1,
          }}>
            This Video Is For <span style={{
              background: '#000000',
              color: '#B5BAFF',
              padding: '4px 18px',
              borderRadius: 8,
            }}>YOU</span>
          </div>
        </div>

        {/* Bottom accent bar */}
        <div style={{
          position: 'absolute',
          bottom: 140,
          left: '50%',
          transform: `translateX(-50%) scaleX(${barIn})`,
          transformOrigin: 'center',
          width: 680,
          height: 6,
          background: '#000000',
          borderRadius: 3,
        }} />

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.45,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_Hook65;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_RealMoney',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_RealMoney: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camPanX = interpolate(frame, [0, durationInFrames], [-15, 15], { extrapolateRight: 'clamp' });

  // Counter animation: $100 → $1000s
  const counterProgress = interpolate(frame, [25, 130], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const counterValue = Math.floor(counterProgress * 2847);
  const counterScale = sp(20, { damping: 10 });

  // Bars rising chart
  const barHeights = [180, 280, 410, 560, 720];
  const barsIn = barHeights.map((_, i) => sp(40 + i * 8));

  // Labels
  const leftLabelIn = sp(15);
  const subBottomIn = sp(95);

  // Shake on counter pop (every 25 frames after frame 30)
  const shake = frame > 30 && frame < 130
    ? Math.sin(frame * 0.6) * 2
    : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5BAFF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(238,238,238,0.25) 0%, rgba(181,186,255,0) 50%, rgba(0,0,0,0.15) 100%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: 100, left: 80,
        width: 480, height: 480, borderRadius: 60,
        background: '#EEEEEE', opacity: 0.07,
        transform: `rotate(${20 + frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: 60, right: 100,
        width: 600, height: 600, borderRadius: '50%',
        background: '#000000', opacity: 0.05,
      }} />

      {/* Texture - diagonal lines */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.1 }}>
        <defs>
          <pattern id="diag02" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag02)" />
      </svg>

      {/* Floating dollar particles */}
      {Array.from({ length: 18 }, (_, i) => {
        const x = seededRandom(i + 10) * 1920;
        const y = (seededRandom(i + 20) * 1080 - frame * (0.5 + seededRandom(i + 30))) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y < 0 ? y + 1080 : y,
            fontFamily: 'Inter', fontSize: 28, fontWeight: 800,
            color: '#000000', opacity: 0.18,
          }}>$</div>
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camPanX}px)` }}>

        {/* Left side - Label */}
        <div style={{
          position: 'absolute',
          top: 120, left: 120,
          opacity: leftLabelIn,
          transform: `translateY(${(1 - leftLabelIn) * -30}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            background: '#000000',
            color: '#B5BAFF',
            padding: '12px 28px',
            fontFamily: 'Inter',
            fontSize: 32,
            fontWeight: 800,
            letterSpacing: 4,
            borderRadius: 6,
          }}>
            EXTRA INCOME UNLOCKED
          </div>
        </div>

        {/* Mega counter */}
        <div style={{
          position: 'absolute',
          top: 240, left: 120,
          transform: `scale(${counterScale}) translateX(${shake}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 40,
            fontWeight: 700,
            color: '#000000',
            opacity: 0.7,
            marginBottom: 10,
          }}>
            UP TO
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono, Inter',
            fontSize: 340,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 0.95,
            letterSpacing: -8,
            textShadow: '6px 6px 0 #EEEEEE',
          }}>
            ${counterValue.toLocaleString()}
          </div>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 44,
            fontWeight: 700,
            color: '#000000',
            marginTop: -10,
            opacity: 0.85,
          }}>
            added per year
          </div>
        </div>

        {/* Right side - Rising bar chart */}
        <div style={{
          position: 'absolute',
          bottom: 220, right: 140,
          display: 'flex', gap: 22, alignItems: 'flex-end',
          height: 750,
        }}>
          {barHeights.map((h, i) => (
            <div key={i} style={{
              width: 90,
              height: h * barsIn[i],
              background: i === barHeights.length - 1 ? '#000000' : '#EEEEEE',
              borderRadius: '8px 8px 0 0',
              border: '3px solid #000000',
              position: 'relative',
            }}>
              {i === barHeights.length - 1 && barsIn[i] > 0.9 && (
                <div style={{
                  position: 'absolute', top: -50, left: '50%',
                  transform: 'translateX(-50%)',
                  fontFamily: 'Inter', fontSize: 32, fontWeight: 900,
                  color: '#000000',
                }}>↑</div>
              )}
            </div>
          ))}
        </div>

        {/* Bottom strip */}
        <div style={{
          position: 'absolute',
          bottom: 90, left: 120,
          opacity: subBottomIn,
          transform: `translateY(${(1 - subBottomIn) * 20}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 56,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: -1,
          }}>
            REAL money. Not coupon clipping.
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.45,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_RealMoney;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_FiveStrategies',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_FiveStrategies: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-0.5, 0.5], { extrapolateRight: 'clamp' });

  // Coupon strike-out
  const couponIn = sp(10);
  const strikeProgress = interpolate(frame, [30, 55], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });
  const couponFade = interpolate(frame, [60, 90], [1, 0.25], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big "5" reveal
  const bigFiveIn = sp(70, { damping: 9, stiffness: 100 });
  const fivePulse = 1 + Math.sin(frame * 0.1) * 0.02;

  // Strategy badges
  const badges = ['STRATEGIES', 'BONUSES', 'LOOPHOLES'];
  const badgesIn = badges.map((_, i) => sp(110 + i * 10));

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5BAFF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 30%, rgba(238,238,238,0.3) 0%, rgba(181,186,255,0) 60%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: -100, right: -150,
        width: 500, height: 500, borderRadius: '50%',
        background: '#000000', opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: -200, left: -100,
        width: 700, height: 700,
        background: '#EEEEEE', opacity: 0.08,
        borderRadius: 100,
        transform: `rotate(${15 + frame * 0.03}deg)`,
      }} />

      {/* Dot texture */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.15 }}>
        <defs>
          <pattern id="dots03" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#000000" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots03)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }, (_, i) => {
        const x = seededRandom(i + 100) * 1920;
        const y = seededRandom(i + 200) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 20;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x + drift, top: y,
            width: 4, height: 4,
            borderRadius: '50%',
            background: '#000000',
            opacity: 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)`, transformOrigin: '50% 50%' }}>

        {/* Left side - Crossed out coupon */}
        <div style={{
          position: 'absolute',
          top: 280, left: 140,
          opacity: couponIn * couponFade,
          transform: `scale(${couponIn}) rotate(-6deg)`,
        }}>
          {/* Coupon card */}
          <div style={{
            background: '#EEEEEE',
            border: '4px dashed #000000',
            padding: '40px 50px',
            borderRadius: 12,
            position: 'relative',
            width: 480,
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 34, fontWeight: 700,
              color: '#000000', opacity: 0.7, marginBottom: 8,
            }}>COUPON SAVINGS</div>
            <div style={{
              fontFamily: 'Yantra One, Inter',
              fontSize: 140,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1,
            }}>50¢</div>
            <div style={{
              fontFamily: 'Inter', fontSize: 30, fontWeight: 600,
              color: '#000000', marginTop: 10,
            }}>off a can of soup</div>
          </div>
          {/* Strike-through line */}
          <svg style={{
            position: 'absolute',
            top: '50%', left: 0,
            width: 480, height: 12,
            transform: 'translateY(-50%) rotate(-8deg)',
          }}>
            <line x1="0" y1="6" x2={480 * strikeProgress} y2="6"
              stroke="#000000" strokeWidth="10" strokeLinecap="round" />
          </svg>
          {/* X stamp */}
          {frame > 55 && (
            <div style={{
              position: 'absolute',
              top: -30, right: -40,
              fontFamily: 'Inter', fontSize: 120, fontWeight: 900,
              color: '#000000',
              transform: `rotate(15deg) scale(${sp(55, { damping: 8 })})`,
            }}>✕</div>
          )}
        </div>

        {/* Arrow pointing right */}
        {frame > 65 && (
          <div style={{
            position: 'absolute',
            top: 460, left: 720,
            opacity: sp(65),
            fontFamily: 'Inter', fontSize: 120, fontWeight: 900,
            color: '#000000',
          }}>→</div>
        )}

        {/* Right side - The "5" big */}
        <div style={{
          position: 'absolute',
          top: 240, right: 180,
          transform: `scale(${bigFiveIn * fivePulse})`,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 38, fontWeight: 800,
            color: '#000000', letterSpacing: 6,
            marginBottom: 10,
          }}>I'M TALKING ABOUT</div>
          <div style={{
            fontFamily: 'Yantra One, Inter',
            fontSize: 540,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 0.9,
            letterSpacing: -8,
            textShadow: '8px 8px 0 #EEEEEE',
          }}>5</div>
        </div>

        {/* Bottom badges */}
        <div style={{
          position: 'absolute',
          bottom: 140, left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex', gap: 30,
        }}>
          {badges.map((b, i) => (
            <div key={i} style={{
              opacity: badgesIn[i],
              transform: `translateY(${(1 - badgesIn[i]) * 30}px) scale(${badgesIn[i]})`,
              background: i === 1 ? '#000000' : '#EEEEEE',
              color: i === 1 ? '#B5BAFF' : '#000000',
              border: '3px solid #000000',
              padding: '20px 38px',
              fontFamily: 'Inter',
              fontSize: 36,
              fontWeight: 800,
              letterSpacing: 3,
              borderRadius: 8,
            }}>
              {b}
            </div>
          ))}
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.45,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_FiveStrategies;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_NobodyKnows',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_NobodyKnows: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom out
  const camZoom = interpolate(frame, [0, durationInFrames], [1.08, 1], { extrapolateRight: 'clamp' });

  // 100 people grid - 1 highlighted
  const peopleIn = sp(15);
  const highlightIn = sp(70, { damping: 8 });
  const highlightPulse = 1 + Math.sin(frame * 0.12) * 0.05;

  // Top label
  const labelIn = sp(5);

  // Stats
  const statIn = sp(90);
  const statValue = Math.floor(interpolate(frame, [90, 150], [0, 99], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  }));

  // Bottom warning
  const warnIn = sp(130);

  // Build 10x10 grid of people
  const rows = 10, cols = 10;
  const highlightIdx = 47; // single highlighted person

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5BAFF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(238,238,238,0.2) 0%, rgba(181,186,255,0) 50%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: 80, left: 60,
        width: 380, height: 380,
        background: '#000000', opacity: 0.04,
        borderRadius: 40,
        transform: `rotate(${frame * 0.04}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: 100, right: 60,
        width: 460, height: 460, borderRadius: '50%',
        background: '#EEEEEE', opacity: 0.08,
      }} />

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.08 }}>
        <defs>
          <pattern id="grid04" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid04)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 5) * 1920,
          top: (seededRandom(i + 15) * 1080 + frame * 0.3) % 1080,
          width: 3, height: 3,
          borderRadius: '50%',
          background: '#000000', opacity: 0.25,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: '50% 50%' }}>

        {/* Top label */}
        <div style={{
          position: 'absolute',
          top: 80, left: '50%',
          transform: `translateX(-50%) translateY(${(1 - labelIn) * -30}px)`,
          opacity: labelIn,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 36, fontWeight: 800,
            color: '#000000', letterSpacing: 6,
          }}>OUT OF EVERY 100 SENIORS</div>
        </div>

        {/* 10x10 Grid of people icons */}
        <div style={{
          position: 'absolute',
          top: 180, left: '50%',
          transform: `translateX(-50%) scale(${peopleIn})`,
          display: 'grid',
          gridTemplateColumns: `repeat(${cols}, 1fr)`,
          gap: 14,
          width: 880,
        }}>
          {Array.from({ length: rows * cols }, (_, i) => {
            const isHighlight = i === highlightIdx;
            return (
              <div key={i} style={{
                width: 70, height: 80,
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                transform: isHighlight ? `scale(${highlightIn * highlightPulse})` : 'scale(1)',
                opacity: isHighlight ? highlightIn : 0.45,
              }}>
                <svg viewBox="0 0 40 50" width="60" height="70">
                  {/* head */}
                  <circle cx="20" cy="12" r="9" fill={isHighlight ? '#000000' : '#000000'} opacity={isHighlight ? 1 : 0.6} />
                  {/* body */}
                  <path d="M 6 50 Q 6 28 20 28 Q 34 28 34 50 Z"
                    fill={isHighlight ? '#000000' : '#000000'}
                    opacity={isHighlight ? 1 : 0.6} />
                  {isHighlight && (
                    <>
                      {/* glow ring */}
                      <circle cx="20" cy="25" r="22" fill="none" stroke="#EEEEEE" strokeWidth="2.5" opacity="0.9" />
                    </>
                  )}
                </svg>
              </div>
            );
          })}
        </div>

        {/* Stat callout */}
        <div style={{
          position: 'absolute',
          bottom: 230, left: 120,
          opacity: statIn,
          transform: `translateY(${(1 - statIn) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, Inter',
            fontSize: 180,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 0.9,
            letterSpacing: -6,
          }}>
            {statValue}<span style={{ fontSize: 100 }}>/100</span>
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 40, fontWeight: 700,
            color: '#000000', marginTop: 4,
          }}>
            MISS OUT
          </div>
        </div>

        {/* Bottom right warning */}
        <div style={{
          position: 'absolute',
          bottom: 230, right: 140,
          opacity: warnIn,
          transform: `translateY(${(1 - warnIn) * 30}px)`,
          textAlign: 'right',
        }}>
          <div style={{
            background: '#000000', color: '#B5BAFF',
            padding: '14px 28px',
            display: 'inline-block',
            fontFamily: 'Inter', fontSize: 32, fontWeight: 800,
            letterSpacing: 3, borderRadius: 6,
            marginBottom: 12,
          }}>
            THE TRUTH
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 50, fontWeight: 800,
            color: '#000000', lineHeight: 1.1,
            maxWidth: 580,
          }}>
            SSA doesn't auto-pay your maximum.
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.45,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_NobodyKnows;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_AskForIt',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_AskForIt: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 3 stacked principle cards
  const card1In = sp(15);
  const card2In = sp(55);
  const card3In = sp(95);

  // Big "ASK" word
  const askIn = sp(130, { damping: 9 });
  const askPulse = 1 + Math.sin(frame * 0.1) * 0.025;

  // Money stack at top right
  const moneyStackIn = sp(35);

  const principles = [
    { num: '01', text: 'They give you what you ASK for' },
    { num: '02', text: 'The money is SITTING there' },
    { num: '03', text: 'Rules are written in YOUR favor' },
  ];
  const ins = [card1In, card2In, card3In];

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5BAFF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 70%, rgba(238,238,238,0.3) 0%, rgba(181,186,255,0) 55%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: -150, right: -100,
        width: 600, height: 600,
        background: '#000000', opacity: 0.05,
        transform: `rotate(${30 + frame * 0.06}deg)`,
        borderRadius: 80,
      }} />
      <div style={{
        position: 'absolute', bottom: -200, left: -150,
        width: 700, height: 700, borderRadius: '50%',
        background: '#EEEEEE', opacity: 0.08,
      }} />

      {/* Texture - hatch */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.1 }}>
        <defs>
          <pattern id="hatch05" width="30" height="30" patternUnits="userSpaceOnUse">
            <line x1="0" y1="30" x2="30" y2="0" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hatch05)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }, (_, i) => {
        const drift = Math.sin(frame * 0.02 + i * 0.5) * 25;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(i + 50) * 1920 + drift,
            top: seededRandom(i + 80) * 1080,
            width: 5, height: 5,
            borderRadius: '50%',
            background: '#000000', opacity: 0.22,
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute',
        top: 70, left: 120,
        opacity: sp(0),
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 32, fontWeight: 800,
          color: '#000000', letterSpacing: 5,
        }}>3 TRUTHS THE SSA WON'T TELL YOU</div>
      </div>

      {/* Three principle cards - left side */}
      <div style={{
        position: 'absolute',
        top: 170, left: 120,
        display: 'flex', flexDirection: 'column', gap: 24,
      }}>
        {principles.map((p, i) => (
          <div key={i} style={{
            opacity: ins[i],
            transform: `translateX(${(1 - ins[i]) * -60}px)`,
            background: i === 1 ? '#000000' : '#EEEEEE',
            color: i === 1 ? '#B5BAFF' : '#000000',
            border: '4px solid #000000',
            padding: '24px 36px',
            borderRadius: 14,
            width: 920,
            display: 'flex', alignItems: 'center', gap: 30,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, Inter',
              fontSize: 90, fontWeight: 900,
              lineHeight: 1, letterSpacing: -3,
              opacity: i === 1 ? 1 : 0.85,
            }}>
              {p.num}
            </div>
            <div style={{
              width: 4, height: 80,
              background: i === 1 ? '#B5BAFF' : '#000000',
              opacity: 0.7,
            }} />
            <div style={{
              fontFamily: 'Inter',
              fontSize: 46, fontWeight: 800,
              lineHeight: 1.15,
              flex: 1,
            }}>
              {p.text}
            </div>
          </div>
        ))}
      </div>

      {/* Right side - money stack visual */}
      <div style={{
        position: 'absolute',
        top: 200, right: 130,
        opacity: moneyStackIn,
        transform: `scale(${moneyStackIn})`,
      }}>
        {/* Stack of "bills" */}
        {[0, 1, 2, 3, 4].map((i) => (
          <div key={i} style={{
            width: 360, height: 50,
            background: i % 2 === 0 ? '#EEEEEE' : '#000000',
            border: '3px solid #000000',
            borderRadius: 4,
            marginBottom: -8,
            transform: `translateX(${i * 4}px)`,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'JetBrains Mono', fontSize: 30, fontWeight: 800,
            color: i % 2 === 0 ? '#000000' : '#B5BAFF',
          }}>
            $$$$$$$$$$$$
          </div>
        ))}
        <div style={{
          marginTop: 30,
          fontFamily: 'Inter', fontSize: 32, fontWeight: 800,
          color: '#000000', textAlign: 'center',
          letterSpacing: 2,
        }}>
          SITTING UNCLAIMED
        </div>
      </div>

      {/* Big bottom word: ASK */}
      <div style={{
        position: 'absolute',
        bottom: 80, right: 120,
        transform: `scale(${askIn * askPulse})`,
        textAlign: 'right',
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 30, fontWeight: 700,
          color: '#000000', opacity: 0.8, letterSpacing: 4,
        }}>YOU JUST HAVE TO</div>
        <div style={{
          fontFamily: 'Yantra One, Inter',
          fontSize: 220, fontWeight: 900,
          color: '#000000', lineHeight: 0.9,
          letterSpacing: -6,
          textShadow: '6px 6px 0 #EEEEEE',
        }}>ASK</div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.45,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_AskForIt;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_ResearchSources',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_ResearchSources: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.04], { extrapolateRight: 'clamp' });

  // Headline
  const headIn = sp(10);

  // Calendar pages flipping
  const calIn = sp(25, { damping: 10 });
  // Month tick
  const monthTick = Math.floor(interpolate(frame, [30, 110], [0, 5], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));
  const months = ['JAN', 'FEB', 'MAR', 'APR', 'MAY', 'JUN'];

  // 3 source docs
  const doc1In = sp(60);
  const doc2In = sp(80);
  const doc3In = sp(100);

  // "I dug into" stamp
  const stampIn = sp(140, { damping: 8 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5BAFF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(0,0,0,0.1) 0%, rgba(181,186,255,0) 40%, rgba(238,238,238,0.25) 100%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: -100, left: 200,
        width: 400, height: 400, borderRadius: '50%',
        background: '#EEEEEE', opacity: 0.08,
        transform: `translateY(${Math.sin(frame * 0.03) * 20}px)`,
      }} />
      <div style={{
        position: 'absolute', bottom: -150, right: 100,
        width: 550, height: 550,
        background: '#000000', opacity: 0.05,
        borderRadius: 60,
        transform: `rotate(${-10 + frame * 0.04}deg)`,
      }} />

      {/* Texture - graph paper */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.1 }}>
        <defs>
          <pattern id="grid06" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 50 0 L 0 0 0 50" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid06)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 70) * 1920,
          top: (seededRandom(i + 80) * 1080 - frame * 0.4) % 1080,
          width: 3, height: 3,
          borderRadius: '50%',
          background: '#000000', opacity: 0.28,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: '50% 50%' }}>

        {/* Top headline */}
        <div style={{
          position: 'absolute',
          top: 80, left: '50%',
          transform: `translateX(-50%) translateY(${(1 - headIn) * -30}px)`,
          opacity: headIn,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 32, fontWeight: 700,
            color: '#000000', opacity: 0.75, letterSpacing: 5,
            marginBottom: 10,
          }}>EVERY MONTH YOU WAIT</div>
          <div style={{
            fontFamily: 'Inter', fontSize: 78, fontWeight: 900,
            color: '#000000', lineHeight: 1, letterSpacing: -2,
          }}>
            MONEY <span style={{
              background: '#000000', color: '#B5BAFF',
              padding: '4px 18px', borderRadius: 8,
            }}>LEFT BEHIND</span>
          </div>
        </div>

        {/* Left side - Calendar with ticking months */}
        <div style={{
          position: 'absolute',
          top: 290, left: 140,
          transform: `scale(${calIn})`,
        }}>
          <div style={{
            width: 460, height: 460,
            background: '#EEEEEE',
            border: '5px solid #000000',
            borderRadius: 16,
            padding: 24,
            position: 'relative',
          }}>
            {/* Header */}
            <div style={{
              background: '#000000', color: '#B5BAFF',
              padding: '14px 0', textAlign: 'center',
              fontFamily: 'Inter', fontSize: 30, fontWeight: 800,
              letterSpacing: 4, borderRadius: 8,
            }}>
              2026
            </div>
            {/* Big current month */}
            <div style={{
              marginTop: 40, textAlign: 'center',
              fontFamily: 'Yantra One, Inter',
              fontSize: 200, fontWeight: 900,
              color: '#000000', lineHeight: 1,
              letterSpacing: -4,
            }}>
              {months[Math.min(monthTick, months.length - 1)]}
            </div>
            {/* Counter */}
            <div style={{
              marginTop: 30, textAlign: 'center',
              fontFamily: 'JetBrains Mono', fontSize: 42, fontWeight: 800,
              color: '#000000',
            }}>
              -${(monthTick * 200).toLocaleString()} lost
            </div>
          </div>
        </div>

        {/* Right side - 3 source documents */}
        <div style={{
          position: 'absolute',
          top: 280, right: 140,
          display: 'flex', flexDirection: 'column', gap: 22,
        }}>
          {[
            { label: 'SOURCE 01', title: 'SSA Guidelines 2026', op: doc1In },
            { label: 'SOURCE 02', title: 'COLA Adjustments', op: doc2In },
            { label: 'SOURCE 03', title: 'Medicare Updates', op: doc3In },
          ].map((d, i) => (
            <div key={i} style={{
              opacity: d.op,
              transform: `translateX(${(1 - d.op) * 60}px)`,
              width: 580,
              background: '#EEEEEE',
              border: '4px solid #000000',
              borderRadius: 12,
              padding: '22px 28px',
              display: 'flex', alignItems: 'center', gap: 22,
            }}>
              {/* doc icon */}
              <svg width="60" height="72" viewBox="0 0 60 72">
                <path d="M 8 4 L 42 4 L 56 18 L 56 68 L 8 68 Z"
                  fill="#B5BAFF" stroke="#000000" strokeWidth="3" />
                <line x1="18" y1="30" x2="46" y2="30" stroke="#000000" strokeWidth="2.5" />
                <line x1="18" y1="42" x2="46" y2="42" stroke="#000000" strokeWidth="2.5" />
                <line x1="18" y1="54" x2="40" y2="54" stroke="#000000" strokeWidth="2.5" />
              </svg>
              <div>
                <div style={{
                  fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700,
                  color: '#000000', opacity: 0.6, letterSpacing: 3,
                }}>{d.label}</div>
                <div style={{
                  fontFamily: 'Inter', fontSize: 38, fontWeight: 800,
                  color: '#000000', marginTop: 2,
                }}>{d.title}</div>
              </div>
            </div>
          ))}
        </div>

        {/* Bottom stamp */}
        <div style={{
          position: 'absolute',
          bottom: 90, left: '50%',
          transform: `translateX(-50%) scale(${stampIn}) rotate(-2deg)`,
          opacity: stampIn,
          background: '#000000', color: '#B5BAFF',
          padding: '18px 50px',
          fontFamily: 'Inter', fontSize: 40, fontWeight: 900,
          letterSpacing: 6, borderRadius: 10,
          border: '4px solid #EEEEEE',
        }}>
          RESEARCH COMPLETE
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.45,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_ResearchSources;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_FiveBonusesReveal',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_FiveBonusesReveal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Header
  const headIn = sp(8);

  // 5 numbered tiles, staggered
  const tileIns = [0, 1, 2, 3, 4].map(i => sp(30 + i * 14));

  // Bottom CTA
  const ctaIn = sp(130);

  const bonuses = [
    { n: '01', t: 'WORKING' },
    { n: '02', t: 'SPOUSAL' },
    { n: '03', t: 'DELAYED' },
    { n: '04', t: 'MEDICARE' },
    { n: '05', t: 'SURVIVOR' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5BAFF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 30%, rgba(238,238,238,0.3) 0%, rgba(181,186,255,0) 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: -120, left: -100,
        width: 540, height: 540,
        background: '#000000', opacity: 0.05,
        borderRadius: 80,
        transform: `rotate(${frame * 0.05}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: -150, right: -80,
        width: 580, height: 580, borderRadius: '50%',
        background: '#EEEEEE', opacity: 0.09,
      }} />

      {/* Concentric circles texture */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.08 }}>
        {[200, 400, 600, 800, 1000].map((r, i) => (
          <circle key={i} cx="960" cy="540" r={r}
            fill="none" stroke="#000000" strokeWidth="1.5" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 24 }, (_, i) => {
        const drift = Math.sin(frame * 0.025 + i) * 30;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(i + 33) * 1920 + drift,
            top: (seededRandom(i + 44) * 1080 + frame * 0.5) % 1080,
            width: 4, height: 4,
            borderRadius: '50%',
            background: '#000000', opacity: 0.25,
          }} />
        );
      })}

      {/* Top header */}
      <div style={{
        position: 'absolute',
        top: 90, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - headIn) * -30}px)`,
        opacity: headIn,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 34, fontWeight: 800,
          color: '#000000', letterSpacing: 6, marginBottom: 14,
        }}>I FOUND EXACTLY</div>
        <div style={{
          fontFamily: 'Yantra One, Inter',
          fontSize: 180, fontWeight: 900,
          color: '#000000', lineHeight: 1,
          letterSpacing: -4,
          textShadow: '5px 5px 0 #EEEEEE',
        }}>
          5 BONUSES
        </div>
      </div>

      {/* Five numbered tiles */}
      <div style={{
        position: 'absolute',
        top: 480, left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex', gap: 26,
      }}>
        {bonuses.map((b, i) => {
          const ins = tileIns[i];
          const float = Math.sin((frame + i * 12) * 0.05) * 6;
          return (
            <div key={i} style={{
              opacity: ins,
              transform: `translateY(${(1 - ins) * 80 + float}px) scale(${ins})`,
              width: 260, height: 320,
              background: i === 4 ? '#000000' : '#EEEEEE',
              color: i === 4 ? '#B5BAFF' : '#000000',
              border: '5px solid #000000',
              borderRadius: 16,
              display: 'flex', flexDirection: 'column',
              alignItems: 'center', justifyContent: 'center',
              padding: 20,
              boxShadow: '8px 8px 0 rgba(0,0,0,0.25)',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono, Inter',
                fontSize: 130, fontWeight: 900,
                lineHeight: 1, letterSpacing: -4,
              }}>{b.n}</div>
              <div style={{
                width: 60, height: 4,
                background: i === 4 ? '#B5BAFF' : '#000000',
                margin: '14px 0',
                opacity: 0.8,
              }} />
              <div style={{
                fontFamily: 'Inter', fontSize: 32, fontWeight: 800,
                letterSpacing: 2,
              }}>{b.t}</div>
              {i === 4 && (
                <div style={{
                  marginTop: 12,
                  fontFamily: 'Inter', fontSize: 18, fontWeight: 700,
                  color: '#B5BAFF', opacity: 0.7,
                }}>★ BIGGEST ★</div>
              )}
            </div>
          );
        })}
      </div>

      {/* Bottom CTA */}
      <div style={{
        position: 'absolute',
        bottom: 80, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - ctaIn) * 30}px)`,
        opacity: ctaIn,
        textAlign: 'center',
        fontFamily: 'Inter',
        fontSize: 48, fontWeight: 800,
        color: '#000000',
      }}>
        Walking through each one — <span style={{ textDecoration: 'underline' }}>step by step</span>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.45,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_FiveBonusesReveal;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_StayUntilEnd',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_StayUntilEnd: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 3 checklist items
  const checkIns = [0, 1, 2].map(i => sp(15 + i * 18));

  // Massive #5 spotlight
  const fiveIn = sp(80, { damping: 9 });
  const fivePulse = 1 + Math.sin(frame * 0.12) * 0.03;

  // Spotlight ring
  const ringScale = interpolate(frame, [80, 140], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // Tag at bottom
  const tagIn = sp(135);

  const checklist = [
    { label: 'WHO QUALIFIES', icon: '?' },
    { label: 'THE NUMBERS', icon: '#' },
    { label: 'HOW TO CLAIM', icon: '!' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5BAFF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 75% 50%, rgba(0,0,0,0.18) 0%, rgba(181,186,255,0) 50%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: 50, right: 60,
        width: 700, height: 700, borderRadius: '50%',
        background: '#000000', opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.05) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: -100, left: -100,
        width: 460, height: 460,
        background: '#EEEEEE', opacity: 0.1,
        borderRadius: 60,
        transform: `rotate(${frame * 0.05}deg)`,
      }} />

      {/* Texture */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.1 }}>
        <defs>
          <pattern id="dots08" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#000000" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots08)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 99) * 1920,
          top: (seededRandom(i + 111) * 1080 + frame * 0.4) % 1080,
          width: 4, height: 4,
          borderRadius: '50%',
          background: '#000000', opacity: 0.25,
        }} />
      ))}

      {/* Left side - Checklist */}
      <div style={{
        position: 'absolute',
        top: 130, left: 120,
        width: 740,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 30, fontWeight: 800,
          color: '#000000', letterSpacing: 5, marginBottom: 30,
        }}>FOR EACH BONUS</div>

        {checklist.map((item, i) => (
          <div key={i} style={{
            opacity: checkIns[i],
            transform: `translateX(${(1 - checkIns[i]) * -50}px)`,
            display: 'flex', alignItems: 'center', gap: 28,
            marginBottom: 28,
            background: '#EEEEEE',
            border: '4px solid #000000',
            borderRadius: 14,
            padding: '24px 32px',
          }}>
            {/* Big icon box */}
            <div style={{
              width: 90, height: 90,
              background: '#000000',
              color: '#B5BAFF',
              display: 'flex', alignItems: 'center', justifyContent: 'center',
              fontFamily: 'Yantra One, Inter',
              fontSize: 72, fontWeight: 900,
              borderRadius: 12,
              lineHeight: 1,
            }}>
              {item.icon}
            </div>
            <div style={{
              fontFamily: 'Inter', fontSize: 48, fontWeight: 800,
              color: '#000000', letterSpacing: 1,
            }}>
              {item.label}
            </div>
            {/* checkmark */}
            <div style={{ flex: 1, textAlign: 'right' }}>
              <svg width="60" height="60" viewBox="0 0 60 60">
                <circle cx="30" cy="30" r="26" fill="#000000" />
                <path d="M 16 30 L 26 40 L 44 20"
                  stroke="#B5BAFF" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
            </div>
          </div>
        ))}
      </div>

      {/* Right side - #5 spotlight */}
      <div style={{
        position: 'absolute',
        top: 220, right: 140,
        textAlign: 'center',
      }}>
        {/* Top label */}
        <div style={{
          opacity: fiveIn,
          fontFamily: 'Inter', fontSize: 30, fontWeight: 800,
          color: '#000000', letterSpacing: 5,
          marginBottom: 6,
        }}>STAY FOR BONUS</div>

        {/* Spotlight ring + 5 */}
        <div style={{
          position: 'relative',
          width: 520, height: 520,
          display: 'flex', alignItems: 'center', justifyContent: 'center',
        }}>
          <svg
            width="520" height="520"
            style={{
              position: 'absolute',
              transform: `scale(${ringScale}) rotate(${frame * 0.5}deg)`,
            }}
          >
            <circle cx="260" cy="260" r="240" fill="none"
              stroke="#000000" strokeWidth="6" strokeDasharray="30 18" />
            <circle cx="260" cy="260" r="200" fill="none"
              stroke="#EEEEEE" strokeWidth="4" opacity="0.6" />
          </svg>
          <div style={{
            transform: `scale(${fiveIn * fivePulse})`,
            fontFamily: 'Yantra One, Inter',
            fontSize: 460, fontWeight: 900,
            color: '#000000', lineHeight: 0.9,
            letterSpacing: -8,
            textShadow: '8px 8px 0 #EEEEEE',
          }}>
            5
          </div>
        </div>
      </div>

      {/* Bottom tag */}
      <div style={{
        position: 'absolute',
        bottom: 70, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - tagIn) * 25}px) scale(${tagIn})`,
        opacity: tagIn,
        background: '#000000', color: '#B5BAFF',
        padding: '18px 44px', borderRadius: 10,
        fontFamily: 'Inter', fontSize: 38, fontWeight: 800,
        letterSpacing: 3,
        border: '4px solid #EEEEEE',
      }}>
        MARRIED COUPLES → LIFE-CHANGING
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.45,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_StayUntilEnd;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_SubscribeCTA',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_SubscribeCTA: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Top kicker
  const kickerIn = sp(8);

  // Subscribe button entrance + press cycle
  const btnIn = sp(35, { damping: 9 });
  const pressCycle = Math.floor(frame / 45) % 2;
  const btnPress = pressCycle === 1
    ? interpolate(frame % 45, [0, 8, 16], [1, 0.92, 1], { extrapolateRight: 'clamp' })
    : 1;

  // Bell entrance + ring
  const bellIn = sp(75, { damping: 9 });
  const bellRing = Math.sin(frame * 0.25) * (frame > 75 ? 12 : 0);

  // Channel name plate
  const plateIn = sp(95);

  // Cursor pointer animation toward button
  const cursorX = interpolate(frame, [50, 100, 130, 175], [1300, 880, 880, 1200], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });
  const cursorY = interpolate(frame, [50, 100, 130, 175], [700, 540, 540, 600], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5BAFF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 60%, rgba(238,238,238,0.3) 0%, rgba(181,186,255,0) 55%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: -150, left: -120,
        width: 560, height: 560, borderRadius: '50%',
        background: '#000000', opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: -180, right: -100,
        width: 640, height: 640,
        background: '#EEEEEE', opacity: 0.1,
        borderRadius: 90,
        transform: `rotate(${15 + frame * 0.04}deg)`,
      }} />

      {/* Radiating lines from center */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.12 }}>
        {Array.from({ length: 24 }, (_, i) => {
          const angle = (i / 24) * Math.PI * 2;
          const x2 = 960 + Math.cos(angle) * 1400;
          const y2 = 540 + Math.sin(angle) * 1400;
          return (
            <line key={i} x1="960" y1="540" x2={x2} y2={y2}
              stroke="#000000" strokeWidth="1.5" />
          );
        })}
      </svg>

      {/* Particles */}
      {Array.from({ length: 24 }, (_, i) => {
        const drift = Math.sin(frame * 0.03 + i) * 25;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(i + 200) * 1920 + drift,
            top: (seededRandom(i + 250) * 1080 - frame * 0.5) % 1080,
            width: 4, height: 4,
            borderRadius: '50%',
            background: '#000000', opacity: 0.28,
          }} />
        );
      })}

      {/* Top kicker */}
      <div style={{
        position: 'absolute',
        top: 100, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - kickerIn) * -30}px)`,
        opacity: kickerIn,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 36, fontWeight: 800,
          color: '#000000', letterSpacing: 6, marginBottom: 12,
        }}>QUICK PAUSE — IMPORTANT</div>
        <div style={{
          fontFamily: 'Inter', fontSize: 78, fontWeight: 900,
          color: '#000000', lineHeight: 1, letterSpacing: -2,
        }}>
          Hit <span style={{
            background: '#000000', color: '#B5BAFF',
            padding: '4px 22px', borderRadius: 8,
          }}>SUBSCRIBE</span> right now
        </div>
      </div>

      {/* Main SUBSCRIBE button */}
      <div style={{
        position: 'absolute',
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) scale(${btnIn * btnPress})`,
      }}>
        <div style={{
          background: '#000000',
          color: '#B5BAFF',
          padding: '38px 100px',
          borderRadius: 18,
          fontFamily: 'Inter',
          fontSize: 110,
          fontWeight: 900,
          letterSpacing: 4,
          border: '6px solid #EEEEEE',
          boxShadow: '12px 12px 0 rgba(0,0,0,0.3)',
          display: 'flex', alignItems: 'center', gap: 30,
        }}>
          <svg width="90" height="90" viewBox="0 0 90 90">
            <rect x="6" y="20" width="78" height="50" rx="14" fill="#B5BAFF" />
            <path d="M 38 35 L 38 55 L 58 45 Z" fill="#000000" />
          </svg>
          SUBSCRIBE
        </div>
      </div>

      {/* Bell with ring */}
      <div style={{
        position: 'absolute',
        top: '50%', right: 200,
        transform: `translateY(-50%) scale(${bellIn}) rotate(${bellRing}deg)`,
        transformOrigin: '50% 20%',
      }}>
        <svg width="180" height="200" viewBox="0 0 180 200">
          <path d="M 90 20 L 90 30 M 50 40 Q 90 30 130 40 L 140 130 L 40 130 Z"
            fill="#000000" stroke="#000000" strokeWidth="4" />
          <path d="M 75 130 Q 90 145 105 130" fill="#000000" />
          <circle cx="90" cy="160" r="10" fill="#000000" />
          {/* sound waves */}
          {frame > 95 && (
            <>
              <path d="M 150 60 Q 165 75 150 95" stroke="#000000" strokeWidth="4" fill="none" />
              <path d="M 165 50 Q 185 75 165 105" stroke="#000000" strokeWidth="4" fill="none" opacity="0.7" />
            </>
          )}
        </svg>
      </div>

      {/* Channel name plate at bottom */}
      <div style={{
        position: 'absolute',
        bottom: 110, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - plateIn) * 30}px)`,
        opacity: plateIn,
        background: '#EEEEEE',
        border: '4px solid #000000',
        padding: '18px 40px',
        borderRadius: 10,
        fontFamily: 'Inter',
        fontSize: 42, fontWeight: 800,
        color: '#000000',
        letterSpacing: 3,
      }}>
        @ Senior Fund USA
      </div>

      {/* Animated cursor pointer */}
      {frame > 50 && frame < 180 && (
        <svg
          width="60" height="60" viewBox="0 0 60 60"
          style={{
            position: 'absolute',
            left: cursorX, top: cursorY,
            filter: 'drop-shadow(2px 2px 0 rgba(0,0,0,0.3))',
          }}
        >
          <path d="M 8 4 L 8 44 L 20 32 L 28 50 L 36 46 L 28 28 L 44 28 Z"
            fill="#EEEEEE" stroke="#000000" strokeWidth="3" strokeLinejoin="round" />
        </svg>
      )}

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.45,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_SubscribeCTA;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_RulesChange',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_RulesChange: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow pan
  const camPanY = interpolate(frame, [0, durationInFrames], [10, -10], { extrapolateRight: 'clamp' });

  // Title
  const titleIn = sp(8);

  // Years progression
  const years = ['2024', '2025', '2026'];
  const yearIns = years.map((_, i) => sp(28 + i * 18));

  // Arrows between years
  const arrowProgress = interpolate(frame, [40, 110], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // 3 change items
  const changeIns = [0, 1, 2].map(i => sp(110 + i * 14));

  const changes = [
    { label: 'INCOME LIMITS', delta: 'SHIFT', dir: '↑' },
    { label: 'NEW PROGRAMS', delta: 'OPEN', dir: '+' },
    { label: 'OLD RULES', delta: 'UPDATE', dir: '↻' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5BAFF', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(238,238,238,0.25) 0%, rgba(181,186,255,0) 50%, rgba(0,0,0,0.12) 100%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: 60, right: 80,
        width: 420, height: 420,
        background: '#000000', opacity: 0.05,
        borderRadius: 60,
        transform: `rotate(${frame * 0.06}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: -160, left: -120,
        width: 600, height: 600, borderRadius: '50%',
        background: '#EEEEEE', opacity: 0.09,
      }} />

      {/* Texture - vertical lines */}
      <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.1 }}>
        <defs>
          <pattern id="vert10" width="80" height="80" patternUnits="userSpaceOnUse">
            <line x1="40" y1="0" x2="40" y2="80" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#vert10)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (seededRandom(i + 300) * 1920 + frame * 0.4) % 1920,
          top: seededRandom(i + 350) * 1080,
          width: 4, height: 4,
          borderRadius: '50%',
          background: '#000000', opacity: 0.25,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `translateY(${camPanY}px)` }}>

        {/* Top title */}
        <div style={{
          position: 'absolute',
          top: 80, left: '50%',
          transform: `translateX(-50%) translateY(${(1 - titleIn) * -30}px)`,
          opacity: titleIn,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 34, fontWeight: 800,
            color: '#000000', letterSpacing: 5, marginBottom: 10,
          }}>EVERY SINGLE YEAR</div>
          <div style={{
            fontFamily: 'Inter', fontSize: 80, fontWeight: 900,
            color: '#000000', lineHeight: 1, letterSpacing: -2,
          }}>
            THE RULES <span style={{
              background: '#000000', color: '#B5BAFF',
              padding: '4px 22px', borderRadius: 8,
            }}>CHANGE</span>
          </div>
        </div>

        {/* Year timeline */}
        <div style={{
          position: 'absolute',
          top: 320, left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex', alignItems: 'center', gap: 40,
        }}>
          {years.map((y, i) => (
            <React.Fragment key={y}>
              <div style={{
                opacity: yearIns[i],
                transform: `scale(${yearIns[i]})`,
                width: 280, height: 200,
                background: i === 2 ? '#000000' : '#EEEEEE',
                color: i === 2 ? '#B5BAFF' : '#000000',
                border: '5px solid #000000',
                borderRadius: 16,
                display: 'flex', flexDirection: 'column',
                alignItems: 'center', justifyContent: 'center',
                boxShadow: i === 2 ? '8px 8px 0 rgba(0,0,0,0.3)' : 'none',
              }}>
                <div style={{
                  fontFamily: 'JetBrains Mono, Inter',
                  fontSize: 110, fontWeight: 900,
                  lineHeight: 1, letterSpacing: -3,
                }}>{y}</div>
                {i === 2 && (
                  <div style={{
                    marginTop: 8,
                    fontFamily: 'Inter', fontSize: 22, fontWeight: 800,
                    letterSpacing: 4,
                  }}>← TODAY</div>
                )}
              </div>
              {i < years.length - 1 && (
                <svg width="100" height="40" viewBox="0 0 100 40">
                  <line x1="5" y1="20" x2={5 + 75 * arrowProgress} y2="20"
                    stroke="#000000" strokeWidth="5" strokeLinecap="round" />
                  {arrowProgress > 0.8 && (
                    <path d="M 75 8 L 92 20 L 75 32"
                      stroke="#000000" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
                  )}
                </svg>
              )}
            </React.Fragment>
          ))}
        </div>

        {/* 3 Change items */}
        <div style={{
          position: 'absolute',
          bottom: 130, left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex', gap: 30,
        }}>
          {changes.map((c, i) => (
            <div key={i} style={{
              opacity: changeIns[i],
              transform: `translateY(${(1 - changeIns[i]) * 40}px) scale(${changeIns[i]})`,
              width: 360, padding: '22px 28px',
              background: '#EEEEEE',
              border: '4px solid #000000',
              borderRadius: 12,
              display: 'flex', alignItems: 'center', gap: 18,
            }}>
              <div style={{
                width: 70, height: 70,
                background: '#000000', color: '#B5BAFF',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                fontFamily: 'Inter', fontSize: 44, fontWeight: 900,
                borderRadius: 10, lineHeight: 1,
              }}>{c.dir}</div>
              <div>
                <div style={{
                  fontFamily: 'Inter', fontSize: 22, fontWeight: 700,
                  color: '#000000', opacity: 0.65, letterSpacing: 2,
                }}>{c.label}</div>
                <div style={{
                  fontFamily: 'Inter', fontSize: 38, fontWeight: 900,
                  color: '#000000', lineHeight: 1.1,
                }}>{c.delta}</div>
              </div>
            </div>
          ))}
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#000000', opacity: 0.45,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_RulesChange;
```

