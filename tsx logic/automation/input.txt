```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_MorningRoutine',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_MorningRoutine: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.08]);

  // Title reveals
  const titleY = interpolate(sp(5), [0, 1], [80, 0]);
  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);

  // Word "सोचो" emphasis
  const sochoScale = interpolate(sp(15, { damping: 8, stiffness: 180 }), [0, 1], [0.5, 1]);

  // Routine icons stagger
  const icons = [
    { label: 'चाय', delay: 50, x: 280 },
    { label: 'गैस', delay: 75, x: 640 },
    { label: 'कैब', delay: 100, x: 1000 },
    { label: 'दूध', delay: 125, x: 1360 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF97D0', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 20%, rgba(255,109,31,0.18) 0%, transparent 55%), radial-gradient(circle at 80% 80%, rgba(9,20,19,0.22) 0%, transparent 60%)',
        }}
      />

      {/* Animated Shape Layers */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '3px solid #091413',
          opacity: 0.06,
          top: -200,
          left: -200,
          transform: `rotate(${frame * 0.3}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          borderRadius: 40,
          backgroundColor: '#FF6D1F',
          opacity: 0.05,
          bottom: -150,
          right: -100,
          transform: `rotate(${-frame * 0.2}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 300,
          height: 300,
          opacity: 0.07,
          top: 200,
          right: 150,
          transform: `rotate(${frame * 0.4}deg)`,
        }}
      >
        <svg viewBox="0 0 100 100" width="100%" height="100%">
          <polygon points="50,5 95,80 5,80" fill="none" stroke="#091413" strokeWidth="2" />
        </svg>
      </div>

      {/* Grid texture */}
      <svg
        width="100%"
        height="100%"
        style={{ position: 'absolute', opacity: 0.08 }}
      >
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#091413" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid1)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = seededRandom(i + 1) * 1920;
        const py = seededRandom(i + 50) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 30;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px + drift,
              top: py + Math.cos(frame * 0.03 + i) * 20,
              width: 6,
              height: 6,
              borderRadius: '50%',
              backgroundColor: '#091413',
              opacity: 0.25,
            }}
          />
        );
      })}

      {/* MAIN CONTENT — Camera wrapper */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, transformOrigin: 'center center' }}>

        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: '50%',
            transform: `translateX(-50%) translateY(${titleY}px)`,
            opacity: titleOp,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}
        >
          <div style={{ width: 60, height: 4, backgroundColor: '#FF6D1F' }} />
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 700,
            color: '#091413',
            letterSpacing: 4,
            textTransform: 'uppercase',
          }}>
            A Regular Morning
          </span>
          <div style={{ width: 60, height: 4, backgroundColor: '#FF6D1F' }} />
        </div>

        {/* HERO WORD सोचो */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: '50%',
            transform: `translateX(-50%) scale(${sochoScale})`,
          }}
        >
          <span style={{
            fontFamily: 'Yantra One',
            fontSize: 220,
            fontWeight: 900,
            color: '#091413',
            lineHeight: 1,
            letterSpacing: -4,
          }}>
            सोचो<span style={{ color: '#FF6D1F' }}>...</span>
          </span>
        </div>

        {/* Sub line */}
        <div
          style={{
            position: 'absolute',
            top: 460,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(sp(35), [0, 1], [40, 0])}px)`,
            opacity: interpolate(sp(35), [0, 1], [0, 1]),
          }}
        >
          <span style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 64,
            fontWeight: 600,
            color: '#091413',
          }}>
            एक पल के लिए — तुम्हारा एक आम दिन
          </span>
        </div>

        {/* Horizontal divider */}
        <div
          style={{
            position: 'absolute',
            top: 580,
            left: '50%',
            transform: 'translateX(-50%)',
            width: interpolate(sp(45), [0, 1], [0, 900]),
            height: 3,
            backgroundColor: '#091413',
            opacity: 0.4,
          }}
        />

        {/* Routine icons row */}
        <div
          style={{
            position: 'absolute',
            top: 660,
            left: 0,
            width: '100%',
            display: 'flex',
            justifyContent: 'center',
            gap: 60,
          }}
        >
          {icons.map((item, i) => {
            const iconScale = interpolate(sp(item.delay, { damping: 9, stiffness: 160 }), [0, 1], [0, 1]);
            const iconY = Math.sin(frame * 0.05 + i) * 6;
            return (
              <div
                key={i}
                style={{
                  transform: `scale(${iconScale}) translateY(${iconY}px)`,
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'center',
                  gap: 18,
                }}
              >
                <div
                  style={{
                    width: 180,
                    height: 180,
                    borderRadius: 30,
                    backgroundColor: '#091413',
                    border: '4px solid #FF6D1F',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    boxShadow: '0 12px 30px rgba(9,20,19,0.25)',
                  }}
                >
                  {/* Icon SVG per index */}
                  {i === 0 && (
                    <svg width="100" height="100" viewBox="0 0 24 24" fill="none" stroke="#FF97D0" strokeWidth="2">
                      <path d="M4 10h14v4a4 4 0 0 1-4 4H8a4 4 0 0 1-4-4v-4z" />
                      <path d="M18 12h2a2 2 0 0 1 0 4h-2" />
                      <path d="M7 6c0-1 1-2 1-3M11 6c0-1 1-2 1-3M15 6c0-1 1-2 1-3" />
                    </svg>
                  )}
                  {i === 1 && (
                    <svg width="100" height="100" viewBox="0 0 24 24" fill="none" stroke="#FF97D0" strokeWidth="2">
                      <path d="M12 3c1 3 4 5 4 8a4 4 0 0 1-8 0c0-2 2-3 2-5 0-1 1-2 2-3z" fill="#FF6D1F" stroke="#FF97D0" />
                      <path d="M10 15c0 1 1 2 2 2s2-1 2-2" />
                    </svg>
                  )}
                  {i === 2 && (
                    <svg width="100" height="100" viewBox="0 0 24 24" fill="none" stroke="#FF97D0" strokeWidth="2">
                      <rect x="3" y="9" width="18" height="8" rx="1.5" />
                      <circle cx="7" cy="19" r="2" fill="#FF6D1F" />
                      <circle cx="17" cy="19" r="2" fill="#FF6D1F" />
                      <path d="M5 9V6a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2v3" />
                    </svg>
                  )}
                  {i === 3 && (
                    <svg width="100" height="100" viewBox="0 0 24 24" fill="none" stroke="#FF97D0" strokeWidth="2">
                      <path d="M9 3h6v3l1 3v10a2 2 0 0 1-2 2h-4a2 2 0 0 1-2-2V9l1-3V3z" />
                      <line x1="9" y1="11" x2="15" y2="11" />
                    </svg>
                  )}
                </div>
                <span style={{
                  fontFamily: 'Yantra One',
                  fontSize: 54,
                  color: '#091413',
                  fontWeight: 800,
                }}>
                  {item.label}
                </span>
              </div>
            );
          })}
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, transparent 50%, rgba(9,20,19,0.35) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#091413',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_MorningRoutine;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_EnergyCommon',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_EnergyCommon: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [-30, 30]);

  // Center hub reveal
  const hubScale = interpolate(sp(10, { damping: 10, stiffness: 140 }), [0, 1], [0, 1]);

  // Orbit lines
  const orbitRot = frame * 0.5;

  // Connector items
  const items = [
    { label: 'चाय', angle: -90 },
    { label: 'गैस', angle: 0 },
    { label: 'कैब', angle: 90 },
    { label: 'ग्रोसरी', angle: 180 },
  ];

  // Question reveal
  const qOp = interpolate(sp(180), [0, 1], [0, 1]);
  const qY = interpolate(sp(180), [0, 1], [30, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF97D0', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(255,109,31,0.12) 0%, transparent 50%, rgba(9,20,19,0.15) 100%)',
        }}
      />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        width: 900,
        height: 900,
        borderRadius: '50%',
        border: '2px dashed #091413',
        opacity: 0.08,
        top: '50%',
        left: '50%',
        transform: `translate(-50%, -50%) rotate(${orbitRot}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 600,
        height: 600,
        borderRadius: '50%',
        border: '2px solid #FF6D1F',
        opacity: 0.1,
        top: '50%',
        left: '50%',
        transform: `translate(-50%, -50%) rotate(${-orbitRot * 1.5}deg)`,
      }} />

      {/* Dot grid texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots2" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#091413" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots2)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const px = seededRandom(i + 10) * 1920;
        const py = seededRandom(i + 80) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + Math.sin(frame * 0.02 + i) * 25,
            top: py + Math.cos(frame * 0.03 + i) * 25,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#FF6D1F', opacity: 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>

        {/* Top tag */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: '50%',
          transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px)`,
          opacity: interpolate(sp(0), [0, 1], [0, 1]),
          padding: '14px 36px',
          backgroundColor: '#091413',
          borderRadius: 40,
        }}>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 32,
            fontWeight: 700,
            color: '#FF6D1F',
            letterSpacing: 3,
            textTransform: 'uppercase',
          }}>
            The Common Thread
          </span>
        </div>

        {/* CENTER HUB */}
        <div style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          transform: `translate(-50%, -50%) scale(${hubScale})`,
        }}>
          <div style={{
            width: 380,
            height: 380,
            borderRadius: '50%',
            backgroundColor: '#091413',
            border: '6px solid #FF6D1F',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            boxShadow: '0 0 60px rgba(255,109,31,0.4)',
          }}>
            <svg width="100" height="100" viewBox="0 0 24 24" fill="none" stroke="#FF6D1F" strokeWidth="2.5">
              <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2" fill="#FF6D1F" />
            </svg>
            <span style={{
              fontFamily: 'Yantra One',
              fontSize: 68,
              color: '#FF97D0',
              fontWeight: 900,
              marginTop: 10,
            }}>
              एनर्जी
            </span>
            <span style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              color: '#F5F5F5',
              fontWeight: 500,
              letterSpacing: 2,
            }}>
              ENERGY
            </span>
          </div>
        </div>

        {/* CONNECTOR ITEMS */}
        {items.map((item, i) => {
          const delay = 30 + i * 18;
          const itemSc = interpolate(sp(delay, { damping: 10, stiffness: 150 }), [0, 1], [0, 1]);
          const rad = (item.angle * Math.PI) / 180;
          const radius = 480;
          const x = Math.cos(rad) * radius;
          const y = Math.sin(rad) * radius;
          return (
            <div key={i} style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              transform: `translate(calc(-50% + ${x}px), calc(-50% + ${y}px)) scale(${itemSc})`,
            }}>
              <div style={{
                padding: '24px 44px',
                backgroundColor: '#FF6D1F',
                borderRadius: 18,
                border: '4px solid #091413',
                boxShadow: '0 8px 24px rgba(9,20,19,0.3)',
              }}>
                <span style={{
                  fontFamily: 'Yantra One',
                  fontSize: 64,
                  color: '#091413',
                  fontWeight: 800,
                }}>
                  {item.label}
                </span>
              </div>
            </div>
          );
        })}

        {/* Connecting lines SVG */}
        <svg width="100%" height="100%" style={{ position: 'absolute', top: 0, left: 0 }}>
          {items.map((item, i) => {
            const delay = 25 + i * 18;
            const op = interpolate(sp(delay), [0, 1], [0, 0.5]);
            const rad = (item.angle * Math.PI) / 180;
            const x = 960 + Math.cos(rad) * 380;
            const y = 540 + Math.sin(rad) * 380;
            return (
              <line
                key={i}
                x1={960}
                y1={540}
                x2={x}
                y2={y}
                stroke="#091413"
                strokeWidth="3"
                strokeDasharray="10 8"
                opacity={op}
              />
            );
          })}
        </svg>

        {/* Question banner bottom */}
        <div style={{
          position: 'absolute',
          bottom: 90,
          left: '50%',
          transform: `translateX(-50%) translateY(${qY}px)`,
          opacity: qOp,
          padding: '28px 60px',
          backgroundColor: '#091413',
          borderLeft: '8px solid #FF6D1F',
          borderRight: '8px solid #FF6D1F',
        }}>
          <span style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 56,
            color: '#FF97D0',
            fontWeight: 700,
          }}>
            क्या दाम <span style={{ color: '#FF6D1F' }}>बढ़े</span> हैं?
          </span>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(9,20,19,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#091413', opacity: 0.4, letterSpacing: 1,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_EnergyCommon;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_NotCoincidence',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_NotCoincidence: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on "NOT" stamp
  const shake = frame > 60 && frame < 75 ? Math.sin(frame * 2) * 6 : 0;

  // Stamp scale impact
  const stampSc = interpolate(sp(55, { damping: 7, stiffness: 220 }), [0, 1], [3, 1]);
  const stampRot = interpolate(sp(55), [0, 1], [-30, -12]);

  // Map reveal
  const mapOp = interpolate(sp(120), [0, 1], [0, 1]);
  const lineProg = interpolate(sp(140), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF97D0', overflow: 'hidden', opacity: exitOp, transform: `translateX(${shake}px)` }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at top, rgba(255,109,31,0.2) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 800, height: 800, borderRadius: 100,
        border: '4px solid #091413', opacity: 0.06,
        top: -300, right: -300,
        transform: `rotate(${45 + frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 400, height: 400, borderRadius: '50%',
        backgroundColor: '#FF6D1F', opacity: 0.08,
        bottom: 100, left: -100,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
      }} />

      {/* Diagonal lines texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="diag3" width="40" height="40" patternUnits="userSpaceOnUse">
            <line x1="0" y1="40" x2="40" y2="0" stroke="#091413" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag3)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 5) * 1920 + Math.sin(frame * 0.02 + i) * 20,
          top: seededRandom(i + 60) * 1080 + Math.cos(frame * 0.025 + i) * 20,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: '#091413', opacity: 0.3,
        }} />
      ))}

      {/* TOP — Question */}
      <div style={{
        position: 'absolute',
        top: 100,
        left: '50%',
        transform: `translateX(-50%) translateY(${interpolate(sp(5), [0, 1], [-50, 0])}px)`,
        opacity: interpolate(sp(5), [0, 1], [0, 1]),
      }}>
        <span style={{
          fontFamily: 'Yantra One',
          fontSize: 80,
          color: '#091413',
          fontWeight: 800,
        }}>
          अगर हाँ, तो...
        </span>
      </div>

      {/* MAIN — संयोग with NOT stamp */}
      <div style={{
        position: 'absolute',
        top: 240,
        left: '50%',
        transform: `translateX(-50%) translateY(${interpolate(sp(25), [0, 1], [60, 0])}px)`,
        opacity: interpolate(sp(25), [0, 1], [0, 1]),
        textAlign: 'center',
      }}>
        <div style={{ position: 'relative', display: 'inline-block' }}>
          <span style={{
            fontFamily: 'Yantra One',
            fontSize: 240,
            color: '#091413',
            fontWeight: 900,
            letterSpacing: -6,
          }}>
            संयोग
          </span>
          {/* Strike line */}
          <div style={{
            position: 'absolute',
            top: '50%',
            left: 0,
            width: `${interpolate(sp(50), [0, 1], [0, 100])}%`,
            height: 14,
            backgroundColor: '#FF6D1F',
            transform: 'translateY(-50%) rotate(-4deg)',
          }} />
        </div>

        {/* NOT stamp */}
        <div style={{
          position: 'absolute',
          top: -20,
          right: -120,
          transform: `rotate(${stampRot}deg) scale(${stampSc})`,
        }}>
          <div style={{
            padding: '20px 50px',
            border: '8px solid #FF6D1F',
            borderRadius: 8,
            backgroundColor: 'rgba(255,151,208,0.4)',
          }}>
            <span style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 80,
              fontWeight: 900,
              color: '#FF6D1F',
              letterSpacing: 4,
            }}>
              NOT!
            </span>
          </div>
        </div>
      </div>

      {/* Connector arrow + reveal text */}
      <div style={{
        position: 'absolute',
        top: 530,
        left: '50%',
        transform: 'translateX(-50%)',
        opacity: interpolate(sp(90), [0, 1], [0, 1]),
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: 8,
      }}>
        <svg width="60" height="80" viewBox="0 0 60 80">
          <line x1="30" y1="0" x2="30" y2="60" stroke="#091413" strokeWidth="5" />
          <polygon points="15,55 45,55 30,75" fill="#091413" />
        </svg>
      </div>

      {/* BOTTOM — Story location card */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: '50%',
        transform: `translateX(-50%) translateY(${interpolate(sp(130), [0, 1], [40, 0])}px)`,
        opacity: mapOp,
        width: 1400,
        padding: '36px 50px',
        backgroundColor: '#091413',
        borderRadius: 20,
        border: '4px solid #FF6D1F',
        display: 'flex',
        alignItems: 'center',
        gap: 40,
      }}>
        {/* Compass / pin icon */}
        <div style={{ flexShrink: 0 }}>
          <svg width="120" height="120" viewBox="0 0 24 24" fill="none" stroke="#FF6D1F" strokeWidth="2">
            <path d="M12 2a8 8 0 0 0-8 8c0 5.5 8 12 8 12s8-6.5 8-12a8 8 0 0 0-8-8z" fill="#FF6D1F" />
            <circle cx="12" cy="10" r="3" fill="#091413" />
          </svg>
        </div>

        <div style={{ flex: 1 }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 26,
            color: '#FF6D1F',
            letterSpacing: 4,
            fontWeight: 700,
            textTransform: 'uppercase',
            marginBottom: 8,
          }}>
            Origin Of The Story
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 78,
            color: '#FF97D0',
            fontWeight: 800,
            lineHeight: 1.1,
          }}>
            मिडल ईस्ट <span style={{ color: '#FF6D1F' }}>के रेगिस्तान</span>
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 32,
            color: '#F5F5F5',
            fontWeight: 500,
            marginTop: 8,
            opacity: 0.7,
          }}>
            ~ 4,200 km away from your kitchen
          </div>
        </div>

        {/* Distance bar */}
        <div style={{
          width: 8,
          height: 140,
          backgroundColor: '#FF6D1F',
          borderRadius: 4,
          transform: `scaleY(${lineProg})`,
          transformOrigin: 'top',
        }} />
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(9,20,19,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#091413', opacity: 0.4, letterSpacing: 1,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_NotCoincidence;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_PocketImpact',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_PocketImpact: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-0.5, 0.5]);

  // Wallet drop
  const walletY = interpolate(sp(15, { damping: 8, stiffness: 150 }), [0, 1], [-200, 0]);

  // Coin animations (falling out)
  const coinsOp = interpolate(sp(60), [0, 1], [0, 1]);

  // Channel reveal
  const channelOp = interpolate(sp(150), [0, 1], [0, 1]);
  const channelX = interpolate(sp(150), [0, 1], [80, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF97D0', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, transparent 0%, rgba(9,20,19,0.18) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 700, height: 700,
        borderRadius: '50%', border: '3px solid #FF6D1F', opacity: 0.1,
        top: '50%', left: '25%',
        transform: `translate(-50%, -50%) rotate(${frame * 0.3}deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500,
        borderRadius: 30, backgroundColor: '#091413', opacity: 0.05,
        bottom: -150, right: 200,
        transform: `rotate(${-15 + frame * 0.1}deg)`,
      }} />

      {/* Circle grid texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="circ4" width="70" height="70" patternUnits="userSpaceOnUse">
            <circle cx="35" cy="35" r="3" fill="none" stroke="#091413" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#circ4)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 15) * 1920 + Math.sin(frame * 0.02 + i) * 25,
          top: seededRandom(i + 70) * 1080 + Math.cos(frame * 0.03 + i) * 25,
          width: 5, height: 5, borderRadius: '50%',
          backgroundColor: '#FF6D1F', opacity: 0.35,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)`, transformOrigin: 'center center' }}>

        {/* TOP TITLE */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px)`,
          opacity: interpolate(sp(0), [0, 1], [0, 1]),
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 28,
            color: '#091413',
            letterSpacing: 5,
            fontWeight: 700,
            textTransform: 'uppercase',
            opacity: 0.7,
          }}>
            Direct Impact On Your
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 180,
            color: '#091413',
            fontWeight: 900,
            letterSpacing: -4,
            lineHeight: 1,
            marginTop: 8,
          }}>
            जेब<span style={{ color: '#FF6D1F' }}>.</span>
          </div>
        </div>

        {/* LEFT — Wallet illustration */}
        <div style={{
          position: 'absolute',
          left: 280,
          top: 480,
          transform: `translateY(${walletY}px)`,
        }}>
          <svg width="380" height="280" viewBox="0 0 100 80">
            {/* Wallet body */}
            <rect x="10" y="20" width="80" height="50" rx="6" fill="#091413" stroke="#FF6D1F" strokeWidth="2" />
            <rect x="10" y="20" width="80" height="15" rx="6" fill="#FF6D1F" />
            {/* Card slot */}
            <rect x="55" y="40" width="28" height="20" rx="3" fill="#FF6D1F" />
            <circle cx="73" cy="50" r="4" fill="#FF97D0" />
          </svg>

          {/* Coins falling */}
          <div style={{ position: 'absolute', top: -20, left: 100, opacity: coinsOp }}>
            {[0, 1, 2, 3].map((i) => {
              const cy = (frame * 4 + i * 35) % 280;
              const cx = i * 30 - 20;
              return (
                <div key={i} style={{
                  position: 'absolute',
                  left: cx,
                  top: cy,
                  width: 40, height: 40,
                  borderRadius: '50%',
                  backgroundColor: '#FF6D1F',
                  border: '3px solid #091413',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: 'Inter',
                  fontWeight: 900,
                  fontSize: 22,
                  color: '#091413',
                  transform: `rotate(${cy * 2}deg)`,
                }}>
                  ₹
                </div>
              );
            })}
          </div>
        </div>

        {/* Arrow connecting */}
        <div style={{
          position: 'absolute',
          left: 700,
          top: 560,
          opacity: interpolate(sp(80), [0, 1], [0, 1]),
        }}>
          <svg width="200" height="80" viewBox="0 0 200 80">
            <path d="M0 40 Q 100 0 180 40" stroke="#091413" strokeWidth="5" fill="none" strokeDasharray="10 6" />
            <polygon points="170,30 195,40 170,50" fill="#091413" />
          </svg>
        </div>

        {/* RIGHT — Channel intro card */}
        <div style={{
          position: 'absolute',
          right: 120,
          top: 440,
          width: 720,
          padding: 50,
          backgroundColor: '#091413',
          borderRadius: 20,
          borderTop: '6px solid #FF6D1F',
          opacity: channelOp,
          transform: `translateX(${channelX}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 26,
            color: '#FF6D1F',
            letterSpacing: 4,
            fontWeight: 700,
            textTransform: 'uppercase',
          }}>
            Hosted By
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 110,
            color: '#FF97D0',
            fontWeight: 900,
            marginTop: 6,
            lineHeight: 1,
          }}>
            कुनाल
          </div>
          <div style={{
            width: 80, height: 4,
            backgroundColor: '#FF6D1F',
            marginTop: 20,
            marginBottom: 20,
          }} />
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 42,
            color: '#F5F5F5',
            fontWeight: 700,
            letterSpacing: 1,
          }}>
            ProBuffet <span style={{ color: '#FF6D1F' }}>HQ</span>
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 32,
            color: '#9E9E9E',
            marginTop: 14,
            fontWeight: 500,
          }}>
            ग्लोबल घटनाएँ → आपकी ज़िंदगी
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(9,20,19,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#091413', opacity: 0.4, letterSpacing: 1,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_PocketImpact;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_GlobalToLocal',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_GlobalToLocal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Globe rotation
  const globeRot = frame * 0.5;
  const globeSc = interpolate(sp(10, { damping: 9, stiffness: 130 }), [0, 1], [0, 1]);

  // House
  const houseSc = interpolate(sp(70, { damping: 9, stiffness: 130 }), [0, 1], [0, 1]);

  // Arrow line progress
  const lineProg = interpolate(sp(110), [0, 1], [0, 1]);

  // Bottom tag pulse
  const tagPulse = 1 + Math.sin(frame * 0.15) * 0.03;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF97D0', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(255,109,31,0.15) 0%, transparent 50%, rgba(9,20,19,0.15) 100%)',
      }} />

      {/* Shape layers — large rings */}
      <div style={{
        position: 'absolute', width: 1200, height: 1200,
        borderRadius: '50%', border: '2px dashed #091413', opacity: 0.07,
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) rotate(${globeRot}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 800, height: 800,
        borderRadius: '50%', border: '2px solid #FF6D1F', opacity: 0.1,
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) rotate(${-globeRot * 0.7}deg)`,
      }} />

      {/* Wave texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="wave5" width="60" height="20" patternUnits="userSpaceOnUse">
            <path d="M 0 10 Q 15 0 30 10 T 60 10" fill="none" stroke="#091413" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#wave5)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 20) * 1920 + Math.sin(frame * 0.02 + i) * 20,
          top: seededRandom(i + 90) * 1080 + Math.cos(frame * 0.03 + i) * 20,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: '#091413', opacity: 0.3,
        }} />
      ))}

      {/* TOP TITLE */}
      <div style={{
        position: 'absolute',
        top: 70,
        left: '50%',
        transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px)`,
        opacity: interpolate(sp(0), [0, 1], [0, 1]),
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 30,
          color: '#091413',
          letterSpacing: 6,
          fontWeight: 700,
          opacity: 0.7,
        }}>
          OUR APPROACH
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 100,
          color: '#091413',
          fontWeight: 900,
          lineHeight: 1,
          marginTop: 6,
        }}>
          ग्लोबल → <span style={{ color: '#FF6D1F' }}>लोकल</span>
        </div>
      </div>

      {/* LEFT — Globe */}
      <div style={{
        position: 'absolute',
        left: 240,
        top: 420,
        transform: `scale(${globeSc})`,
        textAlign: 'center',
      }}>
        <div style={{
          width: 320, height: 320,
          borderRadius: '50%',
          backgroundColor: '#091413',
          border: '6px solid #FF6D1F',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          boxShadow: '0 0 50px rgba(9,20,19,0.4)',
          transform: `rotate(${globeRot * 0.5}deg)`,
        }}>
          <svg width="240" height="240" viewBox="0 0 24 24" fill="none" stroke="#FF6D1F" strokeWidth="1.5">
            <circle cx="12" cy="12" r="10" />
            <ellipse cx="12" cy="12" rx="10" ry="4" />
            <ellipse cx="12" cy="12" rx="4" ry="10" />
            <line x1="2" y1="12" x2="22" y2="12" />
          </svg>
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 64,
          color: '#091413',
          fontWeight: 800,
          marginTop: 30,
        }}>
          दुनिया
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 24,
          color: '#091413',
          letterSpacing: 4,
          fontWeight: 600,
          opacity: 0.7,
        }}>
          BIG EVENTS
        </div>
      </div>

      {/* CENTER — Arrow with line progress */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: 540,
        transform: 'translate(-50%, -50%)',
      }}>
        <svg width="500" height="100" viewBox="0 0 500 100">
          <line
            x1="20" y1="50" x2="480" y2="50"
            stroke="#091413" strokeWidth="6"
            strokeDasharray="500"
            strokeDashoffset={500 - lineProg * 500}
          />
          <polygon
            points="450,30 490,50 450,70"
            fill="#FF6D1F"
            opacity={interpolate(sp(160), [0, 1], [0, 1])}
          />
        </svg>
        <div style={{
          textAlign: 'center',
          marginTop: -10,
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 36,
          color: '#091413',
          fontWeight: 700,
          opacity: interpolate(sp(140), [0, 1], [0, 1]),
        }}>
          जोड़कर समझाते हैं
        </div>
      </div>

      {/* RIGHT — Home / Daily life */}
      <div style={{
        position: 'absolute',
        right: 240,
        top: 420,
        transform: `scale(${houseSc})`,
        textAlign: 'center',
      }}>
        <div style={{
          width: 320, height: 320,
          borderRadius: 30,
          backgroundColor: '#FF6D1F',
          border: '6px solid #091413',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          boxShadow: '0 0 50px rgba(9,20,19,0.4)',
        }}>
          <svg width="200" height="200" viewBox="0 0 24 24" fill="#091413" stroke="#091413" strokeWidth="1.5">
            <path d="M3 12 L12 3 L21 12 V21 H3 Z" fill="#091413" />
            <rect x="10" y="14" width="4" height="7" fill="#FF6D1F" />
            <rect x="5" y="13" width="3" height="3" fill="#FF6D1F" />
            <rect x="16" y="13" width="3" height="3" fill="#FF6D1F" />
          </svg>
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 64,
          color: '#091413',
          fontWeight: 800,
          marginTop: 30,
        }}>
          ज़िंदगी
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 24,
          color: '#091413',
          letterSpacing: 4,
          fontWeight: 600,
          opacity: 0.7,
        }}>
          YOUR DAILY LIFE
        </div>
      </div>

      {/* Bottom tag */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: '50%',
        transform: `translateX(-50%) scale(${tagPulse})`,
        opacity: interpolate(sp(200), [0, 1], [0, 1]),
        padding: '20px 60px',
        backgroundColor: '#091413',
        borderRadius: 50,
        border: '3px solid #FF6D1F',
      }}>
        <span style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 44,
          color: '#FF97D0',
          fontWeight: 700,
        }}>
          आज की बात — <span style={{ color: '#FF6D1F' }}>सीधी और ज़रूरी</span>
        </span>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(9,20,19,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#091413', opacity: 0.4, letterSpacing: 1,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_GlobalToLocal;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_UncomfortableTruth',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_UncomfortableTruth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera subtle zoom
  const camZ = interpolate(frame, [0, durationInFrames], [1.05, 1]);

  // Quote scale
  const quoteSc = interpolate(sp(20, { damping: 9, stiffness: 140 }), [0, 1], [0.85, 1]);
  const quoteOp = interpolate(sp(20), [0, 1], [0, 1]);

  // Warning banner shake at impact
  const warnShake = frame > 140 && frame < 155 ? Math.sin(frame * 1.8) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF97D0', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient — darker mood */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at bottom, rgba(9,20,19,0.3) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 600, height: 600,
        borderRadius: 40, border: '3px solid #FF6D1F', opacity: 0.08,
        top: 100, left: 150,
        transform: `rotate(${15 + frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 700, height: 700,
        borderRadius: '50%', backgroundColor: '#091413', opacity: 0.04,
        bottom: -200, right: -150,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />

      {/* Cross-hatch texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="cross6" width="50" height="50" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="50" y2="50" stroke="#091413" strokeWidth="1" />
            <line x1="50" y1="0" x2="0" y2="50" stroke="#091413" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#cross6)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 25) * 1920 + Math.sin(frame * 0.02 + i) * 25,
          top: seededRandom(i + 100) * 1080 + Math.cos(frame * 0.03 + i) * 25,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: i % 2 === 0 ? '#091413' : '#FF6D1F', opacity: 0.3,
        }} />
      ))}

      <AbsoluteFill style={{ transform: `scale(${camZ})` }}>

        {/* TOP — Warning label */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: '50%',
          transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px)`,
          opacity: interpolate(sp(0), [0, 1], [0, 1]),
          display: 'flex',
          alignItems: 'center',
          gap: 20,
          padding: '14px 40px',
          backgroundColor: '#FF6D1F',
          border: '4px solid #091413',
          borderRadius: 8,
        }}>
          <svg width="32" height="32" viewBox="0 0 24 24" fill="#091413">
            <path d="M12 2 L22 20 L2 20 Z" />
            <rect x="11" y="9" width="2" height="6" fill="#FF6D1F" />
            <rect x="11" y="16" width="2" height="2" fill="#FF6D1F" />
          </svg>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 30,
            fontWeight: 800,
            color: '#091413',
            letterSpacing: 4,
            textTransform: 'uppercase',
          }}>
            Uncomfortable Truth Ahead
          </span>
        </div>

        {/* CENTER — Quote block */}
        <div style={{
          position: 'absolute',
          top: 240,
          left: '50%',
          transform: `translateX(-50%) scale(${quoteSc})`,
          opacity: quoteOp,
          width: 1500,
          textAlign: 'center',
        }}>
          {/* Big opening quote */}
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 200,
            color: '#FF6D1F',
            lineHeight: 0.7,
            opacity: 0.5,
          }}>
            "
          </div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 130,
            color: '#091413',
            fontWeight: 900,
            lineHeight: 1.1,
            letterSpacing: -2,
            marginTop: -20,
          }}>
            सच अक्सर
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 160,
            color: '#FF6D1F',
            fontWeight: 900,
            lineHeight: 1,
            letterSpacing: -3,
            textShadow: '4px 4px 0 #091413',
            marginTop: 10,
          }}>
            असहज
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 130,
            color: '#091413',
            fontWeight: 900,
            lineHeight: 1.1,
            letterSpacing: -2,
          }}>
            होता है।
          </div>
        </div>

        {/* Underline accent */}
        <div style={{
          position: 'absolute',
          top: 760,
          left: '50%',
          transform: 'translateX(-50%)',
          width: interpolate(sp(120), [0, 1], [0, 500]),
          height: 6,
          backgroundColor: '#091413',
        }} />

        {/* BOTTOM — Topic preview */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: '50%',
          transform: `translateX(-50%) translate(${warnShake}px, ${interpolate(sp(150), [0, 1], [40, 0])}px)`,
          opacity: interpolate(sp(150), [0, 1], [0, 1]),
          display: 'flex',
          alignItems: 'center',
          gap: 30,
          padding: '22px 50px',
          backgroundColor: '#091413',
          borderRadius: 16,
          borderLeft: '8px solid #FF6D1F',
        }}>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 26,
            color: '#FF6D1F',
            letterSpacing: 3,
            fontWeight: 700,
          }}>
            TODAY'S TOPIC →
          </span>
          <span style={{
            fontFamily: 'Yantra One',
            fontSize: 56,
            color: '#FF97D0',
            fontWeight: 800,
          }}>
            ईरान × मिडल ईस्ट युद्ध स्थिति
          </span>
        </div>

      </AbsoluteFill>

      {/* Vignette stronger for mood */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 40%, rgba(9,20,19,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#091413', opacity: 0.4, letterSpacing: 1,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_UncomfortableTruth;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_StraitIntro',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_StraitIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Map zoom in
  const mapSc = interpolate(sp(10, { damping: 11, stiffness: 100 }), [0, 1], [0.7, 1]);
  const mapOp = interpolate(sp(10), [0, 1], [0, 1]);

  // Danger pulse
  const dangerPulse = 1 + Math.sin(frame * 0.2) * 0.08;

  // Question mark big reveal
  const qScale = interpolate(sp(180, { damping: 7, stiffness: 200 }), [0, 1], [0, 1]);
  const qRot = interpolate(sp(180), [0, 1], [-20, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF97D0', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 40%, rgba(255,109,31,0.18) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900,
        borderRadius: '50%', border: '2px dashed #091413', opacity: 0.08,
        top: '40%', left: '50%',
        transform: `translate(-50%, -50%) rotate(${frame * 0.3}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 400, height: 400,
        borderRadius: 30, backgroundColor: '#FF6D1F', opacity: 0.07,
        bottom: -100, left: -100,
        transform: `rotate(${20 + frame * 0.1}deg)`,
      }} />

      {/* Topo lines texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="topo7" width="100" height="60" patternUnits="userSpaceOnUse">
            <path d="M 0 30 Q 25 10 50 30 T 100 30" fill="none" stroke="#091413" strokeWidth="1" />
            <path d="M 0 45 Q 25 25 50 45 T 100 45" fill="none" stroke="#091413" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#topo7)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 30) * 1920 + Math.sin(frame * 0.02 + i) * 20,
          top: seededRandom(i + 110) * 1080 + Math.cos(frame * 0.03 + i) * 20,
          width: 5, height: 5, borderRadius: '50%',
          backgroundColor: '#FF6D1F', opacity: 0.35,
        }} />
      ))}

      {/* TOP TITLE */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: '50%',
        transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px)`,
        opacity: interpolate(sp(0), [0, 1], [0, 1]),
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 32,
          color: '#091413',
          letterSpacing: 6,
          fontWeight: 700,
          opacity: 0.7,
        }}>
          THE CHOKE POINT
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 110,
          color: '#091413',
          fontWeight: 900,
          lineHeight: 1,
          marginTop: 4,
        }}>
          Strait of <span style={{ color: '#FF6D1F' }}>Hormuz</span>
        </div>
      </div>

      {/* CENTER — Strait map illustration */}
      <div style={{
        position: 'absolute',
        top: 260,
        left: '50%',
        transform: `translateX(-50%) scale(${mapSc})`,
        opacity: mapOp,
      }}>
        <svg width="1100" height="500" viewBox="0 0 1100 500">
          {/* Iran landmass top */}
          <path
            d="M 0 0 L 1100 0 L 1100 180 Q 900 230 700 200 Q 550 175 400 220 Q 250 250 100 230 L 0 220 Z"
            fill="#091413"
          />
          <text x="500" y="100" fontFamily="Inter" fontSize="50" fontWeight="800" fill="#FF6D1F" textAnchor="middle">
            IRAN
          </text>

          {/* Sea (background showing) */}
          <rect x="0" y="220" width="1100" height="80" fill="#FF97D0" />
          {/* Animated water lines */}
          {[0, 1, 2].map((i) => (
            <path
              key={i}
              d={`M ${-100 + ((frame * 3 + i * 100) % 1200)} ${240 + i * 18} q 30 -8 60 0 t 60 0 t 60 0`}
              fill="none"
              stroke="#091413"
              strokeWidth="2"
              opacity="0.3"
            />
          ))}

          {/* Bottom landmass — UAE/Oman */}
          <path
            d="M 0 500 L 1100 500 L 1100 320 Q 900 280 700 310 Q 550 330 400 295 Q 250 270 100 300 L 0 310 Z"
            fill="#091413"
          />
          <text x="600" y="430" fontFamily="Inter" fontSize="42" fontWeight="800" fill="#FF6D1F" textAnchor="middle">
            UAE / OMAN
          </text>

          {/* Danger zone marker — pulse */}
          <g transform="translate(550, 260)">
            <circle r={70 * dangerPulse} fill="#FF6D1F" opacity="0.25" />
            <circle r={45} fill="#FF6D1F" stroke="#091413" strokeWidth="4" />
            <text x="0" y="8" fontFamily="Inter" fontSize="32" fontWeight="900" fill="#091413" textAnchor="middle">
              ⚡
            </text>
          </g>

          {/* Ship icons drifting */}
          {[0, 1, 2].map((i) => {
            const shx = ((frame * 1.5 + i * 350) % 1300) - 100;
            return (
              <g key={i} transform={`translate(${shx}, 265)`} opacity={interpolate(sp(40 + i * 15), [0, 1], [0, 1])}>
                <path d="M -25 0 L 25 0 L 20 12 L -20 12 Z" fill="#091413" />
                <rect x="-8" y="-10" width="16" height="10" fill="#FF6D1F" />
              </g>
            );
          })}

          {/* Label callout */}
          <line x1="550" y1="210" x2="550" y2="120" stroke="#091413" strokeWidth="3" strokeDasharray="8 6" opacity={interpolate(sp(80), [0, 1], [0, 1])} />
        </svg>

        {/* Danger label */}
        <div style={{
          position: 'absolute',
          top: -30,
          left: '50%',
          transform: 'translateX(-50%)',
          padding: '10px 28px',
          backgroundColor: '#FF6D1F',
          border: '3px solid #091413',
          borderRadius: 8,
          opacity: interpolate(sp(100), [0, 1], [0, 1]),
        }}>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 28,
            fontWeight: 900,
            color: '#091413',
            letterSpacing: 3,
            textTransform: 'uppercase',
          }}>
            ⚡ Barooodi Muhaana
          </span>
        </div>
      </div>

      {/* BOTTOM — Big question */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: '50%',
        transform: `translateX(-50%)`,
        textAlign: 'center',
      }}>
        <div style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: 30,
          padding: '24px 60px',
          backgroundColor: '#091413',
          borderRadius: 20,
          border: '4px solid #FF6D1F',
        }}>
          <span style={{
            fontFamily: 'Yantra One',
            fontSize: 64,
            color: '#FF97D0',
            fontWeight: 800,
          }}>
            ये है क्या
          </span>
          <span style={{
            fontFamily: 'Yantra One',
            fontSize: 120,
            color: '#FF6D1F',
            fontWeight: 900,
            display: 'inline-block',
            transform: `scale(${qScale}) rotate(${qRot}deg)`,
            lineHeight: 0.8,
          }}>
            ?
          </span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(9,20,19,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#091413', opacity: 0.4, letterSpacing: 1,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_StraitIntro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_TwentyPercent',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_TwentyPercent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Number counter 0 → 20
  const counter = Math.floor(interpolate(sp(30), [0, 1], [0, 20], { extrapolateRight: 'clamp' }));

  // Donut chart progress 0 → 20%
  const arcProg = interpolate(sp(30, { damping: 14 }), [0, 1], [0, 0.2]);

  // Donut size
  const donutSc = interpolate(sp(20, { damping: 10, stiffness: 130 }), [0, 1], [0, 1]);

  // Circumference of donut
  const radius = 180;
  const circ = 2 * Math.PI * radius;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF97D0', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(45deg, rgba(255,109,31,0.12) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 1000, height: 1000,
        borderRadius: '50%', border: '2px solid #091413', opacity: 0.06,
        top: '50%', right: -300,
        transform: `translateY(-50%) rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500,
        borderRadius: 40, backgroundColor: '#091413', opacity: 0.05,
        bottom: -200, left: -100,
        transform: `rotate(${-25 + frame * 0.15}deg)`,
      }} />

      {/* Hex pattern */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="hex8" width="60" height="52" patternUnits="userSpaceOnUse">
            <polygon points="30,2 55,15 55,40 30,52 5,40 5,15" fill="none" stroke="#091413" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hex8)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 35) * 1920 + Math.sin(frame * 0.02 + i) * 20,
          top: seededRandom(i + 120) * 1080 + Math.cos(frame * 0.03 + i) * 20,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: '#FF6D1F', opacity: 0.35,
        }} />
      ))}

      {/* TOP LABEL */}
      <div style={{
        position: 'absolute',
        top: 80,
        left: '50%',
        transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px)`,
        opacity: interpolate(sp(0), [0, 1], [0, 1]),
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 30,
          color: '#091413',
          letterSpacing: 6,
          fontWeight: 700,
          opacity: 0.7,
        }}>
          IN SIMPLE WORDS
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 90,
          color: '#091413',
          fontWeight: 900,
          marginTop: 4,
        }}>
          दुनिया का तेल — एक रास्ता
        </div>
      </div>

      {/* CENTER LAYOUT: Donut LEFT + Stat RIGHT */}

      {/* LEFT — Donut chart */}
      <div style={{
        position: 'absolute',
        left: 280,
        top: 380,
        transform: `scale(${donutSc})`,
      }}>
        <svg width="450" height="450" viewBox="0 0 450 450">
          {/* Background ring */}
          <circle cx="225" cy="225" r={radius} fill="none" stroke="#091413" strokeWidth="50" opacity="0.15" />
          {/* Progress arc */}
          <circle
            cx="225" cy="225" r={radius}
            fill="none"
            stroke="#FF6D1F"
            strokeWidth="50"
            strokeDasharray={circ}
            strokeDashoffset={circ - circ * arcProg}
            transform="rotate(-90 225 225)"
            strokeLinecap="round"
          />
          {/* Center text */}
          <text x="225" y="220" fontFamily="Inter" fontSize="120" fontWeight="900" fill="#091413" textAnchor="middle">
            {counter}%
          </text>
          <text x="225" y="270" fontFamily="Inter" fontSize="26" fontWeight="700" fill="#091413" textAnchor="middle" opacity="0.7" letterSpacing="3">
            GLOBAL OIL
          </text>
        </svg>
      </div>

      {/* RIGHT — Explanation cards */}
      <div style={{
        position: 'absolute',
        right: 180,
        top: 360,
        width: 700,
      }}>
        {/* Card 1 */}
        <div style={{
          opacity: interpolate(sp(80), [0, 1], [0, 1]),
          transform: `translateX(${interpolate(sp(80), [0, 1], [60, 0])}px)`,
          padding: '24px 32px',
          backgroundColor: '#091413',
          borderRadius: 16,
          borderLeft: '8px solid #FF6D1F',
          marginBottom: 24,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            color: '#FF6D1F',
            letterSpacing: 4,
            fontWeight: 700,
            marginBottom: 6,
          }}>
            FACT 01
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 54,
            color: '#FF97D0',
            fontWeight: 800,
            lineHeight: 1.1,
          }}>
            दुनिया का <span style={{ color: '#FF6D1F' }}>20%</span> तेल
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 32,
            color: '#F5F5F5',
            fontWeight: 500,
            marginTop: 6,
            opacity: 0.8,
          }}>
            एक ही रास्ते से गुज़रता है
          </div>
        </div>

        {/* Card 2 */}
        <div style={{
          opacity: interpolate(sp(130), [0, 1], [0, 1]),
          transform: `translateX(${interpolate(sp(130), [0, 1], [60, 0])}px)`,
          padding: '24px 32px',
          backgroundColor: '#091413',
          borderRadius: 16,
          borderLeft: '8px solid #FF6D1F',
          marginBottom: 24,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            color: '#FF6D1F',
            letterSpacing: 4,
            fontWeight: 700,
            marginBottom: 6,
          }}>
            FACT 02
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 54,
            color: '#FF97D0',
            fontWeight: 800,
            lineHeight: 1.1,
          }}>
            ईरान ने जलमार्ग
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 36,
            color: '#FF6D1F',
            fontWeight: 700,
            marginTop: 6,
          }}>
            प्रभावित कर दिया
          </div>
        </div>
      </div>

      {/* Bottom stat bar — oil barrel visualization */}
      <div style={{
        position: 'absolute',
        bottom: 60,
        left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex',
        gap: 8,
        opacity: interpolate(sp(170), [0, 1], [0, 1]),
      }}>
        {Array.from({ length: 20 }).map((_, i) => {
          const isHighlighted = i < 4; // 20% = 4 of 20
          const barreldelay = 170 + i * 4;
          const op = interpolate(sp(barreldelay), [0, 1], [0, 1]);
          return (
            <div key={i} style={{
              width: 36,
              height: 60,
              backgroundColor: isHighlighted ? '#FF6D1F' : 'rgba(9,20,19,0.25)',
              border: '2px solid #091413',
              borderRadius: 4,
              opacity: op,
            }} />
          );
        })}
      </div>
      <div style={{
        position: 'absolute',
        bottom: 30,
        left: '50%',
        transform: 'translateX(-50%)',
        fontFamily: 'Inter, Poppins',
        fontSize: 18,
        color: '#091413',
        letterSpacing: 3,
        fontWeight: 600,
        opacity: 0.7,
      }}>
        ████ = AFFECTED VOLUME (20 of 100)
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(9,20,19,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#091413', opacity: 0.4, letterSpacing: 1,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_TwentyPercent;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_PipeBlocked',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_PipeBlocked: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pipe reveals
  const pipeOp = interpolate(sp(10), [0, 1], [0, 1]);

  // Block / clamp drop
  const clampY = interpolate(sp(70, { damping: 8, stiffness: 160 }), [0, 1], [-300, 0]);
  const clampImpact = frame > 95 && frame < 110 ? Math.sin(frame * 2) * 5 : 0;

  // Supply vs demand bars
  const supplyH = interpolate(sp(140), [0, 1], [320, 80]);
  const demandH = 320; // stays constant

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF97D0', overflow: 'hidden', opacity: exitOp, transform: `translateY(${clampImpact}px)` }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at top, rgba(255,109,31,0.18) 0%, transparent 50%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 700, height: 700,
        borderRadius: '50%', border: '3px solid #091413', opacity: 0.07,
        top: 100, right: -200,
        transform: `rotate(${frame * 0.25}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500,
        borderRadius: 30, backgroundColor: '#FF6D1F', opacity: 0.06,
        bottom: -150, left: -50,
        transform: `rotate(${15 + frame * 0.12}deg)`,
      }} />

      {/* Grid */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="grid9" width="80" height="80" patternUnits="userSpaceOnUse">
            <rect width="80" height="80" fill="none" stroke="#091413" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid9)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 40) * 1920 + Math.sin(frame * 0.02 + i) * 20,
          top: seededRandom(i + 130) * 1080 + Math.cos(frame * 0.03 + i) * 20,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: '#091413', opacity: 0.3,
        }} />
      ))}

      {/* TOP TITLE */}
      <div style={{
        position: 'absolute',
        top: 70,
        left: '50%',
        transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px)`,
        opacity: interpolate(sp(0), [0, 1], [0, 1]),
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 30,
          color: '#091413',
          letterSpacing: 6,
          fontWeight: 700,
          opacity: 0.7,
        }}>
          THE ANALOGY
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 95,
          color: '#091413',
          fontWeight: 900,
          marginTop: 4,
        }}>
          तेल की नली पर <span style={{ color: '#FF6D1F' }}>हाथ</span>
        </div>
      </div>

      {/* CENTER — Pipe with clamp visualization */}
      <div style={{
        position: 'absolute',
        top: 290,
        left: '50%',
        transform: 'translateX(-50%)',
        width: 1400,
        height: 240,
        opacity: pipeOp,
      }}>
        {/* Pipe */}
        <svg width="1400" height="240" viewBox="0 0 1400 240">
          {/* Pipe body */}
          <rect x="40" y="80" width="1320" height="80" fill="#091413" rx="6" />
          {/* Pipe inner highlight */}
          <rect x="50" y="92" width="1300" height="20" fill="#FF6D1F" opacity="0.5" />
          {/* End caps */}
          <rect x="20" y="60" width="40" height="120" fill="#091413" rx="4" />
          <rect x="1340" y="60" width="40" height="120" fill="#091413" rx="4" />

          {/* Flow particles before clamp */}
          {Array.from({ length: 8 }).map((_, i) => {
            const flowDelay = frame > 100 ? 0 : 1;
            const fx = 80 + ((frame * 8 + i * 90) % 600);
            return (
              <circle
                key={`p1-${i}`}
                cx={fx}
                cy="120"
                r="8"
                fill="#FF6D1F"
                opacity={flowDelay * 0.9}
              />
            );
          })}

          {/* Flow particles after clamp — slowed/blocked */}
          {Array.from({ length: 3 }).map((_, i) => {
            const fx = 780 + ((frame * 1.2 + i * 200) % 600);
            const op = frame > 110 ? 0.3 : 0.8;
            return (
              <circle
                key={`p2-${i}`}
                cx={fx}
                cy="120"
                r={frame > 110 ? 4 : 8}
                fill="#FF6D1F"
                opacity={op}
              />
            );
          })}
        </svg>

        {/* CLAMP / HAND on top of pipe */}
        <div style={{
          position: 'absolute',
          top: clampY,
          left: 670,
          width: 200,
          height: 280,
        }}>
          <svg width="200" height="280" viewBox="0 0 200 280">
            {/* Hand shape simplified */}
            <rect x="60" y="0" width="80" height="180" rx="20" fill="#FF6D1F" stroke="#091413" strokeWidth="5" />
            <rect x="30" y="160" width="140" height="120" rx="20" fill="#FF6D1F" stroke="#091413" strokeWidth="5" />
            {/* Fingers */}
            <line x1="80" y1="0" x2="80" y2="60" stroke="#091413" strokeWidth="3" />
            <line x1="100" y1="0" x2="100" y2="60" stroke="#091413" strokeWidth="3" />
            <line x1="120" y1="0" x2="120" y2="60" stroke="#091413" strokeWidth="3" />
          </svg>
        </div>

        {/* IRAN label arrow */}
        <div style={{
          position: 'absolute',
          top: -30,
          left: 720,
          opacity: interpolate(sp(120), [0, 1], [0, 1]),
          padding: '8px 24px',
          backgroundColor: '#091413',
          borderRadius: 6,
        }}>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 26,
            fontWeight: 800,
            color: '#FF6D1F',
            letterSpacing: 3,
          }}>
            IRAN
          </span>
        </div>

        {/* Source label */}
        <div style={{
          position: 'absolute',
          left: 60,
          top: 200,
          fontFamily: 'Inter, Poppins',
          fontSize: 24,
          color: '#091413',
          fontWeight: 700,
          letterSpacing: 2,
          opacity: 0.7,
        }}>
          ⛽ SUPPLY
        </div>
        <div style={{
          position: 'absolute',
          right: 60,
          top: 200,
          fontFamily: 'Inter, Poppins',
          fontSize: 24,
          color: '#091413',
          fontWeight: 700,
          letterSpacing: 2,
          opacity: 0.7,
        }}>
          🌍 WORLD
        </div>
      </div>

      {/* BOTTOM — Supply vs Demand bars */}
      <div style={{
        position: 'absolute',
        bottom: 60,
        left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex',
        gap: 100,
        alignItems: 'flex-end',
        opacity: interpolate(sp(150), [0, 1], [0, 1]),
      }}>
        {/* Supply bar */}
        <div style={{ textAlign: 'center' }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 26,
            fontWeight: 800,
            color: '#FF6D1F',
            letterSpacing: 3,
            marginBottom: 8,
          }}>
            ↓ SUPPLY
          </div>
          <div style={{
            width: 140,
            height: supplyH,
            backgroundColor: '#FF6D1F',
            border: '4px solid #091413',
            borderRadius: 8,
            display: 'flex',
            alignItems: 'flex-end',
            justifyContent: 'center',
            paddingBottom: 12,
          }}>
            <span style={{
              fontFamily: 'Yantra One',
              fontSize: 38,
              color: '#091413',
              fontWeight: 900,
            }}>
              रुकी
            </span>
          </div>
        </div>

        {/* Equals/Result */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 80,
          color: '#091413',
          fontWeight: 900,
          alignSelf: 'center',
          marginBottom: 60,
        }}>
          ≠
        </div>

        {/* Demand bar */}
        <div style={{ textAlign: 'center' }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 26,
            fontWeight: 800,
            color: '#091413',
            letterSpacing: 3,
            marginBottom: 8,
          }}>
            → DEMAND
          </div>
          <div style={{
            width: 140,
            height: demandH,
            backgroundColor: '#091413',
            border: '4px solid #091413',
            borderRadius: 8,
            display: 'flex',
            alignItems: 'flex-end',
            justifyContent: 'center',
            paddingBottom: 12,
          }}>
            <span style={{
              fontFamily: 'Yantra One',
              fontSize: 38,
              color: '#FF97D0',
              fontWeight: 900,
            }}>
              वही
            </span>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(9,20,19,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#091413', opacity: 0.4, letterSpacing: 1,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_PipeBlocked;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_PriceSkyrockets',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_PriceSkyrockets: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slight zoom in
  const camZ = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  // Price counter 60 → 100+
  const priceVal = interpolate(sp(50, { damping: 14 }), [0, 1], [60, 102], { extrapolateRight: 'clamp' });
  const priceDisplay = priceVal.toFixed(1);

  // Chart line progress
  const chartProg = interpolate(sp(40), [0, 1], [0, 1]);

  // Arrow shake on impact
  const arrowShake = frame > 130 && frame < 145 ? Math.sin(frame * 2.2) * 5 : 0;

  // Path generation for upward chart line
  const chartPoints = [
    { x: 0, y: 280 },
    { x: 120, y: 260 },
    { x: 240, y: 240 },
    { x: 360, y: 220 },
    { x: 480, y: 180 },
    { x: 600, y: 130 },
    { x: 720, y: 70 },
    { x: 840, y: 20 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF97D0', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient — fiery */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at bottom right, rgba(255,109,31,0.25) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 800, height: 800,
        borderRadius: '50%', border: '3px solid #FF6D1F', opacity: 0.1,
        top: -200, left: -200,
        transform: `rotate(${frame * 0.3}deg) scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500,
        borderRadius: 30, backgroundColor: '#091413', opacity: 0.06,
        bottom: -150, right: -100,
        transform: `rotate(${-20 + frame * 0.15}deg)`,
      }} />

      {/* Diagonal stripe texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="stripe10" width="20" height="20" patternUnits="userSpaceOnUse">
            <line x1="0" y1="20" x2="20" y2="0" stroke="#091413" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#stripe10)" />
      </svg>

      {/* Particles — rising sparks */}
      {Array.from({ length: 30 }).map((_, i) => {
        const px = seededRandom(i + 45) * 1920;
        const py = (1080 - ((frame * 3 + seededRandom(i + 200) * 1080) % 1080));
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#FF6D1F', opacity: 0.5,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZ})` }}>

        {/* TOP — Breaking news strip */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: '50%',
          transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px)`,
          opacity: interpolate(sp(0), [0, 1], [0, 1]),
          display: 'flex',
          alignItems: 'center',
          gap: 24,
          padding: '14px 36px',
          backgroundColor: '#091413',
          borderRadius: 8,
          border: '3px solid #FF6D1F',
        }}>
          <div style={{
            width: 18, height: 18,
            borderRadius: '50%',
            backgroundColor: '#FF6D1F',
            transform: `scale(${1 + Math.sin(frame * 0.3) * 0.3})`,
          }} />
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 30,
            fontWeight: 900,
            color: '#FF6D1F',
            letterSpacing: 5,
            textTransform: 'uppercase',
          }}>
            BREAKING — CRUDE OIL SURGE
          </span>
        </div>

        {/* LEFT — Chart visualization */}
        <div style={{
          position: 'absolute',
          left: 150,
          top: 280,
          width: 900,
          height: 380,
          opacity: interpolate(sp(20), [0, 1], [0, 1]),
        }}>
          {/* Chart container */}
          <div style={{
            width: '100%',
            height: '100%',
            backgroundColor: '#091413',
            borderRadius: 18,
            border: '4px solid #FF6D1F',
            padding: 30,
            boxSizing: 'border-box',
          }}>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 22,
              color: '#FF6D1F',
              letterSpacing: 3,
              fontWeight: 700,
              marginBottom: 14,
            }}>
              CRUDE OIL — $/BARREL
            </div>

            <svg width="100%" height="280" viewBox="0 0 840 300">
              {/* Grid lines */}
              {[0, 1, 2, 3, 4].map((i) => (
                <line key={i} x1="0" y1={i * 60} x2="840" y2={i * 60} stroke="#FF97D0" strokeWidth="0.5" opacity="0.2" />
              ))}

              {/* Chart line (reveal via dashoffset) */}
              <path
                d={chartPoints.reduce((acc, p, i) => acc + (i === 0 ? `M ${p.x} ${p.y}` : ` L ${p.x} ${p.y}`), '')}
                fill="none"
                stroke="#FF6D1F"
                strokeWidth="6"
                strokeLinecap="round"
                strokeLinejoin="round"
                strokeDasharray="1200"
                strokeDashoffset={1200 - chartProg * 1200}
              />

              {/* Filled area below */}
              <path
                d={chartPoints.reduce((acc, p, i) => acc + (i === 0 ? `M ${p.x} ${p.y}` : ` L ${p.x} ${p.y}`), '') + ` L 840 300 L 0 300 Z`}
                fill="#FF6D1F"
                opacity={interpolate(sp(60), [0, 1], [0, 0.2])}
              />

              {/* Endpoint dot pulsing */}
              {chartProg > 0.9 && (
                <circle
                  cx={840}
                  cy={20}
                  r={12 + Math.sin(frame * 0.3) * 4}
                  fill="#FF6D1F"
                  stroke="#FF97D0"
                  strokeWidth="3"
                />
              )}

              {/* $100 marker line */}
              {chartProg > 0.7 && (
                <>
                  <line x1="0" y1="40" x2="840" y2="40" stroke="#FF97D0" strokeWidth="2" strokeDasharray="6 4" opacity="0.7" />
                  <text x="10" y="32" fontFamily="Inter" fontSize="18" fill="#FF97D0" fontWeight="700">$100 LINE</text>
                </>
              )}
            </svg>

            {/* X-axis label */}
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 18,
              color: '#9E9E9E',
              letterSpacing: 2,
              marginTop: 10,
              display: 'flex',
              justifyContent: 'space-between',
            }}>
              <span>BEFORE CRISIS</span>
              <span>NOW</span>
            </div>
          </div>
        </div>

        {/* RIGHT — Big price card */}
        <div style={{
          position: 'absolute',
          right: 150,
          top: 280,
          width: 620,
          padding: 40,
          backgroundColor: '#091413',
          borderRadius: 20,
          border: '4px solid #FF6D1F',
          opacity: interpolate(sp(100), [0, 1], [0, 1]),
          transform: `translateX(${interpolate(sp(100), [0, 1], [60, 0])}px) translateY(${arrowShake}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 24,
            color: '#FF6D1F',
            letterSpacing: 4,
            fontWeight: 700,
            marginBottom: 6,
          }}>
            CURRENT PRICE
          </div>
          <div style={{
            display: 'flex',
            alignItems: 'flex-start',
            gap: 10,
          }}>
            <span style={{
              fontFamily: 'JetBrains Mono, Inter',
              fontSize: 70,
              color: '#FF97D0',
              fontWeight: 800,
              marginTop: 28,
            }}>
              $
            </span>
            <span style={{
              fontFamily: 'JetBrains Mono, Inter',
              fontSize: 200,
              color: '#FF6D1F',
              fontWeight: 900,
              lineHeight: 1,
              letterSpacing: -4,
            }}>
              {priceDisplay}
            </span>
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 28,
            color: '#F5F5F5',
            fontWeight: 600,
            letterSpacing: 2,
            marginTop: -10,
          }}>
            PER BARREL
          </div>

          {/* Up arrow indicator */}
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 14,
            marginTop: 24,
            padding: '14px 22px',
            backgroundColor: '#FF6D1F',
            borderRadius: 10,
          }}>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="#091413">
              <polygon points="12,3 22,16 16,16 16,22 8,22 8,16 2,16" />
            </svg>
            <span style={{
              fontFamily: 'Yantra One',
              fontSize: 46,
              color: '#091413',
              fontWeight: 900,
              letterSpacing: -1,
            }}>
              आसमान छू रहा
            </span>
          </div>
        </div>

        {/* Bottom tagline */}
        <div style={{
          position: 'absolute',
          bottom: 50,
          left: '50%',
          transform: `translateX(-50%) translateY(${interpolate(sp(180), [0, 1], [30, 0])}px)`,
          opacity: interpolate(sp(180), [0, 1], [0, 1]),
        }}>
          <span style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 40,
            color: '#091413',
            fontWeight: 700,
            letterSpacing: 1,
          }}>
            $100/बैरल का <span style={{ color: '#FF6D1F', fontWeight: 900 }}>मनोवैज्ञानिक स्तर</span> टूटा
          </span>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(9,20,19,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#091413', opacity: 0.4, letterSpacing: 1,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_PriceSkyrockets;
```

