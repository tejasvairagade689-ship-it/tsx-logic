```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C1S01_IntezaarHook',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C1S01_IntezaarHook: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Multi-beat phase controller
  const ph = (a: number, b: number) =>
    interpolate(frame, [a, a + 14, b - 14, b], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const beatA = ph(0, 215);    // Intezaar + clock
  const beatB = ph(215, 480);  // July 2026 report stamp
  const beatC = ph(480, durationInFrames); // Rate hike warning

  // Camera: slow push-in + impact shake at stamp slam
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.09], { easing: Easing.inOut(Easing.ease) });
  const shakeT = frame - 228;
  const shake = shakeT > 0 && shakeT < 16 ? Math.sin(shakeT * 2.4) * (16 - shakeT) * 1.4 : 0;

  const clockRot = frame * 3.2; // fast anxious clock hand

  const brutalCard: React.CSSProperties = {
    background: '#123B17',
    border: '6px solid #F7F4ED',
    boxShadow: '18px 18px 0 rgba(5, 24, 9, 0.95)',
    padding: '26px 54px',
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#1B5E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background (darker brutal green) */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${30 + Math.sin(frame * 0.008) * 12}% 30%, #1B5E20 0%, #12401A 46%, #0A2B10 100%)`,
      }} />
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, rgba(6,22,9,0.55) 0%, rgba(6,22,9,0) 35%, rgba(6,22,9,0.65) 100%)' }} />

      {/* Layer 2: Animated shape layers — drifting transparent geometry */}
      {[0, 1, 2].map((i) => (
        <div key={`shape-${i}`} style={{
          position: 'absolute',
          width: 420 + i * 180,
          height: 420 + i * 180,
          left: `${12 + i * 30}%`,
          top: `${8 + i * 22}%`,
          border: '3px solid rgba(217,255,244,0.06)',
          background: i === 1 ? 'rgba(247,244,237,0.03)' : 'transparent',
          borderRadius: i === 2 ? '50%' : 0,
          transform: `translate(${Math.sin(frame * 0.006 + i * 2) * 40}px, ${Math.cos(frame * 0.005 + i) * 30}px) rotate(${frame * 0.04 * (i % 2 === 0 ? 1 : -1)}deg) scale(${1 + Math.sin(frame * 0.01 + i) * 0.04})`,
        }} />
      ))}

      {/* Layer 3: Motion texture — diagonal punk stripes */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(-45deg, rgba(247,244,237,0.05) 0px, rgba(247,244,237,0.05) 2px, transparent 2px, transparent 46px)',
        transform: `translateX(${(frame * 0.5) % 46}px)`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const sx = seededRandom(i + 1) * 1920;
        const sy = seededRandom(i + 40) * 1080;
        const drift = Math.sin(frame * 0.02 + i * 1.7) * 26;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: sx, top: (sy + frame * (0.25 + seededRandom(i) * 0.5)) % 1080,
            width: 3 + seededRandom(i + 7) * 4, height: 3 + seededRandom(i + 7) * 4,
            background: i % 3 === 0 ? '#FFC107' : '#D9FFF4',
            opacity: 0.12 + seededRandom(i + 9) * 0.16,
            transform: `translateX(${drift}px) rotate(45deg)`,
          }} />
        );
      })}

      {/* Layer 5: Content — camera rig */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${shake}px, ${-shake * 0.6}px)` }}>

        {/* BEAT A — "5 SAAL KA INTEZAAR" + brutal clock icon */}
        <AbsoluteFill style={{ opacity: beatA, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ transform: `scale(${sp(8)}) rotate(-2deg)`, ...brutalCard }}>
            <div style={{ fontFamily: 'Yantra One', fontSize: 145, color: '#F7F4ED', lineHeight: 1.02 }}>
              सालों का <span style={{ color: '#FFC107' }}>इंतज़ार</span>
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 55, color: '#D9FFF4', marginTop: 14, opacity: sp(28) }}>
              RBI RATE CUT → EMI कम?
            </div>
          </div>
          {/* Iconographic clock — anxious spinning hand */}
          <svg width="220" height="220" viewBox="0 0 220 220" style={{
            position: 'absolute', right: 150, top: 130,
            transform: `scale(${sp(40)}) rotate(6deg)`,
          }}>
            <circle cx="110" cy="110" r="88" fill="#0E3313" stroke="#F7F4ED" strokeWidth="8" />
            <circle cx="110" cy="110" r="88" fill="none" stroke="#FFC107" strokeWidth="4" strokeDasharray="10 14" />
            <line x1="110" y1="110" x2={110 + 62 * Math.cos((clockRot * Math.PI) / 180)} y2={110 + 62 * Math.sin((clockRot * Math.PI) / 180)} stroke="#D9FFF4" strokeWidth="8" strokeLinecap="square" />
            <line x1="110" y1="110" x2={110 + 40 * Math.cos((clockRot * 0.4 * Math.PI) / 180)} y2={110 + 40 * Math.sin((clockRot * 0.4 * Math.PI) / 180)} stroke="#FFC107" strokeWidth="10" strokeLinecap="square" />
          </svg>
          {/* Collage tape strip */}
          <div style={{ position: 'absolute', left: 260, bottom: 200, width: 240, height: 44, background: '#FFC107', opacity: 0.85, transform: `rotate(-8deg) scaleX(${sp(50)})` }} />
        </AbsoluteFill>

        {/* BEAT B — JULY 2026 REPORT stamp slam */}
        <AbsoluteFill style={{ opacity: beatB, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{
            ...brutalCard, background: '#F7F4ED', transform: `scale(${interpolate(sp(218, { damping: 9 }), [0, 1], [2.4, 1])}) rotate(-4deg)`,
          }}>
            <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 108, color: '#0E3313', letterSpacing: -2 }}>
              JULY 2026 REPORT
            </div>
          </div>
          <div style={{ marginTop: 55, transform: `translateY(${interpolate(sp(258), [0, 1], [90, 0])}px)`, opacity: sp(258) }}>
            <span style={{ fontFamily: 'Yantra One', fontSize: 95, color: '#D9FFF4' }}>RATE CUT </span>
            <span style={{ fontFamily: 'Yantra One', fontSize: 95, color: '#ff0000', textDecoration: 'line-through', textDecorationThickness: 10 }}>CONFIRM</span>
            <span style={{ fontFamily: 'Yantra One', fontSize: 95, color: '#FFC107' }}> CANCEL</span>
          </div>
        </AbsoluteFill>

        {/* BEAT C — Rate hike warning + home loan weight */}
        <AbsoluteFill style={{ opacity: beatC, justifyContent: 'center', alignItems: 'center', flexDirection: 'row', gap: 90 }}>
          {/* Up-arrow danger icon */}
          <svg width="300" height="340" viewBox="0 0 300 340" style={{ transform: `translateY(${interpolate(sp(492), [0, 1], [120, 0]) + Math.sin(frame * 0.09) * 8}px)`, opacity: sp(492) }}>
            <rect x="30" y="30" width="240" height="280" fill="#0E3313" stroke="#F7F4ED" strokeWidth="8" transform="rotate(-2 150 170)" />
            <path d="M150 80 L225 190 L180 190 L180 265 L120 265 L120 190 L75 190 Z" fill="#ff0000" stroke="#F7F4ED" strokeWidth="6" />
          </svg>
          <div style={{ maxWidth: 950 }}>
            <div style={{ fontFamily: 'Yantra One', fontSize: 118, color: '#F7F4ED', lineHeight: 1.05, transform: `translateX(${interpolate(sp(505), [0, 1], [140, 0])}px)`, opacity: sp(505) }}>
              RATE <span style={{ color: '#ff0000' }}>बढ़</span> भी सकता है
            </div>
            <div style={{ marginTop: 30, ...brutalCard, background: '#FFC107', display: 'inline-block', transform: `rotate(2deg) scale(${sp(545)})` }}>
              <span style={{ fontFamily: 'Noto Sans Devanagari', fontWeight: 800, fontSize: 52, color: '#0E3313' }}>
                HOME LOAN = सबसे बड़ी COMMITMENT
              </span>
            </div>
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(4,18,7,0.72) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C1S01_IntezaarHook;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C1S02_JaalTrap',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C1S02_JaalTrap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const ph = (a: number, b: number) =>
    interpolate(frame, [a, a + 14, b - 14, b], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const beatA = ph(0, 260);   // trap bars + frozen EMI/principal
  const beatB = ph(260, 500); // money drain to bank
  const beatC = ph(500, durationInFrames); // Kunal nameplate reveal

  // Camera: lateral pan
  const camX = interpolate(frame, [0, durationInFrames], [30, -30], { easing: Easing.inOut(Easing.ease) });
  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1.1]);

  const brutalCard: React.CSSProperties = {
    background: '#123B17', border: '6px solid #F7F4ED',
    boxShadow: '18px 18px 0 rgba(5,24,9,0.95)', padding: '24px 48px',
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#1B5E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 70% ${40 + Math.cos(frame * 0.007) * 10}%, #1B5E20 0%, #113C18 48%, #08270E 100%)`,
      }} />
      <AbsoluteFill style={{ background: 'linear-gradient(120deg, rgba(5,20,8,0.6) 0%, transparent 45%, rgba(5,20,8,0.6) 100%)' }} />

      {/* Layer 2: Animated shape layers */}
      {[0, 1, 2].map((i) => (
        <div key={`shape-${i}`} style={{
          position: 'absolute', width: 500 + i * 140, height: 340 + i * 120,
          left: `${5 + i * 34}%`, top: `${14 + i * 20}%`,
          border: '3px solid rgba(255,193,7,0.05)',
          background: i === 0 ? 'rgba(217,255,244,0.03)' : 'transparent',
          transform: `translate(${Math.cos(frame * 0.005 + i * 2) * 36}px, ${Math.sin(frame * 0.006 + i) * 28}px) rotate(${8 + frame * 0.03 * (i % 2 ? -1 : 1)}deg) scale(${1 + Math.cos(frame * 0.011 + i) * 0.04})`,
        }} />
      ))}

      {/* Layer 3: Motion texture — coarse grid */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(rgba(247,244,237,0.045) 2px, transparent 2px), linear-gradient(90deg, rgba(247,244,237,0.045) 2px, transparent 2px)',
        backgroundSize: '120px 120px',
        transform: `translate(${(frame * 0.3) % 120}px, ${(frame * 0.18) % 120}px)`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => (
        <div key={`p-${i}`} style={{
          position: 'absolute',
          left: (seededRandom(i + 3) * 1920 + Math.sin(frame * 0.02 + i) * 30) % 1920,
          top: (seededRandom(i + 60) * 1080 + frame * (0.3 + seededRandom(i) * 0.4)) % 1080,
          width: 4, height: 4, background: i % 4 === 0 ? '#FFC107' : '#D9FFF4',
          opacity: 0.1 + seededRandom(i + 11) * 0.15, transform: 'rotate(45deg)',
        }} />
      ))}

      {/* Layer 5: Content — camera rig */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px) scale(${camScale})` }}>

        {/* BEAT A — jail bars drop over frozen EMI + PRINCIPAL meters */}
        <AbsoluteFill style={{ opacity: beatA, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ display: 'flex', gap: 70 }}>
            {[{ label: 'EMI', v: 'सेम की सेम' }, { label: 'PRINCIPAL', v: 'जस का तस' }].map((m, i) => (
              <div key={m.label} style={{ ...brutalCard, transform: `rotate(${i ? 2 : -2}deg) scale(${sp(10 + i * 18)})` }}>
                <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 72, color: '#FFC107' }}>{m.label}</div>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 46, color: '#D9FFF4', marginTop: 8 }}>{m.v}</div>
                {/* frozen meter */}
                <div style={{ marginTop: 20, width: 420, height: 34, border: '5px solid #F7F4ED', background: '#0A2B10' }}>
                  <div style={{ width: '62%', height: '100%', background: '#9E9E9E', opacity: 0.7 }} />
                </div>
                <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 36, color: '#ff0000', marginTop: 10, opacity: 0.6 + Math.sin(frame * 0.2 + i) * 0.4 }}>
                  0% MOVEMENT
                </div>
              </div>
            ))}
          </div>
          {/* Jail bars slam from top — the JAAL */}
          {Array.from({ length: 8 }).map((_, i) => (
            <div key={`bar-${i}`} style={{
              position: 'absolute', left: 240 + i * 200, top: 0, width: 26, height: 1080,
              background: 'linear-gradient(180deg, #0A2B10, #164a1d)', border: '3px solid rgba(247,244,237,0.35)',
              transform: `translateY(${interpolate(sp(70 + i * 6, { damping: 14 }), [0, 1], [-1120, 0])}px)`,
            }} />
          ))}
          <div style={{ position: 'absolute', bottom: 110, fontFamily: 'Yantra One', fontSize: 135, color: '#F7F4ED', textShadow: '10px 10px 0 rgba(5,24,9,0.9)', transform: `scale(${sp(140)})` }}>
            आप <span style={{ color: '#ff0000' }}>जाल</span> में हैं
          </div>
        </AbsoluteFill>

        {/* BEAT B — salary coins draining into BANK block */}
        <AbsoluteFill style={{ opacity: beatB, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 60 }}>
            <div style={{ ...brutalCard, transform: `rotate(-2deg) scale(${sp(268)})` }}>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 62, color: '#D9FFF4' }}>आपकी मेहनत</div>
              <div style={{ fontFamily: 'Yantra One', fontSize: 88, color: '#F7F4ED' }}>की कमाई</div>
            </div>
            {/* animated coin stream */}
            <svg width="420" height="200" viewBox="0 0 420 200" style={{ opacity: sp(290) }}>
              <path d="M10 100 H410" stroke="#FFC107" strokeWidth="6" strokeDasharray="26 20" strokeDashoffset={-frame * 3} />
              <path d="M360 70 L410 100 L360 130 Z" fill="#FFC107" />
              {[0, 1, 2, 3].map((c) => (
                <circle key={c} cx={((frame * 4 + c * 105) % 400) + 10} cy={100 + Math.sin(frame * 0.15 + c) * 14} r="17" fill="#FFC107" stroke="#0E3313" strokeWidth="4" />
              ))}
            </svg>
            <div style={{ ...brutalCard, background: '#F7F4ED', transform: `rotate(2deg) scale(${sp(310, { damping: 9 })})` }}>
              <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 76, color: '#0E3313' }}>BANK</div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 42, color: '#ff0000', fontWeight: 700 }}>INTEREST INCOME</div>
            </div>
          </div>
          <div style={{ marginTop: 60, fontFamily: 'Noto Sans Devanagari', fontSize: 48, color: '#D9FFF4', opacity: sp(350), transform: `translateY(${interpolate(sp(350), [0, 1], [40, 0])}px)` }}>
            हर महीने। बिना रुके। सीधे bank की जेब में।
          </div>
        </AbsoluteFill>

        {/* BEAT C — Kunal / ProBuffet HQ brutal nameplate */}
        <AbsoluteFill style={{ opacity: beatC, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ ...brutalCard, background: '#FFC107', transform: `rotate(-3deg) scale(${sp(508, { damping: 9 })})` }}>
            <div style={{ fontFamily: 'Yantra One', fontSize: 120, color: '#0E3313' }}>मैं कुनाल हूँ</div>
          </div>
          <div style={{ marginTop: 40, ...brutalCard, transform: `rotate(1.5deg) translateY(${interpolate(sp(540), [0, 1], [70, 0])}px)`, opacity: sp(540) }}>
            <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 64, color: '#F7F4ED' }}>ProBuffet HQ</div>
          </div>
          <div style={{ marginTop: 48, display: 'flex', gap: 34 }}>
            {['BANK नहीं बताएगा', 'ADVISOR नहीं बताएगा', 'NEWS नहीं बताएगा'].map((t, i) => (
              <div key={t} style={{
                border: '4px solid #ff0000', padding: '14px 26px', background: 'rgba(10,43,16,0.9)',
                transform: `rotate(${(i - 1) * 3}deg) scale(${sp(575 + i * 15)})`,
              }}>
                <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 38, fontWeight: 700, color: '#D9FFF4' }}>{t}</span>
              </div>
            ))}
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(4,18,7,0.75) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C1S02_JaalTrap;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C1S03_ReutersPoll',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C1S03_ReutersPoll: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const ph = (a: number, b: number) =>
    interpolate(frame, [a, a + 14, b - 14, b], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const beatA = ph(0, 200);   // uncomfortable truth
  const beatB = ph(200, 470); // Reuters poll 72 economists
  const beatC = ph(470, durationInFrames); // NAHI stamp + 5.25% lock

  // Camera: slow tilt-zoom, shake on NAHI stamp
  const camScale = interpolate(frame, [0, durationInFrames], [1.1, 1.0], { easing: Easing.inOut(Easing.ease) });
  const camRot = interpolate(frame, [0, durationInFrames], [-0.7, 0.7]);
  const shakeT = frame - 492;
  const shake = shakeT > 0 && shakeT < 14 ? Math.sin(shakeT * 2.6) * (14 - shakeT) * 1.6 : 0;

  const brutalCard: React.CSSProperties = {
    background: '#123B17', border: '6px solid #F7F4ED',
    boxShadow: '18px 18px 0 rgba(5,24,9,0.95)', padding: '24px 48px',
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#1B5E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 50% ${75 - Math.sin(frame * 0.006) * 10}%, #1B5E20 0%, #10381六 0%, #123C18 46%, #072409 100%)`.replace('#10381六 0%,', ''),
      }} />
      <AbsoluteFill style={{ background: 'linear-gradient(0deg, rgba(4,17,7,0.7) 0%, transparent 40%)' }} />

      {/* Layer 2: Animated shape layers */}
      {[0, 1, 2].map((i) => (
        <div key={`shape-${i}`} style={{
          position: 'absolute', width: 380 + i * 200, height: 380 + i * 200, borderRadius: '50%',
          left: `${58 - i * 22}%`, top: `${-10 + i * 26}%`,
          border: '3px solid rgba(217,255,244,0.055)',
          transform: `translate(${Math.sin(frame * 0.0055 + i * 3) * 34}px, ${Math.cos(frame * 0.0065 + i) * 26}px) scale(${1 + Math.sin(frame * 0.012 + i * 2) * 0.05})`,
        }} />
      ))}

      {/* Layer 3: Motion texture — halftone dots (punk print feel) */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(rgba(247,244,237,0.07) 2.5px, transparent 2.5px)',
        backgroundSize: '54px 54px',
        transform: `translateY(${(frame * 0.4) % 54}px)`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={`p-${i}`} style={{
          position: 'absolute',
          left: (seededRandom(i + 5) * 1920) % 1920,
          top: (seededRandom(i + 80) * 1080 + frame * (0.2 + seededRandom(i) * 0.45)) % 1080,
          width: 5, height: 5, borderRadius: i % 2 ? 0 : '50%',
          background: i % 5 === 0 ? '#FFC107' : '#D9FFF4',
          opacity: 0.1 + seededRandom(i + 13) * 0.14,
          transform: `translateX(${Math.sin(frame * 0.03 + i * 2) * 22}px)`,
        }} />
      ))}

      {/* Layer 5: Content — camera rig */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg) translate(${shake}px, ${shake * 0.5}px)` }}>

        {/* BEAT A — uncomfortable / darane wali picture */}
        <AbsoluteFill style={{ opacity: beatA, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ fontFamily: 'Yantra One', fontSize: 150, color: '#F7F4ED', lineHeight: 1, textAlign: 'center', textShadow: '12px 12px 0 rgba(5,24,9,0.9)', transform: `scale(${sp(8)})` }}>
            ये तस्वीर
          </div>
          <div style={{ display: 'flex', gap: 40, marginTop: 44 }}>
            {[{ t: 'UNCOMFORTABLE', c: '#FFC107' }, { t: 'डराने वाली', c: '#ff0000' }, { t: 'ज़रूरी', c: '#D9FFF4' }].map((w, i) => (
              <div key={w.t} style={{
                border: `5px solid ${w.c}`, padding: '16px 34px', background: 'rgba(10,43,16,0.92)',
                transform: `rotate(${(i - 1) * 4}deg) scale(${sp(35 + i * 22, { damping: 9 })})`,
                boxShadow: '10px 10px 0 rgba(5,24,9,0.9)',
              }}>
                <span style={{ fontFamily: i === 0 ? 'Inter, Poppins' : 'Noto Sans Devanagari', fontWeight: 900, fontSize: 54, color: w.c }}>{w.t}</span>
              </div>
            ))}
          </div>
        </AbsoluteFill>

        {/* BEAT B — Reuters poll header + 72 economists icon row */}
        <AbsoluteFill style={{ opacity: beatB, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ ...brutalCard, background: '#F7F4ED', transform: `rotate(-2deg) scale(${sp(208)})` }}>
            <span style={{ fontFamily: 'JetBrains Mono', fontWeight: 700, fontSize: 56, color: '#0E3313' }}>27 JULY 2026 | REUTERS POLL</span>
          </div>
          {/* 72 economist glyph grid — staggered pop-in */}
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(18, 58px)', gap: 12, marginTop: 55 }}>
            {Array.from({ length: 72 }).map((_, i) => (
              <svg key={i} width="52" height="60" viewBox="0 0 52 60" style={{ transform: `scale(${sp(235 + i * 1.4, { stiffness: 180 })})` }}>
                <circle cx="26" cy="16" r="12" fill="#D9FFF4" opacity="0.9" />
                <rect x="8" y="32" width="36" height="24" fill="#D9FFF4" opacity="0.75" />
              </svg>
            ))}
          </div>
          <div style={{ marginTop: 46, fontFamily: 'Noto Sans Devanagari', fontSize: 56, color: '#FFC107', fontWeight: 700, opacity: sp(360), transform: `translateY(${interpolate(sp(360), [0, 1], [40, 0])}px)` }}>
            72 ECONOMISTS से सवाल — August में rate बदलेगा?
          </div>
        </AbsoluteFill>

        {/* BEAT C — giant NAHI stamp + 5.25% padlock */}
        <AbsoluteFill style={{ opacity: beatC, justifyContent: 'center', alignItems: 'center', flexDirection: 'row', gap: 110 }}>
          <div style={{
            border: '10px solid #ff0000', padding: '30px 70px', background: 'rgba(14,51,19,0.95)',
            transform: `rotate(-8deg) scale(${interpolate(sp(488, { damping: 8 }), [0, 1], [3, 1])})`,
            boxShadow: '20px 20px 0 rgba(5,24,9,0.95)',
          }}>
            <span style={{ fontFamily: 'Yantra One', fontSize: 190, color: '#ff0000', lineHeight: 1 }}>नहीं.</span>
          </div>
          <div style={{ textAlign: 'center' }}>
            {/* padlock icon */}
            <svg width="190" height="210" viewBox="0 0 190 210" style={{ transform: `scale(${sp(540)}) translateY(${Math.sin(frame * 0.08) * 6}px)` }}>
              <rect x="30" y="90" width="130" height="105" fill="#FFC107" stroke="#F7F4ED" strokeWidth="8" />
              <path d="M55 90 V60 a40 40 0 0 1 80 0 V90" fill="none" stroke="#F7F4ED" strokeWidth="14" />
              <circle cx="95" cy="140" r="14" fill="#0E3313" />
              <rect x="89" y="145" width="12" height="28" fill="#0E3313" />
            </svg>
            <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 128, color: '#F7F4ED', opacity: sp(565) }}>
              5.25<span style={{ color: '#FFC107' }}>%</span>
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, color: '#D9FFF4', opacity: sp(590) }}>
              REPO RATE — साल भर यहीं LOCKED
            </div>
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(4,18,7,0.75) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C1S03_ReutersPoll;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C1S04_95Percent',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C1S04_95Percent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const ph = (a: number, b: number) =>
    interpolate(frame, [a, a + 14, b - 14, b], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const beatA = ph(0, 250);   // 95% counter + 68/72 grid
  const beatB = ph(250, 520); // 125 bps cut in 2025
  const beatC = ph(520, durationInFrames); // kya EMI tak pahucha? NAHI

  // Camera: orbit-ish drift
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.1], { easing: Easing.inOut(Easing.ease) });
  const camY = Math.sin(frame * 0.01) * 10;

  // Big animated counter 0 -> 95
  const count95 = Math.round(interpolate(frame, [20, 95], [0, 95], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));

  const brutalCard: React.CSSProperties = {
    background: '#123B17', border: '6px solid #F7F4ED',
    boxShadow: '18px 18px 0 rgba(5,24,9,0.95)', padding: '24px 48px',
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#1B5E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${20 + Math.sin(frame * 0.005) * 8}% 60%, #1C6023 0%, #123C18 50%, #082408 100%)`,
      }} />
      <AbsoluteFill style={{ background: 'linear-gradient(200deg, rgba(4,17,7,0.6) 0%, transparent 50%, rgba(4,17,7,0.65) 100%)' }} />

      {/* Layer 2: Animated shape layers */}
      {[0, 1, 2].map((i) => (
        <div key={`shape-${i}`} style={{
          position: 'absolute', width: 300 + i * 240, height: 300 + i * 240,
          right: `${-6 + i * 24}%`, bottom: `${-14 + i * 18}%`,
          border: '3px solid rgba(255,193,7,0.06)',
          transform: `rotate(${45 + frame * 0.05 * (i % 2 ? 1 : -1)}deg) scale(${1 + Math.sin(frame * 0.009 + i) * 0.05})`,
          background: i === 2 ? 'rgba(217,255,244,0.025)' : 'transparent',
        }} />
      ))}

      {/* Layer 3: Motion texture — checker squares */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(90deg, rgba(247,244,237,0.04) 0 3px, transparent 3px 90px), repeating-linear-gradient(0deg, rgba(247,244,237,0.04) 0 3px, transparent 3px 90px)',
        transform: `translate(${(-frame * 0.35) % 90}px, 0)`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={`p-${i}`} style={{
          position: 'absolute',
          left: (seededRandom(i + 21) * 1920 + Math.cos(frame * 0.025 + i) * 24) % 1920,
          top: (seededRandom(i + 90) * 1080 + frame * (0.22 + seededRandom(i) * 0.5)) % 1080,
          width: 4, height: 4, background: i % 3 === 0 ? '#FFC107' : '#D9FFF4',
          opacity: 0.1 + seededRandom(i + 17) * 0.15,
        }} />
      ))}

      {/* Layer 5: Content — camera rig */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY}px)` }}>

        {/* BEAT A — 95% mega counter + 68/72 fill grid */}
        <AbsoluteFill style={{ opacity: beatA, justifyContent: 'center', alignItems: 'center', flexDirection: 'row', gap: 100 }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 300, lineHeight: 1, color: '#FFC107', textShadow: '14px 14px 0 rgba(5,24,9,0.9)', transform: `scale(${sp(6, { damping: 10 })})` }}>
              {count95}%
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 50, color: '#D9FFF4', marginTop: 8, opacity: sp(60) }}>
              economists एक ही तरफ
            </div>
          </div>
          <div>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(9, 62px)', gap: 10 }}>
              {Array.from({ length: 72 }).map((_, i) => (
                <div key={i} style={{
                  width: 56, height: 56, border: '4px solid rgba(247,244,237,0.55)',
                  background: i < 68 ? '#FFC107' : 'transparent',
                  transform: `scale(${sp(40 + i * 1.6, { stiffness: 200 })})`,
                }} />
              ))}
            </div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 54, color: '#F7F4ED', marginTop: 24, opacity: sp(180) }}>
              68 / 72 → REPO <span style={{ color: '#FFC107' }}>5.25%</span> पर टिका
            </div>
          </div>
        </AbsoluteFill>

        {/* BEAT B — 2025 scissor cut: 125 bps */}
        <AbsoluteFill style={{ opacity: beatB, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ ...brutalCard, background: '#F7F4ED', transform: `rotate(-2deg) scale(${sp(258)})` }}>
            <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 70, color: '#0E3313' }}>2025 FLASHBACK</span>
          </div>
          <div style={{ display: 'flex', alignItems: 'center', gap: 50, marginTop: 60 }}>
            {/* scissors icon */}
            <svg width="200" height="160" viewBox="0 0 200 160" style={{ transform: `rotate(${interpolate(sp(300), [0, 1], [-40, 0])}deg) scale(${sp(300)})` }}>
              <circle cx="42" cy="42" r="26" fill="none" stroke="#D9FFF4" strokeWidth="10" />
              <circle cx="42" cy="118" r="26" fill="none" stroke="#D9FFF4" strokeWidth="10" />
              <line x1="62" y1="58" x2="180" y2="112" stroke="#D9FFF4" strokeWidth="12" strokeLinecap="square" />
              <line x1="62" y1="102" x2="180" y2="48" stroke="#D9FFF4" strokeWidth="12" strokeLinecap="square" />
            </svg>
            <div style={{ ...brutalCard, transform: `scale(${sp(320, { damping: 9 })})` }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 140, color: '#FFC107', lineHeight: 1 }}>-125</div>
              <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 800, fontSize: 52, color: '#D9FFF4' }}>BASIS POINTS CUT</div>
            </div>
          </div>
        </AbsoluteFill>

        {/* BEAT C — cut EMI tak pahucha? broken pipe visual */}
        <AbsoluteFill style={{ opacity: beatC, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ fontFamily: 'Yantra One', fontSize: 100, color: '#F7F4ED', textAlign: 'center', transform: `scale(${sp(528)})`, textShadow: '10px 10px 0 rgba(5,24,9,0.9)' }}>
            क्या CUT आपकी <span style={{ color: '#FFC107' }}>EMI</span> तक पहुँचा?
          </div>
          {/* broken pipeline: RBI -> X -> EMI */}
          <svg width="1200" height="200" viewBox="0 0 1200 200" style={{ marginTop: 40, opacity: sp(560) }}>
            <rect x="20" y="70" width="240" height="70" fill="#123B17" stroke="#F7F4ED" strokeWidth="6" />
            <text x="70" y="118" fontFamily="Inter" fontWeight="900" fontSize="44" fill="#D9FFF4">RBI</text>
            <line x1="260" y1="105" x2="520" y2="105" stroke="#FFC107" strokeWidth="10" strokeDasharray="30 18" strokeDashoffset={-frame * 2.5} />
            {/* break */}
            <path d="M560 40 L595 105 L560 170" stroke="#ff0000" strokeWidth="12" fill="none" transform={`rotate(${Math.sin(frame * 0.3) * 3} 575 105)`} />
            <path d="M640 40 L605 105 L640 170" stroke="#ff0000" strokeWidth="12" fill="none" />
            <line x1="680" y1="105" x2="930" y2="105" stroke="#9E9E9E" strokeWidth="10" strokeDasharray="14 22" opacity="0.5" />
            <rect x="940" y="70" width="240" height="70" fill="#123B17" stroke="#F7F4ED" strokeWidth="6" />
            <text x="975" y="118" fontFamily="Inter" fontWeight="900" fontSize="44" fill="#D9FFF4">आपकी EMI</text>
          </svg>
          <div style={{
            marginTop: 40, border: '8px solid #ff0000', padding: '18px 60px', background: 'rgba(14,51,19,0.95)',
            transform: `rotate(-5deg) scale(${interpolate(sp(620, { damping: 8 }), [0, 1], [2.2, 1])})`, boxShadow: '16px 16px 0 rgba(5,24,9,0.95)',
          }}>
            <span style={{ fontFamily: 'Yantra One', fontSize: 110, color: '#ff0000' }}>जवाब: नहीं</span>
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(4,18,7,0.75) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C1S04_95Percent;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C1S05_TenureStretch',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C1S05_TenureStretch: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const ph = (a: number, b: number) =>
    interpolate(frame, [a, a + 14, b - 14, b], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const beatA = ph(0, 210);   // unkaha trap
  const beatB = ph(210, 560); // stretching timeline 20 -> 23 -> 25
  const beatC = ph(560, durationInFrames); // aapne notice nahi kiya

  // Camera: down-tilt pan
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1.12], { easing: Easing.inOut(Easing.ease) });
  const camX = Math.sin(frame * 0.008) * 14;

  // Timeline stretch: 20y bar grows to 25y between beats
  const stretch = interpolate(frame, [300, 420, 440, 530], [0, 0.6, 0.6, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const yearsNow = (20 + stretch * 5).toFixed(1);

  const brutalCard: React.CSSProperties = {
    background: '#123B17', border: '6px solid #F7F4ED',
    boxShadow: '18px 18px 0 rgba(5,24,9,0.95)', padding: '24px 48px',
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#1B5E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 80% ${20 + Math.cos(frame * 0.007) * 10}%, #1B5E20 0%, #113A17 48%, #07220A 100%)`,
      }} />
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, rgba(4,17,7,0.55) 0%, transparent 45%, rgba(4,17,7,0.7) 100%)' }} />

      {/* Layer 2: Animated shape layers */}
      {[0, 1, 2].map((i) => (
        <div key={`shape-${i}`} style={{
          position: 'absolute', width: 640 + i * 120, height: 120 + i * 60,
          left: `${-8 + i * 26}%`, top: `${18 + i * 26}%`,
          border: '3px solid rgba(217,255,244,0.05)',
          background: i === 1 ? 'rgba(255,193,7,0.03)' : 'transparent',
          transform: `translateX(${Math.sin(frame * 0.006 + i * 2.4) * 46}px) rotate(${-4 + i * 3}deg) scaleX(${1 + Math.sin(frame * 0.01 + i) * 0.05})`,
        }} />
      ))}

      {/* Layer 3: Motion texture — horizontal speed lines */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(0deg, rgba(247,244,237,0.05) 0 2px, transparent 2px 64px)',
        transform: `translateY(${(frame * 0.45) % 64}px)`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => (
        <div key={`p-${i}`} style={{
          position: 'absolute',
          left: (seededRandom(i + 31) * 1920 + frame * (0.4 + seededRandom(i) * 0.6)) % 1920,
          top: (seededRandom(i + 100) * 1080 + Math.sin(frame * 0.02 + i) * 18) % 1080,
          width: 4, height: 4, background: i % 4 === 0 ? '#FFC107' : '#D9FFF4',
          opacity: 0.1 + seededRandom(i + 19) * 0.16, transform: 'rotate(45deg)',
        }} />
      ))}

      {/* Layer 5: Content — camera rig */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX}px)` }}>

        {/* BEAT A — THE UNSAID TRAP typographic slam */}
        <AbsoluteFill style={{ opacity: beatA, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ fontFamily: 'Yantra One', fontSize: 175, lineHeight: 0.98, color: '#F7F4ED', textAlign: 'center', textShadow: '14px 14px 0 rgba(5,24,9,0.9)', transform: `scale(${sp(6, { damping: 9 })}) rotate(-1.5deg)` }}>
            यही है<br />
            <span style={{ color: '#ff0000' }}>अनकहा TRAP</span>
          </div>
          <div style={{ marginTop: 40, ...brutalCard, background: '#FFC107', transform: `rotate(2deg) translateY(${interpolate(sp(70), [0, 1], [60, 0])}px)`, opacity: sp(70) }}>
            <span style={{ fontFamily: 'Noto Sans Devanagari', fontWeight: 800, fontSize: 48, color: '#0E3313' }}>
              EMI घटाई नहीं — TENURE चुपचाप बढ़ा दी
            </span>
          </div>
        </AbsoluteFill>

        {/* BEAT B — rubber-band timeline stretching 20 -> 25 years */}
        <AbsoluteFill style={{ opacity: beatB, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 76, color: '#D9FFF4', marginBottom: 60, transform: `scale(${sp(218)})` }}>
            LOAN TENURE — <span style={{ fontFamily: 'JetBrains Mono', color: '#FFC107' }}>{yearsNow} YRS</span>
          </div>
          <div style={{ width: 1400, position: 'relative' }}>
            {/* base 20y bar */}
            <div style={{ height: 88, width: 1000, background: '#123B17', border: '6px solid #F7F4ED', boxShadow: '14px 14px 0 rgba(5,24,9,0.95)', position: 'relative', overflow: 'visible' }}>
              <span style={{ position: 'absolute', left: 30, top: 16, fontFamily: 'JetBrains Mono', fontSize: 42, color: '#D9FFF4' }}>20 SAAL</span>
              {/* stretching red extension */}
              <div style={{
                position: 'absolute', left: '100%', top: -6, height: 88, width: 400 * stretch,
                background: 'repeating-linear-gradient(-45deg, #ff0000 0 22px, #8f0000 22px 44px)',
                border: '6px solid #F7F4ED', borderLeft: 'none',
              }} />
            </div>
            {/* year ticks */}
            {[20, 23, 25].map((y, i) => (
              <div key={y} style={{ position: 'absolute', left: 1000 + (i * 200) - 10, top: 110, opacity: stretch > i * 0.45 ? 1 : 0.18, transition: 'none' }}>
                <div style={{ width: 6, height: 34, background: '#FFC107', margin: '0 auto' }} />
                <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 52, color: i === 0 ? '#D9FFF4' : '#ff0000' }}>{y}y</div>
              </div>
            ))}
          </div>
          <div style={{ marginTop: 120, fontFamily: 'Noto Sans Devanagari', fontSize: 52, color: '#F7F4ED', opacity: sp(430), transform: `translateY(${interpolate(sp(430), [0, 1], [40, 0])}px)` }}>
            20 साल का loan → <span style={{ color: '#ff0000', fontWeight: 800 }}>23... फिर 25 साल</span>
          </div>
        </AbsoluteFill>

        {/* BEAT C — "aapne notice bhi nahi kiya" — sleeping eye icon */}
        <AbsoluteFill style={{ opacity: beatC, justifyContent: 'center', alignItems: 'center', flexDirection: 'row', gap: 90 }}>
          {/* closed eye icon */}
          <svg width="320" height="200" viewBox="0 0 320 200" style={{ transform: `scale(${sp(568)})` }}>
            <path d="M20 90 Q160 190 300 90" fill="none" stroke="#D9FFF4" strokeWidth="14" strokeLinecap="square" />
            {[0, 1, 2, 3, 4].map((l) => (
              <line key={l} x1={60 + l * 50} y1={125 + Math.abs(l - 2) * -8 + 20} x2={52 + l * 50} y2={168 + Math.abs(l - 2) * -8} stroke="#D9FFF4" strokeWidth="10" />
            ))}
          </svg>
          <div style={{ ...brutalCard, transform: `rotate(-2deg) scale(${sp(590, { damping: 9 })})` }}>
            <div style={{ fontFamily: 'Yantra One', fontSize: 105, color: '#F7F4ED', lineHeight: 1.05 }}>
              आपने <span style={{ color: '#FFC107' }}>NOTICE</span><br />तक नहीं किया
            </div>
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(4,18,7,0.75) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C1S05_TenureStretch;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C1S06_ExtraYearsCost',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C1S06_ExtraYearsCost: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const ph = (a: number, b: number) =>
    interpolate(frame, [a, a + 14, b - 14, b], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const beatA = ph(0, 240);   // 30 saal + EMI same illusion
  const beatB = ph(240, 540); // extra 3-5 saal = lakhon interest, no refund
  const beatC = ph(540, durationInFrames); // 50L case study setup card

  // Camera: push in with micro breathing
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.1], { easing: Easing.inOut(Easing.ease) }) + Math.sin(frame * 0.03) * 0.003;
  const shakeT = frame - 330;
  const shake = shakeT > 0 && shakeT < 14 ? Math.sin(shakeT * 2.8) * (14 - shakeT) * 1.3 : 0;

  const brutalCard: React.CSSProperties = {
    background: '#123B17', border: '6px solid #F7F4ED',
    boxShadow: '18px 18px 0 rgba(5,24,9,0.95)', padding: '24px 48px',
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#1B5E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${55 + Math.sin(frame * 0.006) * 12}% 85%, #1C6023 0%, #113A17 46%, #062008 100%)`,
      }} />
      <AbsoluteFill style={{ background: 'linear-gradient(340deg, rgba(4,17,7,0.6) 0%, transparent 50%, rgba(4,17,7,0.55) 100%)' }} />

      {/* Layer 2: Animated shape layers */}
      {[0, 1, 2].map((i) => (
        <div key={`shape-${i}`} style={{
          position: 'absolute', width: 460 + i * 160, height: 460 + i * 160, borderRadius: i === 0 ? '50%' : 0,
          left: `${64 - i * 30}%`, top: `${-16 + i * 30}%`,
          border: '3px solid rgba(217,255,244,0.055)',
          transform: `translate(${Math.cos(frame * 0.0055 + i) * 34}px, ${Math.sin(frame * 0.007 + i * 2) * 30}px) rotate(${frame * 0.035 * (i % 2 ? -1 : 1)}deg)`,
        }} />
      ))}

      {/* Layer 3: Motion texture — cross/plus pattern */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(rgba(255,193,7,0.05) 3px, transparent 3px), radial-gradient(rgba(247,244,237,0.04) 2px, transparent 2px)',
        backgroundSize: '110px 110px, 70px 70px',
        transform: `translate(${(frame * 0.25) % 110}px, ${(-frame * 0.2) % 70}px)`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={`p-${i}`} style={{
          position: 'absolute',
          left: (seededRandom(i + 41) * 1920 + Math.sin(frame * 0.018 + i * 2) * 28) % 1920,
          top: (seededRandom(i + 120) * 1080 + frame * (0.26 + seededRandom(i) * 0.4)) % 1080,
          width: 5, height: 5, borderRadius: i % 2 ? '50%' : 0,
          background: i % 3 === 0 ? '#FFC107' : '#D9FFF4', opacity: 0.1 + seededRandom(i + 23) * 0.14,
        }} />
      ))}

      {/* Layer 5: Content — camera rig */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${shake}px, ${-shake * 0.5}px)` }}>

        {/* BEAT A — 30 SAAL shock + EMI same illusion */}
        <AbsoluteFill style={{ opacity: beatA, justifyContent: 'center', alignItems: 'center', flexDirection: 'row', gap: 100 }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 280, lineHeight: 1, color: '#ff0000', textShadow: '14px 14px 0 rgba(5,24,9,0.92)', transform: `scale(${sp(8, { damping: 8 })}) rotate(-2deg)` }}>
              30
            </div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 82, color: '#F7F4ED', opacity: sp(40) }}>साल तक पहुँचे LOANS</div>
          </div>
          <div style={{ ...brutalCard, transform: `rotate(2deg) scale(${sp(70)})` }}>
            <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 56, color: '#D9FFF4' }}>EMI SAME:</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 96, color: '#FFC107' }}>₹40-45K</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 42, color: '#9E9E9E', marginTop: 10 }}>
              "फर्क ही नहीं दिखा" — यही ILLUSION है
            </div>
          </div>
        </AbsoluteFill>

        {/* BEAT B — extra years = burning money, no refund */}
        <AbsoluteFill style={{ opacity: beatB, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ display: 'flex', alignItems: 'flex-end', gap: 26, height: 380 }}>
            {/* 5 extra-year money stacks rising */}
            {[0, 1, 2, 3, 4].map((i) => {
              const h = interpolate(sp(260 + i * 16), [0, 1], [0, 140 + i * 55]);
              return (
                <div key={i} style={{ width: 130, height: h, background: i < 3 ? '#FFC107' : '#ff0000', border: '6px solid #F7F4ED', boxShadow: '10px 10px 0 rgba(5,24,9,0.9)', position: 'relative' }}>
                  <span style={{ position: 'absolute', top: -56, left: 0, right: 0, textAlign: 'center', fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 38, color: '#F7F4ED' }}>
                    +{i + 1}y
                  </span>
                </div>
              );
            })}
          </div>
          <div style={{ marginTop: 55, fontFamily: 'Yantra One', fontSize: 92, color: '#F7F4ED', textShadow: '10px 10px 0 rgba(5,24,9,0.9)', transform: `scale(${sp(345, { damping: 9 })})` }}>
            EXTRA 3-5 साल = <span style={{ color: '#ff0000' }}>लाखों का INTEREST</span>
          </div>
          <div style={{ marginTop: 30, border: '5px solid #9E9E9E', padding: '14px 40px', transform: `rotate(-2deg)`, opacity: sp(400) }}>
            <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 46, color: '#D9FFF4' }}>
              NO REFUND — rates गिरें तो भी वापस नहीं
            </span>
          </div>
        </AbsoluteFill>

        {/* BEAT C — ₹50 LAKH case file opens */}
        <AbsoluteFill style={{ opacity: beatC, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ ...brutalCard, background: '#F7F4ED', padding: '40px 80px', transform: `rotate(-2deg) scale(${sp(548, { damping: 9 })})` }}>
            <div style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 54, color: '#9E9E9E', letterSpacing: 6 }}>CASE FILE #01</div>
            <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 150, color: '#0E3313', lineHeight: 1.05 }}>₹50 लाख</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 54, color: '#1B5E20' }}>HOME LOAN — मान लीजिए...</div>
          </div>
          {/* collage tape corners */}
          <div style={{ position: 'absolute', top: '26%', left: '30%', width: 200, height: 40, background: '#FFC107', opacity: 0.9, transform: `rotate(-35deg) scaleX(${sp(580)})` }} />
          <div style={{ position: 'absolute', bottom: '26%', right: '30%', width: 200, height: 40, background: '#FFC107', opacity: 0.9, transform: `rotate(-35deg) scaleX(${sp(595)})` }} />
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(4,18,7,0.75) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C1S06_ExtraYearsCost;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C1S07_SeesawGame',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C1S07_SeesawGame: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const ph = (a: number, b: number) =>
    interpolate(frame, [a, a + 14, b - 14, b], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const beatA = ph(0, 230);   // loan spec ticket
  const beatB = ph(230, 530); // seesaw: rates up -> tenure 25
  const beatC = ph(530, durationInFrames); // stuck at 22-23

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.11], { easing: Easing.inOut(Easing.ease) });
  const camY = interpolate(frame, [0, durationInFrames], [12, -12]);

  // Seesaw tilt: rates up (left down) then partial recovery
  const tilt = interpolate(frame, [270, 340, 420, 500], [0, -9, -9, -4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  const brutalCard: React.CSSProperties = {
    background: '#123B17', border: '6px solid #F7F4ED',
    boxShadow: '18px 18px 0 rgba(5,24,9,0.95)', padding: '24px 48px',
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#1B5E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${28 + Math.cos(frame * 0.008) * 8}% 24%, #1B5E20 0%, #123C18 44%, #06;1F08 100%)`.replace('#06;1F08', '#061F08'),
      }} />
      <AbsoluteFill style={{ background: 'linear-gradient(25deg, rgba(4,17,7,0.65) 0%, transparent 42%, rgba(4,17,7,0.5) 100%)' }} />

      {/* Layer 2: Animated shape layers */}
      {[0, 1, 2].map((i) => (
        <div key={`shape-${i}`} style={{
          position: 'absolute', width: 0, height: 0,
          left: `${10 + i * 34}%`, top: `${16 + i * 24}%`,
          borderLeft: `${130 + i * 60}px solid transparent`,
          borderRight: `${130 + i * 60}px solid transparent`,
          borderBottom: `${220 + i * 100}px solid rgba(217,255,244,${0.035 - i * 0.007})`,
          transform: `translate(${Math.sin(frame * 0.005 + i * 2) * 40}px, ${Math.cos(frame * 0.0062 + i) * 26}px) rotate(${frame * 0.045 * (i % 2 ? 1 : -1)}deg)`,
        }} />
      ))}

      {/* Layer 3: Motion texture — reverse diagonal stripes */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(45deg, rgba(247,244,237,0.045) 0 2px, transparent 2px 52px)',
        transform: `translateX(${(-frame * 0.5) % 52}px)`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={`p-${i}`} style={{
          position: 'absolute',
          left: (seededRandom(i + 51) * 1920 + Math.cos(frame * 0.022 + i) * 30) % 1920,
          top: (seededRandom(i + 140) * 1080 + frame * (0.24 + seededRandom(i) * 0.42)) % 1080,
          width: 4, height: 4, background: i % 4 === 0 ? '#FFC107' : '#D9FFF4',
          opacity: 0.1 + seededRandom(i + 29) * 0.15, transform: 'rotate(45deg)',
        }} />
      ))}

      {/* Layer 5: Content — camera rig */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY}px)` }}>

        {/* BEAT A — loan spec ticket (mono receipt style) */}
        <AbsoluteFill style={{ opacity: beatA, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ ...brutalCard, background: '#F7F4ED', padding: '44px 70px', transform: `rotate(-1.5deg) translateY(${interpolate(sp(10), [0, 1], [500, 0])}px)` }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 40, color: '#9E9E9E', borderBottom: '4px dashed #1B5E20', paddingBottom: 14 }}>LOAN RECEIPT — READ CAREFULLY</div>
            {[
              ['AMOUNT', '₹50,00,000'],
              ['RATE', '8.4 - 8.5%'],
              ['TENURE', '20 YEARS'],
              ['EMI', '₹43,200 / माह'],
            ].map((row, i) => (
              <div key={row[0]} style={{ display: 'flex', justifyContent: 'space-between', gap: 120, marginTop: 26, opacity: sp(50 + i * 20), transform: `translateX(${interpolate(sp(50 + i * 20), [0, 1], [60, 0])}px)` }}>
                <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 800, fontSize: 54, color: '#1B5E20' }}>{row[0]}</span>
                <span style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 60, color: '#0E3313' }}>{row[1]}</span>
              </div>
            ))}
          </div>
        </AbsoluteFill>

        {/* BEAT B — SEESAW: RATE up vs TENURE — bank's move */}
        <AbsoluteFill style={{ opacity: beatB, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ fontFamily: 'Yantra One', fontSize: 88, color: '#F7F4ED', marginBottom: 70, transform: `scale(${sp(238)})`, textShadow: '10px 10px 0 rgba(5,24,9,0.9)' }}>
            BANK का <span style={{ color: '#FFC107' }}>SEESAW GAME</span>
          </div>
          {/* seesaw */}
          <div style={{ position: 'relative', width: 1300, height: 360 }}>
            <div style={{
              position: 'absolute', left: 50, right: 50, top: 130, height: 30,
              background: '#F7F4ED', boxShadow: '10px 10px 0 rgba(5,24,9,0.9)',
              transform: `rotate(${tilt}deg)`, transformOrigin: 'center',
            }}>
              {/* left pan: RATES */}
              <div style={{ position: 'absolute', left: -20, top: -150, width: 320, textAlign: 'center' }}>
                <div style={{ ...brutalCard, padding: '14px 24px', background: '#ff0000' }}>
                  <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 52, color: '#F7F4ED' }}>RATES ↑</span>
                </div>
              </div>
              {/* right pan: TENURE */}
              <div style={{ position: 'absolute', right: -20, top: -150, width: 380, textAlign: 'center' }}>
                <div style={{ ...brutalCard, padding: '14px 24px', background: '#FFC107' }}>
                  <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 52, color: '#0E3313' }}>TENURE → 25y</span>
                </div>
              </div>
            </div>
            {/* fulcrum */}
            <div style={{ position: 'absolute', left: '50%', top: 160, transform: 'translateX(-50%)', width: 0, height: 0, borderLeft: '70px solid transparent', borderRight: '70px solid transparent', borderBottom: '130px solid #123B17', filter: 'drop-shadow(8px 8px 0 rgba(5,24,9,0.9))' }} />
          </div>
          <div style={{ marginTop: 30, fontFamily: 'Noto Sans Devanagari', fontSize: 50, color: '#D9FFF4', opacity: sp(380) }}>
            EMI नहीं बढ़ाई — <span style={{ color: '#ff0000', fontWeight: 800 }}>TENURE बढ़ा दी</span>
          </div>
        </AbsoluteFill>

        {/* BEAT C — stuck at 22-23, never back to 20 */}
        <AbsoluteFill style={{ opacity: beatC, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 60 }}>
            {[
              { y: '20y', c: '#9E9E9E', label: 'ORIGINAL', dead: true },
              { y: '25y', c: '#ff0000', label: 'RATES बढ़ीं' },
              { y: '22-23y', c: '#FFC107', label: 'अभी भी अटके' },
            ].map((s, i) => (
              <React.Fragment key={s.y}>
                <div style={{ ...brutalCard, textAlign: 'center', transform: `rotate(${(i - 1) * 2.5}deg) scale(${sp(540 + i * 22, { damping: 9 })})`, opacity: s.dead ? 0.55 : 1 }}>
                  <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 110, color: s.c, textDecoration: s.dead ? 'line-through' : 'none', textDecorationColor: '#ff0000', textDecorationThickness: 10 }}>{s.y}</div>
                  <div style={{ fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 38, color: '#D9FFF4' }}>{s.label}</div>
                </div>
                {i < 2 && (
                  <svg width="90" height="70" viewBox="0 0 90 70" style={{ opacity: sp(560 + i * 22) }}>
                    <path d="M5 35 H60 M60 35 L38 13 M60 35 L38 57" stroke="#D9FFF4" strokeWidth="10" fill="none" transform={`translateX(${Math.sin(frame * 0.15) * 5}px)`.replace('translateX(', 'translate(').replace('px)', ',0)')} />
                  </svg>
                )}
              </React.Fragment>
            ))}
          </div>
          <div style={{ marginTop: 60, fontFamily: 'Yantra One', fontSize: 84, color: '#F7F4ED', transform: `translateY(${interpolate(sp(640), [0, 1], [50, 0])}px)`, opacity: sp(640), textShadow: '10px 10px 0 rgba(5,24,9,0.9)' }}>
            ORIGINAL 20 पर <span style={{ color: '#ff0000' }}>कभी वापस नहीं</span>
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(4,18,7,0.75) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C1S07_SeesawGame;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C1S08_OnePercentGap',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C1S08_OnePercentGap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const ph = (a: number, b: number) =>
    interpolate(frame, [a, a + 14, b - 14, b], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const beatA = ph(0, 320);   // tower comparison 94.85L vs 1.02Cr
  const beatB = ph(320, 550); // 1% = 7.4L stamp
  const beatC = ph(550, durationInFrames); // bank bonus income disturb

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.03, 1.1], { easing: Easing.inOut(Easing.ease) });
  const shakeT = frame - 338;
  const shake = shakeT > 0 && shakeT < 16 ? Math.sin(shakeT * 2.5) * (16 - shakeT) * 1.5 : 0;

  // Tower growth
  const towerA = interpolate(frame, [40, 150], [0, 380], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const towerB = interpolate(frame, [90, 220], [0, 520], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const brutalCard: React.CSSProperties = {
    background: '#123B17', border: '6px solid #F7F4ED',
    boxShadow: '18px 18px 0 rgba(5,24,9,0.95)', padding: '24px 48px',
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#1B5E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 50% ${30 + Math.sin(frame * 0.0065) * 8}%, #1B5E20 0%, #113A17 46%, #051D07 100%)`,
      }} />
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, rgba(4,17,7,0.5) 0%, transparent 30%, rgba(4,17,7,0.7) 100%)' }} />

      {/* Layer 2: Animated shape layers */}
      {[0, 1, 2].map((i) => (
        <div key={`shape-${i}`} style={{
          position: 'absolute', width: 200 + i * 90, height: 620 + i * 100,
          left: `${6 + i * 38}%`, top: `${-10 + i * 8}%`,
          border: '3px solid rgba(217,255,244,0.05)',
          background: i === 1 ? 'rgba(255,193,7,0.025)' : 'transparent',
          transform: `translateY(${Math.sin(frame * 0.006 + i * 2) * 34}px) rotate(${(i - 1) * 5 + Math.sin(frame * 0.01) * 1.5}deg)`,
        }} />
      ))}

      {/* Layer 3: Motion texture — vertical bars */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(90deg, rgba(247,244,237,0.045) 0 3px, transparent 3px 110px)',
        transform: `translateX(${(frame * 0.3) % 110}px)`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={`p-${i}`} style={{
          position: 'absolute',
          left: (seededRandom(i + 61) * 1920 + Math.sin(frame * 0.02 + i) * 26) % 1920,
          top: (seededRandom(i + 160) * 1080 - frame * (0.2 + seededRandom(i) * 0.4) + 2160) % 1080,
          width: 4, height: 4, background: i % 3 === 0 ? '#FFC107' : '#D9FFF4',
          opacity: 0.1 + seededRandom(i + 31) * 0.15,
        }} />
      ))}

      {/* Layer 5: Content — camera rig */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${shake}px, ${shake * 0.4}px)` }}>

        {/* BEAT A — repayment towers duel */}
        <AbsoluteFill style={{ opacity: beatA, justifyContent: 'flex-end', alignItems: 'center', paddingBottom: 150 }}>
          <div style={{ position: 'absolute', top: 90, fontFamily: 'Yantra One', fontSize: 84, color: '#F7F4ED', textShadow: '10px 10px 0 rgba(5,24,9,0.9)', transform: `scale(${sp(6)})` }}>
            SAME LOAN — <span style={{ color: '#FFC107' }}>अलग RATE</span>
          </div>
          <div style={{ display: 'flex', alignItems: 'flex-end', gap: 170 }}>
            <div style={{ textAlign: 'center' }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 70, color: '#D9FFF4', marginBottom: 16 }}>₹94.85L</div>
              <div style={{ width: 300, height: towerA, background: '#123B17', border: '7px solid #F7F4ED', boxShadow: '14px 14px 0 rgba(5,24,9,0.95)' }} />
              <div style={{ marginTop: 18, fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 56, color: '#D9FFF4' }}>7.25%</div>
            </div>
            <div style={{ textAlign: 'center' }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 70, color: '#ff0000', marginBottom: 16 }}>₹1.02 Cr</div>
              <div style={{ width: 300, height: towerB, background: 'repeating-linear-gradient(-45deg, #7d1010 0 26px, #123B17 26px 52px)', border: '7px solid #F7F4ED', boxShadow: '14px 14px 0 rgba(5,24,9,0.95)' }} />
              <div style={{ marginTop: 18, fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 56, color: '#ff0000' }}>8.25%</div>
            </div>
          </div>
        </AbsoluteFill>

        {/* BEAT B — 1% = ₹7.4 LAKH slam */}
        <AbsoluteFill style={{ opacity: beatB, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 60 }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 260, color: '#FFC107', textShadow: '16px 16px 0 rgba(5,24,9,0.92)', transform: `scale(${interpolate(sp(328, { damping: 8 }), [0, 1], [2.6, 1])}) rotate(-3deg)` }}>
              1%
            </div>
            <svg width="140" height="120" viewBox="0 0 140 120" style={{ opacity: sp(360) }}>
              <path d="M10 60 H100 M100 60 L70 30 M100 60 L70 90" stroke="#F7F4ED" strokeWidth="14" fill="none" transform={`translate(${Math.sin(frame * 0.14) * 8} 0)`} />
            </svg>
            <div style={{ ...brutalCard, background: '#ff0000', transform: `rotate(2deg) scale(${sp(375, { damping: 9 })})` }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 130, color: '#F7F4ED', lineHeight: 1 }}>₹7.4 लाख</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontWeight: 700, fontSize: 46, color: '#F7F4ED', opacity: 0.9 }}>का फर्क — SAME LOAN पर</div>
            </div>
          </div>
        </AbsoluteFill>

        {/* BEAT C — BONUS INCOME rubber stamp + disturb line */}
        <AbsoluteFill style={{ opacity: beatC, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 62, color: '#D9FFF4', transform: `translateY(${interpolate(sp(558), [0, 1], [40, 0])}px)`, opacity: sp(558) }}>
            Extra tenure का हिसाब bank नहीं करेगा, क्योंकि...
          </div>
          <div style={{
            marginTop: 50, border: '10px double #FFC107', padding: '26px 70px',
            transform: `rotate(-7deg) scale(${interpolate(sp(600, { damping: 8 }), [0, 1], [2.4, 1])})`,
            background: 'rgba(14,51,19,0.95)', boxShadow: '18px 18px 0 rgba(5,24,9,0.95)',
          }}>
            <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 118, color: '#FFC107', letterSpacing: 4 }}>BONUS INCOME</span>
          </div>
          <div style={{ marginTop: 55, fontFamily: 'Yantra One', fontSize: 72, color: '#F7F4ED', opacity: sp(660), textShadow: '8px 8px 0 rgba(5,24,9,0.9)' }}>
            यही बात <span style={{ color: '#ff0000' }}>DISTURB</span> करती है
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(4,18,7,0.75) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C1S08_OnePercentGap;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C1S09_InterestShare',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C1S09_InterestShare: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const ph = (a: number, b: number) =>
    interpolate(frame, [a, a + 14, b - 14, b], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const beatA = ph(0, 280);   // 10 saal EMI, principal sirf 15-20%
  const beatB = ph(280, durationInFrames); // 92% vs 75% split bars

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.08, 1.0], { easing: Easing.inOut(Easing.ease) });
  const camRot = Math.sin(frame * 0.006) * 0.5;

  // Donut progress: only 18% principal cleared
  const donutPct = interpolate(frame, [50, 180], [0, 18], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const R = 150;
  const circ = 2 * Math.PI * R;

  // Beat B bars
  const bar92 = interpolate(frame, [320, 420], [0, 92], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const bar75 = interpolate(frame, [420, 520], [0, 75], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const brutalCard: React.CSSProperties = {
    background: '#123B17', border: '6px solid #F7F4ED',
    boxShadow: '18px 18px 0 rgba(5,24,9,0.95)', padding: '24px 48px',
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#1B5E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${72 - Math.sin(frame * 0.007) * 10}% 70%, #1C6023 0%, #113A17 48%, #051C07 100%)`,
      }} />
      <AbsoluteFill style={{ background: 'linear-gradient(300deg, rgba(4,17,7,0.62) 0%, transparent 48%, rgba(4,17,7,0.55) 100%)' }} />

      {/* Layer 2: Animated shape layers */}
      {[0, 1, 2].map((i) => (
        <div key={`shape-${i}`} style={{
          position: 'absolute', width: 360 + i * 220, height: 360 + i * 220, borderRadius: '50%',
          left: `${-12 + i * 8}%`, top: `${30 - i * 16}%`,
          border: `${4 - i}px solid rgba(217,255,244,${0.06 - i * 0.015})`,
          transform: `translate(${Math.sin(frame * 0.0058 + i * 1.8) * 36}px, ${Math.cos(frame * 0.0066 + i) * 28}px) scale(${1 + Math.sin(frame * 0.011 + i * 2) * 0.045})`,
        }} />
      ))}

      {/* Layer 3: Motion texture — concentric ring dots */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(rgba(247,244,237,0.06) 2px, transparent 2px)',
        backgroundSize: '46px 46px',
        transform: `rotate(${frame * 0.02}deg) scale(1.2)`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => (
        <div key={`p-${i}`} style={{
          position: 'absolute',
          left: (seededRandom(i + 71) * 1920 + Math.cos(frame * 0.024 + i * 1.4) * 30) % 1920,
          top: (seededRandom(i + 180) * 1080 + frame * (0.2 + seededRandom(i) * 0.5)) % 1080,
          width: 4 + seededRandom(i + 2) * 3, height: 4 + seededRandom(i + 2) * 3,
          borderRadius: '50%', background: i % 4 === 0 ? '#FFC107' : '#D9FFF4',
          opacity: 0.1 + seededRandom(i + 37) * 0.14,
        }} />
      ))}

      {/* Layer 5: Content — camera rig */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>

        {/* BEAT A — donut: 10 yrs paid, principal only ~15-20% */}
        <AbsoluteFill style={{ opacity: beatA, justifyContent: 'center', alignItems: 'center', flexDirection: 'row', gap: 120 }}>
          <div style={{ position: 'relative', width: 400, height: 400, transform: `scale(${sp(12)})` }}>
            <svg width="400" height="400" viewBox="0 0 400 400">
              <circle cx="200" cy="200" r={R} fill="none" stroke="rgba(247,244,237,0.18)" strokeWidth="52" />
              <circle
                cx="200" cy="200" r={R} fill="none" stroke="#FFC107" strokeWidth="52"
                strokeDasharray={`${(donutPct / 100) * circ} ${circ}`}
                transform="rotate(-90 200 200)" strokeLinecap="butt"
              />
              <circle cx="200" cy="200" r={R - 60} fill="#0E3313" stroke="#F7F4ED" strokeWidth="6" />
            </svg>
            <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center' }}>
              <span style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 92, color: '#FFC107' }}>{Math.round(donutPct)}%</span>
              <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 700, fontSize: 30, color: '#D9FFF4' }}>PRINCIPAL CLEARED</span>
            </div>
          </div>
          <div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 110, color: '#F7F4ED', lineHeight: 1.05, textShadow: '10px 10px 0 rgba(5,24,9,0.9)', transform: `translateX(${interpolate(sp(40), [0, 1], [120, 0])}px)`, opacity: sp(40) }}>
              10 साल EMI<br />भरने के बाद
            </div>
            <div style={{ marginTop: 26, ...brutalCard, background: '#ff0000', display: 'inline-block', transform: `rotate(-2deg) scale(${sp(90)})` }}>
              <span style={{ fontFamily: 'Noto Sans Devanagari', fontWeight: 800, fontSize: 50, color: '#F7F4ED' }}>
                principal सिर्फ 15-20% घटा
              </span>
            </div>
          </div>
        </AbsoluteFill>

        {/* BEAT B — EMI split bars: Year 1 = 92% interest, Year 8 = 75% */}
        <AbsoluteFill style={{ opacity: beatB, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ ...brutalCard, background: '#F7F4ED', marginBottom: 64, transform: `rotate(-1.5deg) scale(${sp(288)})` }}>
            <span style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 58, color: '#0E3313' }}>₹60L | 20Y | 8.75% — EMI का X-RAY</span>
          </div>
          {[{ yr: 'YEAR 1', pct: bar92, target: 92 }, { yr: 'YEAR 8', pct: bar75, target: 75 }].map((row, i) => (
            <div key={row.yr} style={{ display: 'flex', alignItems: 'center', gap: 40, marginTop: i ? 55 : 0, opacity: sp(300 + i * 100), transform: `translateX(${interpolate(sp(300 + i * 100), [0, 1], [-100, 0])}px)` }}>
              <div style={{ width: 240, fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 58, color: '#D9FFF4' }}>{row.yr}</div>
              <div style={{ width: 1050, height: 96, border: '7px solid #F7F4ED', background: '#0E3313', position: 'relative', boxShadow: '12px 12px 0 rgba(5,24,9,0.95)' }}>
                <div style={{ width: `${row.pct}%`, height: '100%', background: 'repeating-linear-gradient(-45deg, #ff0000 0 24px, #a00000 24px 48px)' }} />
                <span style={{ position: 'absolute', left: 24, top: 18, fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 46, color: '#F7F4ED' }}>
                  INTEREST {Math.round(row.pct)}%
                </span>
                <span style={{ position: 'absolute', right: 20, top: 22, fontFamily: 'Inter, Poppins', fontWeight: 700, fontSize: 34, color: '#D9FFF4', opacity: row.pct > row.target - 4 ? 1 : 0 }}>
                  PRINCIPAL {100 - Math.round(row.pct)}%
                </span>
              </div>
            </div>
          ))}
          <div style={{ marginTop: 60, fontFamily: 'Noto Sans Devanagari', fontSize: 52, color: '#FFC107', fontWeight: 700, opacity: sp(560), transform: `scale(${sp(560)})` }}>
            आपकी EMI का बड़ा हिस्सा = BANK का INTEREST
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(4,18,7,0.75) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C1S09_InterestShare;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C1S10_AmortizationTruth',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C1S10_AmortizationTruth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const ph = (a: number, b: number) =>
    interpolate(frame, [a, a + 14, b - 14, b], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const beatA = ph(0, 250);   // 3/4 to bank pie split
  const beatB = ph(250, 500); // 50L paid vs 8-10L principal drop
  const beatC = ph(500, durationInFrames); // amortization curve truth

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.12], { easing: Easing.inOut(Easing.ease) });
  const camX = interpolate(frame, [0, durationInFrames], [-18, 18]);

  // Amortization curve draw-on
  const curveDraw = interpolate(frame, [530, 680], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  const brutalCard: React.CSSProperties = {
    background: '#123B17', border: '6px solid #F7F4ED',
    boxShadow: '18px 18px 0 rgba(5,24,9,0.95)', padding: '24px 48px',
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#1B5E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Layer 1: Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${40 + Math.cos(frame * 0.006) * 10}% 50%, #1B5E20 0%, #10381六 0%, #113A17 46%, #041905 100%)`.replace('#10381六 0%,', ''),
      }} />
      <AbsoluteFill style={{ background: 'linear-gradient(220deg, rgba(4,17,7,0.6) 0%, transparent 46%, rgba(4,17,7,0.68) 100%)' }} />

      {/* Layer 2: Animated shape layers */}
      {[0, 1, 2].map((i) => (
        <div key={`shape-${i}`} style={{
          position: 'absolute', width: 520 + i * 140, height: 520 + i * 140,
          right: `${-14 + i * 26}%`, top: `${-18 + i * 24}%`,
          border: '3px solid rgba(255,193,7,0.05)', borderRadius: i === 1 ? '50%' : 0,
          background: i === 0 ? 'rgba(217,255,244,0.028)' : 'transparent',
          transform: `translate(${Math.sin(frame * 0.0052 + i * 2.2) * 38}px, ${Math.cos(frame * 0.006 + i) * 30}px) rotate(${frame * 0.03 * (i % 2 ? 1 : -1)}deg) scale(${1 + Math.cos(frame * 0.01 + i) * 0.04})`,
        }} />
      ))}

      {/* Layer 3: Motion texture — graph grid */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(rgba(217,255,244,0.05) 1.5px, transparent 1.5px), linear-gradient(90deg, rgba(217,255,244,0.05) 1.5px, transparent 1.5px)',
        backgroundSize: '80px 80px',
        transform: `translate(${(-frame * 0.22) % 80}px, ${(frame * 0.16) % 80}px)`,
      }} />

      {/* Layer 4: Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => (
        <div key={`p-${i}`} style={{
          position: 'absolute',
          left: (seededRandom(i + 81) * 1920 + Math.sin(frame * 0.02 + i * 1.6) * 24) % 1920,
          top: (seededRandom(i + 200) * 1080 + frame * (0.22 + seededRandom(i) * 0.46)) % 1080,
          width: 4, height: 4, background: i % 3 === 0 ? '#FFC107' : '#D9FFF4',
          opacity: 0.1 + seededRandom(i + 41) * 0.15, transform: 'rotate(45deg)',
        }} />
      ))}

      {/* Layer 5: Content — camera rig */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX}px)` }}>

        {/* BEAT A — first 8 years: 3/4 pie goes to bank */}
        <AbsoluteFill style={{ opacity: beatA, justifyContent: 'center', alignItems: 'center', flexDirection: 'row', gap: 110 }}>
          <svg width="440" height="440" viewBox="0 0 440 440" style={{ transform: `scale(${sp(10)}) rotate(${interpolate(sp(10), [0, 1], [-90, 0])}deg)` }}>
            {/* 75% bank slice */}
            <path d="M220 220 L220 30 A190 190 0 1 1 30 220 Z" fill="#ff0000" stroke="#F7F4ED" strokeWidth="8" opacity="0.92" />
            {/* 25% owner slice */}
            <path d="M220 220 L30 220 A190 190 0 0 1 220 30 Z" fill="#FFC107" stroke="#F7F4ED" strokeWidth="8" />
            <text x="255" y="290" fontFamily="Inter" fontWeight="900" fontSize="52" fill="#F7F4ED">BANK 75%</text>
            <text x="66" y="150" fontFamily="Inter" fontWeight="900" fontSize="34" fill="#0E3313">आपका 25%</text>
          </svg>
          <div style={{ maxWidth: 860 }}>
            <div style={{ fontFamily: 'Yantra One', fontSize: 104, color: '#F7F4ED', lineHeight: 1.05, textShadow: '10px 10px 0 rgba(5,24,9,0.9)', transform: `translateX(${interpolate(sp(45), [0, 1], [130, 0])}px)`, opacity: sp(45) }}>
              पहले 8 साल का<br /><span style={{ color: '#ff0000' }}>तीन-चौथाई</span> bank को
            </div>
            <div style={{ marginTop: 26, fontFamily: 'Noto Sans Devanagari', fontSize: 48, color: '#D9FFF4', opacity: sp(95) }}>
              OWNERSHIP में नहीं — INTEREST में
            </div>
          </div>
        </AbsoluteFill>

        {/* BEAT B — paid ₹50L+ vs principal only ₹8-10L */}
        <AbsoluteFill style={{ opacity: beatB, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ display: 'flex', gap: 90, alignItems: 'center' }}>
            <div style={{ ...brutalCard, textAlign: 'center', transform: `rotate(-2deg) scale(${sp(258)})` }}>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, color: '#D9FFF4' }}>10 साल में भरा</div>
              <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 130, color: '#FFC107', lineHeight: 1.05 }}>₹50L+</div>
            </div>
            <svg width="120" height="100" viewBox="0 0 120 100" style={{ opacity: sp(290) }}>
              <path d="M10 50 H85 M85 50 L60 25 M85 50 L60 75" stroke="#F7F4ED" strokeWidth="12" fill="none" transform={`translate(${Math.sin(frame * 0.13) * 7} 0)`} />
            </svg>
            <div style={{ ...brutalCard, background: '#ff0000', textAlign: 'center', transform: `rotate(2deg) scale(${sp(305, { damping: 9 })})` }}>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, color: '#F7F4ED' }}>principal घटा सिर्फ</div>
              <div style={{ fontFamily: 'JetBrains Mono', fontWeight: 800, fontSize: 130, color: '#F7F4ED', lineHeight: 1.05 }}>₹8-10L</div>
            </div>
          </div>
          <div style={{ marginTop: 65, fontFamily: 'Yantra One', fontSize: 86, color: '#F7F4ED', opacity: sp(380), transform: `translateY(${interpolate(sp(380), [0, 1], [50, 0])}px)`, textShadow: '10px 10px 0 rgba(5,24,9,0.9)' }}>
            HELPLESSNESS <span style={{ color: '#FFC107' }}>स्वाभाविक है</span> — RATIONAL है
          </div>
        </AbsoluteFill>

        {/* BEAT C — amortization curve: interest first, by design */}
        <AbsoluteFill style={{ opacity: beatC, justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ ...brutalCard, marginBottom: 44, transform: `scale(${sp(508)})` }}>
            <span style={{ fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 64, color: '#F7F4ED' }}>AMORTIZATION — <span style={{ color: '#ff0000' }}>BY DESIGN</span></span>
          </div>
          <svg width="1250" height="480" viewBox="0 0 1250 480" style={{ opacity: sp(520) }}>
            {/* axes */}
            <line x1="90" y1="30" x2="90" y2="410" stroke="#F7F4ED" strokeWidth="7" />
            <line x1="90" y1="410" x2="1190" y2="410" stroke="#F7F4ED" strokeWidth="7" />
            <text x="1020" y="465" fontFamily="JetBrains Mono" fontSize="34" fill="#9E9E9E">YEARS →</text>
            {/* interest curve (high early, falling) — draw on */}
            <path
              d="M100 80 C 380 95, 600 200, 1180 370"
              fill="none" stroke="#ff0000" strokeWidth="13"
              strokeDasharray={1400} strokeDashoffset={1400 * (1 - curveDraw)}
            />
            {/* principal curve (low early, rising) */}
            <path
              d="M100 380 C 380 365, 600 260, 1180 90"
              fill="none" stroke="#FFC107" strokeWidth="13"
              strokeDasharray={1400} strokeDashoffset={1400 * (1 - curveDraw)}
            />
            <text x="150" y="60" fontFamily="Inter" fontWeight="800" fontSize="40" fill="#ff0000" opacity={curveDraw}>INTEREST पहले</text>
            <text x="150" y="360" fontFamily="Inter" fontWeight="800" fontSize="40" fill="#FFC107" opacity={curveDraw}>PRINCIPAL बाद में</text>
            {/* crossover marker */}
            <circle cx="640" cy="232" r={16 * curveDraw} fill="#D9FFF4" stroke="#0E3313" strokeWidth="5" />
          </svg>
          <div style={{ marginTop: 26, fontFamily: 'Noto Sans Devanagari', fontSize: 50, color: '#D9FFF4', opacity: sp(660), transform: `scale(${sp(660)})` }}>
            शुरुआती सालों में bank <span style={{ color: '#FFC107', fontWeight: 800 }}>अपना interest पहले</span> वसूलता है
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(4,18,7,0.78) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }) }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C1S10_AmortizationTruth;
```

