```tsx
import React from "react";

type WelcomeCardProps = {
  name: string;
};

export const WelcomeCard: React.FC<WelcomeCardProps> = ({ name }) => {
  return (
    <div
      style={{
        padding: "24px",
        borderRadius: "12px",
        background: "#111827",
        color: "#ffffff",
        textAlign: "center",
      }}
    >
      <h1>Welcome, {name}!</h1>
      <p>This is a sample TSX component.</p>
      <button
        onClick={() => alert(`Hello, ${name}!`)}
        style={{
          padding: "10px 16px",
          border: "none",
          borderRadius: "8px",
          cursor: "pointer",
        }}
      >
        Click Me
      </button>
    </div>
  );
};

export default function App() {
  return <WelcomeCard name="Alice" />;
}
```

