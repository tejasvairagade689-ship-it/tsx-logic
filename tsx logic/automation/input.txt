```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_Pause',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_Pause: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pulsing pause circle
  const pulseScale = 1 + Math.sin(frame * 0.08) * 0.04;
  const ringExpand = (frame % 60) / 60;

  // Text reveals
  const ruko = sp(20);
  const socho = sp(70);
  const formText = sp(140);

  // Camera zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 40%, rgba(255,193,7,0.18) 0%, transparent 60%)',
      }} />

      {/* Shape Layer - drifting circles */}
      {[0, 1, 2, 3, 4].map((i) => {
        const seed = seededRandom(i + 1);
        const drift = Math.sin(frame * 0.015 + i) * 30;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${seed * 80 + 5}%`,
            top: `${seededRandom(i + 10) * 70 + 10}%`,
            width: 200 + seed * 250,
            height: 200 + seed * 250,
            borderRadius: '50%',
            border: '2px solid #1F150C',
            opacity: 0.06,
            transform: `translateY(${drift}px) rotate(${frame * 0.2}deg)`,
          }} />
        );
      })}

      {/* Motion texture - grid */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#1F150C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid1)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const s = seededRandom(i + 50);
        const yMove = (frame * (1 + s) + i * 50) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: yMove,
            width: 4 + s * 5,
            height: 4 + s * 5,
            borderRadius: '50%',
            backgroundColor: '#080616',
            opacity: 0.15 + s * 0.2,
          }} />
        );
      })}

      {/* Main pause symbol with concentric rings */}
      <AbsoluteFill style={{
        justifyContent: 'center',
        alignItems: 'center',
        transform: `scale(${camZoom})`,
      }}>
        {/* Expanding rings */}
        {[0, 1, 2].map((i) => {
          const phase = ((frame + i * 20) % 60) / 60;
          return (
            <div key={i} style={{
              position: 'absolute',
              width: 300 + phase * 400,
              height: 300 + phase * 400,
              borderRadius: '50%',
              border: '3px solid #FFC107',
              opacity: (1 - phase) * 0.4,
              top: '28%',
            }} />
          );
        })}

        {/* Pause icon SVG */}
        <div style={{
          position: 'absolute',
          top: '20%',
          transform: `scale(${sp(0) * pulseScale})`,
        }}>
          <svg width="220" height="220" viewBox="0 0 220 220">
            <circle cx="110" cy="110" r="100" fill="#1F150C" />
            <rect x="78" y="65" width="20" height="90" rx="4" fill="#FFC107" />
            <rect x="122" y="65" width="20" height="90" rx="4" fill="#FFC107" />
          </svg>
        </div>

        {/* Hindi text - main */}
        <div style={{
          position: 'absolute',
          top: '58%',
          fontFamily: 'Yantra One',
          fontSize: 187,
          color: '#080616',
          fontWeight: 700,
          transform: `translateY(${(1 - ruko) * 60}px) scale(${ruko})`,
          opacity: ruko,
          letterSpacing: '-2px',
        }}>
          रुकिये
        </div>

        {/* Subtext */}
        <div style={{
          position: 'absolute',
          top: '78%',
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 51,
          color: '#1F150C',
          fontWeight: 500,
          opacity: socho * 0.85,
          transform: `translateY(${(1 - socho) * 30}px)`,
        }}>
          एक पल · और सोचिये
        </div>

        {/* Form indicator */}
        <div style={{
          position: 'absolute',
          bottom: '8%',
          display: 'flex',
          alignItems: 'center',
          gap: 18,
          opacity: formText,
          transform: `translateY(${(1 - formText) * 20}px)`,
        }}>
          <svg width="44" height="44" viewBox="0 0 44 44">
            <rect x="6" y="4" width="32" height="36" rx="3" fill="none" stroke="#FF0000" strokeWidth="2.5" />
            <line x1="12" y1="14" x2="32" y2="14" stroke="#FF0000" strokeWidth="2.5" />
            <line x1="12" y1="22" x2="28" y2="22" stroke="#FF0000" strokeWidth="2.5" />
            <line x1="12" y1="30" x2="24" y2="30" stroke="#FF0000" strokeWidth="2.5" />
          </svg>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 38,
            color: '#1F150C',
            fontWeight: 600,
          }}>
            एडुकेशन लोन फॉर्म
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(8,6,22,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1_Pause;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_DreamChain',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_DreamChain: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 5 chain steps
  const steps = [
    { label: 'पढ़ाई', icon: 'book', delay: 10 },
    { label: 'डिग्री', icon: 'cap', delay: 35 },
    { label: 'नौकरी', icon: 'briefcase', delay: 60 },
    { label: 'EMI', icon: 'money', delay: 85 },
    { label: 'पटरी', icon: 'track', delay: 110 },
  ];

  const renderIcon = (icon: string) => {
    switch (icon) {
      case 'book':
        return (
          <svg width="80" height="80" viewBox="0 0 80 80">
            <rect x="14" y="16" width="52" height="48" rx="3" fill="#FFC107" />
            <line x1="40" y1="20" x2="40" y2="60" stroke="#080616" strokeWidth="2.5" />
            <line x1="20" y1="30" x2="34" y2="30" stroke="#080616" strokeWidth="2" />
            <line x1="46" y1="30" x2="60" y2="30" stroke="#080616" strokeWidth="2" />
          </svg>
        );
      case 'cap':
        return (
          <svg width="80" height="80" viewBox="0 0 80 80">
            <path d="M40 22 L12 36 L40 50 L68 36 Z" fill="#1F150C" />
            <path d="M22 40 L22 52 Q40 62 58 52 L58 40" fill="none" stroke="#1F150C" strokeWidth="3" />
            <line x1="66" y1="36" x2="66" y2="52" stroke="#FFC107" strokeWidth="3" />
          </svg>
        );
      case 'briefcase':
        return (
          <svg width="80" height="80" viewBox="0 0 80 80">
            <rect x="14" y="26" width="52" height="38" rx="3" fill="#1F150C" />
            <rect x="30" y="18" width="20" height="10" rx="2" fill="none" stroke="#1F150C" strokeWidth="3" />
            <line x1="14" y1="42" x2="66" y2="42" stroke="#FFC107" strokeWidth="2.5" />
          </svg>
        );
      case 'money':
        return (
          <svg width="80" height="80" viewBox="0 0 80 80">
            <circle cx="40" cy="40" r="26" fill="#FFC107" />
            <text x="40" y="52" textAnchor="middle" fontSize="36" fontWeight="700" fill="#1F150C" fontFamily="Inter">₹</text>
          </svg>
        );
      case 'track':
        return (
          <svg width="80" height="80" viewBox="0 0 80 80">
            <line x1="14" y1="30" x2="66" y2="30" stroke="#1F150C" strokeWidth="3" />
            <line x1="14" y1="50" x2="66" y2="50" stroke="#1F150C" strokeWidth="3" />
            <line x1="20" y1="22" x2="20" y2="58" stroke="#1F150C" strokeWidth="2" />
            <line x1="34" y1="22" x2="34" y2="58" stroke="#1F150C" strokeWidth="2" />
            <line x1="48" y1="22" x2="48" y2="58" stroke="#1F150C" strokeWidth="2" />
            <line x1="62" y1="22" x2="62" y2="58" stroke="#1F150C" strokeWidth="2" />
          </svg>
        );
      default: return null;
    }
  };

  const camPan = interpolate(frame, [0, durationInFrames], [-15, 15]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,193,7,0.12) 0%, transparent 50%, rgba(31,21,12,0.08) 100%)',
      }} />

      {/* Shape layer - triangles */}
      {[0, 1, 2, 3].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: `${10 + i * 25}%`,
          top: `${15 + (i % 2) * 60}%`,
          width: 0,
          height: 0,
          borderLeft: `${80 + i * 20}px solid transparent`,
          borderRight: `${80 + i * 20}px solid transparent`,
          borderBottom: `${140 + i * 30}px solid #1F150C`,
          opacity: 0.04,
          transform: `rotate(${frame * 0.15 + i * 90}deg)`,
        }} />
      ))}

      {/* Texture - diagonal lines */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="diag2" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#080616" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag2)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const s = seededRandom(i + 100);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${seededRandom(i + 200) * 100}%`,
            width: 6,
            height: 6,
            backgroundColor: '#FFC107',
            opacity: 0.3 + Math.sin(frame * 0.05 + i) * 0.2,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute',
        top: 90,
        left: 0,
        right: 0,
        textAlign: 'center',
        fontFamily: 'Yantra One',
        fontSize: 95,
        color: '#080616',
        opacity: sp(0),
        transform: `translateY(${(1 - sp(0)) * -30}px) translateX(${camPan}px)`,
      }}>
        दिमाग की <span style={{ color: '#FF0000' }}>तस्वीर</span>
      </div>

      {/* Chain of steps */}
      <AbsoluteFill style={{
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'row',
        gap: 30,
        paddingTop: 80,
      }}>
        {steps.map((step, i) => {
          const animIn = sp(step.delay);
          const arrowIn = sp(step.delay + 12);
          return (
            <React.Fragment key={i}>
              <div style={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                gap: 16,
                opacity: animIn,
                transform: `scale(${animIn}) translateY(${(1 - animIn) * 40}px)`,
              }}>
                <div style={{
                  width: 150,
                  height: 150,
                  borderRadius: 20,
                  backgroundColor: '#F5F5F5',
                  border: '4px solid #1F150C',
                  display: 'flex',
                  justifyContent: 'center',
                  alignItems: 'center',
                  boxShadow: '6px 6px 0 #080616',
                }}>
                  {renderIcon(step.icon)}
                </div>
                <div style={{
                  fontFamily: 'Yantra One',
                  fontSize: 51,
                  color: '#1F150C',
                  fontWeight: 700,
                }}>
                  {step.label}
                </div>
              </div>
              {i < steps.length - 1 && (
                <div style={{
                  fontSize: 60,
                  color: '#FFC107',
                  opacity: arrowIn,
                  transform: `scale(${arrowIn}) translateX(${(1 - arrowIn) * -20}px)`,
                  fontWeight: 900,
                  marginBottom: 60,
                }}>
                  →
                </div>
              )}
            </React.Fragment>
          );
        })}
      </AbsoluteFill>

      {/* Bottom tagline */}
      <div style={{
        position: 'absolute',
        bottom: 90,
        left: 0,
        right: 0,
        textAlign: 'center',
        fontFamily: 'Noto Sans Devanagari',
        fontSize: 44,
        color: '#080616',
        fontWeight: 600,
        opacity: sp(135),
      }}>
        जिंदगी पटरी पर आ जाएगी ·
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_DreamChain;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_HiddenTruth',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_HiddenTruth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Split reveal animation
  const leftReveal = sp(15);
  const rightReveal = sp(75);
  const vsBadge = sp(130);

  // Magnifying glass on right card
  const magX = Math.sin(frame * 0.04) * 20;
  const magY = Math.cos(frame * 0.04) * 15;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(255,193,7,0.15) 0%, transparent 50%, rgba(255,0,0,0.12) 100%)',
      }} />

      {/* Shape layer - rectangles */}
      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: `${seededRandom(i + 5) * 80}%`,
          top: `${seededRandom(i + 15) * 80}%`,
          width: 300,
          height: 200,
          border: '2px solid #1F150C',
          opacity: 0.05,
          transform: `rotate(${frame * 0.1 + i * 30}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.1})`,
        }} />
      ))}

      {/* Texture - dots */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots3" width="30" height="30" patternUnits="userSpaceOnUse">
            <circle cx="15" cy="15" r="1.5" fill="#080616" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots3)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 70);
        const drift = Math.sin(frame * 0.03 + i) * 40;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${seededRandom(i + 80) * 100}%`,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#1F150C',
            opacity: 0.2,
            transform: `translateX(${drift}px)`,
          }} />
        );
      })}

      {/* Top headline */}
      <div style={{
        position: 'absolute',
        top: 70,
        left: 0,
        right: 0,
        textAlign: 'center',
        fontFamily: 'Yantra One',
        fontSize: 78,
        color: '#080616',
        opacity: sp(0),
      }}>
        दो <span style={{ color: '#FFC107' }}>तस्वीरें</span>
      </div>

      {/* LEFT CARD - Beautiful dream */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: 230,
        width: 720,
        height: 640,
        backgroundColor: '#F5F5F5',
        border: '5px solid #FFC107',
        borderRadius: 24,
        padding: 40,
        boxShadow: '10px 10px 0 #1F150C',
        opacity: leftReveal,
        transform: `translateX(${(1 - leftReveal) * -100}px) rotate(${(1 - leftReveal) * -5}deg)`,
      }}>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 28,
          color: '#FFC107',
          fontWeight: 800,
          letterSpacing: 4,
        }}>
          DIKHTI HUI
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 76,
          color: '#1F150C',
          marginTop: 12,
          lineHeight: 1.1,
        }}>
          साफ.<br/>सुन्दर.
        </div>

        {/* Sunny illustration */}
        <div style={{ marginTop: 40, display: 'flex', justifyContent: 'center' }}>
          <svg width="320" height="240" viewBox="0 0 320 240">
            <circle cx="160" cy="80" r="50" fill="#FFC107" />
            {[0, 45, 90, 135, 180, 225, 270, 315].map((deg) => {
              const rad = (deg * Math.PI) / 180;
              return (
                <line key={deg}
                  x1={160 + Math.cos(rad) * 60}
                  y1={80 + Math.sin(rad) * 60}
                  x2={160 + Math.cos(rad) * 85}
                  y2={80 + Math.sin(rad) * 85}
                  stroke="#FFC107"
                  strokeWidth="5"
                />
              );
            })}
            <path d="M40 200 Q160 130 280 200 L280 240 L40 240 Z" fill="#1F150C" />
            <rect x="140" y="160" width="40" height="50" fill="#FFC107" />
          </svg>
        </div>

        <div style={{
          marginTop: 30,
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 38,
          color: '#1F150C',
          fontWeight: 600,
          textAlign: 'center',
        }}>
          सपनों वाली कहानी
        </div>
      </div>

      {/* RIGHT CARD - Hidden truth */}
      <div style={{
        position: 'absolute',
        right: 120,
        top: 230,
        width: 720,
        height: 640,
        backgroundColor: '#1F150C',
        border: '5px solid #FF0000',
        borderRadius: 24,
        padding: 40,
        boxShadow: '10px 10px 0 #080616',
        opacity: rightReveal,
        transform: `translateX(${(1 - rightReveal) * 100}px) rotate(${(1 - rightReveal) * 5}deg)`,
      }}>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 28,
          color: '#FF0000',
          fontWeight: 800,
          letterSpacing: 4,
        }}>
          CHUPI HUI
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 76,
          color: '#DFF1F1',
          marginTop: 12,
          lineHeight: 1.1,
        }}>
          बैंक के<br/>कागज़.
        </div>

        {/* Document with magnifier */}
        <div style={{ marginTop: 40, display: 'flex', justifyContent: 'center', position: 'relative' }}>
          <svg width="320" height="240" viewBox="0 0 320 240">
            {/* Paper */}
            <rect x="60" y="20" width="200" height="220" rx="6" fill="#DFF1F1" />
            <line x1="80" y1="50" x2="240" y2="50" stroke="#1F150C" strokeWidth="3" />
            <line x1="80" y1="75" x2="220" y2="75" stroke="#1F150C" strokeWidth="2" />
            <line x1="80" y1="95" x2="240" y2="95" stroke="#1F150C" strokeWidth="2" />
            <line x1="80" y1="115" x2="200" y2="115" stroke="#FF0000" strokeWidth="3" />
            <line x1="80" y1="135" x2="230" y2="135" stroke="#1F150C" strokeWidth="2" />
            <line x1="80" y1="155" x2="210" y2="155" stroke="#FF0000" strokeWidth="3" />
            <line x1="80" y1="175" x2="225" y2="175" stroke="#1F150C" strokeWidth="2" />
            <line x1="80" y1="195" x2="215" y2="195" stroke="#1F150C" strokeWidth="2" />
            <text x="160" y="225" textAnchor="middle" fontSize="14" fill="#FF0000" fontFamily="Inter" fontWeight="700">**FINE PRINT**</text>

            {/* Magnifying glass */}
            <g transform={`translate(${180 + magX}, ${110 + magY})`}>
              <circle cx="0" cy="0" r="42" fill="none" stroke="#FFC107" strokeWidth="6" />
              <circle cx="0" cy="0" r="38" fill="rgba(255,193,7,0.15)" />
              <line x1="30" y1="30" x2="60" y2="60" stroke="#FFC107" strokeWidth="7" strokeLinecap="round" />
            </g>
          </svg>
        </div>

        <div style={{
          marginTop: 30,
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 38,
          color: '#FF0000',
          fontWeight: 700,
          textAlign: 'center',
        }}>
          कोई नहीं दिखाता
        </div>
      </div>

      {/* VS Badge in center */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '50%',
        transform: `translate(-50%, -50%) scale(${vsBadge}) rotate(${(1 - vsBadge) * 360}deg)`,
        opacity: vsBadge,
      }}>
        <div style={{
          width: 130,
          height: 130,
          borderRadius: '50%',
          backgroundColor: '#FFC107',
          border: '6px solid #080616',
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          fontFamily: 'Inter',
          fontSize: 51,
          fontWeight: 900,
          color: '#080616',
          boxShadow: '0 0 40px rgba(255,193,7,0.6)',
        }}>
          VS
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3_HiddenTruth;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4_KunalIntro',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_KunalIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 100, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Sequential text reveals
  const nameIn = sp(10);
  const channelIn = sp(50);
  const line1 = sp(110);
  const line2 = sp(180);
  const line3 = sp(250);

  // Camera tilt
  const camTilt = Math.sin(frame * 0.01) * 0.5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 40%, rgba(255,193,7,0.18) 0%, transparent 60%)',
      }} />

      {/* Shape layer */}
      {[0, 1, 2, 3].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: `${seededRandom(i + 20) * 80 + 5}%`,
          top: `${seededRandom(i + 25) * 70 + 10}%`,
          width: 250 + i * 50,
          height: 250 + i * 50,
          borderRadius: i % 2 === 0 ? '50%' : '20px',
          border: '2px solid #080616',
          opacity: 0.05,
          transform: `rotate(${frame * 0.12 + i * 60}deg)`,
        }} />
      ))}

      {/* Texture - vertical lines */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="vlines4" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="30" y1="0" x2="30" y2="60" stroke="#1F150C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#vlines4)" />
      </svg>

      {/* Floating particles - golden */}
      {Array.from({ length: 30 }).map((_, i) => {
        const s = seededRandom(i + 300);
        const yMove = (frame * (0.8 + s) + i * 30) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: yMove,
            width: 4,
            height: 4,
            borderRadius: '50%',
            backgroundColor: '#FFC107',
            opacity: 0.4,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camTilt}deg)` }}>
        {/* LEFT — Author card */}
        <div style={{
          position: 'absolute',
          left: 140,
          top: 220,
          opacity: nameIn,
          transform: `translateX(${(1 - nameIn) * -80}px)`,
        }}>
          {/* Big name plate */}
          <div style={{
            backgroundColor: '#080616',
            padding: '30px 50px',
            borderRadius: 16,
            display: 'inline-block',
            border: '4px solid #FFC107',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 32,
              color: '#FFC107',
              fontWeight: 600,
              letterSpacing: 6,
            }}>
              I AM
            </div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 187,
              color: '#DFF1F1',
              lineHeight: 0.95,
              marginTop: 8,
            }}>
              कुनाल
            </div>
          </div>

          {/* Channel pill */}
          <div style={{
            marginTop: 30,
            backgroundColor: '#FFC107',
            padding: '20px 36px',
            borderRadius: 60,
            display: 'inline-flex',
            alignItems: 'center',
            gap: 18,
            opacity: channelIn,
            transform: `translateY(${(1 - channelIn) * 30}px) scale(${channelIn})`,
            boxShadow: '6px 6px 0 #1F150C',
          }}>
            <svg width="40" height="40" viewBox="0 0 40 40">
              <rect x="4" y="8" width="32" height="24" rx="6" fill="#FF0000" />
              <polygon points="16,14 16,26 26,20" fill="#F5F5F5" />
            </svg>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 38,
              fontWeight: 800,
              color: '#080616',
            }}>
              ProBuffet HQ
            </div>
          </div>
        </div>

        {/* RIGHT — Statement stack */}
        <div style={{
          position: 'absolute',
          right: 120,
          top: 210,
          width: 780,
          display: 'flex',
          flexDirection: 'column',
          gap: 26,
        }}>
          {/* Line 1 */}
          <div style={{
            backgroundColor: '#F5F5F5',
            padding: '24px 32px',
            borderLeft: '8px solid #FFC107',
            borderRadius: 8,
            opacity: line1,
            transform: `translateX(${(1 - line1) * 60}px)`,
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 44,
              color: '#1F150C',
              fontWeight: 600,
              lineHeight: 1.3,
            }}>
              वो बात करते हैं जो<br/>
              <span style={{ color: '#FF0000', fontWeight: 800 }}>कोई और नहीं करता</span>
            </div>
          </div>

          {/* Line 2 */}
          <div style={{
            backgroundColor: '#1F150C',
            padding: '24px 32px',
            borderLeft: '8px solid #FF0000',
            borderRadius: 8,
            opacity: line2,
            transform: `translateX(${(1 - line2) * 60}px)`,
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 44,
              color: '#DFF1F1',
              fontWeight: 600,
              lineHeight: 1.3,
            }}>
              बातचीत थोड़ी <span style={{ color: '#FFC107', fontWeight: 800 }}>असहज</span> करेगी
            </div>
          </div>

          {/* Line 3 */}
          <div style={{
            backgroundColor: '#FFC107',
            padding: '24px 32px',
            borderRadius: 8,
            opacity: line3,
            transform: `translateX(${(1 - line3) * 60}px) scale(${line3 > 0.8 ? 1 + Math.sin(frame * 0.1) * 0.01 : 1})`,
            border: '4px solid #080616',
          }}>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 53,
              color: '#080616',
              fontWeight: 800,
              lineHeight: 1.2,
            }}>
              यही असहजी<br/>आपको बचाएगी
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4_KunalIntro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5_Machine',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_Machine: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.45, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headIn = sp(0);
  const machineIn = sp(40);
  const dropIn = sp(110);
  const crushIn = sp(180);

  // Gear rotations
  const gear1Rot = frame * 1.5;
  const gear2Rot = -frame * 2;
  const gear3Rot = frame * 1.8;

  // Conveyor belt
  const beltOffset = (frame * 3) % 40;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, transparent 0%, rgba(255,0,0,0.08) 100%)',
      }} />

      {/* Shape layer */}
      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: `${15 + i * 30}%`,
          top: `${20 + i * 20}%`,
          width: 280,
          height: 280,
          borderRadius: '50%',
          border: '3px solid #1F150C',
          opacity: 0.04,
          transform: `rotate(${frame * 0.1 + i * 90}deg)`,
        }} />
      ))}

      {/* Texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="cross5" width="50" height="50" patternUnits="userSpaceOnUse">
            <line x1="25" y1="15" x2="25" y2="35" stroke="#080616" strokeWidth="1" />
            <line x1="15" y1="25" x2="35" y2="25" stroke="#080616" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#cross5)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const s = seededRandom(i + 400);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${seededRandom(i + 500) * 100}%`,
            width: 5,
            height: 5,
            backgroundColor: '#FF0000',
            opacity: 0.25,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 0,
        right: 0,
        textAlign: 'center',
        fontFamily: 'Yantra One',
        fontSize: 102,
        color: '#080616',
        opacity: headIn,
        transform: `translateY(${(1 - headIn) * -30}px)`,
        lineHeight: 1,
      }}>
        एडुकेशन लोन <span style={{ color: '#FF0000' }}>मशीन</span>
      </div>

      {/* MACHINE - Center */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', marginTop: 60 }}>
        <div style={{
          opacity: machineIn,
          transform: `scale(${machineIn})`,
          position: 'relative',
        }}>
          <svg width="900" height="560" viewBox="0 0 900 560">
            {/* Main machine body */}
            <rect x="180" y="180" width="540" height="240" rx="12" fill="#1F150C" />
            <rect x="200" y="200" width="500" height="200" rx="8" fill="#080616" />

            {/* Top funnel - dreams in */}
            <polygon points="350,80 550,80 470,180 430,180" fill="#FFC107" stroke="#080616" strokeWidth="4" />
            <text x="450" y="130" textAnchor="middle" fontSize="22" fill="#080616" fontFamily="Inter" fontWeight="800">सपने</text>

            {/* Gears */}
            <g transform={`translate(290, 300) rotate(${gear1Rot})`}>
              <circle r="55" fill="#FFC107" stroke="#080616" strokeWidth="4" />
              {[0, 45, 90, 135, 180, 225, 270, 315].map(d => {
                const rad = (d * Math.PI) / 180;
                return <rect key={d} x="-8" y="-72" width="16" height="20" fill="#FFC107" stroke="#080616" strokeWidth="3" transform={`rotate(${d})`} />;
              })}
              <circle r="14" fill="#080616" />
            </g>

            <g transform={`translate(450, 300) rotate(${gear2Rot})`}>
              <circle r="70" fill="#FF0000" stroke="#080616" strokeWidth="4" />
              {[0, 36, 72, 108, 144, 180, 216, 252, 288, 324].map(d => {
                return <rect key={d} x="-9" y="-88" width="18" height="22" fill="#FF0000" stroke="#080616" strokeWidth="3" transform={`rotate(${d})`} />;
              })}
              <circle r="18" fill="#080616" />
            </g>

            <g transform={`translate(610, 300) rotate(${gear3Rot})`}>
              <circle r="55" fill="#FFC107" stroke="#080616" strokeWidth="4" />
              {[0, 45, 90, 135, 180, 225, 270, 315].map(d => {
                return <rect key={d} x="-8" y="-72" width="16" height="20" fill="#FFC107" stroke="#080616" strokeWidth="3" transform={`rotate(${d})`} />;
              })}
              <circle r="14" fill="#080616" />
            </g>

            {/* Conveyor belt at bottom */}
            <rect x="160" y="430" width="580" height="40" fill="#1F150C" />
            <rect x="160" y="430" width="580" height="40" fill="none" stroke="#FFC107" strokeWidth="2" />
            {Array.from({ length: 16 }).map((_, i) => (
              <rect key={i} x={170 + i * 36 - beltOffset} y="438" width="20" height="24" fill="#FFC107" opacity="0.6" />
            ))}

            {/* Output - crushed lives */}
            <g opacity={crushIn}>
              <circle cx="780" cy="450" r="14" fill="#FF0000" />
              <circle cx="810" cy="450" r="10" fill="#FF0000" opacity="0.7" />
              <circle cx="840" cy="450" r="8" fill="#FF0000" opacity="0.5" />
              <text x="800" y="510" textAnchor="middle" fontSize="22" fill="#FF0000" fontFamily="Inter" fontWeight="800">CRUSHED</text>
            </g>

            {/* Steam/smoke */}
            <g opacity="0.6">
              <circle cx={250 + Math.sin(frame * 0.1) * 10} cy={170 - (frame * 2) % 60} r="8" fill="#9E9E9E" />
              <circle cx={680 + Math.cos(frame * 0.1) * 10} cy={170 - ((frame + 30) * 2) % 60} r="8" fill="#9E9E9E" />
            </g>
          </svg>

          {/* Falling youth icons */}
          <div style={{
            position: 'absolute',
            top: -20,
            left: '50%',
            transform: 'translateX(-50%)',
            opacity: dropIn,
          }}>
            {[0, 1, 2].map((i) => {
              const fall = ((frame + i * 25) * 4) % 220;
              return (
                <div key={i} style={{
                  position: 'absolute',
                  left: -20 + i * 20,
                  top: fall,
                  opacity: 1 - fall / 220,
                }}>
                  <svg width="36" height="36" viewBox="0 0 36 36">
                    <circle cx="18" cy="10" r="6" fill="#080616" />
                    <rect x="12" y="16" width="12" height="16" rx="2" fill="#080616" />
                  </svg>
                </div>
              );
            })}
          </div>
        </div>
      </AbsoluteFill>

      {/* Bottom callout */}
      <div style={{
        position: 'absolute',
        bottom: 70,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: crushIn,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#FF0000',
          padding: '14px 36px',
          borderRadius: 8,
          fontFamily: 'Yantra One',
          fontSize: 42,
          color: '#F5F5F5',
          fontWeight: 700,
          letterSpacing: 1,
        }}>
          लाखों जिंदगियां पिसती हैं
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(8,6,22,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5_Machine;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6_RBIData',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_RBIData: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.45, stiffness: 115, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Number counter for 1.11
  const countProgress = interpolate(frame, [60, 150], [0, 1.11], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const interestLow = interpolate(frame, [200, 260], [0, 8], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const interestHigh = interpolate(frame, [240, 300], [0, 15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const badgeIn = sp(0);
  const cardIn = sp(50);
  const interestCardIn = sp(180);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 100%, rgba(255,0,0,0.12) 0%, transparent 60%)',
      }} />

      {/* Shape layer - hexagons feel */}
      {[0, 1, 2, 3, 4].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: `${seededRandom(i + 30) * 85}%`,
          top: `${seededRandom(i + 31) * 85}%`,
          width: 180 + i * 30,
          height: 180 + i * 30,
          borderRadius: 24,
          border: '2px solid #1F150C',
          opacity: 0.04,
          transform: `rotate(${frame * 0.1 + i * 72}deg)`,
        }} />
      ))}

      {/* Texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid6" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 50 0 L 0 0 0 50" fill="none" stroke="#1F150C" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid6)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const s = seededRandom(i + 600);
        const yMove = (frame * (0.5 + s) + i * 40) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: yMove,
            width: 4,
            height: 4,
            backgroundColor: '#FFC107',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Source badge - RBI */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: '50%',
        transform: `translateX(-50%) scale(${badgeIn})`,
        opacity: badgeIn,
        display: 'flex',
        alignItems: 'center',
        gap: 16,
        backgroundColor: '#080616',
        padding: '14px 32px',
        borderRadius: 50,
        border: '3px solid #FFC107',
      }}>
        <div style={{
          width: 36,
          height: 36,
          borderRadius: '50%',
          backgroundColor: '#FFC107',
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          fontFamily: 'Inter',
          fontWeight: 900,
          fontSize: 20,
          color: '#080616',
        }}>
          ₹
        </div>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 28,
          color: '#FFC107',
          fontWeight: 800,
          letterSpacing: 3,
        }}>
          RBI DATA · 2024
        </div>
      </div>

      {/* BIG NUMBER */}
      <div style={{
        position: 'absolute',
        top: 180,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: cardIn,
        transform: `scale(${cardIn})`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 51,
          color: '#1F150C',
          fontWeight: 500,
          marginBottom: 6,
        }}>
          कुल बकाया एडुकेशन लोन
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 264,
          color: '#FF0000',
          fontWeight: 900,
          lineHeight: 1,
          letterSpacing: -4,
          textShadow: '6px 6px 0 rgba(8,6,22,0.15)',
        }}>
          ₹{countProgress.toFixed(2)}
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 68,
          color: '#080616',
          fontWeight: 700,
          marginTop: -10,
        }}>
          लाख <span style={{ color: '#FFC107' }}>करोड़</span>
        </div>
      </div>

      {/* INTEREST RATE BAR CHART */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: 0,
        right: 0,
        opacity: interestCardIn,
        transform: `translateY(${(1 - interestCardIn) * 50}px)`,
      }}>
        <div style={{
          textAlign: 'center',
          fontFamily: 'Inter',
          fontSize: 28,
          color: '#1F150C',
          fontWeight: 700,
          letterSpacing: 3,
          marginBottom: 20,
        }}>
          INTEREST RATES RANGE
        </div>

        <div style={{
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'flex-end',
          gap: 60,
        }}>
          {/* 8% bar */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12 }}>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 76,
              color: '#FFC107',
              fontWeight: 900,
            }}>
              {interestLow.toFixed(0)}%
            </div>
            <div style={{
              width: 110,
              height: interpolate(interestLow, [0, 8], [0, 70]),
              backgroundColor: '#FFC107',
              border: '3px solid #080616',
            }} />
            <div style={{
              fontFamily: 'Inter',
              fontSize: 24,
              color: '#080616',
              fontWeight: 700,
            }}>
              MIN
            </div>
          </div>

          {/* Arrow */}
          <div style={{
            fontSize: 60,
            color: '#FF0000',
            fontWeight: 900,
            marginBottom: 60,
          }}>
            →
          </div>

          {/* 15% bar */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12 }}>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 76,
              color: '#FF0000',
              fontWeight: 900,
            }}>
              {interestHigh.toFixed(0)}%
            </div>
            <div style={{
              width: 110,
              height: interpolate(interestHigh, [0, 15], [0, 130]),
              backgroundColor: '#FF0000',
              border: '3px solid #080616',
            }} />
            <div style={{
              fontFamily: 'Inter',
              fontSize: 24,
              color: '#080616',
              fontWeight: 700,
            }}>
              MAX
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6_RBIData;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7_FamilySacrifice',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_FamilySacrifice: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(0);
  const card1 = sp(60);
  const card2 = sp(130);
  const card3 = sp(200);
  const bottomIn = sp(290);

  // Items: land, FD, savings
  const items = [
    {
      label: 'जमीन',
      sub: 'गिरवी रखी',
      bg: '#1F150C',
      accent: '#FFC107',
      textColor: '#DFF1F1',
      delay: card1,
      icon: (
        <svg width="120" height="120" viewBox="0 0 120 120">
          <path d="M10 80 L60 30 L110 80 L110 110 L10 110 Z" fill="#FFC107" stroke="#DFF1F1" strokeWidth="3" />
          <rect x="50" y="80" width="20" height="30" fill="#1F150C" />
          <rect x="25" y="85" width="15" height="15" fill="#1F150C" />
          <rect x="80" y="85" width="15" height="15" fill="#1F150C" />
          <text x="60" y="60" textAnchor="middle" fontSize="22" fontFamily="Inter" fontWeight="800" fill="#1F150C">SOLD</text>
        </svg>
      ),
    },
    {
      label: 'FD',
      sub: 'तोड़ी',
      bg: '#FFC107',
      accent: '#FF0000',
      textColor: '#080616',
      delay: card2,
      icon: (
        <svg width="120" height="120" viewBox="0 0 120 120">
          <rect x="15" y="25" width="90" height="70" rx="6" fill="#F5F5F5" stroke="#080616" strokeWidth="3" />
          <text x="60" y="55" textAnchor="middle" fontSize="22" fill="#080616" fontFamily="Inter" fontWeight="800">FIXED</text>
          <text x="60" y="80" textAnchor="middle" fontSize="22" fill="#080616" fontFamily="Inter" fontWeight="800">DEPOSIT</text>
          <line x1="10" y1="20" x2="110" y2="100" stroke="#FF0000" strokeWidth="6" strokeLinecap="round" />
          <line x1="110" y1="20" x2="10" y2="100" stroke="#FF0000" strokeWidth="6" strokeLinecap="round" />
        </svg>
      ),
    },
    {
      label: 'बचत',
      sub: 'दाव पर',
      bg: '#FF0000',
      accent: '#FFC107',
      textColor: '#F5F5F5',
      delay: card3,
      icon: (
        <svg width="120" height="120" viewBox="0 0 120 120">
          <path d="M30 50 Q60 25 90 50 L90 90 Q60 100 30 90 Z" fill="#FFC107" stroke="#F5F5F5" strokeWidth="3" />
          <circle cx="60" cy="65" r="12" fill="#F5F5F5" />
          <text x="60" y="71" textAnchor="middle" fontSize="18" fontFamily="Inter" fontWeight="900" fill="#1F150C">₹</text>
          <line x1="60" y1="40" x2="60" y2="48" stroke="#FF0000" strokeWidth="3" />
        </svg>
      ),
    },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(255,193,7,0.15) 0%, transparent 60%, rgba(255,0,0,0.1) 100%)',
      }} />

      {/* Shape layer */}
      {[0, 1, 2, 3].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: `${seededRandom(i + 40) * 80}%`,
          top: `${seededRandom(i + 41) * 80}%`,
          width: 260,
          height: 260,
          borderRadius: '50%',
          border: '2px solid #080616',
          opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.1})`,
        }} />
      ))}

      {/* Texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="ptn7" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1" fill="#1F150C" />
            <circle cx="0" cy="0" r="1" fill="#1F150C" />
            <circle cx="40" cy="40" r="1" fill="#1F150C" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#ptn7)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const s = seededRandom(i + 700);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${seededRandom(i + 800) * 100}%`,
            width: 5,
            height: 5,
            backgroundColor: '#FFC107',
            opacity: 0.3 + Math.sin(frame * 0.04 + i) * 0.2,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Top headline */}
      <div style={{
        position: 'absolute',
        top: 70,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: titleIn,
        transform: `translateY(${(1 - titleIn) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 28,
          color: '#FF0000',
          fontWeight: 800,
          letterSpacing: 4,
        }}>
          ₹1.11 LAKH CRORE KE PEECHE
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 110,
          color: '#080616',
          lineHeight: 1,
          marginTop: 8,
        }}>
          करोड़ों <span style={{ color: '#FFC107' }}>परिवार</span>
        </div>
      </div>

      {/* 3 SACRIFICE CARDS */}
      <div style={{
        position: 'absolute',
        top: 350,
        left: 0,
        right: 0,
        display: 'flex',
        justifyContent: 'center',
        gap: 50,
      }}>
        {items.map((item, i) => (
          <div key={i} style={{
            width: 420,
            height: 480,
            backgroundColor: item.bg,
            borderRadius: 24,
            padding: 36,
            border: `5px solid ${item.accent}`,
            boxShadow: '10px 10px 0 #080616',
            opacity: item.delay,
            transform: `translateY(${(1 - item.delay) * 80}px) rotate(${(1 - item.delay) * (i - 1) * 5}deg)`,
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'space-between',
            alignItems: 'center',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 26,
              color: item.accent,
              fontWeight: 800,
              letterSpacing: 4,
            }}>
              0{i + 1}
            </div>

            <div>{item.icon}</div>

            <div style={{ textAlign: 'center' }}>
              <div style={{
                fontFamily: 'Yantra One',
                fontSize: 110,
                color: item.textColor,
                fontWeight: 900,
                lineHeight: 1,
              }}>
                {item.label}
              </div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 44,
                color: item.accent,
                fontWeight: 700,
                marginTop: 8,
              }}>
                {item.sub}
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* Bottom hope line */}
      <div style={{
        position: 'absolute',
        bottom: 70,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: bottomIn,
        transform: `translateY(${(1 - bottomIn) * 30}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#080616',
          padding: '18px 50px',
          borderRadius: 50,
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 42,
          color: '#FFC107',
          fontWeight: 600,
          border: '3px solid #FFC107',
        }}>
          उम्मीद: डिग्री सब ठीक कर देगी
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7_FamilySacrifice;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8_Question',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_Question: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.45, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const qMark = sp(0);
  const taken = sp(70);
  const arrow = sp(150);
  const paid = sp(200);
  const commentBox = sp(310);

  // Pulsing question mark
  const qPulse = 1 + Math.sin(frame * 0.1) * 0.05;

  // Money counter
  const takenAmt = interpolate(frame, [100, 160], [0, 20], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const paidAmt = interpolate(frame, [220, 290], [0, 42], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Comment box typing cursor
  const cursorBlink = Math.floor(frame / 15) % 2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 30%, rgba(255,193,7,0.2) 0%, transparent 60%)',
      }} />

      {/* Shape layer - question themed */}
      {[0, 1, 2, 3, 4].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: `${seededRandom(i + 50) * 85}%`,
          top: `${seededRandom(i + 51) * 80}%`,
          fontFamily: 'Yantra One',
          fontSize: 200,
          color: '#1F150C',
          opacity: 0.04,
          transform: `rotate(${seededRandom(i + 52) * 60 - 30}deg)`,
        }}>
          ?
        </div>
      ))}

      {/* Texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="ptn8" width="70" height="70" patternUnits="userSpaceOnUse">
            <circle cx="35" cy="35" r="20" fill="none" stroke="#1F150C" strokeWidth="0.7" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#ptn8)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const s = seededRandom(i + 900);
        const yMove = (frame * (0.7 + s) + i * 35) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: yMove,
            width: 5,
            height: 5,
            backgroundColor: '#FF0000',
            opacity: 0.25,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* HUGE Question mark on left */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: 200,
        opacity: qMark,
        transform: `scale(${qMark * qPulse})`,
      }}>
        <div style={{
          width: 380,
          height: 380,
          borderRadius: '50%',
          backgroundColor: '#FFC107',
          border: '8px solid #080616',
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          fontFamily: 'Yantra One',
          fontSize: 360,
          color: '#080616',
          fontWeight: 900,
          boxShadow: '12px 12px 0 #1F150C',
        }}>
          ?
        </div>
        <div style={{
          marginTop: 30,
          fontFamily: 'Inter',
          fontSize: 38,
          color: '#080616',
          fontWeight: 800,
          letterSpacing: 4,
          textAlign: 'center',
        }}>
          एक सवाल
        </div>
      </div>

      {/* RIGHT - Comparison */}
      <div style={{
        position: 'absolute',
        right: 120,
        top: 180,
        width: 800,
      }}>
        {/* Question heading */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 60,
          color: '#1F150C',
          marginBottom: 30,
          lineHeight: 1.1,
          opacity: sp(30),
        }}>
          आपने <span style={{ color: '#FF0000' }}>कितना लिया</span><br/>
          <span style={{ color: '#080616' }}>vs</span> <span style={{ color: '#FFC107' }}>कुल कितना चुकाया?</span>
        </div>

        {/* Taken box */}
        <div style={{
          backgroundColor: '#F5F5F5',
          border: '5px solid #FFC107',
          borderRadius: 16,
          padding: 24,
          marginBottom: 20,
          opacity: taken,
          transform: `translateX(${(1 - taken) * 60}px)`,
          boxShadow: '6px 6px 0 #1F150C',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 26,
            color: '#1F150C',
            fontWeight: 700,
            letterSpacing: 3,
          }}>
            LOAN TAKEN
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 110,
            color: '#1F150C',
            lineHeight: 1,
            marginTop: 6,
          }}>
            ₹{takenAmt.toFixed(0)} <span style={{ fontSize: 60, color: '#1F150C' }}>लाख</span>
          </div>
        </div>

        {/* Arrow */}
        <div style={{
          textAlign: 'center',
          fontSize: 56,
          color: '#FF0000',
          opacity: arrow,
          transform: `scale(${arrow})`,
          fontWeight: 900,
          lineHeight: 0.8,
        }}>
          ↓
        </div>

        {/* Paid box */}
        <div style={{
          backgroundColor: '#1F150C',
          border: '5px solid #FF0000',
          borderRadius: 16,
          padding: 24,
          opacity: paid,
          transform: `translateX(${(1 - paid) * -60}px)`,
          boxShadow: '6px 6px 0 #FF0000',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 26,
            color: '#FF0000',
            fontWeight: 700,
            letterSpacing: 3,
          }}>
            TOTAL PAID (15Y)
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 110,
            color: '#FFC107',
            lineHeight: 1,
            marginTop: 6,
          }}>
            ₹{paidAmt.toFixed(0)} <span style={{ fontSize: 60, color: '#DFF1F1' }}>लाख</span>
          </div>
        </div>
      </div>

      {/* Bottom comment box */}
      <div style={{
        position: 'absolute',
        bottom: 70,
        left: '50%',
        transform: `translateX(-50%) scale(${commentBox})`,
        opacity: commentBox,
      }}>
        <div style={{
          backgroundColor: '#FFC107',
          padding: '20px 50px',
          borderRadius: 60,
          border: '4px solid #080616',
          display: 'flex',
          alignItems: 'center',
          gap: 20,
          boxShadow: '6px 6px 0 #1F150C',
        }}>
          <svg width="46" height="46" viewBox="0 0 46 46">
            <path d="M6 8 L40 8 Q42 8 42 10 L42 30 Q42 32 40 32 L20 32 L12 40 L12 32 L8 32 Q6 32 6 30 Z" fill="#1F150C" />
            <circle cx="18" cy="20" r="2" fill="#FFC107" />
            <circle cx="24" cy="20" r="2" fill="#FFC107" />
            <circle cx="30" cy="20" r="2" fill="#FFC107" />
          </svg>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 42,
            color: '#080616',
            fontWeight: 800,
          }}>
            कॉमेंट में बताये{cursorBlink ? '|' : ' '}
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8_Question;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9_Growth',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_Growth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(0);
  const chartIn = sp(50);
  const pctIn = sp(160);
  const studentIn = sp(240);
  const growthBadge = sp(330);

  // Growing bar chart - 7 years
  const years = ['2019', '2020', '2021', '2022', '2023', '2024', '2025'];
  const values = [70, 75, 82, 95, 110, 125, 137];
  const barProgress = interpolate(frame, [60, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const pctCounter = interpolate(frame, [180, 260], [0, 95.83], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const studentCounter = interpolate(frame, [260, 340], [0, 736580], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,193,7,0.18) 0%, transparent 50%, rgba(255,0,0,0.12) 100%)',
      }} />

      {/* Shape layer - growth arrows */}
      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: `${15 + i * 30}%`,
          top: `${seededRandom(i + 60) * 70}%`,
          width: 200,
          height: 300,
          border: '3px solid #1F150C',
          borderRadius: 30,
          opacity: 0.04,
          transform: `rotate(${frame * 0.08 + i * 60}deg)`,
        }} />
      ))}

      {/* Texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="ptn9" width="35" height="35" patternUnits="userSpaceOnUse">
            <line x1="0" y1="35" x2="35" y2="0" stroke="#080616" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#ptn9)" />
      </svg>

      {/* Particles - rising */}
      {Array.from({ length: 30 }).map((_, i) => {
        const s = seededRandom(i + 1000);
        const yMove = 1080 - ((frame * (1.5 + s) + i * 30) % 1080);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: yMove,
            width: 4,
            height: 12,
            backgroundColor: '#FFC107',
            opacity: 0.4,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: titleIn,
        transform: `translateY(${(1 - titleIn) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 28,
          color: '#FF0000',
          fontWeight: 800,
          letterSpacing: 4,
        }}>
          MARCH 2019 → MARCH 2025
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 85,
          color: '#080616',
          lineHeight: 1,
          marginTop: 8,
        }}>
          एडुकेशन लोन का <span style={{ color: '#FF0000' }}>विस्फोट</span>
        </div>
      </div>

      {/* CHART */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: 260,
        width: 1000,
        height: 500,
        opacity: chartIn,
        transform: `translateY(${(1 - chartIn) * 50}px)`,
      }}>
        {/* Y axis */}
        <div style={{
          position: 'absolute',
          left: 0,
          top: 0,
          bottom: 60,
          width: 4,
          backgroundColor: '#080616',
        }} />
        {/* X axis */}
        <div style={{
          position: 'absolute',
          left: 0,
          right: 0,
          bottom: 60,
          height: 4,
          backgroundColor: '#080616',
        }} />

        {/* Bars */}
        <div style={{
          position: 'absolute',
          left: 30,
          right: 0,
          bottom: 64,
          display: 'flex',
          alignItems: 'flex-end',
          gap: 24,
          height: 380,
        }}>
          {years.map((year, i) => {
            const barH = (values[i] / 137) * 360 * barProgress;
            const isLast = i === years.length - 1;
            return (
              <div key={year} style={{
                flex: 1,
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                gap: 8,
                height: '100%',
                justifyContent: 'flex-end',
              }}>
                <div style={{
                  fontFamily: 'Inter',
                  fontSize: 26,
                  fontWeight: 800,
                  color: isLast ? '#FF0000' : '#1F150C',
                  opacity: barH > 20 ? 1 : 0,
                }}>
                  {values[i]}K
                </div>
                <div style={{
                  width: '100%',
                  height: barH,
                  background: isLast ? '#FF0000' : (i > 3 ? '#FFC107' : '#1F150C'),
                  border: '3px solid #080616',
                  borderBottom: 'none',
                }} />
                <div style={{
                  fontFamily: 'Inter',
                  fontSize: 24,
                  fontWeight: 700,
                  color: '#080616',
                  position: 'absolute',
                  bottom: -42,
                }}>
                  {year}
                </div>
              </div>
            );
          })}
        </div>
      </div>

      {/* Right side - Percentage badge */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: 280,
        width: 620,
        opacity: pctIn,
        transform: `scale(${pctIn})`,
      }}>
        <div style={{
          backgroundColor: '#FF0000',
          padding: 36,
          borderRadius: 24,
          border: '6px solid #080616',
          boxShadow: '10px 10px 0 #FFC107',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 28,
            color: '#F5F5F5',
            fontWeight: 800,
            letterSpacing: 3,
          }}>
            6 SAAL MEIN
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 230,
            color: '#F5F5F5',
            lineHeight: 0.95,
            marginTop: 6,
            letterSpacing: -4,
          }}>
            {pctCounter.toFixed(2)}%
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 42,
            color: '#FFC107',
            fontWeight: 800,
            marginTop: 6,
          }}>
            बढ़ोतरी ↑
          </div>
        </div>
      </div>

      {/* Bottom strip - FY2024 PSB */}
      <div style={{
        position: 'absolute',
        bottom: 70,
        right: 80,
        width: 620,
        opacity: studentIn,
        transform: `translateY(${(1 - studentIn) * 30}px)`,
      }}>
        <div style={{
          backgroundColor: '#080616',
          padding: '22px 30px',
          borderRadius: 16,
          border: '4px solid #FFC107',
          display: 'flex',
          alignItems: 'center',
          gap: 24,
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <circle cx="30" cy="22" r="10" fill="#FFC107" />
            <path d="M12 50 Q30 36 48 50 L48 56 L12 56 Z" fill="#FFC107" />
          </svg>
          <div>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 22,
              color: '#FFC107',
              fontWeight: 700,
              letterSpacing: 2,
            }}>
              FY 2024 · PSB STUDENTS
            </div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 64,
              color: '#DFF1F1',
              lineHeight: 1,
            }}>
              {Math.round(studentCounter).toLocaleString('en-IN')}
            </div>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 24,
              color: '#FF0000',
              fontWeight: 700,
              marginTop: 4,
              opacity: growthBadge,
            }}>
              +17% YoY ↑
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9_Growth;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_QueueAndClock',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_QueueAndClock: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(0);
  const queueIn = sp(60);
  const arrowIn = sp(180);
  const clockIn = sp(240);
  const interestIn = sp(330);

  // Queue walking animation
  const queueShift = (frame * 0.6) % 60;

  // Clock hands rotation
  const minuteRot = (frame * 6) % 360;
  const hourRot = (frame * 0.5) % 360;

  // Interest counter
  const interestVal = interpolate(frame, [350, 430], [0, 2.1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Queue figures
  const queueFigures = Array.from({ length: 9 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(255,193,7,0.18) 0%, rgba(255,0,0,0.15) 100%)',
      }} />

      {/* Shape layer */}
      {[0, 1, 2, 3].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: `${seededRandom(i + 70) * 80}%`,
          top: `${seededRandom(i + 71) * 80}%`,
          width: 220 + i * 40,
          height: 220 + i * 40,
          borderRadius: i % 2 === 0 ? '50%' : 20,
          border: '2px solid #080616',
          opacity: 0.04,
          transform: `rotate(${frame * 0.1 + i * 45}deg)`,
        }} />
      ))}

      {/* Texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="ptn10" width="45" height="45" patternUnits="userSpaceOnUse">
            <circle cx="22" cy="22" r="2" fill="#1F150C" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#ptn10)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 1100);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 100}%`,
            top: `${seededRandom(i + 1200) * 100}%`,
            width: 5,
            height: 5,
            backgroundColor: '#FFC107',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute',
        top: 50,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: titleIn,
        transform: `translateY(${(1 - titleIn) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 78,
          color: '#080616',
          lineHeight: 1,
        }}>
          कतार का <span style={{ color: '#FF0000' }}>दूसरा सिरा</span>
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 32,
          color: '#1F150C',
          fontWeight: 600,
          marginTop: 8,
          opacity: 0.8,
        }}>
          लाखों उत्साहित छात्र · एक छिपा सच
        </div>
      </div>

      {/* LEFT - Queue of students */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: 230,
        width: 700,
        height: 350,
        opacity: queueIn,
      }}>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 28,
          color: '#FFC107',
          fontWeight: 800,
          letterSpacing: 3,
          marginBottom: 16,
        }}>
          STUDENTS IN LINE →
        </div>
        <div style={{
          position: 'relative',
          height: 260,
          backgroundColor: '#F5F5F5',
          border: '5px solid #080616',
          borderRadius: 20,
          overflow: 'hidden',
          boxShadow: '6px 6px 0 #1F150C',
        }}>
          {/* Ground line */}
          <div style={{
            position: 'absolute',
            bottom: 30,
            left: 0,
            right: 0,
            height: 4,
            backgroundColor: '#1F150C',
            opacity: 0.5,
          }} />

          {/* Students */}
          {queueFigures.map((_, i) => {
            const bounce = Math.sin((frame * 0.2) + i) * 4;
            return (
              <div key={i} style={{
                position: 'absolute',
                bottom: 36 + bounce,
                left: 40 + i * 70 - queueShift,
              }}>
                <svg width="56" height="100" viewBox="0 0 56 100">
                  <circle cx="28" cy="22" r="14" fill="#080616" />
                  <rect x="20" y="38" width="16" height="36" rx="3" fill={i % 2 === 0 ? '#FF0000' : '#FFC107'} />
                  <line x1="14" y1="50" x2="20" y2="60" stroke="#080616" strokeWidth="4" strokeLinecap="round" />
                  <line x1="42" y1="50" x2="36" y2="60" stroke="#080616" strokeWidth="4" strokeLinecap="round" />
                  <line x1="22" y1="74" x2="22" y2="92" stroke="#080616" strokeWidth="4" strokeLinecap="round" />
                  <line x1="34" y1="74" x2="34" y2="92" stroke="#080616" strokeWidth="4" strokeLinecap="round" />
                </svg>
              </div>
            );
          })}

          {/* Door/exit on right */}
          <div style={{
            position: 'absolute',
            right: 0,
            top: 0,
            bottom: 0,
            width: 80,
            backgroundColor: '#1F150C',
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 22,
              color: '#FF0000',
              fontWeight: 900,
              writingMode: 'vertical-lr',
              letterSpacing: 4,
            }}>
              ENTRY
            </div>
          </div>
        </div>
        <div style={{
          marginTop: 16,
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 32,
          color: '#1F150C',
          fontWeight: 700,
        }}>
          उत्साहित · उम्मीद से भरे
        </div>
      </div>

      {/* CENTER ARROW */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: 380,
        transform: `translateX(-50%) scale(${arrowIn})`,
        opacity: arrowIn,
        zIndex: 5,
      }}>
        <svg width="120" height="80" viewBox="0 0 120 80">
          <polygon points="0,40 80,40 80,10 120,40 80,70 80,40" fill="#FF0000" stroke="#080616" strokeWidth="4" />
        </svg>
      </div>

      {/* RIGHT - Ticking clock & loan info */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: 230,
        width: 600,
        opacity: clockIn,
        transform: `scale(${clockIn})`,
      }}>
        <div style={{
          backgroundColor: '#080616',
          borderRadius: 24,
          padding: 30,
          border: '5px solid #FF0000',
          boxShadow: '10px 10px 0 #FFC107',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 22,
            color: '#FFC107',
            fontWeight: 800,
            letterSpacing: 3,
            textAlign: 'center',
          }}>
            MORATORIUM PERIOD
          </div>

          {/* Clock */}
          <div style={{ display: 'flex', justifyContent: 'center', marginTop: 16 }}>
            <svg width="200" height="200" viewBox="0 0 200 200">
              <circle cx="100" cy="100" r="92" fill="#DFF1F1" stroke="#FFC107" strokeWidth="5" />
              {[0, 30, 60, 90, 120, 150, 180, 210, 240, 270, 300, 330].map((d) => {
                const rad = (d * Math.PI) / 180;
                return (
                  <line key={d}
                    x1={100 + Math.cos(rad - Math.PI / 2) * 75}
                    y1={100 + Math.sin(rad - Math.PI / 2) * 75}
                    x2={100 + Math.cos(rad - Math.PI / 2) * 85}
                    y2={100 + Math.sin(rad - Math.PI / 2) * 85}
                    stroke="#080616"
                    strokeWidth="3"
                  />
                );
              })}
              {/* Hour hand */}
              <line x1="100" y1="100"
                x2={100 + Math.cos((hourRot - 90) * Math.PI / 180) * 45}
                y2={100 + Math.sin((hourRot - 90) * Math.PI / 180) * 45}
                stroke="#1F150C" strokeWidth="7" strokeLinecap="round" />
              {/* Minute hand */}
              <line x1="100" y1="100"
                x2={100 + Math.cos((minuteRot - 90) * Math.PI / 180) * 65}
                y2={100 + Math.sin((minuteRot - 90) * Math.PI / 180) * 65}
                stroke="#FF0000" strokeWidth="5" strokeLinecap="round" />
              <circle cx="100" cy="100" r="7" fill="#FF0000" />
            </svg>
          </div>

          {/* Loan + interest info */}
          <div style={{
            marginTop: 20,
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
          }}>
            <div style={{ textAlign: 'center' }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 18,
                color: '#9E9E9E',
                fontWeight: 600,
              }}>
                LOAN
              </div>
              <div style={{
                fontFamily: 'Yantra One',
                fontSize: 51,
                color: '#FFC107',
              }}>
                ₹20L
              </div>
            </div>
            <div style={{
              fontSize: 32,
              color: '#FF0000',
              fontWeight: 900,
            }}>×</div>
            <div style={{ textAlign: 'center' }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 18,
                color: '#9E9E9E',
                fontWeight: 600,
              }}>
                @ RATE
              </div>
              <div style={{
                fontFamily: 'Yantra One',
                fontSize: 51,
                color: '#FF0000',
              }}>
                10.5%
              </div>
            </div>
          </div>

          {/* Yearly interest */}
          <div style={{
            marginTop: 20,
            backgroundColor: '#FF0000',
            padding: 18,
            borderRadius: 12,
            textAlign: 'center',
            opacity: interestIn,
            transform: `translateY(${(1 - interestIn) * 20}px)`,
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 18,
              color: '#FFC107',
              fontWeight: 700,
              letterSpacing: 2,
            }}>
              SILENT YEARLY INTEREST
            </div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 72,
              color: '#F5F5F5',
              lineHeight: 1,
              marginTop: 4,
            }}>
              ₹{interestVal.toFixed(1)}L
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_QueueAndClock;
```

