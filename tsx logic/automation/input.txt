```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_PehliSeekh',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_PehliSeekh: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Slow camera zoom-in
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.07], {
    extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });

  // Word-by-word stagger for the question
  const words = ['पैसे', 'की', 'पहली', 'सीख'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#06923E', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at 30% 20%, #0BBF57 0%, #06923E 45%, #045A28 100%)' }} />

      {/* Punk collage torn-paper strip (top) */}
      <div style={{
        position: 'absolute', top: -40, left: -60, width: 2100, height: 160,
        background: '#000000', opacity: 0.9,
        clipPath: 'polygon(0 0, 100% 0, 100% 55%, 92% 70%, 80% 50%, 66% 75%, 50% 55%, 34% 78%, 20% 58%, 8% 72%, 0 52%)',
        transform: `translateY(${interpolate(sp(0, { damping: 14 }), [0, 1], [-160, 0])}px)`,
      }} />

      {/* Animated shape layers — drifting transparent geometry */}
      <div style={{
        position: 'absolute', width: 620, height: 620, borderRadius: '50%',
        border: '3px solid #F4F2F2', opacity: 0.06,
        left: 1350, top: 520,
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.03) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 400, height: 400,
        background: '#000000', opacity: 0.05,
        left: -120 + Math.sin(frame * 0.02) * 20, top: 620,
        transform: `rotate(${12 + frame * 0.05}deg)`,
      }} />

      {/* Halftone dot texture (punk print feel) */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(#000000 1.6px, transparent 1.6px)',
        backgroundSize: '30px 30px', opacity: 0.06,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const x = seededRandom(i + 1) * 1920;
        const drift = ((frame * (0.4 + seededRandom(i + 7))) % 1200);
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1120 - drift,
            width: 5 + seededRandom(i + 3) * 6, height: 5 + seededRandom(i + 3) * 6,
            borderRadius: '50%', background: i % 3 === 0 ? '#FFD52D' : '#F4F2F2',
            opacity: 0.18,
          }} />
        );
      })}

      {/* Main content — camera wrapper */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Kicker tag — collage sticker card */}
        <div style={{
          background: '#000000', color: '#FFD52D',
          fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800,
          fontSize: 44, padding: '10px 36px',
          transform: `rotate(-2deg) scale(${sp(5)})`,
          boxShadow: '8px 8px 0 rgba(0,0,0,0.35)', marginBottom: 34,
        }}>
          एक सवाल से शुरू —
        </div>

        {/* Hero line — word by word spring pop */}
        <div style={{ display: 'flex', gap: 30 }}>
          {words.map((w, i) => (
            <span key={i} style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 160, color: '#F4F2F2', fontWeight: 900,
              textShadow: '10px 10px 0 rgba(0,0,0,0.55)',
              display: 'inline-block',
              transform: `translateY(${interpolate(sp(20 + i * 8), [0, 1], [90, 0])}px) rotate(${Math.sin(frame * 0.05 + i) * 0.8}deg)`,
              opacity: sp(20 + i * 8),
            }}>{w}</span>
          ))}
        </div>

        {/* Sub line on black collage card */}
        <div style={{
          marginTop: 40, background: '#F4F2F2', color: '#000000',
          fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800,
          fontSize: 64, padding: '14px 50px',
          border: '5px solid #000000', boxShadow: '12px 12px 0 #000000',
          transform: `rotate(1.5deg) scale(${sp(60, { damping: 10 })})`,
        }}>
          आपको किसने दी थी?
        </div>

        {/* Giant question mark — spring + wobble micro-animation */}
        <div style={{
          position: 'absolute', right: 150, top: 120,
          fontFamily: "'Yantra One', sans-serif", fontSize: 300, fontWeight: 900,
          color: '#FFD52D', textShadow: '12px 12px 0 #000000',
          transform: `scale(${sp(95, { damping: 8 })}) rotate(${Math.sin(frame * 0.09) * 6}deg)`,
          opacity: sp(95),
        }}>?</div>

        {/* Coin SVG icon — flip micro-animation */}
        <svg width="130" height="130" viewBox="0 0 100 100" style={{
          position: 'absolute', left: 170, bottom: 190,
          transform: `scale(${sp(110)}) rotateY(${frame * 4}deg)`,
        }}>
          <circle cx="50" cy="50" r="44" fill="#FFD52D" stroke="#000000" strokeWidth="6" />
          <text x="50" y="66" textAnchor="middle" fontSize="46" fontWeight="900" fill="#000000" fontFamily="Inter, sans-serif">₹</text>
        </svg>
      </AbsoluteFill>

      {/* Yellow tape strips — collage detail */}
      <div style={{ position: 'absolute', top: 150, left: 240, width: 150, height: 36, background: '#FFD52D', opacity: 0.85, transform: 'rotate(-24deg)' }} />
      <div style={{ position: 'absolute', bottom: 130, right: 300, width: 150, height: 36, background: '#FFD52D', opacity: 0.85, transform: 'rotate(18deg)' }} />

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.42) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }),
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene01_PehliSeekh;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_TeenSalah',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_TeenSalah: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan left→right
  const camX = interpolate(frame, [0, durationInFrames], [30, -30], { extrapolateRight: 'clamp' });

  // Three advice cards data — SVG icon per card
  const cards = [
    { who: 'पिताजी', line: '"पढ़ाई करो, नौकरी लगेगी"', delay: 15, rot: -3, icon: 'book' },
    { who: 'माँ', line: '"सोना खरीद लो"', delay: 33, rot: 2, icon: 'gold' },
    { who: 'चाचा', line: '"सरकारी नौकरी सबसे बड़ी"', delay: 51, rot: -2, icon: 'govt' },
  ];

  const renderIcon = (type: string) => {
    if (type === 'book') return (
      <svg width="70" height="70" viewBox="0 0 100 100">
        <path d="M15 20 h32 a8 8 0 0 1 8 8 v55 a8 8 0 0 0 -8 -8 h-32 z" fill="#F4F2F2" stroke="#000" strokeWidth="5" />
        <path d="M85 20 h-32 a8 8 0 0 0 -8 8 v55 a8 8 0 0 1 8 -8 h32 z" fill="#FFD52D" stroke="#000" strokeWidth="5" />
      </svg>
    );
    if (type === 'gold') return (
      <svg width="70" height="70" viewBox="0 0 100 100">
        <path d="M25 60 h50 l8 22 h-66 z" fill="#FFD52D" stroke="#000" strokeWidth="5" />
        <path d="M32 34 h36 l7 20 h-50 z" fill="#FFD52D" stroke="#000" strokeWidth="5" />
      </svg>
    );
    return (
      <svg width="70" height="70" viewBox="0 0 100 100">
        <rect x="18" y="45" width="64" height="38" fill="#F4F2F2" stroke="#000" strokeWidth="5" />
        <path d="M12 45 L50 18 L88 45 Z" fill="#FFD52D" stroke="#000" strokeWidth="5" />
        <rect x="42" y="58" width="16" height="25" fill="#000" />
      </svg>
    );
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#06923E', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background layer — shifted vs previous scene */}
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #045A28 0%, #06923E 50%, #0BBF57 100%)' }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        background: '#000000', opacity: 0.05,
        left: -300, top: -350, transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 0, height: 0, opacity: 0.06,
        borderLeft: '260px solid transparent', borderRight: '260px solid transparent',
        borderBottom: '440px solid #F4F2F2',
        right: -140, bottom: -160, transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Motion texture — diagonal lines */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(45deg, #000 0, #000 2px, transparent 2px, transparent 26px)',
        opacity: 0.05,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 9 }).map((_, i) => {
        const x = seededRandom(i + 11) * 1920;
        const drift = ((frame * (0.35 + seededRandom(i + 5))) % 1150);
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1120 - drift,
            width: 6, height: 6, background: '#FFD52D', opacity: 0.2,
            transform: `rotate(${frame * 2}deg)`,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Headline */}
        <div style={{
          fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
          fontSize: 100, color: '#F4F2F2', fontWeight: 900,
          textShadow: '8px 8px 0 rgba(0,0,0,0.5)', marginBottom: 55,
          transform: `translateY(${interpolate(sp(0), [0, 1], [-70, 0])}px)`, opacity: sp(0),
        }}>
          घर की तीन सलाहें
        </div>

        {/* Punk collage advice cards — staggered spring entrance */}
        <div style={{ display: 'flex', gap: 55 }}>
          {cards.map((c, i) => (
            <div key={i} style={{
              width: 440, background: '#F4F2F2', border: '6px solid #000000',
              boxShadow: '14px 14px 0 #000000', padding: '34px 30px',
              display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 18,
              transform: `rotate(${c.rot + Math.sin(frame * 0.06 + i * 2) * 0.7}deg) translateY(${interpolate(sp(c.delay, { damping: 10 }), [0, 1], [400, 0])}px)`,
              opacity: sp(c.delay),
            }}>
              {/* Tape strip on card */}
              <div style={{ position: 'absolute', top: -20, width: 120, height: 30, background: '#FFD52D', opacity: 0.9, transform: 'rotate(-5deg)' }} />
              <div style={{ transform: `translateY(${Math.sin(frame * 0.08 + i) * 3}px)` }}>{renderIcon(c.icon)}</div>
              <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 58, color: '#000000', fontWeight: 900 }}>{c.who}</div>
              <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 38, color: '#000000', fontWeight: 700, textAlign: 'center' }}>{c.line}</div>
            </div>
          ))}
        </div>

        {/* Payoff line — slides up late */}
        <div style={{
          marginTop: 60, background: '#000000', color: '#FFD52D',
          fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800,
          fontSize: 54, padding: '14px 46px', transform: `rotate(-1deg) scale(${sp(210, { damping: 9 })})`,
          boxShadow: '10px 10px 0 rgba(0,0,0,0.3)',
        }}>
          किसी ने झूठ नहीं बोला — वो उनके ज़माने का सच था
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.42) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }),
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene02_TeenSalah;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_WohZamana',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_WohZamana: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Count-up: 0 → 40 साल
  const count = Math.round(interpolate(frame, [40, 100], [0, 40], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  }));

  // STAMP slam at frame 180 + screen shake on impact
  const stampIn = sp(180, { damping: 9, stiffness: 200 });
  const shake = frame >= 180 && frame <= 196 ? Math.sin(frame * 3.5) * interpolate(frame, [180, 196], [11, 0], { extrapolateRight: 'clamp' }) : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#06923E', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at 70% 80%, #0BBF57 0%, #06923E 40%, #034D22 100%)' }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 700, height: 700, border: '4px solid #000000',
        opacity: 0.06, left: 1280, top: -260,
        transform: `rotate(${45 + frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 460, height: 460, borderRadius: '50%',
        background: '#F4F2F2', opacity: 0.04,
        left: -140, bottom: -140, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
      }} />

      {/* Motion texture — grid */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(#000 1px, transparent 1px), linear-gradient(90deg, #000 1px, transparent 1px)',
        backgroundSize: '90px 90px', opacity: 0.05,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 8 }).map((_, i) => {
        const x = seededRandom(i + 21) * 1920;
        const drift = ((frame * (0.3 + seededRandom(i + 9))) % 1150);
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1120 - drift,
            width: 7, height: 7, borderRadius: '50%',
            background: '#F4F2F2', opacity: 0.15,
          }} />
        );
      })}

      {/* Main content with impact shake */}
      <AbsoluteFill style={{ transform: `translateX(${shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Old-world card: office building icon + 40 साल counter */}
        <div style={{
          display: 'flex', alignItems: 'center', gap: 60,
          background: '#F4F2F2', border: '6px solid #000000', boxShadow: '16px 16px 0 #000000',
          padding: '44px 70px', transform: `rotate(-1.5deg) scale(${sp(10, { damping: 11 })})`,
        }}>
          {/* Sarkari daftar SVG icon — micro bob */}
          <svg width="170" height="170" viewBox="0 0 100 100" style={{ transform: `translateY(${Math.sin(frame * 0.07) * 4}px)` }}>
            <rect x="14" y="38" width="72" height="48" fill="#06923E" stroke="#000" strokeWidth="5" />
            <path d="M8 38 L50 12 L92 38 Z" fill="#FFD52D" stroke="#000" strokeWidth="5" />
            <rect x="24" y="48" width="12" height="14" fill="#F4F2F2" stroke="#000" strokeWidth="3" />
            <rect x="64" y="48" width="12" height="14" fill="#F4F2F2" stroke="#000" strokeWidth="3" />
            <rect x="42" y="58" width="16" height="28" fill="#000" />
          </svg>
          <div>
            {/* Animated counter — the honest math of the old era */}
            <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 190, fontWeight: 900, color: '#000000', lineHeight: 1 }}>
              {count} <span style={{ fontSize: 90 }}>साल</span>
            </div>
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 46, fontWeight: 800, color: '#000000' }}>
              एक ही सरकारी दफ़्तर — एक ही कुर्सी
            </div>
          </div>
        </div>

        {/* Context line */}
        <div style={{
          marginTop: 55, fontFamily: "'Noto Sans Devanagari', sans-serif",
          fontSize: 60, fontWeight: 800, color: '#F4F2F2',
          textShadow: '6px 6px 0 rgba(0,0,0,0.5)',
          opacity: sp(80), transform: `translateY(${interpolate(sp(80), [0, 1], [50, 0])}px)`,
        }}>
          वो सलाह <span style={{ color: '#FFD52D' }}>उस भारत</span> के लिए बनी थी
        </div>

        {/* Punk rubber-stamp slam: "अब है ही नहीं" */}
        <div style={{
          position: 'absolute', top: 90, right: 130,
          border: '10px solid #000000', color: '#000000', background: 'rgba(255,213,45,0.94)',
          fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
          fontSize: 88, fontWeight: 900, padding: '10px 40px',
          transform: `rotate(-14deg) scale(${interpolate(stampIn, [0, 1], [2.6, 1])})`,
          opacity: stampIn,
        }}>
          अब है ही नहीं
        </div>
      </AbsoluteFill>

      {/* Torn black collage corner */}
      <div style={{
        position: 'absolute', bottom: -30, left: -40, width: 560, height: 200, background: '#000000', opacity: 0.85,
        clipPath: 'polygon(0 100%, 0 30%, 12% 45%, 26% 20%, 42% 48%, 58% 25%, 74% 52%, 88% 30%, 100% 55%, 100% 100%)',
      }} />

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.45) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }),
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene03_WohZamana;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_KhatamList',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_KhatamList: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Items get struck out one by one — multi-beat reveal
  const items = [
    { label: 'पेंशन वाली दुनिया', inDelay: 10, strikeAt: 55 },
    { label: 'नौकरी की गारंटी', inDelay: 28, strikeAt: 100 },
    { label: 'मेट्रो में सस्ता घर', inDelay: 46, strikeAt: 145 },
  ];

  // Screen shake on each strike impact
  const shakeSrc = items.reduce((acc, it) => {
    if (frame >= it.strikeAt && frame <= it.strikeAt + 12) {
      return Math.sin(frame * 3) * interpolate(frame, [it.strikeAt, it.strikeAt + 12], [9, 0], { extrapolateRight: 'clamp' });
    }
    return acc;
  }, 0);

  return (
    <AbsoluteFill style={{ backgroundColor: '#06923E', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #034D22 0%, #06923E 55%, #0BBF57 100%)' }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 800, height: 800, borderRadius: '50%',
        border: '3px dashed #F4F2F2', opacity: 0.07,
        right: -280, top: 200, transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 340, height: 340, background: '#FFD52D', opacity: 0.05,
        left: -100, top: 100, transform: `rotate(${frame * 0.06}deg)`,
      }} />

      {/* Motion texture — vertical bars */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(90deg, #000 0, #000 2px, transparent 2px, transparent 60px)',
        opacity: 0.04,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 9 }).map((_, i) => {
        const x = seededRandom(i + 31) * 1920;
        const drift = ((frame * (0.4 + seededRandom(i + 13))) % 1150);
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1120 - drift,
            width: 6, height: 6, background: i % 2 ? '#F4F2F2' : '#FFD52D', opacity: 0.18,
            transform: 'rotate(45deg)',
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${shakeSrc}px)`, justifyContent: 'center', alignItems: 'center', gap: 34 }}>
        {/* Strike-out list — checklist of a dead era */}
        {items.map((it, i) => {
          const strike = interpolate(frame, [it.strikeAt, it.strikeAt + 14], [0, 100], {
            extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.quad),
          });
          const struck = frame > it.strikeAt + 8;
          return (
            <div key={i} style={{
              position: 'relative', width: 1000,
              background: struck ? '#2D2D2D' : '#F4F2F2',
              border: '6px solid #000000', boxShadow: '12px 12px 0 #000000',
              padding: '26px 46px', display: 'flex', alignItems: 'center', gap: 34,
              transform: `rotate(${i % 2 ? 1.2 : -1.2}deg) translateX(${interpolate(sp(it.inDelay, { damping: 11 }), [0, 1], [i % 2 ? 900 : -900, 0])}px)`,
            }}>
              {/* X mark SVG — pops on strike */}
              <svg width="64" height="64" viewBox="0 0 100 100" style={{ transform: `scale(${interpolate(strike, [0, 100], [0, 1])})` }}>
                <line x1="18" y1="18" x2="82" y2="82" stroke="#FFD52D" strokeWidth="16" strokeLinecap="round" />
                <line x1="82" y1="18" x2="18" y2="82" stroke="#FFD52D" strokeWidth="16" strokeLinecap="round" />
              </svg>
              <span style={{
                fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 62, fontWeight: 800,
                color: struck ? '#9E9E9E' : '#000000',
              }}>{it.label}</span>
              {/* Animated strike line — width grows */}
              <div style={{
                position: 'absolute', left: 130, top: '50%', height: 10, background: '#000000',
                width: `${strike * 0.78}%`, transform: 'translateY(-50%) rotate(-1deg)',
              }} />
            </div>
          );
        })}

        {/* Payoff — the advice never updated */}
        <div style={{
          marginTop: 26, background: '#000000', color: '#F4F2F2',
          fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
          fontSize: 66, fontWeight: 900, padding: '16px 56px',
          transform: `rotate(-1deg) scale(${sp(195, { damping: 9 })})`,
        }}>
          लेकिन सलाह — <span style={{ color: '#FFD52D' }}>आज भी वही</span>
        </div>
      </AbsoluteFill>

      {/* Tape strips */}
      <div style={{ position: 'absolute', top: 100, right: 220, width: 140, height: 32, background: '#FFD52D', opacity: 0.85, transform: 'rotate(28deg)' }} />

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.42) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }),
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene04_KhatamList;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_DoosriDuniya',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_DoosriDuniya: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Slow orbit camera: subtle rotate + zoom
  const camRot = Math.sin(frame * 0.008) * 1.2;
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1], { extrapolateRight: 'clamp' });

  // Dotted journey path draw-on
  const pathDraw = interpolate(frame, [90, 200], [900, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#06923E', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background layer — darker, hushed world */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at 50% 30%, #045A28 0%, #023316 80%)' }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 1100, height: 1100, borderRadius: '50%',
        border: '2px solid #FFD52D', opacity: 0.06,
        left: 410, top: -260, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.03}) rotate(${frame * 0.05}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500, background: '#0BBF57', opacity: 0.05,
        left: -160, bottom: -160, borderRadius: '50%',
      }} />

      {/* Motion texture — fine halftone */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(#F4F2F2 1.2px, transparent 1.2px)',
        backgroundSize: '24px 24px', opacity: 0.05,
      }} />

      {/* Floating particles — dust in old godowns */}
      {Array.from({ length: 14 }).map((_, i) => {
        const x = seededRandom(i + 41) * 1920;
        const drift = ((frame * (0.2 + seededRandom(i + 17) * 0.5)) % 1150);
        return (
          <div key={i} style={{
            position: 'absolute', left: x + Math.sin(frame * 0.03 + i) * 24, top: 1120 - drift,
            width: 4, height: 4, borderRadius: '50%', background: '#FFD52D', opacity: 0.25,
          }} />
        );
      })}

      {/* Main content — camera wrapper */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Whisper headline */}
        <div style={{
          fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
          fontSize: 108, fontWeight: 900, color: '#F4F2F2',
          textShadow: '8px 8px 0 rgba(0,0,0,0.6)', marginBottom: 12,
          opacity: sp(5), transform: `scale(${interpolate(sp(5), [0, 1], [0.8, 1])})`,
        }}>
          एक <span style={{ color: '#FFD52D' }}>दूसरी दुनिया</span>
        </div>
        <div style={{
          fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 48, fontWeight: 700,
          color: '#F4F2F2', opacity: sp(35) * 0.9, marginBottom: 60,
        }}>
          बिना किसी शोर के — सदियों से
        </div>

        {/* Journey path connecting two location cards */}
        <svg width="1400" height="120" style={{ position: 'absolute', top: 500, left: 260 }}>
          <path d="M 60 90 Q 700 -30 1340 90" fill="none" stroke="#FFD52D" strokeWidth="5"
            strokeDasharray="18 14" strokeDashoffset={pathDraw} opacity={0.8} />
        </svg>

        {/* Location card: Surat diamond market */}
        <div style={{
          position: 'absolute', left: 180, top: 560, width: 520,
          background: '#F4F2F2', border: '6px solid #000000', boxShadow: '14px 14px 0 #000000',
          padding: '30px 34px', display: 'flex', alignItems: 'center', gap: 26,
          transform: `rotate(-2deg) translateY(${interpolate(sp(70, { damping: 10 }), [0, 1], [300, 0])}px)`,
          opacity: sp(70),
        }}>
          {/* Diamond SVG — sparkle micro-animation */}
          <svg width="96" height="96" viewBox="0 0 100 100" style={{ transform: `rotate(${Math.sin(frame * 0.06) * 5}deg)` }}>
            <path d="M25 20 h50 l18 25 -43 40 -43 -40 z" fill="#0BBF57" stroke="#000" strokeWidth="5" />
            <path d="M25 20 L50 45 L75 20" fill="none" stroke="#000" strokeWidth="4" />
            <circle cx="78" cy="14" r={4 + Math.abs(Math.sin(frame * 0.15)) * 4} fill="#FFD52D" />
          </svg>
          <div>
            <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 54, fontWeight: 900, color: '#000000' }}>सूरत</div>
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 34, fontWeight: 700, color: '#000000' }}>हीरा बाज़ार की पुरानी गद्दियाँ</div>
          </div>
        </div>

        {/* Location card: Chandni Chowk cloth godowns */}
        <div style={{
          position: 'absolute', right: 180, top: 560, width: 560,
          background: '#F4F2F2', border: '6px solid #000000', boxShadow: '14px 14px 0 #000000',
          padding: '30px 34px', display: 'flex', alignItems: 'center', gap: 26,
          transform: `rotate(2deg) translateY(${interpolate(sp(130, { damping: 10 }), [0, 1], [300, 0])}px)`,
          opacity: sp(130),
        }}>
          {/* Cloth roll SVG — gentle sway */}
          <svg width="96" height="96" viewBox="0 0 100 100" style={{ transform: `translateY(${Math.sin(frame * 0.08) * 3}px)` }}>
            <rect x="14" y="30" width="60" height="16" rx="8" fill="#FFD52D" stroke="#000" strokeWidth="5" />
            <rect x="22" y="48" width="60" height="16" rx="8" fill="#0BBF57" stroke="#000" strokeWidth="5" />
            <rect x="14" y="66" width="60" height="16" rx="8" fill="#F4F2F2" stroke="#000" strokeWidth="5" />
          </svg>
          <div>
            <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 54, fontWeight: 900, color: '#000000' }}>चांदनी चौक</div>
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 34, fontWeight: 700, color: '#000000' }}>कपड़ा व्यापारियों के गोदाम</div>
          </div>
        </div>

        {/* Location pins dropping on cards */}
        {[{ x: 420, d: 100 }, { x: 1430, d: 160 }].map((p, i) => (
          <svg key={i} width="60" height="76" viewBox="0 0 60 76" style={{
            position: 'absolute', left: p.x, top: 460,
            transform: `translateY(${interpolate(sp(p.d, { damping: 8 }), [0, 1], [-260, 0])}px)`,
            opacity: sp(p.d),
          }}>
            <path d="M30 76 C30 76 6 42 6 26 a24 24 0 1 1 48 0 C54 42 30 76 30 76 Z" fill="#FFD52D" stroke="#000" strokeWidth="5" />
            <circle cx="30" cy="26" r="9" fill="#000" />
          </svg>
        ))}
      </AbsoluteFill>

      {/* Torn collage edge bottom */}
      <div style={{
        position: 'absolute', bottom: -20, right: -40, width: 700, height: 150, background: '#000000', opacity: 0.8,
        clipPath: 'polygon(0 60%, 10% 35%, 22% 62%, 36% 30%, 52% 58%, 66% 32%, 80% 60%, 92% 38%, 100% 55%, 100% 100%, 0 100%)',
      }} />

      {/* Vignette overlay — heavier for hushed mood */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }),
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene05_DoosriDuniya;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_HisaabKiBaithak',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_HisaabKiBaithak: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Split-panel wipe entrances
  const leftWipe = interpolate(sp(8, { damping: 14 }), [0, 1], [-980, 0]);
  const rightWipe = interpolate(sp(55, { damping: 14 }), [0, 1], [980, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#06923E', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'linear-gradient(100deg, #034D22 0%, #06923E 60%, #0BBF57 100%)' }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 640, height: 640, borderRadius: '50%',
        background: '#FFD52D', opacity: 0.04,
        left: 640, top: 220, transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
      }} />

      {/* Motion texture — ledger ruled lines */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(0deg, #000 0, #000 1.5px, transparent 1.5px, transparent 54px)',
        opacity: 0.05,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 8 }).map((_, i) => {
        const x = seededRandom(i + 51) * 1920;
        const drift = ((frame * (0.5 + seededRandom(i + 23))) % 1150);
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1120 - drift,
            width: 5, height: 5, background: '#F4F2F2', opacity: 0.2, borderRadius: '50%',
          }} />
        );
      })}

      {/* Center divider — zig-zag punk seam */}
      <div style={{
        position: 'absolute', left: 945, top: 0, width: 30, height: 1080, background: '#000000', opacity: 0.9,
        clipPath: 'polygon(0 0, 100% 4%, 20% 12%, 100% 22%, 10% 34%, 90% 44%, 15% 56%, 100% 66%, 20% 78%, 90% 88%, 30% 100%, 0 100%)',
        transform: `scaleY(${sp(0)})`, transformOrigin: 'top',
      }} />

      {/* LEFT PANEL — Chennai hotel counter */}
      <div style={{
        position: 'absolute', left: 90, top: 190, width: 780, transform: `translateX(${leftWipe}px)`,
      }}>
        <div style={{
          background: '#F4F2F2', border: '6px solid #000000', boxShadow: '14px 14px 0 #000000',
          padding: '42px 44px', transform: `rotate(-1.5deg)`,
          display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 22,
        }}>
          {/* Ledger + counter SVG — page flip micro-animation */}
          <svg width="150" height="150" viewBox="0 0 100 100">
            <rect x="16" y="22" width="68" height="60" rx="4" fill="#FFD52D" stroke="#000" strokeWidth="5" />
            <line x1="50" y1="22" x2="50" y2="82" stroke="#000" strokeWidth="4" />
            {[36, 48, 60, 70].map((y, i) => (
              <line key={i} x1="24" y1={y} x2="44" y2={y} stroke="#000" strokeWidth="3"
                opacity={frame > 30 + i * 10 ? 1 : 0.15} />
            ))}
            {[36, 48, 60, 70].map((y, i) => (
              <line key={i} x1="56" y1={y} x2="76" y2={y} stroke="#000" strokeWidth="3"
                opacity={frame > 50 + i * 10 ? 1 : 0.15} />
            ))}
          </svg>
          <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 62, fontWeight: 900, color: '#000000' }}>चेन्नई</div>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 40, fontWeight: 700, color: '#000000', textAlign: 'center' }}>
            होटल काउंटर के पीछे — बाप बेटे को हिसाब समझाता है
          </div>
        </div>
      </div>

      {/* RIGHT PANEL — Marwari baithak */}
      <div style={{
        position: 'absolute', right: 90, top: 190, width: 780, transform: `translateX(${rightWipe}px)`,
      }}>
        <div style={{
          background: '#000000', border: '6px solid #F4F2F2', boxShadow: '14px 14px 0 rgba(0,0,0,0.4)',
          padding: '42px 44px', transform: `rotate(1.5deg)`,
          display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 22,
        }}>
          {/* Baithak rug + generations SVG — breathing */}
          <svg width="150" height="150" viewBox="0 0 100 100" style={{ transform: `translateY(${Math.sin(frame * 0.07) * 3}px)` }}>
            <ellipse cx="50" cy="78" rx="40" ry="10" fill="#06923E" stroke="#F4F2F2" strokeWidth="4" />
            <circle cx="32" cy="52" r="10" fill="#FFD52D" stroke="#F4F2F2" strokeWidth="3" />
            <rect x="24" y="62" width="16" height="14" rx="6" fill="#FFD52D" stroke="#F4F2F2" strokeWidth="3" />
            <circle cx="68" cy="52" r="10" fill="#F4F2F2" stroke="#F4F2F2" strokeWidth="3" />
            <rect x="60" y="62" width="16" height="14" rx="6" fill="#F4F2F2" strokeWidth="3" />
            <path d="M44 56 h12" stroke="#FFD52D" strokeWidth="3" strokeDasharray="4 3"
              strokeDashoffset={-frame * 0.5} />
          </svg>
          <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 62, fontWeight: 900, color: '#FFD52D' }}>मारवाड़ी बैठकें</div>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 40, fontWeight: 700, color: '#F4F2F2', textAlign: 'center' }}>
            राजस्थान से निकले — देश के हर कोने में दुकानें
          </div>
        </div>
      </div>

      {/* Bottom ribbon */}
      <div style={{
        position: 'absolute', bottom: 70, left: 0, right: 0, display: 'flex', justifyContent: 'center',
      }}>
        <div style={{
          background: '#FFD52D', color: '#000000', border: '5px solid #000000',
          fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 48, fontWeight: 800,
          padding: '10px 44px', transform: `rotate(-1deg) scale(${sp(150, { damping: 9 })})`,
          boxShadow: '10px 10px 0 #000000',
        }}>
          यही बातचीत सदियों से चल रही है
        </div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.45) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }),
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene06_HisaabKiBaithak;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_GumnaamCrorepati',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_GumnaamCrorepati: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow tilt-zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { extrapolateRight: 'clamp' });

  // "नहीं था" tags
  const noTags = [
    { t: 'विरासत', d: 95 },
    { t: 'अमीर रिश्तेदार', d: 120 },
    { t: 'जुगाड़', d: 145 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#06923E', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at 50% 110%, #0BBF57 0%, #06923E 45%, #023316 100%)' }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 760, height: 760,
        border: '3px solid #FFD52D', borderRadius: '50%', opacity: 0.07,
        left: 580, top: 160, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.04}) rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 380, height: 380, background: '#000000', opacity: 0.06,
        right: -110, top: -110, transform: `rotate(${frame * 0.07}deg)`,
      }} />

      {/* Motion texture — concentric circles */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-radial-gradient(circle at 50% 50%, transparent 0, transparent 70px, rgba(0,0,0,0.05) 70px, rgba(0,0,0,0.05) 72px)',
      }} />

      {/* Floating particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const x = seededRandom(i + 61) * 1920;
        const drift = ((frame * (0.35 + seededRandom(i + 29))) % 1150);
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1120 - drift,
            width: 6, height: 6, borderRadius: '50%',
            background: i % 3 === 0 ? '#FFD52D' : '#F4F2F2', opacity: 0.18,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Anonymous silhouette card — face hidden by question block */}
        <div style={{
          display: 'flex', alignItems: 'center', gap: 70,
          transform: `translateY(${interpolate(sp(5, { damping: 11 }), [0, 1], [120, 0])}px)`, opacity: sp(5),
        }}>
          {/* Faceless figure SVG inside collage frame */}
          <div style={{
            background: '#F4F2F2', border: '6px solid #000000', boxShadow: '14px 14px 0 #000000',
            padding: 26, transform: `rotate(-2deg)`,
          }}>
            <svg width="220" height="260" viewBox="0 0 100 120">
              <circle cx="50" cy="34" r="22" fill="#2D2D2D" />
              <path d="M14 118 a36 40 0 0 1 72 0 z" fill="#2D2D2D" />
              {/* Question block over the face — identity unknown */}
              <rect x="30" y="16" width="40" height="36" fill="#FFD52D" stroke="#000" strokeWidth="4"
                transform={`rotate(${Math.sin(frame * 0.07) * 4} 50 34)`} />
              <text x="50" y="44" textAnchor="middle" fontSize="28" fontWeight="900" fill="#000" fontFamily="Inter, sans-serif">?</text>
            </svg>
          </div>
          <div>
            <div style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 128, fontWeight: 900, color: '#F4F2F2', lineHeight: 1.05,
              textShadow: '10px 10px 0 rgba(0,0,0,0.55)',
            }}>
              गुमनाम<br /><span style={{ color: '#FFD52D' }}>करोड़पति</span>
            </div>
            <div style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 44, fontWeight: 700,
              color: '#F4F2F2', marginTop: 14, opacity: sp(55),
            }}>
              जिनका नाम आप कभी नहीं सुनेंगे
            </div>
          </div>
        </div>

        {/* "ना" tags — punk stickers popping in */}
        <div style={{ display: 'flex', gap: 40, marginTop: 60 }}>
          {noTags.map((n, i) => (
            <div key={i} style={{
              background: '#000000', color: '#F4F2F2', border: '4px solid #F4F2F2',
              fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 46, fontWeight: 800,
              padding: '10px 34px', position: 'relative',
              transform: `rotate(${i % 2 ? 2 : -2}deg) scale(${sp(n.d, { damping: 8 })})`,
            }}>
              ना {n.t}
              {/* Slash mark micro-detail */}
              <div style={{
                position: 'absolute', left: '8%', top: '48%', width: '84%', height: 6,
                background: '#FFD52D', transform: `rotate(-6deg) scaleX(${sp(n.d + 12)})`, transformOrigin: 'left',
              }} />
            </div>
          ))}
        </div>

        {/* Payoff card — rules older than the market */}
        <div style={{
          marginTop: 55, background: '#FFD52D', color: '#000000', border: '6px solid #000000',
          fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
          fontSize: 58, fontWeight: 900, padding: '16px 52px',
          boxShadow: '12px 12px 0 #000000',
          transform: `rotate(1deg) translateY(${interpolate(sp(200, { damping: 10 }), [0, 1], [200, 0])}px)`,
          opacity: sp(200),
        }}>
          सिर्फ़ नियम — शेयर बाज़ार के जन्म से भी पुराने
        </div>
      </AbsoluteFill>

      {/* Tape strips */}
      <div style={{ position: 'absolute', top: 120, left: 180, width: 150, height: 34, background: '#FFD52D', opacity: 0.8, transform: 'rotate(-20deg)' }} />
      <div style={{ position: 'absolute', bottom: 160, right: 200, width: 130, height: 30, background: '#F4F2F2', opacity: 0.5, transform: 'rotate(14deg)' }} />

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }),
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene07_GumnaamCrorepati;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_MainKunalHoon',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_MainKunalHoon: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Digging pickaxe swing — repeating micro-animation
  const dig = Math.sin(frame * 0.18) * 22;

  // "6" counter badge pulse
  const pulse = 1 + Math.sin(frame * 0.12) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#06923E', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #0BBF57 0%, #06923E 40%, #034D22 100%)' }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 980, height: 980, borderRadius: '50%',
        border: '3px solid #F4F2F2', opacity: 0.05,
        left: -300, top: 300, transform: `rotate(${frame * 0.06}deg) scale(${1 + Math.sin(frame * 0.025) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 420, height: 420, background: '#FFD52D', opacity: 0.05,
        right: -120, bottom: 200, transform: `rotate(${30 + frame * 0.09}deg)`,
      }} />

      {/* Motion texture — cross-hatch */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(45deg, #000 0, #000 1.5px, transparent 1.5px, transparent 40px), repeating-linear-gradient(-45deg, #000 0, #000 1.5px, transparent 1.5px, transparent 40px)',
        opacity: 0.04,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const x = seededRandom(i + 71) * 1920;
        const drift = ((frame * (0.4 + seededRandom(i + 33))) % 1150);
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1120 - drift,
            width: 5, height: 5, background: '#FFD52D', opacity: 0.2,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
        {/* Brand intro card */}
        <div style={{
          background: '#000000', border: '6px solid #FFD52D', boxShadow: '16px 16px 0 rgba(0,0,0,0.4)',
          padding: '38px 80px', display: 'flex', flexDirection: 'column', alignItems: 'center',
          transform: `rotate(-1deg) scale(${sp(8, { damping: 11 })})`,
        }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 50, fontWeight: 700, color: '#F4F2F2' }}>
            मैं कुनाल हूँ —
          </div>
          <div style={{ fontFamily: "'Yantra One', Inter, sans-serif", fontSize: 118, fontWeight: 900, color: '#FFD52D', letterSpacing: 2 }}>
            ProBuffet HQ
          </div>
        </div>

        {/* Digging metaphor: pickaxe + buried rules chest */}
        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 40, marginTop: 55, opacity: sp(70) }}>
          {/* Pickaxe SVG — swing micro-animation */}
          <svg width="170" height="170" viewBox="0 0 100 100" style={{ transform: `rotate(${dig}deg)`, transformOrigin: '75% 75%' }}>
            <rect x="52" y="30" width="10" height="58" rx="4" fill="#FFD52D" stroke="#000" strokeWidth="4" transform="rotate(38 57 59)" />
            <path d="M20 34 Q 55 8 88 30 Q 60 22 20 34 Z" fill="#F4F2F2" stroke="#000" strokeWidth="4" />
          </svg>
          {/* Ground line + buried rule chest revealed */}
          <div style={{ position: 'relative' }}>
            <div style={{ width: 700, height: 8, background: '#000000', opacity: 0.7 }} />
            <div style={{
              marginTop: 18, background: '#F4F2F2', border: '5px solid #000000', boxShadow: '10px 10px 0 #000000',
              fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 44, fontWeight: 800, color: '#000000',
              padding: '16px 36px', transform: `translateY(${interpolate(sp(100, { damping: 10 }), [0, 1], [90, 0])}px)`,
              opacity: sp(100),
            }}>
              वो नियम — जो किताबों में नहीं मिलते
            </div>
          </div>
        </div>

        {/* Two rulebook teaser + "6" badge */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 44, marginTop: 60 }}>
          {/* Count badge — pulsing */}
          <div style={{
            width: 130, height: 130, borderRadius: '50%', background: '#FFD52D',
            border: '6px solid #000000', display: 'flex', justifyContent: 'center', alignItems: 'center',
            fontFamily: "'Yantra One', sans-serif", fontSize: 82, fontWeight: 900, color: '#000000',
            transform: `scale(${sp(160, { damping: 8 }) * pulse})`,
            boxShadow: '8px 8px 0 #000000',
          }}>6</div>
          <div style={{
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 58, fontWeight: 800,
            color: '#F4F2F2', textShadow: '6px 6px 0 rgba(0,0,0,0.5)',
            transform: `translateX(${interpolate(sp(180), [0, 1], [140, 0])}px)`, opacity: sp(180),
          }}>
            बातें = <span style={{ color: '#FFD52D' }}>दो अलग-अलग रूल-बुक</span> की कहानी
          </div>
        </div>
      </AbsoluteFill>

      {/* Collage torn strip left */}
      <div style={{
        position: 'absolute', left: -30, top: 240, width: 130, height: 640, background: '#000000', opacity: 0.75,
        clipPath: 'polygon(0 0, 60% 0, 100% 8%, 55% 18%, 95% 30%, 50% 42%, 100% 55%, 58% 68%, 92% 80%, 52% 92%, 80% 100%, 0 100%)',
      }} />

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.45) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }),
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene08_MainKunalHoon;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_PublicRulebook',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_PublicRulebook: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // The four public steps
  const steps = [
    { t: 'मेहनत करो', d: 30 },
    { t: 'थोड़ा बचाओ', d: 55 },
    { t: 'FD करा लो', d: 80 },
    { t: '60 पर रिटायर', d: 105 },
  ];

  // Low ceiling bar — presses down slowly (the core visual argument)
  const ceilingY = interpolate(frame, [190, 280], [-200, 205], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });
  // Impact shake when ceiling lands
  const shake = frame >= 276 && frame <= 292 ? Math.sin(frame * 3.2) * interpolate(frame, [276, 292], [8, 0], { extrapolateRight: 'clamp' }) : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#06923E', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #06923E 0%, #045A28 70%, #023316 100%)' }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 560, height: 560, borderRadius: '50%',
        background: '#F4F2F2', opacity: 0.04,
        right: -160, top: -160, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 460, height: 460, border: '4px solid #000000', opacity: 0.07,
        left: -140, bottom: -120, transform: `rotate(${frame * 0.08}deg)`,
      }} />

      {/* Motion texture — dotted rows */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(#000 1.4px, transparent 1.4px)',
        backgroundSize: '44px 44px', opacity: 0.05,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 8 }).map((_, i) => {
        const x = seededRandom(i + 81) * 1920;
        const drift = ((frame * (0.3 + seededRandom(i + 37))) % 1150);
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1120 - drift,
            width: 5, height: 5, borderRadius: '50%', background: '#F4F2F2', opacity: 0.16,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateY(${shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Rulebook title tab */}
        <div style={{
          background: '#F4F2F2', color: '#000000', border: '6px solid #000000',
          fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
          fontSize: 84, fontWeight: 900, padding: '10px 58px',
          boxShadow: '12px 12px 0 #000000', marginBottom: 60,
          transform: `rotate(-1deg) translateY(${interpolate(sp(0, { damping: 11 }), [0, 1], [-140, 0])}px)`,
        }}>
          रूल-बुक #1 : <span style={{ background: '#FFD52D', padding: '0 18px' }}>पब्लिक</span>
        </div>

        {/* Conveyor of steps — arrows connect, each springs in */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 22 }}>
          {steps.map((s, i) => (
            <React.Fragment key={i}>
              <div style={{
                background: '#000000', color: '#F4F2F2',
                fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 44, fontWeight: 800,
                padding: '20px 32px', border: '4px solid #F4F2F2',
                transform: `rotate(${i % 2 ? 1.5 : -1.5}deg) scale(${sp(s.d, { damping: 9 })})`,
              }}>{s.t}</div>
              {i < steps.length - 1 && (
                <svg width="52" height="40" viewBox="0 0 52 40" style={{ opacity: sp(s.d + 15), transform: `translateX(${Math.sin(frame * 0.1 + i) * 3}px)` }}>
                  <path d="M2 20 H38 M28 6 L42 20 L28 34" fill="none" stroke="#FFD52D" strokeWidth="7" strokeLinecap="round" />
                </svg>
              )}
            </React.Fragment>
          ))}
        </div>

        {/* Verdict line */}
        <div style={{
          marginTop: 55, fontFamily: "'Noto Sans Devanagari', sans-serif",
          fontSize: 54, fontWeight: 800, color: '#F4F2F2',
          textShadow: '6px 6px 0 rgba(0,0,0,0.5)', opacity: sp(140),
          transform: `translateY(${interpolate(sp(140), [0, 1], [60, 0])}px)`,
        }}>
          नियम ग़लत नहीं — बस <span style={{ color: '#FFD52D' }}>छत बहुत नीची</span> है
        </div>

        {/* LOW CEILING — heavy black bar pressing down over aspiration text */}
        <div style={{ position: 'relative', marginTop: 45, width: 1100, height: 260 }}>
          {/* Aspiration text being capped */}
          <div style={{
            position: 'absolute', bottom: 0, width: '100%', textAlign: 'center',
            fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
            fontSize: 74, fontWeight: 900, color: '#F4F2F2', opacity: sp(170),
          }}>
            गुज़ारे लायक बुढ़ापा — बस।
          </div>
          {/* The ceiling itself */}
          <div style={{
            position: 'absolute', top: ceilingY - 205, left: 0, width: '100%', height: 60,
            background: '#000000', boxShadow: '0 10px 0 rgba(0,0,0,0.35)',
            clipPath: 'polygon(0 0, 100% 0, 100% 70%, 94% 100%, 86% 72%, 74% 100%, 62% 74%, 50% 100%, 38% 74%, 26% 100%, 14% 72%, 6% 100%, 0 70%)',
          }}>
            <div style={{
              fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 800, color: '#FFD52D',
              textAlign: 'center', paddingTop: 8, letterSpacing: 6,
            }}>CEILING — नीची छत</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(0,0,0,0.48) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }),
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene09_PublicRulebook;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_ZameeniCode',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_ZameeniCode: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Rise arrow draw-on from the ZERO
  const riseDraw = interpolate(frame, [120, 220], [700, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // ZERO crack shake at refusal moment
  const crack = sp(95, { damping: 7, stiffness: 220 });
  const shake = frame >= 95 && frame <= 110 ? Math.sin(frame * 3.4) * interpolate(frame, [95, 110], [10, 0], { extrapolateRight: 'clamp' }) : 0;

  // Disclaimer strip slides up late
  const discY = interpolate(sp(380, { damping: 13 }), [0, 1], [160, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#06923E', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at 20% 90%, #0BBF57 0%, #06923E 40%, #023316 100%)' }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 860, height: 860, borderRadius: '50%',
        border: '3px solid #FFD52D', opacity: 0.06,
        right: -240, bottom: -240, transform: `rotate(${frame * 0.07}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 380, height: 380, background: '#F4F2F2', opacity: 0.04,
        left: -100, top: -100, transform: `rotate(${frame * 0.05}deg)`,
      }} />

      {/* Motion texture — soil / ground hatch at bottom */}
      <div style={{
        position: 'absolute', bottom: 0, left: 0, right: 0, height: 300,
        backgroundImage: 'repeating-linear-gradient(-45deg, #000 0, #000 3px, transparent 3px, transparent 22px)',
        opacity: 0.08,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 11 }).map((_, i) => {
        const x = seededRandom(i + 91) * 1920;
        const drift = ((frame * (0.35 + seededRandom(i + 43))) % 1150);
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: 1120 - drift,
            width: 6, height: 6, borderRadius: '50%',
            background: i % 3 === 0 ? '#FFD52D' : '#F4F2F2', opacity: 0.18,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Rulebook title tab — inverse of Scene 09 */}
        <div style={{
          background: '#000000', color: '#F4F2F2', border: '6px solid #FFD52D',
          fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
          fontSize: 84, fontWeight: 900, padding: '10px 58px',
          boxShadow: '12px 12px 0 rgba(0,0,0,0.4)', marginBottom: 40,
          transform: `rotate(1deg) translateY(${interpolate(sp(0, { damping: 11 }), [0, 1], [-140, 0])}px)`,
        }}>
          रूल-बुक #2 : <span style={{ color: '#FFD52D' }}>ज़मीनी कोड</span>
        </div>

        {/* ZERO → rise visual */}
        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 50, height: 420 }}>
          {/* Giant zero with crack — starting point */}
          <div style={{ position: 'relative', transform: `scale(${sp(30, { damping: 10 })})` }}>
            <div style={{
              fontFamily: "'Yantra One', sans-serif", fontSize: 300, fontWeight: 900,
              color: '#F4F2F2', lineHeight: 0.9, textShadow: '12px 12px 0 rgba(0,0,0,0.55)',
            }}>0</div>
            {/* Crack line across zero — the refusal */}
            <svg width="240" height="300" viewBox="0 0 240 300" style={{ position: 'absolute', top: 0, left: 0, opacity: crack }}>
              <path d="M40 40 L110 120 L80 160 L160 250" fill="none" stroke="#FFD52D" strokeWidth="10" strokeLinecap="round" />
            </svg>
          </div>

          {/* Rising stepped arrow (visual metaphor, not a data chart) */}
          <svg width="720" height="400" viewBox="0 0 720 400">
            <path d="M10 380 L170 380 L170 290 L330 290 L330 190 L490 190 L490 90 L640 90"
              fill="none" stroke="#FFD52D" strokeWidth="14" strokeLinecap="round" strokeLinejoin="round"
              strokeDasharray="700" strokeDashoffset={riseDraw} />
            {/* Arrowhead pops when line completes */}
            <path d="M600 50 L680 88 L612 128 Z" fill="#FFD52D" stroke="#000" strokeWidth="5"
              opacity={frame > 215 ? sp(215, { damping: 8 }) : 0}
              transform={`rotate(-18 640 90) scale(${frame > 215 ? sp(215, { damping: 8 }) : 0})`} />
          </svg>
        </div>

        {/* Core line */}
        <div style={{
          marginTop: 26, fontFamily: "'Noto Sans Devanagari', sans-serif",
          fontSize: 58, fontWeight: 800, color: '#F4F2F2',
          textShadow: '6px 6px 0 rgba(0,0,0,0.5)', textAlign: 'center',
          opacity: sp(150), transform: `translateY(${interpolate(sp(150), [0, 1], [60, 0])}px)`,
        }}>
          शून्य से शुरू हुए — और <span style={{ background: '#000000', color: '#FFD52D', padding: '0 20px' }}>शून्य पर मरने से इनकार</span> कर दिया
        </div>
      </AbsoluteFill>

      {/* Honest disclaimer strip — slides up, stays till exit */}
      <div style={{
        position: 'absolute', bottom: 70, left: 0, right: 0, display: 'flex', justifyContent: 'center',
        transform: `translateY(${discY}px)`,
      }}>
        <div style={{
          display: 'flex', alignItems: 'center', gap: 22,
          background: '#F4F2F2', border: '5px solid #000000', boxShadow: '10px 10px 0 #000000',
          padding: '16px 40px', transform: 'rotate(-0.5deg)',
        }}>
          {/* Shield-info SVG icon */}
          <svg width="52" height="52" viewBox="0 0 100 100">
            <path d="M50 8 L88 24 V52 C88 74 70 88 50 94 C30 88 12 74 12 52 V24 Z" fill="#06923E" stroke="#000" strokeWidth="5" />
            <text x="50" y="66" textAnchor="middle" fontSize="46" fontWeight="900" fill="#F4F2F2" fontFamily="Inter, sans-serif">i</text>
          </svg>
          <span style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 36, fontWeight: 800, color: '#000000' }}>
            मैं SEBI रजिस्टर्ड सलाहकार नहीं — ये पूरी बातचीत शिक्षा के मकसद से है
          </span>
        </div>
      </div>

      {/* Tape strips */}
      <div style={{ position: 'absolute', top: 140, right: 260, width: 150, height: 34, background: '#FFD52D', opacity: 0.85, transform: 'rotate(22deg)' }} />

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.8, 0.3], { extrapolateRight: 'clamp' }),
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene10_ZameeniCode;
```

