```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_2019NormalLife',
  durationInSeconds: 5,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_2019NormalLife: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleProgress = sp(8);
  const basketProgress = sp(25, { damping: 15 });
  const numberProgress = sp(45);

  return (
    <AbsoluteFill style={{ 
      background: 'linear-gradient(135deg, #F8FAFC 0%, #E2E8F0 100%)', 
      overflow: 'hidden', 
      opacity: exitOp 
    }}>
      {/* Dynamic Light Gradient Overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(202, 154, 44, 0.08) 0%, transparent 50%)',
      }} />

      {/* Animated Shape Layers - Soft Elegant Forms */}
      <AbsoluteFill>
        {Array.from({ length: 5 }).map((_, i) => {
          const delay = i * 12;
          const scale = 0.6 + seededRandom(i) * 0.7;
          const rot = interpolate(frame, [0, 240], [0, 22 + i * 8], { easing: Easing.linear });
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: `${18 + i * 17}%`,
                top: `${22 + (i % 3) * 19}%`,
                width: `${280 * scale}px`,
                height: `${280 * scale}px`,
                border: '3px solid rgba(74, 55, 40, 0.07)',
                borderRadius: i % 2 === 0 ? '50%' : '18px',
                transform: `rotate(${rot}deg) scale(${sp(delay) * 0.85 + 0.6})`,
                opacity: 0.6 + seededRandom(i + 10) * 0.25,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Motion Texture - Subtle Grid */}
      <AbsoluteFill style={{
        backgroundImage: `linear-gradient(rgba(100, 116, 139, 0.035) 1px, transparent 1px), linear-gradient(90deg, rgba(100, 116, 139, 0.035) 1px, transparent 1px)`,
        backgroundSize: '68px 68px',
        opacity: interpolate(frame, [0, 60], [0.4, 0.75]),
      }} />

      {/* Floating Particles */}
      {Array.from({ length: 18 }).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: `${12 + (i * 7) % 78}%`,
            top: `${15 + seededRandom(i) * 65}%`,
            width: i % 3 === 0 ? '6px' : '4px',
            height: i % 3 === 0 ? '6px' : '4px',
            background: i % 4 === 0 ? '#CA9A2C' : '#4A3728',
            borderRadius: '50%',
            opacity: 0.25 + seededRandom(i + 5) * 0.25,
            transform: `translateY(${Math.sin(frame / 28 + i) * 38}px)`,
          }}
        />
      ))}

      {/* Main Content - Elegant Reveal */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', padding: '0 140px' }}>
        <div style={{
          textAlign: 'center',
          transform: `translateY(${40 * (1 - titleProgress)}px)`,
          opacity: titleProgress,
        }}>
          <div style={{ 
            fontFamily: "'Yantra One', sans-serif",
            fontSize: "148px",
            fontWeight: 700,
            color: "#0F172A",
            lineHeight: 1.05,
            marginBottom: "28px",
            textShadow: "0 4px 28px rgba(15, 23, 42, 0.1)"
          }}>
            2019
          </div>
          <div style={{
            fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontSize: "68px",
            color: "#4A3728",
            fontWeight: 600,
            letterSpacing: "-0.02em",
            marginBottom: "52px"
          }}>
            किराना बिल = ₹5000
          </div>
        </div>

        {/* SVG Shopping Basket Icon */}
        <div style={{
          transform: `scale(${basketProgress}) translateY(${30 * (1 - basketProgress)}px)`,
          filter: "drop-shadow(0 25px 45px rgba(74, 55, 40, 0.18))",
          marginTop: "-30px"
        }}>
          <svg width="210" height="210" viewBox="0 0 200 200" fill="none">
            <path d="M45 55 L65 145 H145 L165 55" stroke="#4A3728" strokeWidth="18" strokeLinejoin="round" strokeLinecap="round"/>
            <circle cx="75" cy="165" r="18" fill="#CA9A2C"/>
            <circle cx="135" cy="165" r="18" fill="#CA9A2C"/>
            <rect x="55" y="35" width="95" height="22" rx="6" fill="#CA9A2C" opacity="0.9"/>
          </svg>
        </div>
      </AbsoluteFill>

      {/* Buffett Stick Figure - Elegant Micro Movement */}
      <div style={{
        position: 'absolute',
        bottom: '92px',
        right: '110px',
        transform: `translateY(${Math.sin(frame * 0.052) * 6}px) rotate(${Math.sin(frame * 0.038) * 2.5}deg)`,
        opacity: sp(55),
        filter: "drop-shadow(8px 18px 22px rgba(0,0,0,0.12))"
      }}>
        <svg width="128" height="168" viewBox="0 0 110 150">
          {/* Head */}
          <circle cx="55" cy="38" r="19" fill="#F5D5B5" stroke="#4A3728" strokeWidth="6"/>
          {/* Golden Glasses */}
          <rect x="39" y="32" width="33" height="12" rx="3" fill="none" stroke="#CA9A2C" strokeWidth="4"/>
          <circle cx="44" cy="37" r="5" fill="none" stroke="#CA9A2C" strokeWidth="3"/>
          <circle cx="71" cy="37" r="5" fill="none" stroke="#CA9A2C" strokeWidth="3"/>
          {/* Body - Chocolate Suit */}
          <rect x="38" y="58" width="35" height="52" rx="4" fill="#4A3728"/>
          {/* Arms & Legs */}
          <line x1="38" y1="68" x2="18" y2="105" stroke="#4A3728" strokeWidth="11" strokeLinecap="round"/>
          <line x1="73" y1="68" x2="93" y2="102" stroke="#4A3728" strokeWidth="11" strokeLinecap="round"/>
          <line x1="48" y1="108" x2="41" y2="148" stroke="#4A3728" strokeWidth="10" strokeLinecap="round"/>
          <line x1="63" y1="108" x2="71" y2="148" stroke="#4A3728" strokeWidth="10" strokeLinecap="round"/>
        </svg>
      </div>

      {/* Vignette Overlay (Light version) */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 68%, rgba(15,23,42,0.08) 94%)',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 34,
        right: 48,
        fontFamily: 'Inter, sans-serif',
        fontSize: '23px',
        fontWeight: 600,
        color: '#475569',
        opacity: 0.45,
        letterSpacing: '0.5px'
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1_2019NormalLife;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_StableLife',
  durationInSeconds: 4.5,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_StableLife: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.35, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const stabilityProgress = sp(12);
  const lineProgress = sp(35);

  return (
    <AbsoluteFill style={{ 
      background: 'linear-gradient(135deg, #F1F5F9 0%, #E0E7EF 100%)', 
      overflow: 'hidden', 
      opacity: exitOp 
    }}>
      {/* Soft Light Gradient Accent */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 80%, rgba(74, 55, 40, 0.06) 0%, transparent 60%)' }} />

      {/* Elegant Drifting Rectangles */}
      {Array.from({ length: 4 }).map((_, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: `${15 + i * 23}%`,
            top: `${-10 + i * 8}%`,
            width: '380px',
            height: '380px',
            border: '2px solid rgba(60, 42, 34, 0.055)',
            transform: `rotate(${i * 25 + frame * 0.028}deg) scale(${0.75 + Math.sin(frame/45) * 0.08})`,
            borderRadius: '42px',
          }}
        />
      ))}

      <AbsoluteFill style={{ paddingTop: '180px', paddingLeft: '160px' }}>
        <div style={{ 
          fontFamily: "'Yantra One', sans-serif",
          fontSize: "172px",
          fontWeight: 700,
          color: "#0F172A",
          opacity: stabilityProgress,
          transform: `translateY(${35 * (1 - stabilityProgress)}px)`
        }}>
          स्थिर
        </div>
        
        <div style={{
          fontFamily: "'Noto Sans Devanagari', system-ui",
          fontSize: "58px",
          color: "#334155",
          maxWidth: "920px",
          lineHeight: "1.35",
          marginTop: "22px",
          fontWeight: 500
        }}>
          2019 का किराना बिल<br/>हमेशा ₹5000 के आसपास
        </div>
      </AbsoluteFill>

      {/* Price Stability Icon */}
      <div style={{
        position: 'absolute',
        right: '140px',
        top: '220px',
        transform: `scale(${lineProgress})`,
      }}>
        <svg width="260" height="260" viewBox="0 0 200 200">
          <circle cx="100" cy="100" r="78" fill="none" stroke="#CA9A2C" strokeWidth="14" strokeDasharray="12 8"/>
          <text x="100" y="112" textAnchor="middle" fill="#4A3728" fontSize="52" fontWeight="700">₹5k</text>
        </svg>
      </div>

      {/* Buffett Character - Gentle Nod */}
      <div style={{
        position: 'absolute',
        bottom: '75px',
        left: '135px',
        transform: `translateY(${Math.sin(frame * 0.045) * 4}px)`,
        opacity: sp(32),
      }}>
        <svg width="135" height="185" viewBox="0 0 120 170">
          <circle cx="60" cy="42" r="21" fill="#F5D5B5" stroke="#334155" strokeWidth="7"/>
          <rect x="43" y="34" width="36" height="14" rx="4" fill="none" stroke="#CA9A2C" strokeWidth="4.5"/>
          <rect x="38" y="64" width="45" height="58" rx="8" fill="#334155"/>
          <line x1="38" y1="78" x2="12" y2="125" stroke="#334155" strokeWidth="13" strokeLinecap="round"/>
          <line x1="83" y1="78" x2="108" y2="119" stroke="#334155" strokeWidth="13" strokeLinecap="round"/>
        </svg>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 65%, rgba(15,23,42,0.07) 100%)' }} />

      <div style={{position: 'absolute', bottom: 32, right: 52, fontFamily: 'Inter', fontSize: '22px', color: '#64748B', opacity: 0.5}}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_StableLife;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_2026Shock',
  durationInSeconds: 5,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_2026Shock: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 10, mass: 0.5, stiffness: 140, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const shockProgress = sp(10);
  const priceJump = interpolate(frame, [25, 65], [5000, 9200], { easing: Easing.quadInOut });

  return (
    <AbsoluteFill style={{ 
      background: 'linear-gradient(145deg, #F8F4F0 0%, #EDE4D9 100%)', 
      overflow: 'hidden', 
      opacity: exitOp 
    }}>
      {/* Chocolate accent gradient */}
      <AbsoluteFill style={{ background: 'linear-gradient(transparent, rgba(74,55,40,0.07))' }} />

      {/* Large Impact Number */}
      <div style={{
        position: 'absolute',
        top: '22%',
        left: '50%',
        transform: 'translateX(-50%)',
        fontFamily: "'Inter', sans-serif",
        fontSize: "260px",
        fontWeight: 800,
        color: "#9F1239",
        opacity: shockProgress * 0.95,
        textShadow: "0 14px 38px rgba(159, 18, 57, 0.22)",
        letterSpacing: "-12px"
      }}>
        {Math.floor(priceJump)}
      </div>

      <div style={{position: 'absolute', top: '58%', left: '50%', transform: 'translateX(-50%)', fontSize: '62px', color: '#4A3728', fontWeight: 700, whiteSpace: 'nowrap'}}>
        MAY 2026 • किराना बिल
      </div>

      {/* Rising Arrow SVG */}
      <div style={{position: 'absolute', top: '180px', right: '180px', transform: `scale(${sp(45)})`, opacity: sp(45)}}>
        <svg width="160" height="210" viewBox="0 0 100 140">
          <line x1="50" y1="125" x2="50" y2="18" stroke="#9F1239" strokeWidth="22" strokeLinecap="round"/>
          <polygon points="15,45 50,8 85,45" fill="#9F1239"/>
        </svg>
      </div>

      {/* Fresh Shape System - Soft Triangles */}
      {Array.from({ length: 6 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: `${10 + i*14}%`,
          top: `${12 + (i%3)*27}%`,
          width: '0',
          height: '0',
          borderLeft: '68px solid transparent',
          borderRight: '68px solid transparent',
          borderBottom: '118px solid rgba(60, 42, 34, 0.06)',
          transform: `rotate(${frame * (0.1 + i*0.04)}deg)`,
          opacity: 0.7,
        }}/>
      ))}

      {/* Professional Stick Figure - Concerned Expression */}
      <div style={{position: 'absolute', bottom: '85px', left: '140px', transform: `translateY(${Math.sin(frame*0.06)*7}px)`}}>
        <svg width="115" height="175" viewBox="0 0 100 160">
          <circle cx="52" cy="36" r="19" fill="#E8C9A8"/>
          <path d="M38 32 Q52 42 67 31" fill="none" stroke="#222" strokeWidth="4" strokeLinecap="round"/> {/* Concerned mouth */}
          <rect x="37" y="57" width="31" height="54" fill="#3F2C22" rx="4"/>
        </svg>
      </div>

      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(15,23,42,0.09) 100%)' }} />

      <div style={{position:'absolute', bottom:'38px', right:'52px', color:'#475569', fontSize:'21px', fontWeight:'600', opacity:'0.45'}}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3_2026Shock;
```

