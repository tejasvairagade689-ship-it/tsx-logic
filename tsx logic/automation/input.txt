```tsx
import React from 'react';
Audio: "Hello friends, if you are turning 62 this year, or you just turned 62, I need you to stay with me until the very end of this video. Because what I'm about to share could cost you..."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_Hello62',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_Hello62: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom-in slow
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  // Hello reveal
  const helloY = sp(5) * 100 - 100;
  const helloOp = interpolate(frame, [5, 25], [0, 1], { extrapolateRight: 'clamp' });

  // 62 hero number
  const num62Scale = sp(30, { damping: 8, stiffness: 100 });
  const num62Rot = interpolate(sp(30), [0, 1], [-15, 0]);

  // Circle ring around 62
  const ringScale = sp(45);
  const ringRot = frame * 1.2;

  // Bottom strip
  const stripX = interpolate(sp(70), [0, 1], [-1920, 0]);

  // Warning text
  const warnOp = interpolate(frame, [110, 130], [0, 1], { extrapolateRight: 'clamp' });
  const warnPulse = 1 + Math.sin(frame * 0.15) * 0.04;

  // Floating particles
  const particles = Array.from({ length: 25 }, (_, i) => i);

  // Shape layer drift
  const shapeDrift = Math.sin(frame * 0.02) * 30;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, #FCD1D1 0%, #f5b8b8 60%, #e89e9e 100%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute',
        width: 900, height: 900,
        borderRadius: '50%',
        background: '#FEEC41',
        opacity: 0.08,
        top: -200 + shapeDrift,
        left: -200,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        width: 700, height: 700,
        background: '#000000',
        opacity: 0.05,
        bottom: -150,
        right: -100 - shapeDrift,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 0, height: 0,
        borderLeft: '300px solid transparent',
        borderRight: '300px solid transparent',
        borderBottom: '500px solid #FEEC41',
        opacity: 0.06,
        top: 200,
        right: 100,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Motion texture - grid lines */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.08 }}>
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid1)" />
      </svg>

      {/* Floating particles */}
      {particles.map((i) => {
        const seed = seededRandom(i + 1);
        const x = seed * 1920;
        const y = (seededRandom(i + 50) * 1080 + frame * (0.5 + seed)) % 1080;
        const size = 4 + seed * 8;
        return (
          <div key={i} style={{
            position: 'absolute',
            width: size, height: size,
            borderRadius: '50%',
            background: i % 2 === 0 ? '#FEEC41' : '#000000',
            opacity: 0.5,
            left: x, top: y,
          }} />
        );
      })}

      {/* CONTENT WRAPPER with camera */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center' }}>

        {/* Top label "HELLO FRIENDS" */}
        <div style={{
          position: 'absolute',
          top: 140,
          left: 0, right: 0,
          textAlign: 'center',
          transform: `translateY(${helloY}px)`,
          opacity: helloOp,
        }}>
          <div style={{
            display: 'inline-block',
            background: '#000000',
            color: '#FEEC41',
            padding: '20px 60px',
            fontFamily: 'Inter, Poppins',
            fontSize: 76,
            fontWeight: 900,
            letterSpacing: 8,
            border: '6px solid #FEEC41',
            transform: 'skewX(-6deg)',
          }}>
            HELLO FRIENDS
          </div>
        </div>

        {/* MASSIVE 62 with ring */}
        <div style={{
          position: 'absolute',
          top: 320,
          left: 0, right: 0,
          textAlign: 'center',
        }}>
          {/* Rotating ring */}
          <svg width="700" height="700" style={{
            position: 'absolute',
            left: '50%',
            top: -50,
            transform: `translateX(-50%) scale(${ringScale}) rotate(${ringRot}deg)`,
          }}>
            <circle cx="350" cy="350" r="320" fill="none" stroke="#000000" strokeWidth="6" strokeDasharray="30 20" />
            <circle cx="350" cy="350" r="280" fill="none" stroke="#FEEC41" strokeWidth="14" strokeDasharray="60 40" />
          </svg>

          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 442,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 1,
            transform: `scale(${num62Scale}) rotate(${num62Rot}deg)`,
            display: 'inline-block',
            textShadow: '12px 12px 0 #FEEC41',
          }}>
            62
          </div>
        </div>

        {/* Bottom warning strip */}
        <div style={{
          position: 'absolute',
          bottom: 180,
          left: 0,
          right: 0,
          transform: `translateX(${stripX}px)`,
        }}>
          <div style={{
            background: '#000000',
            padding: '28px 0',
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 40,
            borderTop: '4px solid #FEEC41',
            borderBottom: '4px solid #FEEC41',
          }}>
            <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
              <path d="M12 2L2 22h20L12 2z" stroke="#FEEC41" strokeWidth="2.5" fill="#FEEC41" fillOpacity="0.2"/>
              <line x1="12" y1="9" x2="12" y2="14" stroke="#FEEC41" strokeWidth="3" strokeLinecap="round"/>
              <circle cx="12" cy="18" r="1.5" fill="#FEEC41"/>
            </svg>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 68,
              fontWeight: 900,
              color: '#FEEC41',
              letterSpacing: 4,
            }}>
              TURNING 62 THIS YEAR?
            </div>
            <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
              <path d="M12 2L2 22h20L12 2z" stroke="#FEEC41" strokeWidth="2.5" fill="#FEEC41" fillOpacity="0.2"/>
              <line x1="12" y1="9" x2="12" y2="14" stroke="#FEEC41" strokeWidth="3" strokeLinecap="round"/>
              <circle cx="12" cy="18" r="1.5" fill="#FEEC41"/>
            </svg>
          </div>
        </div>

        {/* Stay till end - lower */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: warnOp,
          transform: `scale(${warnPulse})`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 51,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 3,
          }}>
            WATCH TILL END — IT COULD COST YOU $$$
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_Hello62;
```

```tsx
import React from 'react';
Audio: "...or save you, hundreds of thousands of dollars over your lifetime. And I mean real money, not the kind of savings where you clip coupons... I'm talking about the single most irreversible financial decision of your retirement, when to claim social security."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_HundredsOfThousands',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_HundredsOfThousands: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter animation for dollars
  const dollarValue = Math.floor(interpolate(frame, [20, 90], [0, 500000], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  }));
  const dollarScale = sp(20, { damping: 10 });

  // Vs label
  const vsOp = interpolate(frame, [110, 130], [0, 1], { extrapolateRight: 'clamp' });
  const vsScale = sp(110);

  // Coin (clip coupon side)
  const coinOp = interpolate(frame, [130, 155], [0, 1], { extrapolateRight: 'clamp' });
  const coinScale = sp(130);

  // Big bottom statement
  const stmtX = interpolate(sp(180), [0, 1], [1920, 0]);

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -20]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #FCD1D1 0%, #f5b8b8 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        width: 1200, height: 1200,
        borderRadius: '50%',
        border: '60px solid #FEEC41',
        opacity: 0.07,
        top: -400,
        right: -400,
        transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.04) * 0.03})`,
      }} />
      <div style={{
        position: 'absolute',
        width: 500, height: 500,
        background: '#000000',
        opacity: 0.05,
        bottom: -100,
        left: -100,
        transform: `rotate(${45 + frame * 0.1}deg)`,
      }} />

      {/* Diagonal lines texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.06 }}>
        <defs>
          <pattern id="diag1" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="60" stroke="#000000" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag1)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }, (_, i) => {
        const seed = seededRandom(i + 5);
        const y = (seed * 1080 - frame * (0.8 + seed * 0.5)) % 1080;
        const adjY = y < 0 ? y + 1080 : y;
        return (
          <div key={i} style={{
            position: 'absolute',
            width: 6, height: 6,
            borderRadius: '50%',
            background: '#FEEC41',
            opacity: 0.6,
            left: seed * 1920,
            top: adjY,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>

        {/* Top label */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: sp(5),
        }}>
          <div style={{
            display: 'inline-block',
            background: '#000000',
            color: '#FEEC41',
            padding: '14px 50px',
            fontFamily: 'Inter, Poppins',
            fontSize: 51,
            fontWeight: 900,
            letterSpacing: 5,
          }}>
            THIS IS ABOUT
          </div>
        </div>

        {/* Massive dollar counter */}
        <div style={{
          position: 'absolute',
          top: 220,
          left: 0, right: 0,
          textAlign: 'center',
          transform: `scale(${dollarScale})`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 280,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 1,
            textShadow: '10px 10px 0 #FEEC41',
          }}>
            ${dollarValue.toLocaleString()}+
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 60,
            fontWeight: 800,
            color: '#000000',
            marginTop: 10,
            letterSpacing: 4,
          }}>
            OVER YOUR LIFETIME
          </div>
        </div>

        {/* VS separator */}
        <div style={{
          position: 'absolute',
          top: 680,
          left: '50%',
          transform: `translateX(-50%) scale(${vsScale})`,
          opacity: vsOp,
        }}>
          <div style={{
            width: 130, height: 130,
            borderRadius: '50%',
            background: '#FEEC41',
            border: '6px solid #000000',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontFamily: 'Inter, Poppins',
            fontSize: 60,
            fontWeight: 900,
            color: '#000000',
          }}>
            VS
          </div>
        </div>

        {/* Clip coupons side */}
        <div style={{
          position: 'absolute',
          top: 690,
          right: 180,
          opacity: coinOp,
          transform: `scale(${coinScale})`,
          textAlign: 'center',
        }}>
          <svg width="100" height="100" viewBox="0 0 24 24" fill="none" style={{ margin: '0 auto' }}>
            <circle cx="12" cy="12" r="10" stroke="#000000" strokeWidth="2" fill="#FEEC41"/>
            <path d="M9 8h6M9 12h6M9 16h4" stroke="#000000" strokeWidth="2" strokeLinecap="round"/>
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 700,
            color: '#000000',
            marginTop: 8,
            textDecoration: 'line-through',
            textDecorationColor: '#FF0000',
            textDecorationThickness: 4,
          }}>
            50¢ COUPONS
          </div>
        </div>

        {/* Real money side */}
        <div style={{
          position: 'absolute',
          top: 690,
          left: 180,
          opacity: coinOp,
          transform: `scale(${coinScale})`,
          textAlign: 'center',
        }}>
          <svg width="100" height="100" viewBox="0 0 24 24" fill="none" style={{ margin: '0 auto' }}>
            <rect x="2" y="6" width="20" height="14" rx="2" stroke="#000000" strokeWidth="2.5" fill="#FEEC41"/>
            <circle cx="12" cy="13" r="3" stroke="#000000" strokeWidth="2"/>
            <text x="12" y="16" textAnchor="middle" fontSize="6" fontWeight="900" fill="#000000">$</text>
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 900,
            color: '#000000',
            marginTop: 8,
          }}>
            REAL MONEY
          </div>
        </div>

        {/* Bottom strip */}
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          right: 0,
          transform: `translateX(${stmtX}px)`,
        }}>
          <div style={{
            background: '#000000',
            padding: '24px 0',
            textAlign: 'center',
            borderTop: '4px solid #FEEC41',
            borderBottom: '4px solid #FEEC41',
          }}>
            <span style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 51,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: 3,
            }}>
              MOST IRREVERSIBLE DECISION:{' '}
            </span>
            <span style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 51,
              fontWeight: 900,
              color: '#FEEC41',
              letterSpacing: 3,
            }}>
              SOCIAL SECURITY
            </span>
          </div>
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_HundredsOfThousands;
```

```tsx
import React from 'react';
Audio: "Here's the thing most people never find out. The moment you turn 62, the Social Security Administration sends you a letter inviting you to claim your benefits."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_TheLetter',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_TheLetter: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Top headline
  const headlineOp = sp(5);
  const headlineX = interpolate(sp(5), [0, 1], [-200, 0]);

  // Envelope drop
  const envY = interpolate(sp(35, { damping: 10, stiffness: 80 }), [0, 1], [-800, 0]);
  const envRot = interpolate(sp(35), [0, 1], [-25, 0]);

  // Letter slide out
  const letterY = interpolate(sp(95, { damping: 14 }), [0, 1], [0, -180]);
  const letterOp = interpolate(frame, [95, 120], [0, 1], { extrapolateRight: 'clamp' });

  // Stamp pulse
  const stampScale = 1 + Math.sin(Math.max(0, frame - 140) * 0.18) * 0.08;
  const stampOp = interpolate(frame, [140, 160], [0, 1], { extrapolateRight: 'clamp' });

  // Arrow + label "SSA"
  const arrowOp = interpolate(frame, [180, 200], [0, 1], { extrapolateRight: 'clamp' });

  // Bottom text
  const bottomOp = interpolate(frame, [220, 245], [0, 1], { extrapolateRight: 'clamp' });

  // Camera subtle zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 70% 30%, #FCD1D1 0%, #f0a8a8 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        width: 800, height: 800,
        background: '#FEEC41',
        opacity: 0.06,
        top: 100,
        left: -300,
        transform: `rotate(${frame * 0.08}deg)`,
        borderRadius: 80,
      }} />
      <div style={{
        position: 'absolute',
        width: 600, height: 600,
        borderRadius: '50%',
        border: '40px solid #000000',
        opacity: 0.05,
        bottom: -200,
        right: -150,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
      }} />

      {/* Dot grid texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.1 }}>
        <defs>
          <pattern id="dots3" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#000000" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots3)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }, (_, i) => {
        const seed = seededRandom(i + 10);
        const x = (seed * 1920 + frame * (0.4 + seed * 0.3)) % 1920;
        return (
          <div key={i} style={{
            position: 'absolute',
            width: 5, height: 5,
            background: '#000000',
            opacity: 0.4,
            left: x,
            top: seed * 1080,
            transform: 'rotate(45deg)',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Headline */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: headlineOp,
          transform: `translateX(${headlineX}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            background: '#000000',
            color: '#FEEC41',
            padding: '18px 55px',
            fontFamily: 'Inter, Poppins',
            fontSize: 64,
            fontWeight: 900,
            letterSpacing: 5,
            transform: 'skewX(-5deg)',
          }}>
            WHAT THEY NEVER TELL YOU
          </div>
        </div>

        {/* Envelope center */}
        <div style={{
          position: 'absolute',
          top: 280,
          left: '50%',
          marginLeft: -340,
          transform: `translateY(${envY}px) rotate(${envRot}deg)`,
        }}>
          {/* Letter peeking out */}
          <div style={{
            position: 'absolute',
            width: 620,
            height: 380,
            background: '#FEEC41',
            border: '6px solid #000000',
            top: 20,
            left: 30,
            transform: `translateY(${letterY}px)`,
            opacity: letterOp,
            padding: 30,
            boxSizing: 'border-box',
          }}>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 30,
              fontWeight: 900,
              color: '#000000',
              borderBottom: '4px solid #000000',
              paddingBottom: 10,
              marginBottom: 18,
            }}>
              SOCIAL SECURITY ADMINISTRATION
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              color: '#000000',
              lineHeight: 1.5,
            }}>
              Dear Future Retiree,<br/>
              You are now eligible<br/>
              to claim your benefits.<br/>
              <span style={{ fontWeight: 900 }}>Sign here ▸ ________</span>
            </div>

            {/* Stamp */}
            <div style={{
              position: 'absolute',
              right: 25,
              bottom: 25,
              width: 130, height: 130,
              borderRadius: '50%',
              border: '5px solid #FF0000',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              transform: `rotate(-15deg) scale(${stampScale})`,
              opacity: stampOp,
              fontFamily: 'Inter, Poppins',
              fontSize: 22,
              fontWeight: 900,
              color: '#FF0000',
              textAlign: 'center',
              lineHeight: 1.1,
            }}>
              CLAIM<br/>NOW!
            </div>
          </div>

          {/* Envelope body */}
          <div style={{
            position: 'relative',
            width: 680,
            height: 420,
            background: '#FEEC41',
            border: '8px solid #000000',
            boxShadow: '14px 14px 0 #000000',
          }}>
            {/* Envelope flap */}
            <svg width="680" height="220" style={{ position: 'absolute', top: -8, left: -8 }}>
              <polygon points="0,0 680,0 340,220" fill="#f0a8a8" stroke="#000000" strokeWidth="8" />
            </svg>
          </div>
        </div>

        {/* SSA arrow */}
        <div style={{
          position: 'absolute',
          top: 360,
          left: 130,
          opacity: arrowOp,
        }}>
          <div style={{
            background: '#000000',
            color: '#FEEC41',
            padding: '14px 30px',
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 900,
            letterSpacing: 2,
            transform: 'rotate(-8deg)',
          }}>
            FROM: SSA
          </div>
          <svg width="160" height="80" style={{ marginLeft: 60, marginTop: 10 }}>
            <path d="M 10 40 Q 80 10 150 40" stroke="#000000" strokeWidth="6" fill="none" strokeLinecap="round"/>
            <polygon points="140,30 155,40 140,50" fill="#000000"/>
          </svg>
        </div>

        {/* Bottom statement */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: bottomOp,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 60,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: 3,
          }}>
            FEELS LIKE A <span style={{ background: '#FEEC41', padding: '6px 24px' }}>REWARD</span>
          </div>
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_TheLetter;
```

```tsx
import React from 'react';
Audio: "It feels like a reward. It feels like you finally get to collect what you paid into the system for decades. But hidden in that letter is a massive, permanent penalty."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_HiddenPenalty',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_HiddenPenalty: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Reward labels
  const reward1Op = sp(5);
  const reward2Op = sp(25);

  // Decades counter
  const decades = Math.floor(interpolate(frame, [40, 100], [0, 40], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));

  // Strike-through
  const strikeWidth = interpolate(frame, [130, 170], [0, 100], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Shake on PENALTY
  const shake = frame > 170 && frame < 210 ? Math.sin(frame * 1.5) * 8 : 0;
  const penaltyScale = sp(170, { damping: 6 });
  const penaltyOp = interpolate(frame, [170, 190], [0, 1], { extrapolateRight: 'clamp' });

  // Warning icon pulse
  const warnPulse = 1 + Math.sin(frame * 0.2) * 0.1;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #FCD1D1 0%, #e89e9e 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 0, height: 0,
        borderLeft: '400px solid transparent',
        borderRight: '400px solid transparent',
        borderBottom: '600px solid #000000',
        opacity: 0.05,
        top: -100,
        left: -200,
        transform: `rotate(${frame * 0.05}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 700, height: 700,
        borderRadius: '50%',
        background: '#FEEC41',
        opacity: 0.08,
        bottom: -300,
        right: -200,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />

      {/* Cross-hatch texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.05 }}>
        <defs>
          <pattern id="cross4" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M0,40 L40,0 M-10,10 L10,-10 M30,50 L50,30" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#cross4)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 15 }, (_, i) => {
        const seed = seededRandom(i + 22);
        const y = (seed * 1080 + frame * (0.6 + seed * 0.4)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            width: 8, height: 8,
            background: i % 2 ? '#FEEC41' : '#000000',
            opacity: 0.4,
            left: seed * 1920,
            top: y,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translate(${shake}px, ${shake * 0.4}px)` }}>

        {/* Top half: "Feels like" reward stack */}
        <div style={{
          position: 'absolute',
          top: 100,
          left: 100,
          right: 100,
          display: 'flex',
          flexDirection: 'column',
          gap: 20,
        }}>
          <div style={{
            opacity: reward1Op,
            background: '#FEEC41',
            border: '6px solid #000000',
            padding: '24px 40px',
            fontFamily: 'Inter, Poppins',
            fontSize: 60,
            fontWeight: 900,
            color: '#000000',
            display: 'flex',
            alignItems: 'center',
            gap: 28,
          }}>
            <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
              <path d="M9 11l3 3L22 4" stroke="#000000" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round"/>
              <path d="M21 12v7a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2h11" stroke="#000000" strokeWidth="3" strokeLinecap="round"/>
            </svg>
            FEELS LIKE A REWARD
          </div>

          <div style={{
            opacity: reward2Op,
            background: '#FEEC41',
            border: '6px solid #000000',
            padding: '24px 40px',
            fontFamily: 'Inter, Poppins',
            fontSize: 60,
            fontWeight: 900,
            color: '#000000',
            display: 'flex',
            alignItems: 'center',
            gap: 28,
          }}>
            <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="10" stroke="#000000" strokeWidth="3" fill="#FCD1D1"/>
              <text x="12" y="17" textAnchor="middle" fontSize="14" fontWeight="900" fill="#000000">$</text>
            </svg>
            YOU PAID FOR <span style={{ background: '#000000', color: '#FEEC41', padding: '0 16px' }}>{decades} YEARS</span>
          </div>
        </div>

        {/* BUT divider */}
        <div style={{
          position: 'absolute',
          top: 470,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [120, 140], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            display: 'inline-block',
            position: 'relative',
            fontFamily: 'Inter, Poppins',
            fontSize: 102,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: 8,
          }}>
            BUT...
            <div style={{
              position: 'absolute',
              left: 0,
              top: '60%',
              height: 8,
              width: `${strikeWidth}%`,
              background: '#FF0000',
            }} />
          </div>
        </div>

        {/* PENALTY block */}
        <div style={{
          position: 'absolute',
          bottom: 100,
          left: 0, right: 0,
          textAlign: 'center',
          transform: `scale(${penaltyScale})`,
          opacity: penaltyOp,
        }}>
          <div style={{
            display: 'inline-block',
            background: '#000000',
            border: '10px solid #FEEC41',
            padding: '30px 70px',
            position: 'relative',
          }}>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 51,
              fontWeight: 800,
              color: '#FEEC41',
              letterSpacing: 3,
            }}>
              HIDDEN IN THE LETTER:
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 145,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1,
              letterSpacing: 6,
            }}>
              PERMANENT PENALTY
            </div>

            {/* Corner warning icons */}
            <div style={{
              position: 'absolute',
              top: -50,
              left: -50,
              transform: `scale(${warnPulse})`,
            }}>
              <svg width="100" height="100" viewBox="0 0 24 24" fill="none">
                <path d="M12 2L2 22h20L12 2z" stroke="#000000" strokeWidth="2.5" fill="#FEEC41"/>
                <line x1="12" y1="9" x2="12" y2="14" stroke="#000000" strokeWidth="3" strokeLinecap="round"/>
                <circle cx="12" cy="18" r="1.8" fill="#000000"/>
              </svg>
            </div>
            <div style={{
              position: 'absolute',
              top: -50,
              right: -50,
              transform: `scale(${warnPulse})`,
            }}>
              <svg width="100" height="100" viewBox="0 0 24 24" fill="none">
                <path d="M12 2L2 22h20L12 2z" stroke="#000000" strokeWidth="2.5" fill="#FEEC41"/>
                <line x1="12" y1="9" x2="12" y2="14" stroke="#000000" strokeWidth="3" strokeLinecap="round"/>
                <circle cx="12" cy="18" r="1.8" fill="#000000"/>
              </svg>
            </div>
          </div>
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_HiddenPenalty;
```

```tsx
import React from 'react';
Audio: "If you claim at 62, the government will slash your monthly check by up to 30 percent. Forever. The money is sitting there. The math is written in federal law."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_ThirtyPercentSlash',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_ThirtyPercentSlash: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Top "CLAIM AT 62"
  const topY = interpolate(sp(5), [0, 1], [-200, 0]);

  // Check stack height counter
  const fullHeight = 380;
  const slashProgress = interpolate(frame, [45, 95], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });
  const currentHeight = fullHeight * (1 - slashProgress * 0.3);

  // 30% reveal
  const num30Scale = sp(100, { damping: 7, stiffness: 100 });
  const num30Op = interpolate(frame, [100, 120], [0, 1], { extrapolateRight: 'clamp' });

  // Slash line animation
  const slashLen = interpolate(frame, [60, 90], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // FOREVER stamp
  const foreverScale = sp(170, { damping: 6 });
  const foreverOp = interpolate(frame, [170, 195], [0, 1], { extrapolateRight: 'clamp' });

  // Bottom law line
  const lawX = interpolate(sp(230), [0, 1], [-1920, 0]);

  // Shake during slash
  const shake = frame > 70 && frame < 100 ? Math.sin(frame * 2) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 30%, #FCD1D1 0%, #e89e9e 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 800, height: 800,
        borderRadius: '50%',
        border: '50px solid #FEEC41',
        opacity: 0.07,
        top: -300,
        left: -200,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 500, height: 500,
        background: '#000000',
        opacity: 0.04,
        bottom: -150,
        right: -100,
        transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.05) * 0.04})`,
      }} />

      {/* Vertical lines texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.06 }}>
        <defs>
          <pattern id="vert5" width="40" height="40" patternUnits="userSpaceOnUse">
            <line x1="20" y1="0" x2="20" y2="40" stroke="#000000" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#vert5)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }, (_, i) => {
        const seed = seededRandom(i + 30);
        const y = (seed * 1080 + frame * (0.5 + seed)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            width: 5, height: 5,
            background: '#FEEC41',
            opacity: 0.5,
            left: seed * 1920,
            top: y,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translate(${shake}px, 0)` }}>

        {/* Top label */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 0, right: 0,
          textAlign: 'center',
          transform: `translateY(${topY}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            background: '#000000',
            color: '#FEEC41',
            padding: '20px 60px',
            fontFamily: 'Inter, Poppins',
            fontSize: 72,
            fontWeight: 900,
            letterSpacing: 5,
            border: '6px solid #FEEC41',
          }}>
            IF YOU CLAIM AT 62
          </div>
        </div>

        {/* Check stack visualization - LEFT (before) */}
        <div style={{
          position: 'absolute',
          top: 280,
          left: 280,
          width: 280,
          textAlign: 'center',
        }}>
          <div style={{
            width: 280,
            height: fullHeight,
            background: '#FEEC41',
            border: '6px solid #000000',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            opacity: 0.5,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 60,
              fontWeight: 900,
              color: '#000000',
            }}>
              $2,000
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              marginTop: 8,
            }}>
              FULL CHECK
            </div>
          </div>
          <div style={{
            marginTop: 20,
            fontFamily: 'Inter, Poppins',
            fontSize: 34,
            fontWeight: 800,
            color: '#000000',
          }}>
            AT AGE 67
          </div>
        </div>

        {/* Arrow */}
        <div style={{
          position: 'absolute',
          top: 380,
          left: 600,
          opacity: interpolate(frame, [40, 60], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <svg width="180" height="120">
            <path d="M 10 60 L 160 60" stroke="#000000" strokeWidth="10" strokeLinecap="round" />
            <polygon points="150,40 175,60 150,80" fill="#000000" />
            <text x="90" y="40" textAnchor="middle" fontFamily="Inter, Poppins" fontSize="34" fontWeight="900" fill="#FF0000">SLASH!</text>
          </svg>
        </div>

        {/* Check stack visualization - RIGHT (after - shrunk) */}
        <div style={{
          position: 'absolute',
          top: 280 + (fullHeight - currentHeight),
          left: 870,
          width: 280,
          textAlign: 'center',
        }}>
          <div style={{
            width: 280,
            height: currentHeight,
            background: '#FEEC41',
            border: '6px solid #000000',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            position: 'relative',
            overflow: 'hidden',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 51,
              fontWeight: 900,
              color: '#000000',
            }}>
              $1,400
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 700,
              color: '#000000',
              marginTop: 6,
            }}>
              SLASHED!
            </div>
            {/* Diagonal slash */}
            <svg style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%' }}>
              <line x1="0" y1="0" x2={280 * slashLen} y2={currentHeight * slashLen}
                stroke="#FF0000" strokeWidth="10" />
            </svg>
          </div>
          <div style={{
            marginTop: 20 + (fullHeight - currentHeight),
            fontFamily: 'Inter, Poppins',
            fontSize: 34,
            fontWeight: 800,
            color: '#FF0000',
          }}>
            AT AGE 62
          </div>
        </div>

        {/* 30% Hero number */}
        <div style={{
          position: 'absolute',
          top: 290,
          right: 140,
          transform: `scale(${num30Scale})`,
          opacity: num30Op,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 32,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 4,
          }}>
            UP TO
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 272,
            fontWeight: 900,
            color: '#FF0000',
            lineHeight: 1,
            textShadow: '8px 8px 0 #000000',
          }}>
            30%
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: 4,
            marginTop: -10,
          }}>
            CUT
          </div>
        </div>

        {/* FOREVER stamp */}
        <div style={{
          position: 'absolute',
          top: 720,
          left: '50%',
          marginLeft: -240,
          transform: `scale(${foreverScale}) rotate(-3deg)`,
          opacity: foreverOp,
        }}>
          <div style={{
            background: '#FF0000',
            color: '#F5F5F5',
            padding: '20px 80px',
            fontFamily: 'Inter, Poppins',
            fontSize: 115,
            fontWeight: 900,
            letterSpacing: 10,
            border: '8px solid #000000',
          }}>
            FOREVER.
          </div>
        </div>

        {/* Bottom federal law */}
        <div style={{
          position: 'absolute',
          bottom: 50,
          left: 0,
          right: 0,
          transform: `translateX(${lawX}px)`,
        }}>
          <div style={{
            background: '#000000',
            padding: '18px 0',
            textAlign: 'center',
            borderTop: '4px solid #FEEC41',
          }}>
            <span style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 43,
              fontWeight: 800,
              color: '#FEEC41',
              letterSpacing: 3,
            }}>
              § WRITTEN IN FEDERAL LAW §
            </span>
          </div>
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_ThirtyPercentSlash;
```

```tsx
import React from 'react';
Audio: "But every month that goes by without you understanding this penalty is another month of locking in a lifetime of poverty. I went through the latest SSA guidelines, the 2026 actuarial tables, and the exact mathematical formulas they use."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_Research2026',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_Research2026: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Calendar months ticking
  const monthIdx = Math.floor((frame / 8) % 12);
  const months = ['JAN', 'FEB', 'MAR', 'APR', 'MAY', 'JUN', 'JUL', 'AUG', 'SEP', 'OCT', 'NOV', 'DEC'];

  // Top warning
  const warnX = interpolate(sp(5), [0, 1], [-300, 0]);

  // 3 research cards
  const card1 = sp(80);
  const card2 = sp(110);
  const card3 = sp(140);

  // Bottom lock-in
  const lockOp = interpolate(frame, [200, 220], [0, 1], { extrapolateRight: 'clamp' });
  const lockScale = sp(200);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(225deg, #FCD1D1 0%, #f5b8b8 50%, #e89e9e 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 700, height: 700,
        borderRadius: 60,
        background: '#FEEC41',
        opacity: 0.06,
        top: -200,
        right: -200,
        transform: `rotate(${15 + frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 600, height: 600,
        borderRadius: '50%',
        border: '40px solid #000000',
        opacity: 0.04,
        bottom: -200,
        left: -200,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
      }} />

      {/* Math symbols texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.07 }}>
        <defs>
          <pattern id="math6" width="120" height="120" patternUnits="userSpaceOnUse">
            <text x="20" y="40" fontFamily="JetBrains Mono" fontSize="30" fill="#000000">∑</text>
            <text x="70" y="80" fontFamily="JetBrains Mono" fontSize="30" fill="#000000">%</text>
            <text x="20" y="100" fontFamily="JetBrains Mono" fontSize="30" fill="#000000">π</text>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#math6)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }, (_, i) => {
        const seed = seededRandom(i + 40);
        return (
          <div key={i} style={{
            position: 'absolute',
            width: 6, height: 6,
            background: '#000000',
            opacity: 0.4,
            left: (seed * 1920 + frame * (0.4 + seed)) % 1920,
            top: seed * 1080,
          }} />
        );
      })}

      {/* Top warning bar */}
      <div style={{
        position: 'absolute',
        top: 50,
        left: 0,
        right: 0,
        transform: `translateX(${warnX}px)`,
      }}>
        <div style={{
          background: '#000000',
          padding: '20px 0',
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 30,
          borderBottom: '4px solid #FEEC41',
        }}>
          <div style={{
            background: '#FF0000',
            color: '#F5F5F5',
            padding: '8px 24px',
            fontFamily: 'JetBrains Mono',
            fontSize: 32,
            fontWeight: 900,
          }}>
            {months[monthIdx]} →
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 55,
            fontWeight: 900,
            color: '#FEEC41',
            letterSpacing: 3,
          }}>
            EVERY MONTH = LIFETIME OF POVERTY LOCKED IN
          </div>
        </div>
      </div>

      {/* Center: "I WENT DEEP" headline */}
      <div style={{
        position: 'absolute',
        top: 200,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(40),
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 60,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 4,
        }}>
          I WENT THROUGH
        </div>
      </div>

      {/* 3 research cards row */}
      <div style={{
        position: 'absolute',
        top: 320,
        left: 80,
        right: 80,
        display: 'flex',
        justifyContent: 'space-between',
        gap: 40,
      }}>
        {/* Card 1: SSA Guidelines */}
        <div style={{
          flex: 1,
          background: '#FEEC41',
          border: '8px solid #000000',
          padding: 30,
          transform: `scale(${card1}) translateY(${(1 - card1) * 100}px)`,
          boxShadow: '12px 12px 0 #000000',
        }}>
          <svg width="80" height="80" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 15 }}>
            <rect x="3" y="3" width="18" height="18" rx="2" stroke="#000000" strokeWidth="2.5" fill="#FCD1D1"/>
            <line x1="7" y1="8" x2="17" y2="8" stroke="#000000" strokeWidth="2.5"/>
            <line x1="7" y1="12" x2="17" y2="12" stroke="#000000" strokeWidth="2.5"/>
            <line x1="7" y1="16" x2="13" y2="16" stroke="#000000" strokeWidth="2.5"/>
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 1.1,
          }}>
            LATEST<br/>SSA GUIDELINES
          </div>
        </div>

        {/* Card 2: 2026 Actuarial */}
        <div style={{
          flex: 1,
          background: '#000000',
          border: '8px solid #FEEC41',
          padding: 30,
          transform: `scale(${card2}) translateY(${(1 - card2) * 100}px)`,
          boxShadow: '12px 12px 0 #000000',
        }}>
          <svg width="80" height="80" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 15 }}>
            <rect x="3" y="3" width="18" height="18" stroke="#FEEC41" strokeWidth="2.5"/>
            <line x1="3" y1="9" x2="21" y2="9" stroke="#FEEC41" strokeWidth="2"/>
            <line x1="3" y1="15" x2="21" y2="15" stroke="#FEEC41" strokeWidth="2"/>
            <line x1="9" y1="3" x2="9" y2="21" stroke="#FEEC41" strokeWidth="2"/>
            <line x1="15" y1="3" x2="15" y2="21" stroke="#FEEC41" strokeWidth="2"/>
          </svg>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 51,
            fontWeight: 900,
            color: '#FEEC41',
            lineHeight: 1,
          }}>
            2026
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 32,
            fontWeight: 800,
            color: '#F5F5F5',
            marginTop: 5,
          }}>
            ACTUARIAL TABLES
          </div>
        </div>

        {/* Card 3: Formulas */}
        <div style={{
          flex: 1,
          background: '#FEEC41',
          border: '8px solid #000000',
          padding: 30,
          transform: `scale(${card3}) translateY(${(1 - card3) * 100}px)`,
          boxShadow: '12px 12px 0 #000000',
        }}>
          <svg width="80" height="80" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 15 }}>
            <path d="M4 4h16v4H4zM4 12h16v4H4zM4 20h10" stroke="#000000" strokeWidth="2.5" strokeLinecap="round"/>
          </svg>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 30,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 1.3,
          }}>
            PIA × AME<br/>= BENEFIT
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 30,
            fontWeight: 800,
            color: '#000000',
            marginTop: 8,
          }}>
            EXACT FORMULAS
          </div>
        </div>
      </div>

      {/* Bottom: LOCKED IN */}
      <div style={{
        position: 'absolute',
        bottom: 70,
        left: 0, right: 0,
        textAlign: 'center',
        transform: `scale(${lockScale})`,
        opacity: lockOp,
      }}>
        <div style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: 25,
          background: '#FF0000',
          color: '#F5F5F5',
          padding: '20px 50px',
          border: '6px solid #000000',
          fontFamily: 'Inter, Poppins',
          fontSize: 64,
          fontWeight: 900,
          letterSpacing: 4,
        }}>
          <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
            <rect x="5" y="11" width="14" height="10" rx="2" stroke="#F5F5F5" strokeWidth="2.5" fill="none"/>
            <path d="M8 11V7a4 4 0 018 0v4" stroke="#F5F5F5" strokeWidth="2.5"/>
          </svg>
          LOCKED IN. NO REVERSAL.
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_Research2026;
```

```tsx
import React from 'react';
Audio: "And I found the brutal truth about claiming early, plus 3 specific types of people who actually benefit from doing it. Stay until the end, because loophole number 3 is the spousal bridge strategy..."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_ThreeLoopholes',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_ThreeLoopholes: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Brutal truth top
  const truthOp = sp(5);

  // Number 3 big reveal
  const num3Scale = sp(40, { damping: 7, stiffness: 100 });

  // 3 number boxes
  const box1 = sp(80);
  const box2 = sp(110);
  const box3 = sp(140);

  // Box 3 spotlight pulse
  const spot = 1 + Math.sin(frame * 0.18) * 0.05;

  // Bottom strategy reveal
  const stratOp = interpolate(frame, [200, 230], [0, 1], { extrapolateRight: 'clamp' });
  const stratX = interpolate(sp(200), [0, 1], [-1920, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, #FCD1D1 0%, #e89e9e 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 1000, height: 1000,
        borderRadius: '50%',
        background: '#FEEC41',
        opacity: 0.06,
        top: '50%',
        left: '50%',
        marginTop: -500,
        marginLeft: -500,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        width: 0, height: 0,
        borderLeft: '300px solid transparent',
        borderRight: '300px solid transparent',
        borderBottom: '500px solid #000000',
        opacity: 0.05,
        top: 50,
        right: 100,
        transform: `rotate(${180 + frame * 0.15}deg)`,
      }} />

      {/* Hex grid texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.07 }}>
        <defs>
          <pattern id="hex7" width="60" height="52" patternUnits="userSpaceOnUse">
            <polygon points="30,4 56,18 56,38 30,52 4,38 4,18" fill="none" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hex7)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }, (_, i) => {
        const seed = seededRandom(i + 55);
        const y = (seed * 1080 - frame * (0.6 + seed * 0.4)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            width: 7, height: 7,
            borderRadius: '50%',
            background: '#FEEC41',
            opacity: 0.5,
            left: seed * 1920,
            top: y < 0 ? y + 1080 : y,
          }} />
        );
      })}

      {/* Top brutal truth */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: truthOp,
      }}>
        <div style={{
          display: 'inline-block',
          background: '#000000',
          color: '#FF0000',
          padding: '16px 50px',
          fontFamily: 'Inter, Poppins',
          fontSize: 51,
          fontWeight: 900,
          letterSpacing: 5,
          border: '5px solid #FEEC41',
        }}>
          BRUTAL TRUTH UNCOVERED
        </div>
      </div>

      {/* Big 3 with label */}
      <div style={{
        position: 'absolute',
        top: 200,
        left: 0, right: 0,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 51,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 4,
          opacity: sp(30),
        }}>
          PLUS THESE
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 240,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 1,
          transform: `scale(${num3Scale})`,
          display: 'inline-block',
          textShadow: '10px 10px 0 #FEEC41',
        }}>
          3
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 51,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 4,
          marginTop: -20,
          opacity: sp(50),
        }}>
          TYPES WHO BENEFIT
        </div>
      </div>

      {/* 3 number boxes */}
      <div style={{
        position: 'absolute',
        bottom: 280,
        left: 0,
        right: 0,
        display: 'flex',
        justifyContent: 'center',
        gap: 40,
      }}>
        {[
          { n: '01', label: 'HEALTH', col: '#FEEC41' },
          { n: '02', label: 'DEBT', col: '#FEEC41' },
          { n: '03', label: 'SPOUSAL', col: '#FF0000', highlight: true },
        ].map((item, idx) => {
          const scale = [box1, box2, box3][idx];
          const pulse = item.highlight ? spot : 1;
          return (
            <div key={idx} style={{
              width: 280,
              height: 220,
              background: item.col,
              border: '8px solid #000000',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              transform: `scale(${scale * pulse})`,
              boxShadow: item.highlight ? '0 0 0 8px #FEEC41, 14px 14px 0 #000000' : '14px 14px 0 #000000',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 102,
                fontWeight: 900,
                color: item.highlight ? '#F5F5F5' : '#000000',
                lineHeight: 1,
              }}>
                {item.n}
              </div>
              <div style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 38,
                fontWeight: 900,
                color: item.highlight ? '#F5F5F5' : '#000000',
                letterSpacing: 3,
                marginTop: 8,
              }}>
                {item.label}
              </div>
            </div>
          );
        })}
      </div>

      {/* Bottom strategy reveal */}
      <div style={{
        position: 'absolute',
        bottom: 60,
        left: 0,
        right: 0,
        transform: `translateX(${stratX}px)`,
      }}>
        <div style={{
          background: '#000000',
          padding: '18px 0',
          textAlign: 'center',
          borderTop: '4px solid #FEEC41',
          borderBottom: '4px solid #FEEC41',
        }}>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 45,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: 3,
          }}>
            #3 ›{' '}
          </span>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 45,
            fontWeight: 900,
            color: '#FEEC41',
            letterSpacing: 3,
          }}>
            SPOUSAL BRIDGE STRATEGY
          </span>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_ThreeLoopholes;
```

```tsx
import React from 'react';
Audio: "...and it could save your marriage from financial ruin if one of you loses a job. Real quick before we get into it, if you're not subscribed to this channel yet, hit that button right now and turn on notifications."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_SubscribeBell',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_SubscribeBell: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Marriage scenario top
  const marrOp = sp(5);

  // Two ring icons
  const ring1 = sp(20);
  const ring2 = sp(35);
  const crack = interpolate(frame, [55, 90], [0, 100], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Subscribe button
  const subScale = sp(140, { damping: 7, stiffness: 100 });
  const subPulse = 1 + Math.sin(Math.max(0, frame - 160) * 0.2) * 0.05;

  // Bell wiggle
  const bellRot = Math.sin(Math.max(0, frame - 180) * 0.4) * 15;
  const bellOp = interpolate(frame, [180, 200], [0, 1], { extrapolateRight: 'clamp' });

  // Arrow point
  const arrowX = interpolate(sp(250), [0, 1], [-200, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #FCD1D1 0%, #f0a8a8 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 900, height: 900,
        background: '#FEEC41',
        opacity: 0.05,
        borderRadius: 100,
        top: -200,
        left: -300,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 600, height: 600,
        borderRadius: '50%',
        border: '50px solid #000000',
        opacity: 0.05,
        bottom: -150,
        right: -150,
      }} />

      {/* Circles texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.08 }}>
        <defs>
          <pattern id="circ8" width="70" height="70" patternUnits="userSpaceOnUse">
            <circle cx="35" cy="35" r="3" stroke="#000000" strokeWidth="1.5" fill="none"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#circ8)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }, (_, i) => {
        const seed = seededRandom(i + 70);
        return (
          <div key={i} style={{
            position: 'absolute',
            width: 5, height: 5,
            background: '#000000',
            opacity: 0.4,
            left: (seed * 1920 + frame * (0.3 + seed * 0.5)) % 1920,
            top: seed * 1080,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Top: marriage protection */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: marrOp,
      }}>
        <div style={{
          display: 'inline-block',
          background: '#000000',
          color: '#FEEC41',
          padding: '14px 40px',
          fontFamily: 'Inter, Poppins',
          fontSize: 47,
          fontWeight: 900,
          letterSpacing: 4,
        }}>
          SAVES YOUR MARRIAGE FROM RUIN
        </div>
      </div>

      {/* Two rings */}
      <div style={{
        position: 'absolute',
        top: 200,
        left: 0, right: 0,
        textAlign: 'center',
      }}>
        <svg width="540" height="240" style={{ display: 'inline-block' }} viewBox="0 0 540 240">
          {/* Ring 1 */}
          <g transform={`translate(160, 120) scale(${ring1})`}>
            <circle cx="0" cy="0" r="85" stroke="#000000" strokeWidth="14" fill="none"/>
            <circle cx="0" cy="-85" r="14" fill="#FEEC41" stroke="#000000" strokeWidth="3"/>
          </g>
          {/* Ring 2 */}
          <g transform={`translate(340, 120) scale(${ring2})`}>
            <circle cx="0" cy="0" r="85" stroke="#000000" strokeWidth="14" fill="none"/>
            <circle cx="0" cy="-85" r="14" fill="#FEEC41" stroke="#000000" strokeWidth="3"/>
          </g>
          {/* Crack line through */}
          <path
            d={`M 250 60 L ${250 + crack * 0.4} 100 L ${240 + crack * 0.5} 140 L ${260 + crack * 0.6} 180`}
            stroke="#FF0000"
            strokeWidth="8"
            fill="none"
            strokeLinecap="round"
          />
        </svg>
      </div>

      {/* "JOB LOSS" label */}
      <div style={{
        position: 'absolute',
        top: 380,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: interpolate(frame, [80, 110], [0, 1], { extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          display: 'inline-block',
          background: '#FF0000',
          color: '#F5F5F5',
          padding: '10px 30px',
          fontFamily: 'Inter, Poppins',
          fontSize: 38,
          fontWeight: 900,
          letterSpacing: 3,
          transform: 'rotate(-3deg)',
        }}>
          ↑ JOB LOSS ↑
        </div>
      </div>

      {/* Subscribe section bottom */}
      <div style={{
        position: 'absolute',
        bottom: 200,
        left: 0, right: 0,
        textAlign: 'center',
        transform: `scale(${subScale * subPulse})`,
      }}>
        <div style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: 30,
          background: '#FF0000',
          padding: '24px 60px',
          border: '8px solid #000000',
          boxShadow: '14px 14px 0 #000000',
        }}>
          <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
            <polygon points="6,4 20,12 6,20" fill="#F5F5F5" stroke="#000000" strokeWidth="2"/>
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 76,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: 5,
          }}>
            SUBSCRIBE
          </div>
          {/* Bell */}
          <div style={{
            transform: `rotate(${bellRot}deg)`,
            opacity: bellOp,
            transformOrigin: 'top center',
          }}>
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
              <path d="M18 16v-5a6 6 0 00-12 0v5l-2 2v1h16v-1l-2-2z" fill="#FEEC41" stroke="#000000" strokeWidth="2"/>
              <path d="M10 21a2 2 0 004 0" stroke="#000000" strokeWidth="2"/>
            </svg>
          </div>
        </div>
      </div>

      {/* Arrow pointing */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: 0, right: 0,
        textAlign: 'center',
        transform: `translateX(${arrowX}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 47,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: 3,
        }}>
          TURN ON NOTIFICATIONS NOW ↓
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_SubscribeBell;
```

```tsx
import React from 'react';
Audio: "The rules around social security change every single year. I cover all of that in plain English, so you always know what you're actually entitled to before it's too late. The people who are subscribed stay ahead of this stuff."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_RulesChange',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_RulesChange: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Year flip
  const years = ['2024', '2025', '2026', '2027'];
  const yearIdx = Math.floor((frame / 18) % years.length);

  // Top headline
  const topOp = sp(5);

  // Year wheel
  const wheelScale = sp(25);

  // Plain English banner
  const banOp = interpolate(frame, [90, 115], [0, 1], { extrapolateRight: 'clamp' });
  const banY = interpolate(sp(90), [0, 1], [80, 0]);

  // Two columns: AHEAD vs BEHIND
  const colL = sp(160);
  const colR = sp(190);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #FCD1D1 0%, #e89e9e 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 700, height: 700,
        borderRadius: '50%',
        background: '#FEEC41',
        opacity: 0.06,
        top: -200,
        right: -200,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        width: 500, height: 500,
        background: '#000000',
        opacity: 0.04,
        bottom: -100,
        left: -100,
        transform: `rotate(${30 + frame * 0.1}deg)`,
        borderRadius: 60,
      }} />

      {/* Diagonal lines */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.06 }}>
        <defs>
          <pattern id="diag9" width="50" height="50" patternUnits="userSpaceOnUse" patternTransform="rotate(-45)">
            <line x1="0" y1="0" x2="0" y2="50" stroke="#000000" strokeWidth="2"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag9)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }, (_, i) => {
        const seed = seededRandom(i + 85);
        return (
          <div key={i} style={{
            position: 'absolute',
            width: 5, height: 5,
            background: '#FEEC41',
            opacity: 0.5,
            left: seed * 1920,
            top: (seed * 1080 + frame * (0.4 + seed)) % 1080,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Top: Rules change */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: topOp,
      }}>
        <div style={{
          display: 'inline-block',
          background: '#000000',
          color: '#FEEC41',
          padding: '18px 50px',
          fontFamily: 'Inter, Poppins',
          fontSize: 55,
          fontWeight: 900,
          letterSpacing: 4,
        }}>
          RULES CHANGE EVERY YEAR
        </div>
      </div>

      {/* Year flipping wheel */}
      <div style={{
        position: 'absolute',
        top: 200,
        left: 0, right: 0,
        textAlign: 'center',
        transform: `scale(${wheelScale})`,
      }}>
        <div style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: 40,
        }}>
          {/* Left arrow */}
          <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
            <path d="M15 18l-6-6 6-6" stroke="#000000" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round"/>
          </svg>

          {/* Year box */}
          <div style={{
            background: '#FEEC41',
            border: '8px solid #000000',
            padding: '20px 70px',
            boxShadow: '12px 12px 0 #000000',
            position: 'relative',
            overflow: 'hidden',
          }}>
            <div key={yearIdx} style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 196,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1,
              animation: 'none',
            }}>
              {years[yearIdx]}
            </div>
          </div>

          {/* Right arrow */}
          <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
            <path d="M9 6l6 6-6 6" stroke="#000000" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round"/>
          </svg>
        </div>
      </div>

      {/* Plain English banner */}
      <div style={{
        position: 'absolute',
        top: 530,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: banOp,
        transform: `translateY(${banY}px)`,
      }}>
        <div style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: 25,
          background: '#000000',
          padding: '16px 50px',
          border: '5px solid #FEEC41',
        }}>
          <svg width="55" height="55" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="10" stroke="#FEEC41" strokeWidth="2.5" fill="none"/>
            <path d="M8 12l3 3 5-6" stroke="#FEEC41" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round"/>
          </svg>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 45,
            fontWeight: 900,
            color: '#FEEC41',
            letterSpacing: 3,
          }}>
            EXPLAINED IN PLAIN ENGLISH
          </span>
        </div>
      </div>

      {/* Two columns: AHEAD vs BEHIND */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: 120,
        right: 120,
        display: 'flex',
        gap: 50,
      }}>
        {/* AHEAD */}
        <div style={{
          flex: 1,
          background: '#FEEC41',
          border: '8px solid #000000',
          padding: '20px 30px',
          transform: `scale(${colL})`,
          boxShadow: '10px 10px 0 #000000',
          display: 'flex',
          alignItems: 'center',
          gap: 20,
        }}>
          <svg width="70" height="70" viewBox="0 0 24 24" fill="none">
            <path d="M5 12l5 5L20 6" stroke="#000000" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round"/>
          </svg>
          <div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 2,
            }}>
              SUBSCRIBED
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 700,
              color: '#000000',
            }}>
              STAYS AHEAD
            </div>
          </div>
        </div>

        {/* BEHIND */}
        <div style={{
          flex: 1,
          background: '#000000',
          border: '8px solid #FF0000',
          padding: '20px 30px',
          transform: `scale(${colR})`,
          boxShadow: '10px 10px 0 #000000',
          display: 'flex',
          alignItems: 'center',
          gap: 20,
        }}>
          <svg width="70" height="70" viewBox="0 0 24 24" fill="none">
            <line x1="6" y1="6" x2="18" y2="18" stroke="#FF0000" strokeWidth="4" strokeLinecap="round"/>
            <line x1="18" y1="6" x2="6" y2="18" stroke="#FF0000" strokeWidth="4" strokeLinecap="round"/>
          </svg>
          <div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 900,
              color: '#FF0000',
              letterSpacing: 2,
            }}>
              NOT SUBSCRIBED
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 700,
              color: '#F5F5F5',
            }}>
              FINDS OUT TOO LATE
            </div>
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_RulesChange;
```

```tsx
import React from 'react';
Audio: "The people who aren't tend to find out too late, right when they realize they can't undo their mistake. So subscribe now and let's get into it."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_TooLate',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_TooLate: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Clock rotation
  const clockHandRot = frame * 6;

  // Top label
  const topOp = sp(5);

  // Hourglass shake
  const shake = frame > 60 && frame < 90 ? Math.sin(frame * 1.5) * 5 : 0;

  // TOO LATE big text
  const tooLateScale = sp(80, { damping: 6 });
  const tooLateOp = interpolate(frame, [80, 105], [0, 1], { extrapolateRight: 'clamp' });

  // Cannot undo stamp
  const stampScale = sp(140);
  const stampRot = interpolate(sp(140), [0, 1], [25, -8]);

  // Subscribe CTA
  const ctaScale = sp(220, { damping: 7 });
  const ctaPulse = 1 + Math.sin(Math.max(0, frame - 240) * 0.2) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 40%, #FCD1D1 0%, #d89090 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        width: 1000, height: 1000,
        borderRadius: '50%',
        border: '60px solid #000000',
        opacity: 0.05,
        top: -300,
        left: -300,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        width: 700, height: 700,
        background: '#FEEC41',
        opacity: 0.07,
        bottom: -250,
        right: -250,
        transform: `rotate(${45 + frame * 0.15}deg)`,
        borderRadius: 80,
      }} />

      {/* Concentric circle texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.06 }}>
        <defs>
          <pattern id="circ10" width="100" height="100" patternUnits="userSpaceOnUse">
            <circle cx="50" cy="50" r="30" stroke="#000000" strokeWidth="1.5" fill="none"/>
            <circle cx="50" cy="50" r="15" stroke="#000000" strokeWidth="1.5" fill="none"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#circ10)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }, (_, i) => {
        const seed = seededRandom(i + 100);
        return (
          <div key={i} style={{
            position: 'absolute',
            width: 6, height: 6,
            background: '#000000',
            opacity: 0.4,
            left: (seed * 1920 + frame * (0.5 + seed * 0.3)) % 1920,
            top: seed * 1080,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translate(${shake}px, 0)` }}>

        {/* Top label */}
        <div style={{
          position: 'absolute',
          top: 50,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: topOp,
        }}>
          <div style={{
            display: 'inline-block',
            background: '#000000',
            color: '#FF0000',
            padding: '14px 40px',
            fontFamily: 'Inter, Poppins',
            fontSize: 47,
            fontWeight: 900,
            letterSpacing: 4,
          }}>
            THE CONSEQUENCE
          </div>
        </div>

        {/* Center: Clock + TOO LATE */}
        <div style={{
          position: 'absolute',
          top: 180,
          left: 0, right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 60,
        }}>
          {/* Clock */}
          <div style={{ transform: `scale(${sp(30)})` }}>
            <svg width="360" height="360" viewBox="0 0 200 200">
              <circle cx="100" cy="100" r="92" fill="#FEEC41" stroke="#000000" strokeWidth="8"/>
              {/* Hour ticks */}
              {Array.from({ length: 12 }).map((_, i) => {
                const angle = (i * 30) * Math.PI / 180;
                const x1 = 100 + Math.sin(angle) * 76;
                const y1 = 100 - Math.cos(angle) * 76;
                const x2 = 100 + Math.sin(angle) * 86;
                const y2 = 100 - Math.cos(angle) * 86;
                return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#000000" strokeWidth="4"/>;
              })}
              {/* Spinning hand */}
              <line
                x1="100" y1="100"
                x2={100 + Math.sin(clockHandRot * Math.PI / 180) * 70}
                y2={100 - Math.cos(clockHandRot * Math.PI / 180) * 70}
                stroke="#FF0000" strokeWidth="6" strokeLinecap="round"
              />
              <line x1="100" y1="100" x2="100" y2="50" stroke="#000000" strokeWidth="8" strokeLinecap="round"/>
              <circle cx="100" cy="100" r="8" fill="#000000"/>
            </svg>
          </div>

          {/* TOO LATE */}
          <div style={{
            transform: `scale(${tooLateScale})`,
            opacity: tooLateOp,
            textAlign: 'left',
          }}>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 196,
              fontWeight: 900,
              color: '#FF0000',
              lineHeight: 0.9,
              textShadow: '8px 8px 0 #000000',
            }}>
              TOO
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 196,
              fontWeight: 900,
              color: '#FF0000',
              lineHeight: 0.9,
              textShadow: '8px 8px 0 #000000',
            }}>
              LATE!
            </div>
          </div>
        </div>

        {/* CAN'T UNDO stamp */}
        <div style={{
          position: 'absolute',
          top: 580,
          left: 220,
          transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
        }}>
          <div style={{
            border: '8px solid #000000',
            padding: '14px 40px',
            background: '#FEEC41',
            fontFamily: 'Inter, Poppins',
            fontSize: 55,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: 4,
          }}>
            ✗ CAN'T UNDO
          </div>
        </div>

        {/* Bottom: SUBSCRIBE CTA */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 0, right: 0,
          textAlign: 'center',
          transform: `scale(${ctaScale * ctaPulse})`,
        }}>
          <div style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 30,
            background: '#FF0000',
            color: '#F5F5F5',
            padding: '24px 70px',
            border: '8px solid #000000',
            boxShadow: '12px 12px 0 #000000',
            fontFamily: 'Inter, Poppins',
            fontSize: 76,
            fontWeight: 900,
            letterSpacing: 5,
          }}>
            <svg width="70" height="70" viewBox="0 0 24 24" fill="none">
              <polygon points="6,4 20,12 6,20" fill="#F5F5F5" stroke="#000000" strokeWidth="2"/>
            </svg>
            SUBSCRIBE NOW
          </div>
        </div>

      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_TooLate;
```

