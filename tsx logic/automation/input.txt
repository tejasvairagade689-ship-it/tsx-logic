```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_QuestionPerspective',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene1_QuestionPerspective: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // Micro-animations & Springs
  const textSpring = spring({ frame: Math.max(0, frame - 10), fps, config: { damping: 12, stiffness: 90 } });
  const subSpring = spring({ frame: Math.max(0, frame - 25), fps, config: { damping: 12, stiffness: 90 } });
  const iconScale = spring({ frame: Math.max(0, frame - 15), fps, config: { damping: 10, stiffness: 120 } });
  
  // Continuous micro float/rotation
  const floatY = Math.sin(frame * 0.05) * 8;
  const pulse = interpolate(Math.sin(frame * 0.08), [-1, 1], [0.97, 1.03]);
  const bgShapeRotate = frame * 0.1;

  // Exit animation
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Poppins, sans-serif' }}>
      {/* Background Animated Shapes (Premium Light Grey theme) */}
      <div style={{
        position: 'absolute', width: 1200, height: 1200, borderRadius: '30%',
        backgroundColor: '#EAEAEA', top: -300, right: -200,
        transform: `rotate(${bgShapeRotate}deg)`, opacity: 0.6
      }} />
      <div style={{
        position: 'absolute', width: 800, height: 800, borderRadius: '50%',
        border: '2px dashed #CCCCCC', bottom: -200, left: -100,
        transform: `rotate(-${bgShapeRotate * 0.5}deg)`, opacity: 0.4
      }} />

      {/* Main Content Layout */}
      <div style={{ display: 'flex', width: '100%', height: '100%', alignItems: 'center', justifyContent: 'space-between', padding: '0 120px' }}>
        
        {/* Left Typography Area */}
        <div style={{ width: '60%', zIndex: 10 }}>
          <h1 style={{
            fontSize: '110px', fontWeight: 900, color: '#1C1C1C', lineHeight: '1.1',
            transform: `translateY(${(1 - textSpring) * 100}px)`, opacity: textSpring,
            marginBottom: '30px'
          }}>
            एक नज़रिया जो <span style={{ color: '#ff0000' }}>जिंदगी</span> बदल दे
          </h1>
          <p style={{
            fontSize: '52px', fontWeight: 600, color: '#2D2D2D',
            transform: `translateY(${(1 - subSpring) * 80}px)`, opacity: subSpring,
            borderLeft: '8px solid #FFC107', paddingLeft: '25px'
          }}>
            क्या आपने कभी खुद से यह सवाल पूछा है?
          </p>
        </div>

        {/* Right Graphic/Icon Area (Question Mark Micro-animation) */}
        <div style={{ width: '35%', display: 'flex', justifyContent: 'center', alignItems: 'center', transform: `scale(${iconScale}) translateY(${floatY}px)` }}>
          <div style={{
            width: '380px', height: '380px', borderRadius: '50%', backgroundColor: '#FFFFFF',
            boxShadow: '0 20px 40px rgba(0,0,0,0.05)', display: 'flex', justifyContent: 'center', alignItems: 'center',
            border: '4px solid #1C1C1C', transform: `scale(${pulse})`
          }}>
            <svg width="180" height="180" viewBox="0 0 24 24" fill="none" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
              <circle cx="12" cy="12" r="10"></circle>
              <path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"></path>
              <line x1="12" y1="17" x2="12.01" y2="17"></line>
            </svg>
          </div>
        </div>
      </div>

      {/* Stick Figure Character (Bottom Left) */}
      <div style={{
        position: 'absolute', bottom: '20px', left: '80px',
        transform: `translateY(${Math.sin(frame * 0.06) * 4}px)`, zIndex: 20
      }}>
        <svg width="140" height="220" viewBox="0 0 100 200" fill="none">
          {/* Head */}
          <circle cx="50" cy="40" r="22" fill="#1C1C1C" />
          {/* Golden Glasses */}
          <circle cx="42" cy="38" r="7" stroke="#FFC107" strokeWidth="3" fill="#F5F5F5" />
          <circle cx="62" cy="38" r="7" stroke="#FFC107" strokeWidth="3" fill="#F5F5F5" />
          <line x1="49" y1="38" x2="55" y2="38" stroke="#FFC107" strokeWidth="2" />
          {/* Body */}
          <line x1="50" y1="62" x2="50" y2="130" stroke="#1C1C1C" strokeWidth="8" strokeLinecap="round" />
          {/* Arms */}
          <line x1="50" y1="80" x2="20" y2="100" stroke="#1C1C1C" strokeWidth="7" strokeLinecap="round" />
          <line x1="50" y1="80" x2="80" y2="70" stroke="#1C1C1C" strokeWidth="7" strokeLinecap="round" />
          {/* Legs */}
          <line x1="50" y1="130" x2="30" y2="190" stroke="#1C1C1C" strokeWidth="8" strokeLinecap="round" />
          <line x1="50" y1="130" x2="70" y2="190" stroke="#1C1C1C" strokeWidth="8" strokeLinecap="round" />
        </svg>
      </div>

      {/* Subtle Premium Vignette */}
      <div style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%', boxShadow: 'inset 0 0 100px rgba(0,0,0,0.03)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: '30px', right: '45px', fontSize: '22px', fontWeight: 700, color: '#9E9E9E', opacity: 0.4, fontFamily: 'Poppins' }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1_QuestionPerspective;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_BrokenPromise',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene2_BrokenPromise: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // Animation layout triggers
  const contractIn = spring({ frame: Math.max(0, frame - 5), fps, config: { damping: 12 } });
  const textIn = spring({ frame: Math.max(0, frame - 15), fps, config: { damping: 12 } });
  const breakTrigger = spring({ frame: Math.max(0, frame - 100), fps, config: { stiffness: 200, damping: 8 } });

  // Micro-shaking during break
  const shakeX = breakTrigger > 0 && breakTrigger < 1 ? Math.sin(frame * 3) * 15 : 0;
  const splitLeft = interpolate(breakTrigger, [0, 1], [0, -40]);
  const splitRight = interpolate(breakTrigger, [0, 1], [0, 40]);
  const splitRotate = interpolate(breakTrigger, [0, 1], [0, 15]);

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Poppins, sans-serif' }}>
      {/* Structural Minimal Grids */}
      <div style={{ position: 'absolute', width: '100%', height: '100%', backgroundImage: 'radial-gradient(#D0D0D0 1px, transparent 1px)', backgroundSize: '40px 40px', opacity: 0.5 }} />

      <div style={{ display: 'flex', width: '100%', height: '100%', alignItems: 'center', justifyContent: 'center', transform: `translateX(${shakeX}px)` }}>
        
        {/* Left Block: Conditions & Rules */}
        <div style={{ width: '45%', paddingLeft: '100px', zIndex: 5 }}>
          <div style={{ transform: `scale(${textIn})` }}>
            <span style={{ backgroundColor: '#1C1C1C', color: '#FFFFFF', fontSize: '32px', fontWeight: 700, padding: '8px 20px', borderRadius: '6px', textTransform: 'uppercase' }}>
              Lifetime Effort
            </span>
            <h2 style={{ fontSize: '85px', fontWeight: 900, color: '#1C1C1C', marginTop: '20px', lineHeight: '1.1' }}>
              शर्तें मानी,<br/>नियमों पर चले
            </h2>
            <p style={{ fontSize: '45px', color: '#666666', marginTop: '15px', fontWeight: 500 }}>
              बदले में मिला सिर्फ एक <span style={{ color: '#ff0000', fontWeight: 800 }}>खोखला वादा</span>
            </p>
          </div>
        </div>

        {/* Right Block: The Breaking Promise Graphic */}
        <div style={{ width: '55%', display: 'flex', justifyContent: 'center', alignItems: 'center', transform: `scale(${contractIn})` }}>
          <div style={{ position: 'relative', width: '450px', height: '550px' }}>
            
            {/* Unified Document background before break */}
            <div style={{
              position: 'absolute', width: '100%', height: '100%', backgroundColor: '#FFFFFF',
              border: '6px solid #1C1C1C', borderRadius: '16px', boxShadow: '0 25px 50px rgba(0,0,0,0.08)',
              display: breakTrigger > 0 ? 'none' : 'flex', flexDirection: 'column', padding: '40px', alignItems: 'center'
            }}>
              <div style={{ width: '80%', height: '12px', backgroundColor: '#1C1C1C', borderRadius: '6px', marginBottom: '30px' }} />
              <div style={{ width: '100%', height: '8px', backgroundColor: '#CCCCCC', marginBottom: '20px' }} />
              <div style={{ width: '90%', height: '8px', backgroundColor: '#CCCCCC', marginBottom: '20px' }} />
              <div style={{ width: '95%', height: '8px', backgroundColor: '#CCCCCC', marginBottom: '60px' }} />
              {/* Handshake Icon */}
              <svg width="120" height="120" viewBox="0 0 24 24" fill="none" stroke="#FFC107" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                <path d="M12 12c-2-2-5-2-7 0s-2 5 0 7 5 2 7 0 2-5 0-7z"></path>
                <path d="M15 9c2-2 5-2 7 0s2 5 0 7-5 2-7 0-2-5 0-7z"></path>
              </svg>
            </div>

            {/* Broken Document Left Piece */}
            <div style={{
              position: 'absolute', width: '50%', height: '100%', backgroundColor: '#FFFFFF',
              borderLeft: '6px solid #1C1C1C', borderTop: '6px solid #1C1C1C', borderBottom: '6px solid #1C1C1C',
              borderRadius: '16px 0 0 16px', borderRight: '4px dashed #ff0000',
              display: breakTrigger > 0 ? 'block' : 'none',
              transform: `translateX(${splitLeft}px) rotate(-${splitRotate}deg)`, transformOrigin: 'bottom left',
              boxShadow: '0 25px 50px rgba(0,0,0,0.08)'
            }} />

            {/* Broken Document Right Piece */}
            <div style={{
              position: 'absolute', right: 0, width: '50%', height: '100%', backgroundColor: '#FFFFFF',
              borderRight: '6px solid #1C1C1C', borderTop: '6px solid #1C1C1C', borderBottom: '6px solid #1C1C1C',
              borderRadius: '0 16px 16px 0', borderLeft: '4px dashed #ff0000',
              display: breakTrigger > 0 ? 'block' : 'none',
              transform: `translateX(${splitRight}px) rotate(${splitRotate}deg)`, transformOrigin: 'bottom right',
              boxShadow: '0 25px 50px rgba(0,0,0,0.08)'
            }} />

            {/* Absolute overlay text when broken */}
            {breakTrigger > 0.5 && (
              <div style={{
                position: 'absolute', top: '45%', left: '-15%', width: '130%', textAlign: 'center',
                backgroundColor: '#ff0000', color: '#FFFFFF', padding: '15px', fontWeight: 900,
                fontSize: '48px', transform: 'rotate(-5deg)', border: '4px solid #1C1C1C', zIndex: 20
              }}>
                PROMISE BROKEN
              </div>
            )}
          </div>
        </div>
      </div>

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: '30px', right: '45px', fontSize: '22px', fontWeight: 700, color: '#9E9E9E', opacity: 0.4 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_BrokenPromise;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_GlobalCrisis',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene3_GlobalCrisis: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // Multi-beat timings
  const titleIn = spring({ frame, fps, config: { damping: 12 } });
  const usaIn = spring({ frame: Math.max(0, frame - 30), fps });
  const euIn = spring({ frame: Math.max(0, frame - 60), fps });
  const indIn = spring({ frame: Math.max(0, frame - 90), fps });

  // Continual rotation of global nodes
  const nodeBreath = Math.sin(frame * 0.05) * 10;

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Poppins, sans-serif' }}>
      {/* Background Graphic Lines representing Connectivity */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', zIndex: 1 }}>
        <line x1="300" y1="600" x2="960" y2="400" stroke="#CCCCCC" strokeWidth="4" strokeDasharray="10 10" />
        <line x1="960" y1="400" x2="1500" y2="650" stroke="#CCCCCC" strokeWidth="4" strokeDasharray="10 10" />
      </svg>

      {/* Top Header */}
      <div style={{ position: 'absolute', top: '80px', width: '100%', textAlign: 'center', zIndex: 10, transform: `scale(${titleIn})` }}>
        <h2 style={{ fontSize: '90px', fontWeight: 900, color: '#1C1C1C', margin: 0 }}>
          वैश्विक <span style={{ color: '#ff0000' }}>पेंशन</span> संकट
        </h2>
        <p style={{ fontSize: '40px', fontWeight: 600, color: '#666666', marginTop: '10px' }}>
          ढांचा जो शुरू से ही खोखला था
        </p>
      </div>

      {/* Concept Global Nodes Layout */}
      <div style={{ display: 'flex', width: '100%', height: '100%', alignItems: 'center', justifyContent: 'space-around', paddingTop: '150px', zIndex: 5 }}>
        
        {/* Node 1: USA */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', transform: `scale(${usaIn}) translateY(${nodeBreath}px)` }}>
          <div style={{
            width: '220px', height: '220px', borderRadius: '50%', backgroundColor: '#FFFFFF',
            border: '6px solid #1C1C1C', display: 'flex', justifyContent: 'center', alignItems: 'center',
            boxShadow: '0 15px 30px rgba(0,0,0,0.1)', position: 'relative'
          }}>
            <svg width="90" height="90" viewBox="0 0 24 24" fill="none" stroke="#1C1C1C" strokeWidth="2">
              <circle cx="12" cy="12" r="10"/>
              <line x1="2" y1="12" x2="22" y2="12"/>
              <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
            </svg>
            {/* Micro Warning Dot */}
            <div style={{ position: 'absolute', top: 10, right: 10, width: 25, height: 25, backgroundColor: '#ff0000', borderRadius: '50%' }} />
          </div>
          <span style={{ fontSize: '42px', fontWeight: 800, color: '#1C1C1C', marginTop: '20px' }}>AMERICA</span>
        </div>

        {/* Node 2: Europe */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', transform: `scale(${euIn}) translateY(${-nodeBreath}px)` }}>
          <div style={{
            width: '280px', height: '280px', borderRadius: '50%', backgroundColor: '#FFFFFF',
            border: '8px solid #1C1C1C', display: 'flex', justifyContent: 'center', alignItems: 'center',
            boxShadow: '0 20px 40px rgba(0,0,0,0.12)', position: 'relative'
          }}>
            <svg width="110" height="110" viewBox="0 0 24 24" fill="none" stroke="#1C1C1C" strokeWidth="2">
              <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
              <circle cx="12" cy="10" r="3"></circle>
            </svg>
            <div style={{ position: 'absolute', top: 15, right: 15, width: 30, height: 30, backgroundColor: '#ff0000', borderRadius: '50%' }} />
          </div>
          <span style={{ fontSize: '48px', fontWeight: 800, color: '#1C1C1C', marginTop: '20px' }}>EUROPE</span>
        </div>

        {/* Node 3: India */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', transform: `scale(${indIn}) translateY(${nodeBreath}px)` }}>
          <div style={{
            width: '220px', height: '220px', borderRadius: '50%', backgroundColor: '#FFFFFF',
            border: '6px solid #1C1C1C', display: 'flex', justifyContent: 'center', alignItems: 'center',
            boxShadow: '0 15px 30px rgba(0,0,0,0.1)', position: 'relative'
          }}>
            <svg width="90" height="90" viewBox="0 0 24 24" fill="none" stroke="#FFC107" strokeWidth="2.5">
              <polygon points="12 2 15 9 22 9 17 14 19 21 12 17 5 21 7 14 2 9 9 9 12 2"></polygon>
            </svg>
            <div style={{ position: 'absolute', top: 10, right: 10, width: 25, height: 25, backgroundColor: '#ff0000', borderRadius: '50%' }} />
          </div>
          <span style={{ fontSize: '42px', fontWeight: 800, color: '#1C1C1C', marginTop: '20px' }}>INDIA</span>
        </div>

      </div>

      {/* Bottom Sticky message */}
      <div style={{
        position: 'absolute', bottom: '60px', left: '0', width: '100%', textAlign: 'center',
        opacity: interpolate(frame, [100, 120], [0, 1], { extrapolateLeft: 'clamp' })
      }}>
        <span style={{ backgroundColor: '#1C1C1C', color: '#FFC107', fontSize: '38px', fontWeight: 700, padding: '12px 35px', borderRadius: '50px' }}>
          किसी ने सच नहीं देखना चाहा
        </span>
      </div>

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: '30px', right: '45px', fontSize: '22px', fontWeight: 700, color: '#9E9E9E', opacity: 0.4 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3_GlobalCrisis;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4_HostWarningIntro',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene4_HostWarningIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // Entrances
  const bannerY = spring({ frame, fps, config: { damping: 14, stiffness: 80 } });
  const contentScale = spring({ frame: Math.max(0, frame - 20), fps });
  
  // Continual micro ripple effect emitting from center icon
  const ripple1 = (frame % 60) / 60;
  const ripple2 = ((frame + 30) % 60) / 60;

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Poppins, sans-serif' }}>
      
      {/* Top Professional Branding Strip */}
      <div style={{
        position: 'absolute', top: 0, left: 0, width: '100%', height: '120px',
        backgroundColor: '#1C1C1C', display: 'flex', alignItems: 'center', justifyContent: 'space-between',
        padding: '0 80px', transform: `translateY(${(1 - bannerY) * -120}px)`, zIndex: 20
      }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '20px' }}>
          <div style={{ width: '30px', height: '30px', backgroundColor: '#FFC107', borderRadius: '50%' }} />
          <span style={{ color: '#FFFFFF', fontSize: '42px', fontWeight: 900, letterSpacing: '2px' }}>PROBUFFET HQ</span>
        </div>
        <span style={{ color: '#FFC107', fontSize: '32px', fontWeight: 600 }}>HOST: KUNAL</span>
      </div>

      {/* Central Warning Container */}
      <div style={{ display: 'flex', flexDirection: 'column', width: '100%', height: '100%', alignItems: 'center', justifyContent: 'center', paddingTop: '80px', transform: `scale(${contentScale})` }}>
        
        {/* Animated Ripple Icon Layout */}
        <div style={{ position: 'relative', width: '220px', height: '220px', display: 'flex', alignItems: 'center', justifyContent: 'center', marginBottom: '40px' }}>
          {/* Ripple Circles */}
          <div style={{ position: 'absolute', width: '100%', height: '100%', borderRadius: '50%', border: '4px solid #ff0000', transform: `scale(${1 + ripple1 * 1.5})`, opacity: 1 - ripple1 }} />
          <div style={{ position: 'absolute', width: '100%', height: '100%', borderRadius: '50%', border: '4px solid #ff0000', transform: `scale(${1 + ripple2 * 1.5})`, opacity: 1 - ripple2 }} />
          
          {/* Main Solid Icon */}
          <div style={{ width: '160px', height: '160px', borderRadius: '50%', backgroundColor: '#ff0000', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 5, border: '6px solid #1C1C1C' }}>
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="#FFFFFF" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round">
              <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"></path>
              <line x1="12" y1="9" x2="12" y2="13"></line>
              <line x1="12" y1="17" x2="12.01" y2="17"></line>
            </svg>
          </div>
        </div>

        {/* Text Area */}
        <h2 style={{ fontSize: '80px', fontWeight: 900, color: '#1C1C1C', textAlign: 'center', margin: 0 }}>
          बातें जो आपको <span style={{ color: '#ff0000', textDecoration: 'underline' }}>असहज</span> करेंगी
        </h2>
        <p style={{ fontSize: '48px', fontWeight: 700, color: '#2D2D2D', marginTop: '20px', maxWidth: '1400px', textAlign: 'center', lineHeight: '1.4' }}>
          "आज की बात थोड़ी तकलीफदेह है, लेकिन <span style={{ backgroundColor: '#FFC107', padding: '0 15px', color: '#1C1C1C' }}>बहुत जरूरी</span> है। ध्यान से सुनिए।"
        </p>
      </div>

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: '30px', right: '45px', fontSize: '22px', fontWeight: 700, color: '#9E9E9E', opacity: 0.4 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4_HostWarningIntro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5_TeacherTimeline',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene5_TeacherTimeline: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // Timeline Fill Math
  const lineProgress = interpolate(frame, [20, 400], [0, 1000], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });
  const yearCounter = Math.floor(interpolate(frame, [20, 400], [1990, 2024], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' }));
  
  // Element popups along timeline
  const startPop = spring({ frame: Math.max(0, frame - 20), fps });
  const endPop = spring({ frame: Math.max(0, frame - 400), fps });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Poppins, sans-serif' }}>
      
      <div style={{ padding: '80px 100px', width: '100%', height: '100%', display: 'flex', flexDirection: 'column', justifyContent: 'space-between' }}>
        
        {/* Narrative Top */}
        <div>
          <span style={{ fontSize: '36px', fontWeight: 800, color: '#ff0000', textTransform: 'uppercase' }}>Case Study: 34 Years of Dedication</span>
          <h2 style={{ fontSize: '85px', fontWeight: 900, color: '#1C1C1C', marginTop: '10px', lineHeight: 1.1 }}>
            सरकारी स्कूल का शिक्षक
          </h2>
          <p style={{ fontSize: '42px', fontWeight: 600, color: '#666666', marginTop: '5px' }}>
            "तनख्वाह कम थी... पर भरोसा था कि बुढ़ापा सुरक्षित रहेगा।"
          </p>
        </div>

        {/* Central Interactive Dynamic Timeline */}
        <div style={{ position: 'relative', width: '100%', height: '300px', display: 'flex', alignItems: 'center' }}>
          
          {/* Base Blank Line */}
          <div style={{ position: 'absolute', width: '1000px', height: '16px', backgroundColor: '#CCCCCC', left: '300px', borderRadius: '8px' }} />
          
          {/* Active Growing Line */}
          <div style={{ position: 'absolute', width: `${lineProgress}px`, height: '16px', backgroundColor: '#1C1C1C', left: '300px', borderRadius: '8px' }} />

          {/* Micro Icon travelling along line */}
          <div style={{
            position: 'absolute', left: `${300 + lineProgress - 40}px`, top: '80px', zIndex: 10,
            transform: `translateY(${Math.sin(frame * 0.1) * 5}px)`
          }}>
            <div style={{ width: '80px', height: '80px', borderRadius: '50%', backgroundColor: '#FFC107', border: '4px solid #1C1C1C', display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
              <svg width="45" height="45" viewBox="0 0 24 24" fill="none" stroke="#1C1C1C" strokeWidth="2.5">
                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                <circle cx="12" cy="7" r="4"></circle>
              </svg>
            </div>
          </div>

          {/* Point A: 1990 */}
          <div style={{ position: 'absolute', left: '260px', top: '20px', textAlign: 'center', transform: `scale(${startPop})` }}>
            <div style={{ width: '30px', height: '30px', borderRadius: '50%', backgroundColor: '#1C1C1C', margin: '0 auto 15px' }} />
            <span style={{ fontSize: '55px', fontWeight: 900, color: '#1C1C1C' }}>1990</span>
            <div style={{ fontSize: '28px', fontWeight: 600, color: '#888888' }}>नौकरी शुरू</div>
          </div>

          {/* Dynamic Floating Year Counter Display */}
          <div style={{ position: 'absolute', left: '720px', top: '-120px', textAlign: 'center' }}>
            <span style={{ fontSize: '130px', fontWeight: 900, color: '#E0E0E0' }}>{yearCounter}</span>
          </div>

          {/* Point B: 2024 Retirement Door */}
          <div style={{ position: 'absolute', left: '1260px', top: '-60px', textAlign: 'center', transform: `scale(${endPop})` }}>
            {/* SVG Door Icon */}
            <svg width="100" height="120" viewBox="0 0 24 24" fill="none" stroke="#ff0000" strokeWidth="2">
              <path d="M18 20V4a2 2 0 0 0-2-2H8a2 2 0 0 0-2 2v16"></path>
              <path d="M2 20h20"></path>
              <circle cx="14" cy="12" r="1"></circle>
            </svg>
            <div style={{ fontSize: '65px', fontWeight: 900, color: '#ff0000', lineHeight: 1 }}>2024</div>
            <div style={{ fontSize: '32px', fontWeight: 800, color: '#1C1C1C' }}>रिटायरमेंट का दरवाजा</div>
          </div>

        </div>

        {/* Footer Punch */}
        <div style={{ backgroundColor: '#1C1C1C', padding: '25px 40px', borderRadius: '12px', display: 'flex', alignItems: 'center', gap: '30px' }}>
          <span style={{ color: '#FFC107', fontSize: '50px', fontWeight: 900 }}>34</span>
          <p style={{ color: '#FFFFFF', fontSize: '36px', margin: 0, fontWeight: 600 }}>
            साल तक लगातार वेतन से पैसा कटवाया... पर आखिर में क्या बचा?
          </p>
        </div>

      </div>

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: '30px', right: '45px', fontSize: '22px', fontWeight: 700, color: '#9E9E9E', opacity: 0.4 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5_TeacherTimeline;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6_EmptyVaultConcept',
  durationInSeconds: 34,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene6_EmptyVaultConcept: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // Animations
  const vaultScale = spring({ frame, fps, config: { damping: 12 } });
  const bottomOpen = spring({ frame: Math.max(0, frame - 60), fps, config: { stiffness: 150 } });
  
  // Coin dropping physics simulation
  const dropY1 = interpolate(frame, [70, 120], [0, 800], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const dropY2 = interpolate(frame, [80, 130], [0, 800], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const dropY3 = interpolate(frame, [90, 140], [0, 800], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Floating micro-movements
  const vaultHover = Math.sin(frame * 0.08) * 8;

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Poppins, sans-serif' }}>
      
      {/* Dynamic diagonal background pattern */}
      <div style={{ position: 'absolute', width: '200%', height: '100%', background: 'repeating-linear-gradient(45deg, #EFEFEF, #EFEFEF 20px, #F5F5F5 20px, #F5F5F5 40px)' }} />

      <div style={{ display: 'flex', width: '100%', height: '100%', alignItems: 'center', justifyContent: 'center', gap: '150px', zIndex: 5 }}>
        
        {/* Left Info Text */}
        <div style={{ width: '45%' }}>
          <div style={{ display: 'inline-block', backgroundColor: '#ff0000', color: '#FFFFFF', fontSize: '32px', fontWeight: 800, padding: '8px 25px', borderRadius: '50px', marginBottom: '20px' }}>
            THE HARSH REALITY
          </div>
          <h2 style={{ fontSize: '95px', fontWeight: 900, color: '#1C1C1C', lineHeight: 1.1, margin: 0 }}>
            फंड में पैसे<br/>है ही नहीं!
          </h2>
          <p style={{ fontSize: '44px', fontWeight: 600, color: '#444444', marginTop: '30px', borderLeft: '6px solid #1C1C1C', paddingLeft: '20px' }}>
            "पेंशन का बुनियादी नियम: आप कमाते हैं, एक हिस्सा फंड में जाता है... लेकिन तिजोरी का बेस गायब है।"
          </p>
        </div>

        {/* Right Graphic: The Broken Vault */}
        <div style={{ width: '40%', display: 'flex', justifyContent: 'center', transform: `scale(${vaultScale}) translateY(${vaultHover}px)` }}>
          <div style={{ position: 'relative', width: '400px', height: '450px' }}>
            
            {/* Vault Main Body */}
            <div style={{
              width: '100%', height: '350px', backgroundColor: '#1C1C1C', borderRadius: '24px 24px 0 0',
              border: '8px solid #2D2D2D', display: 'flex', justifyContent: 'center', alignItems: 'center',
              boxShadow: '0 30px 60px rgba(0,0,0,0.15)', zIndex: 10, position: 'relative'
            }}>
              {/* Giant Locker Wheel Icon */}
              <svg width="150" height="150" viewBox="0 0 24 24" fill="none" stroke="#FFC107" strokeWidth="2">
                <circle cx="12" cy="12" r="10"></circle>
                <circle cx="12" cy="12" r="4"></circle>
                <line x1="12" y1="2" x2="12" y2="8"></line>
                <line x1="12" y1="16" x2="12" y2="22"></line>
                <line x1="2" y1="12" x2="8" y2="12"></line>
                <line x1="16" y1="12" x2="22" y2="12"></line>
              </svg>
              <span style={{ position: 'absolute', top: 20, color: '#FFFFFF', fontSize: '24px', fontWeight: 800, letterSpacing: '3px' }}>GOVT FUND</span>
            </div>

            {/* Dropping Coins out the bottom */}
            {frame > 60 && (
              <>
                {/* Coin 1 */}
                <div style={{ position: 'absolute', left: '120px', top: `${350 + dropY1}px`, zIndex: 5 }}>
                  <circle style={{ width: '50px', height: '50px', borderRadius: '50%', backgroundColor: '#FFC107', border: '3px solid #1C1C1C', display: 'block' }} />
                </div>
                {/* Coin 2 */}
                <div style={{ position: 'absolute', left: '200px', top: `${350 + dropY2}px`, zIndex: 5 }}>
                  <circle style={{ width: '60px', height: '60px', borderRadius: '50%', backgroundColor: '#FFC107', border: '3px solid #1C1C1C', display: 'block' }} />
                </div>
                {/* Coin 3 */}
                <div style={{ position: 'absolute', left: '270px', top: `${350 + dropY3}px`, zIndex: 5 }}>
                  <circle style={{ width: '45px', height: '45px', borderRadius: '50%', backgroundColor: '#FFC107', border: '3px solid #1C1C1C', display: 'block' }} />
                </div>
              </>
            )}

            {/* Vault Trapdoor Bottom falling open */}
            <div style={{
              width: '100%', height: '30px', backgroundColor: '#ff0000', borderRadius: '0 0 12px 12px',
              border: '6px solid #1C1C1C', transformOrigin: 'top left',
              transform: `rotate(${bottomOpen * 75}deg)`, zIndex: 12
            }} />

          </div>
        </div>

      </div>

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: '30px', right: '45px', fontSize: '22px', fontWeight: 700, color: '#9E9E9E', opacity: 0.4 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6_EmptyVaultConcept;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7_DefinedBenefitPillars',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene7_DefinedBenefitPillars: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // Sequential Pillar build
  const p1 = spring({ frame: Math.max(0, frame - 15), fps });
  const p2 = spring({ frame: Math.max(0, frame - 35), fps });
  const p3 = spring({ frame: Math.max(0, frame - 55), fps });

  // Continual gear rotation animation logic inside pillars
  const gearRot = frame * 1.5;

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Poppins, sans-serif' }}>
      
      {/* Top Banner */}
      <div style={{ width: '100%', textAlign: 'center', marginTop: '60px' }}>
        <span style={{ fontSize: '32px', fontWeight: 700, color: '#666666', letterSpacing: '4px' }}>STRUCTURE OF</span>
        <h2 style={{ fontSize: '85px', fontWeight: 900, color: '#1C1C1C', margin: 0 }}>
          Defined Benefit Plan
        </h2>
      </div>

      {/* Three Connected Pillars */}
      <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'flex-end', gap: '80px', height: '650px', paddingBottom: '60px' }}>
        
        {/* Pillar 1 */}
        <div style={{ width: '380px', display: 'flex', flexDirection: 'column', alignItems: 'center', transform: `scaleY(${p1})`, transformOrigin: 'bottom' }}>
          <div style={{ width: '100%', height: '350px', backgroundColor: '#1C1C1C', borderRadius: '16px 16px 0 0', border: '6px solid #1C1C1C', display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center', color: '#FFFFFF' }}>
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="#FFC107" strokeWidth="2" style={{ transform: `rotate(${gearRot}deg)` }}>
              <circle cx="12" cy="12" r="10"></circle>
              <path d="M12 6v6l4 2"></path>
            </svg>
            <span style={{ fontSize: '32px', fontWeight: 800, marginTop: '20px' }}>शर्त #1</span>
          </div>
          <div style={{ width: '110%', backgroundColor: '#FFC107', padding: '15px', textAlign: 'center', fontWeight: 800, fontSize: '32px', color: '#1C1C1C', border: '4px solid #1C1C1C', borderRadius: '8px', marginTop: '-10px' }}>
            सटीक योगदान
          </div>
        </div>

        {/* Pillar 2 */}
        <div style={{ width: '380px', display: 'flex', flexDirection: 'column', alignItems: 'center', transform: `scaleY(${p2})`, transformOrigin: 'bottom' }}>
          <div style={{ width: '100%', height: '420px', backgroundColor: '#1C1C1C', borderRadius: '16px 16px 0 0', border: '6px solid #1C1C1C', display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center', color: '#FFFFFF' }}>
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="#ff0000" strokeWidth="2" style={{ transform: `rotate(-${gearRot}deg)` }}>
              <path d="M3 3v18h18"></path>
              <path d="M18 9l-5 5-3-3-5 5"></path>
            </svg>
            <span style={{ fontSize: '32px', fontWeight: 800, marginTop: '20px' }}>शर्त #2</span>
          </div>
          <div style={{ width: '110%', backgroundColor: '#ff0000', padding: '15px', textAlign: 'center', fontWeight: 800, fontSize: '32px', color: '#FFFFFF', border: '4px solid #1C1C1C', borderRadius: '8px', marginTop: '-10px' }}>
            अनुमानित रिटर्न
          </div>
        </div>

        {/* Pillar 3 */}
        <div style={{ width: '380px', display: 'flex', flexDirection: 'column', alignItems: 'center', transform: `scaleY(${p3})`, transformOrigin: 'bottom' }}>
          <div style={{ width: '100%', height: '350px', backgroundColor: '#1C1C1C', borderRadius: '16px 16px 0 0', border: '6px solid #1C1C1C', display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center', color: '#FFFFFF' }}>
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="#FFC107" strokeWidth="2" style={{ transform: `rotate(${gearRot}deg)` }}>
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
              <circle cx="9" cy="7" r="4"></circle>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
              <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
            </svg>
            <span style={{ fontSize: '32px', fontWeight: 800, marginTop: '20px' }}>शर्त #3</span>
          </div>
          <div style={{ width: '110%', backgroundColor: '#FFC107', padding: '15px', textAlign: 'center', fontWeight: 800, fontSize: '32px', color: '#1C1C1C', border: '4px solid #1C1C1C', borderRadius: '8px', marginTop: '-10px' }}>
            आयु सीमा (Longevity)
          </div>
        </div>

      </div>

      {/* Footer Text Indicator */}
      <div style={{ width: '100%', textAlign: 'center', opacity: frame > 75 ? 1 : 0, transition: 'opacity 0.5s' }}>
        <p style={{ fontSize: '42px', fontWeight: 700, color: '#ff0000' }}>
          "इन तीनों में से एक भी बिगड़ा... तो पूरा सिस्टम ढह जाएगा।"
        </p>
      </div>

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: '30px', right: '45px', fontSize: '22px', fontWeight: 700, color: '#9E9E9E', opacity: 0.4 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7_DefinedBenefitPillars;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8_DeepDiveConditions',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene8_DeepDiveConditions: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // Dynamic Camera Pan effect targeting items based on timeline beats
  // Focus item 1: 0-300, Focus item 2: 300-600, Focus item 3: 600+
  const targetX = interpolate(frame, [0, 50, 250, 300, 550, 600], [0, 300, 300, -300, -300, -900], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  
  // Element breathing
  const float = Math.sin(frame * 0.05) * 10;

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Poppins, sans-serif' }}>
      
      {/* Smooth panning canvas display */}
      <div style={{
        display: 'flex', width: '300%', height: '100%', alignItems: 'center',
        transform: `translateX(${targetX}px)`, transition: 'transform 0.2s ease-out',
        paddingLeft: '300px', gap: '400px'
      }}>
        
        {/* Item 1 Layout */}
        <div style={{ width: '800px', display: 'flex', alignItems: 'center', gap: '50px' }}>
          <div style={{ width: '220px', height: '220px', borderRadius: '24px', backgroundColor: '#1C1C1C', display: 'flex', justifyContent: 'center', alignItems: 'center', transform: `translateY(${float}px)`, border: '6px solid #FFC107' }}>
            <svg width="100" height="100" viewBox="0 0 24 24" fill="none" stroke="#FFFFFF" strokeWidth="2">
              <path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"></path>
              <polyline points="17 21 17 13 7 13 7 21"></polyline>
              <polyline points="7 3 7 8 15 8"></polyline>
            </svg>
          </div>
          <div>
            <span style={{ fontSize: '38px', color: '#ff0000', fontWeight: 900 }}>CONDITION 1</span>
            <h3 style={{ fontSize: '75px', fontWeight: 900, color: '#1C1C1C', margin: 0, lineHeight: 1.1 }}>वादा = असलियत</h3>
            <p style={{ fontSize: '40px', color: '#555555', fontWeight: 600, marginTop: '10px' }}>जितना कंट्रीब्यूट करने का वादा था, उतना सच में फंड में आए।</p>
          </div>
        </div>

        {/* Item 2 Layout */}
        <div style={{ width: '800px', display: 'flex', alignItems: 'center', gap: '50px' }}>
          <div style={{ width: '220px', height: '220px', borderRadius: '24px', backgroundColor: '#1C1C1C', display: 'flex', justifyContent: 'center', alignItems: 'center', transform: `translateY(${float}px)`, border: '6px solid #ff0000' }}>
            <svg width="100" height="100" viewBox="0 0 24 24" fill="none" stroke="#FFFFFF" strokeWidth="2">
              <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"></polyline>
              <polyline points="17 6 23 6 23 12"></polyline>
            </svg>
          </div>
          <div>
            <span style={{ fontSize: '38px', color: '#ff0000', fontWeight: 900 }}>CONDITION 2</span>
            <h3 style={{ fontSize: '75px', fontWeight: 900, color: '#1C1C1C', margin: 0, lineHeight: 1.1 }}>अनुमानित रिटर्न</h3>
            <p style={{ fontSize: '40px', color: '#555555', fontWeight: 600, marginTop: '10px' }}>जितना ब्याज अज्यूम किया गया, बाजार वास्तव में उतना ही दे।</p>
          </div>
        </div>

        {/* Item 3 Layout */}
        <div style={{ width: '800px', display: 'flex', alignItems: 'center', gap: '50px' }}>
          <div style={{ width: '220px', height: '220px', borderRadius: '24px', backgroundColor: '#1C1C1C', display: 'flex', justifyContent: 'center', alignItems: 'center', transform: `translateY(${float}px)`, border: '6px solid #FFC107' }}>
            <svg width="100" height="100" viewBox="0 0 24 24" fill="none" stroke="#FFFFFF" strokeWidth="2">
              <path d="M12 2v20"></path>
              <path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"></path>
            </svg>
          </div>
          <div>
            <span style={{ fontSize: '38px', color: '#ff0000', fontWeight: 900 }}>CONDITION 3</span>
            <h3 style={{ fontSize: '75px', fontWeight: 900, color: '#1C1C1C', margin: 0, lineHeight: 1.1 }}>Longevity Math</h3>
            <p style={{ fontSize: '40px', color: '#555555', fontWeight: 600, marginTop: '10px' }}>लोग उतने साल ही जिएं, जितना कैलकुलेशन में माना गया था।</p>
          </div>
        </div>

      </div>

      {/* Static Visual Guides framing bottom screen */}
      <div style={{ position: 'absolute', bottom: 40, left: '50%', transform: 'translateX(-50%)', display: 'flex', gap: '20px' }}>
        <div style={{ width: 40, height: 8, backgroundColor: targetX > -100 ? '#1C1C1C' : '#CCCCCC', borderRadius: 4 }} />
        <div style={{ width: 40, height: 8, backgroundColor: targetX <= -100 && targetX > -700 ? '#1C1C1C' : '#CCCCCC', borderRadius: 4 }} />
        <div style={{ width: 40, height: 8, backgroundColor: targetX <= -700 ? '#1C1C1C' : '#CCCCCC', borderRadius: 4 }} />
      </div>

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: '30px', right: '45px', fontSize: '22px', fontWeight: 700, color: '#9E9E9E', opacity: 0.4 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8_DeepDiveConditions;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9_PoliticalInvisibility',
  durationInSeconds: 40,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene9_PoliticalInvisibility: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // Scales balance comparison logic
  const leftFade = interpolate(frame, [30, 90], [1, 0.15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const rightScale = spring({ frame: Math.max(0, frame - 60), fps, config: { damping: 10 } });

  // Continual hover logic
  const hover = Math.sin(frame * 0.05) * 6;

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Poppins, sans-serif' }}>
      
      {/* Title strip */}
      <div style={{ position: 'absolute', top: '50px', left: '100px', zIndex: 10 }}>
        <span style={{ backgroundColor: '#1C1C1C', color: '#FFFFFF', fontSize: '28px', fontWeight: 700, padding: '6px 18px', borderRadius: '4px' }}>POLITICAL INCENTIVE</span>
        <h2 style={{ fontSize: '80px', fontWeight: 900, color: '#1C1C1C', margin: '10px 0 0' }}>दिखावा vs अदृश्य भविष्य</h2>
      </div>

      {/* Main Split Layout Comparison */}
      <div style={{ display: 'flex', width: '100%', height: '100%', alignItems: 'center', justifyContent: 'center', paddingTop: '120px' }}>
        
        {/* Left item: Pension fund (Fades out / Invisible) */}
        <div style={{ width: '45%', display: 'flex', flexDirection: 'column', alignItems: 'center', opacity: leftFade, transition: 'opacity 0.1s' }}>
          <div style={{
            width: '280px', height: '280px', borderRadius: '50%', border: '8px dashed #1C1C1C',
            display: 'flex', justifyContent: 'center', alignItems: 'center', backgroundColor: '#EAEAEA'
          }}>
            <svg width="100" height="100" viewBox="0 0 24 24" fill="none" stroke="#1C1C1C" strokeWidth="2">
              <path d="M21 12V7H5a2 2 0 0 1 0-4h14v4"></path>
              <path d="M3 5v14a2 2 0 0 0 2 2h16v-5"></path>
              <path d="M18 12a2 2 0 0 0 0 4h4v-4h-4z"></path>
            </svg>
          </div>
          <h3 style={{ fontSize: '60px', fontWeight: 900, color: '#1C1C1C', marginTop: '25px', textAlign: 'center' }}>पेंशन फंड</h3>
          <span style={{ fontSize: '38px', fontWeight: 800, color: '#ff0000', backgroundColor: '#FFFFFF', padding: '5px 20px', border: '2px solid #ff0000', borderRadius: '50px', marginTop: '10px' }}>आज दिखता नहीं</span>
        </div>

        {/* Center Divider Grid Line */}
        <div style={{ width: '4px', height: '500px', backgroundColor: '#CCCCCC', borderRadius: '2px' }} />

        {/* Right item: Infrastructure (Highly Prominent / Scaled) */}
        <div style={{ width: '45%', display: 'flex', flexDirection: 'column', alignItems: 'center', transform: `scale(${rightScale}) translateY(${hover}px)` }}>
          
          <div style={{ display: 'flex', gap: '20px', marginBottom: '20px' }}>
            {/* Road Icon */}
            <div style={{ width: '130px', height: '130px', borderRadius: '16px', backgroundColor: '#1C1C1C', display: 'flex', justifyContent: 'center', alignItems: 'center', border: '4px solid #FFC107' }}>
              <svg width="60" height="60" viewBox="0 0 24 24" fill="none" stroke="#FFFFFF" strokeWidth="2">
                <path d="M3 21h18"></path>
                <path d="M5 21V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2v16"></path>
                <line x1="12" y1="7" x2="12" y2="11"></line>
                <line x1="12" y1="15" x2="12" y2="19"></line>
              </svg>
            </div>
            {/* Hospital Icon */}
            <div style={{ width: '130px', height: '130px', borderRadius: '16px', backgroundColor: '#1C1C1C', display: 'flex', justifyContent: 'center', alignItems: 'center', border: '4px solid #ff0000' }}>
              <svg width="60" height="60" viewBox="0 0 24 24" fill="none" stroke="#FFFFFF" strokeWidth="2">
                <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                <polyline points="9 22 9 12 15 12 15 22"></polyline>
                <line x1="12" y1="6" x2="12" y2="6.01"></line>
              </svg>
            </div>
          </div>

          <h3 style={{ fontSize: '60px', fontWeight: 900, color: '#1C1C1C', marginTop: '10px', textAlign: 'center' }}>सड़कें व अस्पताल</h3>
          <span style={{ fontSize: '38px', fontWeight: 800, color: '#1C1C1C', backgroundColor: '#FFC107', padding: '5px 20px', border: '4px solid #1C1C1C', borderRadius: '50px', marginTop: '10px' }}>जनता को साफ दिखता है</span>
        </div>

      </div>

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: '30px', right: '45px', fontSize: '22px', fontWeight: 700, color: '#9E9E9E', opacity: 0.4 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9_PoliticalInvisibility;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_ReturnAssumptionCrash',
  durationInSeconds: 46,
  fps: 30,
  width: 1920,
  height: 1080,
};

const Scene10_ReturnAssumptionCrash: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // Typography Spring Entry
  const textIn = spring({ frame, fps, config: { damping: 12 } });
  
  // Dramatic crashing chart animation triggered dynamically
  const crashY = spring({ frame: Math.max(0, frame - 50), fps, config: { stiffness: 180, damping: 9 } });
  
  // Line drawing effect for chart crash
  const chartDraw = interpolate(crashY, [0, 1], [0, 1200], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera Shake on strike
  const shake = crashY > 0.1 && crashY < 0.9 ? Math.sin(frame * 5) * 20 : 0;

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Poppins, sans-serif' }}>
      
      <div style={{ width: '100%', height: '100%', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', transform: `translateY(${shake}px)` }}>
        
        {/* Top Minimal Details */}
        <span style={{ fontSize: '32px', fontWeight: 800, color: '#ff0000', letterSpacing: '2px', textTransform: 'uppercase' }}>CORE ASSUMPTION FLAW</span>
        
        {/* Big Impact Layout text */}
        <div style={{ position: 'relative', marginTop: '20px', transform: `scale(${textIn})`, textAlign: 'center' }}>
          <h2 style={{ fontSize: '180px', fontWeight: 900, color: '#1C1C1C', lineHeight: 0.9, margin: 0 }}>
            7.5% <span style={{ color: '#FFC107', WebkitTextStroke: '4px #1C1C1C' }}>RETURN</span>
          </h2>
          <span style={{ fontSize: '50px', fontWeight: 800, color: '#666666', display: 'block', marginTop: '10px' }}>का अंधा विश्वास</span>
        </div>

        {/* Overlay Interactive Crash Chart element passing right through text */}
        <div style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%', pointerEvents: 'none', zIndex: 20 }}>
          <svg width="100%" height="100%">
            {/* Crashing thick red trending graph path */}
            <path
              d="M 200 300 L 700 450 L 1100 350 L 1700 950"
              fill="none"
              stroke="#ff0000"
              strokeWidth="24"
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeDasharray="2500"
              strokeDashoffset={2500 - (chartDraw * 2.1)}
            />
            {/* End Target Circle marker */}
            {crashY > 0.95 && (
              <circle cx="1700" cy="950" r="30" fill="#1C1C1C" stroke="#ff0000" strokeWidth="8" />
            )}
          </svg>
        </div>

        {/* Reality Check tag emerging post crash */}
        {crashY > 0.8 && (
          <div style={{
            position: 'absolute', bottom: '150px', right: '300px', backgroundColor: '#1C1C1C', color: '#FFFFFF',
            padding: '15px 40px', borderRadius: '8px', border: '4px solid #ff0000',
            animation: 'fadeIn 0.3s ease-out', zIndex: 25
          }}>
            <span style={{ fontSize: '48px', fontWeight: 900, display: 'block' }}>REALITY CRASH</span>
            <span style={{ fontSize: '28px', color: '#FFC107', fontWeight: 600 }}>Interest rates down to bottom levels</span>
          </div>
        )}

      </div>

      {/* Stick Figure Expression Shocked (Bottom Left) */}
      <div style={{ position: 'absolute', bottom: '20px', left: '80px', zIndex: 15 }}>
        <svg width="140" height="220" viewBox="0 0 100 200" fill="none">
          <circle cx="50" cy="40" r="22" fill="#1C1C1C" />
          {/* Glasses Askew from shock */}
          <g transform={`rotate(${crashY * 15} 50 40)`}>
            <circle cx="42" cy="38" r="7" stroke="#FFC107" strokeWidth="3" fill="#F5F5F5" />
            <circle cx="62" cy="38" r="7" stroke="#FFC107" strokeWidth="3" fill="#F5F5F5" />
            <line x1="49" y1="38" x2="55" y2="38" stroke="#FFC107" strokeWidth="2" />
          </g>
          {/* Shocked Mouth circle */}
          {crashY > 0.5 && <circle cx="50" cy="50" r="5" fill="#F5F5F5" />}
          <line x1="50" y1="62" x2="50" y2="130" stroke="#1C1C1C" strokeWidth="8" strokeLinecap="round" />
          {/* Hands holding head */}
          <line x1="50" y1="80" x2="30" y2="40" stroke="#1C1C1C" strokeWidth="7" strokeLinecap="round" />
          <line x1="50" y1="80" x2="70" y2="40" stroke="#1C1C1C" strokeWidth="7" strokeLinecap="round" />
          <line x1="50" y1="130" x2="30" y2="190" stroke="#1C1C1C" strokeWidth="8" strokeLinecap="round" />
          <line x1="50" y1="130" x2="70" y2="190" stroke="#1C1C1C" strokeWidth="8" strokeLinecap="round" />
        </svg>
      </div>

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: '30px', right: '45px', fontSize: '22px', fontWeight: 700, color: '#9E9E9E', opacity: 0.4 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_ReturnAssumptionCrash;
```

