```tsx
import React from 'react';
Audio: "If you are living on a fixed income and every trip to the grocery store or pharmacy is draining your bank account — stay with me. What I'm about to share could put hundreds of dollars back in your pocket every month."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_FixedIncomeHook',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_FixedIncomeHook: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom in
  const cameraScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08], { extrapolateRight: 'clamp' });

  // Headline reveal
  const headlineY = interpolate(sp(10), [0, 1], [80, 0]);
  const headlineOp = interpolate(sp(10), [0, 1], [0, 1]);

  // Subhead reveal
  const subOp = interpolate(sp(45), [0, 1], [0, 1]);
  const subY = interpolate(sp(45), [0, 1], [40, 0]);

  // Receipt animation
  const receiptScale = sp(80);
  const receiptDrain = interpolate(frame, [120, 280], [1, 0.15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Dollar drops
  const dollarCount = 8;

  // Bottom CTA
  const ctaOp = interpolate(sp(200), [0, 1], [0, 1]);
  const ctaY = interpolate(sp(200), [0, 1], [30, 0]);

  // Pulse on key word
  const pulse = 1 + Math.sin(frame * 0.15) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 30% 20%, rgba(250,185,91,0.15) 0%, rgba(65,45,21,0) 60%), radial-gradient(ellipse at 80% 90%, rgba(232,237,242,0.06) 0%, rgba(65,45,21,0) 55%)',
        }}
      />

      {/* Animated shape layers - large transparent geometric forms */}
      <div
        style={{
          position: 'absolute',
          left: -200,
          top: -150,
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '2px solid #FAB95B',
          opacity: 0.05,
          transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          right: -180,
          bottom: -200,
          width: 600,
          height: 600,
          borderRadius: '50%',
          backgroundColor: '#FAB95B',
          opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          left: '60%',
          top: '15%',
          width: 320,
          height: 320,
          border: '1px solid #E8EDF2',
          opacity: 0.06,
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      />

      {/* Motion texture - grid lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line
            key={`v${i}`}
            x1={i * 96 + (frame * 0.3) % 96}
            y1={0}
            x2={i * 96 + (frame * 0.3) % 96}
            y2={1080}
            stroke="#FAB95B"
            strokeWidth={1}
          />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 35 }).map((_, i) => {
        const seed = i + 1;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * (0.5 + seededRandom(seed * 3))) % 1080;
        const size = seededRandom(seed * 4) * 4 + 2;
        const op = seededRandom(seed * 5) * 0.5 + 0.2;
        return (
          <div
            key={`p${i}`}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: size,
              height: size,
              borderRadius: '50%',
              backgroundColor: '#FAB95B',
              opacity: op,
            }}
          />
        );
      })}

      {/* Main content container with camera scale */}
      <AbsoluteFill style={{ transform: `scale(${cameraScale})` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            display: 'flex',
            alignItems: 'center',
            gap: 18,
          }}
        >
          <div style={{ width: 60, height: 2, backgroundColor: '#FAB95B' }} />
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            For Seniors On Fixed Income
          </div>
          <div style={{ width: 60, height: 2, backgroundColor: '#FAB95B' }} />
        </div>

        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: 0,
            right: 0,
            textAlign: 'center',
            transform: `translateY(${headlineY}px)`,
            opacity: headlineOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 153,
              fontWeight: 900,
              color: '#E8EDF2',
              letterSpacing: -3,
              lineHeight: 1.0,
            }}
          >
            EVERY TRIP IS
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 187,
              fontWeight: 900,
              color: '#FAB95B',
              letterSpacing: -4,
              lineHeight: 1.0,
              marginTop: 10,
              transform: `scale(${pulse})`,
              textShadow: '0 0 50px rgba(250,185,91,0.4)',
            }}
          >
            DRAINING YOU
          </div>
        </div>

        {/* Sub headline */}
        <div
          style={{
            position: 'absolute',
            top: 580,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: subOp,
            transform: `translateY(${subY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 64,
              fontWeight: 500,
              color: '#E8EDF2',
              letterSpacing: -1,
              opacity: 0.85,
            }}
          >
            Groceries. Pharmacy. Bills.
          </div>
        </div>

        {/* Visual: Receipt being drained + dollar drops */}
        <div
          style={{
            position: 'absolute',
            top: 700,
            left: '50%',
            transform: `translateX(-50%) scale(${receiptScale})`,
            width: 340,
            height: 260,
          }}
        >
          {/* Receipt */}
          <svg width="340" height="260" viewBox="0 0 340 260">
            <defs>
              <linearGradient id="rec1" x1="0" y1="0" x2="0" y2="1">
                <stop offset="0" stopColor="#E8EDF2" />
                <stop offset="1" stopColor="#A8AEB5" />
              </linearGradient>
            </defs>
            <path
              d="M30 20 L310 20 L310 230 L290 220 L270 230 L250 220 L230 230 L210 220 L190 230 L170 220 L150 230 L130 220 L110 230 L90 220 L70 230 L50 220 L30 230 Z"
              fill="url(#rec1)"
            />
            <line x1="55" y1="60" x2="285" y2="60" stroke="#412D15" strokeWidth="2" />
            <line x1="55" y1="95" x2="240" y2="95" stroke="#412D15" strokeWidth="2" opacity={receiptDrain} />
            <line x1="55" y1="120" x2="260" y2="120" stroke="#412D15" strokeWidth="2" opacity={receiptDrain * 0.9} />
            <line x1="55" y1="145" x2="220" y2="145" stroke="#412D15" strokeWidth="2" opacity={receiptDrain * 0.8} />
            <line x1="55" y1="170" x2="270" y2="170" stroke="#412D15" strokeWidth="2" opacity={receiptDrain * 0.7} />
            <text x="170" y="200" fontFamily="JetBrains Mono" fontSize="22" fontWeight="700" textAnchor="middle" fill="#FF4444">
              TOTAL ▲
            </text>
          </svg>

          {/* Dollar drops falling */}
          {Array.from({ length: dollarCount }).map((_, i) => {
            const delay = 100 + i * 18;
            const startFrame = Math.max(0, frame - delay);
            const dropY = interpolate(startFrame, [0, 60], [0, 200], { extrapolateRight: 'clamp' });
            const dropOp = interpolate(startFrame, [0, 10, 50, 60], [0, 1, 1, 0], { extrapolateRight: 'clamp' });
            const xOffset = (i - dollarCount / 2) * 28;
            return (
              <div
                key={`d${i}`}
                style={{
                  position: 'absolute',
                  left: 170 + xOffset,
                  top: 140 + dropY,
                  opacity: dropOp,
                  fontFamily: 'Inter, Poppins',
                  fontSize: 32,
                  fontWeight: 800,
                  color: '#FAB95B',
                  textShadow: '0 0 12px rgba(250,185,91,0.6)',
                }}
              >
                $
              </div>
            );
          })}
        </div>

        {/* Bottom CTA strip */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: '50%',
            transform: `translateX(-50%) translateY(${ctaY}px)`,
            opacity: ctaOp,
            backgroundColor: '#FAB95B',
            padding: '18px 50px',
            borderRadius: 6,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 800,
              color: '#412D15',
              letterSpacing: 1,
              textTransform: 'uppercase',
            }}
          >
            Stay Till The End — Money Back In Your Pocket
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_FixedIncomeHook;
```

```tsx
import React from 'react';
Audio: "I mean real money — not 50 cents off a can of soup. I'm talking about five completely free government programs that pay for your groceries, prescriptions, and even home repairs."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_FiveFreePrograms',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_FiveFreePrograms: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pan camera
  const camX = interpolate(frame, [0, durationInFrames], [-20, 20]);

  // "50 cents" struck-through
  const struckOp = interpolate(sp(15), [0, 1], [0, 1]);
  const strikeWidth = interpolate(frame, [60, 100], [0, 600], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // BIG "5" number
  const fiveScale = sp(90, { stiffness: 90 });
  const fiveRot = interpolate(sp(90), [0, 1], [-25, 0]);

  // Three pill cards
  const cards = [
    { label: 'GROCERIES', delay: 150 },
    { label: 'PRESCRIPTIONS', delay: 175 },
    { label: 'HOME REPAIRS', delay: 200 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(250,185,91,0.08) 0%, rgba(65,45,21,0) 50%, rgba(232,237,242,0.04) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          left: '70%',
          top: '-10%',
          width: 500,
          height: 500,
          backgroundColor: '#FAB95B',
          opacity: 0.05,
          transform: `rotate(${45 + frame * 0.08}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          left: '-5%',
          top: '50%',
          width: 400,
          height: 400,
          borderRadius: '50%',
          border: '3px solid #E8EDF2',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <line
            key={`dl${i}`}
            x1={i * 80 - 200 + (frame * 0.5) % 80}
            y1={0}
            x2={i * 80 + (frame * 0.5) % 80}
            y2={1080}
            stroke="#FAB95B"
            strokeWidth={1}
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = i + 50;
        const x = (seededRandom(seed) * 1920 + frame * (0.3 + seededRandom(seed * 2) * 0.5)) % 1920;
        const y = seededRandom(seed * 3) * 1080;
        const size = seededRandom(seed * 4) * 3 + 2;
        return (
          <div
            key={`pt${i}`}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: size,
              height: size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#E8EDF2' : '#FAB95B',
              opacity: 0.35,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* Top: struck-through "50 cents off soup" */}
        <div
          style={{
            position: 'absolute',
            top: 110,
            left: '50%',
            transform: 'translateX(-50%)',
            opacity: struckOp,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <div style={{ position: 'relative' }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 68,
                fontWeight: 700,
                color: '#9E9E9E',
                letterSpacing: -1,
              }}
            >
              Not 50¢ off a can of soup
            </div>
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                height: 5,
                width: strikeWidth,
                backgroundColor: '#FF4444',
                transform: 'translateY(-50%)',
              }}
            />
          </div>
        </div>

        {/* Center HUGE "5" */}
        <div
          style={{
            position: 'absolute',
            top: 240,
            left: 0,
            right: 0,
            textAlign: 'center',
            transform: `scale(${fiveScale}) rotate(${fiveRot}deg)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 442,
              fontWeight: 900,
              color: '#FAB95B',
              letterSpacing: -10,
              lineHeight: 0.85,
              textShadow: '0 0 80px rgba(250,185,91,0.5)',
            }}
          >
            5
          </div>
        </div>

        {/* Right side label next to 5 */}
        <div
          style={{
            position: 'absolute',
            top: 350,
            left: '58%',
            opacity: interpolate(sp(110), [0, 1], [0, 1]),
            transform: `translateX(${interpolate(sp(110), [0, 1], [50, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 800,
              color: '#E8EDF2',
              letterSpacing: -1,
              lineHeight: 1.0,
            }}
          >
            FREE
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 800,
              color: '#E8EDF2',
              letterSpacing: -1,
              lineHeight: 1.0,
            }}
          >
            GOVERNMENT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 800,
              color: '#FAB95B',
              letterSpacing: -1,
              lineHeight: 1.0,
            }}
          >
            PROGRAMS
          </div>
        </div>

        {/* Three category cards */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 40,
          }}
        >
          {cards.map((c, idx) => {
            const cardSp = sp(c.delay, { stiffness: 100 });
            return (
              <div
                key={c.label}
                style={{
                  transform: `translateY(${interpolate(cardSp, [0, 1], [60, 0])}px) scale(${cardSp})`,
                  opacity: cardSp,
                  border: '3px solid #FAB95B',
                  backgroundColor: 'rgba(250,185,91,0.08)',
                  padding: '28px 44px',
                  borderRadius: 12,
                  display: 'flex',
                  alignItems: 'center',
                  gap: 18,
                }}
              >
                {/* SVG icon per card */}
                <svg width="56" height="56" viewBox="0 0 56 56">
                  {idx === 0 && (
                    <>
                      <rect x="10" y="18" width="36" height="30" rx="2" fill="none" stroke="#FAB95B" strokeWidth="3" />
                      <path d="M14 18 L18 8 L38 8 L42 18" fill="none" stroke="#FAB95B" strokeWidth="3" />
                      <line x1="22" y1="28" x2="22" y2="40" stroke="#FAB95B" strokeWidth="2" />
                      <line x1="34" y1="28" x2="34" y2="40" stroke="#FAB95B" strokeWidth="2" />
                    </>
                  )}
                  {idx === 1 && (
                    <>
                      <rect x="14" y="10" width="28" height="36" rx="14" fill="none" stroke="#FAB95B" strokeWidth="3" />
                      <line x1="14" y1="28" x2="42" y2="28" stroke="#FAB95B" strokeWidth="3" />
                    </>
                  )}
                  {idx === 2 && (
                    <>
                      <path d="M8 30 L28 12 L48 30" fill="none" stroke="#FAB95B" strokeWidth="3" strokeLinejoin="round" />
                      <rect x="14" y="28" width="28" height="20" fill="none" stroke="#FAB95B" strokeWidth="3" />
                      <rect x="24" y="36" width="8" height="12" fill="#FAB95B" />
                    </>
                  )}
                </svg>
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontSize: 36,
                    fontWeight: 800,
                    color: '#E8EDF2',
                    letterSpacing: 1,
                  }}
                >
                  {c.label}
                </div>
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_FiveFreePrograms;
```

```tsx
import React from 'react';
Audio: "Here's what most people never find out. The federal government is sitting on billions in unclaimed senior benefits — but they won't send a letter. You have to ask, and most seniors are too proud to."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_BillionsUnclaimed',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_BillionsUnclaimed: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Tilt camera
  const cameraTilt = interpolate(frame, [0, durationInFrames], [0, 2]);

  // Counter ticking up
  const counterProgress = interpolate(frame, [30, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const counterValue = Math.floor(counterProgress * 47);

  // Letter envelope shake / stamp
  const stampScale = sp(220, { stiffness: 80 });
  const stampRot = interpolate(sp(220), [0, 1], [-30, -12]);

  const titleOp = interpolate(sp(0), [0, 1], [0, 1]);
  const titleY = interpolate(sp(0), [0, 1], [-40, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 40%, rgba(250,185,91,0.18) 0%, rgba(65,45,21,0) 60%)',
        }}
      />

      {/* Shape layers - triangles */}
      <svg width="1920" height="1080" style={{ position: 'absolute' }}>
        <polygon
          points="200,100 400,500 0,400"
          fill="#FAB95B"
          opacity={0.05}
          transform={`rotate(${frame * 0.1} 200 333)`}
          style={{ transformOrigin: '200px 333px' }}
        />
        <polygon
          points="1700,200 1900,700 1500,600"
          fill="#E8EDF2"
          opacity={0.04}
          transform={`rotate(${-frame * 0.15} 1700 500)`}
          style={{ transformOrigin: '1700px 500px' }}
        />
      </svg>

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 15 }).map((_, i) => (
          <line key={`gh${i}`} x1={0} y1={i * 72} x2={1920} y2={i * 72} stroke="#FAB95B" strokeWidth={1} />
        ))}
      </svg>

      {/* Floating "$" particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const seed = i + 100;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1200 - frame * (0.4 + seededRandom(seed * 3) * 0.6)) % 1200;
        const op = 0.25 + seededRandom(seed * 4) * 0.3;
        return (
          <div
            key={`dol${i}`}
            style={{
              position: 'absolute',
              left: x,
              top: y < 0 ? y + 1200 : y,
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              color: '#FAB95B',
              opacity: op,
              fontWeight: 800,
            }}
          >
            $
          </div>
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${cameraTilt}deg)` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: '50%',
            transform: `translateX(-50%) translateY(${titleY}px)`,
            opacity: titleOp,
            fontFamily: 'Inter, Poppins',
            fontSize: 48,
            fontWeight: 600,
            color: '#E8EDF2',
            letterSpacing: 4,
            textTransform: 'uppercase',
          }}
        >
          The Federal Government Is Sitting On
        </div>

        {/* Counter - massive */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: 0,
            right: 0,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 80,
              fontWeight: 700,
              color: '#FAB95B',
              opacity: interpolate(sp(20), [0, 1], [0, 1]),
              letterSpacing: -1,
            }}
          >
            $
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 374,
              fontWeight: 900,
              color: '#FAB95B',
              lineHeight: 0.9,
              letterSpacing: -10,
              textShadow: '0 0 60px rgba(250,185,91,0.5)',
              opacity: interpolate(sp(20), [0, 1], [0, 1]),
            }}
          >
            {counterValue}B
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 51,
              fontWeight: 700,
              color: '#E8EDF2',
              letterSpacing: 6,
              marginTop: 10,
              textTransform: 'uppercase',
              opacity: interpolate(sp(100), [0, 1], [0, 1]),
            }}
          >
            In Unclaimed Senior Benefits
          </div>
        </div>

        {/* Envelope with "NEVER SENT" stamp */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: '50%',
            transform: 'translateX(-50%)',
            opacity: interpolate(sp(180), [0, 1], [0, 1]),
          }}
        >
          <div style={{ position: 'relative', width: 480, height: 180 }}>
            {/* Envelope SVG */}
            <svg width="480" height="180" viewBox="0 0 480 180">
              <rect x="10" y="20" width="460" height="140" rx="6" fill="#E8EDF2" stroke="#412D15" strokeWidth="3" />
              <path d="M10 20 L240 110 L470 20" fill="none" stroke="#412D15" strokeWidth="3" />
              <rect x="350" y="35" width="100" height="55" fill="#FAB95B" stroke="#412D15" strokeWidth="2" />
              <text x="400" y="70" fontFamily="JetBrains Mono" fontSize="20" fontWeight="700" textAnchor="middle" fill="#412D15">
                USPS
              </text>
              <text x="240" y="155" fontFamily="Inter" fontSize="22" fontWeight="700" textAnchor="middle" fill="#412D15">
                TO: SENIORS OF AMERICA
              </text>
            </svg>
            {/* Stamp */}
            <div
              style={{
                position: 'absolute',
                top: 30,
                left: 60,
                transform: `rotate(${stampRot}deg) scale(${stampScale})`,
                border: '5px solid #FF4444',
                padding: '8px 22px',
                fontFamily: 'Inter, Poppins',
                fontSize: 38,
                fontWeight: 900,
                color: '#FF4444',
                letterSpacing: 3,
                backgroundColor: 'rgba(255,68,68,0.08)',
              }}
            >
              NEVER SENT
            </div>
          </div>
        </div>

        {/* Side caption */}
        <div
          style={{
            position: 'absolute',
            bottom: 50,
            left: '50%',
            transform: 'translateX(-50%)',
            opacity: interpolate(sp(280), [0, 1], [0, 1]),
            fontFamily: 'Inter, Poppins',
            fontSize: 36,
            fontWeight: 700,
            color: '#FAB95B',
            letterSpacing: 2,
          }}
        >
          You have to ask. Most are too proud to.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_BillionsUnclaimed;
```

```tsx
import React from 'react';
Audio: "They think these programs are charity. They are not. You paid taxes for 40 years — this is a return on your investment. I went through the latest 2026 benefit limits and hidden state programs."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_NotCharityROI',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_NotCharityROI: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.05]);

  // "CHARITY" struck word
  const charityOp = interpolate(sp(0), [0, 1], [0, 1]);
  const strikeW = interpolate(frame, [40, 80], [0, 720], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // INVESTMENT word reveal
  const investScale = sp(90, { stiffness: 90 });

  // Bar chart - 40 years of tax bars
  const barsStart = 130;

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(65,45,21,1) 0%, rgba(82,55,28,1) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          left: '5%',
          bottom: '-15%',
          width: 500,
          height: 500,
          borderRadius: '50%',
          backgroundColor: '#FAB95B',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          right: '8%',
          top: '10%',
          width: 320,
          height: 320,
          border: '2px solid #E8EDF2',
          opacity: 0.07,
          transform: `rotate(${frame * 0.15}deg)`,
        }}
      />

      {/* Concentric circles texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <circle
            key={`cc${i}`}
            cx={960}
            cy={540}
            r={150 + i * 120 + Math.sin(frame * 0.04 + i) * 8}
            fill="none"
            stroke="#FAB95B"
            strokeWidth={1}
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = i + 200;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.6) % 1080;
        return (
          <div
            key={`pp${i}`}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 3,
              height: 3,
              borderRadius: '50%',
              backgroundColor: '#FAB95B',
              opacity: 0.4,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        {/* Top: "NOT CHARITY" with strike */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: charityOp,
          }}
        >
          <div style={{ position: 'relative', display: 'inline-block' }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 119,
                fontWeight: 900,
                color: '#9E9E9E',
                letterSpacing: -2,
              }}
            >
              NOT CHARITY
            </div>
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: '50%',
                transform: 'translate(-50%, -50%)',
                width: strikeW,
                height: 8,
                backgroundColor: '#FF4444',
              }}
            />
          </div>
        </div>

        {/* Center: A RETURN ON YOUR INVESTMENT */}
        <div
          style={{
            position: 'absolute',
            top: 270,
            left: 0,
            right: 0,
            textAlign: 'center',
            transform: `scale(${investScale})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 500,
              color: '#E8EDF2',
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            It's A Return On
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 187,
              fontWeight: 900,
              color: '#FAB95B',
              letterSpacing: -4,
              lineHeight: 1.0,
              marginTop: 10,
              textShadow: '0 0 70px rgba(250,185,91,0.5)',
            }}
          >
            YOUR INVESTMENT
          </div>
        </div>

        {/* Bar chart: 40 years */}
        <div
          style={{
            position: 'absolute',
            bottom: 120,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            alignItems: 'flex-end',
            gap: 4,
            height: 180,
            opacity: interpolate(sp(180), [0, 1], [0, 1]),
          }}
        >
          {Array.from({ length: 40 }).map((_, i) => {
            const barDelay = barsStart + i * 4;
            const barH = interpolate(sp(barDelay), [0, 1], [0, 1]) * (40 + i * 3.2);
            return (
              <div
                key={`bar${i}`}
                style={{
                  width: 18,
                  height: barH,
                  backgroundColor: '#FAB95B',
                  borderRadius: '2px 2px 0 0',
                  boxShadow: '0 0 10px rgba(250,185,91,0.4)',
                }}
              />
            );
          })}
        </div>

        {/* Bar chart label */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            justifyContent: 'space-between',
            width: 880,
            fontFamily: 'JetBrains Mono',
            fontSize: 26,
            fontWeight: 600,
            color: '#E8EDF2',
            opacity: interpolate(sp(280), [0, 1], [0, 0.7]),
          }}
        >
          <div>YEAR 1</div>
          <div style={{ color: '#FAB95B' }}>40 YEARS OF TAXES PAID</div>
          <div>YEAR 40</div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_NotCharityROI;
```

```tsx
import React from 'react';
Audio: "And I found five programs millions of seniors are completely ignoring. Stay until the end — program number five is a massive home repair grant."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_FiveProgramsList',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_FiveProgramsList: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan up
  const camY = interpolate(frame, [0, durationInFrames], [20, -20]);

  const programs = [
    { num: '01', name: 'SNAP FOR SENIORS', tag: 'Groceries' },
    { num: '02', name: 'MEDICARE EXTRA HELP', tag: 'Prescriptions' },
    { num: '03', name: 'MEDICARE SAVINGS', tag: 'Doctor Bills' },
    { num: '04', name: 'LIHEAP', tag: 'Energy Bills' },
    { num: '05', name: 'SECTION 504', tag: 'Home Repairs', highlight: true },
  ];

  // Highlight pulse for #5
  const highlightPulse = 1 + Math.sin(frame * 0.12) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(90deg, rgba(65,45,21,1) 0%, rgba(95,65,32,1) 50%, rgba(65,45,21,1) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          left: '-10%',
          top: '20%',
          width: 450,
          height: 450,
          borderRadius: '50%',
          backgroundColor: '#FAB95B',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          right: '-5%',
          bottom: '10%',
          width: 380,
          height: 380,
          border: '3px solid #E8EDF2',
          opacity: 0.06,
          transform: `rotate(${45 + frame * 0.1}deg)`,
        }}
      />

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 20 }).map((_, i) =>
          Array.from({ length: 12 }).map((_, j) => (
            <circle
              key={`d${i}${j}`}
              cx={i * 100 + 50}
              cy={j * 90 + 45}
              r={1.5 + Math.sin(frame * 0.05 + i + j) * 0.5}
              fill="#FAB95B"
            />
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const seed = i + 300;
        const x = (seededRandom(seed) * 1920 + frame * 0.4) % 1920;
        const y = seededRandom(seed * 2) * 1080;
        return (
          <div
            key={`pa${i}`}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 3,
              height: 3,
              borderRadius: '50%',
              backgroundColor: i % 4 === 0 ? '#E8EDF2' : '#FAB95B',
              opacity: 0.4,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}
        >
          <div style={{ width: 70, height: 3, backgroundColor: '#FAB95B' }} />
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 47,
              fontWeight: 800,
              color: '#FAB95B',
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            The Ignored Five
          </div>
          <div style={{ width: 70, height: 3, backgroundColor: '#FAB95B' }} />
        </div>

        {/* Program rows */}
        <div
          style={{
            position: 'absolute',
            top: 180,
            left: 0,
            right: 0,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 18,
          }}
        >
          {programs.map((p, i) => {
            const rowSp = sp(40 + i * 30, { stiffness: 100 });
            const isHighlight = p.highlight;
            return (
              <div
                key={p.num}
                style={{
                  transform: `translateX(${interpolate(rowSp, [0, 1], [-200, 0])}px) scale(${isHighlight ? highlightPulse : 1})`,
                  opacity: rowSp,
                  width: 1200,
                  height: 120,
                  backgroundColor: isHighlight ? 'rgba(250,185,91,0.18)' : 'rgba(232,237,242,0.04)',
                  border: isHighlight ? '4px solid #FAB95B' : '2px solid rgba(232,237,242,0.2)',
                  borderRadius: 10,
                  display: 'flex',
                  alignItems: 'center',
                  padding: '0 40px',
                  gap: 36,
                  boxShadow: isHighlight ? '0 0 50px rgba(250,185,91,0.4)' : 'none',
                }}
              >
                <div
                  style={{
                    fontFamily: 'JetBrains Mono',
                    fontSize: 85,
                    fontWeight: 900,
                    color: isHighlight ? '#FAB95B' : '#9E9E9E',
                    letterSpacing: -2,
                    width: 130,
                  }}
                >
                  {p.num}
                </div>
                <div style={{ flex: 1 }}>
                  <div
                    style={{
                      fontFamily: 'Inter, Poppins',
                      fontSize: 51,
                      fontWeight: 800,
                      color: '#E8EDF2',
                      letterSpacing: 1,
                      lineHeight: 1.0,
                    }}
                  >
                    {p.name}
                  </div>
                  <div
                    style={{
                      fontFamily: 'Inter, Poppins',
                      fontSize: 30,
                      fontWeight: 500,
                      color: isHighlight ? '#FAB95B' : '#9E9E9E',
                      letterSpacing: 3,
                      marginTop: 4,
                      textTransform: 'uppercase',
                    }}
                  >
                    {p.tag}
                  </div>
                </div>
                {isHighlight && (
                  <div
                    style={{
                      fontFamily: 'Inter, Poppins',
                      fontSize: 26,
                      fontWeight: 800,
                      color: '#412D15',
                      backgroundColor: '#FAB95B',
                      padding: '8px 20px',
                      borderRadius: 4,
                      letterSpacing: 2,
                    }}
                  >
                    DON'T MISS
                  </div>
                )}
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_FiveProgramsList;
```

```tsx
import React from 'react';
Audio: "It can fix a leaking roof or broken plumbing for zero dollars, and almost nobody knows it exists. Quick — if you're not subscribed, hit that button and turn on notifications. The rules change every year."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_RoofZeroDollarsSubscribe',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_RoofZeroDollarsSubscribe: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08]);

  // House SVG entrance
  const houseSp = sp(20, { stiffness: 90 });

  // Water drop animation
  const dropCount = 5;

  // "$0" big reveal
  const zeroSp = sp(110, { stiffness: 80 });
  const zeroPulse = 1 + Math.sin(frame * 0.15) * 0.03;

  // Subscribe button shake
  const subSp = sp(220, { stiffness: 100 });
  const subShake = frame > 280 ? Math.sin(frame * 0.6) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 50%, rgba(250,185,91,0.15) 0%, rgba(65,45,21,0) 60%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          right: '5%',
          top: '5%',
          width: 400,
          height: 400,
          borderRadius: '50%',
          border: '2px solid #FAB95B',
          opacity: 0.05,
          transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />

      {/* Wavy lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <path
            key={`w${i}`}
            d={`M 0 ${i * 140 + 60} Q 480 ${i * 140 + 30 + Math.sin(frame * 0.04 + i) * 20} 960 ${i * 140 + 60} T 1920 ${i * 140 + 60}`}
            fill="none"
            stroke="#FAB95B"
            strokeWidth={1}
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i + 400;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.5) % 1080;
        return (
          <div
            key={`pf${i}`}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 3,
              height: 3,
              borderRadius: '50%',
              backgroundColor: '#FAB95B',
              opacity: 0.35,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Left side: House with leaking roof */}
        <div
          style={{
            position: 'absolute',
            top: 220,
            left: 130,
            transform: `scale(${houseSp})`,
          }}
        >
          <svg width="520" height="500" viewBox="0 0 520 500">
            {/* Roof */}
            <path d="M40 220 L260 60 L480 220 Z" fill="none" stroke="#FAB95B" strokeWidth="5" />
            {/* Roof crack */}
            <path d="M180 130 L220 170 L200 200 L240 230" fill="none" stroke="#FF4444" strokeWidth="4" strokeLinecap="round" />
            {/* House body */}
            <rect x="80" y="220" width="360" height="240" fill="none" stroke="#E8EDF2" strokeWidth="5" />
            {/* Door */}
            <rect x="230" y="320" width="80" height="140" fill="none" stroke="#E8EDF2" strokeWidth="4" />
            <circle cx="298" cy="395" r="4" fill="#E8EDF2" />
            {/* Windows */}
            <rect x="120" y="270" width="70" height="70" fill="none" stroke="#E8EDF2" strokeWidth="4" />
            <line x1="155" y1="270" x2="155" y2="340" stroke="#E8EDF2" strokeWidth="3" />
            <line x1="120" y1="305" x2="190" y2="305" stroke="#E8EDF2" strokeWidth="3" />
            <rect x="330" y="270" width="70" height="70" fill="none" stroke="#E8EDF2" strokeWidth="4" />
            <line x1="365" y1="270" x2="365" y2="340" stroke="#E8EDF2" strokeWidth="3" />
            <line x1="330" y1="305" x2="400" y2="305" stroke="#E8EDF2" strokeWidth="3" />
          </svg>

          {/* Water drops from roof crack */}
          {Array.from({ length: dropCount }).map((_, i) => {
            const dropDelay = 60 + i * 20;
            const startF = Math.max(0, frame - dropDelay);
            const cycleF = startF % 60;
            const dropY = interpolate(cycleF, [0, 60], [0, 300]);
            const dropOp = interpolate(cycleF, [0, 10, 50, 60], [0, 1, 1, 0]);
            return (
              <div
                key={`dr${i}`}
                style={{
                  position: 'absolute',
                  left: 215 + (i * 8 - 16),
                  top: 220 + dropY,
                  width: 8,
                  height: 14,
                  borderRadius: '50% 50% 50% 50% / 60% 60% 40% 40%',
                  backgroundColor: '#4A90E2',
                  opacity: dropOp,
                }}
              />
            );
          })}
        </div>

        {/* Right side: $0 reveal */}
        <div
          style={{
            position: 'absolute',
            top: 180,
            right: 100,
            width: 720,
            textAlign: 'center',
            transform: `scale(${zeroSp})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 47,
              fontWeight: 700,
              color: '#E8EDF2',
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            Cost To Fix It
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 374,
              fontWeight: 900,
              color: '#FAB95B',
              letterSpacing: -12,
              lineHeight: 1.0,
              transform: `scale(${zeroPulse})`,
              textShadow: '0 0 80px rgba(250,185,91,0.5)',
            }}
          >
            $0
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 40,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 3,
              textTransform: 'uppercase',
              marginTop: -10,
            }}
          >
            Almost Nobody Knows
          </div>
        </div>

        {/* Bottom: Subscribe CTA */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: '50%',
            transform: `translateX(-50%) scale(${subSp}) translateX(${subShake}px)`,
            opacity: subSp,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
            backgroundColor: '#FF4444',
            padding: '20px 50px',
            borderRadius: 50,
            boxShadow: '0 0 40px rgba(255,68,68,0.5)',
          }}
        >
          <svg width="50" height="50" viewBox="0 0 50 50">
            <rect x="5" y="10" width="40" height="30" rx="6" fill="#E8EDF2" />
            <polygon points="20,18 20,32 32,25" fill="#FF4444" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 43,
              fontWeight: 900,
              color: '#E8EDF2',
              letterSpacing: 2,
              textTransform: 'uppercase',
            }}
          >
            Subscribe + Turn On Notifications
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_RoofZeroDollarsSubscribe;
```

```tsx
import React from 'react';
Audio: "Income limits shift. New programs open. I cover it all in plain English so you know what you're entitled to. Subscribers stay ahead of this stuff."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_PlainEnglishRules',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_PlainEnglishRules: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [-15, 15]);

  // Left side: jargon document (messy)
  const leftSp = sp(10);
  // Right side: clean plain English
  const rightSp = sp(80);

  // Arrow transformation
  const arrowSp = sp(50);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(95,65,32,1) 0%, rgba(65,45,21,1) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          left: '40%',
          top: '-20%',
          width: 600,
          height: 600,
          borderRadius: '50%',
          backgroundColor: '#FAB95B',
          opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.07})`,
        }}
      />

      {/* Vertical line texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 16 }).map((_, i) => (
          <line
            key={`vl${i}`}
            x1={i * 120 + 60}
            y1={0}
            x2={i * 120 + 60}
            y2={1080}
            stroke="#E8EDF2"
            strokeWidth={1}
            strokeDasharray="4 8"
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const seed = i + 500;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.4) % 1080;
        return (
          <div
            key={`pg${i}`}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 3,
              height: 3,
              borderRadius: '50%',
              backgroundColor: '#FAB95B',
              opacity: 0.35,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>
        {/* Top title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            fontFamily: 'Inter, Poppins',
            fontSize: 64,
            fontWeight: 800,
            color: '#FAB95B',
            letterSpacing: 6,
            textTransform: 'uppercase',
          }}
        >
          The Rules Change Every Year
        </div>

        {/* Left: messy government jargon */}
        <div
          style={{
            position: 'absolute',
            top: 220,
            left: 120,
            width: 640,
            transform: `scale(${leftSp})`,
            opacity: leftSp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 700,
              color: '#9E9E9E',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 18,
            }}
          >
            Government Speak
          </div>
          <div
            style={{
              backgroundColor: 'rgba(232,237,242,0.06)',
              border: '2px solid rgba(232,237,242,0.2)',
              borderRadius: 8,
              padding: '32px 28px',
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              fontWeight: 500,
              color: '#9E9E9E',
              lineHeight: 1.5,
              filter: 'blur(0.6px)',
            }}
          >
            §1396a(a)(10)(E)(iii) — Pursuant to Title XIX, the SSI-related FBR for an individual filer shall not exceed 135% of the FPL with countable resources below the prescribed threshold...
          </div>
        </div>

        {/* Arrow in middle */}
        <div
          style={{
            position: 'absolute',
            top: 480,
            left: '50%',
            transform: `translateX(-50%) scale(${arrowSp})`,
          }}
        >
          <svg width="180" height="60" viewBox="0 0 180 60">
            <line x1="0" y1="30" x2="155" y2="30" stroke="#FAB95B" strokeWidth="6" />
            <polygon points="180,30 150,12 150,48" fill="#FAB95B" />
          </svg>
        </div>

        {/* Right: plain English */}
        <div
          style={{
            position: 'absolute',
            top: 220,
            right: 120,
            width: 640,
            transform: `scale(${rightSp})`,
            opacity: rightSp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 18,
            }}
          >
            Plain English
          </div>
          <div
            style={{
              backgroundColor: 'rgba(250,185,91,0.12)',
              border: '3px solid #FAB95B',
              borderRadius: 8,
              padding: '32px 28px',
              fontFamily: 'Inter, Poppins',
              fontSize: 40,
              fontWeight: 700,
              color: '#E8EDF2',
              lineHeight: 1.3,
              boxShadow: '0 0 40px rgba(250,185,91,0.3)',
            }}
          >
            If you make under <span style={{ color: '#FAB95B' }}>$20K/year</span>, the state pays your medical bills.
          </div>
        </div>

        {/* Bottom: Subscribers stay ahead */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(sp(180), [0, 1], [40, 0])}px)`,
            opacity: interpolate(sp(180), [0, 1], [0, 1]),
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            padding: '20px 44px',
            backgroundColor: '#FAB95B',
            borderRadius: 8,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 900,
              color: '#412D15',
              letterSpacing: 2,
              textTransform: 'uppercase',
            }}
          >
            Subscribers Stay Ahead
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_PlainEnglishRules;
```

```tsx
import React from 'react';
Audio: "So subscribe now. To understand why you need these programs, understand the fixed income squeeze. Every year the government gives a COLA — a cost of living adjustment — to your Social Security check."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_COLAExplained',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_COLAExplained: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.05]);

  // SS check entrance
  const checkSp = sp(20, { stiffness: 80 });

  // C-O-L-A letter reveal
  const colaLetters = ['C', 'O', 'L', 'A'];
  const colaWords = ['Cost', 'Of', 'Living', 'Adjustment'];

  // Squeeze visual
  const squeezeSp = sp(220);
  const squeezeAmount = interpolate(frame, [240, durationInFrames - 30], [0, 80], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 30%, rgba(250,185,91,0.12) 0%, rgba(65,45,21,0) 60%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          left: '-8%',
          top: '40%',
          width: 480,
          height: 480,
          borderRadius: '50%',
          border: '3px solid #FAB95B',
          opacity: 0.06,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          right: '-8%',
          bottom: '5%',
          width: 380,
          height: 380,
          backgroundColor: '#E8EDF2',
          opacity: 0.03,
          transform: `rotate(${45 + frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />

      {/* Hex texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <circle
            key={`hc${i}`}
            cx={i * 170 + 50}
            cy={50 + Math.sin(frame * 0.04 + i) * 12}
            r={4}
            fill="#FAB95B"
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i + 600;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.5) % 1080;
        return (
          <div
            key={`pq${i}`}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 3,
              height: 3,
              borderRadius: '50%',
              backgroundColor: '#FAB95B',
              opacity: 0.35,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            fontFamily: 'Inter, Poppins',
            fontSize: 51,
            fontWeight: 800,
            color: '#FAB95B',
            letterSpacing: 6,
            textTransform: 'uppercase',
          }}
        >
          The Fixed Income Squeeze
        </div>

        {/* Social Security Check */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: '50%',
            transform: `translateX(-50%) scale(${checkSp}) translateY(${squeezeAmount}px)`,
            width: 980 - squeezeAmount * 2,
          }}
        >
          <svg width="100%" height="280" viewBox="0 0 980 280" preserveAspectRatio="none">
            <rect x="4" y="4" width="972" height="272" fill="#E8EDF2" stroke="#412D15" strokeWidth="4" rx="8" />
            <rect x="4" y="4" width="972" height="50" fill="#FAB95B" />
            <text x="30" y="40" fontFamily="Inter" fontSize="26" fontWeight="800" fill="#412D15">
              SOCIAL SECURITY ADMINISTRATION
            </text>
            <text x="940" y="40" fontFamily="Inter" fontSize="22" fontWeight="700" fill="#412D15" textAnchor="end">
              2026
            </text>
            <text x="30" y="115" fontFamily="Inter" fontSize="22" fontWeight="600" fill="#9E9E9E">
              PAY TO
            </text>
            <text x="30" y="155" fontFamily="Inter" fontSize="38" fontWeight="800" fill="#412D15">
              SENIOR ON FIXED INCOME
            </text>
            <line x1="30" y1="180" x2="600" y2="180" stroke="#412D15" strokeWidth="1" />
            <text x="940" y="155" fontFamily="JetBrains Mono" fontSize="64" fontWeight="900" fill="#412D15" textAnchor="end">
              $1,907
            </text>
            <text x="30" y="230" fontFamily="Inter" fontSize="22" fontWeight="600" fill="#9E9E9E">
              MEMO: MONTHLY BENEFIT + COLA 2.5%
            </text>
            <text x="940" y="250" fontFamily="JetBrains Mono" fontSize="20" fontWeight="600" fill="#9E9E9E" textAnchor="end">
              #00472-2026
            </text>
          </svg>
        </div>

        {/* Squeeze arrows */}
        <div
          style={{
            position: 'absolute',
            top: 290,
            left: 80,
            opacity: squeezeSp,
            transform: `translateX(${squeezeAmount}px)`,
          }}
        >
          <svg width="120" height="100" viewBox="0 0 120 100">
            <polygon points="0,50 60,20 60,40 120,40 120,60 60,60 60,80" fill="#FF4444" />
          </svg>
        </div>
        <div
          style={{
            position: 'absolute',
            top: 290,
            right: 80,
            opacity: squeezeSp,
            transform: `translateX(${-squeezeAmount}px) scaleX(-1)`,
          }}
        >
          <svg width="120" height="100" viewBox="0 0 120 100">
            <polygon points="0,50 60,20 60,40 120,40 120,60 60,60 60,80" fill="#FF4444" />
          </svg>
        </div>

        {/* C O L A breakdown */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            gap: 22,
          }}
        >
          {colaLetters.map((L, i) => {
            const lSp = sp(120 + i * 25, { stiffness: 110 });
            return (
              <div
                key={L}
                style={{
                  transform: `scale(${lSp})`,
                  opacity: lSp,
                  textAlign: 'center',
                  border: '3px solid #FAB95B',
                  backgroundColor: 'rgba(250,185,91,0.08)',
                  padding: '14px 26px',
                  borderRadius: 8,
                  minWidth: 200,
                }}
              >
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontSize: 81,
                    fontWeight: 900,
                    color: '#FAB95B',
                    lineHeight: 1.0,
                    letterSpacing: -2,
                  }}
                >
                  {L}
                </div>
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontSize: 24,
                    fontWeight: 700,
                    color: '#E8EDF2',
                    letterSpacing: 2,
                    textTransform: 'uppercase',
                    marginTop: 4,
                  }}
                >
                  {colaWords[i]}
                </div>
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_COLAExplained;
```

```tsx
import React from 'react';
Audio: "They say it keeps up with inflation. But look at your real grocery bill, electric bill, pharmacy receipts — you know the truth. The COLA never keeps up with real-world inflation."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_COLAvsInflation',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_COLAvsInflation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Chart progress
  const chartProgress = interpolate(frame, [60, 240], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Build COLA line (low slope) and Inflation line (high slope)
  const chartW = 1100;
  const chartH = 420;
  const chartLeft = 410;
  const chartTop = 280;

  const colaPath = `M 0 ${chartH} L ${chartW * chartProgress * 1.0} ${chartH - 70 * chartProgress}`;
  const inflPath = `M 0 ${chartH} L ${chartW * chartProgress * 1.0} ${chartH - 320 * chartProgress}`;

  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.04]);

  // Gap label
  const gapOp = interpolate(sp(220), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(65,45,21,1) 0%, rgba(82,55,28,1) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          right: '-10%',
          top: '5%',
          width: 460,
          height: 460,
          borderRadius: '50%',
          backgroundColor: '#FAB95B',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }}
      />

      {/* Grid bg for chart */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <line key={`gx${i}`} x1={chartLeft + i * 100} y1={chartTop} x2={chartLeft + i * 100} y2={chartTop + chartH} stroke="#E8EDF2" strokeWidth={1} />
        ))}
        {Array.from({ length: 5 }).map((_, i) => (
          <line key={`gy${i}`} x1={chartLeft} y1={chartTop + i * 100} x2={chartLeft + chartW} y2={chartTop + i * 100} stroke="#E8EDF2" strokeWidth={1} />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = i + 700;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.5) % 1080;
        return (
          <div
            key={`pz${i}`}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 3,
              height: 3,
              borderRadius: '50%',
              backgroundColor: '#FAB95B',
              opacity: 0.35,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 47,
              fontWeight: 700,
              color: '#E8EDF2',
              letterSpacing: 5,
              textTransform: 'uppercase',
            }}
          >
            COLA vs Real Inflation
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 30,
              fontWeight: 500,
              color: '#9E9E9E',
              letterSpacing: 3,
              marginTop: 6,
            }}
          >
            Your check stays flat. Your bills don't.
          </div>
        </div>

        {/* Chart */}
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {/* Axes */}
          <line x1={chartLeft} y1={chartTop} x2={chartLeft} y2={chartTop + chartH} stroke="#E8EDF2" strokeWidth="3" />
          <line x1={chartLeft} y1={chartTop + chartH} x2={chartLeft + chartW} y2={chartTop + chartH} stroke="#E8EDF2" strokeWidth="3" />

          {/* Axis labels */}
          <text x={chartLeft - 20} y={chartTop + chartH + 40} fontFamily="JetBrains Mono" fontSize="22" fontWeight="600" fill="#9E9E9E">
            2020
          </text>
          <text x={chartLeft + chartW - 50} y={chartTop + chartH + 40} fontFamily="JetBrains Mono" fontSize="22" fontWeight="600" fill="#9E9E9E">
            2026
          </text>

          {/* COLA line - flat-ish */}
          <g transform={`translate(${chartLeft}, ${chartTop})`}>
            <path d={colaPath} stroke="#9E9E9E" strokeWidth="6" fill="none" strokeLinecap="round" />
            {chartProgress > 0.05 && (
              <circle cx={chartW * chartProgress} cy={chartH - 70 * chartProgress} r="10" fill="#9E9E9E" />
            )}
          </g>

          {/* Inflation line - steep */}
          <g transform={`translate(${chartLeft}, ${chartTop})`}>
            <path d={inflPath} stroke="#FF4444" strokeWidth="6" fill="none" strokeLinecap="round" />
            {chartProgress > 0.05 && (
              <circle cx={chartW * chartProgress} cy={chartH - 320 * chartProgress} r="10" fill="#FF4444" />
            )}
          </g>
        </svg>

        {/* Legend */}
        <div
          style={{
            position: 'absolute',
            top: 230,
            right: 200,
            display: 'flex',
            flexDirection: 'column',
            gap: 14,
          }}
        >
          <div style={{ display: 'flex', alignItems: 'center', gap: 14 }}>
            <div style={{ width: 36, height: 6, backgroundColor: '#9E9E9E', borderRadius: 3 }} />
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 30, fontWeight: 700, color: '#E8EDF2', letterSpacing: 2 }}>
              COLA RAISE
            </div>
          </div>
          <div style={{ display: 'flex', alignItems: 'center', gap: 14 }}>
            <div style={{ width: 36, height: 6, backgroundColor: '#FF4444', borderRadius: 3 }} />
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 30, fontWeight: 700, color: '#FF4444', letterSpacing: 2 }}>
              REAL INFLATION
            </div>
          </div>
        </div>

        {/* Gap label */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: chartLeft + chartW + 40,
            opacity: gapOp,
            transform: `scale(${interpolate(sp(220), [0, 1], [0.5, 1])})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 900,
              color: '#FAB95B',
              letterSpacing: 3,
              textTransform: 'uppercase',
              lineHeight: 1.0,
            }}
          >
            THE GAP
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 22,
              fontWeight: 600,
              color: '#E8EDF2',
              letterSpacing: 1,
              marginTop: 6,
              maxWidth: 230,
            }}
          >
            Is what's eating your buying power
          </div>
        </div>

        {/* Bottom callout receipts */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: 120,
            display: 'flex',
            gap: 22,
            opacity: interpolate(sp(260), [0, 1], [0, 1]),
          }}
        >
          {['GROCERIES +18%', 'ELECTRIC +12%', 'PHARMACY +22%'].map((t, i) => (
            <div
              key={t}
              style={{
                border: '2px solid #FF4444',
                backgroundColor: 'rgba(255,68,68,0.08)',
                padding: '12px 22px',
                borderRadius: 6,
                fontFamily: 'JetBrains Mono',
                fontSize: 28,
                fontWeight: 800,
                color: '#FF4444',
                letterSpacing: 2,
              }}
            >
              {t}
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_COLAvsInflation;
```

```tsx
import React from 'react';
Audio: "You become cash poor even owning your home. Equity in the walls, no cash in the wallet. The government won't adjust automatically — you must use the safety nets. Let's get into the first one."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_CashPoorEquityWalls',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_CashPoorEquityWalls: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // House (equity) on left
  const houseSp = sp(20, { stiffness: 80 });

  // Wallet (empty) on right
  const walletSp = sp(80, { stiffness: 80 });

  // VS in middle
  const vsSp = sp(140, { stiffness: 110 });
  const vsPulse = 1 + Math.sin(frame * 0.15) * 0.05;

  // Safety net bottom
  const netSp = sp(220);

  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 50%, rgba(250,185,91,0.1) 0%, rgba(65,45,21,0) 70%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          left: '5%',
          top: '70%',
          width: 360,
          height: 360,
          borderRadius: '50%',
          border: '3px solid #FAB95B',
          opacity: 0.06,
          transform: `rotate(${frame * 0.15}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          right: '5%',
          top: '5%',
          width: 320,
          height: 320,
          backgroundColor: '#E8EDF2',
          opacity: 0.03,
          transform: `rotate(${45 + frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />

      {/* Texture - small squares */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 20 }).map((_, i) =>
          Array.from({ length: 12 }).map((_, j) => (
            <rect
              key={`sq${i}${j}`}
              x={i * 100 + 50}
              y={j * 90 + 45}
              width={3}
              height={3}
              fill="#FAB95B"
              opacity={0.6 + Math.sin(frame * 0.05 + i + j) * 0.3}
            />
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i + 800;
        const x = seededRandom(seed) * 1920;
        const y = (seededRandom(seed * 2) * 1080 + frame * 0.4) % 1080;
        return (
          <div
            key={`pj${i}`}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 3,
              height: 3,
              borderRadius: '50%',
              backgroundColor: '#FAB95B',
              opacity: 0.35,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            fontFamily: 'Inter, Poppins',
            fontSize: 60,
            fontWeight: 900,
            color: '#FAB95B',
            letterSpacing: 5,
            textTransform: 'uppercase',
            textShadow: '0 0 30px rgba(250,185,91,0.4)',
          }}
        >
          You're Cash Poor
        </div>

        {/* LEFT — House full of equity */}
        <div
          style={{
            position: 'absolute',
            top: 230,
            left: 160,
            width: 600,
            textAlign: 'center',
            transform: `scale(${houseSp})`,
            opacity: houseSp,
          }}
        >
          <svg width="380" height="320" viewBox="0 0 380 320" style={{ margin: '0 auto', display: 'block' }}>
            <path d="M30 160 L190 40 L350 160 Z" fill="#FAB95B" opacity="0.9" />
            <rect x="60" y="160" width="260" height="140" fill="#FAB95B" opacity="0.9" />
            <rect x="160" y="200" width="60" height="100" fill="#412D15" />
            <rect x="90" y="190" width="50" height="50" fill="#412D15" />
            <rect x="240" y="190" width="50" height="50" fill="#412D15" />
            <text x="190" y="265" fontFamily="JetBrains Mono" fontSize="22" fontWeight="900" fill="#FAB95B" textAnchor="middle">
              $$$
            </text>
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 47,
              fontWeight: 900,
              color: '#E8EDF2',
              letterSpacing: 2,
              textTransform: 'uppercase',
              marginTop: 18,
            }}
          >
            Equity In Walls
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 2,
              marginTop: 6,
            }}
          >
            Home is full of value
          </div>
        </div>

        {/* CENTER — BUT */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: '50%',
            transform: `translateX(-50%) scale(${vsSp * vsPulse})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 102,
              fontWeight: 900,
              color: '#FF4444',
              letterSpacing: -2,
              textShadow: '0 0 40px rgba(255,68,68,0.5)',
            }}
          >
            BUT
          </div>
        </div>

        {/* RIGHT — Empty wallet */}
        <div
          style={{
            position: 'absolute',
            top: 230,
            right: 160,
            width: 600,
            textAlign: 'center',
            transform: `scale(${walletSp})`,
            opacity: walletSp,
          }}
        >
          <svg width="380" height="320" viewBox="0 0 380 320" style={{ margin: '0 auto', display: 'block' }}>
            <rect x="40" y="100" width="300" height="180" rx="14" fill="none" stroke="#E8EDF2" strokeWidth="6" />
            <rect x="40" y="100" width="300" height="50" fill="#E8EDF2" opacity="0.15" />
            <rect x="240" y="170" width="80" height="40" rx="4" fill="none" stroke="#E8EDF2" strokeWidth="4" />
            <circle cx="280" cy="190" r="10" fill="#E8EDF2" />
            <line x1="80" y1="240" x2="200" y2="240" stroke="#FF4444" strokeWidth="6" strokeLinecap="round" />
            <text x="190" y="80" fontFamily="JetBrains Mono" fontSize="32" fontWeight="900" fill="#FF4444" textAnchor="middle">
              EMPTY
            </text>
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 47,
              fontWeight: 900,
              color: '#E8EDF2',
              letterSpacing: 2,
              textTransform: 'uppercase',
              marginTop: 18,
            }}
          >
            No Cash In Wallet
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#FF4444',
              letterSpacing: 2,
              marginTop: 6,
            }}
          >
            Can't buy food or fuel
          </div>
        </div>

        {/* Bottom: Safety net banner */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(netSp, [0, 1], [40, 0])}px)`,
            opacity: netSp,
            backgroundColor: '#FAB95B',
            padding: '18px 50px',
            borderRadius: 6,
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 900,
            color: '#412D15',
            letterSpacing: 2,
            textTransform: 'uppercase',
          }}
        >
          You Must Use The Safety Nets →
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.4,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_CashPoorEquityWalls;
```

