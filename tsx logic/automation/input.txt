```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_PictureInMind',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_PictureInMind: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom in
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08], { extrapolateRight: 'clamp' });

  // Word reveals
  const word1 = sp(10);
  const word2 = sp(30);
  const word3 = sp(55);

  // Eye blink animation
  const eyeOpen = Math.abs(Math.sin(frame * 0.04)) > 0.15 ? 1 : 0.1;
  const eyeScale = sp(70);

  // Thought bubble pulse
  const bubblePulse = 1 + Math.sin(frame * 0.08) * 0.03;

  // Frame entrance
  const frameReveal = sp(90, { damping: 14 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#212121', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 40%, rgba(20,255,236,0.08) 0%, transparent 60%), radial-gradient(ellipse at 70% 70%, rgba(244,206,20,0.06) 0%, transparent 55%)',
      }} />

      {/* Animated shape layers - transparent drifting geometric forms */}
      <div style={{
        position: 'absolute',
        top: '15%',
        left: '8%',
        width: 420,
        height: 420,
        borderRadius: '50%',
        border: '2px solid #14FFEC',
        opacity: 0.05,
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '10%',
        right: '5%',
        width: 520,
        height: 520,
        border: '2px solid #F4CE14',
        opacity: 0.06,
        transform: `rotate(${-frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        width: 280,
        height: 280,
        background: '#14FFEC',
        opacity: 0.03,
        transform: `translate(-50%, -50%) rotate(${frame * 0.3}deg)`,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
      }} />

      {/* Grid motion texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#14FFEC" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid1)" transform={`translate(${frame * 0.3 % 80}, 0)`} />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const r = seededRandom(i + 1);
        const r2 = seededRandom(i + 50);
        const x = r * 1920;
        const y = (r2 * 1080 + frame * (0.4 + r * 0.6)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 4 + r * 4,
            height: 4 + r * 4,
            borderRadius: '50%',
            background: i % 3 === 0 ? '#F4CE14' : '#14FFEC',
            opacity: 0.3 + r * 0.4,
            boxShadow: `0 0 ${8 + r * 8}px currentColor`,
          }} />
        );
      })}

      {/* MAIN CONTENT - Camera wrapped */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>

        {/* Large typographic headline */}
        <div style={{
          position: 'absolute',
          top: '18%',
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 30,
          fontFamily: 'Yantra One',
          fontSize: 180,
          fontWeight: 900,
          lineHeight: 1,
        }}>
          <div style={{
            color: '#F5F5F5',
            transform: `translateY(${(1 - word1) * 80}px)`,
            opacity: word1,
          }}>देखिए</div>
          <div style={{
            color: '#14FFEC',
            transform: `translateY(${(1 - word2) * 80}px)`,
            opacity: word2,
            textShadow: '0 0 40px rgba(20,255,236,0.6)',
          }}>एक</div>
          <div style={{
            color: '#F4CE14',
            transform: `translateY(${(1 - word3) * 80}px)`,
            opacity: word3,
            textShadow: '0 0 40px rgba(244,206,20,0.6)',
          }}>तस्वीर</div>
        </div>

        {/* Subline */}
        <div style={{
          position: 'absolute',
          top: '38%',
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(75)) * 40}px)`,
          opacity: sp(75),
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 76,
          fontWeight: 700,
          color: '#F5F5F5',
          letterSpacing: 2,
        }}>
          अपने दिमाग में बनाईए
        </div>

        {/* Central VISUAL: Brain SVG with thought bubble + eye */}
        <div style={{
          position: 'absolute',
          top: '55%',
          left: '50%',
          transform: `translate(-50%, 0) scale(${frameReveal})`,
          width: 800,
          height: 400,
        }}>
          {/* Picture frame */}
          <svg width="800" height="400" viewBox="0 0 800 400">
            {/* Outer frame */}
            <rect x="50" y="20" width="700" height="360" rx="12"
              fill="none" stroke="#F4CE14" strokeWidth="6" opacity={frameReveal} />
            <rect x="65" y="35" width="670" height="330" rx="6"
              fill="#1C1C1C" stroke="#14FFEC" strokeWidth="2" opacity={frameReveal * 0.8} />

            {/* Eye inside frame */}
            <g transform={`translate(400, 200) scale(${eyeScale * bubblePulse})`}>
              {/* Eye outline */}
              <ellipse cx="0" cy="0" rx="160" ry="80"
                fill="none" stroke="#14FFEC" strokeWidth="5" />
              {/* Iris */}
              <circle cx="0" cy="0" r={70 * eyeOpen}
                fill="#F4CE14" opacity="0.9" />
              {/* Pupil */}
              <circle cx="0" cy="0" r={32 * eyeOpen}
                fill="#1C1C1C" />
              {/* Highlight */}
              <circle cx="-12" cy="-12" r={10 * eyeOpen}
                fill="#F5F5F5" />
              {/* Lashes top */}
              {[-120, -80, -40, 0, 40, 80, 120].map((x, i) => (
                <line key={i} x1={x} y1="-75" x2={x * 1.1} y2="-100"
                  stroke="#14FFEC" strokeWidth="3" strokeLinecap="round" />
              ))}
            </g>

            {/* Frame label */}
            <text x="400" y="395" textAnchor="middle"
              fontFamily="Inter" fontSize="20" fontWeight="600"
              fill="#9E9E9E" letterSpacing="4">IMAGINE</text>
          </svg>

          {/* Thought bubble dots floating up */}
          {[0, 1, 2].map((i) => {
            const opacity = sp(100 + i * 15);
            return (
              <div key={i} style={{
                position: 'absolute',
                top: -30 - i * 35,
                left: 600 + i * 40,
                width: 20 + i * 12,
                height: 20 + i * 12,
                borderRadius: '50%',
                background: '#14FFEC',
                opacity: opacity * 0.7,
                boxShadow: '0 0 20px #14FFEC',
                transform: `scale(${opacity})`,
              }} />
            );
          })}
        </div>

        {/* Bottom hint bar */}
        <div style={{
          position: 'absolute',
          bottom: '8%',
          left: '50%',
          transform: `translateX(-50%) scaleX(${sp(140)})`,
          width: 600,
          height: 4,
          background: 'linear-gradient(90deg, transparent, #F4CE14, transparent)',
          opacity: 0.8,
        }} />
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene01_PictureInMind;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_DadajiGeneration',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_DadajiGeneration: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan right
  const camX = interpolate(frame, [0, durationInFrames], [0, -40], { extrapolateRight: 'clamp' });

  const titleIn = sp(10);
  const yearIn = sp(35);
  const houseIn = sp(60);
  const arrowDraw = interpolate(sp(110), [0, 1], [0, 1]);
  const bankIn = sp(150);

  return (
    <AbsoluteFill style={{ backgroundColor: '#212121', overflow: 'hidden', opacity: exitOp }}>
      {/* Vintage sepia gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(244,206,20,0.05) 0%, transparent 50%, rgba(20,255,236,0.04) 100%)',
      }} />

      {/* Shape layers - vintage circles */}
      <div style={{
        position: 'absolute',
        top: '10%',
        right: '10%',
        width: 600,
        height: 600,
        borderRadius: '50%',
        border: '3px dashed #F4CE14',
        opacity: 0.06,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '-10%',
        left: '-5%',
        width: 700,
        height: 700,
        borderRadius: '50%',
        background: '#14FFEC',
        opacity: 0.03,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.04})`,
      }} />

      {/* Diagonal line texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="lines2" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#F4CE14" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#lines2)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 10);
        const r2 = seededRandom(i + 200);
        const x = (r * 1920 + frame * 0.3) % 1920;
        const y = r2 * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y + Math.sin(frame * 0.02 + i) * 20,
            width: 3 + r * 5,
            height: 3 + r * 5,
            borderRadius: '50%',
            background: '#F4CE14',
            opacity: 0.3 + r * 0.3,
            boxShadow: `0 0 10px #F4CE14`,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* Era label - top */}
        <div style={{
          position: 'absolute',
          top: '8%',
          left: '50%',
          transform: `translateX(-50%) scale(${yearIn})`,
          opacity: yearIn,
          background: '#F4CE14',
          color: '#1C1C1C',
          padding: '12px 40px',
          fontFamily: 'JetBrains Mono',
          fontSize: 32,
          fontWeight: 700,
          letterSpacing: 4,
          borderRadius: 4,
        }}>
          ERA: 1970 - 2000
        </div>

        {/* Main headline */}
        <div style={{
          position: 'absolute',
          top: '18%',
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - titleIn) * 60}px)`,
          opacity: titleIn,
          fontFamily: 'Yantra One',
          fontSize: 140,
          fontWeight: 900,
          color: '#F5F5F5',
          textAlign: 'center',
          lineHeight: 1,
        }}>
          दादाजी की <span style={{ color: '#F4CE14', textShadow: '0 0 30px rgba(244,206,20,0.6)' }}>पीढ़ी</span>
        </div>

        {/* Sub */}
        <div style={{
          position: 'absolute',
          top: '32%',
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: sp(25),
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 56,
          fontWeight: 600,
          color: '#14FFEC',
        }}>
          पैसा बचाती थी → बैंक जाती थी
        </div>

        {/* Visual journey: House → Arrow → Bank */}
        <div style={{
          position: 'absolute',
          bottom: '12%',
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          alignItems: 'center',
          gap: 60,
        }}>
          {/* HOUSE SVG */}
          <div style={{ transform: `scale(${houseIn})`, opacity: houseIn }}>
            <svg width="280" height="280" viewBox="0 0 280 280">
              {/* Roof */}
              <polygon points="140,40 40,140 240,140" fill="#F4CE14" stroke="#F5F5F5" strokeWidth="4" />
              {/* Body */}
              <rect x="60" y="140" width="160" height="120" fill="#2D2D2D" stroke="#14FFEC" strokeWidth="4" />
              {/* Door */}
              <rect x="120" y="180" width="40" height="80" fill="#14FFEC" />
              {/* Windows */}
              <rect x="75" y="160" width="30" height="30" fill="#F4CE14" opacity="0.7" />
              <rect x="175" y="160" width="30" height="30" fill="#F4CE14" opacity="0.7" />
              {/* Chimney smoke */}
              <circle cx="190" cy={60 + Math.sin(frame * 0.1) * 5} r="8" fill="#9E9E9E" opacity="0.6" />
              <circle cx="195" cy={45 + Math.sin(frame * 0.1 + 1) * 5} r="6" fill="#9E9E9E" opacity="0.4" />
              <text x="140" y="278" textAnchor="middle" fontFamily="Inter" fontSize="18" fontWeight="700" fill="#9E9E9E">HOME</text>
            </svg>
          </div>

          {/* Arrow with money trail */}
          <div style={{ position: 'relative', width: 260, height: 80 }}>
            <svg width="260" height="80" viewBox="0 0 260 80">
              <defs>
                <marker id="arr2" markerWidth="14" markerHeight="14" refX="6" refY="7" orient="auto">
                  <polygon points="0 0, 14 7, 0 14" fill="#14FFEC" />
                </marker>
              </defs>
              <line x1="0" y1="40" x2="240" y2="40"
                stroke="#14FFEC" strokeWidth="5" strokeDasharray="240"
                strokeDashoffset={240 - 240 * arrowDraw}
                markerEnd="url(#arr2)" />
            </svg>
            {/* Rupee coin moving */}
            <div style={{
              position: 'absolute',
              left: arrowDraw * 220 - 25,
              top: 10,
              width: 60,
              height: 60,
              borderRadius: '50%',
              background: '#F4CE14',
              border: '3px solid #F5F5F5',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Yantra One',
              fontSize: 38,
              color: '#1C1C1C',
              fontWeight: 900,
              opacity: arrowDraw > 0.05 ? 1 : 0,
              boxShadow: '0 0 25px #F4CE14',
            }}>₹</div>
          </div>

          {/* BANK SVG */}
          <div style={{ transform: `scale(${bankIn})`, opacity: bankIn }}>
            <svg width="320" height="280" viewBox="0 0 320 280">
              {/* Steps */}
              <rect x="20" y="240" width="280" height="20" fill="#9E9E9E" />
              {/* Pillars */}
              <rect x="50" y="100" width="30" height="140" fill="#F5F5F5" />
              <rect x="110" y="100" width="30" height="140" fill="#F5F5F5" />
              <rect x="180" y="100" width="30" height="140" fill="#F5F5F5" />
              <rect x="240" y="100" width="30" height="140" fill="#F5F5F5" />
              {/* Top entablature */}
              <rect x="30" y="80" width="260" height="25" fill="#F4CE14" />
              {/* Pediment */}
              <polygon points="160,20 30,80 290,80" fill="#14FFEC" stroke="#F5F5F5" strokeWidth="3" />
              {/* BANK text */}
              <text x="160" y="68" textAnchor="middle" fontFamily="Inter" fontSize="24" fontWeight="900" fill="#1C1C1C">BANK</text>
              {/* Door */}
              <rect x="140" y="160" width="40" height="80" fill="#1C1C1C" />
              <text x="160" y="278" textAnchor="middle" fontFamily="Inter" fontSize="18" fontWeight="700" fill="#9E9E9E">SAFE • TRUSTED</text>
            </svg>
          </div>
        </div>

        {/* Caption at bottom */}
        <div style={{
          position: 'absolute',
          bottom: '4%',
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: sp(180),
          fontFamily: 'JetBrains Mono',
          fontSize: 28,
          color: '#9E9E9E',
          letterSpacing: 3,
        }}>
          // सुरक्षित • भरोसेमंद • पारंपरिक
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene02_DadajiGeneration;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_FDPassbookSleep',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_FDPassbookSleep: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Card entries staggered
  const card1 = sp(15);
  const card2 = sp(60);
  const card3 = sp(105);

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-0.5, 0.5], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#212121', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 30%, rgba(20,255,236,0.07) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: '20%',
        left: '5%',
        width: 380,
        height: 380,
        background: '#F4CE14',
        opacity: 0.04,
        transform: `rotate(${frame * 0.4}deg)`,
        clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
      }} />
      <div style={{
        position: 'absolute',
        bottom: '10%',
        right: '8%',
        width: 480,
        height: 480,
        borderRadius: '50%',
        border: '2px solid #14FFEC',
        opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
      }} />

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="dots3" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#14FFEC" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots3)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 30);
        const r2 = seededRandom(i + 300);
        const x = r * 1920;
        const y = (r2 * 1080 - frame * (0.3 + r * 0.5) + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 3 + r * 4,
            height: 3 + r * 4,
            borderRadius: '50%',
            background: i % 2 === 0 ? '#14FFEC' : '#F4CE14',
            opacity: 0.4,
            boxShadow: '0 0 12px currentColor',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)` }}>
        {/* Top headline */}
        <div style={{
          position: 'absolute',
          top: '8%',
          left: '50%',
          transform: `translateX(-50%)`,
          opacity: sp(5),
          fontFamily: 'Yantra One',
          fontSize: 110,
          fontWeight: 900,
          color: '#F5F5F5',
          letterSpacing: 1,
        }}>
          तीन <span style={{ color: '#F4CE14' }}>आदतें</span>
        </div>

        {/* Three cards row */}
        <div style={{
          position: 'absolute',
          top: '28%',
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 40,
        }}>
          {/* CARD 1 - FD */}
          <div style={{
            width: 460,
            height: 560,
            background: '#2D2D2D',
            border: '4px solid #F4CE14',
            borderRadius: 16,
            transform: `translateY(${(1 - card1) * 120}px) rotate(${(1 - card1) * -8}deg)`,
            opacity: card1,
            padding: 40,
            boxShadow: '0 20px 60px rgba(244,206,20,0.2)',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#9E9E9E',
              letterSpacing: 3,
              marginBottom: 10,
            }}>01</div>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 92,
              fontWeight: 900,
              color: '#F4CE14',
              lineHeight: 1,
              marginBottom: 20,
            }}>FD</div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 44,
              fontWeight: 700,
              color: '#F5F5F5',
              marginBottom: 30,
            }}>करवाती थी</div>
            {/* FD certificate icon */}
            <svg width="320" height="240" viewBox="0 0 320 240">
              <rect x="20" y="20" width="280" height="200" rx="8" fill="#1C1C1C" stroke="#F4CE14" strokeWidth="3" />
              <rect x="40" y="40" width="100" height="14" fill="#F4CE14" />
              <rect x="40" y="64" width="180" height="8" fill="#9E9E9E" opacity="0.5" />
              <rect x="40" y="80" width="140" height="8" fill="#9E9E9E" opacity="0.5" />
              <text x="40" y="140" fontFamily="Yantra One" fontSize="36" fill="#14FFEC" fontWeight="900">₹1,00,000</text>
              <text x="40" y="180" fontFamily="JetBrains Mono" fontSize="18" fill="#9E9E9E">5 YR @ 8.5%</text>
              <circle cx="260" cy="180" r="22" fill="none" stroke="#F4CE14" strokeWidth="2" />
              <text x="260" y="186" textAnchor="middle" fontFamily="Inter" fontSize="12" fontWeight="700" fill="#F4CE14">SEAL</text>
            </svg>
          </div>

          {/* CARD 2 - Passbook */}
          <div style={{
            width: 460,
            height: 560,
            background: '#2D2D2D',
            border: '4px solid #14FFEC',
            borderRadius: 16,
            transform: `translateY(${(1 - card2) * 120}px) rotate(${(1 - card2) * 6}deg)`,
            opacity: card2,
            padding: 40,
            boxShadow: '0 20px 60px rgba(20,255,236,0.2)',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#9E9E9E',
              letterSpacing: 3,
              marginBottom: 10,
            }}>02</div>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 76,
              fontWeight: 900,
              color: '#14FFEC',
              lineHeight: 1,
              marginBottom: 20,
            }}>पासबुक</div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 44,
              fontWeight: 700,
              color: '#F5F5F5',
              marginBottom: 30,
            }}>संभाल कर</div>
            {/* Passbook icon */}
            <svg width="320" height="240" viewBox="0 0 320 240">
              <rect x="30" y="30" width="260" height="180" rx="6" fill="#14FFEC" />
              <rect x="40" y="40" width="240" height="160" rx="4" fill="#1C1C1C" />
              <text x="160" y="80" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#F4CE14">PASSBOOK</text>
              <line x1="50" y1="100" x2="270" y2="100" stroke="#9E9E9E" strokeWidth="1" />
              <line x1="50" y1="125" x2="270" y2="125" stroke="#9E9E9E" strokeWidth="1" />
              <line x1="50" y1="150" x2="270" y2="150" stroke="#9E9E9E" strokeWidth="1" />
              <line x1="50" y1="175" x2="270" y2="175" stroke="#9E9E9E" strokeWidth="1" />
              <text x="60" y="120" fontFamily="JetBrains Mono" fontSize="14" fill="#14FFEC">01-JAN  CR  5000</text>
              <text x="60" y="145" fontFamily="JetBrains Mono" fontSize="14" fill="#14FFEC">15-FEB  CR  3000</text>
              <text x="60" y="170" fontFamily="JetBrains Mono" fontSize="14" fill="#14FFEC">01-MAR  CR  4500</text>
            </svg>
          </div>

          {/* CARD 3 - Sleep */}
          <div style={{
            width: 460,
            height: 560,
            background: '#2D2D2D',
            border: '4px solid #F5F5F5',
            borderRadius: 16,
            transform: `translateY(${(1 - card3) * 120}px) rotate(${(1 - card3) * -6}deg)`,
            opacity: card3,
            padding: 40,
            boxShadow: '0 20px 60px rgba(255,255,255,0.1)',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#9E9E9E',
              letterSpacing: 3,
              marginBottom: 10,
            }}>03</div>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 76,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1,
              marginBottom: 20,
            }}>चैन की नींद</div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 44,
              fontWeight: 700,
              color: '#14FFEC',
              marginBottom: 30,
            }}>सोती थी</div>
            {/* Moon + Z's */}
            <svg width="320" height="240" viewBox="0 0 320 240">
              {/* Moon */}
              <circle cx="100" cy="120" r="60" fill="#F4CE14" />
              <circle cx="125" cy="110" r="50" fill="#1C1C1C" />
              {/* Stars */}
              <polygon points="220,40 224,52 236,52 226,60 230,72 220,64 210,72 214,60 204,52 216,52" fill="#14FFEC" opacity={0.6 + Math.sin(frame * 0.1) * 0.4} />
              <polygon points="270,90 273,98 281,98 274,104 277,112 270,106 263,112 266,104 259,98 267,98" fill="#F5F5F5" opacity={0.6 + Math.sin(frame * 0.12 + 1) * 0.4} />
              {/* Z letters */}
              <text x="200" y="170" fontFamily="Inter" fontSize="64" fontWeight="900" fill="#14FFEC" opacity={0.5 + Math.sin(frame * 0.08) * 0.3}>Z</text>
              <text x="245" y="200" fontFamily="Inter" fontSize="48" fontWeight="900" fill="#F4CE14" opacity={0.5 + Math.sin(frame * 0.08 + 1) * 0.3}>z</text>
              <text x="278" y="220" fontFamily="Inter" fontSize="36" fontWeight="900" fill="#F5F5F5" opacity={0.5 + Math.sin(frame * 0.08 + 2) * 0.3}>z</text>
            </svg>
          </div>
        </div>

        {/* Bottom caption */}
        <div style={{
          position: 'absolute',
          bottom: '5%',
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: sp(140),
          fontFamily: 'JetBrains Mono',
          fontSize: 28,
          color: '#9E9E9E',
          letterSpacing: 4,
        }}>
          // एक सुरक्षित दिनचर्या
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene03_FDPassbookSleep;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_YoungManApp',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_YoungManApp: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headIn = sp(10);
  const phoneIn = sp(30);
  const screenIn = sp(70);
  const tapPulse = Math.sin(frame * 0.3) > 0 ? 1 : 0;

  // Camera zoom on phone
  const camScale = interpolate(frame, [0, 120, durationInFrames], [1, 1.15, 1.18], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#212121', overflow: 'hidden', opacity: exitOp }}>
      {/* Modern tech gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 30%, rgba(20,255,236,0.1) 0%, transparent 50%), radial-gradient(circle at 20% 80%, rgba(244,206,20,0.06) 0%, transparent 50%)',
      }} />

      {/* Shape layers - modern hexagons */}
      <div style={{
        position: 'absolute',
        top: '15%',
        right: '12%',
        width: 380,
        height: 380,
        background: '#14FFEC',
        opacity: 0.05,
        clipPath: 'polygon(25% 5%, 75% 5%, 100% 50%, 75% 95%, 25% 95%, 0% 50%)',
        transform: `rotate(${frame * 0.3}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '15%',
        left: '8%',
        width: 460,
        height: 460,
        border: '3px solid #F4CE14',
        opacity: 0.06,
        transform: `rotate(${-frame * 0.2}deg)`,
        borderRadius: 30,
      }} />

      {/* Tech grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="techgrid4" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M 100 0 L 0 0 0 100" fill="none" stroke="#14FFEC" strokeWidth="1" />
            <circle cx="0" cy="0" r="2" fill="#14FFEC" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#techgrid4)" />
      </svg>

      {/* Particles - rising data dots */}
      {Array.from({ length: 30 }).map((_, i) => {
        const r = seededRandom(i + 40);
        const r2 = seededRandom(i + 400);
        const x = r * 1920;
        const y = (r2 * 1080 - frame * (0.5 + r * 0.8) + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 3 + r * 4,
            height: 3 + r * 4,
            background: i % 4 === 0 ? '#F4CE14' : '#14FFEC',
            opacity: 0.4 + r * 0.4,
            boxShadow: '0 0 14px currentColor',
            borderRadius: i % 3 === 0 ? '50%' : '2px',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        {/* Left side - text */}
        <div style={{
          position: 'absolute',
          top: '20%',
          left: '6%',
          width: '45%',
        }}>
          <div style={{
            opacity: sp(5),
            fontFamily: 'JetBrains Mono',
            fontSize: 28,
            color: '#F4CE14',
            letterSpacing: 4,
            marginBottom: 20,
          }}>NEW ERA / 2025</div>

          <div style={{
            transform: `translateY(${(1 - headIn) * 60}px)`,
            opacity: headIn,
            fontFamily: 'Yantra One',
            fontSize: 150,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 0.95,
            marginBottom: 30,
          }}>
            आज का<br/>
            <span style={{ color: '#14FFEC', textShadow: '0 0 40px rgba(20,255,236,0.6)' }}>नौजवान</span>
          </div>

          <div style={{
            opacity: sp(45),
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 52,
            fontWeight: 600,
            color: '#F4CE14',
            lineHeight: 1.2,
          }}>
            मोबाइल पर ऐप खोलता है
          </div>

          {/* Time stamp */}
          <div style={{
            marginTop: 40,
            opacity: sp(80),
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}>
            <div style={{
              width: 16, height: 16,
              borderRadius: '50%',
              background: '#14FFEC',
              boxShadow: '0 0 15px #14FFEC',
              opacity: 0.5 + Math.sin(frame * 0.2) * 0.5,
            }} />
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 32,
              color: '#F5F5F5',
              fontWeight: 700,
            }}>2 MIN. SETUP</div>
          </div>
        </div>

        {/* Right side - Phone mockup */}
        <div style={{
          position: 'absolute',
          top: '12%',
          right: '8%',
          transform: `translateY(${(1 - phoneIn) * 200}px) scale(${phoneIn})`,
          opacity: phoneIn,
        }}>
          <svg width="400" height="800" viewBox="0 0 400 800">
            {/* Phone body */}
            <rect x="10" y="10" width="380" height="780" rx="50" fill="#1C1C1C" stroke="#F4CE14" strokeWidth="6" />
            {/* Screen */}
            <rect x="30" y="50" width="340" height="700" rx="20" fill="#2D2D2D" />
            {/* Notch */}
            <rect x="150" y="30" width="100" height="20" rx="10" fill="#1C1C1C" />

            {/* App screen content - reveals later */}
            <g opacity={screenIn}>
              {/* Status bar */}
              <text x="50" y="80" fontFamily="JetBrains Mono" fontSize="14" fill="#14FFEC">9:41 AM</text>
              <text x="350" y="80" textAnchor="end" fontFamily="JetBrains Mono" fontSize="14" fill="#14FFEC">100%</text>

              {/* App header */}
              <text x="200" y="130" textAnchor="middle" fontFamily="Inter" fontSize="28" fontWeight="900" fill="#F4CE14">TRADE APP</text>

              {/* Big number */}
              <text x="200" y="230" textAnchor="middle" fontFamily="Yantra One" fontSize="56" fontWeight="900" fill="#14FFEC">₹50,000</text>
              <text x="200" y="265" textAnchor="middle" fontFamily="Inter" fontSize="18" fill="#9E9E9E">Available Balance</text>

              {/* Stock chart line */}
              <polyline
                points="50,400 90,380 130,420 170,350 210,370 250,310 290,330 330,280 370,300"
                fill="none"
                stroke="#14FFEC"
                strokeWidth="4"
              />
              <polyline
                points="50,400 90,380 130,420 170,350 210,370 250,310 290,330 330,280 370,300"
                fill="#14FFEC"
                opacity="0.15"
              />

              {/* Chart axis */}
              <line x1="50" y1="450" x2="370" y2="450" stroke="#9E9E9E" strokeWidth="1" opacity="0.5" />

              {/* Buy button - pulsing */}
              <rect x="60" y="560" width="280" height="80" rx="40"
                fill="#14FFEC"
                opacity={0.7 + tapPulse * 0.3}
              />
              <text x="200" y="610" textAnchor="middle" fontFamily="Inter" fontSize="32" fontWeight="900" fill="#1C1C1C">BUY NOW</text>

              {/* Quick stats */}
              <text x="60" y="690" fontFamily="JetBrains Mono" fontSize="14" fill="#9E9E9E">NIFTY +1.2%</text>
              <text x="200" y="690" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="14" fill="#F4CE14">SENSEX +0.8%</text>
              <text x="340" y="690" textAnchor="end" fontFamily="JetBrains Mono" fontSize="14" fill="#14FFEC">BANK +2.1%</text>
            </g>

            {/* Tap indicator on button */}
            <circle
              cx="200"
              cy="600"
              r={20 + (frame % 30) * 2}
              fill="none"
              stroke="#F4CE14"
              strokeWidth="3"
              opacity={screenIn * (1 - (frame % 30) / 30)}
            />
          </svg>

          {/* Finger pointing */}
          <div style={{
            position: 'absolute',
            top: 540,
            right: -40,
            opacity: sp(110),
            transform: `translateY(${Math.sin(frame * 0.2) * 8}px)`,
          }}>
            <svg width="100" height="120" viewBox="0 0 100 120">
              <path d="M 50 10 L 50 70 L 30 70 L 30 110 L 70 110 L 70 90 L 90 90 L 90 60 L 60 60 L 60 10 Z"
                fill="#F4CE14" stroke="#F5F5F5" strokeWidth="3" />
            </svg>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene04_YoungManApp;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_DMatAccountSavings',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_DMatAccountSavings: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Stopwatch counter 0 → 120 seconds (2 min)
  const seconds = Math.min(120, Math.floor(interpolate(frame, [20, 100], [0, 120], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' })));
  const minutes = Math.floor(seconds / 60);
  const secs = seconds % 60;

  // Money pouring
  const pourProgress = interpolate(frame, [140, 280], [0, 1], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });

  // Camera slight zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#212121', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(244,206,20,0.06) 0%, transparent 50%, rgba(20,255,236,0.08) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: '5%',
        left: '40%',
        width: 600,
        height: 600,
        borderRadius: '50%',
        border: '4px solid #14FFEC',
        opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '-10%',
        right: '-5%',
        width: 700,
        height: 700,
        background: '#F4CE14',
        opacity: 0.04,
        clipPath: 'polygon(0% 100%, 100% 0%, 100% 100%)',
      }} />

      {/* Circuit texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="circ5" width="120" height="120" patternUnits="userSpaceOnUse">
            <circle cx="60" cy="60" r="3" fill="#F4CE14" />
            <line x1="60" y1="60" x2="120" y2="60" stroke="#F4CE14" strokeWidth="1" />
            <line x1="60" y1="60" x2="60" y2="120" stroke="#F4CE14" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#circ5)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const r = seededRandom(i + 50);
        const r2 = seededRandom(i + 500);
        const x = (r * 1920 + frame * 0.5) % 1920;
        const y = r2 * 1080 + Math.sin(frame * 0.03 + i) * 20;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 4,
            height: 4,
            borderRadius: '50%',
            background: '#14FFEC',
            opacity: 0.4,
            boxShadow: '0 0 12px #14FFEC',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        {/* Top heading */}
        <div style={{
          position: 'absolute',
          top: '6%',
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: sp(5),
          fontFamily: 'JetBrains Mono',
          fontSize: 30,
          color: '#F4CE14',
          letterSpacing: 6,
        }}>
          STEP 01 → STEP 02
        </div>

        {/* LEFT: D-MAT Account creation - stopwatch */}
        <div style={{
          position: 'absolute',
          top: '18%',
          left: '5%',
          width: '42%',
          transform: `translateX(${(1 - sp(10)) * -100}px)`,
          opacity: sp(10),
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 90,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1,
            marginBottom: 20,
          }}>
            <span style={{ color: '#14FFEC' }}>D-MAT</span><br/>
            अकाउंट
          </div>

          {/* Stopwatch visual */}
          <div style={{
            marginTop: 30,
            display: 'flex',
            alignItems: 'center',
            gap: 30,
          }}>
            <svg width="220" height="220" viewBox="0 0 220 220">
              {/* Stopwatch body */}
              <circle cx="110" cy="115" r="90" fill="#2D2D2D" stroke="#F4CE14" strokeWidth="6" />
              {/* Top button */}
              <rect x="100" y="10" width="20" height="20" fill="#F4CE14" />
              {/* Markers */}
              {Array.from({ length: 12 }).map((_, i) => {
                const angle = (i / 12) * Math.PI * 2 - Math.PI / 2;
                const x1 = 110 + Math.cos(angle) * 75;
                const y1 = 115 + Math.sin(angle) * 75;
                const x2 = 110 + Math.cos(angle) * 85;
                const y2 = 115 + Math.sin(angle) * 85;
                return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#14FFEC" strokeWidth="2" />;
              })}
              {/* Hand */}
              <line
                x1="110" y1="115"
                x2={110 + Math.cos((seconds / 60) * Math.PI * 2 - Math.PI / 2) * 70}
                y2={115 + Math.sin((seconds / 60) * Math.PI * 2 - Math.PI / 2) * 70}
                stroke="#F4CE14" strokeWidth="4" strokeLinecap="round"
              />
              <circle cx="110" cy="115" r="6" fill="#F4CE14" />
              {/* Center text */}
              <text x="110" y="170" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="14" fill="#9E9E9E">SECONDS</text>
            </svg>

            <div>
              <div style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 90,
                fontWeight: 900,
                color: '#14FFEC',
                lineHeight: 1,
                textShadow: '0 0 30px rgba(20,255,236,0.6)',
              }}>
                {String(minutes).padStart(2, '0')}:{String(secs).padStart(2, '0')}
              </div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 36,
                color: '#F4CE14',
                marginTop: 8,
                fontWeight: 700,
              }}>
                सिर्फ 2 मिनिट!
              </div>
            </div>
          </div>

          {/* Checklist */}
          <div style={{
            marginTop: 30,
            display: 'flex',
            flexDirection: 'column',
            gap: 12,
          }}>
            {['PAN ✓', 'AADHAAR ✓', 'SELFIE ✓'].map((item, i) => (
              <div key={i} style={{
                opacity: sp(40 + i * 20),
                transform: `translateX(${(1 - sp(40 + i * 20)) * -40}px)`,
                display: 'flex',
                alignItems: 'center',
                gap: 16,
                fontFamily: 'JetBrains Mono',
                fontSize: 28,
                color: '#F5F5F5',
                fontWeight: 700,
              }}>
                <div style={{
                  width: 32, height: 32,
                  background: '#14FFEC',
                  borderRadius: 4,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  color: '#1C1C1C',
                  fontSize: 22,
                  fontWeight: 900,
                }}>✓</div>
                {item}
              </div>
            ))}
          </div>
        </div>

        {/* RIGHT: Money pouring into stock */}
        <div style={{
          position: 'absolute',
          top: '18%',
          right: '5%',
          width: '42%',
          transform: `translateX(${(1 - sp(120)) * 100}px)`,
          opacity: sp(120),
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 90,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1,
            marginBottom: 30,
            textAlign: 'right',
          }}>
            पूरी <span style={{ color: '#F4CE14' }}>सेविंग</span><br/>
            एक स्टॉक में
          </div>

          {/* Wallet pouring into beaker */}
          <div style={{ position: 'relative', height: 380 }}>
            <svg width="500" height="380" viewBox="0 0 500 380" style={{ position: 'absolute', right: 0 }}>
              {/* Wallet at top left */}
              <g transform="translate(60, 30)">
                <rect x="0" y="0" width="120" height="80" rx="10" fill="#F4CE14" stroke="#F5F5F5" strokeWidth="3" />
                <rect x="0" y="20" width="120" height="40" fill="#1C1C1C" />
                <text x="60" y="50" textAnchor="middle" fontFamily="Inter" fontSize="20" fontWeight="900" fill="#F4CE14">SAVINGS</text>
                <text x="60" y="100" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="14" fill="#F5F5F5">₹5,00,000</text>
              </g>

              {/* Pouring stream */}
              <path
                d={`M 120 110 Q 200 ${110 + pourProgress * 50} 350 ${280 - pourProgress * 30}`}
                stroke="#F4CE14"
                strokeWidth={pourProgress > 0 ? 8 : 0}
                fill="none"
                opacity={pourProgress}
              />

              {/* Falling rupee coins */}
              {[0, 1, 2, 3, 4].map((i) => {
                const t = ((frame * 2 + i * 25) % 100) / 100;
                const cx = 120 + t * 230;
                const cy = 110 + t * 170;
                return (
                  <g key={i} opacity={pourProgress * (1 - t)}>
                    <circle cx={cx} cy={cy} r="14" fill="#F4CE14" stroke="#F5F5F5" strokeWidth="2" />
                    <text x={cx} y={cy + 6} textAnchor="middle" fontFamily="Yantra One" fontSize="18" fontWeight="900" fill="#1C1C1C">₹</text>
                  </g>
                );
              })}

              {/* Stock container - beaker */}
              <g transform="translate(310, 200)">
                <path d="M 0 0 L 0 20 L 20 160 L 140 160 L 160 20 L 160 0 Z"
                  fill="none" stroke="#14FFEC" strokeWidth="4" />
                {/* Fill */}
                <rect x="22" y={160 - pourProgress * 120} width="116" height={pourProgress * 120}
                  fill="#14FFEC" opacity="0.4" />
                {/* Stock label */}
                <text x="80" y="100" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#F5F5F5">STOCK</text>
                <text x="80" y="125" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="14" fill="#14FFEC">???</text>
                {/* Bubbles */}
                {pourProgress > 0.3 && [0, 1, 2].map((i) => (
                  <circle key={i}
                    cx={50 + i * 30}
                    cy={150 - (frame * 1.5 + i * 20) % 100}
                    r="4"
                    fill="#F5F5F5"
                    opacity="0.6"
                  />
                ))}
              </g>
            </svg>
          </div>
        </div>

        {/* Bottom warning bar */}
        <div style={{
          position: 'absolute',
          bottom: '5%',
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: sp(200),
          background: '#2D2D2D',
          border: '2px solid #F4CE14',
          padding: '14px 40px',
          borderRadius: 6,
          fontFamily: 'JetBrains Mono',
          fontSize: 26,
          color: '#F4CE14',
          letterSpacing: 3,
        }}>
          // FAST. EASY. RISKY.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene05_DMatAccountSavings;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_ReelTip',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_ReelTip: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Shake on impact at frame 100
  const shake = frame > 100 && frame < 130
    ? Math.sin(frame * 2) * (130 - frame) / 4
    : 0;

  // Reel scroll
  const scrollY = interpolate(frame, [40, 160], [0, -400], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#212121', overflow: 'hidden', opacity: exitOp, transform: `translateX(${shake}px)` }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 50%, rgba(244,206,20,0.08) 0%, transparent 50%), radial-gradient(ellipse at 80% 60%, rgba(20,255,236,0.06) 0%, transparent 50%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: '10%',
        right: '15%',
        width: 400,
        height: 400,
        background: '#F4CE14',
        opacity: 0.04,
        clipPath: 'polygon(50% 0%, 100% 38%, 82% 100%, 18% 100%, 0% 38%)',
        transform: `rotate(${frame * 0.25}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '5%',
        left: '5%',
        width: 350,
        height: 350,
        borderRadius: '50%',
        border: '4px solid #14FFEC',
        opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.05) * 0.06})`,
      }} />

      {/* Wave texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="wave6" width="200" height="100" patternUnits="userSpaceOnUse">
            <path d="M 0 50 Q 50 0 100 50 T 200 50" fill="none" stroke="#F4CE14" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#wave6)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const r = seededRandom(i + 60);
        const r2 = seededRandom(i + 600);
        const x = (r * 1920 + frame * (0.2 + r * 0.3)) % 1920;
        const y = r2 * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 3 + r * 5,
            height: 3 + r * 5,
            borderRadius: '50%',
            background: i % 3 === 0 ? '#F4CE14' : '#14FFEC',
            opacity: 0.35,
            boxShadow: '0 0 12px currentColor',
          }} />
        );
      })}

      {/* LEFT: Calendar showing 3 days */}
      <div style={{
        position: 'absolute',
        top: '15%',
        left: '6%',
        width: '40%',
        opacity: sp(10),
        transform: `translateX(${(1 - sp(10)) * -80}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 26,
          color: '#F4CE14',
          letterSpacing: 5,
          marginBottom: 16,
        }}>TIMELINE</div>

        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 130,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1,
          marginBottom: 40,
        }}>
          तीन <span style={{ color: '#F4CE14', textShadow: '0 0 30px rgba(244,206,20,0.6)' }}>दिन</span><br/>
          पहले...
        </div>

        {/* 3 day calendar boxes */}
        <div style={{ display: 'flex', gap: 20 }}>
          {['Day 1', 'Day 2', 'Day 3'].map((day, i) => (
            <div key={i} style={{
              opacity: sp(40 + i * 25),
              transform: `scale(${sp(40 + i * 25)})`,
              width: 150,
              height: 170,
              background: i === 0 ? '#F4CE14' : '#2D2D2D',
              border: `3px solid ${i === 0 ? '#F4CE14' : '#14FFEC'}`,
              borderRadius: 8,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 16,
                color: i === 0 ? '#1C1C1C' : '#9E9E9E',
                fontWeight: 700,
              }}>{day.toUpperCase()}</div>
              <div style={{
                fontFamily: 'Yantra One',
                fontSize: 80,
                fontWeight: 900,
                color: i === 0 ? '#1C1C1C' : '#F5F5F5',
                lineHeight: 1,
              }}>{i + 1}</div>
              {i === 0 && (
                <div style={{
                  fontFamily: 'Inter',
                  fontSize: 14,
                  color: '#1C1C1C',
                  fontWeight: 700,
                }}>REEL SEEN</div>
              )}
            </div>
          ))}
        </div>
      </div>

      {/* RIGHT: Phone with reel */}
      <div style={{
        position: 'absolute',
        top: '8%',
        right: '12%',
        opacity: sp(20),
        transform: `translateY(${(1 - sp(20)) * 150}px) scale(${sp(20)})`,
      }}>
        <svg width="380" height="780" viewBox="0 0 380 780">
          {/* Phone outline */}
          <rect x="10" y="10" width="360" height="760" rx="50" fill="#1C1C1C" stroke="#14FFEC" strokeWidth="6" />
          {/* Screen */}
          <rect x="30" y="40" width="320" height="700" rx="20" fill="#000" />
          {/* Notch */}
          <rect x="140" y="20" width="100" height="20" rx="10" fill="#1C1C1C" />

          {/* Reel content area with scroll */}
          <g clipPath="url(#screenClip)">
            <defs>
              <clipPath id="screenClip">
                <rect x="30" y="40" width="320" height="700" rx="20" />
              </clipPath>
            </defs>

            <g transform={`translate(0, ${scrollY})`}>
              {/* Reel 1 - Investment guru */}
              <rect x="30" y="40" width="320" height="700" fill="#2D2D2D" />
              <circle cx="190" cy="280" r="80" fill="#F4CE14" />
              <text x="190" y="295" textAnchor="middle" fontFamily="Inter" fontSize="60" fontWeight="900" fill="#1C1C1C">$</text>
              <text x="190" y="450" textAnchor="middle" fontFamily="Inter" fontSize="28" fontWeight="900" fill="#F4CE14">10X RETURNS!</text>
              <text x="190" y="490" textAnchor="middle" fontFamily="Inter" fontSize="20" fill="#F5F5F5">XYZ STOCK</text>
              <text x="190" y="520" textAnchor="middle" fontFamily="Inter" fontSize="16" fill="#9E9E9E">@guruji_official</text>

              {/* Reel 2 */}
              <rect x="30" y="740" width="320" height="700" fill="#1C1C1C" />
              <text x="190" y="900" textAnchor="middle" fontFamily="Inter" fontSize="36" fontWeight="900" fill="#14FFEC">BUY NOW!</text>
              <text x="190" y="950" textAnchor="middle" fontFamily="Inter" fontSize="22" fill="#F5F5F5">MULTIBAGGER ALERT</text>
              <text x="190" y="980" textAnchor="middle" fontFamily="Inter" fontSize="16" fill="#9E9E9E">@stocktips_pro</text>
            </g>
          </g>

          {/* Reel UI overlays */}
          <text x="50" y="80" fontFamily="Inter" fontSize="20" fontWeight="700" fill="#F5F5F5">Reels</text>
          <text x="330" y="80" textAnchor="end" fontFamily="Inter" fontSize="20" fill="#F5F5F5">⊕</text>

          {/* Right side icons */}
          <circle cx="330" cy="500" r="18" fill="#ff0000" />
          <text x="330" y="506" textAnchor="middle" fontFamily="Inter" fontSize="14" fontWeight="900" fill="#F5F5F5">♥</text>
          <text x="330" y="540" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="12" fill="#F5F5F5">2.4M</text>

          <circle cx="330" cy="580" r="18" fill="#14FFEC" opacity="0.3" />
          <text x="330" y="586" textAnchor="middle" fontFamily="Inter" fontSize="14" fontWeight="900" fill="#F5F5F5">💬</text>
          <text x="330" y="620" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="12" fill="#F5F5F5">89K</text>
        </svg>

        {/* "REEL" label tag */}
        <div style={{
          position: 'absolute',
          top: -20,
          left: -30,
          background: '#ff0000',
          color: '#F5F5F5',
          padding: '8px 20px',
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 900,
          letterSpacing: 3,
          borderRadius: 4,
          transform: `rotate(-12deg) scale(${sp(80)})`,
          boxShadow: '0 4px 20px rgba(255,0,0,0.5)',
        }}>REEL</div>
      </div>

      {/* Big text overlay at impact */}
      <div style={{
        position: 'absolute',
        bottom: '8%',
        left: '50%',
        transform: `translateX(-50%) scale(${sp(100, { damping: 8 })})`,
        opacity: sp(100),
        fontFamily: 'Yantra One',
        fontSize: 90,
        fontWeight: 900,
        color: '#ff0000',
        textShadow: '0 0 40px rgba(255,0,0,0.6)',
        letterSpacing: 2,
      }}>
        "बस इतनी रिसर्च!"
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene06_ReelTip;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_SilentChange',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_SilentChange: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom out then in
  const camScale = interpolate(frame, [0, 180, durationInFrames], [1.15, 1, 1.05]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#212121', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, rgba(20,255,236,0.1) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        width: 1000,
        height: 1000,
        borderRadius: '50%',
        border: '2px solid #14FFEC',
        opacity: 0.04,
        transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        width: 700,
        height: 700,
        borderRadius: '50%',
        border: '2px solid #F4CE14',
        opacity: 0.05,
        transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.025 + 1) * 0.05})`,
      }} />

      {/* Faint map dots texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="map7" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#F4CE14" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#map7)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const r = seededRandom(i + 70);
        const r2 = seededRandom(i + 700);
        const x = r * 1920;
        const y = r2 * 1080 + Math.sin(frame * 0.02 + i) * 15;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 4,
            height: 4,
            borderRadius: '50%',
            background: i % 2 === 0 ? '#14FFEC' : '#F4CE14',
            opacity: 0.4 + Math.sin(frame * 0.05 + i) * 0.3,
            boxShadow: '0 0 12px currentColor',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        {/* Big headline at top */}
        <div style={{
          position: 'absolute',
          top: '10%',
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(5)) * 60}px)`,
          opacity: sp(5),
          fontFamily: 'Yantra One',
          fontSize: 200,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 0.9,
          textAlign: 'center',
        }}>
          असली <span style={{ color: '#F4CE14', textShadow: '0 0 50px rgba(244,206,20,0.7)' }}>बदलाव</span>
        </div>

        {/* INDIA MAP visualization - grid of glowing houses */}
        <div style={{
          position: 'absolute',
          top: '38%',
          left: '50%',
          transform: 'translateX(-50%)',
          width: 900,
          height: 380,
        }}>
          <svg width="900" height="380" viewBox="0 0 900 380">
            {/* India outline simplified */}
            <path
              d="M 200 50 Q 100 120 120 220 Q 130 300 200 340 Q 320 360 450 350 Q 580 340 680 300 Q 760 260 780 180 Q 770 100 700 60 Q 600 30 450 40 Q 320 30 200 50 Z"
              fill="none"
              stroke="#14FFEC"
              strokeWidth="3"
              opacity="0.4"
              strokeDasharray="1800"
              strokeDashoffset={1800 - 1800 * sp(20)}
            />

            {/* Grid of houses lighting up across India */}
            {Array.from({ length: 60 }).map((_, i) => {
              const r = seededRandom(i + 100);
              const r2 = seededRandom(i + 200);
              const cx = 150 + r * 600;
              const cy = 70 + r2 * 260;
              const lightUpFrame = 40 + i * 3;
              const isLit = frame > lightUpFrame;
              const litProgress = isLit ? Math.min(1, (frame - lightUpFrame) / 20) : 0;
              const pulse = isLit ? 0.7 + Math.sin(frame * 0.1 + i) * 0.3 : 0;

              return (
                <g key={i} transform={`translate(${cx}, ${cy})`} opacity={litProgress}>
                  {/* House */}
                  <polygon points="-8,0 0,-10 8,0 6,12 -6,12" fill={i % 4 === 0 ? '#F4CE14' : '#14FFEC'} opacity={pulse} />
                  {/* Glow */}
                  <circle cx="0" cy="0" r="14" fill={i % 4 === 0 ? '#F4CE14' : '#14FFEC'} opacity={pulse * 0.3} />
                </g>
              );
            })}

            {/* Connection lines forming silently */}
            {sp(150) > 0.3 && Array.from({ length: 12 }).map((_, i) => {
              const r1 = seededRandom(i * 7);
              const r2 = seededRandom(i * 7 + 1);
              const r3 = seededRandom(i * 7 + 2);
              const r4 = seededRandom(i * 7 + 3);
              return (
                <line key={i}
                  x1={150 + r1 * 600} y1={70 + r2 * 260}
                  x2={150 + r3 * 600} y2={70 + r4 * 260}
                  stroke="#F4CE14"
                  strokeWidth="1"
                  opacity={0.2 * sp(150)}
                />
              );
            })}
          </svg>

          {/* Map label */}
          <div style={{
            position: 'absolute',
            top: 0,
            right: 30,
            fontFamily: 'JetBrains Mono',
            fontSize: 22,
            color: '#9E9E9E',
            letterSpacing: 4,
            opacity: sp(40),
          }}>INDIA / EVERY HOME</div>
        </div>

        {/* Bottom huge text */}
        <div style={{
          position: 'absolute',
          bottom: '8%',
          left: '50%',
          transform: `translateX(-50%)`,
          opacity: sp(150),
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 110,
            fontWeight: 900,
            color: '#14FFEC',
            lineHeight: 1,
            textShadow: '0 0 40px rgba(20,255,236,0.6)',
          }}>
            चुपचाप <span style={{ color: '#F5F5F5' }}>हर घर में</span>
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 28,
            color: '#F4CE14',
            letterSpacing: 8,
            marginTop: 16,
          }}>
            // SILENT REVOLUTION
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene07_SilentChange;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_HiddenLayer',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_HiddenLayer: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Newspaper headline slides in
  const newspaperIn = sp(10);
  // Magnifier scans across
  const magX = interpolate(frame, [60, 240], [0, 800], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });
  // Revealed text appears
  const reveal = sp(140);

  return (
    <AbsoluteFill style={{ backgroundColor: '#212121', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(244,206,20,0.06) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: '5%',
        right: '5%',
        width: 420,
        height: 420,
        background: '#14FFEC',
        opacity: 0.04,
        clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '5%',
        left: '5%',
        width: 500,
        height: 500,
        border: '4px solid #F4CE14',
        opacity: 0.05,
        transform: `rotate(${-frame * 0.15}deg)`,
        borderRadius: 24,
      }} />

      {/* Newspaper texture lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        <defs>
          <pattern id="news8" width="6" height="6" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="6" y2="0" stroke="#F5F5F5" strokeWidth="0.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#news8)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 80);
        const r2 = seededRandom(i + 800);
        const x = r * 1920;
        const y = (r2 * 1080 + frame * 0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 3,
            height: 3,
            borderRadius: '50%',
            background: '#F4CE14',
            opacity: 0.4,
            boxShadow: '0 0 10px #F4CE14',
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute',
        top: '6%',
        left: '50%',
        transform: 'translateX(-50%)',
        opacity: sp(5),
        background: '#F4CE14',
        color: '#1C1C1C',
        padding: '10px 36px',
        fontFamily: 'JetBrains Mono',
        fontSize: 28,
        fontWeight: 900,
        letterSpacing: 6,
        borderRadius: 4,
      }}>
        BREAKING / DEEPER LAYER
      </div>

      {/* Main headline */}
      <div style={{
        position: 'absolute',
        top: '14%',
        left: '50%',
        transform: 'translateX(-50%)',
        opacity: sp(15),
        fontFamily: 'Yantra One',
        fontSize: 130,
        fontWeight: 900,
        color: '#F5F5F5',
        textAlign: 'center',
        lineHeight: 1,
      }}>
        वो <span style={{ color: '#14FFEC', textShadow: '0 0 30px rgba(20,255,236,0.6)' }}>परत</span> जो हेडलाइन में नहीं
      </div>

      {/* Newspaper mockup */}
      <div style={{
        position: 'absolute',
        top: '40%',
        left: '50%',
        transform: `translateX(-50%) translateY(${(1 - newspaperIn) * 200}px) rotate(${(1 - newspaperIn) * -8}deg)`,
        opacity: newspaperIn,
        width: 1100,
        height: 460,
      }}>
        <svg width="1100" height="460" viewBox="0 0 1100 460">
          {/* Newspaper background */}
          <rect x="0" y="0" width="1100" height="460" fill="#F5F5F5" rx="6" />
          {/* Masthead */}
          <text x="550" y="60" textAnchor="middle" fontFamily="Inter" fontSize="44" fontWeight="900" fill="#1C1C1C">THE DAILY MARKET</text>
          <line x1="40" y1="80" x2="1060" y2="80" stroke="#1C1C1C" strokeWidth="3" />
          <text x="60" y="105" fontFamily="JetBrains Mono" fontSize="14" fill="#9E9E9E">VOL.XII / EDITION 247</text>
          <text x="1040" y="105" textAnchor="end" fontFamily="JetBrains Mono" fontSize="14" fill="#9E9E9E">2025</text>

          {/* Big headline */}
          <text x="60" y="170" fontFamily="Inter" fontSize="56" fontWeight="900" fill="#1C1C1C">MARKETS HIT NEW HIGHS</text>
          <text x="60" y="220" fontFamily="Inter" fontSize="32" fontWeight="700" fill="#2D2D2D">Investors celebrate as Sensex soars</text>

          {/* Body lines */}
          {[0, 1, 2, 3, 4, 5].map((i) => (
            <rect key={i} x="60" y={260 + i * 22} width={i % 2 === 0 ? 940 : 800} height="10" fill="#9E9E9E" opacity="0.6" />
          ))}

          {/* Right column image placeholder */}
          <rect x="780" y="260" width="240" height="160" fill="#2D2D2D" />
          <text x="900" y="345" textAnchor="middle" fontFamily="Inter" fontSize="20" fontWeight="700" fill="#F4CE14">CHART</text>
        </svg>

        {/* Magnifier glass scanning */}
        <div style={{
          position: 'absolute',
          left: magX,
          top: 60,
          opacity: magX > 0 ? 1 : 0,
        }}>
          <svg width="280" height="320" viewBox="0 0 280 320">
            <circle cx="120" cy="120" r="100" fill="rgba(28,28,28,0.85)" stroke="#F4CE14" strokeWidth="6" />
            <line x1="195" y1="195" x2="265" y2="265" stroke="#F4CE14" strokeWidth="14" strokeLinecap="round" />
            {/* Hidden truth peeking through magnifier */}
            <text x="120" y="100" textAnchor="middle" fontFamily="Inter" fontSize="18" fontWeight="900" fill="#ff0000">HIDDEN</text>
            <text x="120" y="130" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#F4CE14">TRUTH</text>
            <text x="120" y="160" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="12" fill="#14FFEC">below surface</text>
          </svg>
        </div>
      </div>

      {/* Bottom revealed text */}
      <div style={{
        position: 'absolute',
        bottom: '6%',
        left: '50%',
        transform: `translateX(-50%) scale(${reveal})`,
        opacity: reveal,
        background: '#1C1C1C',
        border: '3px solid #14FFEC',
        padding: '20px 50px',
        borderRadius: 8,
        fontFamily: 'Noto Sans Devanagari',
        fontSize: 48,
        fontWeight: 700,
        color: '#F5F5F5',
        textAlign: 'center',
        boxShadow: '0 0 40px rgba(20,255,236,0.3)',
      }}>
        सच जो खबरों में <span style={{ color: '#F4CE14' }}>नहीं दिखता</span>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene08_HiddenLayer;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_FDLeaving',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_FDLeaving: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // FD slowly breaking/dissolving
  const breakProgress = interpolate(frame, [40, 200], [0, 1], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });

  // Question mark builds
  const questionScale = sp(160, { damping: 8 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#212121', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(244,206,20,0.05) 0%, transparent 50%, rgba(255,0,0,0.05) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: '10%',
        left: '10%',
        width: 420,
        height: 420,
        borderRadius: '50%',
        background: '#F4CE14',
        opacity: 0.04,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '10%',
        right: '8%',
        width: 480,
        height: 480,
        border: '3px solid #14FFEC',
        opacity: 0.05,
        transform: `rotate(${frame * 0.25}deg)`,
      }} />

      {/* Diagonal stripe texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="stripe9" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform="rotate(-30)">
            <line x1="0" y1="0" x2="0" y2="60" stroke="#14FFEC" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#stripe9)" />
      </svg>

      {/* Particles - leaving particles flowing right */}
      {Array.from({ length: 30 }).map((_, i) => {
        const r = seededRandom(i + 90);
        const r2 = seededRandom(i + 900);
        const x = (r * 1920 + frame * 2) % 1920;
        const y = r2 * 1080 + Math.sin(frame * 0.05 + i) * 15;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 4,
            height: 4,
            borderRadius: '50%',
            background: i % 3 === 0 ? '#F4CE14' : '#14FFEC',
            opacity: 0.4,
            boxShadow: '0 0 10px currentColor',
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute',
        top: '6%',
        left: '50%',
        transform: 'translateX(-50%)',
        opacity: sp(5),
        fontFamily: 'JetBrains Mono',
        fontSize: 32,
        color: '#9E9E9E',
        letterSpacing: 8,
      }}>
        BUT THE REAL QUESTION IS...
      </div>

      {/* Main headline */}
      <div style={{
        position: 'absolute',
        top: '14%',
        left: '50%',
        transform: `translateX(-50%) translateY(${(1 - sp(15)) * 60}px)`,
        opacity: sp(15),
        fontFamily: 'Yantra One',
        fontSize: 130,
        fontWeight: 900,
        color: '#F5F5F5',
        textAlign: 'center',
        lineHeight: 1,
      }}>
        सवाल सिर्फ ये <span style={{ color: '#ff0000' }}>नहीं</span>
      </div>

      {/* CENTRAL VISUAL: FD certificate breaking/dissolving */}
      <div style={{
        position: 'absolute',
        top: '38%',
        left: '50%',
        transform: 'translate(-50%, 0)',
        width: 700,
        height: 380,
      }}>
        <svg width="700" height="380" viewBox="0 0 700 380">
          {/* FD certificate body */}
          <g opacity={1 - breakProgress * 0.4}>
            <rect x="50" y="20" width="600" height="340" rx="12" fill="#2D2D2D" stroke="#F4CE14" strokeWidth="5" />

            {/* Header */}
            <rect x="50" y="20" width="600" height="60" rx="12" fill="#F4CE14" />
            <text x="350" y="60" textAnchor="middle" fontFamily="Inter" fontSize="28" fontWeight="900" fill="#1C1C1C">FIXED DEPOSIT CERTIFICATE</text>

            {/* Amount */}
            <text x="80" y="140" fontFamily="JetBrains Mono" fontSize="20" fill="#9E9E9E">PRINCIPAL AMOUNT</text>
            <text x="80" y="200" fontFamily="Yantra One" fontSize="72" fontWeight="900" fill="#14FFEC">₹ 5,00,000</text>

            {/* Details grid */}
            <text x="80" y="250" fontFamily="JetBrains Mono" fontSize="18" fill="#9E9E9E">RATE: 7.5%</text>
            <text x="280" y="250" fontFamily="JetBrains Mono" fontSize="18" fill="#9E9E9E">TENURE: 5 YR</text>
            <text x="480" y="250" fontFamily="JetBrains Mono" fontSize="18" fill="#9E9E9E">STATUS: ?</text>

            {/* Decorative */}
            <circle cx="600" cy="320" r="30" fill="none" stroke="#F4CE14" strokeWidth="2" />
            <text x="600" y="326" textAnchor="middle" fontFamily="Inter" fontSize="14" fontWeight="700" fill="#F4CE14">SEAL</text>
          </g>

          {/* Breaking cracks */}
          {breakProgress > 0.2 && (
            <g opacity={breakProgress}>
              <line x1="100" y1="80" x2="200" y2="240" stroke="#ff0000" strokeWidth="3" />
              <line x1="200" y1="240" x2="180" y2="350" stroke="#ff0000" strokeWidth="3" />
              <line x1="400" y1="40" x2="380" y2="180" stroke="#ff0000" strokeWidth="3" />
              <line x1="380" y1="180" x2="500" y2="280" stroke="#ff0000" strokeWidth="3" />
              <line x1="500" y1="280" x2="620" y2="340" stroke="#ff0000" strokeWidth="3" />
            </g>
          )}

          {/* Floating away pieces */}
          {breakProgress > 0.4 && Array.from({ length: 12 }).map((_, i) => {
            const r = seededRandom(i + 110);
            const r2 = seededRandom(i + 1100);
            return (
              <rect
                key={i}
                x={100 + r * 500}
                y={50 + r2 * 280 - (breakProgress - 0.4) * 200}
                width={20 + r * 30}
                height={15 + r * 25}
                fill="#F4CE14"
                opacity={(breakProgress - 0.4) * 1.5 * (1 - (breakProgress - 0.4))}
                transform={`rotate(${frame * (r > 0.5 ? 2 : -2)} ${100 + r * 500 + 15} ${50 + r2 * 280 + 10})`}
              />
            );
          })}

          {/* "LEAVING" stamp */}
          {breakProgress > 0.3 && (
            <g transform="translate(350, 190) rotate(-15)" opacity={breakProgress}>
              <rect x="-130" y="-40" width="260" height="80" fill="none" stroke="#ff0000" strokeWidth="6" rx="4" />
              <text x="0" y="14" textAnchor="middle" fontFamily="Inter" fontSize="48" fontWeight="900" fill="#ff0000">LEAVING</text>
            </g>
          )}
        </svg>
      </div>

      {/* Bottom question */}
      <div style={{
        position: 'absolute',
        bottom: '6%',
        left: '50%',
        transform: `translateX(-50%) scale(${questionScale})`,
        opacity: questionScale,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 96,
          fontWeight: 900,
          color: '#14FFEC',
          textShadow: '0 0 40px rgba(20,255,236,0.6)',
          lineHeight: 1,
        }}>
          सवाल कुछ और है<span style={{ color: '#F4CE14' }}>?</span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene09_FDLeaving;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_TwoOutcomes',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_TwoOutcomes: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Path split reveal
  const splitProgress = sp(40);
  const leftIn = sp(90);
  const rightIn = sp(120);

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#212121', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(255,0,0,0.08) 0%, transparent 30%, transparent 70%, rgba(20,255,236,0.08) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        width: 800,
        height: 800,
        borderRadius: '50%',
        border: '2px solid #F4CE14',
        opacity: 0.05,
        transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />

      {/* Crossroad texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="cross10" width="80" height="80" patternUnits="userSpaceOnUse">
            <line x1="0" y1="40" x2="80" y2="40" stroke="#F4CE14" strokeWidth="0.5" />
            <line x1="40" y1="0" x2="40" y2="80" stroke="#F4CE14" strokeWidth="0.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross10)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const r = seededRandom(i + 130);
        const r2 = seededRandom(i + 1300);
        const x = r * 1920;
        const direction = i % 2 === 0 ? 1 : -1;
        const xMove = (r * 1920 + direction * frame * 0.8 + 1920) % 1920;
        const y = (r2 * 1080 + frame * 0.3) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: xMove,
            top: y,
            width: 4,
            height: 4,
            borderRadius: '50%',
            background: direction > 0 ? '#14FFEC' : '#ff0000',
            opacity: 0.4,
            boxShadow: '0 0 10px currentColor',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        {/* Top question */}
        <div style={{
          position: 'absolute',
          top: '6%',
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: sp(5),
          fontFamily: 'Yantra One',
          fontSize: 110,
          fontWeight: 900,
          color: '#F5F5F5',
          textAlign: 'center',
          lineHeight: 1,
        }}>
          ये <span style={{ color: '#F4CE14' }}>भीड़</span> क्या करेगी?
        </div>

        {/* Crossroad SVG - splitting paths */}
        <div style={{
          position: 'absolute',
          top: '24%',
          left: '50%',
          transform: 'translateX(-50%)',
          width: 1400,
          height: 200,
        }}>
          <svg width="1400" height="200" viewBox="0 0 1400 200">
            {/* Single starting path */}
            <path
              d="M 700 0 L 700 80"
              stroke="#F5F5F5"
              strokeWidth="14"
              fill="none"
              strokeLinecap="round"
            />
            {/* Splits */}
            <path
              d="M 700 80 Q 700 100 400 180"
              stroke="#ff0000"
              strokeWidth="14"
              fill="none"
              strokeDasharray="500"
              strokeDashoffset={500 - 500 * splitProgress}
              strokeLinecap="round"
            />
            <path
              d="M 700 80 Q 700 100 1000 180"
              stroke="#14FFEC"
              strokeWidth="14"
              fill="none"
              strokeDasharray="500"
              strokeDashoffset={500 - 500 * splitProgress}
              strokeLinecap="round"
            />
            {/* Junction dot */}
            <circle cx="700" cy="80" r="20" fill="#F4CE14" />
            <text x="700" y="86" textAnchor="middle" fontFamily="Inter" fontSize="18" fontWeight="900" fill="#1C1C1C">?</text>
          </svg>
        </div>

        {/* LEFT outcome: किनारे लगाना */}
        <div style={{
          position: 'absolute',
          top: '48%',
          left: '6%',
          width: '40%',
          transform: `translateX(${(1 - leftIn) * -80}px) scale(${leftIn})`,
          opacity: leftIn,
        }}>
          <div style={{
            background: '#2D2D2D',
            border: '5px solid #ff0000',
            borderRadius: 16,
            padding: 40,
            boxShadow: '0 0 50px rgba(255,0,0,0.3)',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#ff0000',
              letterSpacing: 4,
              marginBottom: 10,
            }}>OUTCOME A</div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 84,
              fontWeight: 900,
              color: '#ff0000',
              lineHeight: 1,
              marginBottom: 16,
            }}>किनारे</div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 42,
              fontWeight: 700,
              color: '#F5F5F5',
              marginBottom: 30,
            }}>लगा देगी?</div>

            {/* Down arrow visual */}
            <svg width="100%" height="180" viewBox="0 0 500 180">
              <polyline
                points="20,40 100,80 180,60 260,120 340,100 420,160 480,140"
                fill="none"
                stroke="#ff0000"
                strokeWidth="6"
              />
              <polygon points="480,140 460,130 470,150" fill="#ff0000" />
              {/* Down arrow icon */}
              <text x="450" y="50" fontFamily="Inter" fontSize="56" fontWeight="900" fill="#ff0000">↓</text>
              <text x="50" y="170" fontFamily="JetBrains Mono" fontSize="16" fill="#9E9E9E">CRASH / LOSS</text>
            </svg>
          </div>
        </div>

        {/* RIGHT outcome: माला माल */}
        <div style={{
          position: 'absolute',
          top: '48%',
          right: '6%',
          width: '40%',
          transform: `translateX(${(1 - rightIn) * 80}px) scale(${rightIn})`,
          opacity: rightIn,
        }}>
          <div style={{
            background: '#2D2D2D',
            border: '5px solid #14FFEC',
            borderRadius: 16,
            padding: 40,
            boxShadow: '0 0 50px rgba(20,255,236,0.3)',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              color: '#14FFEC',
              letterSpacing: 4,
              marginBottom: 10,
            }}>OUTCOME B</div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 84,
              fontWeight: 900,
              color: '#14FFEC',
              lineHeight: 1,
              marginBottom: 16,
            }}>मालामाल</div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 42,
              fontWeight: 700,
              color: '#F5F5F5',
              marginBottom: 30,
            }}>कर देगी?</div>

            {/* Up arrow visual */}
            <svg width="100%" height="180" viewBox="0 0 500 180">
              <polyline
                points="20,160 100,140 180,100 260,120 340,80 420,40 480,30"
                fill="none"
                stroke="#14FFEC"
                strokeWidth="6"
              />
              <polygon points="480,30 470,50 460,40" fill="#14FFEC" />
              {/* Up arrow icon */}
              <text x="450" y="170" fontFamily="Inter" fontSize="56" fontWeight="900" fill="#14FFEC">↑</text>
              <text x="50" y="170" fontFamily="JetBrains Mono" fontSize="16" fill="#9E9E9E">WEALTH / GROWTH</text>
            </svg>
          </div>
        </div>

        {/* Bottom centered VS */}
        <div style={{
          position: 'absolute',
          top: '70%',
          left: '50%',
          transform: `translate(-50%, -50%) scale(${sp(150, { damping: 8 })})`,
          opacity: sp(150),
          width: 140,
          height: 140,
          borderRadius: '50%',
          background: '#F4CE14',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          fontFamily: 'Inter',
          fontSize: 64,
          fontWeight: 900,
          color: '#1C1C1C',
          boxShadow: '0 0 50px rgba(244,206,20,0.7)',
          border: '6px solid #F5F5F5',
        }}>VS</div>

        {/* Bottom caption */}
        <div style={{
          position: 'absolute',
          bottom: '4%',
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: sp(200),
          fontFamily: 'JetBrains Mono',
          fontSize: 32,
          color: '#F4CE14',
          letterSpacing: 6,
        }}>
          // ज़िंदगी और मौत का सवाल
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene10_TwoOutcomes;
```

