```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.12], { extrapolateRight: 'clamp' });

  const icons = [
    { d: 'M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5', l: 'Studium' },
    { d: 'M20 7h-4V4c0-1.1-.9-2-2-2h-4c-1.1 0-2 .9-2 2v3H4c-1.1 0-2 .9-2 2v11c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V9c0-1.1-.9-2-2-2zM10 4h4v3h-4V4z', l: 'Job' },
    { d: 'M3 21h18v-2H3v2zm6-4h12v-2H9v2zm-6-4h18v-2H3v2zm6-4h12V7H9v2zM3 3v2h18V3H3z', l: 'Sparen' },
    { d: 'M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z', l: 'Wohnung' },
    { d: 'M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z', l: 'Schutz' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale})` }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 40% 50%, #2D2D2D 0%, #1C1C1C 70%)' }} />
      
      <div style={{ position: 'absolute', width: 700, height: 700, borderRadius: '50%', background: 'rgba(233,215,0,0.04)', top: -150, left: -100, transform: `translate(${Math.sin(frame*0.01)*40}px,${Math.cos(frame*0.012)*30}px) rotate(${frame*0.08}deg)` }} />
      <div style={{ position: 'absolute', width: 500, height: 500, borderRadius: 12, background: 'rgba(255,255,255,0.03)', bottom: -80, right: 300, transform: `translate(${Math.cos(frame*0.009)*50}px,${Math.sin(frame*0.011)*40}px) rotate(${-frame*0.06}deg)` }} />
      
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({length:24}).map((_,i)=><line key={i} x1="0" y1={i*45} x2="1920" y2={i*45} stroke="#F5F5F5" strokeWidth="1" />)}
      </svg>
      
      {Array.from({length:25}).map((_,i)=>{
        const r=seededRandom(i); const x=(r*1920+frame*(0.6+r*0.4))%1920; const y=(seededRandom(i+50)*1080+frame*(0.4+r*0.3))%1080;
        return <div key={i} style={{ position:'absolute',left:x,top:y,width:2+r*3,height:2+r*3,borderRadius:'50%',background:'#F5F5F5',opacity:0.15+r*0.25 }} />;
      })}

      <div style={{ position:'absolute',width:'100%',height:'100%',display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',gap:50 }}>
        <div style={{ fontFamily:"'Anton',sans-serif",fontSize:110,color:'#F5F5F5',textTransform:'uppercase',letterSpacing:6,transform:`scale(${sp(0)})`,opacity:sp(0),textAlign:'center' }}>
          Alles richtig gemacht?
        </div>
        <div style={{ display:'flex',gap:50,alignItems:'flex-start' }}>
          {icons.map((ic,idx)=>(
            <div key={idx} style={{ display:'flex',flexDirection:'column',alignItems:'center',gap:14,transform:`translateY(${sp(12+idx*7)*-30}px) scale(${sp(12+idx*7)})`,opacity:sp(12+idx*7) }}>
              <svg width="72" height="72" viewBox="0 0 24 24" fill="none" stroke="#e9d700" strokeWidth="1.5"><path d={ic.d}/></svg>
              <span style={{ fontFamily:"'Open Sans',sans-serif",fontSize:34,color:'#F5F5F5',fontWeight:700 }}>{ic.l}</span>
            </div>
          ))}
        </div>
      </div>

      <div style={{ position:'absolute',bottom:100,left:'50%',marginLeft:-60,transform:`translateY(${Math.sin(frame*0.05)*2}px)` }}>
        <svg width="120" height="200" viewBox="0 0 120 200" style={{ transform:`rotate(${Math.sin(frame*0.04)*3}deg)` }}>
          <circle cx="60" cy="30" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="50" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="25" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="95" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="30" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="90" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <circle cx="52" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <circle cx="68" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <line x1="58" y1="28" x2="62" y2="28" stroke="#FFC107" strokeWidth="2"/>
        </svg>
        <div style={{ position:'absolute',top:-20,left:-20,width:160,height:240,background:'radial-gradient(circle, rgba(255,193,7,0.15) 0%, transparent 70%)',borderRadius:'50%'}}/>
      </div>

      <AbsoluteFill style={{ boxShadow:'inset 0 0 200px rgba(0,0,0,0.8)',pointerEvents:'none' }} />
      <div style={{ position:'absolute',bottom:30,right:45,fontFamily:"'Inter',sans-serif",fontSize:22,fontWeight:600,color:'#9E9E9E',opacity:0.3 }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camX = interpolate(frame, [0, durationInFrames], [0, 40], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp, transform: `translateX(${camX}px)` }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill style={{ background: 'linear-gradient(210deg, #1C1C1C 0%, #2D2D2D 50%, #1C1C1C 100%)' }} />
      
      <div style={{ position: 'absolute', width: 800, height: 800, background: 'rgba(255,193,7,0.03)', clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)', top: -200, right: -100, transform: `translate(${Math.cos(frame*0.007)*60}px,${Math.sin(frame*0.009)*40}px) rotate(${frame*0.04}deg)` }} />
      <div style={{ position: 'absolute', width: 350, height: 350, borderRadius: '50%', background: 'rgba(255,255,255,0.04)', bottom: 100, left: 80, transform: `translate(${Math.sin(frame*0.01)*30}px,${Math.cos(frame*0.008)*30}px)` }} />
      
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({length:30}).map((_,i)=><circle key={i} cx={seededRandom(i)*1920} cy={seededRandom(i+100)*1080} r={2+seededRandom(i+200)*4} fill="none" stroke="#F5F5F5" strokeWidth="1" />)}
      </svg>
      
      {Array.from({length:20}).map((_,i)=>{
        const r=seededRandom(i); const x=(r*1920+frame*(0.8+r*0.5))%1920; const y=(seededRandom(i+40)*1080-frame*(0.5+r*0.3)+1080)%1080;
        return <div key={i} style={{ position:'absolute',left:x,top:y,width:3+r*4,height:3+r*4,borderRadius:'50%',background:'#e9d700',opacity:0.1+r*0.2 }} />;
      })}

      <div style={{ position:'absolute',width:'100%',height:'100%',display:'flex',alignItems:'center',justifyContent:'center',gap:80 }}>
        <div style={{ display:'flex',flexDirection:'column',alignItems:'center',gap:20,transform:`translateY(${sp(0)*-40}px) scale(${sp(0)})`,opacity:sp(0) }}>
          <svg width="100" height="100" viewBox="0 0 24 24" fill="none" stroke="#ff0000" strokeWidth="1.5"><path d="M18.92 6.01C18.72 5.42 18.16 5 17.5 5h-11c-.66 0-1.21.42-1.42 1.01L3 12v8c0 .55.45 1 1 1h1c.55 0 1-.45 1-1v-1h12v1c0 .55.45 1 1 1h1c.55 0 1-.45 1-1v-8l-2.08-5.99zM6.5 16c-.83 0-1.5-.67-1.5-1.5S5.67 13 6.5 13s1.5.67 1.5 1.5S7.33 16 6.5 16zm11 0c-.83 0-1.5-.67-1.5-1.5s.67-1.5 1.5-1.5 1.5.67 1.5 1.5-.67 1.5-1.5 1.5zM5 11l1.5-4.5h11L19 11H5z"/></svg>
          <span style={{ fontFamily:"'Anton',sans-serif",fontSize:72,color:'#F5F5F5' }}>NEU</span>
          <span style={{ fontFamily:"'Inter',sans-serif",fontSize:32,color:'#9E9E9E',fontWeight:600 }}>Leasing</span>
        </div>
        <div style={{ display:'flex',flexDirection:'column',alignItems:'center',gap:20,transform:`translateY(${sp(10)*-40}px) scale(${sp(10)})`,opacity:sp(10) }}>
          <svg width="100" height="100" viewBox="0 0 24 24" fill="none" stroke="#e9d700" strokeWidth="1.5"><path d="M3.5 18.49l6-6.01 4 4L22 6.92l-1.41-1.41-7.09 7.97-4-4L2 16.99z"/></svg>
          <span style={{ fontFamily:"'Anton',sans-serif",fontSize:72,color:'#F5F5F5' }}>ETF</span>
          <span style={{ fontFamily:"'Inter',sans-serif",fontSize:32,color:'#9E9E9E',fontWeight:600 }}>200 €/Monat</span>
        </div>
        <div style={{ display:'flex',flexDirection:'column',alignItems:'center',gap:20,transform:`translateY(${sp(20)*-40}px) scale(${sp(20)})`,opacity:sp(20) }}>
          <svg width="100" height="100" viewBox="0 0 24 24" fill="none" stroke="#F5F5F5" strokeWidth="1.5"><path d="M20 4H4c-1.11 0-1.99.89-1.99 2L2 18c0 1.11.89 2 2 2h16c1.11 0 2-.89 2-2V6c0-1.11-.89-2-2-2zm0 14H4v-6h16v6zm0-10H4V6h16v2z"/></svg>
          <span style={{ fontFamily:"'Anton',sans-serif",fontSize:56,color:'#F5F5F5',textAlign:'center' }}>PRIVATE<br/>RENTE</span>
        </div>
      </div>

      <div style={{ position:'absolute',bottom:100,left:'50%',marginLeft:-60,transform:`translateY(${Math.sin(frame*0.05)*2}px)` }}>
        <svg width="120" height="200" viewBox="0 0 120 200" style={{ transform:`rotate(${Math.sin(frame*0.04)*3}deg)` }}>
          <circle cx="60" cy="30" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="50" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="25" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="95" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="30" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="90" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <circle cx="52" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <circle cx="68" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <line x1="58" y1="28" x2="62" y2="28" stroke="#FFC107" strokeWidth="2"/>
        </svg>
        <div style={{ position:'absolute',top:-20,left:-20,width:160,height:240,background:'radial-gradient(circle, rgba(255,193,7,0.15) 0%, transparent 70%)',borderRadius:'50%'}}/>
      </div>

      <AbsoluteFill style={{ boxShadow:'inset 0 0 200px rgba(0,0,0,0.8)',pointerEvents:'none' }} />
      <div style={{ position:'absolute',bottom:30,right:45,fontFamily:"'Inter',sans-serif",fontSize:22,fontWeight:600,color:'#9E9E9E',opacity:0.3 }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene2;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const shake = frame > 180 ? interpolate(frame, [180,190,200,210,220,230], [0,-8,6,-4,2,0], {extrapolateLeft:'clamp',extrapolateRight:'clamp'}) : 0;
  const splitOp = interpolate(frame, [120,150], [0,1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp, transform: `translateX(${shake}px)` }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #1C1C1C 0%, #2D2D2D 50%, #1C1C1C 100%)' }} />
      
      <div style={{ position: 'absolute', width: '50%', height: '100%', left: 0, top: 0, background: 'rgba(233,215,0,0.02)', transform: `skewX(${Math.sin(frame*0.02)*2}deg)` }} />
      <div style={{ position: 'absolute', width: '50%', height: '100%', right: 0, top: 0, background: 'rgba(255,0,0,0.02)', opacity: splitOp, transform: `skewX(${Math.cos(frame*0.02)*-2}deg)` }} />
      
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({length:40}).map((_,i)=><line key={i} x1={i*48} y1="0" x2={i*48+200} y2="1080" stroke="#F5F5F5" strokeWidth="1" />)}
      </svg>
      
      {Array.from({length:30}).map((_,i)=>{
        const r=seededRandom(i); const x=(r*1920+frame*(0.3+r*0.2))%1920; const y=(seededRandom(i+20)*1080+frame*(0.6+r*0.4))%1080;
        return <div key={i} style={{ position:'absolute',left:x,top:y,width:2+r*3,height:2+r*3,borderRadius:'50%',background:frame>120?'#ff0000':'#e9d700',opacity:0.2+r*0.3,transition:'background 0.5s' }} />;
      })}

      <div style={{ position:'absolute',width:'100%',height:'100%',display:'flex',alignItems:'center',justifyContent:'center' }}>
        <div style={{ display:'flex',flexDirection:'column',alignItems:'center',gap:30,transform:`scale(${sp(0)})`,opacity:sp(0) }}>
          <div style={{ display:'flex',gap:40,alignItems:'center' }}>
            {['Studium','Job','Sparen','Haus','Rente'].map((t,i)=>(
              <div key={i} style={{ display:'flex',alignItems:'center',gap:12,transform:`translateY(${sp(10+i*5)*-20}px)`,opacity:sp(10+i*5) }}>
                <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#e9d700" strokeWidth="3"><path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/></svg>
                <span style={{ fontFamily:"'Open Sans',sans-serif",fontSize:44,color:'#F5F5F5',fontWeight:700 }}>{t}</span>
              </div>
            ))}
          </div>
          <div style={{ fontFamily:"'Anton',sans-serif",fontSize:140,color:'#ff0000',textTransform:'uppercase',letterSpacing:8,transform:`scale(${sp(120)})`,opacity:sp(120),textShadow:'0 0 60px rgba(255,0,0,0.5)' }}>
            Dann: Krise
          </div>
          <div style={{ fontFamily:"'Open Sans',sans-serif",fontSize:48,color:'#F5F5F5',fontWeight:700,transform:`translateY(${sp(140)*20}px)`,opacity:sp(140),textAlign:'center' }}>
            Nicht die kleine Delle.<br/>Der echte Bruch.
          </div>
        </div>
      </div>

      <div style={{ position:'absolute',bottom:100,left:'50%',marginLeft:-60,transform:`translateY(${Math.sin(frame*0.05)*2}px)`,opacity:interpolate(frame,[60,90],[0,1],{extrapolateLeft:'clamp',extrapolateRight:'clamp'}) }}>
        <svg width="120" height="200" viewBox="0 0 120 200" style={{ transform:`rotate(${Math.sin(frame*0.04)*3}deg)` }}>
          <circle cx="60" cy="30" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="50" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="25" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="95" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="30" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="90" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <circle cx="52" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <circle cx="68" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <line x1="58" y1="28" x2="62" y2="28" stroke="#FFC107" strokeWidth="2"/>
          <path d="M48 38 Q60 45 72 38" fill="none" stroke="#F5F5F5" strokeWidth="2"/>
        </svg>
        <div style={{ position:'absolute',top:-20,left:-20,width:160,height:240,background:'radial-gradient(circle, rgba(255,193,7,0.15) 0%, transparent 70%)',borderRadius:'50%',opacity:0.5}}/>
      </div>

      <AbsoluteFill style={{ boxShadow:'inset 0 0 200px rgba(0,0,0,0.8)',pointerEvents:'none' }} />
      <div style={{ position:'absolute',bottom:30,right:45,fontFamily:"'Inter',sans-serif",fontSize:22,fontWeight:600,color:'#9E9E9E',opacity:0.3 }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene3;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const rot = interpolate(frame, [0, durationInFrames], [-2, 2], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp, transform: `rotate(${rot}deg) scale(1.05)` }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 120%, #2D2D2D 0%, #1C1C1C 60%)' }} />
      
      <div style={{ position: 'absolute', width: 900, height: 900, borderRadius: '50%', border: '1px solid rgba(255,255,255,0.04)', top: -300, left: '50%', marginLeft: -450, transform: `rotate(${frame*0.2}deg)` }} />
      <div style={{ position: 'absolute', width: 600, height: 600, borderRadius: '50%', border: '1px solid rgba(255,255,255,0.03)', top: -150, left: '50%', marginLeft: -300, transform: `rotate(${-frame*0.3}deg)` }} />
      
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({length:50}).map((_,i)=><rect key={i} x={seededRandom(i)*1920} y={seededRandom(i+100)*1080} width={20+seededRandom(i+200)*60} height={1} fill="#F5F5F5" />)}
      </svg>
      
      {Array.from({length:40}).map((_,i)=>{
        const r=seededRandom(i); const x=(r*1920+frame*(0.2+r*0.3))%1920; const y=(seededRandom(i+60)*1080+frame*(0.1+r*0.2))%1080;
        return <div key={i} style={{ position:'absolute',left:x,top:y,width:2+r*2,height:2+r*2,borderRadius:'50%',background:'#9E9E9E',opacity:0.1+r*0.15 }} />;
      })}

      <div style={{ position:'absolute',width:'100%',height:'100%',display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',gap:20 }}>
        <div style={{ fontFamily:"'Anton',sans-serif",fontSize:240,color:'#ff0000',textTransform:'uppercase',letterSpacing:12,transform:`scale(${sp(0)})`,opacity:sp(0),textShadow:'0 0 80px rgba(255,0,0,0.4)' }}>
          WERTLOS
        </div>
        <div style={{ fontFamily:"'Open Sans',sans-serif",fontSize:48,color:'#F5F5F5',fontWeight:700,transform:`translateY(${sp(40)*30}px)`,opacity:sp(40),textAlign:'center' }}>
          Nicht gestohlen. Nicht zerstört.<br/>Einfach wertlos.
        </div>
        <div style={{ marginTop:30,display:'flex',gap:60,transform:`translateY(${sp(80)*20}px)`,opacity:sp(80) }}>
          {['Deutschland','Argentinien','Griechenland','Japan','USA'].map((c,i)=>(
            <div key={i} style={{ display:'flex',flexDirection:'column',alignItems:'center',gap:8 }}>
              <div style={{ width:12,height:12,borderRadius:'50%',background:'#e9d700' }} />
              <span style={{ fontFamily:"'Inter',sans-serif",fontSize:28,color:'#9E9E9E',fontWeight:600 }}>{c}</span>
            </div>
          ))}
        </div>
        <div style={{ fontFamily:"'JetBrains Mono',monospace",fontSize:32,color:'#9E9E9E',marginTop:20,transform:`opacity(${sp(120)})`,opacity:sp(120) }}>
          dokumentierte Geschichte → mechanische Präzision
        </div>
      </div>

      <div style={{ position:'absolute',bottom:100,left:'50%',marginLeft:-60,transform:`translateY(${Math.sin(frame*0.05)*2}px)`,opacity:0.6 }}>
        <svg width="120" height="200" viewBox="0 0 120 200" style={{ transform:`rotate(${Math.sin(frame*0.04)*3}deg)` }}>
          <circle cx="60" cy="30" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="50" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="25" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="95" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="30" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="90" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <circle cx="52" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <circle cx="68" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <line x1="58" y1="28" x2="62" y2="28" stroke="#FFC107" strokeWidth="2"/>
        </svg>
      </div>

      <AbsoluteFill style={{ boxShadow:'inset 0 0 200px rgba(0,0,0,0.8)',pointerEvents:'none' }} />
      <div style={{ position:'absolute',bottom:30,right:45,fontFamily:"'Inter',sans-serif",fontSize:22,fontWeight:600,color:'#9E9E9E',opacity:0.3 }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene4;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const targetScale = interpolate(frame, [0, durationInFrames], [1.3, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, #2D2D2D 0%, #1C1C1C 65%)' }} />
      
      <div style={{ position: 'absolute', width: 1200, height: 1200, borderRadius: '50%', border: '2px solid rgba(255,0,0,0.06)', top: '50%', left: '50%', marginTop: -600, marginLeft: -600, transform: `scale(${targetScale}) rotate(${frame*0.1}deg)` }} />
      <div style={{ position: 'absolute', width: 800, height: 800, borderRadius: '50%', border: '2px solid rgba(255,0,0,0.08)', top: '50%', left: '50%', marginTop: -400, marginLeft: -400, transform: `scale(${targetScale*0.8}) rotate(${-frame*0.15}deg)` }} />
      <div style={{ position: 'absolute', width: 400, height: 400, borderRadius: '50%', border: '2px solid rgba(255,0,0,0.1)', top: '50%', left: '50%', marginTop: -200, marginLeft: -200, transform: `scale(${targetScale*0.6}) rotate(${frame*0.2}deg)` }} />
      
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({length:20}).map((_,i)=><line key={i} x1="0" y1={i*54} x2="1920" y2={i*54} stroke="#ff0000" strokeWidth="1" />)}
      </svg>
      
      {Array.from({length:25}).map((_,i)=>{
        const r=seededRandom(i); const a=(frame*(0.5+r*0.5)+r*360)%360; const rad=a*Math.PI/180; const dist=300+r*500;
        const x=960+Math.cos(rad)*dist; const y=540+Math.sin(rad)*dist;
        return <div key={i} style={{ position:'absolute',left:x,top:y,width:3,height:3,borderRadius:'50%',background:'#ff0000',opacity:0.2+r*0.3 }} />;
      })}

      <div style={{ position:'absolute',width:'100%',height:'100%',display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',gap:30 }}>
        <div style={{ fontFamily:"'Anton',sans-serif",fontSize:72,color:'#ff0000',textTransform:'uppercase',letterSpacing:6,transform:`translateY(${sp(0)*-30}px)`,opacity:sp(0) }}>
          Ziel erfasst
        </div>
        <div style={{ fontFamily:"'Anton',sans-serif",fontSize:160,color:'#F5F5F5',textTransform:'uppercase',letterSpacing:10,transform:`scale(${sp(30)})`,opacity:sp(30) }}>
          Mittelschicht
        </div>
        <div style={{ fontFamily:"'Open Sans',sans-serif",fontSize:48,color:'#9E9E9E',fontWeight:700,transform:`translateY(${sp(60)*20}px)`,opacity:sp(60) }}>
          die brav die Regeln befolgt hat
        </div>
        <div style={{ marginTop:40,display:'flex',gap:40,transform:`translateY(${sp(90)*20}px)`,opacity:sp(90) }}>
          <span style={{ fontFamily:"'Inter',sans-serif",fontSize:36,color:'#e9d700',fontWeight:700 }}>Aktien</span>
          <span style={{ fontFamily:"'Inter',sans-serif",fontSize:36,color:'#9E9E9E',fontWeight:700 }}>→</span>
          <span style={{ fontFamily:"'Inter',sans-serif",fontSize:36,color:'#ff0000',fontWeight:700 }}>Heute: etwas anderes</span>
        </div>
      </div>

      <div style={{ position:'absolute',bottom:120,left:'50%',marginLeft:-60,transform:`translateY(${Math.sin(frame*0.05)*2}px)` }}>
        <svg width="120" height="200" viewBox="0 0 120 200" style={{ transform:`rotate(${Math.sin(frame*0.04)*3}deg)` }}>
          <circle cx="60" cy="30" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="50" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="25" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="95" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="30" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="90" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <circle cx="52" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <circle cx="68" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <line x1="58" y1="28" x2="62" y2="28" stroke="#FFC107" strokeWidth="2"/>
        </svg>
        <div style={{ position:'absolute',top:-20,left:-20,width:160,height:240,background:'radial-gradient(circle, rgba(255,193,7,0.2) 0%, transparent 70%)',borderRadius:'50%'}}/>
      </div>

      <AbsoluteFill style={{ boxShadow:'inset 0 0 200px rgba(0,0,0,0.8)',pointerEvents:'none' }} />
      <div style={{ position:'absolute',bottom:30,right:45,fontFamily:"'Inter',sans-serif",fontSize:22,fontWeight:600,color:'#9E9E9E',opacity:0.3 }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene5;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const darkPulse = interpolate(frame, [0, durationInFrames/2, durationInFrames], [0.3, 0.6, 0.3], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill style={{ background: `radial-gradient(circle at 50% 50%, rgba(255,0,0,${darkPulse*0.15}) 0%, #1C1C1C 70%)` }} />
      
      <div style={{ position: 'absolute', width: '100%', height: '100%', background: 'repeating-linear-gradient(0deg, transparent, transparent 19px, rgba(255,255,255,0.03) 20px)', opacity: 0.5 }} />
      
      <div style={{ position: 'absolute', width: 500, height: 500, background: 'rgba(255,0,0,0.04)', filter: 'blur(80px)', borderRadius: '50%', top: '30%', left: '30%', transform: `translate(${Math.sin(frame*0.01)*50}px,${Math.cos(frame*0.012)*50}px)` }} />
      
      {Array.from({length:20}).map((_,i)=>{
        const r=seededRandom(i); const x=(r*1920); const y=(seededRandom(i+30)*1080+frame*(0.8+r*0.5))%1080;
        return <div key={i} style={{ position:'absolute',left:x,top:y,width:2+r*2,height:8+r*4,background:'#ff0000',opacity:0.1+r*0.15,transform:`rotate(${seededRandom(i+50)*20}deg)` }} />;
      })}

      <div style={{ position:'absolute',width:'100%',height:'100%',display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',padding:'0 200px',gap:30 }}>
        <div style={{ fontFamily:"'Anton',sans-serif",fontSize:90,color:'#F5F5F5',textTransform:'uppercase',textAlign:'center',lineHeight:1.2,transform:`translateY(${sp(0)*-40}px)`,opacity:sp(0) }}>
          Das, worüber<br/>kaum jemand spricht
        </div>
        <div style={{ display:'flex',gap:50,transform:`scale(${sp(40)})`,opacity:sp(40) }}>
          <span style={{ fontFamily:"'Open Sans',sans-serif",fontSize:44,color:'#ff0000',fontWeight:700 }}>unangenehm</span>
          <span style={{ fontFamily:"'Open Sans',sans-serif",fontSize:44,color:'#e9d700',fontWeight:700 }}>Angst</span>
          <span style={{ fontFamily:"'Open Sans',sans-serif",fontSize:44,color:'#9E9E9E',fontWeight:700 }}>infrage gestellt</span>
        </div>
        <div style={{ fontFamily:"'Open Sans',sans-serif",fontSize:48,color:'#F5F5F5',fontWeight:700,textAlign:'center',marginTop:20,transform:`translateY(${sp(80)*30}px)`,opacity:sp(80) }}>
          Was als <span style={{color:'#e9d700'}}>sicher</span> verkauft wurde,<br/>ist oft das <span style={{color:'#ff0000'}}>Gefährlichste</span>
        </div>
      </div>

      <div style={{ position:'absolute',bottom:100,left:'50%',marginLeft:-60,transform:`translateY(${Math.sin(frame*0.05)*2}px)`,opacity:0.4 }}>
        <svg width="120" height="200" viewBox="0 0 120 200" style={{ transform:`rotate(${Math.sin(frame*0.04)*3}deg)` }}>
          <circle cx="60" cy="30" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="50" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="25" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="95" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="30" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="90" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <circle cx="52" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <circle cx="68" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <line x1="58" y1="28" x2="62" y2="28" stroke="#FFC107" strokeWidth="2"/>
        </svg>
      </div>

      <AbsoluteFill style={{ boxShadow:'inset 0 0 250px rgba(0,0,0,0.9)',pointerEvents:'none' }} />
      <div style={{ position:'absolute',bottom:30,right:45,fontFamily:"'Inter',sans-serif",fontSize:22,fontWeight:600,color:'#9E9E9E',opacity:0.3 }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene6;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const mapY = interpolate(frame, [0, durationInFrames], [50, 0], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #1C1C1C 0%, #0a1628 50%, #1C1C1C 100%)' }} />
      
      <div style={{ position: 'absolute', width: 700, height: 700, borderRadius: '50%', border: '1px solid rgba(0,100,200,0.08)', top: '50%', left: '50%', marginTop: -350, marginLeft: -350, transform: `rotate(${frame*0.05}deg)` }} />
      <div style={{ position: 'absolute', width: 500, height: 500, background: 'rgba(0,100,200,0.03)', borderRadius: '40% 60% 70% 30% / 40% 50% 60% 50%', top: '20%', left: '35%', transform: `translate(${Math.sin(frame*0.01)*30}px,${Math.cos(frame*0.012)*30}px)` }} />
      
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({length:15}).map((_,i)=><path key={i} d={`M${i*128} 0 Q${960} ${540+Math.sin(i)*200} ${i*128} 1080`} fill="none" stroke="#F5F5F5" strokeWidth="1" />)}
      </svg>
      
      {Array.from({length:30}).map((_,i)=>{
        const r=seededRandom(i); const x=(r*1920+frame*(0.4+r*0.3))%1920; const y=(seededRandom(i+80)*1080+frame*(0.3+r*0.2))%1080;
        return <div key={i} style={{ position:'absolute',left:x,top:y,width:2+r*3,height:2+r*3,borderRadius:'50%',background:'#e9d700',opacity:0.15+r*0.25 }} />;
      })}

      <div style={{ position:'absolute',width:'100%',height:'100%',display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',gap:20,transform:`translateY(${mapY}px)` }}>
        <div style={{ fontFamily:"'Open Sans',sans-serif",fontSize:44,color:'#9E9E9E',fontWeight:700,transform:`translateY(${sp(0)*-20}px)`,opacity:sp(0) }}>
          Keine Theorie.
        </div>
        <div style={{ fontFamily:"'Anton',sans-serif",fontSize:200,color:'#F5F5F5',textTransform:'uppercase',letterSpacing:8,transform:`scale(${sp(20)})`,opacity:sp(20) }}>
          Griechenland
        </div>
        <div style={{ display:'flex',alignItems:'center',gap:30,transform:`translateY(${sp(50)*20}px)`,opacity:sp(50) }}>
          <div style={{ width:80,height:4,background:'#e9d700' }} />
          <span style={{ fontFamily:"'Inter',sans-serif",fontSize:60,color:'#e9d700',fontWeight:700 }}>2010</span>
          <div style={{ width:80,height:4,background:'#e9d700' }} />
        </div>
      </div>

      <div style={{ position:'absolute',bottom:100,left:'50%',marginLeft:-60,transform:`translateY(${Math.sin(frame*0.05)*2}px)` }}>
        <svg width="120" height="200" viewBox="0 0 120 200" style={{ transform:`rotate(${Math.sin(frame*0.04)*3}deg)` }}>
          <circle cx="60" cy="30" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="50" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="25" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="95" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="30" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="90" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <circle cx="52" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <circle cx="68" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <line x1="58" y1="28" x2="62" y2="28" stroke="#FFC107" strokeWidth="2"/>
        </svg>
        <div style={{ position:'absolute',top:-20,left:-20,width:160,height:240,background:'radial-gradient(circle, rgba(255,193,7,0.15) 0%, transparent 70%)',borderRadius:'50%'}}/>
      </div>

      <AbsoluteFill style={{ boxShadow:'inset 0 0 200px rgba(0,0,0,0.8)',pointerEvents:'none' }} />
      <div style={{ position:'absolute',bottom:30,right:45,fontFamily:"'Inter',sans-serif",fontSize:22,fontWeight:600,color:'#9E9E9E',opacity:0.3 }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene7;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const freezeOp = interpolate(frame, [150, 180], [0, 1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 30%, #1a1a2e 0%, #1C1C1C 70%)' }} />
      
      <div style={{ position: 'absolute', width: 600, height: 600, background: 'rgba(255,255,255,0.02)', clipPath: 'polygon(20% 0%, 80% 0%, 100% 100%, 0% 100%)', left: 200, top: 100, transform: `translate(${Math.sin(frame*0.008)*20}px,${frame*0.2}px)` }} />
      
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({length:30}).map((_,i)=><line key={i} x1={i*64} y1="0" x2={i*64} y2="1080" stroke="#F5F5F5" strokeWidth="1" />)}
      </svg>
      
      {Array.from({length:25}).map((_,i)=>{
        const r=seededRandom(i); const x=(r*1920); const y=(seededRandom(i+10)*1080-frame*(0.5+r*0.4)+1080)%1080;
        return <div key={i} style={{ position:'absolute',left:x,top:y,width:3,height:3,borderRadius:'50%',background:freezeOp>0.5?'#9E9E9E':'#e9d700',opacity:0.15+r*0.2 }} />;
      })}

      <div style={{ position:'absolute',width:'100%',height:'100%',display:'flex',alignItems:'center',justifyContent:'center',gap:60 }}>
        <div style={{ display:'flex',flexDirection:'column',alignItems:'center',gap:16,transform:`translateX(${sp(0)*-60}px) scale(${sp(0)})`,opacity:sp(0) }}>
          <svg width="120" height="120" viewBox="0 0 24 24" fill="none" stroke="#ff0000" strokeWidth="1.5"><path d="M11.5 2C6.81 2 3 5.81 3 10.5S6.81 19 11.5 19h.5v3c4.86-2.5 8-7.25 8-11.5C20 5.81 16.19 2 11.5 2zm1 14.5h-2v-2h2v2zm0-3.5h-2c0-3.25 3-3 3-5 0-1.1-.9-2-2-2s-2 .9-2 2h-2c0-2.21 1.79-4 4-4s4 1.79 4 4c0 2.5-3 2.75-3 5z"/></svg>
          <span style={{ fontFamily:"'Anton',sans-serif",fontSize:80,color:'#ff0000' }}>-40%</span>
          <span style={{ fontFamily:"'Open Sans',sans-serif",fontSize:32,color:'#F5F5F5',fontWeight:700 }}>Rentenkürzung</span>
        </div>
        <div style={{ display:'flex',flexDirection:'column',alignItems:'center',gap:16,transform:`scale(${sp(30)})`,opacity:sp(30) }}>
          <svg width="120" height="120" viewBox="0 0 24 24" fill="none" stroke="#ff0000" strokeWidth="1.5"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
          <span style={{ fontFamily:"'Anton',sans-serif",fontSize:80,color:'#ff0000' }}>-50%</span>
          <span style={{ fontFamily:"'Open Sans',sans-serif",fontSize:32,color:'#F5F5F5',fontWeight:700 }}>Immobilien Athen</span>
        </div>
        <div style={{ display:'flex',flexDirection:'column',alignItems:'center',gap:16,transform:`translateX(${sp(60)*60}px) scale(${sp(60)})`,opacity:sp(60) }}>
          <svg width="120" height="120" viewBox="0 0 24 24" fill="none" stroke="#9E9E9E" strokeWidth="1.5"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>
          <span style={{ fontFamily:"'Anton',sans-serif",fontSize:56,color:'#9E9E9E' }}>EINGEFROREN</span>
          <span style={{ fontFamily:"'Open Sans',sans-serif",fontSize:32,color:'#F5F5F5',fontWeight:700 }}>Keine Käufer</span>
        </div>
      </div>

      <div style={{ position:'absolute',bottom:100,left:'50%',marginLeft:-60,transform:`translateY(${Math.sin(frame*0.05)*2}px)`,opacity:0.5 }}>
        <svg width="120" height="200" viewBox="0 0 120 200" style={{ transform:`rotate(${Math.sin(frame*0.04)*3}deg)` }}>
          <circle cx="60" cy="30" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="50" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="25" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="95" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="30" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="90" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <circle cx="52" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <circle cx="68" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <line x1="58" y1="28" x2="62" y2="28" stroke="#FFC107" strokeWidth="2"/>
        </svg>
      </div>

      <AbsoluteFill style={{ boxShadow:'inset 0 0 200px rgba(0,0,0,0.8)',pointerEvents:'none' }} />
      <div style={{ position:'absolute',bottom:30,right:45,fontFamily:"'Inter',sans-serif",fontSize:22,fontWeight:600,color:'#9E9E9E',opacity:0.3 }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene8;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9',
  durationInSeconds: 22,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const trapClose = interpolate(frame, [120, 180], [0, 1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 0%, #2a1a1a 0%, #1C1C1C 70%)' }} />
      
      <div style={{ position: 'absolute', width: '100%', height: '100%', background: 'repeating-linear-gradient(90deg, transparent, transparent 39px, rgba(255,0,0,0.04) 40px)' }} />
      
      <div style={{ position: 'absolute', left: '50%', top: '50%', marginLeft: -400, marginTop: -300, width: 800, height: 600, border: `6px solid rgba(255,0,0,${0.1+trapClose*0.3})`, transform: `scale(${1+trapClose*0.05})`, transition: 'border-color 0.5s' }} />
      
      {Array.from({length:20}).map((_,i)=>{
        const r=seededRandom(i); const x=(r*1920+frame*(0.3+r*0.2))%1920; const y=(seededRandom(i+20)*1080+frame*(0.6+r*0.4))%1080;
        return <div key={i} style={{ position:'absolute',left:x,top:y,width:2+r*3,height:2+r*3,borderRadius:'50%',background:'#ff0000',opacity:0.1+r*0.2 }} />;
      })}

      <div style={{ position:'absolute',width:'100%',height:'100%',display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',gap:20 }}>
        <div style={{ fontFamily:"'Anton',sans-serif",fontSize:100,color:'#ff0000',textTransform:'uppercase',transform:`scale(${sp(0)})`,opacity:sp(0) }}>
          Die Falle
        </div>
        <div style={{ position:'relative',width:300,height:300,transform:`translateY(${sp(40)*20}px)`,opacity:sp(40) }}>
          <svg width="300" height="300" viewBox="0 0 24 24" fill="none" stroke="#F5F5F5" strokeWidth="0.8">
            <path d="M12 3L1 9l4 2.18v6L12 21l7-3.82v-6l2-1.09V17h2V9L12 3zm6.82 6L12 12.72 5.18 9 12 5.28 18.82 9zM17 15.99l-5 2.73-5-2.73v-3.72L12 15l5-2.73v3.72z"/>
          </svg>
          <div style={{ position:'absolute',top:'110%',left:0,width:'100%',textAlign:'center' }}>
            <span style={{ fontFamily:"'Open Sans',sans-serif",fontSize:48,color:'#F5F5F5',fontWeight:700 }}>Haus</span>
          </div>
          <div style={{ position:'absolute',top:20,right:-120,transform:`translateX(${sp(100)*20}px)`,opacity:sp(100) }}>
            <span style={{ fontFamily:"'Anton',sans-serif",fontSize:72,color:'#ff0000' }}>+Steuer</span>
          </div>
          <div style={{ position:'absolute',bottom:20,left:-150,transform:`translateX(${sp(120)*-20}px)`,opacity:sp(120) }}>
            <span style={{ fontFamily:"'Anton',sans-serif",fontSize:72,color:'#ff0000' }}>-Einkommen</span>
          </div>
        </div>
        <div style={{ fontFamily:"'Open Sans',sans-serif",fontSize:40,color:'#9E9E9E',fontWeight:700,marginTop:40,textAlign:'center',transform:`translateY(${sp(150)*20}px)`,opacity:sp(150) }}>
          Nicht verkaufen. Nicht wegziehen.<br/>Steuerlast frißt alles.
        </div>
      </div>

      <div style={{ position:'absolute',bottom:100,left:'50%',marginLeft:-60,transform:`translateY(${Math.sin(frame*0.05)*2}px)`,opacity:0.5 }}>
        <svg width="120" height="200" viewBox="0 0 120 200" style={{ transform:`rotate(${Math.sin(frame*0.04)*3}deg)` }}>
          <circle cx="60" cy="30" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="50" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="25" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="95" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="30" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="90" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <circle cx="52" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <circle cx="68" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <line x1="58" y1="28" x2="62" y2="28" stroke="#FFC107" strokeWidth="2"/>
        </svg>
      </div>

      <AbsoluteFill style={{ boxShadow:'inset 0 0 200px rgba(0,0,0,0.8)',pointerEvents:'none' }} />
      <div style={{ position:'absolute',bottom:30,right:45,fontFamily:"'Inter',sans-serif",fontSize:22,fontWeight:600,color:'#9E9E9E',opacity:0.3 }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene9;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.15], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp, transform: `scale(${camZoom})` }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, #1a2035 0%, #1C1C1C 70%)' }} />
      
      <div style={{ position: 'absolute', width: 1000, height: 2, background: 'rgba(233,215,0,0.1)', top: '50%', left: '50%', marginLeft: -500, transform: `rotate(${frame*0.02}deg)` }} />
      <div style={{ position: 'absolute', width: 1000, height: 2, background: 'rgba(233,215,0,0.1)', top: '50%', left: '50%', marginLeft: -500, transform: `rotate(${-frame*0.02}deg)` }} />
      
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({length:12}).map((_,i)=><ellipse key={i} cx="960" cy="540" rx={400+i*60} ry={200+i*40} fill="none" stroke="#F5F5F5" strokeWidth="1" />)}
      </svg>
      
      {Array.from({length:40}).map((_,i)=>{
        const r=seededRandom(i); const x=(r*1920+frame*(0.2+r*0.3))%1920; const y=(seededRandom(i+60)*1080+frame*(0.1+r*0.2))%1080;
        return <div key={i} style={{ position:'absolute',left:x,top:y,width:2+r*2,height:2+r*2,borderRadius:'50%',background:'#F5F5F5',opacity:0.1+r*0.15 }} />;
      })}

      <div style={{ position:'absolute',width:'100%',height:'100%',display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',gap:24 }}>
        <div style={{ fontFamily:"'Open Sans',sans-serif",fontSize:44,color:'#9E9E9E',fontWeight:700,transform:`translateY(${sp(0)*-20}px)`,opacity:sp(0) }}>
          Kein exotisches Land.
        </div>
        <div style={{ fontFamily:"'Anton',sans-serif",fontSize:120,color:'#F5F5F5',textTransform:'uppercase',letterSpacing:6,transform:`scale(${sp(30)})`,opacity:sp(30) }}>
          Europa
        </div>
        <div style={{ display:'flex',alignItems:'center',gap:24,transform:`translateY(${sp(60)*20}px)`,opacity:sp(60) }}>
          <div style={{ width:100,height:4,background:'#e9d700' }} />
          <span style={{ fontFamily:"'Inter',sans-serif",fontSize:48,color:'#e9d700',fontWeight:700 }}>vor 10 Jahren</span>
          <div style={{ width:100,height:4,background:'#e9d700' }} />
        </div>
        <div style={{ fontFamily:"'Anton',sans-serif",fontSize:72,color:'#ff0000',textAlign:'center',marginTop:20,transform:`scale(${sp(100)})`,opacity:sp(100) }}>
          Was wurde nutzlos?
        </div>
      </div>

      <div style={{ position:'absolute',bottom:100,left:'50%',marginLeft:-60,transform:`translateY(${Math.sin(frame*0.05)*2}px)`,opacity:0.5 }}>
        <svg width="120" height="200" viewBox="0 0 120 200" style={{ transform:`rotate(${Math.sin(frame*0.04)*3}deg)` }}>
          <circle cx="60" cy="30" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="50" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="25" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="70" x2="95" y2="105" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="30" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <line x1="60" y1="120" x2="90" y2="190" stroke="#F5F5F5" strokeWidth="3"/>
          <circle cx="52" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <circle cx="68" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2"/>
          <line x1="58" y1="28" x2="62" y2="28" stroke="#FFC107" strokeWidth="2"/>
        </svg>
      </div>

      <AbsoluteFill style={{ boxShadow:'inset 0 0 200px rgba(0,0,0,0.8)',pointerEvents:'none' }} />
      <div style={{ position:'absolute',bottom:30,right:45,fontFamily:"'Inter',sans-serif",fontSize:22,fontWeight:600,color:'#9E9E9E',opacity:0.3 }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene10;
```

