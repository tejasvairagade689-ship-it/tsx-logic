```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_Ruko',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_Ruko: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1.0], { extrapolateRight: 'clamp' });

  // countdown ticking hand
  const tick = Math.sin(frame * 0.35) * 2;
  const handRot = interpolate(frame % 60, [0, 60], [0, 360]);

  const rukoIn = sp(8, { stiffness: 140 });
  const shake = frame > 8 && frame < 20 ? Math.sin(frame * 3) * (interpolate(frame, [8, 20], [6, 0])) : 0;

  const line2 = interpolate(sp(40), [0, 1], [40, 0]);
  const line3Op = sp(70);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 40%, #C9CEFB 0%, #B7BDF7 45%, #9AA2EC 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(4)].map((_, i) => {
          const s = seededRandom(i + 1);
          const rot = frame * (0.15 + s * 0.1) + i * 40;
          const breathe = 1 + Math.sin(frame * 0.03 + i) * 0.06;
          const size = 380 + s * 320;
          return (
            <div key={i} style={{
              position: 'absolute', left: `${s * 80}%`, top: `${seededRandom(i + 9) * 75}%`,
              width: size, height: size, borderRadius: i % 2 === 0 ? '50%' : '18%',
              border: `3px solid #000000`, opacity: 0.05,
              transform: `rotate(${rot}deg) scale(${breathe})`,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture — vertical grid lines */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {[...Array(16)].map((_, i) => (
          <div key={i} style={{ position: 'absolute', left: `${(i / 16) * 100}%`, top: 0, bottom: 0, width: 1, background: '#000' }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(26)].map((_, i) => {
        const s = seededRandom(i + 50);
        const y = (s * 1080 - frame * (0.5 + s) * 2) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${s * 100}%`, top: `${(y + 1080) % 1080}px`,
            width: 4 + s * 6, height: 4 + s * 6, borderRadius: '50%',
            background: i % 3 === 0 ? '#FFA3FD' : '#000', opacity: 0.18,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        {/* Ticking clock SVG icon */}
        <div style={{ transform: `translateY(${interpolate(sp(4), [0, 1], [-60, 0])}px)`, marginBottom: 24, opacity: sp(4) }}>
          <svg width="150" height="150" viewBox="0 0 120 120">
            <circle cx="60" cy="60" r="52" fill="none" stroke="#000" strokeWidth="7" />
            <circle cx="60" cy="60" r="52" fill="#FFA3FD" opacity="0.18" />
            <line x1="60" y1="60" x2="60" y2="26" stroke="#000" strokeWidth="6" strokeLinecap="round" transform={`rotate(${handRot} 60 60)`} />
            <line x1="60" y1="60" x2="86" y2="60" stroke="#000" strokeWidth="5" strokeLinecap="round" transform={`rotate(${tick} 60 60)`} />
            <circle cx="60" cy="60" r="6" fill="#000" />
            <rect x="52" y="4" width="16" height="10" rx="3" fill="#000" />
          </svg>
        </div>

        <div style={{
          fontFamily: 'Yantra One, Noto Sans Devanagari, sans-serif', fontSize: 200, lineHeight: 0.95,
          color: '#000', fontWeight: 700, transform: `scale(${rukoIn})`, letterSpacing: '-2px',
          textShadow: '0 6px 0 #FFA3FD',
        }}>
          एक मिनट रुको
        </div>

        <div style={{ fontSize: 60, color: '#000', fontWeight: 700, marginTop: 30, transform: `translateY(${line2}px)`, opacity: sp(40) }}>
          बैंक में <span style={{ background: '#FFA3FD', padding: '0 16px', borderRadius: 12 }}>एक रुपया भी</span> पड़ा है?
        </div>

        <div style={{ fontSize: 44, color: '#1A1A2E', fontWeight: 600, marginTop: 26, opacity: line3Op, maxWidth: 1200, textAlign: 'center' }}>
          जो बात न्यूज़ हेडलाइन या प्राइम टाइम में नहीं आती
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_Ruko;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_Baalti',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_Baalti: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // camera slow pan
  const camX = interpolate(frame, [0, durationInFrames], [30, -30]);

  // water rising level (0 = empty at bottom)
  const waterLevel = interpolate(sp(30, { stiffness: 40 }), [0, 1], [0, 150]);
  const waveShift = Math.sin(frame * 0.15) * 8;

  const bucketIn = sp(6);
  const titleIn = sp(15);
  const labelIn = sp(60);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #C9CEFB 0%, #B7BDF7 50%, #949CEA 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 3);
          const breathe = 1 + Math.sin(frame * 0.04 + i) * 0.07;
          return (
            <div key={i} style={{
              position: 'absolute', left: `${5 + s * 70}%`, top: `${seededRandom(i + 12) * 60}%`,
              width: 420 + s * 260, height: 420 + s * 260, borderRadius: i % 2 ? '24%' : '50%',
              background: '#FFA3FD', opacity: 0.06, transform: `rotate(${frame * 0.1 + i * 30}deg) scale(${breathe})`,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture — concentric circles */}
      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', opacity: 0.05 }}>
        {[...Array(6)].map((_, i) => (
          <div key={i} style={{ position: 'absolute', width: 300 + i * 220, height: 300 + i * 220, border: '2px solid #000', borderRadius: '50%', transform: `scale(${1 + Math.sin(frame * 0.03 + i) * 0.03})` }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(24)].map((_, i) => {
        const s = seededRandom(i + 70);
        const y = (s * 1080 - frame * (0.4 + s) * 2) % 1080;
        return (
          <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${(y + 1080) % 1080}px`, width: 5 + s * 5, height: 5 + s * 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.16 }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px)`, display: 'flex', flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 90 }}>
        {/* Bucket = Bank SVG with rising water */}
        <div style={{ transform: `scale(${bucketIn})` }}>
          <svg width="420" height="460" viewBox="0 0 300 340">
            {/* Deposits water arrows falling in */}
            {[...Array(5)].map((_, i) => {
              const dy = (frame * 4 + i * 30) % 120;
              return <rect key={i} x={90 + i * 30} y={dy - 20} width="6" height="26" rx="3" fill="#FFA3FD" opacity={0.8 - dy / 160} />;
            })}
            {/* bucket body clip */}
            <defs>
              <clipPath id="bucket2">
                <path d="M60 110 L240 110 L215 320 L85 320 Z" />
              </clipPath>
            </defs>
            {/* water */}
            <g clipPath="url(#bucket2)">
              <rect x="60" y={320 - waterLevel} width="180" height="260" fill="#8E7BE8" opacity="0.85" />
              <path d={`M60 ${320 - waterLevel} Q150 ${320 - waterLevel + waveShift} 240 ${320 - waterLevel} L240 340 L60 340 Z`} fill="#FFA3FD" opacity="0.55" />
            </g>
            {/* bucket outline */}
            <path d="M60 110 L240 110 L215 320 L85 320 Z" fill="none" stroke="#000" strokeWidth="8" strokeLinejoin="round" />
            <rect x="50" y="96" width="200" height="22" rx="8" fill="#000" />
            <text x="150" y="220" textAnchor="middle" fontFamily="Yantra One, sans-serif" fontSize="30" fontWeight="700" fill="#000">BANK</text>
          </svg>
        </div>

        {/* Typography column */}
        <div style={{ maxWidth: 720 }}>
          <div style={{ fontSize: 44, color: '#1A1A2E', fontWeight: 600, opacity: sp(0), transform: `translateY(${interpolate(sp(0),[0,1],[-30,0])}px)` }}>
            फिर भी तुम्हारे पैसे से सीधी जुड़ी है
          </div>
          <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 130, lineHeight: 1, color: '#000', fontWeight: 700, marginTop: 20, transform: `scale(${titleIn})`, transformOrigin: 'left' }}>
            बैंक = <span style={{ color: '#000', background: '#FFA3FD', padding: '0 14px', borderRadius: 14 }}>बाल्टी</span>
          </div>
          <div style={{ fontSize: 56, color: '#000', fontWeight: 700, marginTop: 30, opacity: labelIn, transform: `translateX(${interpolate(labelIn,[0,1],[-40,0])}px)` }}>
            ↓ ऊपर से पानी = <span style={{ color: '#7C5BE0' }}>Deposits</span>
          </div>
          <div style={{ fontSize: 42, color: '#1A1A2E', fontWeight: 600, marginTop: 14, opacity: sp(80) }}>
            यानी हमारी और तुम्हारी जमापूंजी
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_Baalti;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_Nal',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_Nal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // camera tilt-in zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08], { extrapolateRight: 'clamp' });

  // draining water level dropping
  const drain = interpolate(sp(35, { stiffness: 35 }), [0, 1], [170, 60]);
  const warnFlash = frame > 90 ? (Math.sin(frame * 0.4) * 0.5 + 0.5) : 0;

  const bucketIn = sp(6);
  const dikkatIn = sp(85);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 60%, #C9CEFB 0%, #B7BDF7 50%, #8E96E6 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(4)].map((_, i) => {
          const s = seededRandom(i + 6);
          const breathe = 1 + Math.sin(frame * 0.035 + i) * 0.06;
          return (
            <div key={i} style={{ position: 'absolute', left: `${s * 82}%`, top: `${seededRandom(i + 20) * 70}%`, width: 300 + s * 300, height: 300 + s * 300, borderRadius: i % 2 ? '50%' : '20%', border: '3px solid #FFA3FD', opacity: 0.07, transform: `rotate(${frame * 0.12 + i * 25}deg) scale(${breathe})` }} />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture — diagonal lines */}
      <AbsoluteFill style={{ opacity: 0.05, transform: 'rotate(-12deg) scale(1.4)' }}>
        {[...Array(20)].map((_, i) => (
          <div key={i} style={{ position: 'absolute', top: `${(i / 20) * 100}%`, left: 0, right: 0, height: 2, background: '#000' }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(22)].map((_, i) => {
        const s = seededRandom(i + 90);
        const y = (s * 1080 + frame * (0.5 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${y}px`, width: 4 + s * 6, height: 4 + s * 6, borderRadius: '50%', background: i % 3 ? '#000' : '#FFA3FD', opacity: 0.16 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 90 }}>
        {/* Bucket with tap draining fast */}
        <div style={{ transform: `scale(${bucketIn})` }}>
          <svg width="440" height="520" viewBox="0 0 300 380">
            {/* small deposit in (slow) */}
            {[...Array(2)].map((_, i) => {
              const dy = (frame * 2 + i * 40) % 90;
              return <rect key={i} x={120 + i * 30} y={dy} width="5" height="20" rx="2" fill="#7C5BE0" opacity={0.7 - dy / 130} />;
            })}
            <defs>
              <clipPath id="bucket3"><path d="M60 100 L240 100 L215 300 L85 300 Z" /></clipPath>
            </defs>
            <g clipPath="url(#bucket3)">
              <rect x="60" y={300 - drain} width="180" height="240" fill="#8E7BE8" opacity="0.85" />
            </g>
            <path d="M60 100 L240 100 L215 300 L85 300 Z" fill="none" stroke="#000" strokeWidth="8" strokeLinejoin="round" />
            <rect x="50" y="86" width="200" height="22" rx="8" fill="#000" />
            {/* tap at bottom */}
            <rect x="140" y="300" width="26" height="30" fill="#000" />
            <rect x="120" y="326" width="70" height="18" rx="6" fill="#000" />
            <circle cx="150" cy="300" r="10" fill="#FFA3FD" stroke="#000" strokeWidth="4" />
            {/* fast draining stream */}
            {[...Array(6)].map((_, i) => {
              const dy = (frame * 8 + i * 22) % 130;
              return <rect key={i} x={148} y={344 + dy} width="8" height="22" rx="4" fill="#FFA3FD" opacity={1 - dy / 150} />;
            })}
          </svg>
        </div>

        {/* Typography */}
        <div style={{ maxWidth: 720 }}>
          <div style={{ fontSize: 56, color: '#000', fontWeight: 700, opacity: sp(4) }}>
            ↓ नल से निकलता पानी = <span style={{ color: '#7C5BE0' }}>Loans</span>
          </div>
          <div style={{
            fontFamily: 'Yantra One, sans-serif', fontSize: 116, lineHeight: 1.02, color: '#000', fontWeight: 700, marginTop: 30,
            transform: `scale(${dikkatIn})`, transformOrigin: 'left',
          }}>
            असली दिक्कत
          </div>
          <div style={{ fontSize: 50, color: '#1A1A2E', fontWeight: 700, marginTop: 22, opacity: sp(100) }}>
            नल <span style={{ background: `rgba(255,163,253,${0.4 + warnFlash * 0.6})`, padding: '0 12px', borderRadius: 10 }}>ऊपर से आते पानी से</span> कहीं ज़्यादा तेज़ चल रहा है
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 340px rgba(0,0,0,0.3)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_Nal;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_Gap',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_Gap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.0], { extrapolateRight: 'clamp' });

  // two diverging line charts
  const prog = interpolate(sp(30, { stiffness: 30 }), [0, 1], [0, 1]);
  const W = 760, H = 360;
  const creditPts = [...Array(9)].map((_, i) => {
    const x = (i / 8) * W;
    const y = H - (i / 8) * H * 0.92 * prog;
    return `${x},${y}`;
  }).join(' ');
  const depositPts = [...Array(9)].map((_, i) => {
    const x = (i / 8) * W;
    const y = H - (i / 8) * H * 0.55 * prog;
    return `${x},${y}`;
  }).join(' ');

  // number counter to 5.4
  const num = (interpolate(sp(70), [0, 1], [0, 5.4])).toFixed(1);
  const numPop = sp(70, { stiffness: 160 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #C9CEFB 0%, #B7BDF7 55%, #9098E8 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 8);
          const breathe = 1 + Math.sin(frame * 0.04 + i) * 0.06;
          return <div key={i} style={{ position: 'absolute', left: `${s * 78}%`, top: `${seededRandom(i + 30) * 65}%`, width: 400 + s * 250, height: 400 + s * 250, borderRadius: '30%', background: '#FFA3FD', opacity: 0.05, transform: `rotate(${frame * 0.1 + i * 40}deg) scale(${breathe})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — dot grid */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {[...Array(10)].map((_, r) => [...Array(18)].map((_, c) => (
          <div key={`${r}-${c}`} style={{ position: 'absolute', left: `${(c / 18) * 100}%`, top: `${(r / 10) * 100}%`, width: 4, height: 4, borderRadius: '50%', background: '#000' }} />
        )))}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(20)].map((_, i) => {
        const s = seededRandom(i + 110);
        const y = (s * 1080 - frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${(y + 1080) % 1080}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 76, color: '#000', fontWeight: 700, marginBottom: 24, opacity: sp(4) }}>
          कर्ज़ बनाम जमा की रफ़्तार
        </div>

        {/* Diverging line chart */}
        <svg width={W + 120} height={H + 60} viewBox={`-60 -20 ${W + 120} ${H + 60}`}>
          {/* axis */}
          <line x1="0" y1={H} x2={W} y2={H} stroke="#000" strokeWidth="3" />
          <line x1="0" y1="0" x2="0" y2={H} stroke="#000" strokeWidth="3" />
          {/* deposit line */}
          <polyline points={depositPts} fill="none" stroke="#000" strokeWidth="7" strokeLinecap="round" strokeLinejoin="round" />
          {/* credit line */}
          <polyline points={creditPts} fill="none" stroke="#FFA3FD" strokeWidth="9" strokeLinecap="round" strokeLinejoin="round" />
          {/* gap bracket */}
          {prog > 0.9 && (
            <>
              <line x1={W} y1={H - H * 0.92 * prog} x2={W} y2={H - H * 0.55 * prog} stroke="#7C5BE0" strokeWidth="5" strokeDasharray="10 8" />
              <text x={W - 8} y={H - H * 0.73 * prog} textAnchor="end" fontFamily="JetBrains Mono, monospace" fontSize="34" fontWeight="700" fill="#7C5BE0">GAP</text>
            </>
          )}
          {/* labels */}
          <circle cx={W} cy={H - H * 0.92 * prog} r="9" fill="#FFA3FD" stroke="#000" strokeWidth="3" />
          <circle cx={W} cy={H - H * 0.55 * prog} r="9" fill="#fff" stroke="#000" strokeWidth="3" />
        </svg>

        <div style={{ display: 'flex', gap: 60, marginTop: 20, fontSize: 34, fontWeight: 700 }}>
          <span style={{ color: '#000' }}>▬ <span style={{ color: '#7C5BE0' }}>Deposits</span> (धीमी)</span>
          <span style={{ color: '#000' }}>▬ <span style={{ background: '#FFA3FD', padding: '0 10px', borderRadius: 8 }}>Credit</span> (तेज़)</span>
        </div>

        {/* Big number payoff */}
        <div style={{ marginTop: 30, transform: `scale(${numPop})`, display: 'flex', alignItems: 'baseline', gap: 16 }}>
          <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 160, fontWeight: 700, color: '#000', textShadow: '0 6px 0 #FFA3FD' }}>{num}%</span>
          <span style={{ fontSize: 40, fontWeight: 700, color: '#1A1A2E' }}>फासला · जून 2026</span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_Gap;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_Tanav',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_Tanav: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.07], { extrapolateRight: 'clamp' });

  // tension pulse waves
  const pulse = Math.sin(frame * 0.25);
  // pipeline dot boardroom -> pocket
  const flow = interpolate(sp(50, { stiffness: 25 }), [0, 1], [0, 1]);

  const nameIn = sp(95);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, #C9CEFB 0%, #B7BDF7 45%, #8A92E4 100%)' }} />

      {/* tension ripple rings */}
      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        {[...Array(3)].map((_, i) => {
          const p = ((frame * 0.02 + i / 3) % 1);
          return <div key={i} style={{ position: 'absolute', width: 200 + p * 1400, height: 200 + p * 1400, border: '3px solid #FFA3FD', borderRadius: '50%', opacity: (1 - p) * 0.25 }} />;
        })}
      </AbsoluteFill>

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 11);
          return <div key={i} style={{ position: 'absolute', left: `${s * 80}%`, top: `${seededRandom(i + 40) * 65}%`, width: 360 + s * 260, height: 360 + s * 260, borderRadius: i % 2 ? '50%' : '22%', border: '3px solid #000', opacity: 0.05, transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — grid */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(14)].map((_, i) => <div key={`v${i}`} style={{ position: 'absolute', left: `${(i / 14) * 100}%`, top: 0, bottom: 0, width: 1, background: '#000' }} />)}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(18)].map((_, i) => {
        const s = seededRandom(i + 130);
        const y = (s * 1080 + frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${y}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 130, lineHeight: 1, color: '#000', fontWeight: 700, opacity: sp(6), transform: `translateY(${interpolate(sp(6),[0,1],[-40,0])}px) scale(${1 + pulse * 0.01})`, textShadow: '0 6px 0 #FFA3FD' }}>
          अनकहा तनाव
        </div>

        {/* Boardroom -> pocket pipeline */}
        <svg width="900" height="180" viewBox="0 0 900 180" style={{ marginTop: 40 }}>
          {/* boardroom icon */}
          <rect x="20" y="50" width="150" height="90" rx="10" fill="#fff" stroke="#000" strokeWidth="5" />
          <rect x="45" y="80" width="100" height="10" rx="4" fill="#000" />
          <circle cx="60" cy="70" r="8" fill="#000" /><circle cx="95" cy="70" r="8" fill="#000" /><circle cx="130" cy="70" r="8" fill="#000" />
          <text x="95" y="165" textAnchor="middle" fontSize="26" fontWeight="700" fill="#000" fontFamily="Noto Sans Devanagari, sans-serif">बोर्डरूम</text>
          {/* pipe */}
          <line x1="180" y1="95" x2="720" y2="95" stroke="#000" strokeWidth="6" strokeDasharray="14 10" />
          {/* moving dot */}
          <circle cx={180 + flow * 540} cy="95" r="14" fill="#FFA3FD" stroke="#000" strokeWidth="4" />
          {/* pocket / wallet */}
          <rect x="730" y="55" width="150" height="90" rx="12" fill="#FFA3FD" stroke="#000" strokeWidth="5" />
          <rect x="800" y="85" width="55" height="30" rx="6" fill="#fff" stroke="#000" strokeWidth="4" />
          <text x="805" y="165" textAnchor="middle" fontSize="26" fontWeight="700" fill="#000" fontFamily="Noto Sans Devanagari, sans-serif">तुम्हारी जेब</text>
        </svg>

        {/* Kunal nameplate */}
        <div style={{ marginTop: 44, opacity: nameIn, transform: `translateY(${interpolate(nameIn,[0,1],[30,0])}px)`, background: '#000', padding: '16px 40px', borderRadius: 16, display: 'flex', alignItems: 'center', gap: 20 }}>
          <span style={{ fontSize: 40, fontWeight: 700, color: '#fff' }}>मैं कुणाल हूँ</span>
          <span style={{ fontSize: 28, fontWeight: 600, color: '#FFA3FD', fontFamily: 'Inter, sans-serif' }}>· ProBuffet HQ</span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 340px rgba(0,0,0,0.3)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_Tanav;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_Rasoi',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_Rasoi: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camX = interpolate(frame, [0, durationInFrames], [-25, 25]);

  const arrowDash = -(frame * 3) % 24;
  const q1 = sp(90);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'linear-gradient(200deg, #C9CEFB 0%, #B7BDF7 50%, #949CEA 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {[...Array(4)].map((_, i) => {
          const s = seededRandom(i + 14);
          return <div key={i} style={{ position: 'absolute', left: `${s * 80}%`, top: `${seededRandom(i + 50) * 70}%`, width: 320 + s * 260, height: 320 + s * 260, borderRadius: i % 2 ? '50%' : '20%', background: '#FFA3FD', opacity: 0.05, transform: `rotate(${frame * 0.09 + i * 30}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — concentric */}
      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', opacity: 0.05 }}>
        {[...Array(5)].map((_, i) => <div key={i} style={{ position: 'absolute', width: 400 + i * 240, height: 400 + i * 240, border: '2px solid #000', borderRadius: '50%' }} />)}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(20)].map((_, i) => {
        const s = seededRandom(i + 150);
        const y = (s * 1080 - frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${(y + 1080) % 1080}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px)`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        {/* Locked room -> kitchen table */}
        <svg width="1000" height="300" viewBox="0 0 1000 300">
          {/* locked room */}
          <g transform={`scale(${sp(6)})`} style={{ transformOrigin: '150px 150px' }}>
            <rect x="60" y="70" width="200" height="160" rx="12" fill="#2D2D3D" stroke="#000" strokeWidth="6" />
            <circle cx="160" cy="150" r="26" fill="none" stroke="#FFA3FD" strokeWidth="7" />
            <rect x="150" y="150" width="20" height="34" rx="4" fill="#FFA3FD" />
            <text x="160" y="270" textAnchor="middle" fontSize="30" fontWeight="700" fill="#000" fontFamily="Noto Sans Devanagari, sans-serif">बंद कमरा</text>
          </g>
          {/* arrow */}
          <line x1="290" y1="150" x2="700" y2="150" stroke="#000" strokeWidth="7" strokeDasharray="12 12" strokeDashoffset={arrowDash} />
          <path d="M700 150 l-26 -16 l0 32 z" fill="#000" transform="translate(20,0)" />
          {/* kitchen table */}
          <g transform={`scale(${sp(45)})`} style={{ transformOrigin: '850px 150px' }}>
            <rect x="740" y="120" width="220" height="26" rx="8" fill="#FFA3FD" stroke="#000" strokeWidth="5" />
            <rect x="760" y="146" width="16" height="80" fill="#000" />
            <rect x="924" y="146" width="16" height="80" fill="#000" />
            <circle cx="820" cy="100" r="18" fill="#fff" stroke="#000" strokeWidth="5" />
            <circle cx="890" cy="100" r="18" fill="#fff" stroke="#000" strokeWidth="5" />
            <text x="850" y="270" textAnchor="middle" fontSize="30" fontWeight="700" fill="#000" fontFamily="Noto Sans Devanagari, sans-serif">रसोई की मेज़</text>
          </g>
        </svg>

        <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 84, color: '#000', fontWeight: 700, marginTop: 40, opacity: sp(60), transform: `translateY(${interpolate(sp(60),[0,1],[30,0])}px)` }}>
          फैसला <span style={{ background: '#FFA3FD', padding: '0 14px', borderRadius: 12 }}>तुम खुद लो</span>
        </div>

        <div style={{ fontSize: 46, fontWeight: 700, color: '#1A1A2E', marginTop: 26, opacity: q1 }}>
          सवाल: बैंक का <span style={{ color: '#7C5BE0' }}>असली धंधा</span> क्या है?
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_Rasoi;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_Dukaan',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_Dukaan: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1.0], { extrapolateRight: 'clamp' });

  const inRate = sp(45);
  const outRate = sp(70);
  const spreadPop = sp(100, { stiffness: 160 });
  const flowIn = -(frame * 4) % 20;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 30%, #C9CEFB 0%, #B7BDF7 50%, #8E96E6 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 17);
          return <div key={i} style={{ position: 'absolute', left: `${s * 78}%`, top: `${seededRandom(i + 60) * 65}%`, width: 380 + s * 240, height: 380 + s * 240, borderRadius: '26%', border: '3px solid #FFA3FD', opacity: 0.06, transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — dot grid */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(8)].map((_, r) => [...Array(16)].map((_, c) => (
          <div key={`${r}-${c}`} style={{ position: 'absolute', left: `${(c / 16) * 100}%`, top: `${(r / 8) * 100}%`, width: 4, height: 4, borderRadius: '50%', background: '#000' }} />
        )))}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(18)].map((_, i) => {
        const s = seededRandom(i + 170);
        const y = (s * 1080 - frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${(y + 1080) % 1080}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 96, color: '#000', fontWeight: 700, opacity: sp(6), textShadow: '0 5px 0 #FFA3FD' }}>
          बैंक = <span style={{ background: '#FFA3FD', padding: '0 14px', borderRadius: 12 }}>दुकान</span>, माल = पैसा
        </div>

        {/* Money flow diagram: in-rate -> bank -> out-rate */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 40, marginTop: 50 }}>
          {/* IN */}
          <div style={{ opacity: inRate, transform: `translateX(${interpolate(inRate,[0,1],[-40,0])}px)`, textAlign: 'center' }}>
            <div style={{ fontSize: 30, fontWeight: 700, color: '#1A1A2E' }}>Savings पर देता</div>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 72, fontWeight: 700, color: '#7C5BE0' }}>2.7-3.5%</div>
          </div>

          <svg width="120" height="60"><line x1="0" y1="30" x2="100" y2="30" stroke="#000" strokeWidth="6" strokeDasharray="10 8" strokeDashoffset={flowIn} /><path d="M100 30 l-22 -13 l0 26 z" fill="#000" transform="translate(18,0)" /></svg>

          {/* BANK box */}
          <div style={{ background: '#000', color: '#fff', borderRadius: 16, padding: '30px 34px', fontFamily: 'Yantra One, sans-serif', fontSize: 48, fontWeight: 700 }}>बैंक</div>

          <svg width="120" height="60"><line x1="0" y1="30" x2="100" y2="30" stroke="#000" strokeWidth="6" strokeDasharray="10 8" strokeDashoffset={flowIn} /><path d="M100 30 l-22 -13 l0 26 z" fill="#000" transform="translate(18,0)" /></svg>

          {/* OUT */}
          <div style={{ opacity: outRate, transform: `translateX(${interpolate(outRate,[0,1],[40,0])}px)`, textAlign: 'center' }}>
            <div style={{ fontSize: 30, fontWeight: 700, color: '#1A1A2E' }}>Loan पर लेता</div>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 60, fontWeight: 700, color: '#000' }}>8.5%</div>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 44, fontWeight: 700, color: '#000', background: '#FFA3FD', borderRadius: 8, marginTop: 6 }}>12-14%</div>
          </div>
        </div>

        {/* Spread payoff */}
        <div style={{ marginTop: 44, transform: `scale(${spreadPop})`, fontSize: 54, fontWeight: 700, color: '#000' }}>
          बीच का <span style={{ background: '#FFA3FD', padding: '0 14px', borderRadius: 10 }}>फर्क</span> = बैंक की असली कमाई
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_Dukaan;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_CDRatio',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_CDRatio: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.0], { extrapolateRight: 'clamp' });

  const formIn = sp(50);
  const numIn = sp(35);
  const denIn = sp(20);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'linear-gradient(150deg, #C9CEFB 0%, #B7BDF7 55%, #9098E8 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 19);
          return <div key={i} style={{ position: 'absolute', left: `${s * 80}%`, top: `${seededRandom(i + 70) * 65}%`, width: 400 + s * 240, height: 400 + s * 240, borderRadius: i % 2 ? '50%' : '24%', background: '#FFA3FD', opacity: 0.05, transform: `rotate(${frame * 0.09 + i * 30}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — vertical lines */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(16)].map((_, i) => <div key={i} style={{ position: 'absolute', left: `${(i / 16) * 100}%`, top: 0, bottom: 0, width: 1, background: '#000' }} />)}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(18)].map((_, i) => {
        const s = seededRandom(i + 190);
        const y = (s * 1080 + frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${y}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ fontFamily: 'Yantra One, sans-serif', fontSize: 84, color: '#000', fontWeight: 700, opacity: sp(4), marginBottom: 40 }}>
          Credit-to-Deposit Ratio
        </div>

        {/* Formula visual: Credit / Deposit */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <div style={{ transform: `translateY(${interpolate(numIn,[0,1],[-40,0])}px) scale(${numIn})`, background: '#FFA3FD', color: '#000', padding: '22px 60px', borderRadius: 18, fontFamily: 'Yantra One, sans-serif', fontSize: 76, fontWeight: 700, border: '5px solid #000' }}>
            कर्ज़ (Credit)
          </div>
          <div style={{ width: 560, height: 10, background: '#000', borderRadius: 6, margin: '22px 0', transform: `scaleX(${sp(30)})` }} />
          <div style={{ transform: `translateY(${interpolate(denIn,[0,1],[40,0])}px) scale(${denIn})`, background: '#fff', color: '#000', padding: '22px 60px', borderRadius: 18, fontFamily: 'Yantra One, sans-serif', fontSize: 76, fontWeight: 700, border: '5px solid #000' }}>
            जमा (Deposit)
          </div>
        </div>

        <div style={{ fontSize: 46, fontWeight: 700, color: '#1A1A2E', marginTop: 48, opacity: formIn, textAlign: 'center', maxWidth: 1200 }}>
          यानी जमा का <span style={{ color: '#7C5BE0' }}>कितना हिस्सा</span> पहले से कर्ज़ में लगा हुआ है
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.26)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_CDRatio;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_82Percent',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_82Percent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.06], { extrapolateRight: 'clamp' });

  // gauge arc to 82.5%
  const gaugeProg = interpolate(sp(30, { stiffness: 30 }), [0, 1], [0, 0.825]);
  const R = 180, C = Math.PI * R; // semicircle circumference
  const dash = C * gaugeProg;

  const numVal = (interpolate(sp(30), [0, 1], [0, 82.5])).toFixed(1);
  const numPop = sp(35, { stiffness: 150 });

  // 100 coins grid, 82 filled
  const coinReveal = Math.floor(interpolate(sp(60, { stiffness: 25 }), [0, 1], [0, 82]));

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 45%, #C9CEFB 0%, #B7BDF7 48%, #8A92E4 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(3)].map((_, i) => {
          const s = seededRandom(i + 21);
          return <div key={i} style={{ position: 'absolute', left: `${s * 78}%`, top: `${seededRandom(i + 80) * 65}%`, width: 380 + s * 240, height: 380 + s * 240, borderRadius: '50%', border: '3px solid #000', opacity: 0.05, transform: `rotate(${frame * 0.09}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — dot grid */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(8)].map((_, r) => [...Array(14)].map((_, c) => (
          <div key={`${r}-${c}`} style={{ position: 'absolute', left: `${(c / 14) * 100}%`, top: `${(r / 8) * 100}%`, width: 4, height: 4, borderRadius: '50%', background: '#000' }} />
        )))}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(16)].map((_, i) => {
        const s = seededRandom(i + 210);
        const y = (s * 1080 - frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${(y + 1080) % 1080}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 90 }}>
        {/* Gauge + big number */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <svg width="440" height="260" viewBox="0 0 440 260">
            <path d="M40 220 A180 180 0 0 1 400 220" fill="none" stroke="#000" strokeWidth="26" opacity="0.15" strokeLinecap="round" />
            <path d="M40 220 A180 180 0 0 1 400 220" fill="none" stroke="#FFA3FD" strokeWidth="26" strokeLinecap="round" strokeDasharray={`${dash} ${C}`} />
          </svg>
          <div style={{ transform: `scale(${numPop})`, marginTop: -70 }}>
            <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 150, fontWeight: 700, color: '#000', textShadow: '0 6px 0 #FFA3FD' }}>{numVal}%</span>
          </div>
          <div style={{ fontSize: 40, fontWeight: 700, color: '#1A1A2E', marginTop: 10, opacity: sp(45) }}>C-D Ratio अब</div>
        </div>

        {/* 100 coins, 82 filled */}
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(10, 1fr)', gap: 8, opacity: sp(55) }}>
          {[...Array(100)].map((_, i) => {
            const filled = i < coinReveal;
            return <div key={i} style={{ width: 32, height: 32, borderRadius: '50%', background: filled ? '#FFA3FD' : '#fff', border: '3px solid #000', transition: 'none', transform: filled ? 'scale(1)' : 'scale(0.8)' }} />;
          })}
        </div>
      </AbsoluteFill>

      {/* Bottom line */}
      <div style={{ position: 'absolute', bottom: 100, left: 0, right: 0, textAlign: 'center', fontSize: 46, fontWeight: 700, color: '#000', opacity: sp(90) }}>
        हर <span style={{ background: '#FFA3FD', padding: '0 10px', borderRadius: 8 }}>100 में से 82 रुपये</span> पहले से किसी के कर्ज़ में
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.28)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_82Percent;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_BhareKamre',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_BhareKamre: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.09], { extrapolateRight: 'clamp' });

  // room filling up (space shrinking)
  const fill = interpolate(sp(30, { stiffness: 30 }), [0, 1], [0, 0.82]);
  // breathing squeeze
  const squeeze = 1 + Math.sin(frame * 0.2) * 0.02;

  // cliffhanger reveal + screen shake on impact
  const modIn = sp(95, { stiffness: 150 });
  const impactShake = frame > 95 && frame < 108 ? Math.sin(frame * 3) * interpolate(frame, [95, 108], [10, 0]) : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B7BDF7', overflow: 'hidden', opacity: exitOp, fontFamily: 'Noto Sans Devanagari, Inter, sans-serif' }}>
      {/* Gradient background layer */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 55%, #C9CEFB 0%, #B7BDF7 45%, #868EE2 100%)' }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {[...Array(4)].map((_, i) => {
          const s = seededRandom(i + 23);
          return <div key={i} style={{ position: 'absolute', left: `${s * 82}%`, top: `${seededRandom(i + 90) * 70}%`, width: 300 + s * 280, height: 300 + s * 280, borderRadius: i % 2 ? '50%' : '20%', border: '3px solid #FFA3FD', opacity: 0.06, transform: `rotate(${frame * 0.11}deg) scale(${1 + Math.sin(frame * 0.04 + i) * 0.05})` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — grid closing in */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[...Array(12)].map((_, i) => <div key={`h${i}`} style={{ position: 'absolute', top: `${(i / 12) * 100}%`, left: 0, right: 0, height: 1, background: '#000' }} />)}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(16)].map((_, i) => {
        const s = seededRandom(i + 230);
        const y = (s * 1080 + frame * (0.4 + s) * 2) % 1080;
        return <div key={i} style={{ position: 'absolute', left: `${s * 100}%`, top: `${y}px`, width: 5, height: 5, borderRadius: '50%', background: i % 2 ? '#FFA3FD' : '#000', opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${impactShake}px, ${impactShake * 0.5}px)`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ fontSize: 50, fontWeight: 700, color: '#1A1A2E', opacity: sp(4) }}>
          आम कारोबार है? हाँ — पर सिर्फ़ <span style={{ background: '#FFA3FD', padding: '0 12px', borderRadius: 10 }}>हद तक</span>
        </div>

        {/* Room filling — less room to breathe */}
        <div style={{ marginTop: 40, width: 640, height: 320, border: '7px solid #000', borderRadius: 16, position: 'relative', overflow: 'hidden', background: '#fff', transform: `scale(${squeeze})` }}>
          {/* filling mass */}
          <div style={{ position: 'absolute', left: 0, right: 0, bottom: 0, height: `${fill * 100}%`, background: 'linear-gradient(180deg, #FFA3FD 0%, #E56FD8 100%)' }} />
          {/* remaining air label */}
          <div style={{ position: 'absolute', top: 20, left: 0, right: 0, textAlign: 'center', fontSize: 34, fontWeight: 700, color: '#000' }}>
            हिलने की जगह ↓
          </div>
          <div style={{ position: 'absolute', bottom: 16, left: 0, right: 0, textAlign: 'center', fontSize: 30, fontWeight: 700, color: '#000', fontFamily: 'Noto Sans Devanagari, sans-serif' }}>
            भरा हुआ कमरा
          </div>
        </div>
        <div style={{ fontSize: 40, fontWeight: 700, color: '#000', marginTop: 22, opacity: sp(55) }}>
          जैसे भरे कमरे में सांस लेने की जगह कम
        </div>

        {/* Cliffhanger */}
        <div style={{ marginTop: 40, transform: `scale(${modIn})`, fontFamily: 'Yantra One, sans-serif', fontSize: 92, fontWeight: 700, color: '#000', textShadow: '0 6px 0 #FFA3FD' }}>
          और असली मोड़ अब आता है
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 340px rgba(0,0,0,0.32)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#000', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_BhareKamre;
```

