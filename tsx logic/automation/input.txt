```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_TheQuestion',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_TheQuestion: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08]);
  const camX = Math.sin(frame * 0.008) * 8;

  // Question mark giant entrance
  const qScale = sp(8, { damping: 9, stiffness: 90 });
  const qFloat = Math.sin(frame * 0.04) * 12;
  const qRotate = Math.sin(frame * 0.025) * 4;

  // Headline "भाई कभी सोचा है?"
  const word1 = sp(20);
  const word2 = sp(32);
  const word3 = sp(44);

  // Sub line "एक आदमी..."
  const subOp = interpolate(frame, [70, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const subY = interpolate(frame, [70, 95], [30, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Three icon reveal: no खेत, no ट्रैक्टर, no अनाज
  const icon1 = sp(140);
  const icon2 = sp(170);
  const icon3 = sp(200);

  // Cross marks on icons
  const cross1 = interpolate(frame, [155, 175], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const cross2 = interpolate(frame, [185, 205], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const cross3 = interpolate(frame, [215, 235], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Pulse on question
  const pulse = 1 + Math.sin(frame * 0.08) * 0.025;

  // Shake on payoff
  const shakeFrame = 250;
  const shake = frame > shakeFrame && frame < shakeFrame + 12
    ? Math.sin((frame - shakeFrame) * 3) * 4
    : 0;

  // BG color #76ABAE | TEXT #E8EDF2 | ACCENT #FAB95B
  const BG = '#76ABAE';
  const TEXT = '#E8EDF2';
  const ACCENT = '#FAB95B';
  const DEEP = '#3D6B6E';
  const SOFT = '#A8C9CC';

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${50 + Math.sin(frame * 0.01) * 10}% ${50 + Math.cos(frame * 0.012) * 10}%, #8FBFC2 0%, #76ABAE 45%, #5C8E91 100%)`,
      }} />

      {/* Animated grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="gridP1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke={TEXT} strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#gridP1)" transform={`translate(${(frame * 0.3) % 80} ${(frame * 0.2) % 80})`} />
      </svg>

      {/* Shape layers — drifting transparent geometry */}
      <div style={{
        position: 'absolute',
        left: 150 + Math.sin(frame * 0.01) * 30,
        top: 120 + Math.cos(frame * 0.012) * 20,
        width: 380,
        height: 380,
        borderRadius: '50%',
        border: `2px solid ${TEXT}`,
        opacity: 0.06,
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        right: 100 + Math.cos(frame * 0.011) * 25,
        bottom: 150 + Math.sin(frame * 0.013) * 20,
        width: 460,
        height: 460,
        border: `2px solid ${ACCENT}`,
        opacity: 0.07,
        transform: `rotate(${-frame * 0.12}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        left: '60%',
        top: '20%',
        width: 240,
        height: 240,
        background: ACCENT,
        opacity: 0.05,
        transform: `rotate(45deg) scale(${1 + Math.cos(frame * 0.025) * 0.08})`,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const r1 = seededRandom(i + 1);
        const r2 = seededRandom(i + 50);
        const r3 = seededRandom(i + 100);
        const px = (r1 * 1920 + frame * (0.3 + r2 * 0.4)) % 1920;
        const py = (r2 * 1080 + Math.sin((frame + i * 10) * 0.02) * 30) % 1080;
        const size = 3 + r3 * 6;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: size,
            height: size,
            borderRadius: '50%',
            background: i % 3 === 0 ? ACCENT : TEXT,
            opacity: 0.15 + r3 * 0.2,
          }} />
        );
      })}

      {/* Camera wrapper */}
      <div style={{
        position: 'absolute',
        inset: 0,
        transform: `scale(${camScale}) translate(${camX + shake}px, ${shake}px)`,
        transformOrigin: '50% 50%',
      }}>
        {/* GIANT question mark on right */}
        <div style={{
          position: 'absolute',
          right: 120,
          top: 80,
          fontSize: 680,
          fontFamily: 'Yantra One, serif',
          fontWeight: 900,
          color: ACCENT,
          opacity: 0.18 * qScale,
          transform: `translateY(${qFloat}px) rotate(${qRotate}deg) scale(${qScale * pulse})`,
          textShadow: `0 0 60px ${ACCENT}`,
          lineHeight: 0.9,
        }}>?</div>

        {/* Main headline block */}
        <div style={{
          position: 'absolute',
          left: 120,
          top: 220,
          display: 'flex',
          flexDirection: 'column',
          gap: 12,
        }}>
          {/* Top tag */}
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 14,
            opacity: sp(4),
            transform: `translateX(${interpolate(sp(4), [0, 1], [-30, 0])}px)`,
          }}>
            <div style={{ width: 60, height: 4, background: ACCENT }} />
            <div style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: ACCENT,
              letterSpacing: 4,
            }}>EPISODE 01 — THE LOOPHOLE</div>
          </div>

          {/* Word 1: भाई */}
          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 200,
            color: TEXT,
            lineHeight: 1,
            opacity: word1,
            transform: `translateY(${interpolate(word1, [0, 1], [60, 0])}px) scale(${interpolate(word1, [0, 1], [0.85, 1])})`,
          }}>भाई,</div>

          {/* Word 2 + 3 */}
          <div style={{ display: 'flex', alignItems: 'baseline', gap: 24 }}>
            <div style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 200,
              color: TEXT,
              lineHeight: 1,
              opacity: word2,
              transform: `translateY(${interpolate(word2, [0, 1], [60, 0])}px)`,
            }}>कभी</div>
            <div style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 200,
              color: ACCENT,
              lineHeight: 1,
              opacity: word3,
              transform: `translateY(${interpolate(word3, [0, 1], [60, 0])}px) scale(${pulse})`,
              textShadow: `0 0 30px ${ACCENT}60`,
            }}>सोचा?</div>
          </div>

          {/* Subline */}
          <div style={{
            marginTop: 30,
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 52,
            fontWeight: 600,
            color: TEXT,
            opacity: subOp * 0.9,
            transform: `translateY(${subY}px)`,
            maxWidth: 1100,
            lineHeight: 1.3,
          }}>
            एक आदमी — जिसके पास <span style={{ color: ACCENT, fontWeight: 800 }}>कुछ भी नहीं</span>
          </div>
        </div>

        {/* Three "NO" icon row at bottom */}
        <div style={{
          position: 'absolute',
          bottom: 110,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 80,
          alignItems: 'center',
        }}>
          {/* Icon 1: खेत */}
          <div style={{
            opacity: icon1,
            transform: `scale(${icon1}) translateY(${interpolate(icon1, [0, 1], [40, 0])}px)`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 14,
            position: 'relative',
          }}>
            <div style={{
              width: 180,
              height: 180,
              background: `${DEEP}40`,
              border: `3px solid ${TEXT}`,
              borderRadius: 24,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              position: 'relative',
            }}>
              {/* Farm field SVG */}
              <svg width="110" height="110" viewBox="0 0 100 100">
                <path d="M10 70 L50 50 L90 70 L90 90 L10 90 Z" fill="none" stroke={TEXT} strokeWidth="3" />
                <line x1="10" y1="78" x2="90" y2="78" stroke={TEXT} strokeWidth="2" />
                <line x1="20" y1="65" x2="20" y2="90" stroke={TEXT} strokeWidth="1.5" />
                <line x1="35" y1="58" x2="35" y2="90" stroke={TEXT} strokeWidth="1.5" />
                <line x1="50" y1="52" x2="50" y2="90" stroke={TEXT} strokeWidth="1.5" />
                <line x1="65" y1="58" x2="65" y2="90" stroke={TEXT} strokeWidth="1.5" />
                <line x1="80" y1="65" x2="80" y2="90" stroke={TEXT} strokeWidth="1.5" />
                <circle cx="75" cy="25" r="8" fill={ACCENT} opacity="0.7" />
              </svg>
              {/* Red cross overlay */}
              <svg width="180" height="180" viewBox="0 0 180 180" style={{ position: 'absolute', inset: 0 }}>
                <line x1="30" y1="30" x2="150" y2="150" stroke="#ff4444" strokeWidth={6 * cross1} strokeLinecap="round" />
                <line x1="150" y1="30" x2="30" y2="150" stroke="#ff4444" strokeWidth={6 * cross1} strokeLinecap="round" />
              </svg>
            </div>
            <div style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 44,
              color: TEXT,
            }}>ना खेत</div>
          </div>

          {/* Icon 2: ट्रैक्टर */}
          <div style={{
            opacity: icon2,
            transform: `scale(${icon2}) translateY(${interpolate(icon2, [0, 1], [40, 0])}px)`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 14,
          }}>
            <div style={{
              width: 180,
              height: 180,
              background: `${DEEP}40`,
              border: `3px solid ${TEXT}`,
              borderRadius: 24,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              position: 'relative',
            }}>
              {/* Tractor SVG */}
              <svg width="120" height="120" viewBox="0 0 100 100">
                <rect x="35" y="30" width="35" height="28" fill="none" stroke={TEXT} strokeWidth="3" rx="3" />
                <rect x="40" y="35" width="12" height="14" fill={ACCENT} opacity="0.8" />
                <rect x="20" y="55" width="55" height="15" fill="none" stroke={TEXT} strokeWidth="3" rx="2" />
                <circle cx="32" cy="75" r="10" fill="none" stroke={TEXT} strokeWidth="3" />
                <circle cx="32" cy="75" r="4" fill={TEXT} />
                <circle cx="65" cy="78" r="13" fill="none" stroke={TEXT} strokeWidth="3" />
                <circle cx="65" cy="78" r="5" fill={TEXT} />
                <line x1="70" y1="32" x2="78" y2="22" stroke={TEXT} strokeWidth="3" />
                <rect x="74" y="18" width="10" height="6" fill={TEXT} />
              </svg>
              <svg width="180" height="180" viewBox="0 0 180 180" style={{ position: 'absolute', inset: 0 }}>
                <line x1="30" y1="30" x2="150" y2="150" stroke="#ff4444" strokeWidth={6 * cross2} strokeLinecap="round" />
                <line x1="150" y1="30" x2="30" y2="150" stroke="#ff4444" strokeWidth={6 * cross2} strokeLinecap="round" />
              </svg>
            </div>
            <div style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 44,
              color: TEXT,
            }}>ना ट्रैक्टर</div>
          </div>

          {/* Icon 3: अनाज */}
          <div style={{
            opacity: icon3,
            transform: `scale(${icon3}) translateY(${interpolate(icon3, [0, 1], [40, 0])}px)`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 14,
          }}>
            <div style={{
              width: 180,
              height: 180,
              background: `${DEEP}40`,
              border: `3px solid ${TEXT}`,
              borderRadius: 24,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              position: 'relative',
            }}>
              {/* Grain sack SVG */}
              <svg width="110" height="110" viewBox="0 0 100 100">
                <path d="M30 30 Q30 20 40 20 L60 20 Q70 20 70 30 L75 85 Q75 92 68 92 L32 92 Q25 92 25 85 Z"
                      fill="none" stroke={TEXT} strokeWidth="3" />
                <line x1="38" y1="20" x2="38" y2="15" stroke={TEXT} strokeWidth="2" />
                <line x1="50" y1="20" x2="50" y2="13" stroke={TEXT} strokeWidth="2" />
                <line x1="62" y1="20" x2="62" y2="15" stroke={TEXT} strokeWidth="2" />
                <text x="50" y="65" fontSize="22" fill={ACCENT} textAnchor="middle" fontWeight="bold">₹</text>
              </svg>
              <svg width="180" height="180" viewBox="0 0 180 180" style={{ position: 'absolute', inset: 0 }}>
                <line x1="30" y1="30" x2="150" y2="150" stroke="#ff4444" strokeWidth={6 * cross3} strokeLinecap="round" />
                <line x1="150" y1="30" x2="30" y2="150" stroke="#ff4444" strokeWidth={6 * cross3} strokeLinecap="round" />
              </svg>
            </div>
            <div style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 44,
              color: TEXT,
            }}>ना अनाज</div>
          </div>
        </div>

        {/* Bottom payoff strip */}
        <div style={{
          position: 'absolute',
          bottom: 40,
          left: '50%',
          transform: `translateX(-50%) scale(${interpolate(frame, [240, 260], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })})`,
          opacity: interpolate(frame, [240, 260], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          padding: '14px 38px',
          background: ACCENT,
          borderRadius: 8,
          fontFamily: 'Yantra One, serif',
          fontSize: 38,
          color: '#1C1C1C',
          fontWeight: 900,
          letterSpacing: 1,
        }}>
          फिर भी… <span style={{ color: '#8B0000' }}>TAX ZERO?</span>
        </div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 40%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 1,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene01_TheQuestion;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_ITRPaper',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_ITRPaper: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#76ABAE';
  const TEXT = '#E8EDF2';
  const ACCENT = '#FAB95B';
  const DEEP = '#3D6B6E';

  // Camera pan
  const camY = interpolate(frame, [0, durationInFrames], [-15, 15]);

  // Paper entrance
  const paperScale = sp(10, { damping: 11, stiffness: 100 });
  const paperRot = interpolate(sp(10), [0, 1], [-8, -2]);

  // Stamp slam at frame 180
  const stampFrame = 180;
  const stampScale = frame < stampFrame ? 0 :
    interpolate(frame, [stampFrame, stampFrame + 8, stampFrame + 18], [3, 0.9, 1], {
      extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
    });
  const stampRot = interpolate(frame, [stampFrame, stampFrame + 12], [-25, -12], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Shake on stamp
  const shake = frame > stampFrame && frame < stampFrame + 15
    ? Math.sin((frame - stampFrame) * 4) * 6
    : 0;

  // Typewriter effect on lines
  const line1Chars = Math.floor(interpolate(frame, [30, 90], [0, 28], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const line2Chars = Math.floor(interpolate(frame, [100, 170], [0, 32], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  const line1Full = 'Income Source: AGRICULTURE';
  const line2Full = 'Tax Liability: ₹ 0.00';

  // Cursor blink
  const cursor = Math.floor(frame / 10) % 2 === 0 ? '|' : '';

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: `linear-gradient(135deg, #8FBFC2 0%, #76ABAE 50%, #5C8E91 100%)`,
      }} />

      {/* Diagonal line texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="diagP" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke={TEXT} strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diagP)" />
      </svg>

      {/* Floating shape layers */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: 100,
        width: 320,
        height: 320,
        background: ACCENT,
        opacity: 0.05,
        borderRadius: 40,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        right: 80,
        bottom: 80,
        width: 380,
        height: 380,
        border: `3px solid ${TEXT}`,
        opacity: 0.06,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.08})`,
      }} />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r1 = seededRandom(i + 5);
        const r2 = seededRandom(i + 60);
        const px = (r1 * 1920 + frame * 0.4) % 1920;
        const py = r2 * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px,
            top: py + Math.sin((frame + i * 8) * 0.03) * 20,
            width: 4 + r2 * 4,
            height: 4 + r2 * 4,
            borderRadius: '50%',
            background: TEXT,
            opacity: 0.2,
          }} />
        );
      })}

      <div style={{
        position: 'absolute',
        inset: 0,
        transform: `translateY(${camY + shake}px) translateX(${shake}px)`,
      }}>
        {/* Section title */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(2)})`,
          display: 'flex',
          alignItems: 'center',
          gap: 20,
        }}>
          <div style={{ width: 70, height: 3, background: ACCENT }} />
          <div style={{
            fontFamily: 'Inter',
            fontSize: 30,
            fontWeight: 700,
            color: ACCENT,
            letterSpacing: 5,
          }}>THE CLAIM</div>
          <div style={{ width: 70, height: 3, background: ACCENT }} />
        </div>

        {/* ITR Paper document */}
        <div style={{
          position: 'absolute',
          left: 130,
          top: 200,
          width: 880,
          minHeight: 720,
          background: '#FAFAF5',
          borderRadius: 8,
          padding: 50,
          transform: `scale(${paperScale}) rotate(${paperRot}deg)`,
          boxShadow: '0 30px 80px rgba(0,0,0,0.4), 0 10px 30px rgba(0,0,0,0.3)',
          fontFamily: 'JetBrains Mono, monospace',
        }}>
          {/* Header */}
          <div style={{ borderBottom: '3px solid #1C1C1C', paddingBottom: 18, marginBottom: 30 }}>
            <div style={{ fontSize: 36, fontWeight: 800, color: '#1C1C1C', fontFamily: 'Inter' }}>
              INCOME TAX RETURN
            </div>
            <div style={{ fontSize: 22, color: '#666', marginTop: 6 }}>FORM ITR-2 | AY 2025-26</div>
          </div>

          {/* Field rows */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 26 }}>
            <div>
              <div style={{ fontSize: 20, color: '#888', marginBottom: 6 }}>NAME</div>
              <div style={{ fontSize: 36, color: '#1C1C1C', fontWeight: 600, fontFamily: 'Inter', borderBottom: '1px dashed #999', paddingBottom: 6 }}>
                Mr. [REDACTED]
              </div>
            </div>

            <div>
              <div style={{ fontSize: 20, color: '#888', marginBottom: 6 }}>PAN</div>
              <div style={{ fontSize: 36, color: '#1C1C1C', fontWeight: 600, borderBottom: '1px dashed #999', paddingBottom: 6 }}>
                ABCDE••••F
              </div>
            </div>

            <div style={{ background: '#FFF8E0', padding: 20, borderLeft: `6px solid ${ACCENT}` }}>
              <div style={{ fontSize: 20, color: '#666', marginBottom: 8 }}>INCOME SOURCE</div>
              <div style={{ fontSize: 38, color: '#1C1C1C', fontWeight: 800, fontFamily: 'Inter' }}>
                {line1Full.substring(0, line1Chars)}<span style={{ color: ACCENT }}>{cursor}</span>
              </div>
            </div>

            <div style={{ background: '#FFE8E8', padding: 20, borderLeft: '6px solid #c0392b' }}>
              <div style={{ fontSize: 20, color: '#666', marginBottom: 8 }}>TAX LIABILITY</div>
              <div style={{ fontSize: 52, color: '#c0392b', fontWeight: 900, fontFamily: 'Inter' }}>
                {line2Full.substring(0, line2Chars)}
              </div>
            </div>
          </div>

          {/* Bottom signature line */}
          <div style={{ marginTop: 50, display: 'flex', justifyContent: 'space-between', fontSize: 18, color: '#999' }}>
            <div>Date: 31.07.2025</div>
            <div>SUBMITTED ✓</div>
          </div>
        </div>

        {/* RIGHT side — big TYPOGRAPHY */}
        <div style={{
          position: 'absolute',
          right: 80,
          top: 240,
          width: 740,
        }}>
          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 110,
            color: TEXT,
            lineHeight: 1.05,
            opacity: sp(50),
            transform: `translateX(${interpolate(sp(50), [0, 1], [40, 0])}px)`,
          }}>
            "मेरी कमाई<br /><span style={{ color: ACCENT }}>खेती से है"</span>
          </div>

          <div style={{
            marginTop: 50,
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 56,
            fontWeight: 700,
            color: TEXT,
            opacity: sp(100),
            transform: `translateX(${interpolate(sp(100), [0, 1], [40, 0])}px)`,
            lineHeight: 1.2,
          }}>
            और टैक्स?
          </div>

          {/* Massive Zero */}
          <div style={{
            marginTop: 10,
            position: 'relative',
            transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
          }}>
            <div style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 320,
              color: '#c0392b',
              fontWeight: 900,
              lineHeight: 0.9,
              textShadow: '0 0 30px rgba(192, 57, 43, 0.6)',
            }}>
              ZERO
            </div>
            {/* Stamp border */}
            <div style={{
              position: 'absolute',
              left: -20,
              top: -10,
              width: 640,
              height: 280,
              border: '8px solid #c0392b',
              borderRadius: 12,
              opacity: 0.7,
            }} />
          </div>
        </div>

        {/* Arrow connecting paper to ZERO */}
        {frame > 165 && (
          <svg width="200" height="120" style={{
            position: 'absolute',
            left: 1000,
            top: 500,
            opacity: interpolate(frame, [165, 185], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}>
            <path d="M 10 60 Q 100 30 180 60" stroke={ACCENT} strokeWidth="6" fill="none" strokeLinecap="round" strokeDasharray="0" />
            <polygon points="170,50 190,60 170,72" fill={ACCENT} />
          </svg>
        )}
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene02_ITRPaper;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_IndiaScale',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_IndiaScale: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#76ABAE';
  const TEXT = '#E8EDF2';
  const ACCENT = '#FAB95B';
  const DEEP = '#3D6B6E';

  // Question phase: 0-90
  // Map reveal: 90-220
  // Counter: 150 onwards
  const qOp = interpolate(frame, [0, 20, 80, 95], [0, 1, 1, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const qScale = sp(2);

  const mapScale = sp(90, { damping: 11 });
  const mapBreath = 1 + Math.sin(frame * 0.03) * 0.015;

  // Counter from 0 to 25000+
  const counterStart = 150;
  const counter = Math.floor(interpolate(frame, [counterStart, counterStart + 90], [0, 27450], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  }));

  // Dots on India map
  const dotCount = Math.floor(interpolate(frame, [100, 280], [0, 80], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at 50% 60%, #8FBFC2 0%, #76ABAE 50%, #4A7A7D 100%)`,
      }} />

      {/* Concentric circle texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06, left: 0, top: 0 }}>
        {[100, 250, 400, 550, 700, 850, 1000].map((r, i) => (
          <circle key={i} cx="960" cy="540" r={r + Math.sin(frame * 0.02 + i) * 5} fill="none" stroke={TEXT} strokeWidth="1" />
        ))}
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: '12%',
        top: '15%',
        width: 280,
        height: 280,
        border: `3px solid ${ACCENT}`,
        opacity: 0.08,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        right: '8%',
        bottom: '12%',
        width: 360,
        height: 360,
        background: TEXT,
        opacity: 0.04,
        borderRadius: '50%',
      }} />

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const r1 = seededRandom(i + 11);
        const r2 = seededRandom(i + 77);
        const px = (r1 * 1920 + frame * (0.2 + r2 * 0.5)) % 1920;
        const py = r2 * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px,
            top: py + Math.sin((frame + i * 12) * 0.025) * 25,
            width: 3 + r2 * 5,
            height: 3 + r2 * 5,
            borderRadius: '50%',
            background: i % 4 === 0 ? ACCENT : TEXT,
            opacity: 0.18 + r1 * 0.2,
          }} />
        );
      })}

      <div style={{
        position: 'absolute',
        inset: 0,
        transform: `scale(${camScale})`,
        transformOrigin: '50% 50%',
      }}>
        {/* PHASE 1 — Question */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: '50%',
          transform: `translateX(-50%) scale(${qScale})`,
          opacity: qOp,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 130,
            color: TEXT,
            lineHeight: 1,
          }}>
            अजीब लगता है <span style={{ color: ACCENT }}>ना?</span>
          </div>
        </div>

        {/* PHASE 2 — Top label "BUT THE TRUTH IS" */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(85)})`,
          opacity: interpolate(frame, [85, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          display: 'flex',
          alignItems: 'center',
          gap: 20,
        }}>
          <div style={{ width: 60, height: 4, background: ACCENT }} />
          <div style={{
            fontFamily: 'Inter',
            fontSize: 32,
            fontWeight: 800,
            color: ACCENT,
            letterSpacing: 6,
          }}>BUT — THE TRUTH</div>
          <div style={{ width: 60, height: 4, background: ACCENT }} />
        </div>

        {/* India map SVG (simplified outline) */}
        <div style={{
          position: 'absolute',
          left: 200,
          top: 200,
          width: 700,
          height: 760,
          transform: `scale(${mapScale * mapBreath})`,
          transformOrigin: '50% 50%',
        }}>
          <svg viewBox="0 0 400 440" width="700" height="760">
            <defs>
              <filter id="glowMap">
                <feGaussianBlur stdDeviation="4" result="blur" />
                <feMerge>
                  <feMergeNode in="blur" />
                  <feMergeNode in="SourceGraphic" />
                </feMerge>
              </filter>
            </defs>
            {/* Simplified India outline */}
            <path d="M 180 30 L 220 35 L 250 55 L 280 80 L 295 110 L 290 140 L 305 170 L 310 200
                     L 295 230 L 280 260 L 260 290 L 245 320 L 225 355 L 200 390 L 180 415
                     L 165 410 L 150 380 L 135 350 L 125 320 L 115 290 L 110 260 L 105 230
                     L 100 200 L 95 170 L 100 140 L 110 110 L 130 85 L 150 60 L 170 40 Z"
                  fill={`${ACCENT}15`}
                  stroke={ACCENT}
                  strokeWidth="3"
                  filter="url(#glowMap)" />
            {/* Internal lines (states hint) */}
            <path d="M 130 130 L 270 140 M 120 200 L 290 210 M 130 280 L 250 290 M 150 360 L 220 370"
                  stroke={TEXT} strokeWidth="0.8" opacity="0.3" />

            {/* Hotspot dots — animated */}
            {Array.from({ length: dotCount }).map((_, i) => {
              const r1 = seededRandom(i + 200);
              const r2 = seededRandom(i + 400);
              // Random points inside india roughly
              const cx = 110 + r1 * 190;
              const cy = 60 + r2 * 340;
              const pulse = 0.8 + Math.sin((frame + i * 5) * 0.1) * 0.4;
              return (
                <g key={i}>
                  <circle cx={cx} cy={cy} r={2.5 * pulse} fill="#ff4444" opacity={0.9} />
                  <circle cx={cx} cy={cy} r={6 * pulse} fill="#ff4444" opacity={0.25} />
                </g>
              );
            })}
          </svg>
        </div>

        {/* RIGHT side counter + headline */}
        <div style={{
          position: 'absolute',
          right: 100,
          top: 240,
          width: 780,
        }}>
          {/* Headline */}
          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 92,
            color: TEXT,
            lineHeight: 1.05,
            opacity: sp(95),
            transform: `translateX(${interpolate(sp(95), [0, 1], [50, 0])}px)`,
          }}>
            ये पूरे <span style={{ color: ACCENT }}>इंडिया</span> में<br />हो रहा है
          </div>

          {/* Counter box */}
          <div style={{
            marginTop: 60,
            padding: '30px 40px',
            background: 'rgba(255,255,255,0.08)',
            borderLeft: `6px solid ${ACCENT}`,
            borderRadius: 10,
            opacity: sp(145),
            transform: `translateX(${interpolate(sp(145), [0, 1], [40, 0])}px)`,
            backdropFilter: 'blur(8px)',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 24,
              color: TEXT,
              opacity: 0.7,
              letterSpacing: 3,
              marginBottom: 12,
            }}>SUSPICIOUS ITR FILINGS (Est.)</div>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 130,
              color: ACCENT,
              fontWeight: 800,
              lineHeight: 1,
              textShadow: `0 0 20px ${ACCENT}40`,
            }}>
              {counter.toLocaleString('en-IN')}<span style={{ color: TEXT, fontSize: 60 }}>+</span>
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 32,
              color: TEXT,
              marginTop: 8,
              fontWeight: 600,
            }}>
              और लगातार बढ़ रहा है
            </div>
          </div>

          {/* Bottom scale indicator */}
          <div style={{
            marginTop: 40,
            display: 'flex',
            alignItems: 'center',
            gap: 16,
            opacity: sp(220),
          }}>
            <div style={{
              width: 14,
              height: 14,
              borderRadius: '50%',
              background: '#ff4444',
              boxShadow: '0 0 12px #ff4444',
              transform: `scale(${1 + Math.sin(frame * 0.2) * 0.3})`,
            }} />
            <div style={{
              fontFamily: 'Inter',
              fontSize: 26,
              color: TEXT,
              fontWeight: 600,
            }}>HIGH RISK ZONES across India</div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene03_IndiaScale;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_Hammer',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_Hammer: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#76ABAE';
  const TEXT = '#E8EDF2';
  const ACCENT = '#FAB95B';
  const DEEP = '#3D6B6E';

  // Hammer phase
  // Lift: 0-90 frames (hammer enters from top, hangs)
  // Drop: 90-130
  // Impact: 130
  // Aftermath: 130+

  const impactFrame = 150;

  const hammerEntry = sp(5, { damping: 10, stiffness: 80 });

  // Hammer Y: above screen → high position → SLAM
  const hammerY = frame < 90
    ? interpolate(hammerEntry, [0, 1], [-400, -50])
    : frame < impactFrame
      ? interpolate(frame, [90, impactFrame], [-50, 350], { extrapolateRight: 'clamp', easing: Easing.in(Easing.cubic) })
      : 350;

  // Hammer rotation: little swing before drop
  const hammerRot = frame < 90
    ? Math.sin(frame * 0.08) * 8 - 25
    : frame < impactFrame
      ? interpolate(frame, [90, impactFrame], [-25, -5], { extrapolateRight: 'clamp' })
      : interpolate(frame, [impactFrame, impactFrame + 8], [-5, 0], { extrapolateRight: 'clamp' });

  // Anticipation pulse before impact
  const antPulse = frame > 60 && frame < 90
    ? 1 + Math.sin((frame - 60) * 0.6) * 0.04
    : 1;

  // Screen shake on impact
  const shake = frame > impactFrame && frame < impactFrame + 20
    ? Math.sin((frame - impactFrame) * 2.5) * (20 - (frame - impactFrame))
    : 0;

  // Crack lines appear after impact
  const crackOp = interpolate(frame, [impactFrame, impactFrame + 15], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Text after impact
  const txtOp = interpolate(frame, [impactFrame + 10, impactFrame + 35], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const txtScale = sp(impactFrame + 10, { damping: 10 });

  // Impact flash
  const flash = frame > impactFrame && frame < impactFrame + 6
    ? 1 - (frame - impactFrame) / 6
    : 0;

  // Sparks
  const sparkOp = interpolate(frame, [impactFrame, impactFrame + 30], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `linear-gradient(180deg, #5C8E91 0%, #76ABAE 40%, #8FBFC2 100%)`,
      }} />

      {/* Texture lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 25 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 50} x2="1920" y2={i * 50} stroke={TEXT} strokeWidth="0.5" />
        ))}
      </svg>

      <div style={{
        position: 'absolute',
        inset: 0,
        transform: `translate(${shake}px, ${shake * 0.7}px)`,
      }}>
        {/* Flash overlay */}
        <AbsoluteFill style={{
          background: '#FFFFFF',
          opacity: flash * 0.7,
          pointerEvents: 'none',
          mixBlendMode: 'overlay',
        }} />

        {/* Top section: "GOVERNMENT" label */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(2)})`,
          display: 'flex',
          alignItems: 'center',
          gap: 20,
        }}>
          <div style={{ width: 80, height: 4, background: ACCENT }} />
          <div style={{
            fontFamily: 'Inter',
            fontSize: 32,
            fontWeight: 800,
            color: ACCENT,
            letterSpacing: 6,
          }}>SARKARI HAMMER • INCOMING</div>
          <div style={{ width: 80, height: 4, background: ACCENT }} />
        </div>

        {/* Target — Loophole text/box being hammered */}
        <div style={{
          position: 'absolute',
          left: '50%',
          top: 540,
          transform: `translate(-50%, 0) scale(${antPulse})`,
        }}>
          <div style={{
            padding: '50px 90px',
            background: `${DEEP}90`,
            border: `4px solid ${TEXT}`,
            borderRadius: 16,
            textAlign: 'center',
            position: 'relative',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 26,
              color: TEXT,
              opacity: 0.7,
              letterSpacing: 4,
              marginBottom: 14,
            }}>THE LOOPHOLE</div>
            <div style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 90,
              color: ACCENT,
              fontWeight: 900,
              lineHeight: 1,
            }}>
              TAX = ZERO
            </div>
            {/* Crack lines */}
            {frame > impactFrame && (
              <svg width="700" height="280" viewBox="0 0 700 280" style={{
                position: 'absolute',
                left: 0,
                top: 0,
                opacity: crackOp,
                pointerEvents: 'none',
              }}>
                <path d="M 350 0 L 320 80 L 380 130 L 290 200 L 360 280" stroke="#1C1C1C" strokeWidth="6" fill="none" />
                <path d="M 350 30 L 280 110 L 220 180" stroke="#1C1C1C" strokeWidth="5" fill="none" />
                <path d="M 350 30 L 430 100 L 510 160 L 480 230" stroke="#1C1C1C" strokeWidth="5" fill="none" />
                <path d="M 280 110 L 180 90" stroke="#1C1C1C" strokeWidth="3" fill="none" />
                <path d="M 430 100 L 540 70" stroke="#1C1C1C" strokeWidth="3" fill="none" />
              </svg>
            )}
          </div>
        </div>

        {/* THE HAMMER */}
        <div style={{
          position: 'absolute',
          left: '50%',
          top: hammerY,
          transform: `translate(-50%, 0) rotate(${hammerRot}deg) scale(${hammerEntry * antPulse})`,
          transformOrigin: '50% 100%',
          zIndex: 10,
        }}>
          <svg width="380" height="500" viewBox="0 0 200 260">
            <defs>
              <linearGradient id="hammerHead" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" stopColor="#5a5a5a" />
                <stop offset="50%" stopColor="#2a2a2a" />
                <stop offset="100%" stopColor="#1a1a1a" />
              </linearGradient>
              <linearGradient id="hammerHandle" x1="0%" y1="0%" x2="100%" y2="0%">
                <stop offset="0%" stopColor="#8B4513" />
                <stop offset="50%" stopColor="#A0522D" />
                <stop offset="100%" stopColor="#6B3410" />
              </linearGradient>
            </defs>
            {/* Hammer head */}
            <rect x="30" y="170" width="140" height="60" fill="url(#hammerHead)" rx="6" />
            <rect x="30" y="170" width="140" height="10" fill="#777" rx="3" />
            <rect x="30" y="220" width="140" height="10" fill="#1a1a1a" rx="3" />
            {/* Hammer head highlights */}
            <rect x="40" y="180" width="20" height="40" fill="#888" opacity="0.5" rx="3" />
            {/* Claw side */}
            <path d="M 30 175 L 10 180 L 10 220 L 30 225 Z" fill="url(#hammerHead)" />
            {/* Handle */}
            <rect x="92" y="0" width="16" height="180" fill="url(#hammerHandle)" rx="4" />
            <rect x="92" y="0" width="16" height="20" fill={ACCENT} rx="4" opacity="0.8" />
            {/* Grip lines */}
            <line x1="92" y1="40" x2="108" y2="40" stroke="#5a3010" strokeWidth="2" />
            <line x1="92" y1="60" x2="108" y2="60" stroke="#5a3010" strokeWidth="2" />
            <line x1="92" y1="80" x2="108" y2="80" stroke="#5a3010" strokeWidth="2" />
          </svg>
        </div>

        {/* Sparks on impact */}
        {frame > impactFrame && Array.from({ length: 18 }).map((_, i) => {
          const r1 = seededRandom(i + 33);
          const angle = (i / 18) * Math.PI * 2;
          const dist = (frame - impactFrame) * (8 + r1 * 4);
          const x = Math.cos(angle) * dist;
          const y = Math.sin(angle) * dist - dist * 0.3;
          return (
            <div key={i} style={{
              position: 'absolute',
              left: `calc(50% + ${x}px)`,
              top: 590 + y,
              width: 6,
              height: 6,
              borderRadius: '50%',
              background: ACCENT,
              opacity: sparkOp * Math.max(0, 1 - (frame - impactFrame) / 40),
              boxShadow: `0 0 10px ${ACCENT}`,
            }} />
          );
        })}

        {/* Bottom payoff text */}
        <div style={{
          position: 'absolute',
          bottom: 100,
          left: '50%',
          transform: `translateX(-50%) scale(${txtScale})`,
          opacity: txtOp,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 96,
            color: TEXT,
            lineHeight: 1,
          }}>
            हथोड़ा <span style={{ color: '#ff4444' }}>तैयार है</span>
          </div>
          <div style={{
            marginTop: 14,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 38,
            color: TEXT,
            opacity: 0.85,
            fontWeight: 600,
          }}>
            सरकार ने इस बार सब track कर लिया है
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 40%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene04_Hammer;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_KunalIntro',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_KunalIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#76ABAE';
  const TEXT = '#E8EDF2';
  const ACCENT = '#FAB95B';
  const DEEP = '#3D6B6E';

  // Logo + name reveal
  const logoOp = sp(8);
  const nameOp = sp(28);
  const tagOp = sp(50);

  // Channel ID card slide in
  const cardSlide = sp(15, { damping: 11 });

  // CA vs ProBuffet comparison reveal at 100+
  const cmpOp = sp(110);

  // Items pop in
  const item1 = sp(130);
  const item2 = sp(155);
  const item3 = sp(180);
  const item4 = sp(205);

  // CA list strikes through
  const strike1 = interpolate(frame, [150, 175], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const strike2 = interpolate(frame, [175, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const strike3 = interpolate(frame, [200, 225], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Bottom highlight box
  const boxScale = sp(260, { damping: 9, stiffness: 100 });

  // Pulse
  const pulse = 1 + Math.sin(frame * 0.08) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `linear-gradient(135deg, #5C8E91 0%, #76ABAE 50%, #8FBFC2 100%)`,
      }} />

      {/* Dot grid pattern */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dotsP5" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill={TEXT} />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dotsP5)" transform={`translate(${(frame * 0.2) % 50} 0)`} />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        right: -100,
        top: -80,
        width: 500,
        height: 500,
        background: ACCENT,
        opacity: 0.06,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute',
        left: -120,
        bottom: -100,
        width: 460,
        height: 460,
        border: `3px solid ${TEXT}`,
        opacity: 0.07,
        borderRadius: 40,
        transform: `rotate(${frame * 0.08}deg)`,
      }} />

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const r1 = seededRandom(i + 17);
        const r2 = seededRandom(i + 84);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r1 * 1920 + frame * 0.3) % 1920,
            top: r2 * 1080 + Math.sin((frame + i * 9) * 0.025) * 20,
            width: 4 + r2 * 5,
            height: 4 + r2 * 5,
            borderRadius: '50%',
            background: i % 3 === 0 ? ACCENT : TEXT,
            opacity: 0.15 + r1 * 0.2,
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 130,
        opacity: sp(2),
        transform: `translateY(${interpolate(sp(2), [0, 1], [-30, 0])}px)`,
        display: 'flex',
        alignItems: 'center',
        gap: 20,
      }}>
        <div style={{ width: 70, height: 4, background: ACCENT }} />
        <div style={{
          fontFamily: 'Inter',
          fontSize: 30,
          fontWeight: 800,
          color: ACCENT,
          letterSpacing: 6,
        }}>HOST INTRO</div>
      </div>

      {/* LEFT — Kunal ID Card */}
      <div style={{
        position: 'absolute',
        left: 130,
        top: 200,
        width: 720,
        opacity: cardSlide,
        transform: `translateX(${interpolate(cardSlide, [0, 1], [-60, 0])}px)`,
      }}>
        {/* Card */}
        <div style={{
          background: `${DEEP}A0`,
          backdropFilter: 'blur(10px)',
          borderRadius: 20,
          padding: 40,
          border: `2px solid ${TEXT}30`,
          position: 'relative',
          overflow: 'hidden',
        }}>
          {/* Accent corner */}
          <div style={{
            position: 'absolute',
            top: 0,
            left: 0,
            width: 8,
            height: '100%',
            background: ACCENT,
          }} />

          {/* Avatar circle */}
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 30,
            marginBottom: 30,
          }}>
            <div style={{
              width: 160,
              height: 160,
              borderRadius: '50%',
              background: ACCENT,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Yantra One, serif',
              fontSize: 100,
              color: '#1C1C1C',
              fontWeight: 900,
              boxShadow: `0 0 40px ${ACCENT}60`,
              transform: `scale(${pulse})`,
            }}>
              K
            </div>
            <div>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 24,
                color: TEXT,
                opacity: 0.6,
                letterSpacing: 3,
                marginBottom: 4,
              }}>HOST</div>
              <div style={{
                fontFamily: 'Yantra One, serif',
                fontSize: 90,
                color: TEXT,
                lineHeight: 1,
              }}>KUNAL</div>
            </div>
          </div>

          {/* Channel name */}
          <div style={{
            paddingTop: 26,
            borderTop: `1px solid ${TEXT}30`,
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 22,
              color: TEXT,
              opacity: 0.6,
              letterSpacing: 3,
              marginBottom: 8,
            }}>CHANNEL</div>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 62,
              color: ACCENT,
              fontWeight: 900,
              letterSpacing: -1,
            }}>
              ProBuffet HQ
            </div>
            <div style={{
              marginTop: 14,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 28,
              color: TEXT,
              opacity: 0.85,
              fontWeight: 500,
            }}>
              पैसे की वो बातें — जो किसी ने नहीं बताई
            </div>
          </div>
        </div>
      </div>

      {/* RIGHT — Comparison: CA vs ProBuffet */}
      <div style={{
        position: 'absolute',
        right: 100,
        top: 220,
        width: 780,
        opacity: cmpOp,
      }}>
        {/* Header */}
        <div style={{
          fontFamily: 'Yantra One, serif',
          fontSize: 64,
          color: TEXT,
          lineHeight: 1,
          marginBottom: 30,
        }}>
          तुम्हारा <span style={{ color: ACCENT }}>CA</span> क्या बताता है?
        </div>

        {/* CA bullets — getting struck through */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 18 }}>
          {[
            { text: 'सिर्फ ITR file करना सिखाता है', strike: strike1 },
            { text: 'जो form पर लिखा है, वही बताता है', strike: strike2 },
            { text: 'Loophole या tricks पर चुप', strike: strike3 },
          ].map((item, idx) => (
            <div key={idx} style={{
              opacity: idx === 0 ? item1 : idx === 1 ? item2 : item3,
              transform: `translateX(${interpolate(idx === 0 ? item1 : idx === 1 ? item2 : item3, [0, 1], [40, 0])}px)`,
              padding: '18px 28px',
              background: 'rgba(255,255,255,0.07)',
              borderLeft: `5px solid ${TEXT}50`,
              borderRadius: 8,
              position: 'relative',
              display: 'flex',
              alignItems: 'center',
              gap: 20,
            }}>
              <div style={{
                width: 36,
                height: 36,
                borderRadius: '50%',
                background: '#ff444440',
                border: `2px solid #ff4444`,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                color: '#ff4444',
                fontFamily: 'Inter',
                fontSize: 24,
                fontWeight: 800,
              }}>✕</div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 32,
                color: TEXT,
                fontWeight: 600,
                position: 'relative',
                flex: 1,
              }}>
                {item.text}
                {/* Strike line */}
                <div style={{
                  position: 'absolute',
                  left: 0,
                  top: '50%',
                  width: `${item.strike * 100}%`,
                  height: 3,
                  background: '#ff4444',
                  transform: 'translateY(-50%)',
                }} />
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* BOTTOM — payoff box */}
      <div style={{
        position: 'absolute',
        bottom: 70,
        left: '50%',
        transform: `translateX(-50%) scale(${boxScale})`,
        padding: '24px 60px',
        background: ACCENT,
        borderRadius: 12,
        boxShadow: `0 0 50px ${ACCENT}80`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 22,
          color: '#1C1C1C',
          opacity: 0.7,
          letterSpacing: 4,
          marginBottom: 6,
        }}>MY JOB</div>
        <div style={{
          fontFamily: 'Yantra One, serif',
          fontSize: 60,
          color: '#1C1C1C',
          fontWeight: 900,
          lineHeight: 1,
        }}>
          वो बातें — जो CA भी नहीं बताता
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene05_KunalIntro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_YourPocket',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_YourPocket: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#76ABAE';
  const TEXT = '#E8EDF2';
  const ACCENT = '#FAB95B';
  const DEEP = '#3D6B6E';

  // Camera orbit slight
  const camRot = Math.sin(frame * 0.008) * 0.5;
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.05]);

  // Headline reveal
  const h1 = sp(5);
  const h2 = sp(25);

  // Wallet center entrance
  const walletScale = sp(40, { damping: 10, stiffness: 90 });
  const walletFloat = Math.sin(frame * 0.04) * 8;

  // Coins falling into wallet
  const coinFalling = (delay: number) => {
    const localFrame = frame - delay;
    if (localFrame < 0) return { y: -500, op: 0 };
    const y = interpolate(localFrame, [0, 30], [-400, 0], { extrapolateRight: 'clamp', easing: Easing.in(Easing.cubic) });
    const op = interpolate(localFrame, [0, 5, 30, 35], [0, 1, 1, 0], { extrapolateRight: 'clamp' });
    return { y, op };
  };

  // Three personas
  const p1 = sp(140);
  const p2 = sp(170);
  const p3 = sp(200);

  // Connecting lines
  const linesOp = interpolate(frame, [220, 260], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Bottom message
  const bottomOp = sp(280);
  const bottomScale = sp(280, { damping: 9 });

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at 50% 50%, #8FBFC2 0%, #76ABAE 50%, #4A7A7D 100%)`,
      }} />

      {/* Circle ring texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {[200, 350, 500, 650, 800].map((r, i) => (
          <circle key={i} cx="960" cy="540" r={r} fill="none" stroke={TEXT} strokeWidth="1" strokeDasharray="4 8" />
        ))}
      </svg>

      <div style={{
        position: 'absolute',
        inset: 0,
        transform: `scale(${camScale}) rotate(${camRot}deg)`,
      }}>
        {/* Floating shape layers */}
        <div style={{
          position: 'absolute',
          top: '12%',
          left: '8%',
          width: 280,
          height: 280,
          background: ACCENT,
          opacity: 0.05,
          borderRadius: '50%',
        }} />
        <div style={{
          position: 'absolute',
          bottom: '15%',
          right: '8%',
          width: 320,
          height: 320,
          border: `3px solid ${TEXT}`,
          opacity: 0.07,
          transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }} />

        {/* Particles */}
        {Array.from({ length: 24 }).map((_, i) => {
          const r1 = seededRandom(i + 22);
          const r2 = seededRandom(i + 88);
          return (
            <div key={i} style={{
              position: 'absolute',
              left: (r1 * 1920 + frame * 0.3) % 1920,
              top: r2 * 1080 + Math.sin((frame + i * 10) * 0.03) * 22,
              width: 3 + r2 * 5,
              height: 3 + r2 * 5,
              borderRadius: '50%',
              background: i % 3 === 0 ? ACCENT : TEXT,
              opacity: 0.15 + r2 * 0.2,
            }} />
          );
        })}

        {/* Top headline */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${h1})`,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 28,
            fontWeight: 800,
            color: ACCENT,
            letterSpacing: 6,
            marginBottom: 16,
          }}>SIDHA TUMHARI POCKET</div>
          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 92,
            color: TEXT,
            lineHeight: 1,
          }}>
            ये बात <span style={{ color: ACCENT }}>तुम्हारी जेब</span> की है
          </div>
        </div>

        {/* CENTER — Big Wallet */}
        <div style={{
          position: 'absolute',
          left: '50%',
          top: 380,
          transform: `translate(-50%, 0) scale(${walletScale}) translateY(${walletFloat}px)`,
        }}>
          <svg width="380" height="280" viewBox="0 0 380 280">
            <defs>
              <linearGradient id="walletG" x1="0%" y1="0%" x2="0%" y2="100%">
                <stop offset="0%" stopColor={ACCENT} />
                <stop offset="100%" stopColor="#D89540" />
              </linearGradient>
            </defs>
            {/* Wallet body */}
            <rect x="20" y="60" width="340" height="200" rx="20" fill="url(#walletG)" />
            {/* Wallet flap */}
            <path d="M 20 60 Q 20 30 50 30 L 280 30 Q 310 30 310 60 L 310 100 L 20 100 Z" fill="#D89540" />
            <rect x="20" y="95" width="340" height="14" fill="#B57828" />
            {/* Card slots */}
            <rect x="50" y="135" width="200" height="40" rx="6" fill="#FFFFFF20" stroke="#FFFFFF40" strokeWidth="2" />
            <rect x="50" y="185" width="160" height="40" rx="6" fill="#FFFFFF20" stroke="#FFFFFF40" strokeWidth="2" />
            {/* Button */}
            <circle cx="290" cy="180" r="22" fill="#1C1C1C" />
            <circle cx="290" cy="180" r="14" fill={ACCENT} />
            {/* Stitching */}
            <rect x="30" y="70" width="320" height="180" rx="14" fill="none" stroke="#1C1C1C30" strokeWidth="2" strokeDasharray="4 4" />
          </svg>
        </div>

        {/* Coins falling */}
        {[
          { delay: 60, x: -60 },
          { delay: 80, x: 20 },
          { delay: 100, x: -20 },
          { delay: 120, x: 60 },
        ].map((c, i) => {
          const { y, op } = coinFalling(c.delay);
          return (
            <div key={i} style={{
              position: 'absolute',
              left: `calc(50% + ${c.x}px)`,
              top: 360 + y,
              opacity: op,
              transform: `rotate(${frame * 2}deg)`,
            }}>
              <svg width="60" height="60" viewBox="0 0 60 60">
                <circle cx="30" cy="30" r="26" fill={ACCENT} stroke="#1C1C1C" strokeWidth="3" />
                <text x="30" y="40" fontSize="28" textAnchor="middle" fontWeight="900" fill="#1C1C1C" fontFamily="Inter">₹</text>
              </svg>
            </div>
          );
        })}

        {/* THREE PERSONAS row below wallet */}
        <div style={{
          position: 'absolute',
          bottom: 200,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 80,
          alignItems: 'center',
        }}>
          {[
            { label: 'नौकरी पेशा', sub: 'Salaried', sp: p1, color: '#5BC0EB' },
            { label: 'बिस्निसमैन', sub: 'Business Owner', sp: p2, color: ACCENT },
            { label: 'साइड इंकम', sub: 'Side Hustle', sp: p3, color: '#9BC95B' },
          ].map((p, i) => (
            <div key={i} style={{
              opacity: p.sp,
              transform: `translateY(${interpolate(p.sp, [0, 1], [40, 0])}px) scale(${p.sp})`,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              gap: 12,
            }}>
              <div style={{
                width: 130,
                height: 130,
                borderRadius: 20,
                background: `${p.color}25`,
                border: `3px solid ${p.color}`,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                boxShadow: `0 0 25px ${p.color}40`,
              }}>
                {i === 0 && (
                  <svg width="70" height="70" viewBox="0 0 100 100">
                    <circle cx="50" cy="30" r="14" fill="none" stroke={p.color} strokeWidth="3.5" />
                    <path d="M 25 80 Q 25 60 50 60 Q 75 60 75 80 L 75 90 L 25 90 Z" fill="none" stroke={p.color} strokeWidth="3.5" />
                    <rect x="40" y="65" width="20" height="6" fill={p.color} />
                  </svg>
                )}
                {i === 1 && (
                  <svg width="70" height="70" viewBox="0 0 100 100">
                    <rect x="20" y="40" width="60" height="50" fill="none" stroke={p.color} strokeWidth="3.5" rx="3" />
                    <path d="M 25 40 L 25 30 L 75 30 L 75 40" fill="none" stroke={p.color} strokeWidth="3.5" />
                    <rect x="38" y="60" width="24" height="18" fill={p.color} />
                    <circle cx="50" cy="20" r="6" fill={p.color} />
                  </svg>
                )}
                {i === 2 && (
                  <svg width="70" height="70" viewBox="0 0 100 100">
                    <path d="M 20 80 L 20 50 L 35 50 L 35 80" fill="none" stroke={p.color} strokeWidth="3.5" />
                    <path d="M 45 80 L 45 35 L 60 35 L 60 80" fill="none" stroke={p.color} strokeWidth="3.5" />
                    <path d="M 70 80 L 70 20 L 85 20 L 85 80" fill="none" stroke={p.color} strokeWidth="3.5" />
                    <path d="M 15 85 L 90 85" stroke={p.color} strokeWidth="3.5" />
                  </svg>
                )}
              </div>
              <div style={{
                fontFamily: 'Yantra One, serif',
                fontSize: 36,
                color: TEXT,
                textAlign: 'center',
              }}>{p.label}</div>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 18,
                color: p.color,
                letterSpacing: 2,
                fontWeight: 700,
              }}>{p.sub}</div>
            </div>
          ))}
        </div>

        {/* Connecting arrows from personas → wallet */}
        <svg width="1920" height="1080" style={{
          position: 'absolute',
          inset: 0,
          opacity: linesOp,
          pointerEvents: 'none',
        }}>
          <path d="M 660 720 Q 800 600 880 540" stroke={ACCENT} strokeWidth="3" fill="none" strokeDasharray="6 8" opacity="0.6" />
          <path d="M 960 720 L 960 600" stroke={ACCENT} strokeWidth="3" fill="none" strokeDasharray="6 8" opacity="0.6" />
          <path d="M 1260 720 Q 1120 600 1040 540" stroke={ACCENT} strokeWidth="3" fill="none" strokeDasharray="6 8" opacity="0.6" />
        </svg>

        {/* Bottom message */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: '50%',
          transform: `translateX(-50%) scale(${bottomScale})`,
          opacity: bottomOp,
          padding: '18px 50px',
          background: 'rgba(28,28,28,0.6)',
          borderRadius: 10,
          border: `2px solid ${ACCENT}`,
        }}>
          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 42,
            color: TEXT,
            textAlign: 'center',
          }}>
            कोई हो — <span style={{ color: ACCENT }}>सबकी जेब काटी जा रही है</span>
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene06_YourPocket;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_LoopholeHole',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_LoopholeHole: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#76ABAE';
  const TEXT = '#E8EDF2';
  const ACCENT = '#FAB95B';
  const DEEP = '#3D6B6E';

  // Topic reveal
  const topicOp = sp(5);

  // Tax system box draws in
  const boxScale = sp(50, { damping: 11 });

  // Hole appears in the middle of the box
  const holeScale = sp(110, { damping: 8, stiffness: 90 });
  const holePulse = 1 + Math.sin(frame * 0.08) * 0.04;

  // Money flying out of hole
  const moneyOp = interpolate(frame, [140, 170], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Side label "LOOPHOLE"
  const labelOp = sp(180);

  // Bottom stat: "बरसों से चल रहा है"
  const statOp = sp(250);

  // Camera slow zoom in
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08]);

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `linear-gradient(180deg, #4A7A7D 0%, #76ABAE 50%, #5C8E91 100%)`,
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="bigGridP" width="120" height="120" patternUnits="userSpaceOnUse">
            <path d="M 120 0 L 0 0 0 120" fill="none" stroke={TEXT} strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#bigGridP)" />
      </svg>

      <div style={{
        position: 'absolute',
        inset: 0,
        transform: `scale(${camScale})`,
        transformOrigin: '50% 50%',
      }}>
        {/* Shapes */}
        <div style={{
          position: 'absolute',
          right: 100,
          top: 80,
          width: 280,
          height: 280,
          border: `3px solid ${ACCENT}`,
          opacity: 0.08,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute',
          left: 80,
          bottom: 80,
          width: 360,
          height: 360,
          background: ACCENT,
          opacity: 0.04,
          transform: `rotate(${frame * 0.1}deg)`,
        }} />

        {/* Particles */}
        {Array.from({ length: 22 }).map((_, i) => {
          const r1 = seededRandom(i + 27);
          const r2 = seededRandom(i + 95);
          return (
            <div key={i} style={{
              position: 'absolute',
              left: (r1 * 1920 + frame * 0.35) % 1920,
              top: r2 * 1080 + Math.sin((frame + i * 11) * 0.025) * 24,
              width: 4 + r2 * 4,
              height: 4 + r2 * 4,
              borderRadius: '50%',
              background: TEXT,
              opacity: 0.18,
            }} />
          );
        })}

        {/* TOPIC tag top */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: '50%',
          transform: `translateX(-50%) scale(${topicOp})`,
          textAlign: 'center',
        }}>
          <div style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 18,
            padding: '14px 40px',
            background: `${ACCENT}20`,
            border: `2px solid ${ACCENT}`,
            borderRadius: 40,
            marginBottom: 24,
          }}>
            <div style={{
              width: 14,
              height: 14,
              borderRadius: '50%',
              background: ACCENT,
              transform: `scale(${1 + Math.sin(frame * 0.15) * 0.3})`,
              boxShadow: `0 0 10px ${ACCENT}`,
            }} />
            <div style={{
              fontFamily: 'Inter',
              fontSize: 26,
              fontWeight: 800,
              color: ACCENT,
              letterSpacing: 5,
            }}>THE TOPIC</div>
          </div>
          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 88,
            color: TEXT,
            lineHeight: 1,
          }}>
            AGRICULTURAL INCOME <span style={{ color: ACCENT }}>LOOPHOLE</span>
          </div>
        </div>

        {/* CENTER — Tax System Box with hole */}
        <div style={{
          position: 'absolute',
          left: '50%',
          top: 380,
          transform: `translate(-50%, 0) scale(${boxScale})`,
        }}>
          {/* Big rectangle — Tax System */}
          <div style={{
            width: 900,
            height: 460,
            background: `${DEEP}80`,
            border: `5px solid ${TEXT}`,
            borderRadius: 16,
            position: 'relative',
            overflow: 'hidden',
            boxShadow: '0 20px 60px rgba(0,0,0,0.4)',
          }}>
            {/* Label inside */}
            <div style={{
              position: 'absolute',
              top: 28,
              left: 40,
              fontFamily: 'Inter',
              fontSize: 22,
              color: TEXT,
              opacity: 0.6,
              letterSpacing: 4,
            }}>INDIA • TAX SYSTEM</div>

            <div style={{
              position: 'absolute',
              top: 60,
              left: 40,
              fontFamily: 'Yantra One, serif',
              fontSize: 70,
              color: TEXT,
              lineHeight: 1,
            }}>
              Income Tax Act <span style={{ color: ACCENT }}>1961</span>
            </div>

            {/* Brick pattern */}
            <svg width="900" height="460" style={{ position: 'absolute', inset: 0, opacity: 0.15 }}>
              {Array.from({ length: 8 }).map((_, row) => (
                <g key={row}>
                  {Array.from({ length: 12 }).map((_, col) => (
                    <rect
                      key={col}
                      x={col * 80 + (row % 2 === 0 ? 0 : 40)}
                      y={row * 60 + 180}
                      width="76"
                      height="56"
                      fill="none"
                      stroke={TEXT}
                      strokeWidth="1"
                    />
                  ))}
                </g>
              ))}
            </svg>

            {/* THE HOLE */}
            <div style={{
              position: 'absolute',
              left: '50%',
              top: '55%',
              transform: `translate(-50%, -50%) scale(${holeScale * holePulse})`,
            }}>
              {/* Outer glow */}
              <div style={{
                width: 240,
                height: 240,
                borderRadius: '50%',
                background: 'radial-gradient(circle, rgba(255,68,68,0.6) 0%, rgba(28,28,28,0.95) 60%, rgba(0,0,0,1) 100%)',
                boxShadow: '0 0 60px rgba(255,68,68,0.4), inset 0 0 40px rgba(0,0,0,0.8)',
                position: 'relative',
              }}>
                {/* Inner deep hole */}
                <div style={{
                  position: 'absolute',
                  left: '50%',
                  top: '50%',
                  transform: 'translate(-50%, -50%)',
                  width: 160,
                  height: 160,
                  borderRadius: '50%',
                  background: 'radial-gradient(circle, #000 0%, #1C1C1C 80%)',
                  border: `3px solid ${ACCENT}`,
                }}>
                  {/* Money symbol falling in */}
                  <div style={{
                    position: 'absolute',
                    left: '50%',
                    top: '50%',
                    transform: 'translate(-50%, -50%)',
                    fontFamily: 'Yantra One, serif',
                    fontSize: 80,
                    color: ACCENT,
                    fontWeight: 900,
                    opacity: 0.5,
                  }}>₹</div>
                </div>
              </div>
            </div>

            {/* Money symbols flying out */}
            {moneyOp > 0 && Array.from({ length: 8 }).map((_, i) => {
              const r1 = seededRandom(i + 50);
              const angle = (i / 8) * Math.PI * 2;
              const dist = 120 + (frame * 1.2) % 200;
              const x = Math.cos(angle) * dist;
              const y = Math.sin(angle) * dist;
              return (
                <div key={i} style={{
                  position: 'absolute',
                  left: `calc(50% + ${x}px)`,
                  top: `calc(55% + ${y}px)`,
                  transform: 'translate(-50%, -50%)',
                  fontFamily: 'Yantra One, serif',
                  fontSize: 38 + r1 * 20,
                  color: ACCENT,
                  opacity: moneyOp * (1 - (dist - 120) / 200),
                  fontWeight: 900,
                  textShadow: `0 0 10px ${ACCENT}`,
                }}>₹</div>
              );
            })}
          </div>

          {/* Side LOOPHOLE label */}
          <div style={{
            position: 'absolute',
            right: -380,
            top: '40%',
            transform: `translateY(-50%) scale(${labelOp})`,
            opacity: labelOp,
            display: 'flex',
            alignItems: 'center',
            gap: 14,
          }}>
            <svg width="80" height="20" viewBox="0 0 80 20">
              <path d="M 0 10 L 70 10" stroke="#ff4444" strokeWidth="3" />
              <polygon points="60,4 75,10 60,16" fill="#ff4444" />
            </svg>
            <div style={{
              padding: '12px 22px',
              background: '#ff4444',
              borderRadius: 8,
              fontFamily: 'Inter',
              fontSize: 32,
              color: '#FFFFFF',
              fontWeight: 800,
              letterSpacing: 2,
              boxShadow: '0 0 20px rgba(255,68,68,0.6)',
              whiteSpace: 'nowrap',
            }}>LOOPHOLE ✕ CHHED</div>
          </div>
        </div>

        {/* Bottom stat */}
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: '50%',
          transform: `translateX(-50%) scale(${statOp})`,
          opacity: statOp,
          display: 'flex',
          gap: 50,
          alignItems: 'center',
        }}>
          <div style={{
            padding: '20px 40px',
            background: 'rgba(255,255,255,0.08)',
            borderLeft: `5px solid ${ACCENT}`,
            borderRadius: 8,
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 18,
              color: TEXT,
              opacity: 0.6,
              letterSpacing: 3,
              marginBottom: 4,
            }}>SINCE</div>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 64,
              color: ACCENT,
              fontWeight: 800,
              lineHeight: 1,
            }}>60+ YEARS</div>
          </div>
          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 48,
            color: TEXT,
          }}>
            ये छेद बरसों से <span style={{ color: ACCENT }}>खुला है</span>
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene07_LoopholeHole;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_Section10',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_Section10: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#76ABAE';
  const TEXT = '#E8EDF2';
  const ACCENT = '#FAB95B';
  const DEEP = '#3D6B6E';

  // Header
  const headerOp = sp(5);

  // Big book of law slide in
  const bookScale = sp(20, { damping: 11 });
  const bookFloat = Math.sin(frame * 0.035) * 4;

  // Section 10(1) highlight zoom
  const highlightScale = sp(90, { damping: 9 });

  // Highlight marker underline
  const ulineW = interpolate(frame, [120, 160], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Translation box reveal
  const transOp = sp(180);
  const transScale = sp(180, { damping: 10 });

  // The "TAX = 0" stamp at the end
  const stampScale = sp(260, { damping: 8, stiffness: 100 });
  const stampRot = interpolate(sp(260), [0, 1], [-20, -5]);

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `linear-gradient(135deg, #5C8E91 0%, #76ABAE 50%, #8FBFC2 100%)`,
      }} />

      {/* Stripe pattern */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="stripeP8" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform="rotate(-25)">
            <line x1="0" y1="0" x2="0" y2="60" stroke={TEXT} strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#stripeP8)" />
      </svg>

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        right: 60,
        top: 100,
        width: 260,
        height: 260,
        background: ACCENT,
        opacity: 0.05,
        borderRadius: 32,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        left: 60,
        bottom: 60,
        width: 340,
        height: 340,
        border: `3px solid ${TEXT}`,
        opacity: 0.06,
        borderRadius: '50%',
      }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r1 = seededRandom(i + 31);
        const r2 = seededRandom(i + 102);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r1 * 1920 + frame * 0.3) % 1920,
            top: r2 * 1080,
            width: 4,
            height: 4,
            borderRadius: '50%',
            background: TEXT,
            opacity: 0.2,
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 130,
        opacity: headerOp,
        transform: `translateY(${interpolate(headerOp, [0, 1], [-30, 0])}px)`,
        display: 'flex',
        alignItems: 'center',
        gap: 18,
      }}>
        <div style={{ width: 70, height: 4, background: ACCENT }} />
        <div style={{
          fontFamily: 'Inter',
          fontSize: 28,
          fontWeight: 800,
          color: ACCENT,
          letterSpacing: 5,
        }}>THE LAW BOOK</div>
      </div>

      {/* LEFT — Big Law book */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: 180,
        width: 820,
        transform: `translateY(${bookFloat}px) scale(${bookScale})`,
        transformOrigin: '0 0',
      }}>
        {/* Book shape */}
        <div style={{
          background: '#1a1a1a',
          borderRadius: '4px 14px 14px 4px',
          padding: '40px 50px 40px 80px',
          minHeight: 700,
          position: 'relative',
          boxShadow: '0 30px 80px rgba(0,0,0,0.5)',
          color: '#F5F5F5',
        }}>
          {/* Spine */}
          <div style={{
            position: 'absolute',
            left: 0,
            top: 0,
            bottom: 0,
            width: 36,
            background: 'linear-gradient(90deg, #FAB95B 0%, #B57828 100%)',
            borderRadius: '4px 0 0 4px',
          }} />

          {/* Page edge lines */}
          <div style={{
            position: 'absolute',
            right: 0,
            top: 20,
            bottom: 20,
            width: 6,
            background: '#F5F5F5',
          }} />

          {/* Book content */}
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 18,
            color: '#888',
            letterSpacing: 2,
            marginBottom: 8,
          }}>OFFICIAL DOCUMENT</div>

          <div style={{
            fontFamily: 'Inter',
            fontSize: 50,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.05,
            marginBottom: 6,
          }}>
            THE INCOME-TAX
          </div>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 50,
            fontWeight: 900,
            color: ACCENT,
            lineHeight: 1.05,
            marginBottom: 30,
          }}>
            ACT, 1961
          </div>

          <div style={{
            height: 3,
            background: ACCENT,
            width: 80,
            marginBottom: 30,
          }} />

          {/* Section text */}
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 22,
            color: '#888',
            letterSpacing: 2,
            marginBottom: 14,
          }}>CHAPTER III — INCOMES NOT INCLUDED</div>

          <div style={{
            fontFamily: 'Inter',
            fontSize: 32,
            color: '#F5F5F5',
            lineHeight: 1.4,
            position: 'relative',
          }}>
            <span style={{
              padding: '4px 14px',
              background: frame > 90 ? `${ACCENT}` : 'transparent',
              color: frame > 90 ? '#1C1C1C' : '#F5F5F5',
              fontWeight: 900,
              transform: `scale(${highlightScale})`,
              display: 'inline-block',
            }}>Section 10(1)</span>
            {' '}— In computing the total income of a previous year, the following income shall not be included:
          </div>

          <div style={{
            marginTop: 22,
            padding: '20px 24px',
            background: `${ACCENT}20`,
            borderLeft: `5px solid ${ACCENT}`,
            borderRadius: 4,
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 30,
              color: '#F5F5F5',
              fontWeight: 700,
              lineHeight: 1.3,
            }}>
              "<span style={{ color: ACCENT, fontWeight: 900 }}>agricultural income</span>"
            </div>
            {/* underline animation */}
            <div style={{
              marginTop: 6,
              height: 3,
              width: `${ulineW}%`,
              background: '#ff4444',
            }} />
          </div>
        </div>
      </div>

      {/* RIGHT — translation block */}
      <div style={{
        position: 'absolute',
        right: 90,
        top: 220,
        width: 700,
        opacity: transOp,
        transform: `scale(${transScale}) translateX(${interpolate(transScale, [0, 1], [50, 0])}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 24,
          fontWeight: 800,
          color: ACCENT,
          letterSpacing: 4,
          marginBottom: 14,
        }}>SIMPLY MEANS</div>

        <div style={{
          fontFamily: 'Yantra One, serif',
          fontSize: 78,
          color: TEXT,
          lineHeight: 1.05,
        }}>
          खेती से<br />आई इंकम पर<br /><span style={{ color: ACCENT }}>कोई टैक्स नहीं</span>
        </div>

        {/* Big stamp */}
        <div style={{
          marginTop: 40,
          display: 'inline-block',
          transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
        }}>
          <div style={{
            padding: '24px 50px',
            background: '#FFFFFF',
            border: '5px solid #c0392b',
            color: '#c0392b',
            fontFamily: 'Inter',
            fontSize: 64,
            fontWeight: 900,
            letterSpacing: 4,
            borderRadius: 8,
            boxShadow: '0 0 30px rgba(192,57,43,0.4)',
            position: 'relative',
          }}>
            TAX = 0
            <div style={{
              position: 'absolute',
              top: 8,
              left: 8,
              right: 8,
              bottom: 8,
              border: '2px solid #c0392b',
              pointerEvents: 'none',
            }} />
          </div>
        </div>

        {/* Subtle annotation */}
        <div style={{
          marginTop: 26,
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 26,
          color: TEXT,
          opacity: 0.75,
          fontWeight: 500,
        }}>
          सीधी सी बात — किसान को राहत
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene08_Section10;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_1961Context',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_1961Context: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#76ABAE';
  const TEXT = '#E8EDF2';
  const ACCENT = '#FAB95B';
  const DEEP = '#3D6B6E';

  // Year tag
  const yearOp = sp(5);
  const yearScale = sp(5, { damping: 9 });

  // Pie chart animate
  const pieFill = interpolate(frame, [50, 130], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // Counter
  const counterVal = Math.floor(interpolate(frame, [60, 140], [0, 75], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  }));

  // Story text
  const storyOp = sp(160);

  // Bottom message
  const bottomOp = sp(240);

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.04]);

  // Pie chart calculation — 75% slice
  const angle = pieFill * 270; // 270 degrees represents 75%
  const radius = 220;
  const cx = 280;
  const cy = 280;
  const startAngle = -90;
  const endAngle = startAngle + angle;
  const startRad = (startAngle * Math.PI) / 180;
  const endRad = (endAngle * Math.PI) / 180;
  const x1 = cx + radius * Math.cos(startRad);
  const y1 = cy + radius * Math.sin(startRad);
  const x2 = cx + radius * Math.cos(endRad);
  const y2 = cy + radius * Math.sin(endRad);
  const largeArc = angle > 180 ? 1 : 0;
  const piePath = `M ${cx} ${cy} L ${x1} ${y1} A ${radius} ${radius} 0 ${largeArc} 1 ${x2} ${y2} Z`;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at 30% 70%, #8FBFC2 0%, #76ABAE 60%, #5C8E91 100%)`,
      }} />

      {/* Vintage paper texture lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 40 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 30} x2="1920" y2={i * 30 + Math.sin(i) * 3} stroke={TEXT} strokeWidth="0.5" />
        ))}
      </svg>

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        top: 60,
        right: 80,
        width: 300,
        height: 300,
        border: `3px solid ${ACCENT}`,
        opacity: 0.07,
        borderRadius: 30,
        transform: `rotate(${15 + frame * 0.05}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: 100,
        left: 50,
        width: 280,
        height: 280,
        background: TEXT,
        opacity: 0.04,
        borderRadius: '50%',
      }} />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r1 = seededRandom(i + 41);
        const r2 = seededRandom(i + 130);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r1 * 1920 + frame * 0.3) % 1920,
            top: r2 * 1080,
            width: 3 + r2 * 4,
            height: 3 + r2 * 4,
            borderRadius: '50%',
            background: i % 4 === 0 ? ACCENT : TEXT,
            opacity: 0.15 + r1 * 0.2,
          }} />
        );
      })}

      <div style={{
        position: 'absolute',
        inset: 0,
        transform: `scale(${camScale})`,
      }}>
        {/* TOP — YEAR tag */}
        <div style={{
          position: 'absolute',
          top: 50,
          left: '50%',
          transform: `translateX(-50%) scale(${yearScale})`,
          opacity: yearOp,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 28,
            fontWeight: 800,
            color: ACCENT,
            letterSpacing: 8,
            marginBottom: 10,
          }}>FLASHBACK • CONTEXT</div>
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 30,
            justifyContent: 'center',
          }}>
            <div style={{ width: 80, height: 4, background: ACCENT }} />
            <div style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 130,
              color: TEXT,
              lineHeight: 1,
            }}>
              <span style={{ color: ACCENT }}>1961</span> इंडिया
            </div>
            <div style={{ width: 80, height: 4, background: ACCENT }} />
          </div>
        </div>

        {/* LEFT — Pie chart */}
        <div style={{
          position: 'absolute',
          left: 160,
          top: 280,
          opacity: sp(40),
          transform: `scale(${sp(40)})`,
        }}>
          <svg width="560" height="560" viewBox="0 0 560 560">
            <defs>
              <filter id="pieGlow">
                <feGaussianBlur stdDeviation="6" result="blur" />
                <feMerge>
                  <feMergeNode in="blur" />
                  <feMergeNode in="SourceGraphic" />
                </feMerge>
              </filter>
            </defs>
            {/* Background circle */}
            <circle cx={cx} cy={cy} r={radius} fill={`${TEXT}15`} stroke={TEXT} strokeWidth="3" />
            {/* Pie slice — 75% farmers */}
            <path d={piePath} fill={ACCENT} opacity="0.9" filter="url(#pieGlow)" />
            {/* Center circle for donut effect */}
            <circle cx={cx} cy={cy} r="120" fill="#3D6B6E" stroke={TEXT} strokeWidth="3" />
            {/* Center value */}
            <text x={cx} y={cy - 10} fontSize="80" fill={TEXT} textAnchor="middle" fontWeight="900" fontFamily="Yantra One, serif">
              {counterVal}%
            </text>
            <text x={cx} y={cy + 30} fontSize="20" fill={TEXT} textAnchor="middle" fontFamily="Inter" letterSpacing="3" opacity="0.8">
              FARMERS
            </text>
          </svg>

          {/* Legend */}
          <div style={{
            display: 'flex',
            gap: 30,
            marginTop: 30,
            justifyContent: 'center',
          }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
              <div style={{ width: 22, height: 22, background: ACCENT, borderRadius: 4 }} />
              <div style={{ fontFamily: 'Inter', fontSize: 22, color: TEXT, fontWeight: 600 }}>खेती पर निर्भर</div>
            </div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
              <div style={{ width: 22, height: 22, background: `${TEXT}30`, borderRadius: 4, border: `2px solid ${TEXT}` }} />
              <div style={{ fontFamily: 'Inter', fontSize: 22, color: TEXT, fontWeight: 600 }}>बाकी</div>
            </div>
          </div>
        </div>

        {/* RIGHT — story */}
        <div style={{
          position: 'absolute',
          right: 90,
          top: 280,
          width: 740,
          opacity: storyOp,
          transform: `translateX(${interpolate(storyOp, [0, 1], [40, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 22,
            color: ACCENT,
            letterSpacing: 4,
            fontWeight: 700,
            marginBottom: 12,
          }}>WHY THIS LAW WAS RIGHT — BACK THEN</div>

          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 72,
            color: TEXT,
            lineHeight: 1.05,
          }}>
            ये बात <span style={{ color: ACCENT }}>सही</span> भी थी
          </div>

          {/* Bullet stats */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 18, marginTop: 36 }}>
            {[
              { val: '70-80%', lbl: 'आबादी खेती पर निर्भर' },
              { val: 'GDP', lbl: 'का आधा खेती से' },
              { val: 'किसान', lbl: 'की हालत पतली थी' },
            ].map((s, i) => (
              <div key={i} style={{
                opacity: sp(170 + i * 12),
                transform: `translateX(${interpolate(sp(170 + i * 12), [0, 1], [30, 0])}px)`,
                display: 'flex',
                alignItems: 'center',
                gap: 22,
                padding: '14px 22px',
                background: `${DEEP}50`,
                borderLeft: `4px solid ${ACCENT}`,
                borderRadius: 6,
              }}>
                <div style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 38,
                  color: ACCENT,
                  fontWeight: 800,
                  minWidth: 180,
                }}>{s.val}</div>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 30,
                  color: TEXT,
                  fontWeight: 600,
                }}>{s.lbl}</div>
              </div>
            ))}
          </div>
        </div>

        {/* Bottom message */}
        <div style={{
          position: 'absolute',
          bottom: 50,
          left: '50%',
          transform: `translateX(-50%) scale(${bottomOp})`,
          opacity: bottomOp,
          padding: '18px 50px',
          background: 'rgba(28,28,28,0.6)',
          border: `2px solid ${ACCENT}`,
          borderRadius: 10,
        }}>
          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 38,
            color: TEXT,
            textAlign: 'center',
          }}>
            <span style={{ color: ACCENT }}>उस वक्त की</span> जरुरत थी — टैक्स से राहत
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene09_1961Context;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_2026Question',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_2026Question: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const BG = '#76ABAE';
  const TEXT = '#E8EDF2';
  const ACCENT = '#FAB95B';
  const DEEP = '#3D6B6E';

  // Phase 1: 1961 → 2026 transition (frames 0 - 80)
  const transitionProg = interpolate(frame, [10, 80], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });
  const year1961 = interpolate(transitionProg, [0, 0.5, 1], [1, 0.6, 0]);
  const year2026 = interpolate(transitionProg, [0, 0.5, 1], [0, 0.4, 1]);

  // Year scale punch
  const year2026Scale = interpolate(transitionProg, [0, 0.6, 1], [0.5, 1.15, 1]);

  // Question reveal
  const qOp = sp(95);
  const qScale = sp(95, { damping: 9 });

  // Two contrasting figures
  const fig1Op = sp(150);
  const fig2Op = sp(180);

  // VS divider
  const vsScale = sp(165, { damping: 8, stiffness: 110 });

  // Final big question
  const finalOp = sp(240);
  const finalPulse = 1 + Math.sin(frame * 0.1) * 0.025;

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.06]);
  const camX = Math.sin(frame * 0.01) * 6;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at 50% 50%, #8FBFC2 0%, #76ABAE 50%, #4A7A7D 100%)`,
      }} />

      {/* Grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="gridP10" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M 100 0 L 0 0 0 100" fill="none" stroke={TEXT} strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#gridP10)" transform={`translate(${(frame * 0.2) % 100} ${(frame * 0.15) % 100})`} />
      </svg>

      {/* Floating shapes */}
      <div style={{
        position: 'absolute',
        top: 60,
        right: 80,
        width: 350,
        height: 350,
        border: `3px solid ${ACCENT}`,
        opacity: 0.07,
        borderRadius: '50%',
        transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: 60,
        left: 60,
        width: 380,
        height: 380,
        background: ACCENT,
        opacity: 0.05,
        borderRadius: 40,
        transform: `rotate(${-frame * 0.08}deg)`,
      }} />

      {/* Particles */}
      {Array.from({ length: 26 }).map((_, i) => {
        const r1 = seededRandom(i + 45);
        const r2 = seededRandom(i + 145);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (r1 * 1920 + frame * 0.35) % 1920,
            top: r2 * 1080 + Math.sin((frame + i * 9) * 0.03) * 22,
            width: 3 + r2 * 5,
            height: 3 + r2 * 5,
            borderRadius: '50%',
            background: i % 3 === 0 ? ACCENT : TEXT,
            opacity: 0.18 + r1 * 0.2,
          }} />
        );
      })}

      <div style={{
        position: 'absolute',
        inset: 0,
        transform: `scale(${camScale}) translateX(${camX}px)`,
      }}>
        {/* TOP — Year transition */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: 'translateX(-50%)',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 26,
            fontWeight: 800,
            color: ACCENT,
            letterSpacing: 6,
            marginBottom: 18,
          }}>FAST FORWARD ▸▸</div>
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 60,
            justifyContent: 'center',
          }}>
            <div style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 120,
              color: TEXT,
              opacity: year1961,
              lineHeight: 1,
              textDecoration: transitionProg > 0.5 ? 'line-through' : 'none',
              textDecorationColor: '#ff4444',
              textDecorationThickness: '6px',
            }}>1961</div>

            <svg width="100" height="40" viewBox="0 0 100 40" style={{ opacity: transitionProg }}>
              <path d="M 5 20 L 85 20" stroke={ACCENT} strokeWidth="5" />
              <polygon points="75,10 92,20 75,30" fill={ACCENT} />
            </svg>

            <div style={{
              fontFamily: 'Yantra One, serif',
              fontSize: 150,
              color: ACCENT,
              opacity: year2026,
              transform: `scale(${year2026Scale})`,
              lineHeight: 1,
              textShadow: `0 0 40px ${ACCENT}80`,
            }}>2026</div>
          </div>
        </div>

        {/* MIDDLE — The question */}
        <div style={{
          position: 'absolute',
          top: 360,
          left: '50%',
          transform: `translateX(-50%) scale(${qScale})`,
          opacity: qOp,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 72,
            color: TEXT,
            lineHeight: 1.05,
          }}>
            अब इस छूट का फायदा<br />
            <span style={{ color: ACCENT }}>कौन उठा रहा है?</span>
          </div>
        </div>

        {/* Two figures comparison */}
        <div style={{
          position: 'absolute',
          top: 620,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          alignItems: 'center',
          gap: 80,
        }}>
          {/* Left — Real Farmer */}
          <div style={{
            opacity: fig1Op,
            transform: `scale(${fig1Op}) translateX(${interpolate(fig1Op, [0, 1], [-30, 0])}px)`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 14,
          }}>
            <div style={{
              padding: 30,
              background: `${DEEP}80`,
              border: `3px solid ${TEXT}`,
              borderRadius: 20,
              width: 280,
              textAlign: 'center',
            }}>
              <svg width="100" height="100" viewBox="0 0 100 100" style={{ display: 'block', margin: '0 auto' }}>
                {/* Farmer figure */}
                <circle cx="50" cy="30" r="12" fill="none" stroke={TEXT} strokeWidth="3" />
                <path d="M 38 25 L 62 25" stroke={TEXT} strokeWidth="3" />
                <ellipse cx="50" cy="20" rx="18" ry="6" fill="none" stroke={TEXT} strokeWidth="3" />
                <path d="M 32 25 L 68 25 L 65 22 L 35 22 Z" fill={TEXT} opacity="0.3" />
                <path d="M 50 42 L 50 75" stroke={TEXT} strokeWidth="3" />
                <path d="M 50 50 L 35 65 M 50 50 L 65 60" stroke={TEXT} strokeWidth="3" />
                <path d="M 50 75 L 40 92 M 50 75 L 60 92" stroke={TEXT} strokeWidth="3" />
                {/* Wheat stalk */}
                <path d="M 65 60 L 80 45 M 78 47 L 82 43 M 76 50 L 80 46 M 74 53 L 78 49" stroke={ACCENT} strokeWidth="2" fill="none" />
              </svg>
              <div style={{
                fontFamily: 'Yantra One, serif',
                fontSize: 36,
                color: TEXT,
                marginTop: 10,
              }}>असली किसान</div>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 22,
                color: '#9BC95B',
                marginTop: 6,
                fontWeight: 700,
              }}>Real Farmer</div>
            </div>
          </div>

          {/* VS */}
          <div style={{
            transform: `scale(${vsScale})`,
            opacity: vsScale,
          }}>
            <div style={{
              width: 110,
              height: 110,
              borderRadius: '50%',
              background: '#ff4444',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Yantra One, serif',
              fontSize: 54,
              color: '#FFFFFF',
              fontWeight: 900,
              boxShadow: '0 0 30px rgba(255,68,68,0.6)',
              transform: `rotate(${Math.sin(frame * 0.05) * 4}deg)`,
            }}>?</div>
          </div>

          {/* Right — Suit businessman */}
          <div style={{
            opacity: fig2Op,
            transform: `scale(${fig2Op}) translateX(${interpolate(fig2Op, [0, 1], [30, 0])}px)`,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 14,
          }}>
            <div style={{
              padding: 30,
              background: `${ACCENT}25`,
              border: `3px solid ${ACCENT}`,
              borderRadius: 20,
              width: 280,
              textAlign: 'center',
              boxShadow: `0 0 30px ${ACCENT}30`,
            }}>
              <svg width="100" height="100" viewBox="0 0 100 100" style={{ display: 'block', margin: '0 auto' }}>
                {/* Suited figure */}
                <circle cx="50" cy="28" r="13" fill="none" stroke={ACCENT} strokeWidth="3" />
                {/* Tie + suit */}
                <path d="M 30 50 L 50 42 L 70 50 L 70 90 L 30 90 Z" fill={`${ACCENT}20`} stroke={ACCENT} strokeWidth="3" />
                <path d="M 50 42 L 47 60 L 50 75 L 53 60 Z" fill={ACCENT} />
                <line x1="50" y1="42" x2="50" y2="42" />
                {/* Briefcase */}
                <rect x="65" y="65" width="20" height="14" fill={ACCENT} />
                <path d="M 70 65 L 70 62 L 80 62 L 80 65" stroke={ACCENT} strokeWidth="2" fill="none" />
                {/* Money sign on chest */}
                <text x="50" y="65" fontSize="12" textAnchor="middle" fontWeight="900" fill={ACCENT}>₹</text>
              </svg>
              <div style={{
                fontFamily: 'Yantra One, serif',
                fontSize: 36,
                color: TEXT,
                marginTop: 10,
              }}>कागज़ी किसान</div>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 22,
                color: ACCENT,
                marginTop: 6,
                fontWeight: 700,
              }}>Paper Farmer</div>
            </div>
          </div>
        </div>

        {/* Bottom — final tease */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          left: '50%',
          transform: `translateX(-50%) scale(${finalPulse})`,
          opacity: finalOp,
          padding: '14px 40px',
          background: ACCENT,
          borderRadius: 8,
          boxShadow: `0 0 30px ${ACCENT}80`,
        }}>
          <div style={{
            fontFamily: 'Yantra One, serif',
            fontSize: 40,
            color: '#1C1C1C',
            fontWeight: 900,
            letterSpacing: 1,
          }}>
            जवाब आगे — चौंका देगा
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene10_2026Question;
```

